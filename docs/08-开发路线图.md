# 08 · 开发路线图

## 里程碑总览

| 里程碑 | 目标 | 可演示的成果 |
|---|---|---|
| **M0** 骨架 | 工程可跑通 | `pnpm dev` 启动一个透明置顶、点击穿透的空白桌宠窗口 + 托盘 |
| **M1** 数据 | 行情进得来、存得下 | 添加自选股 → 拉到日线与实时价 → 面板列表显示涨跌幅 |
| **M2** 引擎 | 信号算得对 | 回测 CLI 可跑；单元测试覆盖全部指标；面板显示今日信号列表 |
| **M3** 提醒 | 提醒不烦人 | 桌宠表情 + 气泡 + 系统通知三级生效；免打扰与去重可验证 |
| **M4** 打磨 | 可长期使用 | 皮肤动画、影子运行绩效、设置全项、安装包 |

---

## M0 · 工程骨架

**产出**：可运行的空壳。

- [x] 设计文档体系（本目录）
- [ ] `package.json` / TS 配置 / electron-vite 配置 / ESLint + Prettier
- [ ] 目录结构与核心类型契约（`src/core/types.ts`、`src/shared/ipc-types.ts`）
- [ ] 主进程入口 + WindowManager + Pet 窗口（透明/置顶/skipTaskbar/focusable:false）
- [ ] 点击穿透机制（矩形命中区版本）→ **优先验证 [06](./06-桌宠交互与非干扰设计.md) 的 C1、C2**
- [ ] 托盘 + 右键菜单 + 退出
- [ ] preload 的 contextBridge 骨架 + 一条 ping/pong 通道
- [ ] Vitest 跑通，CI 脚本

**出口条件**：C1（不抢焦点）与 C2（点击穿透）在真机验证通过。这两条若做不到，整个产品形态需要重新设计——所以放在最前面验证。

---

## M1 · 数据层

- [ ] SQLite 初始化 + migration 机制 + 全部表结构（[03 §4.2](./03-数据源与存储设计.md)）
- [ ] 代码规范化与校验
- [ ] `QuoteProvider` 接口 + eastmoney 实现（日线 + 批量快照 + 基础信息）
- [ ] sina / tencent 备源实现
- [ ] `ProviderRegistry`：优先级、超时重试、降级、健康度记录
- [ ] 交易日历 + 时段状态机 + Scheduler 降频
- [ ] 自选股 CRUD + Panel 窗口基础列表 UI
- [ ] 复权双轨存储；数据质量校验（[07 §4](./07-回测与验证方案.md)）
- [ ] Provider 集成测试（录制响应回放）

**出口条件**：连续运行一个完整交易日，`provider_health` 成功率 > 99%，日线无缺口。

---

## M2 · 引擎层

- [ ] `src/core/indicators`：SMA/EMA/MACD/BOLL+BBW+分位/ADX+DMI/RSI/量能
- [ ] 指标交叉验证脚本 + 黄金用例固化（[07 §2.1](./07-回测与验证方案.md)）
- [ ] 周线聚合
- [ ] `RegimeClassifier` + 迟滞
- [ ] 三个策略的全部子信号
- [ ] `SignalCombiner`：动态权重 + 投票 + 冲突裁决
- [ ] 风控层规则表
- [ ] `engine_version` 机制与指标缓存失效
- [ ] **回测 CLI**（`src/backtest`）：未来函数防护、次日开盘成交、成本滑点、分 Regime 归因
- [ ] 参数标定：训练/验证/测试三段切分，产出出厂默认参数
- [ ] 面板「今日信号」列表 + 依据展开

**出口条件**：`src/core` 覆盖率 ≥ 90%；回测报告产出并据此确定默认参数；分 Regime 归因能回答「权重切换是否有效」。

> 若归因显示动态权重切换无效，**在此处停下来重新设计组合层**，不要带着无效的复杂度进入 M3。

---

## M3 · 提醒层

- [ ] `AlertDispatcher`：分级 → 防抖 → 冷却 → 频率上限 → 免打扰
- [ ] 免打扰探测（`SHQueryUserNotificationState` + 回退方案 + 锁屏）
- [ ] Bubble 窗口 + 淡入淡出 + 完全不可点击
- [ ] 系统通知 + 托盘角标 + 图标闪烁
- [ ] 桌宠状态机 + 首套表情动画
- [ ] 收盘确认轮：PROVISIONAL → CONFIRMED / INVALIDATED，含失效提示
- [ ] 提醒日志视图（含被抑制的信号与原因）
- [ ] 持仓录入 + 持仓风控强制通道
- [ ] 手动免打扰（双击 / 托盘 / 定时）

**出口条件**：自用一周，主观评价「没有被打扰过，也没有漏掉重要信号」；提醒日志中被抑制条目的原因分布合理。

---

## M4 · 打磨与发布

- [ ] 皮肤系统 + 完整动画集 + 极简模式
- [ ] 影子运行（模拟持仓绩效）
- [ ] 设置页全项（轮询频率、灵敏度三档、静默时段、数据源优先级、参数编辑、数据目录、清缓存）
- [ ] 多显示器与 DPI 缩放
- [ ] 开机自启、单实例锁
- [ ] 日志滚动、崩溃兜底、数据库备份与裁剪
- [ ] electron-builder 打包（NSIS + portable），代码签名（如有证书）
- [ ] 应用内免责声明与首次启动引导
- [ ] E2E 测试（Playwright）+ 手工验收清单

---

## 后续（P2，不承诺）

| 项 | 前置条件 |
|---|---|
| LLT 低延迟趋势线 | 找到可复现的公式并通过 M2 的验证流程 |
| 贝叶斯优化参数搜索 | 参数维度确实超出网格能力，且验证集流程稳定 |
| 分钟级 K 线与日内策略 | 数据源稳定性与请求配额允许 |
| 背离检测计入得分 | 回测证明其不引入过拟合 |
| 自适应止损（基于 ATR） | 固定比例止损的绩效数据积累足够 |
| 全市场扫描选股 | 数据获取与本地算力评估 |
| macOS / Linux 支持 | Windows 版稳定后；点击穿透与全屏检测需重做 |

---

## 关键决策点

开发过程中有三个地方需要**停下来重新判断**，而不是硬着头皮推进：

1. **M0 出口**：若点击穿透或不抢焦点做不到 → 桌宠形态存疑，考虑改为托盘 + 轻量悬浮条。
2. **M2 出口**：若分 Regime 归因显示动态权重无效 → 简化为单一策略 + 风控，砍掉组合层复杂度。
3. **M3 出口**：若自用一周发现提醒要么太吵要么太哑 → 问题多半在 [04 §4.2](./04-指标与信号引擎.md) 的阈值或 [05 §4](./05-风控与提醒规则.md) 的冷却策略，回到参数标定而非增加新指标。

增加指标几乎总是错误的应对方式。信号质量问题优先从数据质量、参数标定、提醒策略三处找原因。

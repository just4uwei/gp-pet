# 08 · 开发路线图

## 里程碑总览

| 里程碑 | 目标 | 可演示的成果 |
|---|---|---|
| **M0** 骨架 | 工程可跑通 | `pnpm dev` 启动一个透明置顶、点击穿透的悬浮窗口 + 托盘 |
| **M1** 数据 | 行情进得来、存得下 | 添加自选股 → 拉到日线与实时价 → 面板列表显示涨跌幅 |
| **M2** 引擎 | 信号算得对 | 回测 CLI 可跑；单元测试覆盖全部指标；面板显示今日信号列表 |
| **M3** 提醒 | 提醒不烦人 | 状态点 + 气泡两个渠道生效；免打扰与去重可验证 |
| **M4** 打磨 | 可长期使用 | 影子运行绩效、设置全项、安装包、开机自启与单实例 |

---

## M0 · 工程骨架

**产出**：可运行的空壳。

- [x] 设计文档体系（本目录）
- [x] `package.json` / TS 配置 / electron-vite 配置 / ESLint + Prettier
- [x] 目录结构与核心类型契约（`src/core/types.ts`、`src/shared/ipc-types.ts`）
- [x] 主进程入口 + WindowManager + Overlay 窗口（透明/置顶/skipTaskbar/focusable:false）
- [x] 点击穿透机制（矩形命中区版本）→ **优先验证 [06](./06-桌宠交互与非干扰设计.md) 的 C1、C2**
- [x] 托盘 + 右键菜单 + 退出
- [x] preload 的 contextBridge 骨架 + 一条 ping/pong 通道
- [x] Vitest 跑通，CI 脚本
- [x] ~~皮肤加载与校验 + 缺资源时的降级路径~~ —— **2026-08-13 随皮肤系统一并删除**。
      产品不再依赖任何美术素材；仍需图片的只剩托盘图标，缺失时退到内置兜底图标

**出口条件**：C1（不抢焦点）与 C2（点击穿透）在真机验证通过。这两条若做不到，整个产品形态需要重新设计——所以放在最前面验证。

> **状态：代码已就绪，等待真机验收。** 逐条操作见 [checklists/M0-零干扰验收](./checklists/M0-零干扰验收.md)。
> **2026-08-13：桌宠形态已删除**，验收对象是 `src/renderer/bar/` 那条 300×38 的条子。
> 注意 C5 的口径也变了 —— 提醒已经没有任何声音渠道，那一条从「默认无声」变成「永不发声」。
> C1/C2 无法自动化断言（「打字没被打断」「点击落到了下层应用」都要人来判），
> 所以这一格必须由人勾，不能由 CI 代劳。

---

## M1 · 数据层

- [x] SQLite 初始化 + migration 机制 + 全部表结构（[03 §4.2](./03-数据源与存储设计.md)）
- [x] 代码规范化与校验
- [x] `QuoteProvider` 接口 + eastmoney 实现（日线 + 批量快照 + 基础信息）
- [x] sina / tencent 备源实现
- [x] `ProviderRegistry`：优先级、超时重试、降级、健康度记录
- [x] 交易日历 + 时段状态机 + Scheduler 降频
- [x] 自选股 CRUD + Panel 窗口基础列表 UI
- [x] 复权双轨存储；数据质量校验（[07 §4](./07-回测与验证方案.md)）
- [x] Provider 集成测试（录制响应回放）

**出口条件**：连续运行一个完整交易日，`provider_health` 成功率 > 99%，日线无缺口。

> **状态：代码已就绪，出口条件未验。** 出口条件按定义要跑满一个交易日，无法由 CI 代劳。
> 2026-08-12 实测修正了 M1 的一个诊断：**eastmoney 不是被出口网络封锁，是间歇性失败**
>（undici 下成功率约 78%，`provider_health` 记着 83 成功 / 24 失败；curl 则 100% 失败 ——
> 客户端指纹差异，不能用 curl 判断这个接口的可用性）。它的 fixture 仍是手写的 ——
> `pnpm fixtures:record -- --provider eastmoney` 现在能跑，但该脚本没有重试，
> 4 个请求里通常只成功 2 个，得先给它加重试才能录全。逐条操作见
> [checklists/M1-数据层验收](./checklists/M1-数据层验收.md)，偏差见
> [M1-偏差报告](./notes/M1-偏差报告.md)。

---

## M2 · 引擎层

- [x] `src/core/indicators`：SMA/EMA/MACD/BOLL+BBW+分位/ADX+DMI/RSI/量能
- [x] 指标交叉验证脚本 + 黄金用例固化（[07 §2.1](./07-回测与验证方案.md)）
- [x] 周线聚合
- [x] `RegimeClassifier` + 迟滞
- [x] 三个策略的全部子信号
- [x] `SignalCombiner`：等权汇总 + 投票 + 冲突裁决（动态权重已于 2026-08-12 实测无效后删除）
- [x] 风控层规则表
- [x] `engine_version` 机制与指标缓存失效
- [x] **回测 CLI**（`src/backtest`）：未来函数防护、次日开盘成交、成本滑点、分 Regime 归因
- [x] 参数标定的**流程与判据**：三段切分 + 逐折配对比较 + 三态裁决（2026-08-13，见下）
- [ ] 参数标定的**逐参数归档**：已写回 1 项 · 已测保持出厂值 13 项（2 项**有正面证据**、
      1 项列为**最优先复核**）· 已判惰性或算术无效 9 项 · 4 项**回测原理上测不到**（归影子运行 / M3）·
      **一个网格都没跑过的还剩 `macd` / `boll` / `volume` / `rsi`**
- [x] 面板「今日信号」列表 + 依据展开

**出口条件**：`src/core` 覆盖率 ≥ 90%；回测报告产出并据此确定默认参数；分 Regime 归因能回答「权重切换是否有效」。

> **第二条出口条件的读法（2026-08-13 明确）**：「据此确定默认参数」**不等于**「每个参数都换一个新值」。
> 一个参数标定完有三种合法结局，三种都算这个参数**已确定**：
> ① 写回新值（清单 4.9a）② 已测、保持出厂值（裁决 `KEEP`）③ 已判惰性 / 不可标定（清单 4.9c）。
> 另有第四类是**回测原理上测不到**的（`provisionalDiscount`、`alert.bubbleScore`、
> `lateBuyCutoffMinutes`、`staleSnapshotMs` —— 回测里根本走不到那条分支），
> 它们的依据只能来自影子运行或 M3 的提醒日志，**不属于这条出口条件**。
> 原先没有 ②③④ 的归档位置，于是「测过了、结论是保持原值」只能记成「没有候选够格」，
> 读起来像还没做完 —— 这一格因此挂了十一轮，期间同一个网格被跑过两遍。
> 依据见 [M2-偏差报告 §5.15](./notes/M2-偏差报告.md)。

> 若归因显示动态权重切换无效，**在此处停下来重新设计组合层**，不要带着无效的复杂度进入 M3。

> 第三条出口条件**已达成**（2026-08-12）：答案是「无效」，权重表已删除。见下面的四续。

> **状态：覆盖率出口已达成；标定已开始落地，但「据此确定默认参数」仍未达成 —— 只完成了一项。**
> （2026-08-12 六续：`strategy.squeezeBbwPct` 是第一个够格且**已经人工复核写回**的出厂参数
> ——10 被验证集红线淘汰，16–30 是通过的高原，取 20。剩下二十来个数值仍是转述猜测，
> 尤其 `combine` 的四个数、`downtrendBuyPenalty`、`adx`/`regime` 阈值，
> 所以 `ENGINE_VERSION` 仍带 `-unvalidated`，这条出口条件不算达成。）
>
> 2026-08-12：数据障碍已排除（`pnpm fetch:history` 从腾讯拉 2018 年至今的真实日线），
> 标定在 39 只宽基分层样本上跑完了三段切分。结论是**没有候选参数够格写回 `params.ts`**：
> 出厂默认的 `0.60 / 3 票` 被红线淘汰（验证集年化为负），而优胜者的验证集 Calmar 只有 0.05、
> 样本外还亏 0.28%。**把它写进 params.ts 等于用噪音替换猜测。**
>
> 更值得注意的是分 Regime 归因给出的答案：引擎 **67% 的时间停在 TRANSITION**，
> 动态权重（92 笔 / -1.16% / 夏普 -0.38）**没有跑赢**固定 0.5/0.5 对照组
>（11 笔 / -0.32% / 夏普 -0.46）。按下面的关键决策点 2，这里应当停下来重新审视组合层，
> 而不是继续调阈值 —— 阈值已经调过了，那不是问题所在。
>
> 详细数字、标的池选取规则与消不掉的幸存者偏差，见 [M2-偏差报告 §5.4](./notes/M2-偏差报告.md)。
>
> **2026-08-12 续：67% 那条已追到根因，且触发了关键决策点 2。**
> 新增 `pnpm audit:regime` 做成因分解，结论是 regime 层不是问题所在
>（预热占 0%、迟滞占比几乎为零；把三条突变条件整条删掉，TRANSITION 也只从 66.6% 降到 53.7%）。
> 根因在组合层：得分是「已成立子信号的加权平均 × 策略权重」，**单策略得分的上限恰好等于该策略的权重**，
> 而 `TRANSITION` 的权重是 0.50 < 出厂阈值 0.60 —— 三分之二的时间里单策略信号在算术上无法触发，
> 保守档 0.72 更是高于所有状态的上限。把阈值降到上限以下后交易量足够，对照组随即给出明确答案：
> **三个参数点上固定 0.5/0.5 都不输于动态权重，各 Regime 的胜率与平均盈亏也没有可辨别差异。**
>
> 因此下一步不是调参，而是先解掉「得分上限 × 阈值」的耦合口径（要动 docs/04 §4.1/§4.2），
> 解耦后用同一套三点对照重测；**若仍然无效，按关键决策点 2 砍掉组合层复杂度**。
> 全部数字与探针见 [M2-偏差报告 §5.5](./notes/M2-偏差报告.md)。
>
> **2026-08-12 再续：耦合已解掉，重测后决策点 2 需要人来拍板。**
> 组合层得分改为除以「该状态行内最大权重」（`ENGINE_VERSION` → `0.2.1-unvalidated`，
> 一个参数值都没动）。出厂 `0.60/3` 从 92 笔 / -1.16% / 夏普 -0.38 变成
> **414 笔 / +1.73% / +0.22**，`TRANSITION` 从 0 笔变成 198 笔（胜率 37.4%，四态最高）——
> 引擎在三分之二的时间里终于会出信号了。但**动态 vs 固定 0.5/0.5 的差值在四个参数点上
> 正负交替、都在 1pp 以内**，仍然看不出动态权重有效；这次是在排除了口径 bug 之后看不出的，
> 上一轮那句「是耦合压着它」不能再用。
>
> 按关键决策点 2 的规定动作，此处应当**砍掉组合层复杂度、退回单一策略 + 风控**。
> 这改的是引擎形态，报告只把判据摆齐，等人拍板；在拍板前不重跑阈值标定
>（靶子会随组合层去留一起动）。绝对绩效仍不构成任何结论：+1.73% 是 8.6 年累计，
> 同期基准 +14.10%。数字与两条不变量测试见 [M2-偏差报告 §5.6](./notes/M2-偏差报告.md)。
>
> **2026-08-12 三续：策略消融跑完了，判据从「动态权重无效」变成「均值回归净负贡献」。**
> 新增消融开关 `params.enabledStrategies`（测量工具，出厂两项皆 true，`ENGINE_VERSION` →
> `0.2.2-unvalidated`，默认行为逐笔不变）。出厂 `0.60/3` 下：**只留趋势 435 笔 / +2.67% / 夏普 0.32**，
> 两个策略都留 414 笔 / +1.73% / 0.22，**只留均值回归 0 笔**。也就是说均值回归不但一次都没独立出手，
> 还通过冲突裁决压掉了趋势侧 21 笔。但这不等于「均值回归没用」——
> R1–R4 从未标定、4 子信号与 5 子信号共用一条票数线，两个混淆项都还在。
> 三条可选路径（砍到底 / 只砍动态权重 / 先修不对等再判）与各自代价见
> [M2-偏差报告 §5.7](./notes/M2-偏差报告.md)。**仍然等人拍板。**
>
> **2026-08-12 四续：拍板结果 —— 动态权重删除，均值回归暂留。**
> 采纳的是三条路径里的 B。判据不是绩效差值（+0.94pp 与被判为噪音的 1.09pp 同量级，
> 用它下结论就是双重标准），而是**机制是否惰性**：权重表在任何阈值下都测不出效果，
> 是它自己没用；而均值回归的 0 笔有明确的、尚未排除的成因（4 子信号与 5 子信号共用一条
> `voteThreshold = 3`），先修不对等再判。`params.weights`、回测的固定权重对照组、
> 两条权重不变量测试一并删除（`ENGINE_VERSION` → `0.2.3-unvalidated`）。
> 删完重跑出厂 `0.60/3`：**419 笔 / +1.74% / 夏普 0.22 / 最大回撤 2.53%**，
> 与删除前的 414 笔 / +1.73% / 0.22 基本同量级，但下面换了一批交易：
> **RANGE 从 0 笔变成 24 笔**（0.3/0.7 曾把趋势单策略的得分钉在 3/7 ≈ 0.43，够不着 0.60），
> **TREND_UP 少了 19 笔**（反过来，均值回归在趋势市里不再被压到 3/7，冲突裁决拦掉的更多了）。
> 数字见 [M2-偏差报告 §5.8](./notes/M2-偏差报告.md)。**下一步是给两个策略各自的票数线，重跑消融。**
>
> **2026-08-12 五续：票数线的不对等修掉了，均值回归被救回来，但贡献仍测不出。**
> `combine.voteThreshold` 改成按策略给（趋势 3 / 均值回归 2，两个数由同一比例换算），
> 合并规则从「两边票数相加」改成「任一策略达到自己的线」（`ENGINE_VERSION` → `0.2.4-unvalidated`）。
> 新出厂档 `0.60 / 3 / 2` 下：**趋势 + 均值回归 662 笔 / +2.98% / 夏普 0.31，
> 只留趋势 459 笔 / +2.84% / 0.33，只留均值回归 292 笔 / +0.15% / 0.03**。
> 均值回归从「0 笔」变成「292 笔独立出手、其中 290 笔在 RANGE」——
> §5.7 那个「一次都没出手」确实是票数线造成的，**不是策略结构问题**，混淆项 (b) 排除。
> 但把它加到趋势上只值 +0.14pp 收益 / −0.02 夏普 / +0.42pp 回撤，仍在同一条噪音带里，
> 且冲突裁决压掉了 89 笔（459 + 292 = 751 → 662）。
> **决策点 2 的最后一问仍待人拍板**：砍掉均值回归（贡献测不出且要付裁决代价），
> 还是先标定 R1–R4 再判（它现在第一次有 292 笔可优化的交易，混淆项 (a) 变得可测）。
> 组合层其余部分已收口，**阈值标定的靶子不再会动**，两条路都可以立刻开跑。
> 数字见 [M2-偏差报告 §5.9](./notes/M2-偏差报告.md)。
>
> **2026-08-12 六续：标定 R1–R4 之后，均值回归留下 —— 问题一直出在 R3 的压缩线。**
> 走的是「先标定再判」那条。两个网格（`grid-mean-reversion.json` 27 组 +
> `grid-mean-reversion-squeeze.json` 10 组）× 两种基线（只开均值回归 / 完整引擎），
> 三段切分照 07 §3 走。结论：**`strategy.squeezeBbwPct = 10` 在两种基线下都被验证集红线淘汰**，
> 而 16–30 是一整片通过的高原（不是孤立峰值，±20% 邻域不断崖）。
> 换成高原上任意一个值，均值回归对组合的边际贡献从 **+0.14pp（噪音带内）变成 +2.0 ~ +3.4pp**，
> 夏普 +0.11 ~ +0.24，回撤不变 —— 同一把尺子下这次是能测出来的，`sq20` 处
> **两个都留 762 笔 / +6.25% / 夏普 0.57**（只留趋势 459 笔 / +2.84% / 0.33）。
> 那个 10 来自来源文档对带宽分位的定性描述「< 10 变盘前夜」，是 ADR-0003 说的典型转述值。
> 附带查出两条**结构**问题（标定解决不了）：`R2` 对唯一参数 `revertLookback` 完全惰性
>（取 2/3/5 结果逐位相同），`R1` 的 RSI 带宽在收紧方向上近乎惰性。
> **已经过人工复核写回 `params.ts`：`squeezeBbwPct = 20`**，`ENGINE_VERSION` 递增到
> `0.2.5-unvalidated`。取 20 而不是工具按验证集 Calmar 排的首选 30，理由是 30 在
> 「只留均值回归」的口径下明显更差（0.16 vs 0.36），靠的是与趋势策略的交互、证据更薄；
> 20 是高原上唯一在训练集与验证集上都靠前的值。**`-unvalidated` 后缀留着** ——
> 它跟的是整套参数的状态，一个参数标定完不足以摘（M2 清单 4.9a/4.9b 已按此拆开）。
> 依据与失效条件记在 [CHANGELOG](../CHANGELOG.md)。
> 全部数字、测试集纪律的削弱之处、剩下的混淆项 (c) 见 [M2-偏差报告 §5.10](./notes/M2-偏差报告.md)。
>
> **2026-08-12 七续：R2 的惰性是口径错，改完了；但真正的约束在票数线上，不在子信号层。**
> 新增诊断工具 `pnpm audit:subsignals`，把 R2 的三项合取拆开逐项计数（39 只 × 61534 根判定日）。
> 根因是两项错得方向相反：「当日 `close > MID`」在 **49% 的判定日上成立**（合取里等于没有条件，
> 于是 `revertLookback` 没有东西可约束），而「`HIST` 转正」= `DIF` 上穿 `DEA` 是慢事件
> （破轨到过零的中位数 **35 根以上**，回溯窗口只有 2–5 根，时间常数差一个量级）。
> 改成「**曾在轨道外 + 今日穿越中轨（事件）+ HIST 朝有利方向（软确认）**」——
> 触发数 90（0.15%）→ **213（0.35%）**，`revertLookback` 变成活参数且单调。
> **绩效没有变好**：全期 769 笔 / +5.93% / 夏普 0.543，对比旧口径 762 笔 / +6.25% / 0.573，
> 在 ±1pp 噪音带内；`revertLookback` ∈ {3,5,8,12,20} 的验证集 Calmar **逐位相同**（全 0.084）。
> **拍板：改写留下**，理由是口径正确（穿越是事件，与 04 §2 的穿越纪律一致）+ 参数从死变活，
> 不是它更赚钱（`ENGINE_VERSION` → `0.2.6-unvalidated`，口径变更、无参数标定）。
> 更重要的是共现测量给出的结构结论：213 次触发里 **185 次（87%）是孤立的** ——
> R2 要求价格回到中轨附近，而 R1/R3 要求触外轨、R4 要求偏离 ≥1.5σ，**几何互斥**，
> 而 `voteThreshold.meanReversion = 2` 要同策略两票，**R2 在结构上无法自己促成一次触发**。
> 所以 `voteThreshold.meanReversion` 进入 `combine` 块标定的必测维度，不另开一轮
> （避免再触碰测试窗口）。`R1` 的惰性不改：它两个条件各自都是真约束，惰性来自交集小，
> 属于「这条规则本来就罕见」，不是口径错误。数字见 [M2-偏差报告 §5.11](./notes/M2-偏差报告.md)。
>
> **2026-08-12 八续：混淆项 (c) 收口 —— 冲突裁决「压掉双方」是对的，三个混淆项全部解决。**
> 关键是发现这个机制之争**不需要改代码就能测**：`adjudicate()` 判矛盾的条件是
> `gap < conflictBand`，把 `conflictBand` 设成 0 就恒假，行为恰好等于「弱侧让路」。
> 全期 39 只：band 0（弱侧让路）897 笔 / +5.67% / 夏普 0.473 / 回撤 2.50%，
> 出厂 0.15 是 769 笔 / +5.93% / 0.543 / 2.44%。**多出来的 128 笔在每个维度上都是负担**
>（还多花 6900 手续费）。band 0.50 与 0.80 逐位相同 —— 机制在 218 笔可争议交易处饱和，
> 说明夏普的单调上升不是「少交易所以看起来稳」的无界代理。
> **但这证的是机制不是取值**：全区间跑的、没走三段切分，`conflictBand` 的出厂值仍待标定。
> 顺带修正 §5.10 的一个错数：裁决的作用量是 128 笔，不是 188 笔 ——
> 那个数是消融差值，把「裁决压掉的 / 得分相加新触发的 / 单持仓挤掉的」算成了一件事。
> 见 [M2-偏差报告 §5.12](./notes/M2-偏差报告.md)。
>
> **2026-08-12 九续：「超额收益为负」这条一直挂着的坏消息，很大一部分是口径问题。**
> 基准（沪深300 买入持有）是 **100% 满仓**的，而信号策略的**平均资金占用只有 4.15%** ——
> 两个收益率根本不在同一个分母上。同期最大回撤 2.44% vs 基准 45.60%，
> Calmar 0.275 vs 0.034，**风险调整后方向是反的**。所以 −8.17pp 的超额不足以给策略定罪。
> 反过来也不许吹：「按占用资金折算年化 10.8%」不能当绩效说，理由三条列在 §5.13。
> 这一轮把 `averageExposure()` 做成了常驻指标（`performance.exposure`，
> 文本报告里紧挨着超额那一行打印），因为这个数缺席时超额一定会被读反。
> 附带解释了一件一直没解释的事：**之前每次消融差值都落在 ±1pp 的「噪音带」里，
> 是因为那是总资金口径 —— 4% 的占用把真实差异压扁了约 24 倍。**
> 仍未回答的是标的池只有 39 只、有幸存者偏差、单一 8.5 年窗口，
> 以及没有构造占用率对齐的基准。见 [M2-偏差报告 §5.13](./notes/M2-偏差报告.md)。
>
> **2026-08-12 十续：`combine` 块标定 —— 四个参数一个也定不下来，但工具修好了两处。**
> OFAT 16 组：`downtrendBuyPenalty` **惰性**（0.25/0.5/0.75 逐位相同，只有 1.0 动了训练集）；
> `scoreThreshold` 与 `conflictBand` **训练集与验证集的梯度方向相反**（§5.12 那张全区间表的
> 「越宽越好」是训练窗口的假象，当时写的「证的是机制不是取值」是必要的谨慎）；
> `voteThreshold.trend = 4` 名义第一但训练集几乎垫底、验证集只有 59 笔，是孤峰不是高原。
> §5.11 挂下来的那问也有了答案：`meanReversion = 1` **确实**让 R2 参与（489 → 1047 笔），
> 但多出来的交易把 6 年训练集打成 −3.73%，**不采用**，票数线维持 {3, 2}。
> **整块保持出厂值，`ENGINE_VERSION` 不动。**
> 同轮查出并修掉标定工具两处测量缺陷：分段净值曲线混进了预热前缀
>（验证集年化被压小 5.1 倍、基准收益连符号都反），以及没有一条红线拦得住
> 「训练集亏钱、验证集赚钱」。**段内排名不受影响，但跨段的衰减红线此前是坏的（3/16 假阳性）。**
> 这一轮之所以定不下来，根因大概率还是 §5.13 那条：测量对象只有总资金的 4%。
> 见 [M2-偏差报告 §5.14](./notes/M2-偏差报告.md)。
>
> **2026-08-13 十一续：这一格挂了十一轮的原因查清了 —— 判据不在工具里，且效应量比噪音小一个量级。**
> 这一轮**没有跑新网格**，而是问「这条流程为什么产不出结论」。两层原因：
>
> **① 工具侧：真正在做决策的判据从来不在工具里。** 到 §5.14 为止工具的输出是
> 「按验证集 Calmar 排序的候选表」，推荐 = 第一行。十一轮里它报出的第一名被人否掉了
> 绝大多数次（孤峰 / 落在网格边界 / 参数惰性 / 训练集垫底），**唯一写回的那次
>（`squeezeBbwPct`）用的判据是「16–30 是一整片高原」，工具排的第一名是 30，被否了**。
> 判据留在人脑子里，这一格的推进速度就等于「有人愿意手工看表的次数」。
> 已把那四种理由全部写进工具：出厂值永远入表并**逐折配对**比较、折单元 = 标的子集 × 时间片
>（默认 4 × 3 = 12，**从同一次模拟里切出来，加折不加耗时**）、孤峰与边界自动标记、
> **三态裁决 `WRITE_BACK` / `KEEP` / `INCONCLUSIVE`**、测试段改成 `--touch-test` 默认不读。
>
> **② 数据侧：`combine` 这一层的效应量比测量噪音小一个量级。** 用新判据重跑同一个
> OFAT 网格（16 组、39 只、12 折）：**裁决 `KEEP`，而且这次没有一处需要人来判**。
> 出厂值自己 12 折的总收益标准差是 **2.25pp**，而全部 15 个候选的动态范围是
> **−0.44pp ~ +0.11pp**；最大的改进 +0.11pp（t = 0.3）需要约 **430 折**才分辨得出，
> 是现在的 36 倍 ≈ 同窗口下 1400 只标的。**`downtrendBuyPenalty` 的三个取值与
> `conflictBand = 0.10` 一折都没动**（逐折逐位相同）—— 那不是效应小，是没有效应。
> 顺带修了新工具自己的两处测量缺陷（折上不能用 Calmar，分母塌；胜率要按受影响的折算）。
>
> **处置：`combine` 块归档为「已测、保持出厂值」，`ENGINE_VERSION` 不动，不扩标的池。**
> 下一步的性价比明显在**一阶参数**上 —— `adx` / `regime` 的门与 `risk` 的止损线
> 一个网格都还没跑过，而它们决定「有没有信号」，不是「已有的信号怎么合」。
> 全部数字见 [M2-偏差报告 §5.15](./notes/M2-偏差报告.md)。
>
> **2026-08-13 十二续：`adx` / `regime` 网格（21 组，一直挂着没跑）跑完了 —— 仍是 KEEP，
> 但第一次拿到了「支持出厂值」的正面证据。**
> 这一轮重要的不是结论，而是**它证明了新判据不是一台只会说「测不出」的机器**：
> 4 个候选被验证集红线直接淘汰，另有 3 个 `|Δ|/标准误 ≥ 1.8`，最差的
> `adx.baseThreshold = 16` 是 **−1.06pp / t 4.1 / 0-11 折为正**。
> 出厂值 20 的两个邻居（16 与 24）**都**显著更差 —— 这不是「怎么动都一样」，
> 是「往测过的方向动都有代价」。工具因此新增一行输出把两种 KEEP 分开印，
> 别把这一轮读成 §5.15 那一轮（那边 15 个候选最大 t = 1.4）。
>
> 附带两条：① **`adx.maxThreshold` 在出厂配置下是算术上的死参数** ——
> `clamp` 的被夹项 ∈ [base, base + volScale] = [20, 28]，而 `maxThreshold` 正好是 28，
> 上界永远咬不住（`28 → 34` 逐折逐位相同）。它只在 `base + volScale > maxThreshold` 时才活，
> **别再扫它的上侧**。② 这张网格每个维度只给了中心 ± 一档，于是唯一的正向候选
>（`hysteresisDays = 3`，+0.05pp）落在网格边界上、按敏感性红线不能采用 ——
> 这次是机器在跑完时当场指出来的，而不是像 §5.10 那样扩网格重跑之后才发现代价。
> **`regime` 的两个数因此是当前唯一值得再跑一轮网格的方向**（加宽取值范围）。
> 全部数字见 [M2-偏差报告 §5.16](./notes/M2-偏差报告.md)。
>
> **2026-08-13 十三续：加宽后的 `adx` / `regime` 网格也跑完了 —— 仍是 KEEP，
> 但出厂值在 ±2 的邻域里赢了每一个测过的方向。**
> `adx` 的 8 个候选**没有一个** Δ 为正：`baseThreshold` 18 与 16 让六年训练集亏钱（红线淘汰），
> 22 是 −0.17pp、24 是 −0.48pp / t 2.2；`volScale` 的 6 与 10 各 −0.16pp。
> 报告里顺手记了一条统计学差别：同样「自己最高、两侧都塌」的形状，
> **出现在候选身上要判孤峰，出现在出厂值身上却是正面证据** ——
> 孤峰红线防的是「在 N 个候选里取 argmax」带来的赢家诅咒，而出厂值是**预先登记**的
>（转述在前、测量在后），不受这个偏差影响。所以 `adx.baseThreshold = 20` / `volScale = 8`
> 可以归档为「已测 + 有正面证据」，但**仍然不能写进「已标定」清单** —— 只知道它好于
> 16/18/22/24，不知道它最优。
> `regime` 那边给出一条能用的定性结论：**`hysteresisDays` 不能低于 2（1 是 −0.34pp），
> 而 2–5 是一片分辨不出的高原** —— 也就是「把迟滞加长不花钱」。
> 这对 M3 的去重与冷却有用，但判据应该是提醒日志的每日改口次数，不是回测 Calmar，
> 所以这一轮不动出厂值。`rangeMidBand` 四个取值全负但非单调、t ≤ 1.6，读作噪音。
> **这两张网格不要再跑第三轮**（M2 §5.17）。
>
> **2026-08-13 十四续：标定 `risk` 与 `multiTf` —— 先修了「胜率」的口径，
> 然后找到全项目至今最一致的候选（仍不写回）。**
> 起因是产品侧要求「提高预测胜率」。第一步去量胜率就撞上一个口径错误：
> 报告里的「胜率」按**行**算，而回撤减仓会把一次建仓拆成两三行 ——
> **逐笔 33.16% vs 建仓级 49.28%**，差 16 个百分点；同一个成因还让分状态归因的「平均」
> 四行全打负（−1.2% ~ −1.6%）而净盈亏是 **+231,154 元**。已修（按仓位加权 + 建仓级统计）。
> 修完露出一条新事实：**RANGE + TRANSITION 拿走了全部利润，TREND_UP 187 次建仓只赚 7.7k、
> 胜率低 12 个百分点** —— 旧口径下四行都是负的，看着一样差。
>
> 还有一条决定了后续方向：**96.6% 的离场由风控规则触发，策略卖出子信号只占 3.4%**。
> 所以「卖点」不是策略在判，「提高胜率」的杠杆在 `risk` 那五个转述阈值上。
>
> 两张网格（`risk` 16 + 11 组、`multiTf` 17 组）都判 **KEEP**，但形状完全不同：
> `multiTf` 大半取值**动 0/12 折**（改了等于没改），按 4.9c 归档；
> `risk` 的 `profitProtectTrigger` 验证集 Calmar 翻倍（0.427 → 1.07）**但是陷阱** ——
> 六个取值的训练集 Calmar 与胜率全部低于出厂、且非单调，**新加的「建仓级胜率不得下降」
> 门槛自动挡下了它**（这条门槛就是为此加的）。真正值得记的是
> **`drawdownReducePct` 7% → 10%：训练 Calmar ↑、训练胜率 +6.7pp、验证 Calmar ↑、
> 验证胜率 +4.2pp，四个口径同向，下侧 5% 被红线淘汰，且 ≥12% 后饱和**（0.12/0.16/0.20 逐位相同）
> —— 即真正的问题是「7% 是不是太紧」这个二值问题。**t = 0.77 仍不达标，不写回**，
> 但列为**扩池后第一个要复核的项**（需要约 264 只标的，比 `combine` 估的 1400 只便宜得多）。
> 全部数字见 [M2-偏差报告 §5.18](./notes/M2-偏差报告.md)。
>
> **仍然不用合成数据代替**：在自己编的序列上「标定」出来的参数只反映编数据时的假设，
> 比不标定更危险 —— 它会带着一份看起来很正规的报告。
> 因此 M2 交付的是**标定的能力**（`pnpm backtest --grid`，含 [07 §3](./07-回测与验证方案.md) 的三条过拟合红线），
> 不是标定的结论；`params.ts` 里的数值仍是 ADR-0003 说的初始猜测，`ENGINE_VERSION` 保留 `-unvalidated`。
> 逐条操作见 [checklists/M2-引擎层验收](./checklists/M2-引擎层验收.md)，偏差见 [M2-偏差报告](./notes/M2-偏差报告.md)。

---

## M3 · 提醒层

- [x] `AlertDispatcher`：分级 → 防抖 → 冷却 → 频率上限 → 免打扰
      —— 四道闸门与 21 条单测（`src/main/alerts/dispatcher.ts`），
      **已接线**：`controller.onSignals()` → `AlertService` → alert_log + 四个渠道
- [x] 免打扰探测（`SHQueryUserNotificationState` + 缓存/退避 + 锁屏 + 静默时段）
- [x] Bubble 窗口 + 淡入淡出 + 完全不可点击
- [x] ~~系统通知 + 托盘角标 + 图标闪烁~~ —— **2026-08-13 移除**，提醒的可见出口只剩气泡
- [x] 状态机（`src/main/alerts/pet-state.ts`，含 3s 最短驻留与回落重推）
      —— 表现落在悬浮条左端那颗状态点上，不再有表情动画这一档
- [x] 收盘确认轮：PROVISIONAL → CONFIRMED / INVALIDATED，含失效提示
- [x] 提醒日志视图（含被抑制的信号与原因）
- [x] 持仓录入 + 持仓风控强制通道
- [x] 手动免打扰（右键菜单 / 托盘 / 定时。悬浮条的双击是开面板，见 docs/06 §4）

**出口条件**：自用一周，主观评价「没有被打扰过，也没有漏掉重要信号」；提醒日志中被抑制条目的原因分布合理。

> **状态：代码已就绪，出口条件未验**（2026-08-13）。出口条件按定义要自用一周，
> 无法由 CI 代劳；逐条操作见 [checklists/M3-提醒层验收](./checklists/M3-提醒层验收.md)。
>
> **接线后的形状**（一句话）：`tick → SignalEngine → AlertService → 四道闸门 →
> alert_log + PET/BUBBLE`。这两个渠道之外**没有第二条点亮路径** ——
> 状态点只认 `PetStateMachine`，而它只接受过了闸门的提醒。
> 这条纪律 M2 期间写在三处注释里，现在有 `alert-service.test.ts` 的两条用例钉着。
>
> **2026-08-13 渠道从四个减到两个**：托盘角标、图标闪烁与系统通知全部移除，
> `AlertChannel` 只剩 `PET | BUBBLE`。**级别没有合并** —— L2 与 L3 表现相同，
> 但冷却窗口（2h vs 当日一次）与每日上限不同，那才是三级制在管的事。
> 历史 alert_log 行里仍有 `TRAY` / `OS_NOTIFY`，读取端不过滤（否则当时真弹过的行
> 会看起来像被静默了）。
>
> **全屏 / 演示 / 专注助手探测走 PowerShell，不是原生绑定**（2026-08-13 的取舍）。
> Electron 没有 `SHQueryUserNotificationState`，也拿不到前台窗口尺寸（docs/05 §4.4
> 写的那条回退方案同样需要原生调用）。选 PowerShell 是因为原生 FFI 会引入**第二个**
> 需要跟 Electron ABI 对齐、需要在打包时外置的原生模块（better-sqlite3 已经是一个），
> 而这条探测**失败的后果只是照常提醒**，不是功能不可用。
> 代价是每次探测起一个进程（实测 100–300ms），所以：分发路径只读缓存（TTL 15s，
> 一轮 tick 30s）、单飞、连续 3 次失败即永久停用并留一条 warn。
> 停用后状态为 `UNKNOWN` → **判为可以提醒**，方向与 §4 的不对称一致
> （多发会被抱怨，漏发用户发现不了）。已在真机验证返回值（`5` = ACCEPTS_NOTIFICATIONS）。
>
> **C4「不遮挡全屏应用」仍未验收**：探测能力有了，但「全屏时窗口是否真的不遮挡」
> 要真机看，与 C1/C2 一样必须由人勾。
>
> **两条已知的、故意留下的边界**：
> ① 分发器状态（防抖计数、冷却、当日配额）全在内存里，重启即清零 ——
> 落库会让「重启后被冷却挡住、什么都不弹」变成极难查的问题，见 dispatcher.ts 的头注释。
> ② ~~托盘角标是 tooltip 里的数字 + 图标闪烁~~ —— 2026-08-13 整块移除。
> 未读计数仍然在算（面板的提醒日志用它），只是不再有人把它画成角标。

> **闸门必须先有单测再谈自用**（2026-08-13）：主观感受**发现不了漏发** ——
> 被吃掉的那条信号不会来告诉你它被吃了。两个方向的错误不对称：
> 多发会被立刻抱怨，少发（尤其漏掉止损）用户根本不知道。
> 所以 `AlertDispatcher` 的四道闸门先写了 21 条用例，包括
> 「强制类不受冷却但要按 2% 台阶复发」「超限是降级不是丢弃」
> 「每小时上限用滑动窗口而不是整点桶（否则 10:59 六条 + 11:00 六条 = 两分钟十二条）」
> 「L3 的当日一次不能写成 24 小时」这几条容易写错的。
>
> 顺带一条经验：**基准时刻本身也要挑。** 用例原先取 `1_700_000_000_000` 当基准，
> 它落在 UTC 22:13，于是「同日 6 小时后」实际上跨了日、把当日冷却重置掉 ——
> 用例反而通不过。已改成「当日 09:00」。
>
> **接线之后又补了 79 条**（2026-08-13）：候选构造、免打扰聚合、探针的缓存与退避、
> 状态机的驻留与回落、编排、alert_log 仓储、气泡落点。其中三条是同一类不对称错误的守卫：
> 「用户的整体级别偏移**不作用于**持仓强制类」（把止损从 L3 调成 L2 = 跌停那天不响）、
> 「探测不到时判为可以提醒」、「落库失败不能把提醒一起吃掉」。

---

## M4 · 打磨与发布

- [x] ~~皮肤系统 + 完整动画集 + 极简模式~~ —— **2026-08-13 判为不做并删除既有实现**：
      悬浮条已经覆盖了「极简模式」想解决的问题，而皮肤是纯成本项
- [x] 影子运行（模拟持仓绩效）—— `src/main/shadow/`（记账 / 推进 / 汇总三块）+ schema v2 的四张表。
      前向累积、一个交易日一根、**成交价用次日开盘**（与回测同源），成本模型直接复用
      `src/backtest/costs.ts`。两道闸门：`shadow_equity.trade_date` 主键保证幂等，
      引擎版本一变**立刻停止累积**（两套参数的绩效混进同一条曲线之后无法事后拆开）。
      docs/07 §2.3 的「满 3 个月前不做正面宣称」落成 `ShadowSummary.seasoned`，UI 据它决定能不能下结论
- [x] 设置页全项（轮询频率、灵敏度三档、静默时段、数据源优先级、**参数只读**、数据目录、清缓存）
      —— 两处与清单原文有出入，都是刻意的：① **参数不可编辑**，只给一张逐项标注标定状态的
      只读表（ADR-0003：让用户把一个未标定的猜测换成另一个没有依据的值，期望收益为零，
      而「改坏了 → 零信号 → 以为程序坏了」这条排查路径用户走不出来）；
      ② `AppSettings.sensitivity` 此前是**死设置**（schema 里有、没人读，与 `autoLaunch` 同一类问题，
      但更隐蔽 —— 开机自启不生效用户能发现，灵敏度不生效发现不了）。现在真的重建引擎参数，
      因此换档会递增引擎版本、清指标缓存、暂停影子运行，UI 上先弹确认框说明这个代价
- [~] 多显示器与 DPI 缩放 —— 拓扑变化校验与 `moveTo` 的取整修正已在（见 docs/06 §7），
      125%/150% 下的真机走查未做。**E2E 已经量到一条实测差异**（2026-08-13，150% 缩放）：
      悬浮条标称 300×38 而 `getSize()` 报 **300×40** —— 缩放比不是 100% 时 Electron 在 DIP 与
      物理像素之间两个方向都按「包住」取整，标称高度被抬 1–2 DIP。
      **不影响 C2**（命中区在渲染层按 `window.innerHeight` 实测重算，窗口抬多少就盖多少），
      是外观问题。逐条走查见 [M4 清单 §3](./checklists/M4-打磨验收.md)
- [x] 开机自启（`src/main/auto-launch.ts`）—— `AppSettings.autoLaunch` 此前是**死设置**
      （schema 里有、没人实现）。三条取舍：未打包不写注册表（否则注册的是裸 electron.exe）、
      只在与系统当前状态不一致时才写（否则会覆盖用户在「任务管理器 → 启动」里的手动禁用）、
      写失败只 warn 不抛（组策略锁注册表时不该连带把设置保存弄失败）
- [x] 单实例锁（`src/main/index.ts`）—— **数据完整性问题，不是打磨**：两个实例会各写一份
      `market.db`，且分发器的冷却/配额在内存里各有一份，「每小时 ≤ 6 条」在用户那里变成 12 条。
      已真机验证两侧：第二个实例 exit 0 并留一行日志，第一个收到 `second-instance` 后打开面板
- [x] 日志滚动（`src/main/logging.ts`）—— 按天分文件 `main-YYYY-MM-DD.log` + 保留 7 天。
      electron-log 自带的是按大小滚动且只留一份归档，前几天的日志会被今天的刷屏挤掉；
      `maxSize` 保留为**单日**硬上限。清理时认不出名字的文件一律不删（含改动前的 `main.log`）
- [x] 崩溃兜底（`log.errorHandler.startCatching`，不弹框、不上报远端）
- [x] 数据库裁剪（`storage/retention.ts` 的 `pruneIfDue`，已接在 tick 上）
- [x] 数据库**备份**（`storage/backup.ts`）—— 走 `VACUUM INTO` 而不是拷文件：WAL 下
      `market.db` 不是自足的（最近的写还在 `-wal` 里），拷主文件会得到一个**少了最后几分钟数据
      却照样能打开**的库，那种备份最坏，因为它看起来成功了。可在运行中做，顺带压掉裁剪留下的空洞。
      落在 `<数据目录>/backups/market-YYYY-MM-DD-HHmm.db`，保留最近 3 份，
      **认不出名字的文件一律不删**（与 logging.ts 同一条纪律）。自动备份挂在**休市维护**里
      （与裁剪同一处，排在它之后），盘中不跑 —— `VACUUM INTO` 要读全库，会和取数抢同一个连接。
      与 db.ts 那个「迁移前备份」是两件事：那个只留 1 份、是 schema 变更的一次性保险
- [~] electron-builder 打包（NSIS + portable），代码签名（如有证书）
      —— `electron-builder.yml` **已存在且配置完整**（此处早先那句「该文件不存在、
      `pnpm package` 必然失败」已过期，2026-08-13 更正）。三处与本项目强相关的取舍写在该文件
      头部：`npmRebuild: false`、`.node` 要 `asarUnpack`、`electronDist` 指向已解压目录
      （绕开 Defender 按住句柄导致的间歇性 EPERM）。
      **2026-08-13 已经真跑过一次**：产出 97 MB 的 NSIS + portable 两个包，`.node` 正确解到
      `app.asar.unpacked`，用干净 userData 跑 `release/win-unpacked/GP Pet.exe` 能起来 ——
      迁移到 v2、引擎与提醒就绪、自动备份产出，且日志里**没有** better-sqlite3 回退警告
      （即预编译产物在 Electron 43 下直接可用，文件头那条 `npmRebuild: false` 的判断成立）。
      同轮顺手把包瘦了 25 MB（122 → 97）：排掉 7 个用不到平台的预编译（15 MB）与
      `better-sqlite3/deps`（sqlite3.c 合并源码 10 MB，`npmRebuild: false` 下永不编译），
      并把 `files` 从 `out/**` 收紧成三个入口目录 —— 原先会把 `out/types/*.tsbuildinfo`
      （TypeScript 增量元数据，含全部源文件与测试文件清单）一起打进 asar。
      **仍未做的是装一遍 / 卸一遍**（开机自启只在打包后才写注册表，卸载是否带走它、
      `%APPDATA%` 是否保留，只能真装过才知道），代码签名也没有证书。
      **⚠ 排除 node_modules 里的东西只能用排除式 glob** —— `files` 里出现任何一条
      **正向的** `node_modules/...` 条目，electron-builder 26 会**原地改写源码树的
      `package.json`**（`scripts` 与 `devDependencies` 整块消失），接着 pnpm 按
      「没有 devDependencies」把开发依赖整批剪掉。恢复步骤见 [M4 清单 §5 踩过的坑](./checklists/M4-打磨验收.md)
- [x] 应用内免责声明与首次启动引导 —— docs/01 §8 要求「须在应用内展示」，落成
      **挡在面板前面的一屏**而不是页脚一行小字：确认之前主界面不渲染，确认时刻写进
      `AppSettings.disclaimerAcceptedAt`（存**时刻**而不是布尔值，声明文本日后若实质变更
      能按时间戳判断「他确认的是哪一版」）。引导只讲四条**边界**、不讲操作步骤，
      顺序是先说边界再说功能 —— 反过来（先夸功能、末尾一行免责）是理财类软件的通用做法，
      而这个软件的参数几乎全部未标定。声明文本在 `disclaimer.ts` 一处，引导与设置页「关于」共用
- [x] E2E 测试（Playwright）+ 手工验收清单 —— `tests/e2e/` 6 条，**真启 Electron**。
      抓的是「typecheck + build 全绿也照样炸」那一类：漏外置 `electron`、preload 打成 ESM。
      顺带钉住 `focusable: false`、preload 白名单、单实例锁、首启引导落盘。
      `workers = 1` 是必须的（单实例锁与并行互斥）。手工清单见
      [checklists/M4-打磨验收](./checklists/M4-打磨验收.md)

> **状态：代码项已全部完成，三项真机验收未做**（2026-08-13）。
> 剩下的是 125%/150% 缩放走查、真跑一次 `pnpm package` 并安装、代码签名（无证书）——
> 三项都只能由人做，逐条操作见 [checklists/M4-打磨验收](./checklists/M4-打磨验收.md)。
>
> **这一轮新增的两条纪律**（都写进了代码注释与用例，不只是文档）：
>
> ① **影子运行必须真的是前向的。** 它比任何历史回测都可信，前提就在这一条上。
> 所以：不补跑历史（补出来的那个叫回测，而它会带着一份看起来像实盘的报告）、
> 成交价用次日开盘（当日收盘价在 15:00 之后已经买不到了）、成本照扣。
> 引擎参数一变就**停止累积**而不是继续 —— 混进去的曲线不属于任何一套参数，
> 而它无法事后拆开（历史 K 线能重算回测，重算不出前向记录）。
>
> ② **设置页不给参数编辑框。** docs/01 §5.5 写的是「策略参数」，这里落成只读表 +
> 逐项标定状态。判据是 ADR-0003：真正标定过并写回的**只有一项**，
> 一张不分档的参数表会让二十来个转述猜测看起来同等可信。
> `tests/unit/main/params-view.test.ts` 钉住「`CALIBRATED` 恰好只有
> `strategy.squeezeBbwPct`」—— 往那一档加行就是把一个参数的证据扩张成整套参数的背书。
---

## 后续（P2，不承诺）

**2026-08-13 逐条核对了前置条件**，状态一栏是核对结果而不是计划：

| 项 | 前置条件 | 状态（2026-08-13） |
|---|---|---|
| LLT 低延迟趋势线 | 找到可复现的公式并通过 M2 的验证流程 | ⛔ **闸住**。这是新增指标，撞约束 5；而信号质量问题该先从数据质量、参数标定、提醒策略三处找 |
| 贝叶斯优化参数搜索 | 参数维度确实超出网格能力，且验证集流程稳定 | ⛔ **闸住**。`risk`/`multiTf`/`macd`/`boll`/`volume` **一个网格都还没跑过**，远没到网格不够用 |
| 分钟级 K 线与日内策略 | 数据源稳定性与请求配额允许 | ⛔ **闸住**。M1 出口条件（跑满一个交易日 + 健康度 > 99%）未达成，eastmoney 成功率约 78% |
| 背离检测计入得分 | 回测证明其不引入过拟合 | ⛔ **闸住**。同样是新增指标（约束 5），且日线回测尚未跑完既有参数的网格 |
| 自适应止损（基于 ATR） | 固定比例止损的绩效数据积累足够 | ⛔ **闸住**。`risk` 块零标定，没有可比基线 —— 拿一个未标定的方案去替另一个未标定的方案，测不出高下 |
| 全市场扫描选股 | 数据获取与本地算力评估 | ✅ **评估已出**：[P2-全市场扫描评估](./notes/P2-全市场扫描评估.md)。结论是**当前形态下不可做**，卡在取数（`attemptDeadlineMs` 与共用限流器）而不是算力 |
| 接入 AI 分析：针对个股，把当前分析结果给 AI，让它给出解释和合理预测；需用户自己配置模型接口才开启 | （表里原本没有前置条件） | ✅ **已实现**（`src/main/ai/`）。见下 |

### 接入 AI 分析（已实现，2026-08-13）

形状：本地引擎算完 → 结构化上下文 → **用户自己配置**的 OpenAI 兼容接口 → 流式解释。
不配置就整块不存在：设置页显示未配置，信号列表里**不渲染** AI 按钮，也没有任何网络行为。

四条写进代码注释与用例、不只是文档的纪律：

1. **它是只读的解释层。** 结果不回流到信号、闸门、状态点或影子运行。
   状态点只认 `PetStateMachine`，而它只接受过了四道闸门的提醒 ——
   让一段模型输出去点亮状态点，等于开一条绕过冷却与免打扰的旁路。
2. **API key 不进 `AppSettings`。** `config:export` 会把设置整份写进用户选的 JSON 文件，
   而那个文件的用途恰恰是「发给另一台机器」。配置整块住 `ai.json`，
   key 走 `safeStorage`（Windows 下 DPAPI）；**加密不可用时拒绝保存**，
   不退化成明文落盘 —— 那是「看起来成功了」的那一类失败。
   `tests/unit/main/config-transfer.test.ts` 有一条用例钉着导出文件里搜不到 key。
3. **不复用 `net/http.ts`。** 那一层只有 `get()`、默认 3s 超时，
   而且限流器与行情取数共用（全局并发 4）——
   一次 40 秒的 LLM 调用挂上去会把盘中每 30 秒一轮的 tick 饿死。
4. **上下文必须带参数标定状态。** 这一条是整个功能里最重要的一行：缺了它，
   模型会**默认**引擎结论经过验证，然后用非常有说服力的语气把一套未标定的转述阈值
   讲成定论（ADR-0003 要防的正是这件事）。`ai-context.test.ts` 钉着这一块必须存在。

措辞取舍（用户 2026-08-13 确认）：**允许模型给方向性预测**，但
① 提示词要求它必须同时给出依据与失效条件；
② 输出区**固定**包一层来源标注（「由你配置的外部模型生成，不是本应用的策略结论」）
与免责小字 —— 这两行是渲染层的 DOM，**不经过模型**，模型改不了它。
提示词里的禁令是尽力而为，DOM 那一层才是保证。

**未做**：Anthropic 原生 adapter。Anthropic 没有 OpenAI 兼容端点，
要支持 Claude 得另写一个走 `/v1/messages` 的实现（`x-api-key` + `anthropic-version` 头，
SSE 事件形状也不同）。`AiTransport` 与 client.ts 的分层留着口子。

---

## 关键决策点

开发过程中有三个地方需要**停下来重新判断**，而不是硬着头皮推进：

1. **M0 出口**：若点击穿透或不抢焦点做不到 → 桌宠形态存疑，考虑改为托盘 + 轻量悬浮条。
   **（2026-08-13：形态已定为「托盘 + 轻量悬浮条」，桌宠与皮肤系统整块删除。
   但这条决策点**没有**被触发 —— C1/C2 尚未真机验收，没有任何证据说明桌宠做不到。
   这是**产品选择**：一行字比一只猫更快说清发生了什么，且整个产品因此不依赖外包素材。
   见 [docs/06 §2.1](./06-桌宠交互与非干扰设计.md)。
   **M0 出口条件不变**：C1/C2/C3 仍要真机验收。）**
2. **M2 出口**：若分 Regime 归因显示动态权重无效 → 简化为单一策略 + 风控，砍掉组合层复杂度。
   **（2026-08-12 已触发并**收口**：权重表删除、两个策略等权（四续）；票数线改为按策略给（五续）；
   标定 R1–R4 后均值回归的贡献变得可测（+2.0 ~ +3.4pp，六续），**因此不退到单一策略**。
   两个策略都留，`enabledStrategies` 出厂两项皆 true。）**
3. **M3 出口**：若自用一周发现提醒要么太吵要么太哑 → 问题多半在 [04 §4.2](./04-指标与信号引擎.md) 的阈值或 [05 §4](./05-风控与提醒规则.md) 的冷却策略，回到参数标定而非增加新指标。

增加指标几乎总是错误的应对方式。信号质量问题优先从数据质量、参数标定、提醒策略三处找原因。

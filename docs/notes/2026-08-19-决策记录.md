# 2026-08-19 决策记录

> **这一天做了三类事**（`ENGINE_VERSION` **全程不变**，62 个叶子的标定归档**不变**，
> 引擎参数与判定逻辑**一行没动**）：
>
> | 类 | 内容 | 落点 |
> |---|---|---|
> | **口径对照** | 拿聚宽公开数据字典做第三方交叉验证：**指标口径**（BOLL/RSI 逐位一致、ATR 与 ADX 不同）· **绩效口径**（夏普差近一倍） | [M2 §5.38](./M2-偏差报告.md) · [§5.39](./M2-偏差报告.md) · [信源台账 §7](./信源台账.md) |
> | **缺陷** | 后复权价位让主池标的静默地一笔不建仓（平安银行整段），报告上与「没信号」无法区分 | [M2 §5.40](./M2-偏差报告.md)（**当天拍板走 A：只让它可见，已实施**） |
> | **方向（用户拍的）** | **停止在个股日线择时上找边缘** —— 见 §三 | 本文件 · CLAUDE.md · [困境说明 Q1](./困境说明-2026-08-17.md) |
> | **判据口径**（§三 之后的第二趟聚宽） | **两个候选判据被否**（CAPM alpha / 日胜率）· 两个进报告（beta / 除法版超额，用户拍板允许调整） | [M2 §5.41](./M2-偏差报告.md) · [论证 §9](./配置形态-论证.md) · §四 |
>
> **只读一节的话读 §三**，那是唯一改变「明天做什么」的；§四 改变的是「结论怎么读」。

---

## 一、口径对照：两项一致，两项不同

方法上值得记一笔：**不需要账号、不需要 Python、不触碰 JQData 协议** ——
聚宽公开数据字典页里每个指标函数都印着返回值示例，而他们的备注是
「计算方式与通达信、东方财富和同花顺相同」⇒ 拿它对等于一次性与三家国内平台对照。
工具落成 `scripts/verify/jq-crosscheck.ts`。

| | 结果 |
|---|---|
| BOLL | ✅ 逐位一致（1e-14）⇒「标准差除 n 不除 n−1」被外部证实，且除 n−1 的变体相对差 3e-4~7e-4 ⇒ **这次对照有分辨力** |
| RSI | ✅ 逐位一致（1e-9） |
| MACD 柱 | ✅ `2×(DIF−DEA)` 由他们自己印的三组数字代数确认（与日期无关） |
| ATR | ⚠ 口径不同：他们 `MA(TR,14)`（残差 0），我们 `Wilder(TR)/14` |
| ADX/DMI | ⚠ 口径不同：他们通达信 `EXPMEMA`（α=2/15）+ **ADX 用 MM=6**，我们 Wilder + 14 |
| 夏普 | ⚠ 口径不同：他们 `(年化几何 − 4%)/σ`，我们 `日均×√243 / σ, rf=0` ⇒ 沪深300 2005–2017 我们 **0.533** vs 他们 **0.281** |

**最花时间的一步单独记**：他们示例里写的 `check_date='2017-01-04'` 与打印出来的数值
**不是同一天**（真实日期靠 MA20 + 标准差反查是 2017-01-26）。第一版对照全红，
看起来像我们整套指标都错了。

**ADX 那条的后果最大**：`adx.baseThreshold = 20` 转述自国内平台语境，
而两套 ADX 在 699,907 个「股票·交易日」上「≥20 与否」有 **30.4%** 结论相反
⇒ **抄对了数、没抄对量纲**（约束 2 最隐蔽的一种形状）。分位等价值 **17.29**
落在已测邻域（18/20/22）之外 ⇒ 下一轮 `adx` 网格把下侧探到 16–18（**待办，非结论**）。

---

## 二、缺陷：后复权价位让部分标的静默地不建仓（4 只真被吞、10 只价位上够不着）

拿聚宽的「未来函数」四类清单逐条对 docs/07 陷阱表时，发现「复权」那一行还写着
「全程用前复权序列」（2026-08-12 就换成后复权了）。顺着核实语义撞出来的：

- 回测 fixture 的 `*Adj` 是**后复权、锚在上市日** ⇒ 平安银行 2018 首根真实 13.7 元、
  后复权 **2069 元** ⇒ 一手 20.7 万 > 默认每标的 10 万 ⇒ `lotsAffordable` 返回 **0 手**；
- 实测：`SZ000001` / `SH600309` / `SZ000858` **0 笔交易**，同批 `SH600000` 正常 9 笔。
  报告上只显示「收益 0.00% / 0 笔」，**与「引擎没给信号」无法区分**，无任何告警；
- 范围：主池 261 只里 **10 只**受影响，按「标的·交易日」加权 **1.48%**；
- **偏差方向不随机**：后复权倍数 ≈ 累计分红送转 ⇒ 命中的是分红历史最长的大盘股。

**用户当天拍板走 A（只让它可见），已实施**：`CodeResult.unaffordable` 计数 + 报告告警行
（点名标的与次数），两条用例钉着。**绩效数字一个没变**（同命令重跑：总收益 −1.99% ·
建仓 1097 次 · 建仓胜率 43.21% · 平均每次 −477 元，与既有基线逐位一致）。

**实施后才拿到确切量级**（此前只知道「0 笔」）：训练窗口 **31 次建仓意图被吞掉 / 1097 次建仓
= 2.8%**，真被吞的是 4 只（`SZ000001`×13 · `SH600309`×7 · `SZ000858`×7 · `SH600809`×4）。
⚠ **「10 只受影响」（按价位算）与「4 只真被吞」（按建仓意图算）不是同一个数**，引用别混。

候选 B（按真实价定手数）**仍挂着**，不是被否 —— 它要重跑全部基线。

---

## 三、方向：**停止在个股日线择时上找边缘**（用户拍的）

### 决定前的事实（比 08-17 提这个问题时变了形状）

问题原本的提法是「只在 RANGE 里有边缘（65%），而 66.6% 的时间处在 TRANSITION（22%）」。
两次实测把这个前提改掉了：

| | 原（08-17） | 现 |
|---|---|---|
| RANGE | 65% | **35.5%**（[§5.35](./M2-偏差报告.md)：对照组收窄到「当天也有信号的票」之后） |
| TRANSITION | 22% | 26.0%（同上）· 38.5%（[§5.36](./M2-偏差报告.md) 相关性调整后的无条件口径） |
| TREND_UP | — | **99.5%**（§5.35，154 次建仓）—— 一个**新假设**，不是救援 |
| 分类器改造上界 | — | 三条突变条件**整条删掉**，TRANSITION 只从 66.6% 降到 53.7% |

⇒ **占样本 88% 的两个最大层（RANGE + TRANSITION）都低于 50%。**
「只在 RANGE 里有边缘」这句话已经不成立了。

### 决定

**不再投入研发去修「个股 · 日线 · 择时」这条线的边缘**，力气转向：

1. **仓位 / 配置层** —— [§5.37](./M2-偏差报告.md) 那条分叉（纯波动率目标化：W1 夏普 +0.288、
   回撤 72.30% → 41.67%，W2 归零 ⇒ 主判据不通过但**不是干净的否**），
   三条可能的下一步在[配置形态-论证](./配置形态-论证.md) §8；
2. **ETF 双轨的前向记录** —— 它不需要新的研发，**时间自己在攒证据**
   （ETF 池是唯一见过训练窗口 Calmar > 0 的形态，但 59 次建仓 + 验证窗口反向 ⇒ 还不算测试）。

### ⚠ 这个决定**不是**什么（写清楚，免得下一轮读成别的）

- **不是「个股路线死亡」。** 个股照常评估、照常提醒、照常进影子运行与实盘记账 ——
  停的是**研发投入**，不是功能。CLAUDE.md 那条「『个股无法进化』由用户主观认定」仍然有效，
  用户**没有**做那个认定。
- **不是「回测/标定工具作废」。** 工具照用（配置形态实验就在用），只是不再拿它去搜
  「哪个参数能把个股择时救回来」。
- **不是把 A/C 判死。** A（TRANSITION 不出手）与 C（改分类器）是**因为已知上界太低**而不选：
  A 砍 30% 建仓且剩下的 RANGE 也只有 35.5%，C 的天花板实测只有 12.9pp。
  哪天有新证据改变这两个上界，可以重开。
- **不是丢掉 TREND_UP 99.5%。** 它进待办（见下），**等样本，不等灵感**。

### 由此产生的待办

| # | 待办 | 触发条件 |
|---|---|---|
| 1 | **TREND_UP 那 99.5% 立项** | 建仓样本从 154 涨到能说话的量级。要另立候选、另写论证与预注册（[§3.11.4](./下一阶段取舍与迭代计划.md) 第 2 条：分层不许拿来救原候选），且要先排除「小样本 + 那层绝对绩效极差」共同造出的假象 |
| 2 | ~~**§5.40 的处置**~~ | ✅ 当天选 A 并实施；B 仍挂着，触发条件是「愿意重跑全部基线」时 |
| 3 | **`adx` 网格下侧探到 16–18** | 若日后重开个股线才有意义；现在不做 |

**Q1（[困境说明](./困境说明-2026-08-17.md) §7）到此结案**，Q2（胜率与盈亏比的耦合）与
Q3（解锁标定的判据）随之**降级为「若重开个股线再谈」**，不再是活的阻塞项。

---

## 四、同日第二趟聚宽（第四轮）：**判据的口径对照 —— 两个候选判据被自己否掉**

§三 把方向转到「仓位/配置层」之后，[配置形态论证 §4 ③](./配置形态-论证.md) 里那句
「净值层面的判据需要一个现在还没有的工具」变成了下一步的前置。所以这一趟读的**不是策略、
不是数据，是判据**：聚宽 API 文档的「风险指标」与「投资组合优化」两节（公开页、
`pnpm fetch:page --render`、18 万字），把通行定义搬到**四份已经存在的报告**上重算。
全部数字见 [M2 §5.41](./M2-偏差报告.md)，判据清单落在[论证 §9](./配置形态-论证.md)。
**没跑新模拟**，`--touch-test` 仍是 5。

### 结论：能用的两个加进了报告，不能用的两个写死了「别加」

| | 判断 | 依据 |
|---|---|---|
| ❌ **CAPM alpha** | **不采纳当判据** | 低暴露策略几乎全程持现 ⇒ `α ≈ Rp − Rf(1−β) − βRm`，`Rf` 支配符号：单指数择时 2005–2017 rf=0 → **+1.29%/年**、rf=4%（他们的默认）→ **−2.57%/年**，而事实是 +24.14% vs 被动 +301.70% |
| ❌ **日胜率** | **不采纳**（择时形态） | 空仓日日收益为 0，基准跌它就「赢」⇒ 实测 ≈ 基准下跌天数占比，单指数那份 **45.9% vs 45.9%** |
| ✅ **beta** | 进报告 | 与「平均资金占用」逐份同向（3.49% ↔ 0.0205 · 11.52% ↔ 0.0371）⇒ 那条老纪律第一次有了**净值层面、无参数**的独立确认量 |
| ✅ **除法版超额** | 进报告（减法版保留） | 减法版在大基准涨幅上给出 **−277.56%**（读不出意思），除法版 **−69.10%** = 净值只有被动的 30.9% |

**用户当天拍板「允许调整」**⇒ 后两个已实施（`metrics.ts` 的 `betaOf` / `ratioExcessReturn`
+ `PerformanceBlock` 两个新字段 + 报告两行、四条用例）。**绩效数字一个没变**，
既有门槛一个字没动；同命令重跑与独立脚本 `scripts/verify/jq-riskmetrics.mjs`
在旧 JSON 上算的**逐位相同**（beta 0.0177 / 除法 +16.75%）。

### 顺手查出来的一个读反风险（报告一直在打印它）

训练窗口基线的**减法超额是 +14.06pp**（基准跌 16%、我们几乎空仓），
而同一条曲线的 **alpha(rf=0) 是 −0.28%/年** —— **两个口径符号相反**。
⇒ 别引用「训练窗口跑赢基准 14 个点」：那句话在算术上成立、在含义上是空的，
它说的全部内容是「那六年我们基本没投钱」。

### 这一趟最该记住的方法论

**外部指标清单的价值一半在「学到能用的」，另一半在「知道哪个不能用」。**
这次四个里两个被否，而**否掉的那两个恰恰是看起来最专业的** ——
如果不做这次量化对照，日后某轮很可能会有人算出一个正的 alpha 或一个 50% 以上的日胜率，
拿它去说「风险调整后是有效的」，**而那个结论会是错的**。
所以 `betaOf` 的头注释里把「刻意不做 alpha 与日胜率」连同数字一起写死了 ——
指标缺口看起来像疏漏，必须让它看起来像决定。

⚠ **同时写死了一条禁令**：这张新判据表**不许拿去重读 §5.37 那个已经报过的实验**
（[论证 §9](./配置形态-论证.md)）。用新度量回头读旧结果，与「换个 `σ_target` 重跑到通过」
是同一件事的两种做法 —— 移动球门。表的唯一用途是**下一次预注册时从里面挑，挑完写死再跑**。

---

## 五、凯利公式：**列入日后做仓位管理的参考项**（用户当天要求记下）

正文在[配置形态论证 §10](./配置形态-论证.md)，触发条件在[迭代计划 §4.8](./下一阶段取舍与迭代计划.md)。
这里只留三句最该记住的：

1. **凯利不是「把仓位调大」的工具，是边缘的换算器。** 拿我们自己的数：主池训练窗口
   `p = 43.21% · b = 1.112 ⇒ f* = −7.85%`；1097 次建仓的经验分布上最大化 `E[log(1+f·r)]`
   得 **`f* = 0.00`** ⇒ **处方是「不下注」**，与「每次建仓期望 −477 元」是同一件事。
2. **它在小样本上不是偏大，而是估不出来。** 同一份 59 次建仓的 ETF 样本：
   二元公式给 **+8.21%**、经验分布给 **+0.88**（88% 资金），**差一个数量级** ——
   因为 59 个观测撑不起分布的尾部。这条比「样本小要小心」硬得多，是本节最值钱的一句。
3. **我们现在其实没有仓位管理**：回测每标的独立 sleeve 买满 · 影子每笔固定 10 万名义 ·
   唯一的变仓位动作是回撤减仓**固定 50%**（硬编码常数）· 实盘只给方向不给量。
   ⇒ 日后要谈仓位，先跟这个基线比。

⚠ **禁令三条**（写在论证 §10.4）：不许用回测搜出来的 `μ` 定仓位 · 不许把凯利当参数偷偷引进来
（它自带 `μ`/`σ` 窗口、半凯利系数、单标的上限三个未标定数）· 不许用它回头解释已经报过的实验。

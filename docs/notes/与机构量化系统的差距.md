# 与机构量化系统的差距：工具、方法、模型

> 2026-08-17 起草。**这份文档是对标，不是自检** —— 自检看
> [量化系统现状盘点](./量化系统现状盘点.md)（按十二环节问「今天站在哪儿」），
> 这里问的是「与一个机构级系统相比，缺的到底是什么，缺了会怎样，补它值多少」。
>
> **写它的理由**：这些判断散在几次会话的讨论里，不落盘就会消失，
> 而下一轮迭代很可能在同一个地方重新想一遍。
>
> **两条纪律**：
> 1. **每一条都要挂在本项目的具体数字上**，不写教科书条目。没有本地证据的只列进 §8 待验证。
> 2. **区分「结构性差距」与「能补的缺口」** —— 前者是形态选择的代价（改它等于换个产品），
>    后者是工程量。混在一起会让人去补一个补不上的东西。

---

## 0. 先说不是差距的

避免下一轮又从这里开始想：

| | 事实 |
|---|---|
| **算力** | 全 A 5400 只一轮评估 **2.7–3.8 秒** CPU（[P2 评估 §3](./P2-全市场扫描评估.md) 实测）。机构的算力优势用在高频与大规模搜索上，不在这个形态里 |
| **方法论纪律** | 配对逐折比较（`\|Δ\|/stderr ≥ 2`）、训练集红线、测试集触碰计数、`KEEP`/`INCONCLUSIVE` 三态裁决、随机化零点、内生性控制（`--shuffle-spans`）、跨种子稳健、加权与中位双口径、「每个候选机制只报一次分位」—— 多数个人项目没有，部分机构团队也做得更松 |
| **未来函数** | 由架构消除（`src/core` 纯函数 + `assertNoFuture` 双保险），不是靠自律（[ADR-0004](../adr/ADR-0004-核心引擎与-electron-解耦.md)） |

---

## 1. 结构性差距（改它 = 换一个产品）

### 1.1 横截面 vs 时序 —— 影响最大的一条

机构主流是**横截面**：同一天对全市场打分，取相对排序（多头超配前 N / 多空对冲）。
本项目是**时序择时**：对每只票单独判断「现在该不该买」。

| | 横截面 | 本项目（时序） |
|---|---|---|
| 市场 beta | 相对排序自动中性化 | 混在收益里，[§5.21](./M2-偏差报告.md) 要专门造随机基准才能剥出「池子漂移 +0.25%」 |
| 有效样本 | 股票数 × 交易日数（每天数千个相对判断） | **建仓次数**：8 年 **1675 次** |
| 零点 | 自带（中位数股票） | 没有，得自己造（`audit:random` 存在的全部理由） |
| 单次实验 | IC 是向量化的秒级操作 | 一个候选 **4–11 分钟**；八张网格约 5 小时 |

**后果**：1675 次建仓要支撑 62 个叶子参数的标定，样本量与自由度之比在结构上就很紧张。
这也是[现状盘点 §2](./量化系统现状盘点.md) 那条「规则触发而非因子打分」的统计学后半句。

**能不能补**：不能整体改（产品形态是「跟踪用户自选的几十只」，
[全市场扫描评估](./P2-全市场扫描评估.md)判为当前取数架构下做不了）。
**但可以局部借**：见 §2.1 的因子分析口径。

### 1.2 容量约束 —— 唯一朝我们这边倾斜的一条

机构的策略必须能承载资金，这砍掉大量策略空间：小市值、低流动性、高换手、短窗口事件驱动。
本项目**完全不受这个约束**（不下单、单标的独立资金、平均占用 3.5%）。

**这是机构有钱也买不走的位置。** 但要用上它的前提是先有一个正 alpha ——
目前手里只有 `RANGE / R3+R4` 那个弱正（配对胜率 66.5%~77.0%，[§5.26 ⑥](./M2-偏差报告.md)）。

### 1.3 执行

机构：算法交易（VWAP/TWAP/IS）、冲击成本模型、程序化下单。
本项目：**不下单**，滑点是常数 0.1%（[§5.22](./M2-偏差报告.md) 实测那已是行业区间 0.2%–0.5% 的下缘），
用户手动执行的滑移不可控。

> **有一个测量它的基建已经在了但没用起来**：`src/main/trades/ledger.ts` 的实盘记账与影子组合
> **刻意共用同一套 `costs.ts`**，所以两者的差值就是执行滑移的真实测量 ——
> 机构做 implementation shortfall 分析的个人版。缺的只是把它当成一个指标去看。

---

## 2. 能补的缺口（按性价比排）

### 2.1 风险因子归因 —— ✅ **2026-08-27 三层做完：负 IC 主要是一个 20 日动量暴露**

> ✅ **行业中性化已做**（[M2 §5.68 预注册 / §5.69 结果](./M2-偏差报告.md)，
> `pnpm audit:ic -- … --industry data/industry/sw-industry-history.json`）。
> 用 08-27 落地的申万变迁史（**按时点取归属**，用今天的归属回标是未来函数）
> 对 §5.46 那个负 IC 做了第一次风险因子分解：
>
> | 训练窗口 · `positiveOnly` | 原始 IC | **行业中性 IC** | Δ |
> |---|---|---|---|
> | 10 日（主判据） | −4.981%（t −5.38） | **−4.399%**（t −4.27） | **+0.582pp** |
>
> ⇒ **结局 A：负 IC 活下来了，那个负号是个股层面的，不是行业倾斜的副产品。**
> ⚠ **两条不许漏的限制**：① **Δ 的符号在两个窗口相反**（验证窗口中性化让 IC 更负，
> 10 日 −0.765pp）⇒ **「行业解释了 11.7%」这个数不许引用**，能引用的只有
> 「中性化之后负 IC 基本不变」；② **功效很低** —— 组规模中位只有 **2**
> （`positiveOnly` 丢掉 70.8% 的判定根 ⇒ 每日横截面 60 只不是 252，
> 60/28 个行业 ≈ 2.1）⇒ 结论应读成「**在这个粗糙的行业分辨率下看不到行业分量**」，
> 不是「已排除行业」。
> ⇒ ~~仍然缺的：市值层与动量层的中性化~~ ✅ **同日做完，见下。**

> ✅ **市值与动量也做了，而这一层出了结论**（[M2 §5.70 预注册 / §5.71 结果](./M2-偏差报告.md)，
> `pnpm audit:ic -- … --liquidity data/liquidity-em --risk-factors`）。
> 方法从「分组去均值」换成**秩上的横截面 OLS**（连续量只吃 1 个自由度，
> 而 §5.69 的分组法在组规模中位 2 上已经到顶）。
>
> | 控制变量（`positiveOnly` · 10 日） | 训练 IC | Δ | 验证 IC | Δ |
> |---|---|---|---|---|
> | 原始 | −4.981%（t −5.38） | — | −0.868% | — |
> | 市值 | −5.165% | **−0.184pp** | −1.610% | −0.742pp |
> | **动量20** | **−1.434%** | **+3.548pp** | +2.423% | **+3.291pp** |
> | **市值 + 动量20（主判据）** | **−1.458%**（t −1.97） | **+3.523pp** | +1.805%（t +1.66） | +2.673pp |
>
> ⇒ **结局 B：那个负 IC 主要是一个 20 日动量暴露**，市值几乎零贡献
> （「小市值暴露」这条在 §5.45 被证伪过一次，这里换方法第二次同向）。
> **⇒ 三层做完了**：行业无贡献且不稳（§5.69）· 市值无贡献 · **动量是主要贡献者**。
>
> ⚠ **三条不许漏**：
> ① **剥掉动量之后剩下的约等于零，且符号不稳**（训练 −1.458% / 验证 **+1.805%**，两个 \|t\| < 2）
> ⇒ **不许说「中性化之后转正了」**；
> ② **分不开两种解释**（预注册限制 2 事先写着）：「负 IC 是动量的影子」与
> 「中性化把得分本身砍掉了」在数据上等价 ⇒ 能说的只是「**得分的横截面信息与 20 日动量高度重合**」；
> ③ **本项目少见地看到机制量级跨窗口复现**（动量20 的 Δ = +3.548 / +3.291pp，差 7%）——
> 但**稳定的是「暴露有多大」，不是「策略有多好」**。
>
> ⚠ **把「中性化掉动量」变成引擎改动是完全独立的一件事**，要走
> [docs/07 §3.6](../07-回测与验证方案.md) 的五条门槛。§5.71 **不构成**那个候选的论证。

#### 补之前的原文（保留供对照）：现在完全没有

机构有 Barra/Axioma 这类风险模型，能回答「这 −0.09% 里多少是市值暴露、多少是动量、
多少是行业、多少是真 alpha」。

本项目能做的是**策略内部**分解（状态效应 / 挑选效应 / 风控效应，[§5.21](./M2-偏差报告.md) 做得很好），
但那**不是风险因子分解**。

**~~一个具体的、今天就能测的假设~~ -- ✅ 2026-08-20 由 [§5.45](./M2-偏差报告.md) 测了：「前 5 日涨幅最高那一档」中位配对胜率 0.5% ⇒ 「T3 买在高动量上很差」被证实，但形状是倒 U 不是单调反转 ⇒ 机制是「极端动量不利于买入」不是「反转」。**

> `T3_BREAKOUT` = 收在上轨之外 + 带宽扩张 = 典型的**短期动量暴露**。
> 而 A 股短期（1–4 周）最稳健的异象恰恰是**反转**。
> `T1_MA_CROSS`（金叉）位置靠前、暴露弱得多 —— 这可能就是
> `T2+T3+T4` 配对胜率 3–4% 而 `T1+T2+T4` 是 58–63.5%（[§5.21](./M2-偏差报告.md)）的机制层解释。

**怎么验**：把建仓按「入场前 N 日涨幅分位」分层看配对胜率，单调递减即支持。
这是 `audit:*` 类只读工具（与 `audit:regime` / `audit:random` 同类），**不新增指标，不碰约束 5**。

### 2.2 ✅ 池外参照 —— **2026-08-22 补上了（GH1），M²/GH2 判不能用**

[§5.27](./M2-偏差报告.md) 的结论：**任何「池内配对随机化」在结构上都测不了「池子本身好不好」**，
因为基准就在同一个池里。实测两次证伪：补进退市股后绝对绩效变差 0.15pp，
而默认口径（择时）配对胜率升 8pp、跨票口径（选股）升 7.5pp。

⇒ **「选到烂票」这个维度目前没有任何判据能碰。**
报告里 `benchmarkReturn` 是池外的，但它满仓而我们占用 3.5%，两者不可比（[§5.13](./M2-偏差报告.md)）。

~~**缺的工具**：**相同资金占用下的被动持有**作为参照。这是目前优先级最高的一个缺口。~~

**✅ 2026-08-22 补上了**（[M2 §5.52](./M2-偏差报告.md)，`scripts/verify/outside-pool.ts`）。
口径是 **GH1**（**Graham & Harvey**, JFE 42 (1996) 397–421 / FAJ 53 (1997) 54–66）：
把**基准**与现金按恒定权重混到 `σ` 等于组合的 `σ`，再比收益。

| 样本 | `w(σ)` | **GH1** | 而报告现在印的除法版超额 |
|---|---|---|---|
| 训练 2018–2023 | 4.18% | **−1.71pp** | **+16.75pp** |
| 验证（修正后·评估期） | 5.06% | **−0.37pp** | −13.51pp |
| 指数择时 2005–2017（校准） | 19.01% | **−16.40pp** | −69.10pp |

⇒ **同风险下，把 4~5% 的钱放进沪深300、其余持现，比这套主动择时更好**（两个窗口同向）。
⚠ **训练窗口上它与现有口径符号相反**（+16.75 vs −1.71）——「跑赢了下跌的基准」
与「输给同风险的被动持有」**同时为真**，而报告只印了前者。
⇒ 纪律：**引用超额时两个必须一起给**。

**三条限制**：① **匹配口径是个能翻符号的自由度**（指数那份上占用匹配给 **+0.72pp**）
⇒ 要预承诺，已登记成拍板项；② **GH2 / M² 判不能用** —— 需要 `σ_m/σ_p ≈ 20–24` 倍杠杆
（拿不到）、`R_f` 系数 22.9 ⇒ **符号由 rf 支配**（与 §2.1 否掉 CAPM alpha 同源、放大 24 倍）、
且 `M² ≡ (SR_p−SR_m)·σ_m` ⇒ **零新信息**；
③ **必须只在评估期上算** —— 带预热段等于白送被动参照一段市场暴露。

### 2.3 多重比较的正式处理 —— ✅ **2026-08-20 补上了 DSR，PBO 判「刻意不做」**

> **结论先写**（详见 [M2 §5.48](./M2-偏差报告.md) 与 [CHANGELOG](../../CHANGELOG.md) 顶条）：
> - **DSR 能算，口径有一手依据，脚本 `scripts/verify/dsr.ts` 自检复现作者算例（0.8997 vs 作者的「90%」）。**
>   门槛：6 年训练窗口上要**年化夏普 ≈ 1.0** 才算发现（N=98）。
>   **但它不进写回门槛** —— 现有门槛判逐折配对 Δ，DSR 判单条曲线的夏普，不是同一个问题。
>   唯一合法用途是**进下一次预注册**。
> - **最值钱的副产品**：那 1.0 里**多重比较只占 0.30**，剩下 **0.68 是「1457 根就是不够长」**
>   ⇒ 即使一次试验都不多做，夏普 0.3 的候选在这个窗口上也说明不了任何事。
> - **PBO / CSCV 刻意不做**：输入不存在（`T × N` 逐期矩阵，`foldScores` 108 行全空），
>   补上要重跑 98 次模拟；**且算出来承不了重**（它量排名持续性，而我们 0/98 个候选夏普为正）。
>   重开条件：候选族里出现多个夏普为正、需要从里面挑一个的那天。
> - **仍然裸露的那一半**：`N` 由研究者自己申报、外部无法审计 ⇒ 三档 `N` 一起报是刻意的。

下面是补之前的原文，保留供对照。

[§5.25](./M2-偏差报告.md) 自己写了「88 个分层没做多重比较校正」并因此不采信那个 78.5%——
判断对了，但**靠的是人记得**。机构会把「试了多少次」量化进检验：

| 工具 | 补什么 |
|---|---|
| **PBO**（回测过拟合概率）/ CSCV | 把搜索次数正式计入 |
| **Deflated Sharpe Ratio**（Bailey & López de Prado） | 同上，输出一个校正后的显著性 |
| **Harvey, Liu & Zhu (2016)** | 因子检验门槛应是 t ≥ 3 而非 2 |

> ⚠ **Harvey 那条的读法要反过来。** 本项目的问题是 t 值普遍**太低**
> （`combine` 块最大 1.4、`drawdownReducePct` 0.77），机械地把门槛提到 3.0 只会让更多东西
> 变成「测不出」。它的实际价值是**读数校正**：现在报的 `\|Δ\|/stderr ≥ 2` 已经是乐观的。

### 2.4 序列相关调整（Newey-West）

[docs/09 §2.2](../09-下一阶段开发计划.md) 已经承认「横截面折之间不独立（A 股同涨同跌），
264 只是**下界**」，但标准误没有做相应调整 ⇒ **当前的 t 值在结构上偏乐观**。

补它是纯统计改动，不动任何交易逻辑。

> **2026-08-18 升级为「已定位」** —— 用户问「做回归时检查过残差自相关吗」引出的一次盘点。
> 先纠前提：**本项目没有任何回归**（`src/` 全仓无 OLS / linreg），所以没有残差可查；
> 但同一个病（不独立 ⇒ 标准误偏小 ⇒ 显著性虚高）落在**三处**：
>
> | 处 | 状态 |
> |---|---|
> | `calibrate.ts` 的折间 `stderr = stdev(Δ)/√n`（12 折从同一次模拟切出） | **中招**，且报告里**没有任何一行标注它未调整** |
> | Sharpe `(mean/sd) × √243` | **中招**（占用 3.5–4%，日收益既不独立也不正态）⇒ 只能当口径统一的排序量 |
> | `audit:random` 随机化 | **一半躲过**：择时口径逐次独立抽日 ⇒ 破坏了真实建仓的时间聚集（549 天同日 ≥2 只、最大 17 只）⇒ 零分布方差偏小；**跨票口径固定日期 ⇒ 歪打正着地对齐了市场因子** |
>
> 落点、修法（聚类标准误 / block bootstrap / block permutation）与
> **「必须在第一个候选逼近门槛之前修」**的时机论证，见
> [迭代计划 §4.6](./下一阶段取舍与迭代计划.md)。docs/07 §3 已就地加了「t 是上界」的标注。
>
> **2026-08-20：第四处出现并已修 —— rank IC 的 t**（[M2 §5.47](./M2-偏差报告.md)）。
> 它是 §4.6 结案**之后**新长出来的，而且比另外三处**更硬**：前瞻收益重叠
> ⇒ IC 序列带 **MA(h−1)**，那不是「可能不独立」的估计而是机械已知的结构。
> Bartlett 核 NW 调整后朴素 t 缩 **1.5–2.4×**（缩小倍数随 h **严格单调**，机制被证实）：
> 训练窗口 `|t|` 3.7–6.1 仍稳过，验证窗口 5/10 日测不出、20 日 −2.01 压线。
> ⇒ **§5.46 那句「t 是上界别引用」已可撤回**，主判据第一次有了能引用的数。
> ⚠ 学到的两条口径细节：`⌊4(T/100)^(2/9)⌋` 该归 **Andrews (1991)** 不是 NW(1987)；
> **滞后截断阶 ≠ 带宽**。以及一条方法论：**调滞后阶到显著为止是文献里有记录的
> p-hacking 通道**（有论文专门建模这种行为）⇒ 滞后阶规则必须预承诺。
>
> **2026-08-21：第五处（夏普方差）也做了，这一行到此结案**（[M2 §5.50](./M2-偏差报告.md)）。
> 补的是 Lo (2002) 的 `V_GMM`（`sharpeRatioHac`，HAC 长期协方差；`lag=0` 逐位退回
> Mertens/Christie 闭式）。**结论两半**：① §5.49 那个「×1.3」**当时就是对的** ——
> 夏普方差 VIF **1.3208 / 1.2975** vs 均值 VIF 1.3126 / 1.2865（差 < 1%），
> 因为日频 `SR_日 ≈ 0` 让 `S₁₁/σ²` 占 100.5%；**别外推到月频高夏普策略**；
> ② **`η(q)` 在 `q = 243` 上判不能用**（满阶 22.0 vs 截断 13.5 vs `√243` 15.6，
> 相差 63.4% 且方向不一致）。
> 顺带纠了 `stats.ts` 一处归属错误（正确链条：Jobson & Korkie 1981 → Lo 2002 →
> **Mertens 2002 是对 Lo 的更正** → Christie 2005 → Opdyke 2007 证明后两者同式），
> 并记住那个陷阱：**「闭式在平稳遍历下成立」≠「自相关已处理」**（它用同期矩）。
> ⇒ **夏普那半格不再是「刻意不做」，是做完了**；仍然**不参与写回门槛**（门槛判逐折配对 Δ）。

> ✅ **2026-08-26 补上最后一格：两条曲线夏普之差的标准误**（[M2 §5.60](./M2-偏差报告.md)，
> `sharpeDiffHac` + `npx tsx scripts/verify/sharpe-diff.ts`）。
> 归属链与上面那条**不是同一条**：**Jobson & Korkie (1981) → Memmel (2003) →
> Ledoit & Wolf (2008)**（⚠ **Mertens ≠ Memmel**，差一个字母、两篇文章、两个问题）。
> 缺口的量级：朴素合成 SE（两条单腿平方相加，隐含 `Cov = 0`）实测高估
> **1.35 倍**（ρ = 0.423）到 **3.5 倍**（ρ = 0.92）⇒ **把测得出的差别判成测不出，方向不保守**。
> 三条读法：① **不推翻 §2.4a 引的那张 §5.51 ③ 表** —— 那里两腿是**不相交子段**、本来就独立；
> 受影响的只有**同期两条曲线**（消融对照 / ETF vs 个股 / 目标化 vs 满仓）；
> ② **`beta` 小不代表 `ρ` 小**（3.5% 占用、beta 0.0205 的策略与基准 ρ 仍有 0.423）
> ⇒ 这个修正在「看起来几乎无关」的配对上也有 1.35 倍；
> ③ LW 原文自己说 **HAC 在中小样本上偏自由** ⇒ `p` 刚过 0.05 不许承重，已登记。

### 2.4a ✅ **平稳性 —— 2026-08-21 第一次检验，H1 在方差上被拒**

上面 §2.4 那五处 HAC/聚类调整全部建立在**平稳性**上，而这个假设此前**一次都没被检验过**
（「平稳」在整个仓库里只出现在归属注释里）。2026-08-21 补上，全部记在
[M2 §5.51](./M2-偏差报告.md)。

口径：CUSUM-of-squares，IID 版归 **Inclán & Tiao (1994)**、HAC 分母那版是
**Sansó, Aragó & Carrion (2004)** 的 **κ₂**，临界值 1.358（布朗桥上确界 5%）。
自检是经验水平与功效（水平 2.5–3.5% / 名义 5%，偏保守，与 Garbade 1977 / Hackl 1980 一致）。

**结论不是「工具作废」，是「每个数带上窗口」**：

| 判 | 处 |
|---|---|
| **站得住** | MinTRL（输入是**假设**的夏普，肥尾项在我们的效应量上只值 0.3–4%）· `pairedDelta` 的 CR1（聚类稳健标准误**本来就允许簇间异方差**，它的软肋是 G=3 不是平稳性）|
| **只在这个窗口里站得住** | `sharpeRatioHac` 的 VIF（全段 1.32、分段 1.09–1.56）· DSR 门槛 0.98（依赖实测矩）· `audit:random` 的块置换（低波段与高波段的块不可交换；`REGIME_BLOCK` 部分缓解但没量过）|
| **站不住** | 训练/验证划分（σ 0.38× · γ₄ 6.06× · 验证窗口夏普由最后一个月的几天决定）|
| ⚠ **没测到** | **rank IC 的 NW `t`** —— 而它是唯一承重主判据的那一处。`audit:ic` 只落聚合量、逐日 IC 序列没落盘 ⇒ 已登记成条目 |

**最有迁移价值的一条**：**方差的不平稳一测就中，均值的不平稳在这个样本量下根本测不动**
（三段夏普两两 \|z\| = 0.86 / 1.08 / 0.24）。⇒ 这就是「验证窗口反向」反复出现却推不出
任何结论的**统计学根因**，不是我们不够仔细。

### 2.5 ✅ Meta-labeling —— **2026-08-24 取了口径，产出是「§3.6 的门槛管不到出场类改动」**

主模型定方向、副模型定「这次要不要下注」（López de Prado, *AFML* **第 3 章**；下注规模第 10 章）。

<!-- ITEM id=alpha-gate-exit-class | 类=待测 | 桶=就绪 | 代价=中 | 来源=docs/07 §3.6a | 判据=docs/notes/M2-偏差报告.md:出场类改动的 alpha 已测 | 标题=audit:random 的配对胜率对出场类改动咬不咬得住从未验证（random-*.json 里没有一份 abl-*），既不许假设它咬得住也不许假设它不咬 -->

**2026-08-24 落到本地的结果**（[M2 §5.57](./M2-偏差报告.md)，工具 `scripts/verify/meta-label.ts`，
只读 7 份已有报告）—— 映射本身成立：**③ 就是 precision**、**② 就是保留率**，
两者相乘在纯过滤下隐含 **recall ≥ 0.85**（⇒「§3.6 缺一条召回门槛」这个猜测**不成立**）。
**而它查出来的东西比这个映射大**：

- **门槛②③ 对「出场类改动」结构上不设约束** —— 出场改动几乎不动建仓集合
  （实测保留率 **97.99–99.27%**，门槛线 85%）⇒ ② 咬不住；而 ③ 只看胜率，
  能被「拿盈亏比换胜率」机械满足（§5.31：关回撤减仓胜率 +7.9pp、盈亏比 1.11→0.91）。
  实测 **4/6 个候选同时过②③**，而 §5.30/§5.31 早已判它们全部**没过红线**。
  ⇒ **2026-08-25 装成闸门了**：[docs/07 §3.6a](../07-回测与验证方案.md)（先按「共有建仓 / `pnl` 变动比例」
  两个数**分类**，出场类的 ③ 换成「每次建仓的期望不降」、主判据换成**逐次建仓的配对 Δ**
  —— 那套判据被 §3.6 排除的理由是「逻辑改动会改变交易的集合」，而出场类改动恰好不改变它）。
  条目 `gate-exit-class` 已关闭；新长出两条（工具 `exit-paired-delta` · 待测 `alpha-gate-exit-class`）。
- **`F1` 判不能用**：它在**已判为负**的候选上从 0.6034 升到 **0.6751**
  （`F1` 内嵌 1:1 错误代价、完全不看真阴 ⇒ 不是经济目标）。数字写死在工具头注释里。
- **`t3fix.json` 的窗口是全期、与基线的训练窗口不可比** ⇒ §5.24 那次判定逻辑改动
  事后算不出 precision/recall。⇒ **2026-08-25 写进 §3.6 的程序纪律**
  （验收件必须与基线**同窗口**；想看全期就跑两份，别让一份顶两个用途）。
  条目 `logic-change-same-window` 已关闭。

**参考价值不在于「引入一个模型」** —— 1675 次建仓训练任何分类器都是过拟合机器 ——
而在于它给这个做法一套评估框架（主模型召回 vs 副模型精度的权衡如何量化）。
**用起来必须克制到「一条规则」，不是 GBDT。**
⚠ **本项目已经做过一次手工 meta-label**：`T3_BREAKOUT:BUY` 在 `TREND_UP` 不计票
（[§5.24](./M2-偏差报告.md)），配对胜率 19.5/27.0/29.5% → 66.5/67.0/69.0%。

⇒ 它确实让 [docs/07 §3.6](../07-回测与验证方案.md) 那四条门槛「从经验规则变成有理论位置的东西」，
**只是那个位置显示出其中两条对一半的改动类型是空的**。

### 2.6 ✅ 成本模型的凸性 —— **2026-08-24 量了，那句「影响有限」不成立**

机构按成交量占比建凸成本函数；本项目是常数比例（[现状盘点 §3](./量化系统现状盘点.md)）。

~~对 3.5% 占用的小资金影响有限，**但任何「放大资金」的讨论里它会立刻变成主导项**。~~
⇒ **前半句被实测推翻**（[M2 §5.54](./M2-偏差报告.md)，`npx tsx scripts/verify/impact.ts`）。

平方根冲击律 `I = Y·σ_D·√(Q/V_D)`（**Loeb 1983 → Torre & Ferrari 1997 (BARRA) →
Grinold & Kahn 1999/2000 → Almgren et al., Risk 18(7) 2005**）套在既有基线的
1097 次建仓上（覆盖率 100%），保守口径 `Y=1`：

- **中位冲击 7.33 bp = 出厂那条 10 bp 滑点线的 73%**，且 **27.6% 的建仓已经超线**
  ⇒ 「影响有限」只在 `Y ≤ 0.5` 或指数 0.6 那两档下成立。
- 临界资金倍数 `k*` 中位在三档口径下是 **1.9× / 4.8× / 7.4×** ——
  而 [§5.44](./M2-偏差报告.md) 那次实验用的正是 **5×**。**这道题今天判不了**
  （`Y` 文献取值 0.34–1.0 ⇒ 水平约 3× 不确定度），但**中心估计说它越线了**。
- **候选 B（按真实价定手数）与成本模型是耦合的**：B 干的事恰好是把 `Q` 从「模拟实际」
  拉到「名义」，实测这一步把冲击中位 **4.09 → 7.33 bp**、超线占比 **8.7% → 27.6%**。
- **失效有两条独立路径**（`σ` 与参与率 Spearman **−0.47**，两个因子相互抵消而非放大）：
  高波动·低参与率（`SH600744` σ 7.46% / 0.36%）与低波动·高参与率（`SH600234` σ 1.14% / 5.14%）
  ⇒ 换模型时**单维度分档会漏掉一条**。

**本轮没改 `DEFAULT_COSTS`**，也不许拿这些数重判任何已报绩效（M2 §5.54 的 P6）。

**⚠ 同日追加：这件事没有廉价替代路线**（[M2 §5.55](./M2-偏差报告.md)，
`npx tsx scripts/verify/volume-cap.ts`）。外部成熟平台（聚宽）在这个问题上用的**不是**凸成本
—— 三种滑点全部与成交量无关，大单靠 `set_option('order_volume_ratio')` 这个**成交量硬上限**兜，
而它**默认 1.0 = 不设限**。实测那个形态**替代不了**：

- **两者几乎正交**：按冲击排序与按参与率排序的 top-50 **Jaccard 仅 0.176**（对 N 不敏感）——
  冲击名单的 `σ` 中位 **2.69%** vs 参与率名单 **1.25%**（全样本 1.70%）
  ⇒ **凸成本抓的是波动，硬上限抓的是流动性**，这是上面那条「两条独立路径」的定量版本；
  冲击最大的那一笔（`SH600744`，44.45 bp）在参与率排序里排 **311/1097**，任何 `ratio` 都抓不到。
- **门槛差 10–30 倍**：凸成本 `k*` 中位 1.9–5.2×，而最严的 `ratio=0.10` 要到 `m*` 中位 **55×**
  （最先被截的 5% 也要 12×）⇒ **出厂资金下三档 `ratio` 的截断次数全是 0**，加了等于恒等变换。
- ⇒ 顺带一个外部事实：**聚宽的默认配置在 1× ~ ~12× 这整段个人资金区间里两道机制一道都不响**。
  「成熟平台有现成方案可抄」在这里不成立。
- **硬上限的正确定位是补充不是替代**（它无自由参数，管的是「物理上下不下得去」），
  且只在 ≥ 10× 才有意义 ⇒ **今天不落地**。

<!-- ITEM id=convex-cost-before-scaling | 类=落地 | 桶=等条件 | 前置=真要放大单标的资金（> 20 万）或做 §5.44 候选 B「按真实价定手数」时 | 代价=中 | 来源=M2 §5.54 | 判据=src/backtest/costs.ts:平方根冲击 | 标题=放大资金或修手数 artifact 之前必须换凸成本模型（两件事耦合，此前分开登记） -->

### 2.7 Point-in-time 数据基建

机构数据库存「当时能看到的版本」：财务数据的历次修订、成分股变更历史、退市股。

项目**有这个意识**（[build-universe.mjs](../../scripts/build-universe.mjs) 明确写了
「不要改成按市值或流动性筛 —— 那是今天的属性，会引入前视偏差」），
**能力上今天补上了一块**：退市股（[§5.26](./M2-偏差报告.md)，幸存者偏差 = **−0.15pp**）。

仍然缺的：财务数据的修订历史、指数成分股变更历史。

> ✅ **2026-08-26：财务的 PIT 那一格补上了一半，而另一半是个硬限制**
> （[M2 §5.62](./M2-偏差报告.md)，`scripts/fetch-financials.mjs` + `scripts/verify/factor-ic.ts`）。
> **能用的那一半**：东财 `RPT_LICO_FN_CPD` 的 `NOTICE_DATE` 是真的公告日
> ⇒ 261 只 × 2018–2025 逐日取「`noticeDate <= 当日` 的最新一期」，**覆盖率 100.00%**
> （403,197 个「股票·交易日」，落空 0）。⇒ 本项目第一次有价格/成交量以外的维度。
> **做不了的那一半**：🛑 **这个端点不覆盖退市股**（236/236 返回「数据为空」，
> 手工复探确认不是限流）⇒ **它自己带幸存者偏差且无法在本源内去偏**。
> ⇒ 「财务的修订历史」这条缺口**没有**因此关闭；关闭的只是「有没有当期 PIT 数据」。
> ⚠ 后果直接落在结论上：§5.62 唯一过门槛的 `B/P` **因此只能当上界**
> —— 高账面市值比常常是「便宜得有理由」，而真便宜到退市的那些根本不在池子里。

⚠ **行业分类那半句 2026-08-22 已经不成立，别照着旧文重新诊断一遍。**
原文写的是「`security.industry` 列在 schema 里但**没有任何 provider 填它**」——
[台账「行业分类」那一行](./信源台账.md)当天的更正是：**整条链路早就通了**
（东财 `f127` 在取 · `refreshProfiles()` 挂在休市维护 · 写库那句有 `COALESCE`），
根因是**降级链静默吃掉了这个字段**（eastmoney 间歇失败 → 腾讯成功但不提供 industry
⇒ 拿到「有名字没行业」的 profile 却报成功）。修法（**按字段重试**）已落在
`engine/watchlist.ts` 的 `profileOf()`，历史留痕表是 `014_industry_history.sql`。
**PIT 意义上仍然缺的是「历史」而不是「当前」**：数据源只给**当前**行业名，
拿它回标历史是未来函数 ⇒ 只能从接上的那天起**逐日攒**，每晚一天就永久少一天。

> ✅ **2026-08-27：上面那句「只能逐日攒」不再成立 —— 历史归属有零鉴权源了**
> （[M2 §5.65](./M2-偏差报告.md) · [台账 §4](./信源台账.md)）。
> **申万** `swsresearch.com/swindex/pdf/SwClass2021/StockClassifyUse_stock.xls`
> 实测 **HTTP 200 / 1.16 MB / OLE2 真 `.xls`**，每只股票**每次行业调整一行**
> （外部实测 12,893 行 / 5,905 只 / 38 个一级行业，有标的变更过 10 次）
> ⇒ 它是这一格的**第四条路，也是唯一一条既零鉴权又给历史归属的**
> （东财 `f127` 只有当前 · 巨潮/Tushare 要自己拼历史版本 · 聚宽 `get_history_industry`
> 直接给纳入剔除历史但**许可禁止入库分发**）。
> ⚠ 三个坑一起读：**本机 TLS 链校验失败**（外部实测正常 ⇒ 是本机 CA/代理问题不是站点问题）·
> **申万只发代码不发中文名**（东财/通达信的行业名不能套，体系不同）·
> **层级码要补足 6 位**（`48` → `480000`）。
> ✅ **2026-08-27 已落地：`pnpm fetch:industry`**（`scripts/fetch-industry.mjs`）。
> **零新增依赖** —— 自己写了 CFB + BIFF8 的窄解析器（这份表只用到
> `LABELSST` / `NUMBER` / `RK` 三种单元格记录、单个 Sheet）。
> 产出 `data/industry/sw-industry-history.json`（**gitignored，可重出**）。
> **实测 12,903 行 / 5,912 只 / 38 个一级行业 / 最多变更 10 次**
> —— 与外部工具包 08-19 报的 12,893 / 5,905 / 38 / 10 **独立复现**（差 10 行 = 八天的更新）。
> **对我们池子的覆盖（`--as-of` 自查，两个池都 100% 在表里）**：
>
> | as-of | 存活 264 可定位 | 一级行业数 | 退市 236 可定位 |
> |---|---|---|---|
> | 2018-01-01 | **183** | 28 | 232 |
> | 2023-12-31 | **254** | 30 | 236 |
>
> ⚠ 「表里有」与「该日可定位」的差是**那天还没上市**，不是抓取缺口。
> ⚠ **TLS 那条坑的修法要看清楚**：服务器**只发叶证书、漏了中间证书**
> ⇒ 修法是把 AIA 里那张（`GeoTrust G2 TLS CN RSA4096 SHA256 2022 CA1`，链到
> DigiCert Global Root G2，有效期至 2032）连同系统根一起传给 undici，
> 存在 `scripts/certs/`。**`NODE_TLS_REJECT_UNAUTHORIZED=0` 是错的修法** ——
> 它把这一次的「链不全」变成永久的「谁都不验」。
> ⚠ 这**不关闭**这一格 —— `014_industry_history.sql` 那条逐日留痕**照常继续**
> （两者口径不同：申万是自己的分类体系，东财 `f127` 是另一套；
> **不许把申万的历史回填进 `security.industry`**，那会让同一列混着两套体系）。

> ⚠ **2026-08-27 同一格里查出并修掉一个 PIT 缺陷，但只修了一半**（[M2 §5.66/§5.67](./M2-偏差报告.md)）。
> `openFixtureSource` 此前**不传名字**给 `fallbackProfile` ⇒ `isST` 恒 `false`
> ⇒ 全部 fixture 按 ±10% 算涨跌停，而 549 份里 **225 份**名称带 ST/退（**退市池 212/233 = 91%**）。
> 修后（读 `_meta.nameAtFetch`）退市腿收益 **−2.4028% → −2.1929%**（0.21pp），
> 而**幸存者偏差只从 −0.1322 动到 −0.1189pp**（0.0133pp，**小于 §5.26 自己两个估计间的 0.017pp**）
> ⇒ **−0.13pp 那条结论不变。**
> ⚠ **没修的那一半**：`nameAtFetch` 是**抓取时（2026-08）的名字**，不是逐日状态
> ⇒ 「用今天的 ST 状态回标 8 年」仍是时点错误（方向不定，与「行业只有当前归属」同一形状）。

<!-- ITEM id=pit-isst-daily | 类=落地 | 桶=等条件 | 前置=有一个装得上 pandas 的 Python（3.11/3.12），或找到 HTTP 形态的逐日 ST 源 | 代价=中 | 来源=M2 §5.67 | 判据=src/backtest/data.ts:逐日 isST | 标题=isST 用的是抓取时的名字（2026-08）不是逐日状态，「用今天的 ST 状态回标 8 年」仍是时点错误 -->

---

## 3. 数据维度的差距

现有五层引擎**全部建立在价量之上，而价量是所有人都有的**。

| 维度 | 现状 | 备注 |
|---|---|---|
| 价量日线 | 有（三源 + 后复权两轨） | |
| 分钟/tick | **没有** | 这是 `UNTESTABLE` 那 11 个参数（`tTrade` 3 个、`lateBuyCutoffMinutes`…）**日线原理上测不到**的根因 |
| 公告 | 有标题，**无正文** | [§5.25](./M2-偏差报告.md) 三轮实测：公告作为二值筛子对入场质量**零增量**，且建仓在公告日上零富集（13.5% vs 14.4%） |
| 财务 / 一致预期 | 没有 | 一致预期修正是日线频率、与价量正交、学术证据最强的一类；免费拿不到 |
| Level-2 / 资金流 | 没有 | **不建议补**：alpha 半衰期以分钟计，与「日线判定 + 不打断工作」的形态不匹配 |
| 融资融券 / 龙虎榜 | 没有 | |

> **补任何新维度之前先做必要条件检验**，模板是 [§5.25](./M2-偏差报告.md)：
> 用 `audit:random` 测「它对**现有**建仓的入场质量有没有增量」。
> 公告那一轮花了 30 分钟抓 25 万条，结论是负的 —— 那个否定结论本身很值钱。

---

## 4. 参考资料（可持续补充）

**方法**
- López de Prado, *Advances in Financial Machine Learning* (2018) — meta-labeling（第 3 章）、PBO、CPCV。⚠ 全书有实证方面的争议，别照单全收；上述几个统计量本身是稳的
- Bailey & López de Prado, *The Deflated Sharpe Ratio* (2014)
- Harvey, Liu & Zhu, *…and the Cross-Section of Expected Returns* (2016) — t ≥ 3 门槛；读法见 §2.3
- Liu, Stambaugh & Yuan, *Size and Value in China* (JFE 2019) — 中国版三因子（CH-3）。**最实操的一条：剔除市值最小的 30%**，因为 A 股小市值涨跌大量由壳价值驱动。本项目 261 只是按代码段分层等距抽的，必然含大量小市值票 —— 可用现有建仓按入场市值分位分层自检

**工具（参考口径，不换栈）**
- **Alphalens** — 因子分析的**输出形态**值得抄：分位数收益单调性、IC 衰减曲线、换手率。项目是 TS，不引入这个库，但 `audit:*` 可以照它的口径出表
- **Qlib**（微软，A 股为主）— 值得看它的数据层设计（point-in-time、表达式引擎）。**不要换栈**：`src/core` 的纯函数约束是本项目最值钱的架构决定
- **BaoStock `query_all_stock(day=…)`** — 免费的**时点**证券列表（用历史某日查，能拿到当时在交易、后来退市的代码）。本项目最终没用它（巨潮 `szse_stock.json` 就够），但它是另一条路

---

### 2.8 ✅ **rank IC —— 2026-08-20 补上了，而且它是目前唯一不依赖零分布的判据**

机构做横截面因子检验的标准口径（rank IC / IC 衰减 / 分层单调性），我们此前完全没有。
补它的直接动机是 [M2 §5.43](./M2-偏差报告.md)/[§5.45](./M2-偏差报告.md) 量出配对胜率有
**两个**控制不住的自由度（零点定义 20–30pp、加权/中位 28pp）——
而 rank IC 是描述性统计，那两个自由度都不适用于它。

工具 `pnpm audit:ic`。**首测结论是负的**：买入得分与前瞻收益的横截面秩相关
**训练窗口 ≈ −4.4% ~ −5.0%、验证窗口 −0.9% ~ −3.2%（符号一致、强度差 2–5 倍）**，
五等分近单调 ⇒ **得分越高、未来收益越低**。详见 [§5.46](./M2-偏差报告.md)，含三条使用限制
（t 是上界 · 分位水平含市场共同项 · 不许推「反着做能赚」）。

---

## 5. 明确不值得追的

- **深度学习 / 高容量 ML**：1675 次建仓，任何高容量模型都是过拟合机器
- **高频与微观结构**：与产品形态不匹配（见 §3 Level-2 那行）
- **全市场实时扫描**：[P2 评估](./P2-全市场扫描评估.md) 的三条卡点是架构问题，硬件买不掉；
  且它的产品出口不该是提醒（全局配额下 5400 只会让「今天提醒了哪几只」变成随机采样）
- **把权重表加回来 / 把 `enabledStrategies` 当标定维度**：都被测过，见 CLAUDE.md

---

## 6. 一句话版本

差距**不在**能不能算、能不能验，而在于：
机构做的是「**相对排序 + 因子归因 + 可承载资金**」，本项目做的是「**绝对择时 + 策略内归因 + 提醒出口**」。
前者的每一环都在系统性提高信噪比；后者的每一环都得自己造零点、自己剥 beta、自己防过拟合 ——
而这三件事项目**都做了，且做得相当干净**。

**真正的差距是形态选择带来的信噪比劣势，不是执行水平。**

---

## 7. 优先级（下一轮迭代的候选）

| 序 | 事项 | 出处 | 代价 |
|---|---|---|---|
| 1 | ~~**池外参照**（等资金占用的被动持有）~~ ✅ **2026-08-22 补上**（M2 §5.52）：**GH1** 训练 **−1.71pp** / 验证 **−0.37pp**，两窗同向为负，且**与现有的除法版超额符号相反**；**GH2/M² 判不能用**（要 20–24× 杠杆 · 符号由 rf 支配 · 恒等于夏普之差）。进报告前要拍匹配口径 ⇒ 已登记 | §2.2 | 小；一个 audit 工具 |
| 2 | ~~**短期反转暴露自检**~~ ✅ **2026-08-20 已做**（M2 §5.45：倒 U，追高那档中位配对 0.5%） | §2.1 | 小；只读分层 |
| 3 | ~~**小市值 / 壳价值自检**~~ ✅ **2026-08-20 已做**（M2 §5.45：无可辨认暴露，推论被证伪） | §4 CH-3 | 小；只读分层 |
| 4b | ~~**两条相关曲线的夏普之差**（同期两腿，不是子段）~~ ✅ **2026-08-26**（[M2 §5.60](./M2-偏差报告.md)）：Ledoit & Wolf (2008) 的 `∇f′Ψ∇f` 进 `metrics.ts`，**朴素合成 SE 高估 1.35–3.5 倍**。顺带证伪两条：网上流传的两种 Memmel 闭式**都错**（一种小到看不见、一种差 11.4 倍）· **beta 小 ≠ ρ 小**。**不进写回门槛**；循环块自举已登记 | §2.4 | 小；纯统计，只读 |
| 4 | ~~Newey-West 调整标准误~~ ✅ **结案（2026-08-21）**。rank IC 那一处 08-20 做（§5.47）· 折间 CR1 与择时块位移 08-19/08-20 做（§4.6 结案）· **夏普方差 08-21 做**（§5.50，Lo 2002 的 `V_GMM`；顺带判 `η(q)` 在日频**不能用**）。夏普的**点估计**仍不动、仍不参与门槛 | §2.4 | 中；纯统计 |
| 5 | **执行滑移** —— ⚠ **2026-08-22 取了口径（Perold 1988）后发现这一行的提法本身是错的**（M2 §5.53）：`ledger × 影子` 的差主项是 **`C_d`（延迟）且符号是反的**（人比纸面组合**早** —— 影子只吃 CONFIRMED + 次日开盘成交）⇒ 那是**前向纪律的代价不是执行质量**。实测重叠标的差 ≈ **41 bp**，拆开是 10 bp 设计滑点 + **31 bp 一天的时间差**（`\|C_d\|/\|C_i\| ≈ 3.1×`，n=1）。⇒ 改成「**先分解，再谈执行**」；而对 L0 这个形态 **`C_o`（没照提醒执行的）比 `C_i` 重要得多**。缺四样输入，已登记 | §1.3 | 中；基建**只有一半** |
| 6 | ~~PBO / DSR~~ ✅ **2026-08-20**：**DSR 做了**（M2 §5.48，门槛年化夏普 ≈ 1.0，**不进写回门槛**）· **PBO 刻意不做**（输入不存在 + 算出来承不了重）⇒ 这一行**结案** | §2.3 | 中 |

| **8** | ~~**成本模型的凸性**～「常数比例滑点在多大资金上失效」~~ ✅ **2026-08-24 做了**（[M2 §5.54](./M2-偏差报告.md)）：平方根冲击律套在 1097 次建仓上，`Y=1` 下**中位 7.33 bp / 27.6% 超线**、`k*` 中位 **1.9×** ⇒ **§2.6 那句「影响有限」被推翻**；顺带查出**候选 B 与成本模型耦合**。已登记。**同日追加**（[§5.55](./M2-偏差报告.md)）：去看外部平台怎么做，发现**聚宽根本没有凸成本模型**（靠 `order_volume_ratio` 成交量硬上限，默认 1.0 = 不设限），而那个形态**替代不了** —— 两者 Jaccard 仅 **0.176**（凸成本抓波动 / 硬上限抓流动性）、门槛差 **10–30 倍** ⇒ **没有廉价替代路线，那条条目必须真做** | §2.6 | 小；只读既有报告 |

| **7** | ~~**MinTRL**～「ETF 前向记录要攒多长才算数」~~ ✅ **2026-08-20 当天就做了**（[M2 §5.49](./M2-偏差报告.md)）：**年数 ≈ 2.71/夏普²** ⇒ 夏普 0.5 要 **11 年**、0.3 要 **30 年**，再 ×1.3。⇒ **「等前向记录变显著」不是一个可执行的计划** —— 这是一道**拍板题**，不是结论 | §2.3 同族（Bailey & LdP，*Sharpe Ratio Efficient Frontier*, J. Risk 15(2)） | 小；纯统计，只读 |

<!-- ITEM id=sharpe-diff-bootstrap | 类=落地 | 桶=等条件 | 前置=出现一次「要承重」的夏普之差比较，且它的 p 落在 0.01–0.10 之间（现有三个配对 p = 0.12 / 0.21 / 0.43，都不在这个区间） | 代价=中 | 来源=M2 §5.60 | 判据=src/backtest/metrics.ts:sharpeDiffBootstrap | 标题=LW 原文推荐的是循环块自举，我们用的是 Bartlett HAC，而原文自己说 HAC 在中小样本上偏自由 -->

- **`sharpeDiffHac` 用的是 Bartlett 核 + Andrews 滞后阶，不是 LW 原文推荐的
  预白化 QS 核 + 循环块自举**（Politis & Romano 1992 的 circular block bootstrap）。
  原文明说 **HAC 推断在中小样本上偏自由**（拒真过多、置信区间覆盖不足）。
  ⚠ **现在不做**：三个配对的 `p` 是 0.12 / 0.21 / 0.43，离 0.05 都很远
  ⇒ 换一个更严的核不会改变任何一条结论，而自举要选块长（**又一个自由参数**）。
  等到真有一次比较的 `p` 落在 0.01–0.10 且要拿它承重时再做。

| **9** | **退市股的后复权口径要第三个源** —— 2026-08-27 第一次拿新浪复权因子核腾讯 `hfqday`（[M2 §5.65](./M2-偏差报告.md)，工具 `scripts/verify/adjust-crosscheck.ts`）：**存活股两家高度一致**（373 次除权事件逐次收益差中位 **0.067pp**）⇒ 回测输入拿到外部背书；**但退市股全窗累计漂移中位 38.12%**（存活组 2.39%，**差 16 倍**，分歧 top-10 **10/10 是退市股**，抽查水平差达 **11×**）。**两家都是非官方源 ⇒ 现有证据判不出谁对**。⇒ 幸存者偏差 **−0.13pp**（§5.26）从「已解决」降为「**有一条未量化的口径风险**」—— **不推翻**（分歧方向不定、配对随机化对标的自身 beta 不敏感），但引用时要带这句 | §2.7 / M2 §5.26 | 中；要第三个源（同花顺日线备胎已探通，复权口径未测） |

<!-- ITEM id=delisted-adjust-third-source | 类=待测 | 桶=就绪 | 代价=中 | 来源=M2 §5.65 | 判据=docs/notes/M2-偏差报告.md:退市股复权第三源 | 标题=退市股的后复权腾讯与新浪分歧中位 38%（存活股只有 2.4%），两家都是非官方源判不出谁对，而幸存者偏差 −0.13pp 依赖的正是这一列 -->

**1 与 2 都是只读分层、不碰交易逻辑、不新增指标**（不触约束 5），是性价比最高的两个。

> **⚠ 7 当天就做了，而它的答案改变了 1 与 5 的优先级。** 用户 2026-08-17 把 ETF 的判据
> 换成「前向记录攒够时间」，MinTRL 现在说「够」= **夏普 0.5 要 11 年 / 0.3 要 30 年**（再 ×1.3）
> ⇒ **那条判据在现有效应量上不可执行**。这**不是**「ETF 路线被否」（见 §2.3 的三条读法），
> 而是把一道**拍板题**摆到了用户面前 —— 见[迭代计划 §4.11](./下一阶段取舍与迭代计划.md)。
> ⇒ 剩下的 1（池外参照）与 5（执行滑移）都还在，但**它们回答不了这道题**。

---

## 8. 待验证（**没有本地证据，不许当结论引用**）

- ~~「A 股短期是反转、T3 是动量暴露的错误一侧」~~ —— **2026-08-20 测了，一半被证伪**
  （[M2 §5.45](./M2-偏差报告.md)，与下面那条小市值同一轮）：按入场前 5 日涨幅分位五等分，
  **「追高那一档」（n=550）中位配对胜率 0.5%** ⇒ 「T3 买在高动量上很差」**成立**，
  但形状是**倒 U 不是单调递减** ⇒ 机制是「**极端动量不利于买入**」而不是「反转」。
  ⚠ 两半必须一起读：拿它去说「A 股短期是反转」是把被证伪的那一半当结论。
  §2.1 的划线记的是同一件事（那里的日期 08-22 是笔误，M2 §5.45 是 **08-20**）。
- ~~「261 只里的小市值票靠壳价值驱动、污染了结论」~~ —— **2026-08-20 测了，被证伪**
  （[M2 §5.45](./M2-偏差报告.md)）：1675 次建仓按**入场时流通市值的当日横截面分位**五等分，
  中位口径配对胜率 24.3 / 49.2 / 7.7 / 47.3 / 4.0 —— **非单调、无可辨认暴露**。
  覆盖率满格（250/250 只 · 1675/1675 次建仓）。⇒ 这条推论结案。
  同一轮把 §7 优先级 2「短期反转暴露自检」也做了：**反转与动量两个假设双双被证伪**，
  实际形状是倒 U，而「前 5 日涨幅最高那一档」（n=550）中位配对胜率 **0.5%**。
- **「筹码分布（CYQ）有价量之外的信息」** —— 2026-08-27 从外部工具包
  [`a-stock-data`](https://github.com/simonlin1212/a-stock-data) 记下的假设
  （[M2 §5.65](./M2-偏差报告.md)）。它的诱人之处恰恰是**便宜**：纯本地推演，
  输入只要 OHLC + **换手率**，而换手率我们 2026-08-17 就整段拿到了（东财 `f61`）
  ⇒ **零新增数据源**。输出是获利比例 / 平均成本 / 90-70 成本区间与集中度 / 筹码峰。
  ⚠ **三条一起读，少一条就会把它做成又一个未论证指标**：
  ① **它不是新维度** —— 全部由价量推演，§3 那张表里「价量日线」那一格已经有了，
  而 IC 那一轮（§5.46）测出来引擎的价量得分是**负向**因子；
  ② 外部实现自己标了一个硬缺陷：**初始筹码播种为首日全部流通盘**，
  从零起步会把窗口前的存量持仓一笔勾销（「两个 1% 换手日会被算成 50/50，真实应约 99%/1%」）
  ⇒ 预热长度是个**自由参数**，而本项目已经有过「自由参数被调到好看为止」的形状；
  ③ **约束 5**（不新增未在文档中论证的指标）+ 2026-08-19 用户拍板**停止在个股·日线·择时找边缘**。
  ⇒ **不做**。要重开必须先有论证、先走 `docs/07 §3.6`，而**「复用成本低」不是理由**
  （同「公告不做 AI 解读」那条）。
- 「跨票 74.5% 反映的是选股能力」—— [§5.27](./M2-偏差报告.md) 已列出替代解释
  （可能只是「选了活跃的票」），且**只报了一次分位、未做跨种子检验**

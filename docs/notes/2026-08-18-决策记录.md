# 2026-08-18 决策记录

> **这一天做了四类事**（`ENGINE_VERSION` **全程不变**，62 个叶子的标定归档**不变**，
> 引擎参数与判定逻辑**一行没动**）：
>
> | 类 | 内容 | 落点 |
> |---|---|---|
> | **产品面** | 取消「行业 ETF 不设持仓」+ 三处连带待遇 · 分屏按板块 · 日报滚到当前交易日并逐节标数据时刻 | §一–§六 |
> | **缺陷** | 影子补跑把「跳过」报成「已推进」（**日志说了假话**）· 跑马灯收窄从未生效 · 日报把「昨天的价 + 今天的信号」标成已定稿 | §三 · §五 · §七 + [CHANGELOG](../../CHANGELOG.md) |
> | **实验** | §3.11 横截面：当天立、当天跑完 S0 与 S1、**当天判死** | [M2 §5.33–§5.35](./M2-偏差报告.md) |
> | **判据与经验** | 相关性调整（t 是上界）· 归档缺口（子信号权重不在 62 叶子里）· **三个被自己推翻的判断** | [迭代计划 §4.5b/§4.6](./下一阶段取舍与迭代计划.md) · §八 · §九 |
>
> **读的顺序建议**：只想知道「今天改了什么」看 §一–§七；
> 想知道「今天学到什么」直接跳 **§八 与 §九** —— 那两节是这份记录里唯一可迁移的部分。
>
> 前一天的实验线见 [M2 §5.32](./M2-偏差报告.md)（ETF 择时 ≠ beta 择时 + 池外参照）。

---

## 一、用户拍的三条

| # | 决定 | 原来是什么 |
|---|---|---|
| 1 | **行业 ETF 可以设持仓**，面板给「仓」入口 | 2026-08-15 起整组不给持仓入口 |
| 2 | **所有 ETF 归到同一屏**，两屏分「个股 / ETF」；添加框做在 tab 那一行、两屏共用；添加后自动切到对应屏并滚到新行 | 两屏按 `WatchItem.group` 分（自选 / 行业ETF），添加框只在个股屏 |
| 3 | **悬浮条跑马灯只跑「有持仓的 + 今天出了未静默信号的」** | 名义上是「全部自选，排除行业ETF 组」 |

第 1 条另外附带两个由我提出、用户确认的连带项（见下一节）：
**有持仓即转 `PRIMARY` 轨**、**有持仓即进日报**。
内置那 15 只**仍不给「移除」按钮**（删了下次启动会被 `data-layer` 补回来，
那是一个点了会复活的按钮）；用户自己加的 ETF 照常可删。

---

## 二、为什么「有持仓」必须翻回 `PRIMARY`（这一条是安全项，不是体验项）

2026-08-17 的双轨设计里，`OBSERVE` 轨有两条性质，都是**为观察名单量身做的**：

1. 日配额只有 `observeDailyLimit`（默认 **2** 条）；
2. **抢不到气泡** —— 一次只弹一个，挑的时候先按轨道再按得分。

这两条用在没有持仓的行业 ETF 上是对的。但持仓一旦存在，
止损 / 回撤减仓那套**持仓强制类 L3** 就会对它生效，而留在 OBSERVE 轨的后果是：
跌破止损那一条可能被 2 条日配额吃掉、也可能被任何一条 PRIMARY 抢走气泡 —— **一声不响**。

这是本项目反复踩过的形状（08-17 那次「免打扰吃掉止损台阶」是同一个）：
**少发的错误用户发现不了**。所以判据从「分组」改成「有没有持仓」，
落在 `src/main/alerts/track.ts`（纯函数 + 用例）。

**代价说清楚**：08-17 记着「`PRIMARY` 行为逐位不变，这是刻意的：
M3 正在攒的提醒日志证据不能被干扰」—— **这条从今天起不再成立**。
接受它的理由是：混进来的那些行对应**真持仓**，本来就属于「该不该被打断」这个问题的样本，
而且按 code 就能拆开。

同理日报：日报答的是「我这些票今天怎么样」，一只压着真金白银的 ETF 不在里面是漏不是克制
（`report/build.ts` 的 `reportableItems`，有用例）。
**「今日环境」那一节仍列全部 15 只**，含已被收进个股几节的那几只 —— 重复是刻意的，
两节答的是两个问题，缺了你恰好持有的那个行业，行业全景就是残的。

---

## 三、顺带修掉的一处静默失效

**「跑马灯不含行业ETF」这条从 2026-08-15 起就没有生效过。**

`bar/App.tsx` 里那行过滤只作用于 `buildTicker` 的 `items` 入参，
而 `buildTicker` 的主集合是 `items ∪ quotes ∪ 今日有信号的`（`ticker.ts`），
`push:quoteTick` 推的又是**全部**自选 —— 于是 15 只行业 ETF 一直照常在条子上滚，
而代码与文档都写着它们不在。

新的收窄因此做成 `visibleTicker(entries, held)`，**作用在产物上**，
并且用例里那条「只从 quotes 进来的标的也要被滤掉」就是专门用来让旧写法过不了的。

空态也跟着多了一档：有自选、有报价、但既无持仓也无今日信号 —— 那是**常态**，
显示成「等待行情…」等于报一个不存在的故障，现在写「无持仓 · 今日无信号」。

---

## 四、分屏判据换成板块（不是分组）

`shared/watch-split.ts`：`board === 'ETF'`（`src/core/code.ts` 的 SH 51/56/58 + SZ 15/16，
即全部场内基金，与成本模型的 `isFundBoard` 同一口径）。

**从代码段推，不读 `watchlist.board`** —— 与 `controller.boardOf` 同一条理由：
后者是 provider 抓来的画像，可能缺（缺失时仓储层回退成 `MAIN`，
那会让一只 ETF 静默地跑到个股屏去）。

两屏现在逐字同规则：**持仓优先、段内保持用户排的顺序、上移下移按段禁用**。
最后一条是 2026-08-16 踩出来的：跨段交换之后显示顺序一点变化都没有，表现是「点了没反应」。

「行业ETF」这个分组名因此**只剩三个用途**：不可移除、无持仓时走 OBSERVE 轨、无持仓时不进日报。

---

## 五、日报：滚到当前交易日 + 每节标数据时刻（当天下午追加）

用户报的现象：北京 15:08 打开日报，看到的是 **08-17 的「已定稿」**。

### 根因是两件事叠在一起

1. **日期取错了口径。** 旧判据是「库里最后一根日线的日期」。当日日线数据源
   15:05–15:30 才发布，而应用 15:10（`SESSION_BOUNDS.settleEnd`）之后 `needsQuotes`
   变 false 不再取数 ⇒ 今天那根要到**次日盘前**才入库 ⇒ 整个今天（**含盘中**）
   日报都停在昨天，`stage` 还算出 `FINAL`。当天实测：15:05 那一轮真的去补了，
   但股票日线还没发布、`SH000300` 与几只 ETF 两个源都失败（腾讯对指数/基金返回
   里没有 `qfqday`，eastmoney `other side closed`），所以 08-18 一根日线都没入库。
2. **同一屏的另一半按「今天」切。** 信号 / 提醒 / 观察点统计用的是
   `dayStart = 今天北京 00:00` ⇒ **一份报告里混着「昨天的价 + 今天的信号」**，
   而界面上完全看不出来 —— 这正是「两个数据源不许混」那条纪律的更高一层。

### 拍板三条

| # | 决定 | 为什么不选另一个 |
|---|---|---|
| 1 | **只让日报滚到今天**（`reportSubjectDate`：开盘 09:30 之后就是今天） | 另一个选项是把 SETTLE 取数窗口从 15:10 延到 15:40，让今天的日线当天入库、当晚就能「已定稿」。它会连带改变**收盘确认轮的时机**（`CONFIRMED` 当天就出）与**影子运行的接线** —— 那是另一件事的风险，单独排期 |
| 2 | 栏目时间按**数据时刻**（这一节的数字实际来自何时） | 「重算时刻」每节相同、且会说谎：「今日提醒」可能从早上 09:03 起就没变过 |
| 3 | 页头加**刷新按钮 + 生成时刻** | 不加定时轮询 —— 与「让一份收盘总结每 30 秒跳数字既没必要也让人分心」相反 |

### 连带的三处

- **badge 文案** `PROVISIONAL` 从「盘中数据」改成「**未定稿**」：新口径下它在 16:00
  也会出现，那时「盘中」是错的。具体几点由旁边的数据时刻回答。
  同理两句陈述里的「收盘后可能微调」改成「**次日盘前定稿后**可能微调」。
- **时刻一律北京时间。** 面板上六处（提醒日志 / 信号列表 / 观察点 / 盘前简报 /
  AI 历史 / 气泡）原先各写一份 `getHours()` 格式化 = **宿主本地时区**，
  本机（UTC+7）上北京 09:03 的提醒写成 08:03。日报新加的「今日提醒 09:03」
  会与提醒日志里同一条对不上，所以一起收进 `shared/time.ts`
  （`shanghaiHhmm` / `shanghaiHhmmss` / `shanghaiMdHhmm` / `shanghaiDate`）。
- **时刻一个都没进 `reportFactDigest`。** 它每 30 秒就变，进指纹会让
  「这段 AI 评价已过期」恒亮 —— `report-ai.test.ts` 加了两条钉住。

### 代价

日报当晚仍到不了「已定稿」（今天的日线次日盘前才入库），只是从今天起它至少
**报的是今天、并且说清了自己是几点的数据**。盘中生成的 AI 评价会随价格变动很快
显示「已过期」—— 那是 `digest.ts` 设计好的行为，不是回归。

---

## 六、当天产生的两个候选（一个当天结案、一个未拍板）

> **§3.11 横截面**（下面这一段的产物）：当天立、当天跑完 S0 与 S1、**当天判死**。
> 结论见 [M2 §5.33–§5.35](./M2-偏差报告.md)：`audit:random --cross-pool` 收窄对照组之后，
> ALL 配对胜率 **55.0%**，压在预注册判死线上。连带两条：
> §5.27 的「选股 74.5%」不能再单独引用；气泡「按得分抢占」的**前提被否一次**（docs/05 §4.3 已标注）。
>
> **§3.12 外部环境**（当天傍晚用户提的方向性问题）：策略是否各有适配场景、
> 是否该朝「细分场景 → 适配不同策略」演进。我的判断是**不朝那走**
> （场景细分是放大器不是来源；`params.weights` 已测过同构机制并删除；
> 统计套利/中性/指数增强需要做空、组合层与 L3，是另一个产品），
> 但「我们的场景定义差」为真（`TRANSITION` 占 66.6%）。
> 于是写了一条**窄口子**候选：只测「大盘 regime 带不带增量信息」，不做策略切换。
> **行业版本当场判 `BLOCKED`** —— `industry` 在真机库（79/79 空）、回测 fixture、
> 15 只行业 ETF 的 fixture（只有 9 只）三处都不可用。论证与预注册在 §3.12，**代码一行没写**。

用户让我评一个开源量化框架（FinceptTerminal）并与本项目的因子/策略做对比。
**那次对比的结论对本项目只有一个可执行的落点**，记在这里是为了让 §3.11 有出处：

- 它的量化「核心算法」不是自研的 —— 是 Qlib / RD-Agent / QuantLib / VectorBT / LEAN 的
  集成层（`fincept-qt/scripts` 的清单 + `PythonRunner` 桥）。所以真正的对手是 **Qlib 范式**：
  横截面 ML alpha vs 我们的单标的时序规则。
- 它那 37 个投资人 persona agent 与 Alpha Arena 类 LLM 交易**属于本项目已判否的方向**
  （M2 §5.25 三轮实测 + docs/11 N4）。Alpha Arena 第一季 4/6 亏损、主办方归因「过度交易 +
  手续费吃掉小额获利」，与我们实测「成本占负期望 69%」是同一现象的两个独立观测。
- **AGPL-3.0 是硬约束**：它的代码进不了要分发的 `src/`，只能当离线研究工具。
  架构上也进不来（ADR-0004：`src/core` 纯函数）。
- ⇒ 唯一值得做的是**我们自己那条早就写好的待办**：横截面排序，见
  [§3.11](./下一阶段取舍与迭代计划.md)。论证（含「可能错在哪」六条与事前预测）按
  [docs/07 §3.6 ①](../07-回测与验证方案.md) 写在代码之前 ——
  **当天随后就跑完并判死了**（上面那个框）。次序没有被绕过：论证 → 预注册 → 跑 → 归档。

⚠ 它**部分重开了** §3.10 那条「因子化改造不做」。重开的依据不是外部框架，
而是本仓库的一行代码：`alerts/dispatcher.ts:270` 按得分降序发配额与气泡 ——
**产品每天都在做横截面排序，而那个排序从未被验证过**。整套因子化改造仍然不做。

---

## 七、影子运行：那句「已推进影子运行」是假的（当天最早的一件事）

时序上这件在 §五 之前 —— 用户当天第一句话是「今天影子运行还是没有任何操作」。

**查出来的链**：08-17 08:02:59（修复前的代码，每轮 tick 都推进）在**盘前**用空 outcomes
写下了 08-17 的净值行 ⇒ 08-18 08:04 的补跑一进 `shadow.advance('2026-08-17')` 就撞上
`shadow_equity.trade_date` 主键的幂等闸门、`ALREADY_DONE` 返回 **null** ⇒
**第 ⑥ 步「挂明天的委托」根本没跑**；而 `settleDay()` 把「没抛错」读成「已推进」，
日志照样写「**已推进影子运行**」。

**修法与代价见 [CHANGELOG](../../CHANGELOG.md) 那一条**（`ENGINE_VERSION` 不变）。
这里只留三句结论：

1. **08-17 那天的影子记录永久缺失**（影子不补跑历史），**没有做任何数据补写** ——
   当天 CONFIRMED 只有 3 只 `REDUCE` 而影子无持仓，本来也挂不出委托，经济内容为零；
2. **这类缺陷最贵的地方不是少做，是让「少做」不可见** —— 与 08-17
   「台阶被降级成 L1 的提醒消耗掉」同一形状（少发的错误用户发现不了）；
3. 当天实测的现状：`shadow_equity` **1 行**、`shadow_order` / `shadow_position` /
   `shadow_trade` **全 0**；`SH000300` 的 `benchmark` 为 null（那行是盘前写的，当日收盘线还不存在）。
   **第一张委托最快出现在 08-19 盘前**（若 08-18 那 3 只 `NEXT_DAY_WATCH` 被收盘轮确认），
   而它的**成交要到 08-20 盘前那一跳**才落账。

---

## 八、⚠ 当天被自己推翻的三个判断（这一节最该被读）

**三次都是我先写下一个判断、当天又被实测推翻。** 记下来不是为了自责，
是因为它们的**形状一样**，而那个形状可以预防。

| # | 我写下的判断 | 被什么推翻 | 间隔 |
|---|---|---|---|
| 1 | §3.11 的 S0 判死条件：「同日多信号的**天数占比** < 20% **或中位数只有 1 只** ⇒ 判死」 | 我自己的测量。中位数 = 1 ⇒ 字面判死；但**我数的是「建仓」（代理量），而预注册的量是「买入方向信号」**。换成信号级：中位 **2**、占比 **30.7%–57.0%**（三个分母都过）⇒ 通过 | ~1 小时 |
| 2 | 「JQData **一次性离线拉一张表落 `params/*.json` 可以**，已有两次先例（巨潮退市名单 / `fetch:liquidity`）」 | 登录官网读**协议原文**：**5.1** 只做研究目的、不用作商业用途；**3.2** 不得提供给第三方；**4.1** 加工后数据产权归公司 ⇒ 落进仓库并随 `pnpm package` 分发 = 违约 | ~1 小时 |
| 3 | §2.3 的现状「换手率**可能根本拿不到**，eastmoney 还没探通」（08-15 写的，我当天还照着它往下推理） | **同一个仓库里的产物**：`params/float-shares.json` 的 `_meta.source` = 「eastmoney kline f56/**f61**（…反推）」，`fetch-liquidity.mjs` 的 `IDX = { …, turnover: 10 }` —— **早就取通并用过** | 一次 grep |

### 共同形状：**我在没看原始证据的情况下复述了一个二手结论**

三个二手来源分别是：**我自己一小时前写的判据措辞**（#1）· **对先例的类比**（#2）·
**一份三天前的文档**（#3）。

### 由此得到的三条可执行规则

1. **判据里的每个量都要指名道姓** —— 数的是什么、分母是什么。
   #1 里「天数占比」有两个合法分母（19.2% / 48.2%），一个过一个不过；
   而「中位数」对偏态分布是错的统计量（均值 2.01、最大 12、74.3% 的建仓有同日同伴）。
2. **先例不能跨许可类比。** 巨潮公告与公开行情接口没有使用条款，商业数据服务有。
   写「可以」之前要读条款原文，而不是类比形状。
3. **动手前先 grep 自己的仓库。** 跨文档的「现状」记录会过期，**产物不会说谎**
   （`_meta.source` 那一行比三份文档都可靠）。

---

## 九、可迁移的方法论经验（当天新增或被再次确认）

1. **看过数据之后不许改判据 —— 但可以去测那个真正被预注册的量。**
   这是唯一合法的出口，而且必须由用户拍板（当天就是这么走的）。
   我自己发现判据写坏时**没有权限放宽它**，只能把选项摊开。
2. **边界值不重跑。** S1 的 ALL = **55.00%**（110/200）恰好压在「≤ 55% 判死」上。
   差一次试验就换种子，正是「每个候选机制只报一次分位」在防的事。
3. **分层是必报项，但不能拿来救候选。** S1 的 `TREND_UP` 99.5% 很好看（n=154），
   而占 88% 样本的 `RANGE` 35.5% / `TRANSITION` 26.0% 低于 50% ——
   **ALL 是混合值不是效应**；TREND_UP 那条只能是新候选。
4. **负面结果的价值是把断言升级为结论。** §3.10 那条「因子化改造不做」原来是判断，
   现在有数据（收窄对照组后从 74.5% 掉到 55.0%）。
5. **报告缺陷也是缺陷。** `calibrate` 打印 `|Δ|/stderr` 却没有一行说它**未做相关性调整**
   ⇒ 报告在替一个偏乐观的数担保（§4.6）。
6. **判据升级要在「门口没有候选」时做。** 否则就是看见 `t = 2.3` 才想起要不要做
   Newey-West —— 与 #1 那个形状一模一样。
7. **工具失败 ≠ 目标不可达。** WebFetch 对所有域名失败（它的校验要摸不可达的 `claude.ai`），
   而 curl 同一时刻 200。我当时把它读成「站点被拦」，因此少读了一整份源码。
8. **许可与架构是硬约束，不是「顺手复用」的成本项。** AGPL-3.0（fincept）· JQData 协议 5.1/3.2 ·
   ADR-0004（`src/core` 纯函数）—— 三条各自否掉了一条看起来很省事的路。
9. **「日志说了假话」是最贵的一类缺陷。** 当天两个实例：影子的 `ALREADY_DONE` 被报成已推进（§七）、
   日报把「昨天的价 + 今天的信号」标成「已定稿」（§五）。**它们让缺陷不可见。**
10. **代理量与目标量差一个身位就能翻结论**（#1）：建仓 = 信号 − 硬抑制 − 涨停买不到 −
    买不起一手 − 顺延，而那四道**都不影响「横截面上有几个候选」**。

---

## 十、迭代四问（收盘后跑 `pnpm iterate` 之后的答案）

### ① 现在在阶梯的哪一级，卡住升级的是哪一条

**L0。卡 L2 的第 ① 条** —— 看板读数：训练窗口（`recheck-after-idx.json`，
**2018-01-01 → 2023-12-31**，252 只）区间收益 **−1.99%** · 建仓级胜率 43.21% · 夏普 −0.412。
**红线仍然过不去，今天没有任何改动指向它** —— §3.11 走完并判死，那是**排除**一条路，不是转正。

L2 第 ② 条（`GUESS` + `BLOCKED` 清零）今天也没动：**1 / 16 / 13 / 11 / 21 / 0** 一格未变。
但今天查出一个**新的、不在那 62 个叶子里**的归档缺口（子信号权重五个常量，§4.5b）——
它不改变计数，却让「62 个叶子」这个**分母本身不完整**。

**L1（成交记账闭环）**：基建就绪、仍未用起来。影子净值点 **1**，
第一张委托最快 08-19 盘前（今天 3 只 `NEXT_DAY_WATCH` 若被收盘轮确认）。

### ② 手上有什么资源没用起来

**今天新增三件**（都已进仓库）：`pnpm audit:crosssec`（横截面计数，自检「信号 ⊇ 建仓」）·
`audit:random --cross-pool`（收窄候选池）· `pnpm fetch:page`（取页，WebFetch 在本机永久坏）。

**今天盘点出来、还没用起来的老资源** —— 三条里有两条是「我们以为没有，其实有」：

| 资源 | 状态 |
|---|---|
| **东财 `f127` 行业名** | **快照路径已经在取，只差落库**（信源台账 §4 早就记着）⇒ §3.12 的行业版本**可能根本不需要 JQData**。代价：它是**当前**分类，回标历史是未来函数 |
| **eastmoney `f61` 逐日换手率** | 联网路径本来就取，只是跑的是离线展开路径 ⇒ **重跑一次就有** ⇒ §3.8 的离线实验可解锁 |
| **记账 × 影子 = 执行滑移** | 仍未用起来（这是 L1 的全部内容） |
| **池外参照** | 仍是真空。今天顺带确认聚宽有「历史任意时刻的指数成分股」而**我们没有** —— 那正是做「相同资金占用下的被动持有」缺的那块 |

### ③ 这一轮要验的是什么，判据定死了吗

今天验的是 **§3.11 横截面**：判据在跑之前写进 §3.11.6，**S1 判死**，全程只报一次分位，
归档 [M2 §5.33–§5.35](./M2-偏差报告.md)。**今天不再开新实验** ——
日信息增量 0.048%，而一次合格的改动实测要一整天。

下一轮的候选与判据**已写好但未跑**：§3.12（大盘 regime 分层，判死线「极差 < 10pp」，
且**必须在个股 regime 层内切**以避开 beta 回声）。

### ④ 信源台账更新了吗

**更新了**（这是收尾动作不是可选项）：新增 §7 聚宽（站点结构 · 试用门槛 ·
**协议 5.1/3.2/4.1 的入库分发禁令** · SDK 实读的 API 面）与 §8 取页通路
（**WebFetch 在本机永久不可用**，根因是 `claude.ai` 不可达）；
§4「行业分类」那格补上**三条来源与成本排序**；更新记录两行，其中一行是**自我纠正**
（计划文档 §2.3 说换手率拿不到，而台账 08-17 那条早就记着可得 —— **台账是对的**）。

### 顺带修掉的两处看板读数缺陷（`tools/iterate/status.ts`）

| 缺陷 | 修法 |
|---|---|
| 表头写死「**全期收益**」，而选中的报告 `meta.to` 是 **2023-12-31**（训练窗口）⇒ 给了一个它没挣到的标签 | 打印真实窗口、表头改「区间收益」、加一句「这不是全期」 |
| 干净交易日判据是「启动次数 == 0」⇒ 今天唯一那次启动在**北京 16:49（盘后）**也被判脏 ⇒ **M3 的计数器结构上永远到不了 5** | 按启动时刻分**四档**：零重启 / **只在盘外** / 盘中重启 / 读不到。实测 08-13 十四次**全在盘外**（17:39–00:25 北京）、08-14 二十七次**其中盘中 12 次** |

⚠ **「只在盘外重启」算不算 M3 的干净交易日，看板不替你定** —— 当日的冷却/配额是当日语义、
没被污染，但 `lastForcedLoss`（强制类 2% 台阶）是**跨日**的，清零会让**下一个交易日**
的同一条止损多发一次。这是判据问题，写进了 [迭代计划 §1.2](./下一阶段取舍与迭代计划.md) 等你拍。

---

## 十一、这一天**没有**改的东西

- 引擎、参数、`ENGINE_VERSION`、标定归档：一个字没动。
- `announcements()` / `dailyBrief()` 仍整组排除行业 ETF —— ETF 没有个股公告，
  放进去只是多打一圈请求换回一堆空。
- `alertable()` 钩子仍然没有调用方（日后要「完全不进提醒链路」的东西还靠它）。
- ETF 池那条研究线（12 只、`N_eff = 2.97`）与这里的 15 只观察名单**是两件事**，
  不要因为都叫 ETF 就混起来引用。

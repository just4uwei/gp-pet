# M2 偏差报告

实现 M2 时，与设计文档不一致、或超出文档范围的地方。**每条都要么改了文档，要么在这里备案。**
不备案的偏差下一个人只会当成 bug 再改回去。

日期：2026-08-12。对应 [docs/08 M2](../08-开发路线图.md)。

---

## 0. 一句话结论

**引擎五层、回测 CLI、标定工具、面板信号列表都已实现且有测试；
参数标定本身没做，因为本机拿不到真实历史行情。**
`src/core/params.ts` 里的每个数字仍是 ADR-0003 说的「待标定的初始猜测」，
`ENGINE_VERSION` 因此保留 `-unvalidated` 后缀。详见 §5。

---

## 1. 与 docs/04 的口径差异（已在源码里逐条注明）

| # | 位置 | 文档写的 | 实现的 | 为什么 |
|---|---|---|---|---|
| 1.1 | `indicators/dmi.ts` | `ATR = Wilder(TR,14)`（**和式**） | `atr = Wilder(TR,14) / 14`（均值） | 导出的 `atr` 会被 [docs/05 §2.3](../05-风控与提醒规则.md) 的自适应止损（`2×ATR/close`）与波动率分位直接使用，和式会让那两处静默地偏大 14 倍。DI 是两个和式相除，除不除都一样 |
| 1.2 | `indicators/boll.ts` | 「20 + 250 = **270** 根才有首个分位值」 | 第 **269** 根（下标 268） | 文档把当根重复计了一次。实现按「窗口严格取 250 个样本」，差 1 根，不影响任何判定 |
| 1.3 | `regime/index.ts` | 突变条件「ADX 3 日变化 > 5」 | 取**绝对值** | 原文括注为「趋势快速形成**或瓦解**」，瓦解就是下降。与 BBW 那条（原文明确写了绝对值）保持一致 |
| 1.4 | `combine/index.ts` | 冲突条款只提「两个方向都触发」 | 「对侧得分也过阈值、只是票数不够」时同样判为矛盾 | 多空得分几乎相等的本质是规则互相打架，与对侧差一票无关。此时提醒用户买入是不负责任的 |
| 1.4b | `combine/index.ts` | `rawScore = Σ strategyWeight × (Σ sub.score × sub.weight)` | 策略内取**加权平均**（除以已成立子信号的权重和） | 见下 |
| 1.4c | `risk/index.ts` | 移动止损「盈利 ≥ 5% 后，从 peak 回撤 ≥ 3%」 | 取**当前**浮盈 ≥ 5%（盈利保护那条取「曾达」浮盈） | 见下 |

### 1.4b 组合层得分为什么改成加权平均

字面读法是「未成立的子信号按 0 计入」。那样一来，趋势市里要过 0.60 就得让 5 条趋势子信号中的
4 条同时接近满分（`0.7 × 0.857`），而同一节又写着「`votes ≥ 3` 即可触发」——
**两个数在字面读法下不可能同时成立**，票数条件形同虚设，而实际效果是引擎几乎永远不出信号。

取加权平均后，得分的含义变成「已成立规则的平均强度 × 该策略在当前状态下的权重」：
一致性由票数负责、强度由得分负责，0.60 / 3 票才是一组自洽的阈值。
副作用是**震荡市里纯趋势信号最高只有 0.3，永远够不着 0.60** —— 这正是动态权重该有的效果，
也正是回测的分 Regime 归因要检验的那个假设。

### 1.4c 移动止损与盈利保护为什么必须一个取「当前」一个取「曾达」

两条都按 peak 浮盈读的话，④ 盈利保护几乎永远够不着：
`peakProfit ≥ 5%` 且 `profit < 2%` 反推出 `fromPeak ≤ -2.86%`，与 ② 移动止损的 `-3%` 几乎重合，
② 会把 ④ 整个吃掉（写完用例才发现 ④ 是死代码）。

按「当前 / 曾达」区分后两条规则不重叠，各自都有明确语义：
② 是「趁还赚着落袋」，④ 是「已经回吐大半，保住剩下的」。
原文（docs/05 §2.3）的措辞正是「盈利 ≥ 5% **后**」与「**曾达** +5% 后」。
| 1.5 | `strategies/trend.ts` | T2 打分式 `0.5 + 0.5×min(HIST[i]/HIST[i-1]−1, 1)` | `HIST[i-1] ≤ 0` 时改用固定 0.6 | 金叉当日柱子刚由负转正，几乎必然 `HIST[i-1] ≤ 0`，直接套公式会得到负比值甚至除零 |
| 1.6 | `risk/index.ts` | 「新股次新：上市 < 60 个交易日」 | 用**已入库根数**近似 | 上市日期数据源不一定给。回补目标是 320 根，因此 `bars < 60` 基本只可能是次新股或回补失败，两种情况都不该出信号 |

### 1.7 票数判据实际等价于「成立的子信号个数」

docs/04 §4.2 的 `votes(d) = 满足 sub.score ≥ 0.5 的子信号个数`，而 `strategies/context.ts`
的统一打分把 **0.5 定义为「刚好满足条件」**，1.0 为「满足到 span 倍」。
因此任何成立的子信号分数都 ≥ 0.5，票数 = 成立的子信号个数。

这与原文「3/5 策略一致确认」的意图一致，但**「≥ 0.5」这道门槛事实上不起筛选作用**。
若标定时发现票数门槛没有区分度，应当改的是打分函数的下界（比如让弱条件给 0.3），
而不是调票数阈值 —— 记在这里，免得下一个人对着 `votes` 调半天。

---

## 2. 类型契约的变更

| # | 改动 | 为什么 |
|---|---|---|
| 2.1 | `IndicatorSet.thresholds` 增加 `volPct` | ADX 动态阈值的输入。落进 evidence 才能解释「为什么今天的门槛是 24」 |
| 2.2 | `EngineContext.candles / weekly` 改为 `readonly` | 回测靠 `Object.freeze(candles.slice(0, i+1))` 从物理上消除未来函数，引擎不得改写调用方数组 |
| 2.3 | `EngineContext` 增加可选 `industryShare` | 行业集中度降级规则（docs/05 §2.2）的输入。**可选**：未统计时不触发该规则，不能用 0 顶替「未知」 |
| 2.4 | `EngineContext.now` 增加可选 `atMs` | 「快照是否陈旧」这一条风控要墙上时刻。回测没有快照也就没有陈旧一说，缺省时该规则不参与 |
| 2.5 | `GatedSignal` 增加 `suppressed` | 面板要显示「今日被静默的 N 条」及原因。由 verdicts 推导会在 UI 与 M3 两处各写一遍，容易走岔 |
| 2.6 | `Evaluation.candle` | 上层要用被判定那根的**不复权**价做展示、成本与峰值更新 |
| 2.7 | `EngineParams` 由 `typeof DEFAULT_PARAMS` 改为放宽字面量的映射类型 | 否则 `macd.fast` 的类型是字面量 `12`，回测**无法**构造候选参数集 —— 而标定正是 M2 的出口条件 |

`params.ts` 新增了三个块：`strategy`（回溯窗口与带位阈值）、`multiTf`（原文写死的 25 / 20 / ±0.1）、
`alert`（分级得分线 0.75）。它们原本散落在文档正文里，提出来是为了标定时不会漏掉。

---

## 3. 结构上的取舍

### 3.1 `regime` 每次从头算整条序列，而不是「读上次状态 + 增量推进」

盘中最后一根是临时线。若把它的结论持久化，收盘后就再也分不清
「状态是真的切了」还是「上午那次抖动被记成了历史」。整条重算的代价是 O(n)，可以忽略。

### 3.2 `src/backtest` 用相对路径 import，且自带一个 SQLite 只读壳

- CLI 由 tsx 直接跑，而根 `tsconfig.json` 是 solution 风格、不带 `paths`，tsx 解析不到 `@core/*`。
- 按 CLAUDE.md 的分层，`src/backtest` 只复用 `src/core`。十几行驱动壳换来的是「回测不依赖主进程装配」。

### 3.3 仓位模型：每只标的一个等额独立仓位

组合净值 = 各仓位净值之和，缺 K 线的日子前值填充（停牌期间那部分资金确实还在）。
这不是资金管理策略，只是让「信号本身值不值钱」可比 —— 引入调仓与权重优化会让绩效差异
分不清是策略的还是资金管理的。报告里标注了这一点。

### 3.4 回测里「明日开盘观察」= 买入

收盘确认的 BUY 信号必然落在 T+1 尾盘规则之后（判定时刻是 15:00），因此风控会把它改写为
`NEXT_DAY_WATCH`。而回测的成交模型本来就是**次日开盘**成交 —— 两者是同一件事。
这反过来验证了 T+1 规则与回测执行模型自洽。

### 3.5 信号落库去重

盘中每 30s 一轮，100 只股票一天 800 轮。落库签名取
`日期 | 方向 | 阶段 | 级别 | 是否抑制 | 首要依据`，**刻意不含得分** ——
得分随临时 K 线连续抖动，含它等于没去重。

---

## 4. 文档没写、但实现绕不过去的

### 4.1 基准指数要跟着自选股一起回补

RSI 的动态阈值靠沪深300 的近 20 日收益分位算（docs/04 §1.6），但基准不在自选股表里。
`tick.ts` 因此多了一个 `auxCodes()`：需要日线、但不产出信号也不取快照的代码。
少了它，情绪值会一直退化为中性 0.5，RSI 阈值静默地停在 75/25，**没人看得出来**。

### 4.2 大盘情绪的「逐根序列」与「当期标量」必须同源

实盘只需要当期一个值（`marketSentiment`），回测每根都要（`sentimentSeries`）。
两者若分叉，回测用的 RSI 阈值曲线就和实盘不是同一条。
有一条用例专门断言「序列末值 === 当期值」。

### 4.3 `IndicatorSet.thresholds` 里的 RSI 两条阈值是「当期值铺满整条序列」

情绪是**当期**标量，历史位置上的阈值不代表当时的真实阈值 —— **只有最后一根有意义**。
回测里每根 K 线各自调用一次引擎，那时的「最后一根」才是被判定的那根，因此不受影响。
已写进 `types.ts` 与 `thresholds.ts` 的注释。

### 4.4 收盘确认轮只改库里的 `stage`，不发提醒

docs/04 §6 的确认轮包含「主动发一条撤销提示」，那属提醒层（M3）。
M2 实现了 `PROVISIONAL → CONFIRMED / INVALIDATED` 的状态推进，
否则 M3 上线时会拿到一堆永远停在 PROVISIONAL 的历史行，分不清哪些当初被确认过。

---

## 5. **M2 出口条件的达成情况**（最重要的一节）

docs/08 的 M2 出口条件有三条：

| 出口条件 | 状态 | 说明 |
|---|---|---|
| `src/core` 覆盖率 ≥ 90% | ✅ 已达成 | `pnpm test:cov` 的 CI 门槛即此，未达标会直接失败 |
| 回测报告产出并据此确定默认参数 | ⚠️ **报告已产出，参数未确定** | 标定跑完了，结论是没有候选够格 —— 见 §5.4 |
| 分 Regime 归因能回答「权重切换是否有效」 | ✅ **能回答，答案是「无效」** | 三个参数点上固定 0.5/0.5 都不输于动态权重；各 Regime 的胜率与平均盈亏无可辨别差异 —— 见 §5.5 |

### 5.1 为什么没做参数标定

> **2026-08-12 更新：数据这条障碍已排除，本节原来的判断是错的。**
> 原文写「三家接口全部不通」，实测**三家都能用**：
> tencent 的 `web.ifzq.gtimg.cn/appstock/app/fqkline/get` 与 sina 快照稳定 200；
> eastmoney 在 undici 下也能通（成功率约 78%，重试可过），只有 curl 对它 100% 失败 ——
> 即 M1 偏差报告 §2「出口网络拦截」的诊断是 curl 单一客户端的假象。
> 而标定要的恰恰是历史日线，腾讯那个接口不复权与前复权两轨都给。
> `scripts/fetch-history.mjs`（`pnpm fetch:history`）已能把 2018 年至今拉成回测 fixture，
> 并已跑出第一份基于真实行情的报告。**未做的部分从「拿不到数据」变成了「标的池未定、
> 标定结论未复核」**，见 §5.4。

标定要 2018–2026 的真实日线。当时的判断是本机到三家行情接口全部不通
（见 [M1 偏差报告 §2](./M1-偏差报告.md)），`market.db` 里一根真实 K 线都没有。

**不能用合成数据代替**：合成序列的统计性质是我自己编的，在它上面「标定」出来的参数
只反映我编数据时的假设，比不标定更危险 —— 它会带着一份看起来很正规的报告。

### 5.2 因此 M2 交付的是「标定的能力」，不是「标定的结论」

已就绪、可直接用的：

```bash
# 单组参数的回测报告（含分 Regime 归因 + 固定权重对照组）
pnpm backtest -- --codes SH600000,SZ000001 --from 2018-01-01 --to 2026-06-30 --out reports/a.json

# 粗网格标定：训练/验证/测试三段切分，自动执行 docs/07 §3 的三条过拟合红线
pnpm backtest -- --codes ... --grid params/grid.json --out reports/calibration.json
```

红线已内置：交易 < 30 笔直接淘汰、验证集年化为负淘汰、验证集相对训练集断崖衰减标记为疑似过拟合、
排名用**验证集** Calmar（用训练集排名等于直接过拟合）、测试集只跑一次。

**工具不会自动改 `params.ts`** —— 自动写回会让「出厂默认值从哪来」变成没人复核过的黑箱，
而 ADR-0003 的整个立场就是不接受未经复核的数值。

### 5.3 接下来要做的事

1. `pnpm fetch:history -- --codes <标的池> --from 2018-01-01`
   （不必先跑通 `pnpm dev`：抓取脚本是独立的 Node 脚本，不经过主进程与 better-sqlite3）
2. `pnpm backtest -- --codes <标的池> --fixtures ./data/history --from 2018-01-01 --to <昨天>`，看分 Regime 归因
3. **若归因显示动态权重切换无效** → 按 [docs/08 关键决策点 2](../08-开发路线图.md)
   停下来简化组合层，不要带着无效的复杂度进入 M3
4. 跑网格标定 → 人工复核优胜候选 → 写回 `params.ts` → `ENGINE_VERSION` 去掉 `-unvalidated`
   → CHANGELOG 记录标定依据 → 删掉 `tests/unit/core/params.test.ts` 里那条断言后缀的用例

### 5.4 首次真实行情标定（2026-08-12）：跑完了，但**不产出可写回的参数**

#### 数据与标的池

两个池子分开跑，结论分开看：

| 池 | 构成 | 来源 |
|---|---|---|
| 自选股 | 7 只（用户真实自选，含 1 只 ETF） | `market.db` 的 `watchlist` |
| 宽基 | 39 只 | [`params/universe-broad.json`](../../params/universe-broad.json)，由 `pnpm build:universe` 按规则生成 |

宽基池的规则写在生成脚本头部：**按代码段分层 + 层内按代码升序等距抽样**，
不按市值 / 流动性 / ST 筛选（那都是**今天**的属性，用来筛 2018 年的样本就是前视偏差），
不含科创板（2019-07 才开市）。样本里因此混着 ST宁科、\*ST特迅这类票 —— 那是对的，
2018 年没人知道谁会变成 ST。

**消不掉的偏差：幸存者偏差。** 已退市股票在任何免费接口上都取不到日线，必然缺席，
方向是绩效被系统性高估。换规则解决不了，只能写明。

原抽样 40 只，`SZ301717`（2026 年新股，从未除权、只有 `day` 没有 `hfqday`）被
`fetch-history.mjs` 的断言拦下，实际入池 39 只。

#### 出厂猜测参数下的表现

| 池 | 交易 | 胜率 | 盈亏比 | 总收益 | 夏普 | 基准 |
|---|---|---|---|---|---|---|
| 自选股 7 只 | **2 笔** | 100% | — | +2.76% | 0.54 | +14.10% |
| 宽基 39 只 | **92 笔** | 18.48% | 2.45 | **-1.16%** | **-0.38** | +14.10% |

宽基池的期望值为负：`0.1848 × 2.45 − 0.8152 = −0.36`。

#### 三条发现

| # | 现象 | 数字 | 含义 |
|---|---|---|---|
| 1 | **出厂阈值几乎不出信号** | 7 只 × 8.5 年 = 59 个标的年只出 2 笔 | 印证了 §1.4b 的担心：`0.60 / 3 票` 这组阈值在加权平均口径下过严。标定里它**在验证集上 0 笔交易**被直接淘汰 |
| 2 | **TRANSITION 主导** | 宽基池判定 61534 根中 **41003 根（67%）** 为 TRANSITION | 动态权重的设计前提是四种状态各占相当比例。引擎三分之二时间停在过渡态，「权重切换是否有效」这个出口条件根本没被充分检验。~~该查 `regime` 的迟滞与阈值~~ → **已查，迟滞与阈值都不是原因，见 §5.5** |
| 3 | **动态权重没有跑赢固定权重** | 动态 92 笔 / -1.16% / 夏普 -0.38；固定 0.5/0.5 对照 11 笔 / -0.32% / 夏普 -0.46 | 两边都是负的，且两边都不够 30 笔红线，当时比不出东西。**§5.5 用交易量足够的两个参数点重测，结论是动态权重无效** |

#### 修掉一个让三段切分失效的 bug（**先看这条，它改变了标定结论**）

第一次标定跑出来「优胜候选验证集 Calmar 3.62、测试集 0 笔交易」，看着像是策略不出信号。
查下来是工具的问题：`runSplit` 把每一段**单独切出来**当独立序列喂进引擎，
于是 300 根预热（`params.data.fullBars`）在**每段内部重来一遍**：

| 段 | 区间根数 | 预热后真正被判定的根数 |
|---|---|---|
| train | 1457 | 1157 |
| validation | 359 | **59** |
| test | 272 | **0** —— 段长短于预热，结构上不可能出信号 |

即：验证集的排名建立在 59 天上，测试集恒为 0 笔。**这类错误不抛异常、不让测试变红，
只让标定结论静静地变成噪音。** 已修（`calibrate.ts` 的 `warmupForSplit()`：
喂到 `split.to` 为止的全部历史，但预热到 `split.from` 才开始判），并加了回归用例。

#### 标定结果（修复后）：没有可以写回的参数

网格：组合层阈值 5 组（[`params/grid-combine.json`](../../params/grid-combine.json)），
三段切分 train 2018-01→2023-12 / validation 2024-01→2025-06 / test 2025-07→2026-08-11。

| 候选 | 训练集交易 | 验证集交易 | 验证集 Calmar | 裁决 |
|---|---|---|---|---|
| `0.40 / 2 票` | 1343 | 406 | **0.05** | 优胜（但见下） |
| `0.35 / 1 票` | 3567 | 1372 | 0.01 | 通过 |
| `0.60 / 3 票`（**出厂默认**） | **44** | 22 | -0.07 | 淘汰：**验证集年化为负** |
| `0.50 / 2 票` | 952 | 354 | -0.08 | 淘汰：验证集年化为负 |
| `0.72 / 4 票` | **3** | — | — | 淘汰：训练集仅 3 笔（< 30） |

优胜候选的三段表现：

| 段 | 交易 | 年化 | 最大回撤 |
|---|---|---|---|
| train | 1343 | **-0.14%** | 5.05% |
| validation | 406 | +0.19% | 4.07% |
| test | 408 | **-0.03%** | 3.01% |

三段全部在零附近，训练集与测试集还是负的 —— 这不是「绩效一般的参数」，是**没有信号**。
顺带看清了出厂默认的另一面：`0.60 / 3` 在 39 只 × 6 年的训练集上只出 **44 笔**
（约每只每 6 年 1 笔），`0.72 / 4` 只出 3 笔。这两组阈值不是保守，是不工作。

所以标定的产出是：**`params.ts` 一个数都不能改**，理由是没有候选值得改成：

- **出厂默认 `0.60 / 3` 被红线淘汰** —— 验证集年化为负。它不是「保守」，是不成立；
- 优胜者的验证集 Calmar 是 **0.05**，与 0 没有实质区别，样本外还亏 0.28%。
  把它写进 `params.ts` 等于用噪音替换猜测，比保留猜测更糟 —— 后者至少诚实地标着未标定。

`ENGINE_VERSION` 的 `-unvalidated` 后缀保留。这一次它的含义变了：
不再是「没跑过标定」，而是「跑过了，没有候选够格」。

#### 下一步该做什么（以及不该做什么）

按 [约束 5](../../CLAUDE.md) 与 docs/08 关键决策点，**不要加指标**。三条发现指向的是同一个地方：
信号率。建议顺序是

1. 先查 `regime` 为什么三分之二时间停在 TRANSITION（迟滞窗口 / ADX 与 BBW 阈值）——
   它同时压着发现 2 与 3；
2. 再看 §1.7 记的那件事：票数门槛「≥ 0.5」事实上不起筛选作用，
   若要让票数有区分度，该改的是**打分函数的下界**而不是票数阈值；
3. 组合层阈值的标定要等前两条稳定之后重跑，否则标的是一个会变的靶子。

#### 工具侧记录的两件事

- **`fetch:history` 的抓取行为**：见下面第 1–4 条。
- **标定 CLI 在 39 只标的上偶发无声退出**：exit 1，stdout / stderr 都为空，
  同样的命令重跑即通过（6 次里挂了 3 次，挂的位置不固定 —— 有一次在候选 13/20 的中途，
  有一次在跑完全部候选之后）。8 只标的时未复现。**没有定位到原因**；
  曾怀疑与 `--out` 有关，但中途挂的那次发生在任何写盘之前，不成立。
  跑大网格时**必须检查退出码**，不能只看有没有报告文件 —— 它失败时什么都不留。

抓取侧有三条口径必须记住（都写在 `scripts/fetch-history.mjs` 的文件头）：

1. **复权轨用后复权（hfq），不是 docs/04 写的前复权 —— 这是被迫的，也是本次最重要的一条。**
   腾讯的前复权是加性的（减价差，[M1 §5.5](./M1-偏差报告.md) 实测），高分红股上累计分红
   一旦超过当年股价，前复权价就变**负数**：实测 SH601919（中远海控）2018–2026 有
   **714 根 `closeAdj` ≤ 0**，最低 -5.145。负价格上算出来的 MA / MACD / RSI / 布林带全部无意义，
   而回测又按复权价计净值 —— 这不是可以「记进报告」的偏差，是数据不可用。
   换后复权后，该股「复权后跳变 > 20%」的可疑计数从 **42 降到 0**，SZ000157 从 1 降到 0，
   反过来证明那些跳变本就是加性前复权的假象。
   **对判定与绩效等价**：指标全是比率型（MACD 穿越点、BBW、RSI、ADX 的 TR/ATR）在整条序列
   乘正常数后不变，净值用相邻复权价之比算，常数约掉；真实成交价那一路用的是不复权 `close`
   （止损与持仓成本），本来就没走复权轨。附带好处是 **hfq 的历史值不随抓取日变**，
   序列可复现，而 qfq 每天重抓都是新的一条。
   代价：`Candle.*Adj` 的字段语义与 docs/04 的措辞不一致（没改字段名 —— 那要动 core 的类型契约）。
   `fetch-history.mjs` 里有「复权价必须恒正」的断言，改回 qfq 会立刻炸。
2. **腾讯日线的量是「手」**，脚本 ×100 转股；日线不给成交额，`amount` 一律 `null`。
3. **当天那根一律剔除**：盘中它是临时 K 线，混进标定集等于让结论依赖跑它的时刻。
   首次跑就撞上了 —— 个股两轨只有一轨有当天，指数两轨都有。
4. **不写 `profile`**：响应带证券名称，但那是今天的名称，用它标 2018 年那根的 ST 状态
   是未来函数。宁可留着 `fallbackProfile` 那个方向已知的乐观偏差。

主源 eastmoney 的 fixture 仍未录成（间歇性失败 + `record-fixtures.mjs` 没有重试），
[M1 偏差报告 §2](./M1-偏差报告.md) 那条待办不受影响。

### 5.5 追查「TRANSITION 占 67%」（2026-08-12）：根因不在 regime 层

> 本节的**诊断**仍然成立，但其中的**回测数字都是解耦前的**（组合层口径已于同日改掉）。
> 要看改后的数、以及被这次改动修正的一处结论，直接读 [§5.6](#56-解耦得分上限与阈值2026-08-12口径改了决策点-2-的答案没变)。

§5.4 的三条发现里，发现 2 与 3 都指向 regime 层，所以先查它。结论是**方向错了**：
regime 层的判定确实很难成立，但让信号出不来的不是它，是组合层的一处乘性耦合。

新增工具 [`pnpm audit:regime`](../../src/backtest/regime-audit.ts)：读 regime 层自己发布的
`evidence` 字段（不复写判定逻辑 —— 复写的话审计与被审计对象可以一起错），
判定根集合与 `simulate.ts` 逐条对齐（同样的 warmup / 跳过末根 / 跳过 `hasGap` / 同样的
`lookback` 切片），因此占比可以直接与回测报告对账：审计得到 41003/61534，与 §5.4 的数字一致。

#### 成因分解（39 只宽基样本，61534 根判定）

| 成因 | 根数 | 占比 | 说明 |
|---|---|---|---|
| ① 未预热 | 0 | **0.0%** | 预热与数据量**不是**原因 |
| ② 突变条件命中 | 15012 | 24.4% | 三条突变线中「量比 > 1.5」独占 9.9% |
| ③ ADX 死区 | 11719 | 19.0% | ADX 落在 `[adxRange, adxTrend]` 之间，趋势与震荡都不可判 |
| ④ 四条件合取不满足 | 14086 | 22.9% | ADX 够了，但 DI / MA20 / 排列不配合 |
| ⑤ 迟滞压制（与①–④交叉） | 4017 | 6.5% | 原始判定已非 TRANSITION，生效仍是 |

**迟滞不是原因**：原始判定 TRANSITION 占 66.3%，带迟滞后 66.6% —— 几乎不动。
`hysteresisDays: 2` 温和到近乎无操作，§5.4 里「该查迟滞」的猜测不成立。

ADX 的动态阈值恰好落在实际分布的正中间：ADX 均值 **25.04**，`adxTrend` 均值 **23.85**，
于是 47.1% 的根在趋势侧、28.8% 在震荡侧、24.1% 卡在中间的死区。

#### 四个反事实探针：TRANSITION 有个约 50% 的地板

| 探针 | 改动 | TRANSITION | 相对出厂 |
|---|---|---|---|
| — | 出厂默认 | 66.6% | — |
| A | 三条突变条件**全部关闭** | 53.7% | −12.9 |
| B | 突变放宽（量比 3 / ADX 10 / BBW 40） | 55.8% | −10.8 |
| C | ADX 死区 `rangeGap` 5 → 2 | 63.0% | −3.6 |
| D | B + C 同时 | 51.6% | −15.0 |

即使把三条突变条件整条删掉，TRANSITION 仍有 53.7%；死区几乎消灭（24.1% → 6.8%）
只买回 3.6 个点，腾出来的根大半掉进「④规则不满足」而不是变成 RANGE。
**调 regime 的阈值最多买回 15 个点，买不到「四种状态各占相当比例」那个设计前提。**

#### 根因：得分上限 = 策略权重，与 `scoreThreshold` 乘性耦合

[`combine/index.ts`](../../src/core/combine/index.ts) 第 128 行：

```
raw = w_trend × trendPart + w_meanRev × meanRevPart      // 两个 part 都是 0..1 的加权平均
```

两个 part 是**已成立子信号的加权平均**（§1 那条偏差的产物，本身是对的）。
后果是：**只有一个策略出信号时，`raw` 的上限恰好等于该策略在当前 regime 的权重。**

| Regime | 只有趋势 | 只有均值回归 | 出厂阈值 0.60 可达？ |
|---|---|---|---|
| TREND_UP | 0.70 | 0.30 | 趋势可，均值回归**不可能** |
| TREND_DOWN | 0.70 | 0.15（BUY） | 买入**不可能**（这条是设计意图） |
| RANGE | 0.30 | 0.70 | 趋势**不可能**，均值回归可 |
| **TRANSITION** | **0.50** | **0.50** | **两者都不可能** |

**在 66.6% 的判定根上，单策略信号在算术上无法触发。** 唯一出路是趋势与均值回归
同向共振 —— 而它们本就是对立的两套逻辑。这解释了 §5.4 全部三条发现：

- 92 笔交易里 **84 笔来自 TREND_UP** —— 唯一一个单策略（趋势 0.70）能过 0.60 的状态；
- RANGE 与 TREND_DOWN 合计 11378 根判定、**0 笔交易**；
- 保守档 `0.72` 高于**所有**单策略上限（最高的 TREND_UP 趋势也只有 0.70）
  → 任何 regime 下单策略都不可能触发，标定里它 6 年只出 3 笔；
- 标定的网格排名其实不是在比信号质量，是在比「阈值有没有掉到上限以下」：
  0.35 → 3567 笔、0.40 → 1343、0.50 → 952、0.60 → 44、0.72 → 3，
  断崖正好落在 TRANSITION 的 0.50 上限处。

权重表（docs/04 §4.1）与 `scoreThreshold`（§4.2）来自来源文档的不同段落，
**两者从未相互校对过** —— ADR-0003 说的正是这种情况。已在
[`tests/unit/combine/combine.test.ts`](../../tests/unit/combine/combine.test.ts)
用一组用例把这个上限固化下来（固化现状，不主张现状正确：改权重表、改阈值、
或改成按贡献策略归一化，都会让它们失败，那时应当连同 docs/04 §4.1 一起复核）。

#### 决策点 2 的答案：没有证据说明动态权重有效

出厂参数下动态只有 92 笔、固定只有 11 笔，都不够 docs/07 §3 的 30 笔红线，比不出东西。
把阈值降到上限以下后交易量足够了，于是同一次运行里的对照组可以直接比：

| 组合层参数 | 动态权重 | 固定 0.5/0.5 对照 |
|---|---|---|
| `0.60 / 3`（出厂） | 92 笔 / -1.16% / 夏普 -0.38 | 11 笔 / -0.32% / -0.46 |
| `0.40 / 2`（标定优胜） | 2187 笔 / **+0.21%** / 0.02 | 1687 笔 / **+2.94%** / **0.15** |
| `0.35 / 1` | 5902 笔 / **-9.06%** / -0.16 | 5676 笔 / **-6.54%** / **-0.11** |

两个交易量足够的参数点上，**固定权重在总收益与夏普上都赢了动态权重**，
成本差只解释了缺口的约三成（0.40/2：成本差 2.9 万，盈亏差 10.4 万）。

> ⚠ 这个差距的**大小**是耦合的产物。解耦后同样三点重测，差值缩到 1pp 以内且正负交替
>（[§5.6](#56-解耦得分上限与阈值2026-08-12口径改了决策点-2-的答案没变)）。
> 「看不出动态权重有效」这个方向没变，但**不要引用上表的幅度**。

分 Regime 归因（`0.40 / 2`，2187 笔）更直接：

| Regime | 交易 | 判定根 | 胜率 | 平均盈亏 |
|---|---|---|---|---|
| TREND_UP | 1135 | 9153 | 30.1% | -1.93% |
| RANGE | 273 | 5208 | 30.8% | -1.83% |
| TRANSITION | 769 | 41003 | **33.9%** | **-1.58%** |
| TREND_DOWN | 10 | 6170 | 0% | -1.54% |

`0.35 / 1` 下同样的形状（32.6% / 33.6% / 32.8%，平均盈亏 -1.27% / -1.46% / -1.49%）。
**各状态的胜率与平均盈亏没有可辨别的差异，而「无法分类」的 TRANSITION 反而最好。**
regime 对交易结果不携带区分信息 —— 这正是 [docs/08 关键决策点 2](../08-开发路线图.md)
说的「归因显示动态权重切换无效」的情形，按其规定应当**在此处停下来重新设计组合层**。

注意这里比的是「动态 vs 固定」，不是「哪组参数好」：三个参数点的绝对绩效都在零附近
或为负，没有一组构成可用策略。上表**不得**用于宣称任何绩效。

#### 因此，下一步

§5.4 列的三步里，第 1 步（查 regime）已执行完毕，结论是**不要在 regime 层继续调阈值**。
修正后的建议：

1. ~~**先解掉得分上限与阈值的耦合**~~ —— 已于同日执行，见
   [§5.6](#56-解耦得分上限与阈值2026-08-12口径改了决策点-2-的答案没变)。
   实现时否掉了「按有贡献策略的权重和归一化」这一种（它会把 §4.1 的压制意图一起删掉），
   改成除以行内最大权重。
2. **然后才谈组合层的去留。** ——已可以谈了：解耦后重测，动态权重仍看不出效果，
   决策点 2 的触发条件在排除口径 bug 之后依然满足。
3. 组合层阈值的标定要等第 1 步落定之后重跑：现在的靶子会随口径一起动。
   ——仍然成立，而且现在要等的是**第 2 步的拍板**（组合层留不留决定了标定什么）。

**不要做的事**：加指标（约束 5）、继续微调 regime 的三条突变线与 `rangeGap`
（探针 A–D 已经量化过上界，最多 15 个点，且买不到设计前提）、
以及把上面任何一个数字写进 `params.ts`（ADR-0003）。

### 5.6 解耦得分上限与阈值（2026-08-12）：口径改了，决策点 2 的答案没变

执行 §5.5 第 1 步。**只改口径，一个参数值都没动**（`params.ts` 里的数字仍是 ADR-0003 的初始猜测），
`ENGINE_VERSION` 因算法变更递增到 `0.2.1-unvalidated`。

#### 改法与为什么是这一种

```
改前：raw = Σ_s  w_s × part_s                              // part_s ∈ [0,1] ⇒ 单策略上限 = w_s
改后：raw = clamp( Σ_s (w_s / max_s' w_s') × part_s, 0, 1 ) // 主策略满分 = 1.0
```

§5.5 提了两种解法，实现时否掉了「按**有贡献策略的权重和**归一化」那种：
单策略出手时它的分母恒等于分子里那个权重，得分永远是 1.0，
于是**震荡市里的孤立趋势信号与趋势市里的一样强** —— docs/04 §4.1 想表达的压制意图会被一起删掉。
除以行内最大权重则两头都保住：

| Regime | 趋势可信度 | 均值回归可信度 |
|---|---|---|
| `TREND_UP` / `TREND_DOWN` | 1.00 | 0.43（`TREND_DOWN` 的 BUY 0.21） |
| `RANGE` | 0.43 | 1.00 |
| `TRANSITION` | 1.00 | 1.00 |

两条不变量写进了 `tests/unit/combine/combine.test.ts`：**可达性**（每个状态都存在能拿满分的单策略信号）
与**区分度**（次策略单独出手只有 `w_minor / w_major`）。两条同时钉着，才不会退回原来的坑、
也不会滑到「归一化过头、权重表形同虚设」的另一头。

三个副作用要记下来：

1. **权重表的含义变了**：只有行内比值有意义，`0.70/0.30` 与 `0.35/0.15` 等价。
   「`TRANSITION` 整体不如 `TREND_UP` 可信」这层意思**丢掉了** —— 它原先是靠 `0.5 < 0.7` 顺带表达的，
   而那正是不可达的成因。真要表达，得新增一个显式的状态级折价参数并单独标定，不能再塞回权重表。
2. **夹紧点在加调整项之前**：共振时商可到 2.0，不夹的话 §3.3 的 `M3_FALSE_BREAKOUT`（−0.15）
   会被溢出的那截吸收掉，在最该起作用的场景里失效。代价是顶部没有分辨率了，那段交给票数。
3. **`alert.bubbleScore = 0.75` 这类分级线现在才有一致含义**：改前它在 `TRANSITION` 下不可达。

#### 出厂参数点：从「基本不出信号」变成「会出信号」

同一份数据（39 只、2018-01-01 → 2026-08-11、`--fixtures ./data/history`、每只 10 万本金）：

| 出厂 `0.60 / 3` | 交易 | 总收益 | 夏普 | 胜率 |
|---|---|---|---|---|
| 解耦前 | 92 | -1.16% | -0.38 | — |
| 解耦后 | **414** | **+1.73%** | **+0.22** | 34.1% |

分 Regime 归因也从「84/92 挤在 TREND_UP」变成两态分担：

| Regime | 交易 | 判定根 | 胜率 | 平均盈亏 |
|---|---|---|---|---|
| TREND_UP | 216 | 9153 | 31.0% | -1.67% |
| TRANSITION | **198** | 41003 | **37.4%** | **-1.12%** |
| RANGE | 0 | 5208 | — | — |
| TREND_DOWN | 0 | 6170 | — | — |

`TRANSITION` 从 0 笔变成 198 笔，正是这次要修的那条。

> ⚠ **绝对绩效仍不构成任何结论**：+1.73% 是 8.6 年累计，同期 SH000300 基准 +14.10%，超额 -12.37pp。
> 这些数字只用于**比较口径与参数**，不得用于宣称绩效，标的池本身也带选择偏差（见 §5.4 的标的池说明）。

#### `RANGE` / `TREND_DOWN` 仍是 0 笔 —— 现在卡的是票数，不是得分

补跑一个只降票数的探针（`0.60 / 2`，得分线不动）：

| 参数 | 总交易 | RANGE | TREND_DOWN | 总收益 | 夏普 |
|---|---|---|---|---|---|
| `0.60 / 3` | 414 | 0 | 0 | +1.73% | 0.22 |
| `0.60 / 2` | 2520 | **239** | **10** | -4.33% | -0.15 |

得分线不变、只放松一票，`RANGE` 就从 0 笔变成 239 笔 —— 所以这两个状态的沉默是
**3 票的一致性要求**造成的，不是得分够不着。均值回归只有 4 条子信号（趋势有 5 条），
要求其中 3 条同时成立是个很强的过滤器。它显然在做实事（放松后总收益从 +1.73% 掉到 -4.33%），
但「票数门槛对两个策略不等价」是个待处理的口径问题，不要靠调 `voteThreshold` 一个数糊过去。

#### 决策点 2：解耦后重测，答案没变

| 组合层参数 | 动态权重 | 固定 0.5/0.5 对照 | 差（动态 − 固定） |
|---|---|---|---|
| `0.60 / 3`（出厂） | 414 笔 / +1.73% / 0.22 | 419 笔 / +1.74% / 0.22 | -0.01pp |
| `0.60 / 2` | 2520 / -4.33% / -0.15 | 2540 / -3.24% / -0.11 | -1.09pp |
| `0.40 / 2` | 2636 / -2.80% / -0.09 | 2602 / -3.38% / -0.11 | **+0.58pp** |
| `0.35 / 1` | 6541 / -11.20% / -0.19 | 6575 / -10.80% / -0.18 | -0.40pp |

**差值在四个参数点上正负交替、量级都在 1pp 以内 —— 这是噪音，不是效果。**
动态权重仍然没有可辨认的收益。

同时要更正 §5.5 的一句话：那里写「三个参数点上固定权重都不输于动态权重」，
其中 `0.40/2` 的 +2.94% vs +0.21% 那个显眼的差距**是耦合的产物**，解耦后缩到 0.58pp 并且反了号。
结论的方向没变（看不出动态权重有效），但当时那个差距的**大小**不能当证据用。

分 Regime 归因同样没有变：出厂参数下 `TRANSITION`（37.4%）的胜率高于 `TREND_UP`（31.0%），
`0.40/2` 与 `0.35/1` 下四态的胜率仍都落在 31%–34% 的窄带里（`0.35/1` 的 `TREND_DOWN` 是 99 笔 47.5%，
样本太小且与其余三态方向相反，不能当信号）。**「无法分类」的状态不比「分类清楚」的状态差**
这一现象，在解耦后依然成立。

#### 因此，下一步（决策点 2 已经可以判了）

按 [docs/08 关键决策点 2](../08-开发路线图.md)，「归因显示动态权重切换无效」的条件已经满足，
且这次是在**排除了口径 bug 之后**满足的 —— 上一轮还可以说「是耦合压着它」，这一轮不能了。
规定动作是**简化为单一策略 + 风控，砍掉组合层复杂度**。这件事改的是产品的引擎形态，
需要人来拍板，本报告只把判据摆齐，不代劳。

若决定保留组合层，那么保留的理由不能是绩效（上表没有），只能是别的（比如「多策略投票让
『为什么提醒』更好解释」）—— 那就要明说，并且**不要再把 regime 权重当成绩效手段**来标定。

在拍板之前**不要**做的事：重跑阈值标定（靶子会随组合层去留一起动）、
把上面任何数字写进 `params.ts`（ADR-0003）、加指标（约束 5）。

### 5.7 策略消融（2026-08-12）：出厂参数下均值回归贡献 0 笔，且拿掉它更好

§5.6 把决策点 2 的判据摆到了「动态权重无效」这一步，但那还不足以在
「只砍动态权重」和「一路砍到单一策略 + 风控」之间做选择 —— 差一个数：
**均值回归到底贡献了几笔交易**。这一节把它测出来了。

#### 先说为什么需要新加一个开关

不能靠「把某策略的权重设成 0」做消融。组合层的**票数不分策略**
（任何 `sub.score ≥ 0.5` 都计一票，见 `combine/index.ts`），权重 0 的策略得分贡献是 0、
票照投 —— 那测的是「关掉它的得分、留着它的票」，不是关掉这个策略。

因此新增 `params.enabledStrategies`（`{ trend, meanReversion }`，出厂两项皆 `true`）：
被关掉的策略在 `runStrategies` 里一个子信号都不产出，对组合层既不供分也不供票。
多周期调整项不受影响（它不属于这两个策略）。`ENGINE_VERSION` 因参数结构变更递增到
`0.2.2-unvalidated`；出厂形态下重跑基线得到与 §5.6 完全一致的 414 笔 / +1.73% / 夏普 0.22，
**开关本身对默认行为是中性的**。

它是测量工具，不是可标定参数 —— 网格标定不得把它当维度扫。

#### 结果（39 只宽基样本 · 2018-01-01 → 2026-08-11 · 每标的 10 万）

| 组合层参数 | 启用的策略 | 交易 | 总收益 | 夏普 | 最大回撤 |
|---|---|---|---|---|---|
| `0.60 / 3`（出厂） | 趋势 + 均值回归 | 414 | +1.73% | 0.22 | 2.46% |
| `0.60 / 3` | **只留趋势** | **435** | **+2.67%** | **0.32** | 2.17% |
| `0.60 / 3` | 只留均值回归 | **0** | — | — | — |
| `0.60 / 2` | 趋势 + 均值回归 | 2520 | -4.33% | -0.15 | 10.10% |
| `0.60 / 2` | 只留趋势 | 2439 | -3.61% | -0.13 | 10.15% |
| `0.60 / 2` | 只留均值回归 | 292 | +0.15% | 0.03 | 2.13% |

三条读数：

1. **出厂参数下均值回归单独出手一次都没有。** 不是「贡献小」，是 0。
   直接成因是票数：它只有 4 个子信号（R1–R4），要凑够 3 票得四中有三同时成立；
   趋势有 5 个（T1–T5），同一条 `voteThreshold` 对两者并不对等。
2. **它在组合里是净负贡献。** 加上它，交易从 435 笔降到 414 笔（`TRANSITION` −20、`TREND_UP` −1），
   收益 +2.67% → +1.73%，夏普 0.32 → 0.22。机制是它的反向子信号触发了冲突裁决
   （`conflictBand`）把趋势那一侧的信号也一并压掉 —— 它自己不出信号，却在拦别人。
   在 `0.60 / 2` 上同向重现（−3.61% → −4.33%）。
3. **它并非「结构上永远不可能出信号」。** 把票数放到 2，它能出 292 笔，且几乎全在 `RANGE`
   （290 笔，`TRANSITION` 只有 2 笔）—— 与 §4.1 想表达的「震荡市靠均值回归」是一致的。
   但那 292 笔的成绩是 +0.15% / 夏普 0.03，接近零。

#### 这个结果能证明什么，不能证明什么

**能证明**：在当前这组未标定参数、这套票数规则下，均值回归对最终交易没有正贡献，
把它整条拿掉反而更好。决策点 2 里「简化为单一策略 + 风控」的规定动作，
现在有了直接判据，而不只是「动态权重无效」这个间接判据。

**不能证明**：均值回归这类策略本身没用。至少三个混淆项没有被排除 ——
(a) R1–R4 的阈值（RSI 极值、`revertLookback`、`midReversionStd`…）全是 ADR-0003 说的初始猜测，
从未标定过；(b) 4 子信号 vs 5 子信号共用一条 `voteThreshold`，对它系统性不利；
(c) 冲突裁决目前是「压掉双方」，另一种写法是「弱侧让路」，那会改变第 2 条读数。
**所以这里测到的是「这一版实现 + 这一组猜测参数下的均值回归」，不是「均值回归」。**

#### 三条可选路径（这一步需要人拍板，报告不代劳）

| 路径 | 动作 | 代价 | 风险 |
|---|---|---|---|
| A. 砍到底 | 删掉均值回归策略与组合层的权重/裁决，只留趋势 + 风控 | 改动最大，docs/04 §3.2/§4 大段作废 | 若失败原因其实是 (a)(b)(c)，等于把没调过的东西判了死刑 |
| B. 只砍动态权重 | 权重表固定 0.5/0.5、删 regime → 权重链路，保留得分/票数/裁决 | 纯删除，风险最低 | 留着一个已知净负贡献的策略 |
| C. 先修不对等再判 | 给两个策略各自的票数线（或改成按权重比例的票数），标定 R1–R4 后重测 | 多一轮标定 | 可能标定完仍是这个结论，多花一轮 |

按已测到的东西，**A 与 B 都不会让绩效变差**（A 直接 +0.94pp，B 实测 ±0.01pp）。
差别在于「保留多少可标定的空间」。C 是唯一能把混淆项 (a)(b) 排除掉的路径，
但它要求先接受「再花一轮标定」。

在拍板之前，§5.6 末尾那三条「不要做的事」继续有效。

---

### 5.8 拍板（2026-08-12）：删掉动态权重，均值回归暂留一轮

选的是路径 **B**。

#### 判据是「机制是否惰性」，不是绩效差值

这一点值得写清楚，因为它决定了为什么不顺手把均值回归一起砍掉（那可是白捡的 +0.94pp）。

§5.6 里，动态权重与固定 0.5/0.5 的差值在四个参数点上是 ±1pp 以内、正负交替，
我据此判它「看不出效果」。那么 §5.7 里「只留趋势」比「两个都留」好的 +0.94pp，
是同一个量级的数 —— 用它来支持一个删除决定，就是拿同一把尺子量出两个相反的结论。

能把两件事分开的不是绩效，是**成因是否已经查清**：

- 权重表：两轮测量（带口径 bug 的和解耦后的）都测不出效果，而且现在知道它唯一还在起作用的
  地方是**把 `RANGE` 里的趋势单策略得分钉死在 3/7 ≈ 0.43**（够不着 0.60）—— 那是副作用，不是效果。
  它是惰性的，且惰性的原因已经查清。
- 均值回归：0 笔有一个明确的、**尚未排除**的成因（4 子信号 vs 5 子信号共用 `voteThreshold = 3`，
  即 §5.7 的混淆项 (b)）。在排除它之前判死刑，就是给一个没调过的东西判死刑。

所以：删已经查清是惰性的那个，留下一个能在一小时内排除的混淆项先去排除。

#### 实际删了什么

| 删除对象 | 位置 |
|---|---|
| `weights` 表（四行状态 × 两个策略） | `src/core/params.ts` |
| `weightsFor()` 与得分式里的权重项 | `src/core/combine/index.ts` |
| 「每行和为 1」「趋势市与震荡市取向相反」两条不变量 | `tests/unit/core/params.test.ts` |
| 固定权重对照组（`fixedWeightParams()`、`--no-fixed-weights`、报告里的对照块） | `src/backtest/{args,cli,report}.ts` |

得分式从

```
raw = clamp01( (w_t·part_t + w_mr·part_mr·buyPenalty) / max(w_t, w_mr) )
```

变成

```
raw = clamp01( part_t + part_mr · buyPenalty )
```

`downtrendBuyPenalty`（下跌趋势里均值回归**买入**的 0.5 折价）**保留**：它是方向级抑制
（docs/04 §4.1 的「别接飞刀」），与「按状态切换策略可信度」不是一回事，且从未被单独测过。
`ENGINE_VERSION` → `0.2.3-unvalidated`。

删除处都留了墓碑注释，写明「不要把权重表加回来 —— 它已经被测过一次了」。

#### 删完的实测（出厂 `0.60 / 3`，39 只，2018-01-01 → 2026-08-11）

| | 交易 | 总收益 | 夏普 | 最大回撤 | 胜率 | 平均持仓 |
|---|---|---|---|---|---|---|
| 删除前（动态权重） | 414 | +1.73% | 0.22 | 2.46% | 34.06% | 14.3 日 |
| 删除后（等权） | **419** | **+1.74%** | **0.22** | 2.53% | 34.13% | 14.8 日 |

总量几乎没动，但分状态归因下面换了一批交易：

| Regime | 删除前 | 删除后 |
|---|---|---|
| `TREND_UP` | 216 | 197（−19，胜率 31.02% → 30.96%） |
| `TREND_DOWN` | 0 | 0 |
| `RANGE` | **0** | **24**（胜率 37.50%） |
| `TRANSITION` | 198 | 198（胜率 37.37% → 36.87%） |

两处变化各有成因，都是权重行内比值的直接后果：

- **`RANGE` 从「一笔都没有」变成 24 笔** —— 0.3/0.7 那一行让趋势单策略的得分封顶在
  3/7 ≈ 0.43，够不着 0.60，**震荡市里的趋势信号在算术上不可能触发**。这是 §5.5 那个耦合坑的
  残留分支：§5.6 的「除以行内最大权重」解掉了「上限恰等于权重」，但没解掉「行内比值压制弱侧策略」。
- **`TREND_UP` 少了 19 笔** —— 反方向的同一件事。0.7/0.3 曾把均值回归在趋势市里的得分压到
  3/7，现在它满额参与，于是更多反向子信号进入冲突裁决，压掉了一部分趋势侧交易。
  这与 §5.7 第 2 条读数（均值回归「自己不出信号，却在拦别人」）是同一个机制，
  权重表删掉后它拦得更多了 —— **等权化本身没有削弱这个已知问题，反而略微放大了它。**
  这条也是下一步先修票数线、暂不砍策略的另一个理由：现在均值回归的净影响比 §5.7 测的更大，
  更值得把混淆项排干净再判。

> 一个口径提醒：删除前那一列的「固定 0.5/0.5 对照组」曾测得 419 笔 / +1.74%，与删除后完全一致，
> 但两者**不是同一份配置** —— 旧对照组顺手把 `meanReversionBuyPenalty` 也设成了 1，
> 新引擎保留 0.5。数字撞上是因为 `TREND_DOWN` 两边都是 0 笔，那条折价没有作用对象。
> 别把「对上了」当成两份配置等价的证据。

绝对绩效仍然不构成任何结论：+1.74% 是 8.6 年累计，同期基准 +14.10%（超额 −12.37%）。
这一步做的是**删掉一个测不出效果的机制**，不是「提升了绩效」。

#### 还没做的

下一步是 §5.7 路径 C 的前半段：**给两个策略各自的票数线**，把 4 vs 5 的不对等修掉再跑一次消融。
预期是救不回来（2 票时均值回归已经是 292 笔 / +0.15% / 夏普 0.03），
但跑完之后再执行路径 A，才不是在给一个没调过的东西判死刑。

在这一步跑完之前，仍然**不重跑阈值标定**（靶子会随组合层去留一起动）。

---

### 5.9 修掉票数线的不对等（2026-08-12）：均值回归被救回来了，但仍然测不出贡献

§5.7 混淆项 (b) 说的是：趋势 5 个子信号、均值回归 4 个，共用一条 `voteThreshold = 3`，
对后者是四中取三（75%）、对前者是五中取三（60%）。这一节把它修掉再测。

#### 改了什么

`combine.voteThreshold` 从一个整数变成 `{ trend, meanReversion }`，三档灵敏度按**同一个比例**
换算再取整：趋势 2/3/4 票即 5 个子信号的 40%/60%/80%，均值回归 4 × 同比例 = 1.6/2.4/3.2 ⇒ 2/2/3。
出厂档因此从「3 票」变成「趋势 3 票 / 均值回归 2 票」。`ENGINE_VERSION` → `0.2.4-unvalidated`。

同时**合并规则也变了**：触发要求从「两边票数之和 ≥ 线」改成「**任一策略达到自己的线**」。
这是必然的 —— 一条按策略给的线没法和一个跨策略的总和比。理由本身也站得住：
票数衡量的是同一个策略内部有多少条规则互相印证，「趋势 2 票 + 均值回归 1 票」凑出来的 3
不具备这个含义。但它确实是**第二处改动**，所以下面分两步测，不把两者混在一起。

#### 第一步：只换合并规则（两条线都设 3 票）

| | 交易 | 总收益 | 夏普 | 最大回撤 |
|---|---|---|---|---|
| §5.8 的基线（总票数 ≥ 3） | 419 | +1.74% | 0.22 | 2.53% |
| 分策略计票，线仍是 3/3 | 414 | +2.02% | 0.26 | 2.30% |

差 5 笔 —— 那 5 笔正是靠「跨策略凑票」触发的，现在不算数了。绩效变化在噪音带内，
这一步的意义是**把语义改动的影响量化出来只有 5 笔**，后面那一大段变化都不是它造成的。

#### 第二步：把均值回归的线降到 2（新的出厂档 0.60 / 3 / 2）

| 启用的策略 | 交易 | 总收益 | 夏普 | 最大回撤 | 胜率 | 盈亏比 |
|---|---|---|---|---|---|---|
| 趋势 + 均值回归 | **662** | **+2.98%** | 0.31 | 2.58% | 32.02% | 2.54 |
| 只留趋势 | 459 | +2.84% | **0.33** | **2.16%** | 35.08% | 2.36 |
| 只留均值回归 | 292 | +0.15% | 0.03 | 2.13% | 31.51% | 2.21 |

分状态（趋势 + 均值回归 那一行）：`TREND_UP` 200 · `TREND_DOWN` 0 · `RANGE` **275** · `TRANSITION` 187。

**均值回归从「0 笔」变成「292 笔独立出手」，其中 290 笔在 `RANGE`** —— 这正是 §4.1 给它设计的
位置（震荡市靠均值回归）。所以 §5.7 的读数 1「它一次都没独立出手」确实是票数线不对等造成的，
**不是这个策略结构上出不了信号**。混淆项 (b) 到此排除。

#### 但贡献仍然测不出来

把它加到趋势上：收益 +2.84% → +2.98%（**+0.14pp**），夏普 0.33 → 0.31（**−0.02**），
最大回撤 2.16% → 2.58%（**+0.42pp**），胜率 35.08% → 32.02%。

这是同一条「±1pp 内、正负交替」的噪音带 —— 我用它判过动态权重无效（§5.6），
也用它拒绝过「+0.94pp 就砍均值回归」（§5.8）。**同一把尺子量下来，现在的结论是
「加不加它，测不出差别」，而不是「它有用」或「它有害」。**

值得单独记一笔的是冲突裁决的损耗：459 + 292 = 751，合起来只有 662，**89 笔被裁决压掉**
（`RANGE` −47、`TRANSITION` −32、`TREND_UP` −10）。§5.7 读数 2 说的「它自己不出信号却在拦别人」
现在变成「两边互相拦」—— 这一层的代价是实打实的，而收益是测不出的。

#### 现在的状态与还剩的混淆项

- 混淆项 (b)（票数线不对等）：**已排除**。
- 混淆项 (a)（R1–R4 从未标定）：**仍在**，但性质变了 —— 之前均值回归根本不出手，标定它没有意义；
  现在它有 292 笔可优化的交易，**标定 R1–R4 第一次成为一件做得动的事**。
- 混淆项 (c)（冲突裁决「压掉双方」vs「弱侧让路」）：**仍在**，且这次量化了代价（89 笔）。

顺带一提，新出厂档 `0.60 / 3 / 2` 的绝对数字（662 笔 / +2.98% / 夏普 0.31）比 §5.8 的
`0.60 / 3`（419 笔 / +1.74% / 0.22）好，但**这不构成「参数标定完成」** —— 它仍是同一份
39 只、全样本内的数据，没过 docs/07 §3 的三段切分与三条红线。同期基准 +14.10%，超额仍是 −11.12%。

**下一步该由人拍板**：均值回归是按路径 A 砍掉（判据：贡献测不出，且要为冲突裁决付 89 笔的代价），
还是先标定 R1–R4 再判（判据：混淆项 (a) 现在第一次可测）。组合层的其余部分已经收口，
**阈值标定的靶子不再会动**，两条路都可以立刻开跑。

---

### 5.10 标定 R1–R4（2026-08-12）：贡献测不出来的原因是 R3 的压缩线，不是策略本身

选了后一条路。结论先写在前面：

> **均值回归留下。** §5.9 里那条「测不出贡献」的读数是 `strategy.squeezeBbwPct = 10` 造成的 ——
> 那个 10 直接来自来源文档的「BBW_PCT < 10 是变盘前夜」（docs/04 §1.4），是 ADR-0003 说的
> 典型「未经验证的转述值」。把它放宽到 16–30 之间的任意一个值，均值回归的边际贡献就从
> **+0.14pp（噪音带内）跳到 +2.0 ~ +3.4pp（噪音带的两三倍以上）**，而且回撤不变。
> 这是本项目第一个**由标定流程产出、而不是靠猜**的参数结论。

#### 怎么标的

两个网格文件、两种基线配置，四次运行：

| 网格 | 扫什么 | 基线 |
|---|---|---|
| `params/grid-mean-reversion.json` | `rsi`（R1 带宽，3 档）× `strategy`（R2/R3/R4，9 组 OFAT）= 27 组 | ① 只开均值回归 ② 完整引擎 |
| `params/grid-mean-reversion-squeeze.json` | `strategy.squeezeBbwPct` ∈ {16, 24, 30, 40, 50}，两条 R2/R4 剖面 = 10 组 | 同上 |

- 三段切分是 docs/07 §3 的默认：训练 2018-01-01→2023-12-31 / 验证 2024-01-01→2025-06-30 /
  测试 2025-07-01→2026-08-11。标的池仍是 `params/universe-broad.json` 的 39 只。
- 「只开均值回归」用 `params/mean-reversion-only.json` 经 `--params` 传入。
  `enabledStrategies` 是消融开关、**不是网格维度**，两个网格文件里都没有它。
- `strategy` 块内刻意用**一次一因子**（OFAT）而不是 3×3×3 全展开：全展开要跑两个多小时，
  而 OFAT 让 docs/07 §3 的第三条红线（邻域 ±20% 是否断崖）可以直接从同一张报告里读出来。
- 第二个网格是因为第一个网格的最优点 `squeezeBbwPct = 20` **落在网格边界上**，
  而 5 → 10 → 20 是单调上升的 —— 不往外扩就只知道「20 好过 10」，不知道 20 是不是峰。
  扩展的依据是验证集的单调性与缺失的 ±20% 邻居，**不是**测试集数字（见文末的纪律说明）。

#### 读数 1：R2 对它唯一的参数是**惰性的**

`revertLookback` 取 2 / 3 / 5，在两种基线 × 三档 RSI 共 6 条剖面上，
训练集与验证集的**笔数与收益逐位相同**：

| 配置 | rv2 | rv3 | rv5 |
|---|---|---|---|
| 只开均值回归 · rsi 出厂 | 199 笔 / −0.98% | 199 笔 / −0.98% | 199 笔 / −0.98% |
| 完整引擎 · rsi 出厂 | 409 笔 / +1.19% | 409 笔 / +1.19% | 409 笔 / +1.19% |

把回溯窗口从 2 日拉到 5 日，一笔交易都没多、没少。R2 的三个条件里
「近 N 日曾收在轨道外」根本不是约束项，真正卡住它的是同一根上的
「HIST 转正/转负」翻转（docs/04 §3.2）。**`revertLookback` 不是一个可标定参数**，
调它等于什么都没调 —— 这一条要么接受（保持 3），要么改 R2 的结构，标定给不出答案。

#### 读数 2：R1 在收紧方向上几乎也是惰性的

RSI 带宽三档（`osBase/obBase` = 20/60、15/65、10/70，即 s=0.5 时的 30/70、25/75、20/80）：

| 剖面（只开均值回归，训练集） | 20/60 | 15/65（出厂） | 10/70 |
|---|---|---|---|
| sq10 rv3 md1.5 | 208 笔 / −0.60% | 199 笔 / −0.98% | 197 笔 / −0.88% |
| sq20 rv3 md1.5 | 357 笔 / +0.60% | 351 笔 / −0.54% | 339 笔 / −0.82% |

出厂 → 更严（15/65 → 10/70）只少 2~12 笔，验证集分数**逐位相同**；
放松（→ 20/60）多出 6~9 笔，验证集在几乎每一行都更差。
也就是说 R1 在「RSI 深度超卖**且**触下轨」这个双重条件下本来就很少成立，
带宽在 ±5 点内怎么挪都改不了什么。

#### 读数 3：R3 的压缩线是唯一有效的杠杆，出厂值 10 明显偏紧

验证集 Calmar（排名依据；rv3 / md1.5 / rsi 出厂剖面）：

| `squeezeBbwPct` | 5 | 10（出厂） | 16 | 20 | 24 | 30 | 40 | 50 |
|---|---|---|---|---|---|---|---|---|
| 只开均值回归 | −0.02 ✗ | −0.01 ✗ | 0.31 | **0.36** | 0.26 | 0.16 | 0.06 | 0.04 |
| 完整引擎 | −0.01 ✗ | −0.01 ✗ | 0.05 ⚠ | 0.08 | 0.09 | **0.17** | 0.05 | 0.05 |

✗ = 被红线淘汰（验证集年化为负）；⚠ = 被标记疑似过拟合（相对训练集衰减 > 50%）。

**出厂的 10 在两种基线下都被红线淘汰**，而 16 以上是一整片高原 —— 不是一个孤立的峰值。
docs/07 §3 的第三条红线（±20% 邻域是否断崖）因此直接读得出来：以 20 为中心，
16 = 0.31、24 = 0.26，都在中心值的一半以上，**不是噪音峰值**。
完整引擎那一行的 30 反而更脆（40 = 0.05 < 0.17 的一半）。

两种基线给出的「最优」不同（只开均值回归 → 20，完整引擎 → 30），但结论不依赖于选哪个：
**16 到 30 之间的任何一个值都远好过出厂的 10。** 若要在高原上挑一个，
`20` 是唯一在训练集与验证集上**同时**靠前的（完整引擎训练集 +1.60%，是该剖面最高的一档）；
工具按验证集 Calmar 排名会给出 30，但它的训练集只有 +0.41%。**这一步是人的选择，本报告不代劳。**

#### 读数 4：换成标定后的压缩线，均值回归的贡献一下子测得出来了

全区间（2018-01-01 → 2026-08-11，39 只，每标的 10 万，出厂 `0.60 / 3 / 2`）：

| 配置 | 交易 | 总收益 | 夏普 | 最大回撤 |
|---|---|---|---|---|
| 只留趋势（不受 R3 影响） | 459 | +2.84% | 0.33 | 2.16% |
| sq10（出厂）两个都留 | 662 | +2.98% | 0.31 | 2.58% |
| sq16 两个都留 | 737 | +5.37% | 0.51 | 2.27% |
| **sq20 两个都留** | **762** | **+6.25%** | **0.57** | **2.16%** |
| sq24 两个都留 | 788 | +4.87% | 0.44 | 2.08% |
| sq30 两个都留 | 805 | +5.66% | 0.49 | 2.19% |

均值回归的**边际贡献**（相对「只留趋势」）：

| `squeezeBbwPct` | 收益 | 夏普 | 回撤 |
|---|---|---|---|
| 10（出厂） | +0.14pp | −0.02 | +0.42pp |
| 16 | +2.52pp | +0.18 | +0.11pp |
| 20 | **+3.41pp** | **+0.24** | ±0.00pp |
| 24 | +2.03pp | +0.11 | −0.08pp |
| 30 | +2.82pp | +0.16 | +0.03pp |

用同一把尺子：我判动态权重无效、判 §5.9 里的均值回归「测不出贡献」，用的都是
「差值在 ±1pp 内、正负交替」这条噪音带。**这里的 +2.0 ~ +3.4pp 在整片高原上同号、
是噪音带的两三倍以上，而且不以回撤为代价** —— 按同一判据，这次是能测出来的。

#### 这个结果不能证明什么

- **不是「标定完成」。** 只标了 R1–R4 中的一个参数有效维度（R3），
  `combine` 的四个数、`macd`/`adx`/`regime`/`risk` 全块、`downtrendBuyPenalty` 都还没标。
  `ENGINE_VERSION` 仍带 `-unvalidated`，**没有任何数值被写回 `src/core/params.ts`** ——
  那是人的动作（ADR-0003 / docs/07 §3 ⑤）。
- **均值回归单独跑仍然不赚钱。** 35 组候选里，只开均值回归的**训练集总收益全是负的**
  （−0.43% ~ −2.93%），唯一的正数出现在 18 个月的验证窗口里。
  它的价值体现在与趋势的组合上，不体现在自己身上 —— 这与「它该在 `RANGE` 里补位」的设计一致，
  但也意味着**不能单独把它作为一个策略推给用户**。
- **样本仍是那 39 只、仍带幸存者偏差**（§5.4），同期基准 +14.10%，`sq20` 的超额仍是 **−7.85%**。
  这一层改的是「引擎内部哪个参数错得离谱」，不是「这套策略跑赢了市场」。
- **测试集的纪律被削弱了一点，要如实记下**：docs/07 §3 ④ 要求测试集只跑一次。
  实际上每次 `--grid` 调用都会对该次的优胜者跑一遍测试窗口，而我为了处理「最优点落在网格边界」
  跑了两轮网格 × 两种基线 = 四次。四次的测试集读数分别是
  +1.46% / 57 笔（只开均值回归 · sq20）、+1.24% / 56 笔（同 · sq16）、
  +2.77% / 131 笔（完整引擎 · sq20 rv5 md1）、+2.79% / 123 笔（完整引擎 · sq30）。
  **扩网格的决定是照着验证集的单调性做的，不是照着这些数字做的**，但「只跑一次」这条已经不成立，
  上面的结论应当只按训练集 + 验证集来读。出厂 sq10 的测试窗口数字**没有跑**，
  正是为了不让它变成一个选择依据。

#### 三个混淆项的收口状态

- (a) R1–R4 从未标定：**已解决**，且答案是「问题出在 R3」。
  附带查清 R2 对参数惰性、R1 近乎惰性 —— 这两条是**结构问题，标定解决不了**。
- (b) 票数线不对等：§5.9 已排除。
- (c) 冲突裁决「压掉双方」vs「弱侧让路」：**仍在**。代价还变大了：
  459 + 491 = 950 → 762，`sq20` 下有 **188 笔**被裁决压掉（出厂 sq10 时是 89 笔）。
  结果照样变好，说明被压掉的不全是好交易，但这一层依然没被单独测过。
  → **已在 §5.12 测完，出厂机制是对的。** 另外这里的 188 笔是个错数
  （消融差值把三件事算成了一件），单独关掉裁决测出来是 128 笔。

#### 拍板（2026-08-12）：写回 20，后缀留着

人工复核后 `strategy.squeezeBbwPct` 由 10 改为 **20**，`ENGINE_VERSION`
`0.2.4-unvalidated` → `0.2.5-unvalidated`，依据与失效条件记入 [CHANGELOG](../../CHANGELOG.md)。
**这是本项目第一个被标定过的出厂参数。**

取 20 而不是工具按验证集 Calmar 排出的首选 30：30 只在完整引擎口径下领先（0.17 vs 0.08），
在只留均值回归的口径下明显更差（0.16 vs 0.36），说明它靠的是与趋势策略的交互而非 R3 本身变准 ——
两个口径分歧时取两段都靠前的那个值，证据更厚。

**`-unvalidated` 后缀没有摘。** 原先 M2 清单 4.9 把「写回参数」与「摘后缀」绑成一条，
那是按「整套参数一次标定完」写的；实际是一个一个标定的。此刻摘后缀会让回测报告
不再打「参数未标定」提示（`src/backtest/report.ts` 的 `unvalidatedParams`、清单 5.4），
等于用一个参数的证据给另外二十来个转述值背书。清单 4.9 因此拆成
**4.9a 部分写回**（递增补丁位、保留后缀、`params.ts` 顶部维护一张逐项的「已标定」清单）与
**4.9b 标定完成**（改 `0.3.0`、删掉 `params.test.ts` 的后缀断言）。

#### 建议的下一步（按性价比排序）

1. **R2 的结构**：它对唯一的参数惰性，等于四个子信号里有一个不可调。
   要么接受、要么按 docs/04 §3.2 重新论证它的触发条件 —— 不属于标定的范畴。
   → **已做，见 §5.11。结论比预期难看：口径确实错了、改了，但绩效没变好，
   而且真正的约束被证明在组合层的票数线上。**
2. 混淆项 (c)：冲突裁决现在压掉 188 笔，值得单独做一次消融。
   → **已做，见 §5.12。出厂机制（压掉双方）正确，弱侧让路在每个维度上都更差；
   顺带修正：裁决的实际作用量是 128 笔，不是 188 笔。**
3. 其余参数块的标定（`combine` 四个数、`downtrendBuyPenalty`、`adx`/`regime`）。
   **§5.11 给这一项加了一个必测维度：`voteThreshold.meanReversion`；
   §5.12 给 `conflictBand` 划出了取值区间（机制已验证，取值待三段切分）。**
4. **超额为负这件事**：`sq20` 全期 +6.25%，同期基准 +14.10%，超额 −7.85%。
   前面所有标定都是在「相对自己的其他参数」这把尺子上比的，没有一条回答过
   「这套策略比买入持有好吗」。它可能推翻的是策略本身而不是某个参数，量级最大。

---

### 5.11 修 R2 的口径（2026-08-12）：错找到了、改了，但绩效不动，约束在组合层

§5.10 附带查出「R2 对 `revertLookback` ∈ {2,3,5} 逐位惰性」。这一节是它的收口。
新增诊断工具 `pnpm audit:subsignals`（`src/backtest/subsignal-audit.ts`）：
它按子信号 ID × 方向统计触发数，并对 R2 把三项合取**逐项拆开**计数、
统计「只差一项」的近失数与「②③ 都成立时，最近一次在轨道外距今几根」的分布。

> 工具的诚实性保证：近失分析必须重算 `evaluate()` 在条件不成立时不会发布的合取项，
> 因此工具里复制了一份判定逻辑。`main()` 里有一条硬自检 ——
> **重算的 R2 触发数与 `evaluate()` 实际产出的不相等就直接报错退出**，不是打个 warning。
> 一旦 `strategies/mean-reversion.ts` 改了而工具没跟上，先修工具再看结论。

#### 读数 1：三项合取里，一项形同虚设，一项永远等不到

39 只 × 2018-01-01→2026-08-11，61534 根判定日，出厂 `revertLookback = 3`：

| 合取项 | 单独成立的根数 | 占比 |
|---|---|---|
| ① 近 3 日曾 `close < LOWER` | 3168 | 5.1% |
| ② `close > MID` | ~30100 | **49%** |
| ③ `HIST` 转正（过零翻转） | 2646 | 4.3% |
| ②③ 同时 | 1529 | 2.5% |
| ①②③ 同时（= R2 实际触发） | **45** | **0.073%** |

（BUY 方向的数；SELL 方向同构，两方向合计 90 次 / 0.15%。）

**② 成立于一半的交易日 —— 放在合取里等于不存在。** 而它一旦不是约束项，
`revertLookback` 也就没有东西可约束：把窗口从 2 放宽到 5，只是让本就不稀缺的 ①
更容易成立，卡住结果的 ③ 与窗口无关，于是回测逐位相同。**这是惰性的机制**，
不是「窗口取值不敏感」。

#### 读数 2：③ 与 ① 的时间常数差一个量级

②③ 都成立的 1529 根上，「最近一次收盘在轨道外距今几根」的分布：

| 距今 | ≤2 根 | ≤4 根 | ≤10 根 | ≤30 根 | 60 根内没有 |
|---|---|---|---|---|---|
| 累计占比 | 2.9%（45 根） | 10% | ~22% | 40% | 19–26% |

`d ≤ 2` 的 45 根**正好等于** R2 的实际触发数（`existsWithin` 含当根，
窗口 3 的有效回溯是 2 根）—— 分解与 `evaluate()` 对得上。
中位数落在 **35–40 根**：`HIST` 过零就是 `DIF` 上穿 `DEA`，是个慢事件，
而 `revertLookback` 是 2–5 根。**两个条件在时间尺度上凑不到一起，
任何 `revertLookback` 取值都救不了它** —— 这确认了「标定解决不了」。

#### 改法与效果

改成 **曾在轨道外（窗口）+ 今日穿越中轨（事件，`crossUp`/`crossDown`）+
`HIST` 朝有利方向（软确认，不要求过零）**。论证写在 [docs/04 §3.2](../04-指标与信号引擎.md)。

| | 触发数 | 占判定根 | `revertLookback` |
|---|---|---|---|
| 旧口径 | 90 | 0.15% | 惰性（2/3/5 逐位相同） |
| 新口径 | **213** | **0.35%** | **活的**：3 → 213、5 → 592、11 → 1807 |

`crossUp(close, MID)` 自身在 5.7% 的判定日成立 —— 从「49% 的伪条件」变成了真正的约束项。

#### 但绩效没有变好

全期（39 只，2018-01-01→2026-08-11，出厂 `0.60 / 3 / 2`，`sq20`）：

| 口径 | 交易 | 总收益 | 夏普 | 最大回撤 |
|---|---|---|---|---|
| 旧 R2 | 762 | +6.25% | 0.573 | 2.16% |
| 新 R2 | 769 | +5.93% | 0.543 | 2.44% |

差 −0.32pp 收益 / −0.03 夏普 —— 按本报告一直用的那条噪音带（±1pp、正负交替）读，
**这是「测不出差别」，不是「改坏了」**。更说明问题的是网格：
`params/grid-r2-lookback.json`（`revertLookback` ∈ {3,5,8,12,20}，其余固定）
的五个候选，**验证集 Calmar 全是 0.084、验证集交易数全是 127**，
训练集交易数 489/485/485/485/486。参数活了，输出还是不动。

#### 为什么不动：R2 与其余三个子信号几何互斥，凑不够票

工具的共现统计（新口径 213 次触发）：

| | 次数 | 占比 |
|---|---|---|
| 自身 `score ≥ 0.5`（够一票） | 213 | 100% |
| 同一根上有同方向的其他均值回归子信号 | 28 | 13% |
| —— 其中陪跑者是 `R3_SQUEEZE` | 28 | 全部 |
| **孤立触发（没有任何同方向同伴）** | **185** | **87%** |

原因是条件在几何上互斥：**R2 要求价格在中轨上**，而 R1 / R3 要求当根触外轨、
R4 要求 `|close − MID| ≥ midReversionStd × STD`。同一根上不可能既在中轨附近
又深度偏离中轨。那 28 次例外全是 R3，因为 R3 用的是当根**最高/最低价**而非收盘价 ——
长影线可以同时满足「收在中轨上」与「最低价触下轨」。

而 `combine.voteThreshold.meanReversion = 2` 要求同策略两票。
**R2 在结构上无法自己促成一次触发**，它 0.30 的权重只能给得分一点微调。
所以 R2 改得再准，也要等票数线让它有意义 —— 约束从子信号层转移到了组合层。

#### 这一节的处置

- **改写留下。** 理由是口径正确（穿越是事件，与 §2 的穿越纪律一致）+ 参数从死变活，
  **不是它更赚钱**。`ENGINE_VERSION` → `0.2.6-unvalidated`（口径变更，非参数标定），
  记入 [CHANGELOG](../../CHANGELOG.md)。
- **`voteThreshold.meanReversion` 进入 §5.10 下一步第 3 项（`combine` 块标定）的必测维度**，
  不单独为它开一轮 —— 它本来就是那一块的参数，单独测会再触碰一次测试窗口。
- **R1 的惰性不改。** R1 的两个条件各自都是真约束（不像 R2 的原 ②），
  惰性来自交集小，属于「这条规则本来就罕见」，不构成口径错误。
- 遗留：`sensitivityFlags()` 仍未接进 CLI —— 若它已接上，R2 的逐位相同本该在
  §5.10 那一轮就被自动标出来，而不是靠人去比较两份 JSON。

### 5.12 混淆项 (c) 收口（2026-08-12）：冲突裁决「压掉双方」是对的，弱侧让路每个维度都更差

§5.7 起挂了三个混淆项，(a) 与 (b) 已解决，(c)「冲突时压掉双方 vs 弱侧让路」一直没测。
这一节把它测完。

**不需要改代码**：`adjudicate()` 判矛盾的条件是 `gap < conflictBand`，
把 `conflictBand` 设为 0 会让它恒假，行为**恰好等于**「取 final 较高者」——
也就是弱侧让路。于是这个机制之争可以直接用一次参数扫描回答。

全期 39 只、2018-01-01→2026-08-11、`sq20` + 新 R2、其余出厂：

| `conflictBand` | 交易 | 总收益 | 夏普 | 最大回撤 | 胜率 | 交易成本 |
|---|---|---|---|---|---|---|
| 0.00（= 弱侧让路） | 897 | +5.67% | 0.473 | 2.50% | 32.8% | 48966 |
| 0.05 | 847 | +5.72% | 0.497 | 2.47% | 33.3% | 45680 |
| **0.15（出厂）** | **769** | **+5.93%** | 0.543 | 2.44% | 33.2% | 42067 |
| 0.30 | 689 | +5.56% | 0.572 | 2.13% | 33.8% | 36485 |
| 0.50 | 679 | +5.82% | **0.596** | 2.13% | 33.9% | 36214 |
| 0.80 | 679 | +5.82% | 0.596 | 2.13% | 33.9% | 36214 |

#### 读数 1：弱侧让路更差，而且是全维度更差

出厂 0.15 相对 band = 0：少 128 笔交易，收益 **+0.26pp**、夏普 **+0.070**、
回撤 **−0.06pp**、手续费 **−6900**。没有任何一个维度支持「让弱侧让路能救回一些交易」。
**多空得分几乎相等时，那一笔本来就不该做** —— 文档里那句「此时提醒用户买入是不负责任的」
现在有实测支持了。

#### 读数 2：机制在 0.50 处饱和，所以夏普的上升不是「少交易」的代理

0.50 与 0.80 **逐位相同**：897 − 679 = **218 笔**是被裁决触及的全部交易，
band ≥ 0.5 时全部压掉，再放宽没有东西可压。这条饱和是这一节里最该记住的 ——
夏普从 0.473 单调升到 0.596，如果它只是「交易越少越稳」的假象，那么继续放宽 band
应该继续变好，但它到顶了。同时收益不是单调的（0.15 最高、0.30 回落、0.50 又回升），
0.15 与 0.50 的收益差 0.11pp 在噪音带内。

#### 读数 3：顺带修正一个此前的数字

§5.10 写过「`sq20` 下有 188 笔被裁决压掉」，那是从 `459 + 491 = 950 → 762` 推出来的差值，
**把三件事算成了一件**：裁决压掉的、两个策略得分相加后新触发的、以及单标的单持仓约束挤掉的。
直接测出来的裁决口径是 **128 笔**（897 → 769，新 R2 口径下）。
教训与 §5.11 同源：**消融差值不等于某个机制的作用量**，要测这个机制就得单独关掉它。

#### 这一节不能证明什么

- **不是「`conflictBand = 0.15` 已标定」。** 这是全区间跑的，没走 docs/07 §3 的三段切分，
  而且 0.15 是出厂值 —— 拿全期数据说「出厂值最好」正是标定流程要防的那种论证。
  证的是**机制方向**（压掉双方 > 弱侧让路，且效应有界），取值仍归 `combine` 块那一轮。
- 样本仍是那 39 只、仍带幸存者偏差，超额仍为负。

#### 三个混淆项的最终状态

- (a) R1–R4 从未标定：**已解决**（§5.10，问题出在 R3 的压缩线）。
- (b) 票数线不对等：**已解决**（§5.9）。
- (c) 冲突裁决：**已解决**（本节，出厂机制正确）。
  §5.11 又新挂了一条待测：`voteThreshold.meanReversion = 2` 让 R2 无法参与投票。
  它不是混淆项，是一个明确的标定维度，归 `combine` 块。

### 5.13 「超额为负」先别急着推翻策略（2026-08-12）：平均资金占用只有 4.15%

§5.10 把「全期超额 −7.85%」列为量级最大的待查项，说它「可能推翻的是策略本身」。
查了。**在现在这个口径下，这个数不能用来判定策略好坏**，因为分母不是同一回事。

新 R2 + `sq20` 全期（39 只，2018-01-01→2026-08-11，2088 个交易日，初始资金 390 万）：

| | 策略 | 基准（沪深300 满仓） |
|---|---|---|
| 总收益 | +5.93% | +14.10% |
| 年化 | +0.67% | +1.55% |
| 最大回撤 | **2.44%** | **45.60%** |
| Calmar（年化 ÷ 最大回撤） | **0.275** | 0.034 |
| **平均资金占用** | **4.15%** | 100% |
| 超额 / 信息比率 | −8.17pp / −0.143 | — |

**策略平均只用了 4.15% 的资金**（持仓市值 × 持仓日数 ÷ 初始资金 × 交易日数，按建仓价计价）。
拿一个几乎全程空仓的策略去比满仓指数，−8.17pp 里有多少是「策略不行」、
多少是「钱根本没投进去」，光看超额一个数分不出来。同一张表里回撤是 2.44% vs 45.60%，
Calmar 是 0.275 vs 0.034 —— **风险调整后方向是反过来的。**

#### 但也不能反过来吹

按占用资金折算，8.56 年 +143%（年化约 10.8%）。**这个数不许拿去说事**，三条理由：
- 折算隐含「闲置的 95.85% 资金可以另作他用」，而策略并不能提前告诉你什么时候会用到它；
- 回测里闲置资金按 0 收益计，折算等于把这部分机会成本一笔勾销；
- 分母小 24 倍，噪音同样被放大 24 倍 —— 折算后的数字比原数字更不稳。

#### 这解释了一件一直没解释的事

前面所有消融的差值都在 ±1pp 这条噪音带里打转（动态权重、均值回归贡献、R2 改写）。
**那条带子是在总资金口径上的**：4.15% 的占用意味着总资金口径把所有差异压扁了约 24 倍。
这不改变任何排名（分母对所有候选相同），但说明「差值只有 0.5pp 所以无关紧要」
这种读法是错的 —— 该看的是差值相对同口径其他差值的大小，本报告一直用的
「正负交替 = 噪音」判据没有这个毛病，继续用它。

#### 处置

- **报告里加了 `performance.exposure`**（`src/backtest/metrics.ts` 的 `averageExposure()`），
  文本报告中**紧挨着超额那一行**打印，并注明「基准为满仓 100%」。
  这不是新指标，是让一个已有指标不再被误读的必要上下文。
- docs/07 §「输出指标」补了口径说明。
- **`--benchmark` 的语义没变**，仍是沪深300 满仓买入持有。要做同占用率的基准对照
  （或「每单位持仓时间的收益」这个更贴合提醒工具的口径）是另一件事，未做。
- **扩标的池仍然要做**：这一节回答的是「超额为负能不能证明策略失败」（不能），
  没有回答「这套策略有没有用」。39 只的幸存者偏差、样本量、以及只有一个 8.5 年区间
  这三条都没变。

---

### 5.14 标定 `combine` 块（2026-08-12）：四个参数一个也定不下来，但查出了标定工具的两处测量缺陷

`params/grid-combine-ofat.json`，OFAT 16 个候选（中心 = 出厂值，四个维度各自单独变），
39 只 × 2018-01-01 → 2026-08-11，三段切分同 docs/07 §3。

**这一轮的直接结论是否定的：`combine` 块的四个参数，没有一个能从这份数据里定出值来。**
但过程里查出了标定工具本身的两处测量缺陷，那部分是确定的收获。

#### 先说工具：分段绩效的 `bars` 里混进了预热前缀

`simulateCode` 对喂进去的**每一根** K 线都 push 一个净值点，包括 `split.from` 之前的预热段
（`warmupForSplit` 故意把段前历史全喂进去，见 §5.4）。`runSplit` 又把整条曲线交给
`performanceOf`，于是验证集的 `bars` 是 **1816**（数据起点 → 2025-06-30）而不是本段的 **359**。
后果按严重程度排：

| 指标 | 受影响 | 验证集实测（修正前 → 修正后） |
|---|---|---|
| `benchmarkReturn` / `excessReturn` | **完全错** | **−3.70% → +16.23%**（差 19.9pp，**连符号都反了**） |
| `annualized`（及由它算的 Calmar / `score`） | 压小 `bars` 倍 | 0.17% → **0.87%**（5.1×）；Calmar 0.084 → **0.427** |
| `sharpe` | 被一串 0 收益稀释 ≈ ×√(本段/全长) | 0.274 → **0.618**（预测 0.617） |
| `totalReturn` / `maxDrawdown` | 不受影响 | 1.28% / 2.03% 不变（预热段净值恒等于初始资金） |

`benchmarkReturn` 那一行最离谱：验证集拿的是**沪深300 在 2018→2025-06 全程**的涨跌，
去减一个只赚了 18 个月的策略收益。

**段内排名不受影响**（同一段里每个候选的 `bars` 相同），16 个候选按修正口径重排后
名次逐位相同 —— 所以下面的读数仍然成立。**但跨段的「Calmar 衰减 > 50%」红线是坏的**：
这一轮它对 3 个候选报了过拟合（`conflictBand` 0.25 / 0.40，`scoreThreshold` 0.72），
修正后这三个的衰减分别是 **−68% / −43% / −78%**，也就是验证集比训练集**更好**，三个全是假阳性。

**已修**：`runSplit` 把净值曲线按 `point.date >= split.from` 切回本段再算绩效。
修完用单候选网格实跑验证过，上表右列就是实跑数（不是推算）。

> **没有完全消掉的部分**：训练集的 `from` 就是数据起点，切了也还是 1457 根，
> 其中头 300 根是预热死区（占 21%）。这是训练窗口的真实属性（那 300 根里策略确实没法交易），
> 不是记账错误，所以不改。但它仍然让训练集的 Calmar 系统性偏低 ≈1.26 倍，
> 方向是**让衰减红线更不容易触发**。跨段比 Calmar 时要知道这一层。

#### 再说工具：没有任何一条红线拦得住「训练集亏钱、验证集赚钱」

`voteThreshold = {trend:3, meanReversion:1}` 这个候选，训练集 **1047 笔 / −3.73% / 夏普 −0.29**，
验证集 **359 笔 / +2.77% / Calmar 0.539**，按验证集 Calmar 排**第 2**。
三条红线一条都不拦：训练集交易数 1047 远超 30；验证集年化为正；衰减红线的判据带
`trainScore > 0` 前置，训练集 Calmar 是负的，判都不判。

排名用验证集本身是对的（用训练集排名等于直接过拟合），但它有这个盲区。
**已加第四条红线**：训练集 Calmar ≤ 0 直接淘汰，且不跑验证集
（`calibrate.ts`，测试见 `report-calibrate.test.ts`「训练集年化为负的候选直接淘汰」）。

#### 16 个候选（Calmar 按修正口径重算，训练集 1457 根 / 验证集 359 根）

| 得分线 | 票数线 趋势/均值 | 冲突带 | 下跌折价 | 训练笔 | 训练收益 | 训练Calmar | 验证笔 | 验证收益 | 验证回撤 | 验证Calmar |
|---|---|---|---|---|---|---|---|---|---|---|
| 0.6 | **4/2** | 0.15 | 0.5 | 340 | 0.40% | 0.029 | 59 | 1.58% | 1.20% | **0.887** |
| 0.6 | **3/1** | 0.15 | 0.5 | 1047 | **−3.73%** | −0.084 | 359 | 2.77% | 3.47% | 0.539 |
| **0.55** | 3/2 | 0.15 | 0.5 | 491 | 0.96% | 0.065 | 136 | 1.37% | 1.79% | 0.517 |
| **0.5** | 3/2 | 0.15 | 0.5 | 484 | 1.17% | 0.079 | 129 | 1.21% | 1.62% | 0.505 |
| 0.6 | 3/2 | 0.15 | 0.5 | 489 | 1.33% | 0.090 | 127 | 1.28% | 2.03% | 0.427 |
| 0.6 | 3/2 | **0.10** | 0.5 | 514 | 1.60% | 0.107 | 127 | 1.28% | 2.03% | 0.427 |
| 0.6 | 3/2 | 0.15 | **0.25** | 489 | 1.33% | 0.090 | 127 | 1.28% | 2.03% | 0.427 |
| 0.6 | 3/2 | 0.15 | **0.75** | 489 | 1.33% | 0.090 | 127 | 1.28% | 2.03% | 0.427 |
| 0.6 | 3/2 | 0.15 | **1.0** | 499 | 1.14% | 0.077 | 127 | 1.28% | 2.03% | 0.427 |
| 0.6 | 3/2 | **0.05** | 0.5 | 528 | 1.32% | 0.089 | 139 | 1.19% | 2.18% | 0.370 |
| **0.72** | 3/2 | 0.15 | 0.5 | 402 | 1.72% | 0.159 | 121 | 1.25% | 2.37% | 0.356 |
| **0.66** | 3/2 | 0.15 | 0.5 | 488 | 1.45% | 0.097 | 127 | 1.06% | 2.11% | 0.340 |
| 0.6 | 3/2 | **0.40** | 0.5 | 441 | 2.33% | 0.179 | 107 | 0.92% | 1.93% | 0.322 |
| 0.6 | 3/2 | **0.25** | 0.5 | 459 | 1.96% | 0.141 | 111 | 0.85% | 1.93% | 0.299 |
| 0.6 | **3/3** | 0.15 | 0.5 | 194 | 0.49% | 0.069 | 77 | −0.08% | 1.94% | −0.028 |
| 0.6 | **2/2** | 0.15 | 0.5 | 1645 | **−5.07%** | −0.110 | 503 | −1.39% | 4.04% | −0.233 |

（加粗的是该行相对中心改动的那一个维度。中心行 = 出厂值 0.6 / 3-2 / 0.15 / 0.5。）

#### 逐维读数

**① `downtrendBuyPenalty` 是惰性的 —— 这是第三次遇到同一种病。**
0.25 / 0.5 / 0.75 三个值**训练集与验证集逐位相同**（489 笔 / +1.33% / 127 笔 / +1.28%）。
只有 1.0（= 不折价）动了训练集（499 笔 / +1.14%），而验证集**连 1.0 都不动**。
也就是说这个参数实际是个二值开关（折不折价），**幅度完全没有作用**，
而且连那个二值差别都传不到验证集。

根因与 §5.11 是同一个：下跌趋势里的均值回归**买入**几乎从不是**单独**的触发者
（R2 有 87% 的触发是孤立的，而孤立的均值回归子信号凑不够两票）。折价作用在一个
「本来也不决定成败」的得分项上，所以调它的幅度没有可观测后果。

**这不构成删掉它的理由。** docs/04 §4.1 的「别接飞刀」是方向级抑制的原则，
标定说的只是「在这 39 只 / 这个窗口 / 当前票数线下测不出差别」。出厂值保持 0.5。

**② `voteThreshold.meanReversion = 1`（§5.11 挂下来的那个问题）：能让 R2 参与，但那些交易在训练集上是亏的。**
票数线降到 1 之后交易数从 489 → **1047**（翻倍多），这印证了 §5.11 的判断 ——
约束确实在票数线上，R2 那 185 次孤立触发在 `= 1` 时全都能自己成事。
但多出来的 558 笔把 6 年训练集从 +1.33% 打成 **−3.73%**、夏普 0.195 → −0.29。
验证集的 +2.77% 是那 18 个月正好友好，不是稳健性。**不采用。**
（也正是这个候选暴露了上面第二条工具缺陷。）

另一端 `= 3` 同样不行：训练集 194 笔 / +0.49%，验证集直接转负 −0.08%，被红线淘汰。
**所以 `meanReversion = 2` 是这三个取值里唯一站得住的，出厂值不动。**

**③ `voteThreshold.trend = 4` 是名义优胜者，但不建议写回。**
验证集 Calmar 0.887，是第二名的 1.6 倍。但同一行的训练集是 **340 笔 / +0.40% / Calmar 0.029** ——
在 14 个非负候选里几乎垫底（中心是 0.090）。**训练集与验证集给出相反的方向**，
而验证集那一侧只有 **59 笔**交易撑着。这正是「验证窗口正好友好」的形状，
只是它没触发上面新加的红线（训练集是正的，只是很小）。
docs/07 §3 的「参数敏感性」红线本来就是拦这个的：3（0.427）→ 4（0.887）是孤峰不是高原，
而 2（`2/2` 那行）是 −0.233。**孤峰不采用。**

**④ `conflictBand`：§5.12 的「越宽越好」是训练窗口的假象，三段切分把它反过来了。**
训练集 Calmar 随带宽单调上升（0.05→0.089、0.10→0.107、0.15→0.090、0.25→0.141、0.40→0.179），
**验证集反过来**（0.370 / 0.427 / 0.427 / 0.299 / 0.322）。0.10 与 0.15 的验证集逐位相同，
区分不开；0.25 与 0.40 在验证集上明显更差。
§5.12 那张全区间表当时就写了「证的是机制不是取值」—— 这一轮证实了那句谨慎是必要的。
**机制结论（压掉双方 > 弱侧让路）不变，取值仍定不下来，保持 0.15。**

**⑤ `scoreThreshold`：训练集与验证集的梯度方向相反，没有高原。**
训练集偏好高（0.5→0.079 … 0.72→0.159），验证集偏好低（0.55→0.517 最高，0.66→0.340 最低）。
两边打架、且验证集上 0.5/0.55/0.6 的差距（0.505 / 0.517 / 0.427）小于同一参数换个冲突带
就能造出的差距。**读作噪音，保持 0.6。**

#### 为什么整块都定不下来（不是「网格设计得不好」）

三个可叠加的原因，都不是靠加密网格能解决的：

1. **测量对象只有总资金的 4%**（§5.13）。所有候选的验证集收益都挤在 −1.4% ~ +2.8% 之间，
   而这是**总资金口径**；折到实际占用的资金上差别会放大约 24 倍，但噪音同样放大。
2. **`combine` 是二阶旋钮。** 到目前为止唯一测出一阶效果的参数是 `strategy.squeezeBbwPct`
   （子信号的门），它决定「有没有信号」；`combine` 的四个数只决定「已有的信号怎么合」。
   §5.11 的 87% 孤立率说明这一层的实际自由度比参数个数小得多。
3. **验证集只有 359 根 / 59–503 笔。** 18 个月一个市场状态，`trend = 4` 那行 59 笔就能排第一，
   本身就说明这个窗口分辨不出这个量级的差异。

#### 处置

- **`combine` 块四个参数全部保持出厂值**（0.6 / {3,2} / 0.15 / 0.9 / 0.5），
  `ENGINE_VERSION` 不动 —— 没有写回，就没有要记的标定依据（ADR-0003 / docs/07 §3 ⑤）。
- **两处工具缺陷已修**（净值切段 + 训练集红线），带测试。**这两处修的是测量，不是参数**，
  所以不占「写回出厂参数」的流程（params/README「四件事」）。
- **测试窗口这一轮没有被读**：验证实跑用的是 `--to 2025-06-30`，测试段没有数据。
  网格本身跑出的 `test` 块（`trend=4` 的 57 笔 / +2.02%）**不作为结论使用** ——
  它的优胜者按上面 ③ 已被判为孤峰。累计触碰次数按 docs/07 §3 ④ 记：**5 次**。
- **`adx` / `regime` 块的标定（`params/grid-regime.json`，21 组）必须在修完的工具上跑**，
  修之前跑出来的分段 Calmar 和衰减标记都不能用。

---

### 5.15 重写标定工具（2026-08-13）：把判据搬进工具，并换掉逐折统计量

#### 为什么重写：十一轮标定，工具报了十一个优胜者，人否掉了十个

到 §5.14 为止，工具的输出是「按验证集 Calmar 排序的候选表」，**推荐 = 第一行**。
但真正的判据一直在人脑子里，而且每一轮都用上了：

| 轮次 | 工具报的第一名 | 人否掉的理由 |
|---|---|---|
| §5.10 | `squeezeBbwPct` 某个值 | 最优点落在网格边界 → 扩网格重跑，才找到 16–30 的高原（**唯一写回的一次**） |
| §5.11 | `revertLookback` 五个候选 | 验证集逐位相同 —— 参数是惰性的，排第一没有意义 |
| §5.14 ③ | `voteThreshold.trend = 4`（0.887，第二名的 1.6 倍） | 训练集垫底 + 验证集只有 59 笔 + 3→4 是孤峰不是高原 |
| §5.14 ② | `voteThreshold.meanReversion = 1`（排第 2） | 训练集 −3.73% —— 这一条后来变成了工具的第四条红线 |

**判据留在人脑子里，就只有人有空的那一轮能推进。** 十一轮、二十来个参数、一个写回，
不是因为参数都定不下来，而是因为每一轮的产出都是「一张要人再判一次的表」。
这一轮把上面四种理由全部变成工具里的代码。

#### 改了什么

| 改动 | 说明 |
|---|---|
| **折单元** | 判定窗口切成 `--code-folds` 个标的子集 × `--time-slices` 个时间片（默认 4 × 3 = 12 折）。折是从**同一次模拟**里切出来的，`--code-folds 8` 不多跑一次回测 |
| **配对 Δ** | 每个候选与出厂值**逐折相减**，报 Δ 均值 / 标准误 / t / 胜折数。出厂值那一行由工具自动注入（`queue.unshift({})`），不必写进网格 |
| **三态裁决** | `WRITE_BACK` / `KEEP` / `INCONCLUSIVE`，印在报告最上面。`KEEP` **是结论不是失败** |
| **自动标记孤峰 / 边界** | OFAT 轴上左右邻居的绩效断崖 → 「孤峰」；候选落在网格端点 → 「该侧邻域未测」。§5.14 ③ 那次人工判断现在是机器判的 |
| **分辨率** | `KEEP` 时报「要分辨已观测到的这个 Δ 大约需要多少折」，据此决定扩样本还是归档为不可标定（清单 4.9c） |
| **`--touch-test` 默认关** | 测试集要显式加开关才读，每次按 docs/07 §3 ④ 记账 |
| **退出码** | `0` = WRITE_BACK 或 KEEP，`2` = INCONCLUSIVE，`1` 是真的失败 |

标的按代码升序**轮转**分组，不是切段 —— 切段会让某一折全是创业板。

#### 逐折统计量：Calmar 不能按折算，换成本折总收益

第一版用「本折 Calmar」当逐折得分，跑完 `combine` 才发现这个统计量在折的粒度上是坏的。
两处硬伤，都是实测：

**① 分母塌了。** 出厂值 12 折的 Calmar 是
`−1.97, 13.53, 1.25, −0.80, 3.41, 5.83, −0.21, −1.42, 17.67, −1.50, −1.80, −0.97`。
一折三四十只标的、一百多根 K 线，最大回撤经常只有零点几个百分点，
年化除下去就飞到 13 / 17。**折间量纲差一个数量级，均值与标准误都被两三折支配。**
`trend = 4` 的逐折 Δ 是 `0.12, −1.39, 0.73, —, 0.30, −3.59, 4.56, —, −9.00, −0.18, −0.10, 16.53`：
均值 +0.796 全部来自最后一折的 +16.53 和第九折的 −9.00。

**② 候选不交易的折被静默丢掉 —— 而那正是它赢的折。** 上面那两个 `—`：
`trend = 4` 在第 4、第 8 折一笔没做，最大回撤为 0，Calmar 无定义，
于是配对比较在 **10 折**而不是 12 折上做（`cells: 10`）。
但这两折出厂值是**亏的**（−0.15% / −1.49%），「不出手」在这两折上是候选的胜利。
**把「候选没动作」的折丢掉，等于系统性地惩罚更保守的候选。**

换成**本折总收益**后同两折变成 `0.00000`，Δ = +0.149pp / +1.493pp，正常计入。
新口径下 `trend = 4` 的逐折 Δ 是
`+1.31, −0.48, +0.25, +0.15, −0.41, −1.86, +0.92, +1.49, −1.30, −0.55, +0.06, +1.69`（pp）——
没有哪一折支配结果，胜 7/12。

**这不是把结论改得好看了，是把两个结论的符号改回来了。** 同一份数据、同一批候选，
换统计量之后有两个候选的 Δ **翻了符号，且翻成与整池排名一致**：

| 候选 | 整池验证 Calmar | 逐折 Calmar 口径 Δ | 逐折总收益口径 Δ |
|---|---|---|---|
| `scoreThreshold = 0.55` | **0.517**（优于出厂 0.427） | **−0.599**，胜 1/12 | **+0.05pp**，胜 2/5 |
| `scoreThreshold = 0.72` | **0.356**（劣于出厂 0.427） | **+0.567**，胜 6/12 | **−0.01pp**，胜 4/9 |

旧口径下「整池说好、配对说坏」两次都出现，方向还相反 —— 那是分母塌了的症状，不是真发现。

> **合成用例里最容易把这一层测糊。** `pairedDelta` 在折数 < 2 **或方差为 0** 时
> `stderr` 与 `t` 都返回 `null`，不是「无穷显著」。随手写「候选每折都恰好比出厂值高 0.10」
> 的 fixture，stderr 只剩浮点误差、t ≈ 6e15，用例是绿的但测的是 IEEE754。
> `calibrate-folds.test.ts` 里为此钉了两条：折间 Δ 必须真的抖一下，且 t 有**上界**。

#### `combine` OFAT 在新工具上重跑：**裁决 KEEP**，这次是机器判的

`reports/folds/combine-ofat-v2.json`，同一个网格（16 组 OFAT）、同一批 39 只标的、
判定窗口收到 `--to 2025-06-30`（测试段不跑），12 折。
出厂值：训练 489 笔 / +1.33% / Calmar 0.090，验证 127 笔 / +1.28% / Calmar 0.427。

| 候选（变动的那一维） | 整池验证 Calmar | Δ | 标准误 | t | 胜/动 | 工具标记 |
|---|---|---|---|---|---|---|
| `trend = 4` | 0.887 | +0.11pp | ±0.32pp | 0.3 | 7/12 | **孤峰**（下侧邻居 3 断崖 0.427 vs 0.887）+ 上边界未测 |
| `scoreThreshold = 0.55` | 0.517 | +0.05pp | ±0.08pp | 0.7 | 2/5（动 5/12） | 通过 |
| `scoreThreshold = 0.5` | 0.505 | −0.02pp | ±0.13pp | 0.1 | 3/7（动 7/12） | 下边界未测 |
| **出厂值** | **0.427** | 基准 | | | | |
| `conflictBand = 0.10` | 0.427 | +0.00pp | — | — | **动 0/12** | 通过 |
| `downtrendBuyPenalty` 0.25 / 0.75 / 1.0 | 0.427 | +0.00pp | — | — | **动 0/12** | 0.25 下边界 / 1.0 上边界 |
| `conflictBand = 0.05` | 0.370 | −0.03pp | ±0.03pp | 1.1 | 0/2（动 2/12） | 下边界未测 |
| `scoreThreshold = 0.72` | 0.356 | −0.01pp | ±0.22pp | 0.0 | 4/9（动 9/12） | 上边界未测 |
| `scoreThreshold = 0.66` | 0.340 | −0.07pp | ±0.11pp | 0.6 | 1/5（动 5/12） | 通过 |
| `conflictBand = 0.40` | 0.322 | −0.11pp | ±0.21pp | 0.5 | 5/10（动 10/12） | 上边界未测 |
| `conflictBand = 0.25` | 0.299 | −0.14pp | ±0.21pp | 0.6 | 4/9（动 9/12） | 通过 |
| `meanReversion = 3` | −0.028 | −0.44pp | ±0.32pp | 1.4 | 3/10 | **淘汰**：验证集年化为负 |
| `meanReversion = 1` | — | — | | | | **淘汰**：训练集 Calmar −0.08（§5.14 加的第四条红线） |
| `trend = 2` | — | — | | | | **淘汰**：训练集 Calmar −0.11 |

**结论与 §5.14 逐条相同，但这一轮没有一处需要人来判**：三条红线自己淘汰了三个候选，
孤峰与边界标记自己排除了名义第一名，剩下的没有一个 Δ 大于自己的抖动（最大 t = 1.4，
门槛是 2）。§5.14 里「训练集垫底 + 只有 59 笔 + 孤峰」那段人工论证，
现在被 `Δ +0.11pp ± 0.32pp, t = 0.3` 一行代替 —— **而且是更强的说法**：
「比第二名好 1.6 倍」那个头条本身就落在它自己的折间噪音里。

#### 「动了 N/12 折」把惰性说得比 §5.14 更准

新报的 `affected`（Δ ≠ 0 的折数）直接量出了参数的作用范围：

- `downtrendBuyPenalty` 的 0.25 / 0.75 / 1.0 与 `conflictBand = 0.10`：**动 0/12 折**，
  即在判定窗口内**逐折逐位相同**。§5.14 只能说「验证集整池数字相同」，
  现在能说「切成 12 个互不重叠的子样本，每一个都相同」—— 这不是效应小，是**没有效应**。
- `conflictBand = 0.05` 只动 2/12 折，`scoreThreshold = 0.55` / `0.66` 只动 5/12。
  胜率因此按 `affected` 算而不是按 12 算，否则「动了 2 折、赢 0 折」会被读成「胜 0/12」，
  看着像大败，其实只有两折有信息。

#### 分辨率那一行怎么读

报告末尾：**观测到的最大改进 Δ = 0.11pp，12 折下的可分辨门槛 0.64pp，
要让它可分辨约需 430 折。**

先看这个门槛是从哪来的 —— 出厂值自己 12 折的总收益（pp）：

```
−2.44  2.14  0.44  −0.15  4.66  3.05  −0.11  −1.49  2.73  −0.69  −2.14  −1.16
```

均值 +0.40pp，**折间标准差 2.25pp**。也就是说**换 10 只票、换半年行情，
同一套参数的结果就能差好几个 pp**，而整个 `combine` 块 15 个候选的动态范围是
**−0.44pp ~ +0.11pp**。效应量比折间抖动小一个量级 —— 这就是这一格十一轮定不下来的
数据侧原因，现在它有数了。

430 折不是「去凑 430 折」的施工单。它等价于说「在当前标的池 / 窗口 / 4% 的平均资金占用下，
`combine` 这一层的效应量比测量噪音小一个量级」。对应动作是 params/README 的第三档：
**归档为不可标定（清单 4.9c），不要再重跑同一个网格**。
真要推进只有两条路，都不在这一轮里：扩标的池（往两三百只走）、或换成占用率对齐的口径
让 4% 那个压扁因子（§5.13）消失。

#### 扩样本能不能解决？能，但代价与偏差要一起算

430 折 ÷ 12 折 ≈ **36 倍**。一折 ≈ 10 只 × 半年 ≈ 5 个「标的年」，
所以 430 折 ≈ 2150 标的年的判定窗口，而现在是 39 × 1.5 ≈ **58 标的年**。
折算成标的数：在同一个 18 个月窗口里要 **1400 只左右**。A 股全集 5542 只
（`params/universe-broad.json` 的 `universeSize`），所以这**不是做不到** ——
它是一个数据获取工程（现在 39 只 23MB，1400 只约 1GB、抓取要几小时）。

但三条必须一起说，否则「扩到 1400 只就能定参数」会变成一句错的承诺：

1. **横截面折之间不独立。** A 股同涨同跌，市场因子是所有折的共同项，
   `√n` 的收敛只在独立样本下成立。**430 折是下界**，真实需要更多。
2. **幸存者偏差不随样本量减小**（§5.4）。退市股在免费接口上取不到，
   扩到 1400 只仍然全是活着的票，方向仍是绩效被系统性高估。
3. **`combine` 是二阶旋钮**（§5.14 读数）。它只决定「已有的信号怎么合」，
   而 §5.11 测出 R2 有 87% 的触发是孤立的 —— 这一层的实际自由度比参数个数小得多。
   花几小时抓 1400 只票去分辨 0.11pp，性价比明显低于先去测那些**一个网格都没跑过**
   的一阶参数（`adx` / `regime` 的门、`risk` 的止损线）。

#### 还有一类参数，日线回测**原理上**给不出依据

docs/08 那一格的原话是「产出出厂默认参数」。但 `params.ts` 里有一批数值
回测永远走不到那条分支：

| 参数 | 为什么测不到 |
|---|---|
| `combine.provisionalDiscount` | 只在 `stage === 'PROVISIONAL'` 时生效（`combine/index.ts:85`），而回测喂的全是收盘定稿的 K 线，`engine.ts:74` 判出来永远是 `CONFIRMED` |
| `alert.bubbleScore` | 提醒分级的线，属 M3；回测不产生提醒 |
| `risk.lateBuyCutoffMinutes` | 盘中分钟口径，日线回测没有分钟 |
| `data.staleSnapshotMs` | 实时快照新鲜度，回测没有快照 |

这四个的归属不是「还没标定」，而是**要靠影子运行（docs/07 §2.3）或 M3 的提醒日志**
才可能有依据。把它们算进这一格，这一格就永远不可能勾掉 —— 所以 docs/08 那一条已经
按「回测能定的 / 回测定不了的」拆开重写。

#### 这一节不能证明什么

- **没有证明出厂的 `0.6 / {3,2} / 0.15 / 0.5` 是好参数。** 证明的是
  「在 39 只 × 18 个月这个测量精度下，网格里没有一个候选能被判定为更好」。
  这两句话差别很大，别混用。
- **`--code-folds` / `--time-slices` 的取值本身没有标定过。** 4 × 3 是拍的：
  折太少估不出离散度，折太薄每折没有交易。改这两个数会改变 `requiredCells` 的数值，
  但不会改变「效应量 ≪ 折间抖动」这个结论 —— 那个比值在任何切法下都是同一个量级。
- **`resolution.requiredCells` 是量级估计，不是样本量计算**：它假设 Δ 与离散度都不随样本变，
  且折间独立（上面刚说了不独立）。
- **判据变严了，代价是「真有效但幅度小」的参数也会被判 KEEP。** 这是有意的取舍：
  ADR-0003 的立场是宁可留着标着「未标定」的猜测，也不写回一个用噪音换来的数。

#### 处置

- **`combine` 块四个参数仍然全部保持出厂值**（0.6 / {3,2} / 0.15 / 0.9 / 0.5），
  `ENGINE_VERSION` 不动。§5.14 的结论不变，这一轮把它从「人看表判的」升级成「工具判的」。
- **`downtrendBuyPenalty` 的幅度与 `conflictBand` 归档为不可标定**（动 0/12 与 2/12），
  按清单 4.9c 记，别再重跑。**归档不等于删参数** —— `downtrendBuyPenalty` 的方向级抑制
  是 docs/04 §4.1 的原则，标定说的只是「在这 39 只 / 这个窗口下测不出幅度差别」。
- **测试集这一轮仍然没读**（`--to 2025-06-30`）。累计触碰次数不变：**5 次**。
- 旧口径的那份报告 `reports/folds/combine-ofat.json` 留着做对照，
  **但它的逐折 Δ / t / 胜率不可引用**（分母塌 + 丢折两处硬伤都在里面）。

---

### 5.16 标定 `adx` / `regime`（2026-08-13）：仍是 KEEP，但**第一次有了支持出厂值的正面证据**

`params/grid-regime.json`（`adx` 7 × `regime` 3 = 21 组笛卡尔积），39 只、三段切分、12 折、
测试段未读。这张网格从 §5.14 挂到现在一直没跑过，而它是**一阶参数** ——
`adx.baseThreshold` 与 `regime` 的三个数决定「有没有信号 / 算哪种状态」，
不像 `combine` 只决定「已有的信号怎么合」。报告在 `reports/folds/regime.json`。

**裁决 `KEEP`。但这一轮的 KEEP 与 §5.15 那个 KEEP 完全不是一回事。**

#### 读数 1：往任一方向动都测得出代价 —— 6 个候选显著更差

| 变动的维度 | 取值（出厂 → 候选） | 整池验证 Calmar | Δ | t | 胜/动 | 裁决 |
|---|---|---|---|---|---|---|
| `adx.baseThreshold` | 20 → **16** | −0.467 | **−1.06pp** | **4.1** | **0/11** | 淘汰（验证集年化为负） |
| `adx.baseThreshold` | 20 → **16**（配 hyst 3） | −0.343 | −0.84pp | 3.5 | 1/11 | 淘汰 |
| `adx.baseThreshold` | 20 → **24** | −0.041 | −0.48pp | 2.2 | 3/11 | 淘汰 |
| `adx.volScale` | 8 → **4**（配 midBand 0.05） | −0.032 | −0.48pp | 3.2 | 3/11 | 淘汰 |
| `adx.volScale` | 8 → **4** | 0.135 | −0.26pp | 2.0 | 3/8 | 显著更差 |
| `adx.volScale` | 8 → 4（配 hyst 3） | 0.155 | −0.26pp | 1.9 | 1/10 | 更差（临界） |
| `adx.maxThreshold` | 28 → **24** | 0.223 | −0.18pp | 1.8 | 3/7 | 更差（临界） |
| `regime.rangeMidBand` | 0.03 → 0.05 | 0.309 | −0.08pp | 0.3 | 3/11 | 测不出 |
| `adx.volScale` | 8 → 12 | 0.325 | −0.10pp | 0.9 | 2/7 | 测不出 |
| **`adx.maxThreshold`** | **28 → 34** | 0.427 | **0.00pp** | — | **动 0/12** | **惰性** |
| `regime.hysteresisDays` | 2 → **3** | 0.508 | **+0.05pp** | 0.6 | 3/8 | 唯一正向，不显著 + 在网格边界 |

**这是这个项目第一次出现「显著更差」这一栏。** 之前十二轮里所有的差值都在噪音带里
（§5.15 的 15 个候选最大 t = 1.4），于是每一轮的结论都只能是「测不出」。
这一轮有 4 个候选被验证集红线直接淘汰、另外 3 个 t ≥ 1.8 ——
**同一套判据、同一批数据、同样 12 折，能分辨出这些，就说明它不是一台只会说「测不出」的机器。**
这一点对前面几轮的解读很重要：§5.15 说 `combine` 定不下来，不是因为门槛设得不可能过。

#### 读数 2：因此 `adx.baseThreshold = 20` 有了正面证据，但**不是**「已标定」

出厂值 20 的两个邻居 16 与 24 **都**显著更差（−1.06pp / t 4.1 与 −0.48pp / t 2.2），
`volScale = 8` 的下侧邻居 4 也显著更差（−0.26pp / t 2.0）。
按 §5.15 的高原判据，出厂值这一行自己就是那片高原的顶点。

**但这只说明「20 好于 16 和 24」，不说明「20 是最优」** —— 网格只有这三个点，
真正的最优可能在 18 或 22，而那两个值一个都没测。**所以这不是 4.9a 的写回，
也不是 §5.10 那种「一整片高原」的标定**，它是一种此前没有归档位置的结果：
**已测、保持出厂值，且有支持它的正面证据**。

工具因此加了一行输出（`calibrate.ts` 的 KEEP 分支）：只要存在 Δ < 0 且 t ≥ 2 的候选，
就明确打印「出厂值不是随便取的也一样」并给出最差的那个 Δ。
只印「没有候选够格」会把这一轮读成 §5.15 那一轮，**白扔掉这个项目最缺的那种证据**。

#### 读数 3：`adx.maxThreshold` 在出厂配置下**是死参数，而且是算术上的死**

`28 → 34` **动 0/12 折**（逐折逐位相同），而 `28 → 24` 有 −0.18pp / t 1.8。
查了公式，这不是「这个样本上恰好没咬住」，是**恒等式**：

```
adxTrend = clamp(baseThreshold + volScale × volPct, baseThreshold, maxThreshold)
volPct = ATR/close 的 250 日分位 / 100 ∈ [0, 1]        （thresholds.ts:62）
⇒ 被 clamp 的那一项 ∈ [20, 20 + 8] = [20, 28]          （出厂 base 20 / volScale 8）
⇒ maxThreshold = 28 恰好等于上端，**上界永远咬不住**
```

也就是说**出厂配置下 `maxThreshold` 这一行完全不参与计算**，调到 34 / 100 / ∞ 都一样。
它只在 `baseThreshold + volScale > maxThreshold` 时才活（比如 `volScale = 12` ⇒
上端 32 > 28，这时才咬得住）。往下调到 24 有作用，正是因为 24 < 28 让上界重新咬住。

**这比「惰性」强一档**：惰性是「在这个样本上测不出差别」，
这里是「在这组取值下它在算术上无效」，换多少数据都一样。
按 4.9c 归档时要写清这个条件，否则下一轮还会有人把它放进网格 ——
这一轮就白扫了一列（21 组里 `maxThreshold = 34` 那些行全是重复计算）。

顺带这是 §5.10 那类事实的另一个形态：**参数的两个方向不对称，只往一个方向扫会得出错的结论。**
`squeezeBbwPct` 是一侧太紧，`maxThreshold` 是一侧在算术上无效。

#### 读数 4：这张网格设计得太窄，工具把它标出来了

21 个候选里**除中心行以外几乎每一行都带「该侧邻域未测」** —— 因为每个维度只给了
中心 ± 一档（`hysteresisDays` 只有 2 和 3，`rangeMidBand` 只有 0.03 和 0.05）。
后果是**即使那个 +0.05pp 显著，也不能采用**：它落在网格边界上，另一侧没测过，
分不清「高原的起点」还是「孤峰的半边」。docs/07 §3 ④ 说的「设计网格时先把取值范围想宽一点，
比事后补跑便宜」这一次是被机器提前指出来的，而不是像 §5.10 那样在扩网格重跑之后
才发现代价（多看一次测试窗口）。

**下一轮该怎么改**：`hysteresisDays` 加 1 与 4、`rangeMidBand` 加 0.02 与 0.08、
`baseThreshold` 加 18 与 22（20 的正面证据就是从这两个点来的），
`maxThreshold` 只往下扫（上侧算术无效）。**但不要顺手把网格乘大**：
21 组已经跑了 16 分钟，`combine` 那 16 组跑了 28 分钟。

#### 处置

- **`adx` / `regime` 块全部保持出厂值**，`ENGINE_VERSION` 不动。
- **`adx.baseThreshold = 20` 与 `volScale = 8` 归档为「已测、保持出厂值 + 有正面证据」** ——
  这是比 §5.15 的 KEEP 强一档的结论，归档时要分开写，别混成「都测不出」。
- **`adx.maxThreshold` 归档为「出厂配置下算术无效」**（4.9c，条件是
  `baseThreshold + volScale ≤ maxThreshold`），**别再把它放进网格的上侧**。
- **`regime.hysteresisDays` / `rangeMidBand` 仍未定**：唯一的正向候选在网格边界上，
  要按上面那份加宽方案重跑一轮才能判。这是当前**唯一**一条我建议继续跑网格的方向 ——
  它是一阶参数，而且这一轮证明了在这个数据量上一阶参数的效应是可分辨的。
- **测试集这一轮仍然没读**。累计触碰次数不变：**5 次**。

---

### 5.17 加宽 `adx` / `regime` 网格（2026-08-13）：出厂值在 ±2 的邻域里赢了每一个方向

§5.16 挂出来的两张加宽网格（`params/grid-adx-fine.json` 与 `params/grid-regime-fine.json`，
各 8 组 OFAT）跑完了，`regime` 那张的最高分压在边界上，又补了第三张
（`params/grid-regime-hysteresis-wide.json`，7 组）。39 只、12 折、测试段未读。
**三轮全是 `KEEP`。**

#### 读数 1：`adx` —— 8 个候选**没有一个**的 Δ 为正

| 变动的维度 | 出厂 → 候选 | 整池验证 Calmar | Δ | t | 动/12 | 裁决 |
|---|---|---|---|---|---|---|
| `baseThreshold` | 20 → 16 | — | — | — | — | 淘汰：训练集 Calmar −0.01 |
| `baseThreshold` | 20 → **18** | — | — | — | — | **淘汰：训练集 Calmar −0.03** |
| `baseThreshold` | 20 → **22** | 0.218 | −0.17pp | 0.9 | 10 | 孤峰（上侧邻居 24 被红线淘汰） |
| `baseThreshold` | 20 → 24 | −0.041 | −0.48pp | **2.2** | 11 | 淘汰：验证集年化为负 |
| `volScale` | 8 → **6** | 0.234 | −0.16pp | 1.3 | 7 | 更差 |
| `volScale` | 8 → **10** | 0.259 | −0.16pp | 1.7 | 5 | 更差 |
| `maxThreshold` | 28 → 26 | 0.357 | −0.05pp | 1.0 | **2** | 更差（只动 2 折，与 §5.16 的算术解释一致） |
| `maxThreshold` | 28 → 24 | 0.223 | −0.18pp | 1.8 | 7 | 更差 |

**出厂的 `baseThreshold = 20` / `volScale = 8` 在 ±2 的邻域里赢了每一个测过的方向**，
而且两侧都不是「略差」：18 与 16 让**六年训练集亏钱**（红线淘汰），22 与 24 分别是
−0.17pp 与 −0.48pp。§5.16 只测了 ±4（16 / 24），这一轮把 ±2 补上，结论没有松动。

#### 读数 2：一个必须说清的统计学差别 —— 为什么同一个形状对出厂值是证据、对候选是危险信号

上面那张表的形状（自己最高、两侧都塌）**如果出现在一个候选身上，本报告会判它「孤峰」不予采用**
（§5.14 的 `voteThreshold.trend = 4` 就是这么被否的）。那为什么同样的形状出现在出厂值上，
反而算支持它的证据？

因为**孤峰红线防的是「挑出来的最大值」这件事本身带来的向上偏差**：在 N 个候选里取 argmax，
噪音会系统性地让赢家显得更好，邻域塌陷正是这种赢家诅咒的指纹。
而 `baseThreshold = 20` **不是从这张网格里挑出来的** —— 它是来源文档的转述值，
在任何一次测量之前就写在 `params.ts` 里了（ADR-0003 的立场恰恰是「它没有依据」）。
一个**预先登记**的值在事后测量中赢了它的四个邻居，不受赢家诅咒的影响。

**所以这两件事都成立、不矛盾**：
- 不能把 `20` 写进「已标定」清单 —— 我们只知道它好于 16/18/22/24，不知道它是最优
  （19 与 21 没测，也不该为了这个再烧一轮）；
- 但可以把它归档为**「已测、保持出厂值 + 有正面证据」**，这比 `combine` 那一档
  （怎么动都测不出差别）强，而且是这个项目第一次拿到这种证据。

#### 读数 3：`regime.hysteresisDays` —— 高原是 2–6，两端都塌

`regime-fine` 里这一轴的最高分（5）压在网格上边界上，按 §5.15 的规矩不能就地下判，
于是补跑了第三张 `params/grid-regime-hysteresis-wide.json`（3/4/5/6/8/10/12，
把 3/4/5 一起带上是为了让整条轴落在同一份报告里 —— 否则孤峰判据看不见邻居）。
两份报告拼起来是完整的一条轴：

| `hysteresisDays` | 1 | **2（出厂）** | 3 | 4 | 5 | 6 | 8 | 10 | 12 |
|---|---|---|---|---|---|---|---|---|---|
| 整池验证 Calmar | 0.072 | 0.427 | 0.508 | 0.485 | 0.580 | **0.599** | 0.401 | 0.027 | −0.004 |
| Δ（pp） | **−0.34** | 基准 | +0.05 | +0.01 | +0.07 | +0.06 | −0.08 | −0.40 | −0.43 |
| t | 1.3 | — | 0.6 | 0.1 | 0.5 | 0.4 | 0.4 | 1.4 | 1.5 |
| 训练笔数 | — | 489 | 464 | 455 | 436 | 406 | 340 | 322 | 279 |
| 裁决 | 更差 | 基准 | 通过 | 通过 | 通过 | 通过 | 孤峰 | 过拟合+孤峰 | 淘汰：验证集年化为负 |

形状是**单峰**的：1 明显更差（−0.34pp），2–6 一片高原，8 开始 **Δ 转负**（验证 Calmar 0.401 仍为正，只是低于出厂），10 触发过拟合红线
（训练 0.081 → 验证 0.027，衰减 > 50%），12 被验证集年化为负淘汰。
**上边界的疑问因此消掉了** —— 涨势没有一直延续下去，5 的领先不是「网格切早了」，
它就是高原的顶部附近；顶点 6（0.599）与出厂值 2 的差距仍然只有 +0.06pp、t = 0.4。
按写回门槛高原上一个都不够格，出厂值 2 保持不动。

但这里有一条**用得上的定性结论**：**在 2–6 这一段里，把迟滞加长不花钱**。
迟滞窗口越长 → 状态翻转越少 → 信号改口越少（触发笔数从 489 单调降到 406），
而绩效在这一段里测不出差别。这对 M3 是有意义的（docs/05 的冷却与去重要处理
「今天说买明天说卖」），**但它属于提醒层的取舍，不是绩效标定**，所以这一轮不动出厂值 ——
真要往上调，判据应该是 M3 的提醒日志（每日改口次数），不是回测 Calmar，
**而且这条路的上限是 6**：再往上（8 起）绩效就开始付代价了。

#### 读数 4：`regime.rangeMidBand` —— 四个取值全是负的，但形状是噪音

| `rangeMidBand` | 0.02 | **0.03（出厂）** | 0.04 | 0.05 | 0.08 |
|---|---|---|---|---|---|
| Δ（pp） | −0.19 | 基准 | **−0.26** | −0.08 | −0.09 |
| t | 1.4 | — | 1.6 | 0.3 | 0.3 |

四个方向都比出厂值差，但**非单调**（0.04 最差，比它两侧的 0.02 与 0.05 都差），
且没有一个到得了 t = 2。**这是噪音的形状，不是「0.03 是最优」的证据** ——
与读数 1 的 `adx` 不同，那里是单调且有两个红线淘汰。
`rangeMidBand` 归档为「已测、保持出厂值（测不出差别）」，与 `combine` 同一档。

#### 处置

- **`adx` 三个数与 `regime` 两个数全部保持出厂值**，`ENGINE_VERSION` 不动。
- 归档分档（这是重点，别混写）：
  | 参数 | 归档 |
  |---|---|
  | `adx.baseThreshold = 20`、`adx.volScale = 8` | 已测、保持出厂值 **+ 有正面证据**（±2 邻域全负，2 个邻居被红线淘汰） |
  | `adx.maxThreshold = 28` | 上侧**算术无效**（§5.16）；下侧 24 / 26 更差 ⇒ 保持 |
  | `regime.hysteresisDays = 2` | 已测、保持出厂值；**1 明显更差，2–6 分辨不出，8 起转负** ⇒ 往上调的判据留给 M3 的提醒日志，且不得超过 6 |
  | `regime.rangeMidBand = 0.03` | 已测、保持出厂值（测不出差别，形状是噪音） |
- **这三张网格不要再跑第四轮。** 加宽已经做完了：`adx` 三条轴与 `regime` 两条轴的
  两侧邻域都测到了红线或转负为止，没有一条还压在边界上。想再推进只有两条路，
  都不是加密网格：扩标的池（§5.15 读数 4 的量级估计）、或把判据换成 M3 的提醒日志（迟滞那一条）。
- **测试集这三轮仍然没读**。累计触碰次数不变：**5 次**。

---

### 5.18 标定 `risk` 与 `multiTf`（2026-08-13）：先修了「胜率」的口径，然后找到全项目最一致的候选（仍不写回）

这一轮起因是产品侧的一句话：**「还是需要想办法增加预测胜率，这是最核心的能力」**。
于是先去量「胜率现在是多少」—— 结果第一步就撞上一个口径错误，它比后面的标定结论更重要。

#### 读数 1：报告里的「胜率」与「平均」都被逐行口径读错了

出厂参数全期（39 只、2018-01-01 → 2026-08-11）报告原本这样打：

```
交易 769 笔  胜率 33.16%  盈亏比 2.69
TREND_UP    187 笔  胜率 31.55%  平均 −1.58%
RANGE       406 笔  胜率 33.99%  平均 −1.26%
TRANSITION  176 笔  胜率 32.95%  平均 −1.34%
```

**四种市况的「平均」全是负的，而净盈亏是 +231,154 元。** 两个数都没算错，
错的是口径：一行 `trade` 是**一次卖出**，而回撤减仓会把一次建仓拆成「先卖一半、后卖一半」，
两行的仓位差一倍 —— 逐行**未加权**平均于是把半仓那一行和满仓那一行等同看待。
实测 491 行是减仓行（平均仓位 2.3 万、平均 −2.79%），278 行是清仓行（平均仓位 5.5 万、+1.19%）。

同一个成因也让「胜率」偏低了 16 个百分点。按 `code + entryDate` 归并成**建仓**之后：

| 口径 | 数值 | 回答的问题 |
|---|---|---|
| 逐笔（769 行） | 胜率 **33.16%** · 盈亏比 2.69 | 每一次卖出动作赚不赚 —— 看止损止盈规则本身 |
| **建仓（278 次）** | 胜率 **49.28%** · 盈亏比 1.39 · 平均 +831 元（+0.84%） | 「我按提醒买了一次，最后赚没赚」—— **用户口径** |

修完的分状态归因（平均按仓位加权、胜率按建仓）**换了一个结论出来**：

| 状态 | 建仓胜率 | 平均（加权） | 盈亏 | 判定根数 |
|---|---|---|---|---|
| TREND_UP | **40.00%** | +0.13% | **+7,734 元** | 9153 |
| TREND_DOWN | — | — | 0（风控禁止趋势下跌买入） | 6170 |
| RANGE | 52.03% | +1.08% | **+156,539 元** | 5208 |
| TRANSITION | 52.31% | +1.07% | +66,881 元 | 41003 |

**RANGE + TRANSITION 拿走了 223k 里的全部，TREND_UP 187 次建仓只赚了 7.7k、胜率低 12 个百分点。**
这条在旧口径下完全看不见（四行都是负的，看着一样差）。它与 §5.10 的消融方向一致
（均值回归贡献 +2.0 ~ +3.4pp），但**不要就此去砍趋势策略**：这是全期在样本内的分组，
没有过三段切分，而且 regime 归属不是随机分配的。列为待验的观察。

**处置**：`groupPositions()` 进 `metrics.ts`，`PerformanceBlock.positions` 与
`RegimeAttribution.weightedPnlPct` / `positionWinRate` 进报告，文本报告里建仓那一行
**紧跟着**逐笔那一行打（缺了它前者一定会被当成用户口径读）。
另加一栏**离场规则分布** —— 见读数 2。`TradeStats.avgPnlPct` 保留但注释了「符号可与净盈亏相反」。

#### 读数 2：97% 的离场由风控决定，所以「卖点」根本不是策略在判

| 离场规则 | 次数 |
|---|---|
| DRAWDOWN_REDUCE | 410 |
| PROFIT_PROTECT | 154 |
| TRAILING_STOP | 115 |
| STOP_LOSS | 64 |
| **风控小计** | **743 / 769 = 96.6%** |
| 策略卖出子信号（R3/T2/T3/T1/R2/R4 合计） | **26 = 3.4%** |

这解释了为什么「提高胜率」的杠杆在 `risk` 块而不在策略层：**一次建仓最终落在盈利还是亏损区，
96.6% 的情况下是那五个转述阈值决定的**，而它们一个网格都没跑过。
52.9% 的建仓中途触发过 7% 回撤减仓 —— 一半以上的出手是「先亏了再说」。

#### 读数 3：`multiTf` 七个数几乎全惰性，归档

`params/grid-multitf.json`（OFAT 17 组）。**裁决 KEEP**，而且是最弱的那种 KEEP：

- `dayRsiBuyMax` 40 / 50、`dayRsiSellMin` 45 / 65、`weekAdxConfirm` 20、`weekCrossLookback` 2
  —— **动 0/12 折**（逐折逐位相同，改了等于没改）；
- `weekCrossLookback` 5 / 8、`dayRsiBuyMax` 55、`weekAdxConfirm` 30 —— 只动 **1/12 折**；
- 唯一有点样子的是 `weekAdxWeak = 25`（验证 Calmar 0.627、建仓胜率 48.9%、Δ +0.15pp、t 1.8、
  受影响 5 折里赢 4），但它落在网格边界；
- 明确更差的是 `resonanceDelta = 0.2`（0.267 / Δ −0.15pp）与 `falseBreakoutDelta = −0.05`
  （0.321 / Δ −0.09pp / 0 胜 3 负）。

**处置：整块保持出厂值，按 4.9c 归档为「大部分取值在这个样本上不改变任何一折」。**
`weekAdxWeak` 留一条待办：它是唯一动得多的那个，要跟着下一轮加宽的网格一起测。
多周期共振是「二阶旋钮」的又一例（§5.14 的分类）—— 它只调整已成立信号的得分。

#### 读数 4：`risk` 块两个方向都有信号，而且**方向相反的两种「好」在这里同时出现**

`params/grid-risk.json`（OFAT 16 组）+ `params/grid-risk-fine.json`（加宽 11 组）。
出厂值验证段基线：建仓 48 次 / 胜率 45.8% / 盈亏比 1.54 / Calmar 0.427；训练段 175 次 / 胜率 47.4% / Calmar 0.090。

**(a) `profitProtectTrigger`（盈利保护触发线，出厂 5%）—— 看着最惊人，是陷阱。**

| 取值 | 训练 Calmar | 训练建仓胜率 | 验证 Calmar | 验证胜率 | Δ | t |
|---|---|---|---|---|---|---|
| 0.03 | −0.032 | 49.2% | **淘汰**（训练年化为负） | — | — | — |
| **0.05（出厂）** | **0.090** | **47.4%** | **0.427** | **45.8%** | 基准 | — |
| 0.08 | 0.074 | 44.3% | 0.935 | 44.7% | +0.30pp | 1.4 |
| 0.10 | 0.045 | 43.0% | **1.074** | 44.7% | +0.44pp | 1.9 |
| 0.12 | 0.059 | 43.0% | 0.945 | 44.7% | +0.36pp | 1.5 |
| 0.15 | 0.038 | 42.4% | 0.722 | 44.7% | +0.23pp | 1.1 |
| 0.20 | 0.074 | 42.4% | 1.021 | 44.7% | +0.49pp | 1.8 |
| 0.30 | 0.057 | 42.7% | 0.795 | 44.7% | +0.36pp | 1.3 |

验证集 Calmar 翻了一倍以上（0.427 → 1.07），收益 1.28% → 2.64%，看着是全项目最大的一次改进。
**但六个取值的训练集 Calmar 与训练集胜率全部低于出厂值**，验证集胜率也低，
而且验证 Calmar 沿取值**非单调**（0.10 → 1.074、0.15 → 0.722、0.20 → 1.021）。
训练与验证方向相反 + 两段胜率都降 + 非单调 = **§5.14 那个 `voteThreshold.trend = 4` 的形状**，
读作「验证窗口正好友好」。**不采用。**

**这一次是工具自动挡下的**：新加的「建仓级胜率不得低于出厂值」门槛（44.7% < 45.8%）
直接把它排除在 `passable` 之外，不需要人看表。这条门槛正是为了这个而加的 ——
「提高胜率」若单独作为目标可以被机械满足，而只按收益排名又会放过这种胜率下降的候选。

**(b) `drawdownReducePct`（回撤减仓线，出厂 7%）—— 全项目至今最一致的候选。**

| 取值 | 训练 Calmar | 训练建仓胜率 | 验证 Calmar | 验证胜率 | Δ | t |
|---|---|---|---|---|---|---|
| 0.05 | −0.074 | 42.1% | **淘汰**（训练收益 −0.93%） | — | — | — |
| **0.07（出厂）** | 0.090 | 47.4% | 0.427 | 45.8% | 基准 | — |
| **0.10** | **0.110** | **54.1%** | **0.533** | **50.0%** | +0.15pp | 0.8 |
| 0.12 | 0.106 | 54.4% | 0.426 | 50.0% | +0.06pp | 0.3 |
| 0.16 | 0.106 | 54.4% | 0.426 | 50.0% | +0.06pp | 0.3 |
| 0.20 | 0.106 | 54.4% | 0.426 | 50.0% | +0.06pp | 0.3 |

**四个口径同向**：训练 Calmar ↑、训练建仓胜率 **+6.7pp**、验证 Calmar ↑、验证建仓胜率 **+4.2pp**。
下侧（0.05）被训练集红线淘汰，说明这个方向有真实代价。
**0.12 / 0.16 / 0.20 三个值逐位完全相同** —— 机制在 0.12 处**饱和**（减仓几乎不再触发），
所以真正的问题不是「取多少」，而是二值的「**7% 是不是太紧**」。

**但它没有过显著性门槛**：Δ +0.147pp、标准误 0.192pp、**t = 0.77**，逐折 Δ 是
`0.87 0 0.11 0 −0.21 1.94 −0.37 −0.46 −0.02 0 −0.35 0.25` —— 12 折里 3 折没被改动、
正的 5 折、负的 4 折，第 6 折的 +1.94pp 一个人贡献了大半。
要让这个 Δ 达到 t ≥ 2，需要约 **82 折 ≈ 6.8 倍数据 ≈ 同窗口下 264 只标的**。

**处置：`risk` 五个数全部保持出厂值，`ENGINE_VERSION` 不动。** 但归档方式与前面几轮不同 ——
`drawdownReducePct` 记为**「最优先复核项」**而不是「测不出差别」：它是至今唯一一个
训练/验证/两段胜率四个口径同时改善、且下侧有明确代价的候选。**扩标的池到 250 只上下时，
第一个要重跑的就是它**（相比 §5.15 给 `combine` 估的 1400 只，这个量级便宜得多）。

#### 这一节不能证明什么

- **没有提高胜率。** 出厂参数的建仓级胜率仍是 49.28%（全期）/ 45.8%（验证段），
  这一轮只是把它**量准了**，并找到了一个大概率能提高它的方向。
- **`profitProtectTrigger` 的验证集数字不许引用。** 训练集反向 + 胜率下降 + 非单调，
  引用它等于把「验证窗口正好友好」当成绩效。
- **读数 1 的分状态结论是样本内的**（全期、未切分），TREND_UP 那 40% 不足以支持
  「不要在趋势市买入」这种改动。
- 测试集这三轮仍然没读。累计触碰次数不变：**5 次**。

### 5.19 六条从未绿过的用例（2026-08-13）：三个 fixture 缺陷 + 两条过期断言

接 M3 时发现 `pnpm test` 有 6 条红的。**它们不是 M3 引入的，也不是 `squeezeBbwPct` 写回造成的回归**
—— 在 `4450467`（「引擎层开发」，即引入这些用例的那次提交）上建 worktree 重跑，
同样这 6 条、一条不差地红着。**它们从写下来那天起就没绿过，跨了三次提交。**

诊断结果分三类，其中最值得记的是第一类。

#### ① `chopCloses` 太规则 → ADX 恒 ≈ 8 → 所有 ADX 确认项永远不成立

`goldenCrossBreakout` 最后几根的探针输出：

| i | crossUp(MA5,MA20) | adx | adxRange |
|---|---|---|---|
| 319 | false | 7.9 | 19.2 |
| **320** | **true** | **9.3** | **20.7** |
| 325 | false | 25.0 | 23.0 |

`T1_MA_CROSS:BUY` 要 `crossUp(MA5,MA20) 且 adx > adxRange`。唯一一次金叉发生在 i=320，
那时 ADX 只有 9.3 而门槛是 20.7；等 ADX 在 i=325 终于够了，金叉事件早就过去了
（穿越是**事件**，不会因为「涨得更多」补发一次，docs/04 §2）。

旧的 `chopCloses` 是 `((i%7)-3)/3 + ((i%3)-1)/6` 的固定错拍波形：**振幅够，但方向性运动几乎为零**
—— 每一步的涨跌幅都取自同一个小集合，+DM 与 −DM 长期抵消，320 根之后 ADX ≈ 8。
于是**凡是建立在 `chopCloses` 之上的 fixture，T1 BUY 与 T4 都在结构上无法触发**。

**这一度被误判成引擎的结构缺陷**（「金叉总是早于 ADX 确认」）。去真实数据上量了一下，不是：

> 46 只、2018-01-01 → 2026-08-11，共 **2709 次** `crossUp(MA5, MA20)`，
> 其中 **1642 次（60.6%）当日就满足 `adx > adxRange`**。
> 未触发的那些，ADX 之后 60 根内才越线的**中位滞后 9 根**，34 次始终不越线。

即真实横盘的 ADX 在 20 上下，不是 8。**引擎没问题，是 fixture 不像真的。**
`chopCloses` 已改为确定性伪随机游走（LCG，种子写死）+ 温和回拉，横盘段 ADX ≈ 18，
落在 `adxRange` 与 `adxTrend` 之间 —— 那正是这两条线要表达的语义：
**够得上「不是纯震荡」，够不上「已成趋势」**。改完 `rangeBound()` 的「震荡不出 T4_ALIGNMENT」仍然成立。

> **这是 fixture 文件开头那个坑的更隐蔽版本。** 它已经警告过「真直线会让 STD=0、BBW 恒为 0，
> 测出来的『不触发』是假通过」—— 一眼能看出来。而「规则锯齿让 ADX 恒低」不是：
> 序列看起来完全正常，振幅也对，只有 ADX 这一个指标被悄悄压住了。
> **判据得来自真实数据的分布，不能靠肉眼看序列像不像横盘。**

#### ② 涨停 fixture 产不出买单 → 「买单作废」这条路径是零覆盖

`次日开盘涨停 → 买单作废` 那条用例就地造了 320 根**字面意义的直线**（全部 10.000、
open=high=low=close、成交量恒定）接 30 根涨停。实测：**27 次评估、0 次抑制、0 张委托**，
`limitBlocked` 恒为 0。而同一条用例里的 `expect(trades).toHaveLength(0)` **在真空里通过着** ——
这条安全路径此前实际上没有任何覆盖，界面上却像有人管。

已新增 fixture `limitUpBreakout()`（横盘 → 回调 → **2 根**放量拉升 → 3 根一字涨停）。
拉升只给 2 根是有原因的：再长下去 RSI 进超买区，末根的组合结论会翻成卖出，
待成交的委托就不是买单了，照样测不到。现在 `limitBlocked = 1`、`trades = 0`，
并顺带走到了 `HARD_LIMIT_UP` 的硬抑制分支。

#### ③ 两条纯粹的断言过期

- `tick.test`：`toEqual({ lastTickAt, lastCtx, lastSnapshots })` —— M2 给 `TickState` 加了
  第四个字段 `lastSignals`，`toEqual` 是全等比较，漏一个就红。
- `simulate.test` 的「硬抑制计入 suppressed」：两个 `suppressed` 断言都过了
  （INSUFFICIENT_DATA 9、NEW_LISTING 29），挂的是**多出来的** `expect(trades).toHaveLength(0)`。
  `NEW_LISTING` 只压制前 60 根而窗口有 120 根，后 60 根本来就该正常出手（9 行 = 1 次建仓被回撤减仓拆开）。
  断言比用例名承诺的多。已改为「抑制期内一笔都不成交」。

#### 顺带修掉的一处「假通过」

`signals.test` 的编排用例此前全部跑在**出厂阈值**（0.60 分 / 趋势 3 票）上，
而 fixture 里没有任何一根凑得够票数（实测最好的一根 2 票：T1+T3），于是 `persist()` 一行都不写，
「同一条信号连续多轮只落一行」这类去重用例退化成 `0 === 0`。
已改为与 `simulate.test` 同一个做法：**编排层的用例把阈值调低到 0.3 分 / 1 票**，
理由也一样 —— 这里验的是编排，不是信号质量。信号稀疏本身是对的
（§5.13：真实回测里平均资金占用只有 4.15%），但不该让编排层无从验起。

同轮把「收盘确认轮」那条用例拆成两条并去掉了一个脆弱前提：它原先假定
「这段是上涨突破，收盘结论不可能是 SELL」，而 8 根 3.5% 拉升之后 RSI 深度超买，
末根最强的子信号恰恰是 R1 的 SELL。现在方向从 `run()` 的实际结论里取，
CONFIRMED 与 INVALIDATED 两个分支各测一条。

#### 教训

**「测试是绿的」与「测试在测东西」是两件事。** 这六条里有三条属于前者的反面
（红着但没人处理），另有至少四条属于后者的反面（绿着但断言恒真）。
本项目已经踩过的同一形状：M2 §5.11「合取里混着一个几乎总成立的项，会让同组其他参数集体失去作用」——
fixture 里混着一个恒低的指标，会让依赖它的规则集体失去覆盖，**而且不会变红**。

---

### 5.20 扩标的池到 261 只（2026-08-14）：分辨力买到了，但出厂参数在训练窗口上亏钱，标定被自己的红线挡住

执行 [docs/09 §2.2 B0](../09-下一阶段开发计划.md)。**结论一句话：池换对了，然后发现要标定的那套出厂参数
在一个有代表性的池子上是亏钱的 —— 于是 B1/B2/B3 的每一张网格都只能裁决 `INCONCLUSIVE`。**
这不是工具坏了，是 §5.14 立的那条红线（「训练集 Calmar ≤ 0 直接淘汰」）在说：先解决出厂值自己站不住这件事。

#### ① 池：40 → 264 抽样 / 261 拿到数据

`scripts/build-universe.mjs` 的 `take` 从 15/15/10 提到 **98/86/80**。三层比例按**全市场分层规模**取
（沪主板 1699 / 深主板 1494 / 创业板 1401 ⇒ 37.0/32.5/30.5，抽样后 37.1/32.6/30.3），
不是沿用旧的 37.5/37.5/25 —— docs/09 的验收条件是「与全市场大致同构」。
抽样规则一个字没改（代码段分层 + 层内代码升序等距、不按市值/流动性/ST 筛、不含科创板）。

264 只里 **261 只拿到 2018 以来的日线**，缺 3 只：`SZ001330` / `SZ301583` / `SZ301717` ——
腾讯对这三只的某些年返回 `day` 而**没有 `hfqday`**，`assertKey()` 因此拒绝（它挡的正是
「把不复权当复权用」）。这三只不是抓漏，是那一轨在源上不存在；`SZ301717` 还是上市几天的新股。

**折数不会因为扩池自己变多。** `--code-folds` 默认 4 ⇒ 4 × 3 = 12 折，与 40 只那版**一模一样** ——
扩池后必须显式给 `--code-folds 28`（28 × 3 = **84 折** ≥ §5.18 外推的 82 折）。
每折约 9 只，与旧池每折 10 只的厚度相当，而折数多了 7 倍。这一条最容易漏，漏了等于池白扩。

#### ② 换池换掉了收益的**符号**（同一份代码、同一套出厂参数）

出厂参数、`2018-01-01 → 2026-08-13`、预热 300 根、基准 SH000300：

| 池 | 建仓 | 总收益 | 建仓胜率 | 最大回撤 | 平均资金占用 | 净盈亏 |
|---|---|---|---|---|---|---|
| 40 只（旧） | 278 | **+5.93%** | 49.28% | 2.44% | 4.14% | **+231,154 元** |
| 261 只（新） | 1853 | **−0.94%** | 44.85% | 4.24% | 3.69% | **−249,777 元** |

**旧池那一行是复现出来的**（净盈亏 231,154 元与 §5.18 逐位相同，建仓胜率 49.28%、回撤 2.44%、
占用 4.14% 也都对上），所以差异不是流水线变了、也不是这次抓的数据坏了 —— **是池。**

分窗口看更直接（判定自 2019-04 起，前 300 根被预热吃掉）：

| 池 | 训练窗口 2018-01-01 → 2023-12-31 | Calmar | 红线 |
|---|---|---|---|
| 40 只 | +1.33%（年化 +0.22%） | **+0.09** | 刚好过线 |
| 261 只 | **−2.55%**（年化 −0.43%） | **−0.125** | **被淘汰** |

也就是说：**此前十一轮标定之所以能跑，是因为出厂值在 40 只上勉强站住了那条线**（+1.33%），
而这个「勉强」在一个有代表性的池子上不成立。

#### ③ 实测裁决：`INCONCLUSIVE`，出厂值本身被淘汰

261 只 / 84 折 / `volume` 两组候选的探针跑（`reports/calib/probe.txt`，退出码 **2**）：

```
参数标定报告 · 裁决 INCONCLUSIVE
  c38e329b  Calmar — （基准）淘汰：训练集年化为负（Calmar -0.13），验证集再好也只是窗口运气
  870b22ab  Calmar — Δ —    淘汰：训练集年化为负（Calmar -0.13），验证集再好也只是窗口运气
出厂值：训练 3581 笔 / -2.55% / Calmar -0.125
```

出厂值在跑验证集之前就被淘汰 ⇒ `incumbent.foldScores` 为空 ⇒ **所有候选的 `delta` 都是 null**
⇒ 配对比较这套判据整体失效，`winner` 必为 null，裁决必为 `INCONCLUSIVE`。
**所以在这个池子上，B1/B2/B3 的网格无论跑多少组都得不到 `KEEP` 或 `WRITE_BACK`。**

#### ④ 亏损是弥散的，不是几只坏数据

| 检查 | 数 |
|---|---|
| 收益为负 / 为正 / 为零的标的 | **147 / 103 / 11** |
| 逐标的收益中位数 | **−2.58%** |
| 最差 10 只占全部负收益之和 | **19.2%** |

排除了「几只脏数据拖垮全池」这条解释。分市况归因（按建仓时状态，仓位加权）指得更准：

| 建仓时状态 | 建仓 | 建仓胜率 | 平均 | 净盈亏 |
|---|---|---|---|---|
| TREND_UP | 1245 | **39.39%** | −1.01% | **−385,997 元** |
| RANGE | 2560 | 47.83% | +0.17% | +152,221 元 |
| TRANSITION | 1654 | 43.74% | −0.03% | −16,002 元 |
| TREND_DOWN | 0 | — | — | 0 元 |

**亏的是「上涨趋势里买入」这一类**（RANGE 反而是赚的）。离场分布仍与 §5.18 一致：
`DRAWDOWN_REDUCE` 3044 · `PROFIT_PROTECT` 994 · `TRAILING_STOP` 652 · `STOP_LOSS` 598，
策略卖出子信号合计 171 ⇒ **96.9% 的离场由风控触发**。

#### ⑤ 顺带用「读代码」归档掉 5 个 GUESS（这类证据比网格强）

准备 B1 的网格时逐个确认「这个参数有没有读者、那条分支走不走得到」，结果有 5 项根本不必上网格 ——
它们在网格上只会表现为「动 0/84 折」，而**「没有读者」比「这次没测出差别」强一个档次**：

| 参数 | 归档 | 依据 |
|---|---|---|
| `ma.periods` | `INERT` | 信号层只读 5/20/60（`trend.ts` 里是**字面量**），这张表只决定「算哪几条」。加 10/120 对信号零影响；去掉 5/20/60 是**关掉 T1/T4** 而不是调参。**但 10/120 不能删** —— 观察点指标与 AI 上下文在读 |
| `volume.shrinkRatio` | `INERT` | 全仓搜索**无读者**。docs/04 §1.6 的「ratio ≤ 0.8 为缩量」从未被任何子信号消费 |
| `macd.preset` | `INERT` | 纯标签：`MacdParams` 只有 fast/slow/signal，没有代码读 preset。改它**会变参数指纹**（指标缓存作废 + 影子运行停止累积）却不改任何行为 —— 有代价没效果 |
| `data.minBars` | `UNTESTABLE` | 回测预热下限 = `fullBars` 300 > `minBars` 40，这条分支走不到。它管的是「新股上市初期不出信号」，判据只能来自真机 |
| `data.insufficientPenalty` | `UNTESTABLE` | 同一条分支（`bars < fullBars` 或 BBW 分位未预热）走不到，除非把 `--warmup` 钉住 |

`data.fullBars` 留在 `GUESS`，但扫它**必须钉住 `--warmup`** —— 它在回测里同时充当预热下限，
不钉的话每个候选的判定窗口都不一样，测出来的是窗口而不是参数。

加上 ⑦ 里又判掉的两项（`strategy.expandedBbwPct` 与误归档的 `risk.industryConcentrationCap`），
这一轮结束时 `paramRows()` 是
`CALIBRATED 1 · KEPT 16 · INERT 13 · UNTESTABLE 11 · GUESS 21`（共 62 个叶子）。
**`GUESS` 从 27 降到 21，全部靠「读代码 + 实测逐位相同」，一个都不是靠网格裁决拿到的。**

#### ⑥ 取数上踩到的两件事（都已改进 `scripts/fetch-history.mjs`）

1. **`--concurrency 4` 跑到第 99 只被腾讯挡下**：之后每个请求都是 `HTTP 501` + 一张 JS 挑战页，
   单只重试三次全灭，一次跑掉 165 只。501 **不是**「这只没数据」，是本机被拦；
   十分钟后仍未恢复，而 sina 那条一直是通的（**别拿别的接口判断它恢复没有**）。
   上限已压到 2，并按「宁可慢一小时」处理。
2. **拦截是按 host 的**：同一个接口还有 `ifzq.gtimg.cn` 与 `proxy.finance.qq.com` 两个门，
   `web.` 被拦时这两个照样通、返回的 `hfqday` 逐位相同。脚本因此改成三个门按序降级、
   撞到 501 就换门并记住（`_meta.source` 记的是**实际用了哪个门**）。换门后 165 只 13 分钟补完。
3. 顺带把「先全拉完再统一写盘」改成**逐只落盘** —— 被拦那次前 98 只已经拉到手却因为还没写盘一起丢了。

#### ⑦ 网格照跑了一遍，问的不是「哪个值最好」而是「有没有哪个值能把训练窗口翻正」

裁决全是 `INCONCLUSIVE`（预期之内），但**逐候选的训练集数字是真的**，可以回答一个具体问题。
261 只 / 84 折 / 各块 OFAT，训练窗口 2018-01-01 → 2023-12-31，按训练集 Calmar 排（`reports/calib/`）：

| 块 | 最好的那个候选 | 训练总收益 | 训练 Calmar | 回撤 | 建仓 | 建仓胜率 | 出厂值同窗口 |
|---|---|---|---|---|---|---|---|
| `risk` | `drawdownReducePct` 0.12（0.16/0.20 逐位相同 = 饱和） | **−1.35%** | −0.083 | 2.74% | 1164 | **50.77%** | −2.55% / −0.125 / 3.44% / 43.30% |
| `volume` | `breakoutRatio` 1.6（单调：1.1 → 1.6 逐档变好） | −2.21% | −0.109 | 3.41% | 1147 | 43.77% | 同上 |
| `boll` | `period` 22 · 次之 `k` 2.5 | **−0.73%**（k 2.5） | −0.064（period 22） | 1.70% | 782 | 45.01% | 同上 |

**一个都没翻正。** 最好的三个候选把训练集从 −2.55% 拉到 −0.73% ~ −1.35%，仍然是负的，
而它们的共同点是**交易更少**（`boll.k` 2.5 把建仓从 1187 压到 782）或**风控更松**
（`drawdownReducePct` 从 7% 放到 12%）—— 也就是「少做少错」，不是「做对了」。

三条具体读数值得记：

1. **`risk.drawdownReducePct` 的方向第三次同向**（§5.18 在 40 只上是四个口径同向、t 0.77）：
   这次在 261 只上是**收益 +1.20pp、建仓胜率 +7.5pp、回撤 −0.70pp** 同时改善，且 **≥0.12 饱和**
   ——与 §5.18「真问题是二值的『7% 是不是太紧』」完全一致。**但仍然不能写回**：
   出厂值被红线淘汰 ⇒ 拿不到逐折 Δ 与标准误 ⇒ 没有 `|Δ|/stderr ≥ 2` 这一关的依据。
   这是全项目目前**证据最强却仍不能动**的一个数。
2. **`risk.profitProtectTrigger` 又演了一遍 §5.18 那个陷阱**：往上调（0.05 → 0.3）Calmar 从
   −0.125 升到 −0.108 看着像改善，而**建仓胜率从 43.30% 掉到 35.37%**。
   两条门槛一起卡才拦得住它 —— 这次它连第一关都过不了，因为收益也没有变正。
3. **`boll.bbwLookback` 的 375 / 500 两组不能按「参数变差」读**：建仓从 1187 掉到 **328**，
   因为分位数要 `bbwLookback` 根样本才有首个有效值，而回测预热是 `fullBars` = 300 ——
   窗口拉长之后**大批标的（尤其 2021 年后上市的）整段没有 `bbwPct`**，T3/R3 直接不出手。
   要干净地测它必须同时抬高 `--warmup`，否则测的是「有多少标的够长」。
   这与 `data.fullBars` 的坑是同一个形状（§5.20 ⑤）。

第二批三张网格（`macd` 13 组 / `strategy` 11 组 / `regime` 剩余四数 14 组）**同样一个都没翻正**，
而且更平：

| 块 | 最好 | 训练总收益 | 训练 Calmar | 与出厂值的差 |
|---|---|---|---|---|
| `macd` | `slow` = 26（**就是经典 12/26/9**） | −2.25% | −0.108 | +0.30pp |
| `strategy` | `midReversionStd` = 2.0 | −1.13% | −0.083 | +1.42pp（建仓 1187 → **945**） |
| `regime` 剩余 | `rangeBbwPct` = 20 | −2.16% | −0.117 | +0.39pp |

- **`macd` 整块是一片平地**：13 组落在 −2.25% ~ −2.79%，两端都没有断崖。
  来源文档的 12/17/9 与经典 12/26/9 在这个池上分不出来（经典略好 0.30pp，远在噪音带里）——
  「更灵敏所以假信号更多」这句话在数据上既没被证实也没被推翻。
- **`regime` 剩余四数也是平地**：15 组落在 −2.16% ~ −2.73%，`adxSlopeTrigger` 的 8 与 12
  **逐位相同**（那一侧已经饱和）。这与 `audit:regime` 的上界一致：三条突变条件加起来最多
  买回 12.9 个百分点的死区，而买回来也不改善绩效。
- **`strategy.expandedBbwPct` 70/80/95/98 四档与出厂 90 逐位相同** —— 建仓 1187、胜率 43.30%
  一字不差。**根因不是不敏感，是回测读不到它**：它只产 `VOLATILITY_EXPANDED` 这条 `DOWNGRADE`
  （改提醒级别），而回测的 `toOrder()` 只读 `direction`。已改档 `UNTESTABLE`，
  判据与 `alert.bubbleScore` 同一类（提醒日志，不是 Calmar）。
- 顺着这条线**查出一处早先的误归档**：`risk.industryConcentrationCap` 原记 `KEPT`
  （「随 risk 块一同上过网格」），但它同样只产 `DOWNGRADE`，而且回测**根本不传 `industryShare`**
  （只有 `main/engine/signals.ts` 传）—— 两重读不到。§5.18 那轮的 KEEP 对它零信息量，已改 `UNTESTABLE`。
  **教训**：「跟着某个块一起上过网格」不等于「这个参数被测过」，归档时要确认它在回测里
  **有没有一条能改变 `direction` 的路径**。

第三批（`rsi` 16 组 / `data` 8 组，后者配 `--warmup 300` 把预热钉住）：

| 块 | 最好 | 训练总收益 | 训练 Calmar | 建仓 | 建仓胜率 |
|---|---|---|---|---|---|
| `rsi` | `osBase` = 25 | **−1.40%** | **−0.078**（全部 82 组候选里最好的） | **1471**（出厂 1187） | 44.60% |
| `rsi` | `period` = 6 | −1.46% | −0.089 | **1603** | **45.85%** |
| `data` | `fullBars` = 400（500 逐位相同） | −1.65% | −0.109 | 906 | 44.15% |

**`rsi` 是唯一一个「改善不是靠少做」的块**：放宽超卖基线（15 → 25）与缩短周期（14 → 6）
都让建仓数**增加** 24%–35%、胜率同时上升。别的块最好的候选全是把建仓数压下去换来的。
这条差别值得记 —— 它指的方向与 §5.20 ④ 的归因一致（亏在买点，而 RANGE/均值回归那一侧是赚的）。
`osBase` 的 5 与 10 逐位相同（下侧饱和），`sentimentScale` 10 → 30 单调变好。

`data` 块的三条**完全按预测落地**，这也是 §5.20 ⑤ 那几条「读代码」结论的实测背书：
`minBars` 20/60/80 与 `insufficientPenalty` 0.5/0.95 四组**与出厂值逐位相同**（分支走不到），
`fullBars` 250 也相同（< 预热 300 就没有效果），只有 400/500 动了 ——
而它动的方式是「把前 100–200 根判定期降级」，等于**少做**，不是把参数调对。

#### ⑧ 买点诊断（2026-08-14，261 只基线的 1853 次建仓拆开看）

⑦ 排除了「换一个一阶取值就够了」，于是按 §5.20 ④ 的指向往买点查。
把回测报告的逐笔按 `code@entryDate` 归并成**建仓级**，再按建仓时的市场状态与入场子信号拆开
（`reports/calib/buypoint.mjs`）：

| 建仓时状态 | 建仓 | 建仓胜率 | 净盈亏 | 仓位加权收益 | 持仓中位数 |
|---|---|---|---|---|---|
| TREND_UP | 396 | 39.39% | **−385,997 元** | −1.01% | 8 根 |
| RANGE | 922 | 47.83% | **+152,221 元** | +0.17% | 15 根 |
| TRANSITION | 535 | 43.74% | −16,002 元 | −0.03% | 8 根 |
| TREND_DOWN | 0 | — | — | — | — |

三条读数，每一条都指向同一个方向：

1. **TREND_UP 里没有一种入场组合是赚的。** 最差的是 `T2+T3+T4`（143 次 / 胜率 38.46% / −182,221 元 /
   −1.34%），最好的 `T1+T2+T4` 也只是 −0.68%。而 `T4_ALIGNMENT` 出现在 **396/396 = 100%**
   的 TREND_UP 建仓里 —— 它在这个状态下**没有任何区分度**，等于常量。
2. **得分越高越糟。** TREND_UP 按入场得分分档：0.75–0.8 那档 89 次几乎打平（+0.10%），
   而 **0.8 以上的 198 次是 −1.39%（胜率 37.88%）**，0.7–0.75 是 −0.86%。
   组合层的得分在这个状态下**是反向指标** —— 这是「追高」的形状，也解释了为什么
   §5.20 ⑦ 里所有「改善」都来自少做：把交易砍掉就是在砍这一堆高分买入。
3. **全池唯一赚钱的引擎是 RANGE 里的均值回归**：`R3_SQUEEZE + R4_MID_REVERSION` 组合
   911 次 / 胜率 47.97% / **+182,213 元**，几乎等于 RANGE 全部的盈利。
   （§5.10 把均值回归从「砍掉」救回来这件事，这里第二次拿到了独立证据。）

离场结构在三种状态下几乎一样（`DRAWDOWN_REDUCE` 53–61%、`STOP_LOSS` 约 11%），
**所以差异不在卖点**：TREND_UP 与 RANGE 用同一套离场规则，一个亏 1.01%、一个赚 0.17%。
TREND_UP 的持仓中位数只有 8 根而 RANGE 是 15 根 —— 趋势买入更快被风控请出去。

**下一步该问的问题因此变了**：不是「趋势策略的哪个参数该调」，而是
**「TREND_UP 里高分买入为什么系统性追在高点」** —— 候选解释有三个，都还没测：
① `T3_BREAKOUT` 要求「收在上轨之外 + 带宽在扩张」，本身就是追高的定义；
② 得分把「几条规则同时成立」等同于「更可信」，而在趋势末端它们**同时**成立恰恰是最危险的时刻；
③ 风控只在**买入之后**起作用（`expandedBbwPct` 那条降级只改提醒级别、不改 direction，见 ⑦）。
其中 ③ 是唯一一条「改了就能测」的：把带宽极高时的**买入**从降级改成抑制，是一次判定逻辑改动，
不是调参 —— 按约束 5 与 ADR-0003，这要先论证再做，不能顺手改。

#### ⑨ 这一节改变了什么读法

- **不许再引用 40 只池上的正收益**（+5.93% / +231,154 元）来说明「策略是赚钱的」。
  它在 261 只上是 −0.94%，而 261 只仍然全是**今天还活着**的股票 ⇒ 幸存者偏差的方向不变，
  真实数字比 −0.94% 更差（§5.15 保留意见三条依旧成立：折间不独立、幸存者偏差、代表性）。
- **docs/09 §2.1 的四种合法结局在这个池上少了两种**：`CALIBRATED` 与 `KEPT` 都要求出厂值先过红线。
  于是「把 27 个 GUESS 清零」这个出口条件在当前配置下**不可达**，不是差几张网格的问题。
- 但 §5.18 那句「杠杆在 `risk` 块」得到了独立复核：96.9% 的离场仍由风控触发，
  而这次多了一条更具体的指向 —— **亏损集中在 TREND_UP 的买入**，这是「买点」而不是「卖点」的问题。
- **八张网格、82 组候选跑完，最好的一个训练集 Calmar 是 −0.078**（`rsi.osBase = 25`）。
  也就是说「换掉某一个一阶参数的取值就能让这套参数在有代表性的池子上不亏」这条路
  **在 OFAT 的范围内已经被排除**。剩下的可能性只有三类：跨参数交互（没测，笛卡尔积跑不完）、
  **判定逻辑本身**（不是取值问题）、或者「就是不该在 TREND_UP 里买」。
  第一类要设计网格，后两类要人拍板 —— 都超出「给已有参数找依据」这一阶段的范围。
- 留下一个可用的方向：**唯一「改善不是靠少做」的块是 `rsi`** —— 放宽超卖基线（15 → 25）
  或缩短周期（14 → 6）让建仓数**增加** 24%–35%、胜率同时从 43.30% 升到 44.6%/45.9%。
  别的块最好的候选全是把建仓数压下去换来的（`boll.k` 2.5 少做 34%、`midReversionStd` 2.0 少做 20%）。
  真要在买点上动手，数据指的第一个方向是这里。

---

### 5.21 随机入场基准（2026-08-15）：给 −0.94% 装上零点，结论是**负 alpha，而且没有一个状态例外**

#### 为什么需要它

§5.20 报出「TREND_UP 建仓 396 次 / 仓位加权 −1.01%」，但这个数**缺一个零点**。
唯一的基准是沪深300，而它满仓、我们平均资金占用只有 3.69%（§5.13）。
于是两件事分不开，而它们的对策完全相反：

| | 含义 | 对策 |
|---|---|---|
| **A** 策略没信息 | 同池同期随机买入也是 −1% | 问题是「该不该在 TREND_UP 里买」 |
| **B** 策略有信息但符号反了 | 随机明显好于 −1% | 「得分越高越糟」不是噪音，判定逻辑值得深挖 |

#### 方法：配对随机化

`src/backtest/random-audit.ts`（`pnpm audit:random`）。对**每一次真实建仓**，
在**同一只票**上随机挑一个入场日、持有**同样的根数**、走**同一套 `costs.ts`**。
标的构成、持仓时长分布、投入资金、费率滑点全部对齐，**唯一变化的是入场时点**。
成交口径与 `simulate.ts` 逐条对齐（次日开盘成交、涨停开盘买不到、跌停开盘顺延 ≤ 5 根、
`hasGap` 不成交、手数向下取整）。RNG 由 `--seed` 定，可复现。

**三列的差别只有两处，读的时候不许混**：

| 列 | 入场日 | 离场 |
|---|---|---|
| 真实（含风控） | 信号 | 风控（止损 / 回撤减仓 / 移动止损 / 盈利保护） |
| **真实入场·被动** | 信号 | **被动持有同 span** |
| **随机·被动** | **骰子** | **被动持有同 span** |

**只有后两列之差是入场质量** —— 它们结构完全一致。前两列之差才是风控离场的作用。
另有「同 regime 随机」一档：随机日限定在与真实建仓相同的市场状态里，
用来把「状态本身差」与「状态内挑得差」拆开。

> **regime 自检 1853/1853 = 100.00%**。同 regime 抽样需要整条序列一次算完的 regime，
> 而 `simulate` 用的是每根切 320 根窗口。两者等价是**推理**，所以工具拿真实建仓的
> `regimeAtEntry` 逐条比对、对不上就报错退出（与 `audit:subsignals` 同一条纪律）。
> 100% 命中，等价性从推理变成了事实。

#### 结果：仓位加权收益的四段分解

261 只 · 1853 次建仓全部配对成功（跳过 0）· 200 次试验 · seed=1。

| 状态 | 建仓 | 池子漂移<br>（无条件随机） | 同 regime<br>随机 | 真实入场<br>·被动 | 真实<br>含风控 | **状态效应** | **挑选效应** | **风控效应** |
|---|---|---|---|---|---|---|---|---|
| ALL | 1853 | +0.25% | +0.24% | −0.04% | −0.14% | −0.01pp | **−0.28pp** | −0.10pp |
| **TREND_UP** | 396 | +0.21% | **−0.24%** | **−0.93%** | −1.01% | **−0.46pp** | **−0.69pp** | −0.08pp |
| RANGE | 922 | +0.33% | +0.48% | +0.34% | +0.17% | +0.14pp | −0.14pp | **−0.17pp** |
| TRANSITION | 535 | +0.12% | +0.18% | −0.06% | −0.03% | +0.06pp | −0.24pp | +0.02pp |

「真实入场·被动」落在随机分布里的分位（200 次试验）：

| 状态 | 无条件分位 | 同 regime 分位 | 胜率（同 regime）分位 |
|---|---|---|---|
| ALL | 10.0% | 9.0% | 76.5% |
| **TREND_UP** | **1.5%** | **3.5%** | **4.5%** |
| RANGE | 53.5% | 34.0% | **100.0%** |
| TRANSITION | 34.0% | 32.5% | 12.5% |

**种子稳健**：seed 1/2/7 三次，TREND_UP 的收益分位是 1.0% / 0.0% / 0.5%，
胜率分位三次都是 0.0%（无条件档）。不是一次抽样的运气。

#### 四条读数

**① 答案是 B，不是 A。** TREND_UP 的入场在 200 次随机试验里排到第 1.5%–3.5% 分位，
胜率排到第 4.5%。**这套规则不是「没信息」，是在系统性地挑更差的日子** ——
随机的东西不会连着三个种子都落在分布的尾巴上。§5.20 ⑧ 那条「得分是反向指标」
（得分 ≥ 0.8 的 198 次 −1.39%）由此拿到独立佐证。

**② TREND_UP 的 −1.01% 拆得开，而且挑选比状态更贵。**
池子漂移 +0.21% → 站在 TREND_UP 里 −0.46pp → 状态内挑选 **−0.69pp** → 风控离场 −0.08pp。
「不在 TREND_UP 里买」只买回其中的 0.46pp，**剩下 0.69pp 在子信号与得分那一层**。
两个方向都得动，只做一个不够。

**③「差异不在卖点」得到独立确认，而且要加一句限定。**
TREND_UP 的风控效应只有 −0.08pp（近乎中性），§5.20 ⑧ 那条结论成立。
**但 RANGE 的风控效应是 −0.17pp** —— 把 +0.34% 削成 +0.17%，是四个状态里最大的一个。
风控离场在趋势里不伤，在震荡里伤。

**④ 推翻 §5.20 ⑧ 的一个读法：RANGE 赚的是 beta，不是 alpha。**
「全池唯一赚钱的引擎是 RANGE 里的 `R3+R4`（+182,213 元）」这句在**绝对盈亏**上没错，
但 RANGE 的「真实入场·被动」+0.34% **低于**同 regime 随机的 +0.48%，分位 34%（噪音带内）。
也就是说那笔钱是「那段时间在震荡状态里随便买也能赚」，不是挑出来的。
**严格地说：四个状态里没有一个的入场是正 alpha。**

> **但 RANGE 有一处真的 alpha，方向却是错的**：胜率分位 **100.0%**
> ——「真实入场·被动」50.87%，而 200 次随机试验没有一次达到（随机均值 46.23%）。
> 收益分位却只有 34%。**挑中了更多小赢，同时也挑中了更大的亏** ——
> 正是 §5.18 那条「『提高胜率』不能单独当标定目标」警告的形状，
> 这次它出现在**已有的出厂参数**上，而不是某个候选上。

#### 这一节不能证明什么

- **不能**说「策略整体不如随机」。本表的持仓根数是照抄真实建仓的（内生量：跌得快的被止损在
  8 根、涨上去的被拖到 15 根），所以它答的是「**在同样的持有时长安排下，点选得好不好**」。
  要答「策略整体好不好」得连离场规则一起随机化，是另一个实验。
- **不能**外推到别的池子或别的窗口。幸存者偏差在两组里同向，所以**分位数**大体不受影响，
  但绝对值仍然乐观。
- **不能**当成「反着做就能赚」。分位 1.5% 说的是这套规则挑的日子比随机差，
  不等于取反之后能覆盖摩擦成本 —— 那要单独测，且要先过 §5.20 ⑨ 那条「改善不能靠少做」。

#### 续（同日）：分层到子信号与得分档，并做两道稳健性控制

工具加了三样东西：按**入场子信号组合**与**入场得分档**分层（52 层，`--min-count` 默认 30）、
**逐次建仓收益率中位数**（抗离群）、`--shuffle-spans`（剥 `holdingBars` 内生性）。

##### 控制一：中位数——两个「正 alpha」是单笔妖股撑起来的

| 分层 | 建仓 | 加权分位 | **中位分位** | 说明 |
|---|---|---|---|---|
| 信号 `T1+T2+T3+T4` | 78 | **95.0%** | **0.0%** | 加权 +2.53%，但**最赚的 3 笔占总盈亏 235%**，去掉后翻成 −2.56% |
| `TREND_UP · 得分 0.75-0.80` | 89 | 68.0% | 0.5% | 同一形状 |

那笔离群是 **SZ002969 2025-12-03 → 2026-01-14，+325.8%**（不复权 +375.6%、复权 +345.3%，
两轨一致，**数据无误**，就是一次妖股行情）。
**教训**：细分层只有几十次建仓，只看加权就会把一次行情读成一个机制。
`Summary.medianPnlPct` 因此进了工具，报告里两个口径并排打印。

##### 控制二：打散跨度——内生性确实在放大负 alpha，但没有推翻方向

`holdingBars` 是内生的（跌得快的被止损在 8 根、涨上去的被拖到 15 根），
于是「真实入场 + 原 span」这一列里短 span 天然配着下跌，而随机组的短 span 配的是随机结果。
`--shuffle-spans` 每次试验把 span 在建仓之间随机置换，**两组共用同一个置换**，
于是 span 与结果解耦、两边分布仍逐位相同。读数是**逐试验配对**的「真实赢的试验占比」
（50% = 与随机无异）。

| 分层 | 建仓 | 原口径<br>加权分位 | **打散后配对胜率**<br>seed 1 / 2 / 7 | 判定 |
|---|---|---|---|---|
| **`TREND_UP · 信号 T2+T3+T4`** | 143 | 3.0% | **3.0 / 4.0 / 4.0%** | **硬负 alpha**，内生性对它几乎无影响 |
| `TREND_UP · 得分 ≥0.80` | 198 | 2.5% | 13.5 / 25.0 / 24.0% | 负，但原口径夸大了约一倍 |
| `TREND_UP` | 396 | 3.5% | 19.5 / 27.0 / 29.5% | 负 |
| `ALL` | 1853 | 9.0% | 30.0 / 27.0 / 33.5% | 弱负 |
| **`TREND_UP · 信号 T1+T2+T4`** | 149 | 47.0% | **58.0 / 63.0 / 63.5%** | **弱正** |
| **`RANGE` / 信号 `R3+R4`** | 922 / 911 | 34% / 41% | **67.5 / 64.0 / 59.0%**<br>**69.0 / 66.0 / 63.5%** | **弱正** |

##### 三条结论（前一节有一条要更正）

**① 负 alpha 不是「TREND_UP 里买」这件事，而是一个具体组合：`T2+T3+T4`。**
同在 TREND_UP、样本量几乎相同（143 vs 149），`T2+T3+T4` 配对胜率 **3–4%**，
而 `T1+T2+T4` 是 **58–63.5%**。两者的差别是 **有 T3 无 T1 / 有 T1 无 T3**：

> `T3_BREAKOUT` 的成立条件是「收在上轨之外 **+ 带宽在扩张**」——**定义本身就是追高**。
> `T1_MA_CROSS` 是均线金叉，位置上更靠前。
> **数据说的是：T3 单独带队时是负 alpha，有 T1 同时成立时不是。**
> 这是 §5.20 ⑧ 候选解释 ① 的**精确化版本**——不是「T3 有毒」，是「T3 缺了 T1 的确认时有毒」。

**② ⚠ 更正本节前半部分的结论 ④：RANGE 不是「只有 beta」。**
剥掉内生性之后 `RANGE` / `R3+R4` 的配对胜率是 **59–69%**，是**弱正 alpha**。
前半部分基于原 span 口径判「分位 34% ⇒ beta 不是 alpha」，
那个 34% 里混着内生性造成的系统性压低。**§5.20 ⑧「唯一赚钱的引擎是 RANGE 的 R3+R4」这句话
在 alpha 意义上也站得住**，此前那句更正撤回。
（但要限定：中位口径上 RANGE 与随机基本持平（−0.54% vs −0.56%），
所以它的优势在**加权**而非**典型一次**——与胜率分位 100% 但收益分位偏低是同一件事的三种测法。）

**③ 得分反向仍然成立，但幅度比原口径小。** `TREND_UP · 得分 ≥0.80` 打散后 13.5–25%，
仍在 50% 以下，但不是原口径读出来的 2.5%。

##### 这两道控制改变的读法

- **任何细分层的结论必须同时看加权与中位**，背离时以中位为准（一次妖股能翻转加权的符号）。
- **凡是拿真实 `holdingBars` 做配对的读数，都被内生性系统性压低**——包括本节前半部分那张四段
  分解表。方向可信，**幅度要打折**。
- 52 个分层没有做多重比较校正，所以只有**跨种子稳定**且**两个口径同向**的形状才算读数。
  按这条筛下来，全部结论只剩上面三条。

#### 处置

- 工具进仓库：`src/backtest/random-audit.ts` + `pnpm audit:random`，**只统计不改参数**，
  与 `audit:regime` / `audit:subsignals` 同类。
  三个开关：`--match-regime`（同状态抽样）· `--shuffle-spans`（剥内生性）· `--min-count`（细分层门槛）。

---

### 5.22 滑点敏感性（2026-08-15）：出厂 0.1% 是行业常规区间的下缘，结论只会更差

行业常规做法给滑点的区间是 **0.2%–0.5%**，而 `costs.ts` 的出厂值是 **0.1%**。
把它扫一遍（261 只 × 84 折，只传参不改出厂值）：

| 滑点 | 全期收益 | Δ | 建仓数 | 建仓级胜率 | 建仓加权 | 最大回撤 | 夏普 | 平均占用 |
|---|---|---|---|---|---|---|---|---|
| **0.1%（出厂）** | **−0.94%** | — | 1853 | 44.85% | −0.14% | 4.24% | −0.121 | 3.69% |
| 0.2% | −2.60% | −1.66pp | 1854 | 42.88% | −0.39% | 4.96% | −0.346 | 3.68% |
| 0.3% | −3.73% | −2.79pp | 1852 | 42.39% | −0.56% | 5.57% | −0.500 | 3.66% |
| 0.5% | −6.34% | **−5.40pp** | 1853 | 39.88% | −0.97% | 7.30% | −0.869 | 3.59% |

**三条读数**

**① 单调、线性、且与算术对得上。** 建仓加权每档降约 0.21pp / 0.17pp / 0.41pp，
而每 +0.1pp 滑点的**往返**成本恰是 2 × 0.1pp = 0.2pp。数字自洽，说明成本模型接得没错
（这也是这一轮顺带做的一次实现校验）。

**② 不是「少做」效应。** 建仓数在四档下几乎不动（1853 / 1854 / 1852 / 1853），
所以绩效下降全部来自摩擦本身，不是交易被成本挡掉。
——这一点必须点明，因为 §5.20 ⑦ 里几乎所有「改善」都是靠少做换来的，
如果这里建仓数也变了，这张表就没法读。

**③ 出厂 0.1% 是区间下缘 ⇒ 现有结论偏乐观。** 取行业中位的 0.3%，
全期从 −0.94% 变成 **−3.73%**（四倍），夏普从 −0.121 变成 −0.500。
**「策略不赚钱」这个结论在整个常规区间上都成立，而且越往区间中部走越硬。**

#### 处置：**不改出厂值**（计划文档 §1.1 已拍板）

理由不是「0.1% 更准」，是 `costs.ts` **三方共用**（回测 / 影子运行 / 实盘成交记账）——
改出厂值会连带改实盘记账的手续费，而那里用的是**用户真实成交价**，本来就不套滑点
（`trades/ledger.ts` 头注释那条）。0.1% 对流动 A 股 + 散户仓位也站得住。

**但这一节的数字要当「结论稳健度」引用**：往后凡是说「−0.94%」，
都应当同时知道它在 0.3% 滑点下是 −3.73%。
产物：`reports/calib/slip-{0.001,0.002,0.003,0.005}.json`。
**测试集未被触碰**（四次都没加 `--touch-test`），累计仍是 **5 次**。
- 产物：`reports/calib/random-uncond.json` / `random-regime.json`（`reports/` 不进版本控制，
  重跑命令记在本节开头）。
- **测试集未被触碰**（本工具读的是既有基线报告的 trades，不跑新的切分）。累计仍是 **5 次**。
- 这一节回答了 [下一阶段取舍与迭代计划](./下一阶段取舍与迭代计划.md) §3.1，
  并把 §1.1 拍板题 A 的依据补齐：**方向 ② 已被排除**（40 只池的正收益同样是 beta），
  ①、③ 的选择现在有数可依。

---

### 5.23 TREND_UP 的时间维度（2026-08-15）：**假设反了 —— 「刚进入就买」没问题，「走了 11 根以上再买」才是追高**

#### 做了什么

`simulate.ts` 的 `BacktestTrade` 加了一列 `barsInRegimeAtEntry`，取的是**引擎自己发布的**
`RegimeState.heldDays`（不在回测里重算 —— 重算一份会让「统计用的持续根数」与「判定用的」
悄悄分叉，与 `audit:regime` 读 `evidence` 而不复写判定逻辑同理）。
重跑 261 只基线：**5459 行逐笔与旧基线逐位相同、全期 −0.9377% 一字不差**，
加字段没有改变任何一笔交易。然后按持续根数分层跑 `audit:random`。

#### 结果（TREND_UP，`--match-regime --shuffle-spans`，三个种子）

| 持续根数 | 建仓 | 配对胜率<br>seed 1 / 2 / 7 | 加权分位 | 判定 |
|---|---|---|---|---|
| **1-3 根（刚进入）** | 223 | **~56 / 56.0 / 57.5%** | 34 / 40 / 35.5% | 与随机无异，**略偏正** |
| **4-10 根** | 102 | ~52 / 55.5 / 49.5% | 16 / 16 / 15% | 与随机无异 |
| **11-30 根** | 69 | **3.0 / 3.0 / 3.0%** | 7 / 6 / 9.5% | **强负 alpha** |
| >30 根 | 2 | — | — | 样本不足，未打印 |

**不是渐变，是一道坎**：10 根以内 ≈ 50%（与随机无异），11 根以上直接掉到 3%。

#### 这一节推翻了什么

[下一阶段取舍与迭代计划](./下一阶段取舍与迭代计划.md) §3.3 写的假设是
「**刚进入 TREND_UP 就买**的错误率可能远高于 TREND_UP 持续一段后买入」。
**数据是反的**：刚进入那 223 次是三档里最好的（配对 56–57.5%，甚至略偏正），
而走了 11 根以上的那 69 次是全项目最极端的负 alpha 之一。

这个方向对得上「追高」的直觉——**趋势已经走了两周多，突破上轨再进场，就是在末端接棒**——
但它与那条假设相反，说明当时那个猜测是从「状态切换有滞后」推的，而不是从数据来的。

#### 与 §5.21 的关系：**大幅重叠，不是两个独立发现**

| | 建仓 | 与「持续 ≥ 11 根」的交集 |
|---|---|---|
| `TREND_UP · 持续 ≥ 11 根` | 71 | — |
| `TREND_UP · 信号 T2+T3+T4` | 143 | **51（占长持续的 72%）** |
| `TREND_UP · 得分 ≥ 0.80` | 198 | 39 |

**72% 的长持续建仓同时也是 `T2+T3+T4`。** 所以这两条读数说的基本是同一批交易，
只是从两个角度描述：`T3_BREAKOUT`（收在上轨外 + 带宽扩张）**在趋势走了一段之后触发**，
就是末端追高的形状。**不要把它们当成两个独立证据去相加。**

三者并集覆盖 TREND_UP 的 276/396。

#### 落到一个可测的候选

`T2+T3+T4` 那一条更宽（143 vs 71）且同样极端（配对 3–4%），所以它是更好的单一靶子。
但「持续 ≥ 11 根不买」有一个实际优势：**它是一条与子信号无关的、正交的闸门**，
砍掉 71/1853 = **3.8% 的建仓**，远低于拍板题 B 那条「建仓数不得下降超过 15%」的门槛。

**两条都还没测**（改判定逻辑要走拍板题 B 的三条门槛，且每个候选机制只报一次分位）。

> **一处必须先说清的陷阱**：这两条候选的样本重叠 72%，
> 所以**不能先测一条、再在剩下的样本上测另一条**——那会把同一批交易的效应记两次。
> 要测就分别在**完整基线**上各改一次、各报一次分位。

---

### 5.24 第一次判定逻辑改动（2026-08-15）：`T3_BREAKOUT:BUY` 在 `TREND_UP` 不计票，四条门槛全过

#### 这一节与前面二十节的性质不同

§5.4–§5.23 全是**调参与测量**。这一次改的是「哪些信号成立」本身 ——
交易的**集合**变了，所以配对 Δ 那套判据不适用（它的前提是两边测同一批东西）。
按 [docs/07 §3.6](../07-回测与验证方案.md)（这次立的）走四条门槛。

- **论证**：[docs/04 §3.1a](../04-指标与信号引擎.md)，**写在代码之前**。
- **依据**：§5.21（TREND_UP 入场排第 3–4 分位）+ §5.20 ⑧（`T4` 在 396/396 恒成立）。
- **`ENGINE_VERSION` `0.2.7-unvalidated` → `0.2.8-unvalidated`**。

#### 改了什么

`src/core/strategies/trend.ts`：`T3_BREAKOUT:BUY` 增加一个条件 `ctx.regime !== 'TREND_UP'`。
一句话理由：在已被判定为上升趋势的状态里，「又创新高 + 带宽扩张」不提供新信息 ——
「在涨」是判定前提，`T4_ALIGNMENT` 几乎必然同时成立，而带宽扩张在趋势中段是延续、
末端是加速赶顶，日线上分不开。**不引入任何数值**（结构条件，不产生新的 `GUESS`）。

#### 四条门槛逐条

| 门槛 | 改动前 | 改动后 | 结论 |
|---|---|---|---|
| ① 先有论证 | — | docs/04 §3.1a（含「可能错在哪」） | ✅ |
| ② 建仓数下降 ≤ 15% | 1853 | 1675 | **−9.6%** ✅ |
| ③ 建仓级胜率不降 | 44.85% | 45.31% | **+0.47pp** ✅ |
| ④ alpha 上移 | 见下表 | 见下表 | ✅ |

**门槛④ 主判据**（`--match-regime --shuffle-spans` 的配对胜率，三个种子）：

| 分层 | 改动前 1/2/7 | 改动后 1/2/7 | 判定 |
|---|---|---|---|
| ALL | 30.0 / 27.0 / 33.5% | **46.5 / 45.0 / 46.5%** | **+16pp**，接近 50%（与随机无异） |
| **TREND_UP** | **19.5 / 27.0 / 29.5%** | **66.5 / 67.0 / 69.0%** | **+42pp**，从负 alpha 翻成正 |
| RANGE | 67.5 / 64.0 / 59.0% | 65.0 / 64.5 / 61.5% | **不动** ← 见下 |
| TRANSITION | 30.5 / 24.0 / 28.5% | 22.0 / 22.5 / 25.5% | 略低，区间重叠 |

**门槛④ 第二部分（子集稳定性）在改动之前就做了**——这是它的正确顺序。
规则命中那批交易在四个独立子集上的表现（vs 该子集基线）：

| 子集 | 命中批 | 该子集基线 | 差距 |
|---|---|---|---|
| 代码前半（奇偶轮转） | −0.77%(115) | −0.04% | −0.73pp |
| 代码后半 | −1.75%(73) | −0.25% | −1.50pp |
| 时间前段 <2022-07 | −1.87%(64) | −0.43% | −1.44pp |
| 时间后段 ≥2022-07 | −0.79%(124) | +0.03% | −0.82pp |

四个子集**同向且量级接近**（−0.73 ~ −1.50pp）⇒ 抓的是机制不是某段行情。
（对比被否掉的候选「TREND_UP 持续 ≥ 11 根不买」：同样四个切法下差距是 −0.26 ~ −3.30pp，
**相差 12 倍**，效应几乎全部来自 2022 前的 23 笔。见 §5.23。）

#### 两条对论证的独立验证

**① 改动确实只落在 TREND_UP。** 分 regime 的建仓数：

| | 改动前 | 改动后 |
|---|---|---|
| TREND_UP | 396 | **207** |
| RANGE | 922 | **923** |
| TRANSITION | 535 | **545** |

RANGE 与 TRANSITION 基本没动（+1 / +10 是资金释放与持仓位释放的二阶效应）。
docs/04 §3.1a 列的第一条证伪条件是「若 RANGE 同时变差，说明 T3 的作用不是我说的这样」——
**没有发生**，RANGE 的配对胜率 65.0/64.5/61.5% 与改动前 67.5/64.0/59.0% 落在同一区间。

**② 预测的砍掉量与实际接近但不相等**：静态预测 188 笔，实际 1853 − 1675 = **178 笔**。
差的 10 笔是二阶效应（腾出的资金与持仓位让别的信号补位）。
**这也是为什么「静态删行重算」只能当方向性参考，不能当结论。**

#### 绩效顺带（**不是判据**）

| | 改动前 | 改动后 |
|---|---|---|
| 全期收益 | −0.94% | **−0.09%** |
| 净盈亏 | −249,777 元 | **−27,208 元** |
| 最大回撤 | 4.24% | 3.86% |
| 夏普 | −0.121 | −0.008 |
| 平均资金占用 | 3.69% | 3.49% |

**⚠ 这一组数不许拿去说「策略变好了」。** 砍掉 9.6% 的交易本来就会改善绩效
（§5.20 ⑦ 那八张网格的教训）。真正的判据是上面那张配对胜率表。
而且**全期仍然是负的**（−0.09%），训练窗口也没有翻正 ——
`calibrate.ts` 的红线依旧咬着，21 个 `BLOCKED` 参数一个都没解锁。

#### 没有解决的

- **TREND_UP 剩下的 207 次建仓仍是 −0.85% 加权**（改动前 396 次是 −1.01%）。
  配对胜率翻正说明「剩下的这批入场不比随机差」，但**它们仍然在亏钱** ——
  那是 beta（这些票在这些窗口里本来就跌）而不是 alpha 问题。
- **`ALL` 的配对胜率 45–46.5% 仍略低于 50%**，主要拖累项变成了 `TRANSITION`（22–25.5%）。
  下一个候选应该看那里，而不是继续在 TREND_UP 上加码。
- 测试集**未被触碰**（本节全部在训练+验证窗口上跑），累计仍是 **5 次**。

#### 一条要记住的程序事实

**`ENGINE_VERSION` 变了 ⇒ 参数指纹变 ⇒ 指标缓存整体作废重算 ⇒ 影子运行按纪律停止累积。**
这是改引擎的固定代价，不是这次的副作用。影子账本本来就还没有第一个净值点（§5.23 之前已记），
所以这次没有实际损失 —— 但下一次改动之前要先确认影子里有没有攒着东西。

---

### 5.38 口径的第三方交叉验证：**BOLL / RSI 逐位一致，ATR 与 ADX 口径不同**（2026-08-19）

> §6 那条开放缺口（「算法一致性已验证，**口径一致性未验证**」）从 M2 起挂到今天。
> 阻塞理由一直写作「本机无 Python 科学计算库 / 需要行情软件截图」。
> **两条都绕过去了**：聚宽公开的数据字典页里，每个技术指标函数都印着一组返回值示例，
> 而他们对这几个指标的备注是「计算方式与通达信、东方财富和同花顺相同」
> ⇒ 拿它对，等于**一次性与三家国内平台对照**，不需要账号、不装任何东西，
> 也**不触碰 JQData 用户协议**（那三条管的是 JQData 拉下来的数据，见[信源台账 §7](./信源台账.md)）。
>
> 工具：`npx tsx scripts/verify/jq-crosscheck.ts`（数据 `pnpm fetch:history --out ./data/verify-jq`）。

#### ⚠ 先说最花时间的一步：**文档写的 `check_date` 与打印出来的数字不是同一天**

按示例里写的 `2017-01-04` 比，**一项都对不上** —— 连 BOLL 中轨这种「20 个收盘价求平均」
都差 0.8%。按 MA20 与 20 日标准差反查，真实日期是 **2017-01-26**；
DMI 那一组又落在另一天（约 2016-05-04，且万科那一格是它 2015-12-18 停牌前的最后一个值）。
⇒ **示例字典是各函数分别生成的，日期字面不可信。**
这条单独记，是因为它会让人在「口径不同」与「日期不对」之间浪费一整轮：
第一版对照全红，看起来像我们整套指标都错了。

#### 结果（2017-01-26，平安银行 / 万科A，不复权价，喂 750 根）

| 指标 | 我们 | 聚宽 | 相对差 | 结论 |
|---|---|---|---|---|
| BOLL 上轨 | 9.28999458862 | 9.28999458862 | **9.1e-14** | ✅ 逐位一致 |
| BOLL 中轨 | 9.17450000000 | 9.17450000000 | 7.7e-16 | ✅ |
| BOLL 下轨 | 9.05900541138 | 9.05900541138 | 9.5e-14 | ✅ |
| RSI(6) | 86.6977850029 | 86.6977849416 | **7.1e-10** | ✅ Wilder 平滑口径相同 |
| BOLL 上轨（万科） | 21.3780281109 | 21.3780281109 | 4.8e-15 | ✅ |
| RSI(6)（万科） | 45.6698392556 | 45.6698393531 | 2.1e-9 | ✅ |

**① 布林带的标准差「除 n 不除 n−1」被外部证实，而且这不是「精度内说不清」**：
同一窗口改成除 n−1，上轨会变成 9.29299494770 / 21.3931612313，相对差 **3.2e-4 / 7.1e-4** ——
比命中的 1e-14 大十个数量级 ⇒ 这次对照**有分辨力**。docs/04 §1.4 与 CLAUDE.md 那句
「国内平台口径」从此有实证。

**② `MACD 柱 = 2×(DIF−DEA)` 与日期无关地确认**：用他们自己印的三组 (DIF, DEA, 柱)
做代数 —— 三只票逐位吻合（如 −0.0144009349611 = 2×(0.0244744579641 − 0.0316749254446)）。
DIF/DEA 本身没找到匹配日期，所以「我们的 EMA 与他们的 EMA 是不是同一个数」**仍未验证**。

**③ ATR 口径不同，而且是逐位可判的**：他们的文档写「求真实波幅的 N 日**移动平均**」，
实测 `MA(TR,14)` 命中**残差 0**（0.0600000000000 / 0.511428571429），
而我们是 `Wilder(TR)/14`（0.0712 / 0.5775，差 13~19%）。
⇒ 我们的 ATR 系统性更平滑、更滞后。**这不是 bug**（docs/04 §1.5 写的就是 Wilder），
但 docs/05 §2.3 的自适应止损是 `2×ATR/close`，那两个数因此**不是同一把尺**。

**④ ADX / DMI 口径不同 —— 这一条影响最大**。把八种平滑组合在三只票上各扫一遍，
排名第一的一致是 **`EXPMEMA(14)` 平滑 DI + `EXPMEMA(6)` 平滑 ADX**（残差 0.2~1.7，
其余组合 2.3~10.5），也就是通达信那套公式；我们是 **Wilder(1/14) + ADX Wilder(14)**。
两处差别：平滑系数 **2/15 vs 1/14**（快一倍），ADX 的平滑周期 **6 vs 14**。

#### ④ 的量级：在我们自己的池子上跑两套，**「ADX ≥ 20」有 30.4% 的日子结论相反**

699,907 个「股票·交易日」（`data/history` 里 549 个 fixture，后复权）：

| 口径 | 均值 | 中位 | p25 | p75 | ≥20 占比 |
|---|---|---|---|---|---|
| **我们**（Wilder/14 + ADX Wilder 14） | 25.11 | 23.06 | 17.49 | 30.75 | **63.4%** |
| **通达信**（EXPMEMA 14 + ADX EXPMEMA 6） | 33.71 | 30.26 | 20.34 | 44.25 | **75.9%** |

**「≥20 与否」结论相反的日子占 30.4%。** 按分位对齐：通达信口径下 ADX<20 的占 24.07%，
我们口径下同一分位对应的阈值是 **17.29**。

**读法（三条，少一条就会读反）**：

1. **这不是说 `adx.baseThreshold = 20` 错了。** 那个值已经在本地标定过，且是
   **`KEPT` 里少数有正面证据的一个**（±2 邻域两个方向都更差）—— 本地标定量的是我们自己的 ADX，
   结论在我们的量纲里成立，不受本节影响。
2. **它说的是「20 这个数的来历不构成证据」**：它转述自国内平台语境，而那个语境下的 ADX
   与我们算的**不是同一个量**。约束 2 / ADR-0003 又添一个实例，而且是最隐蔽的一种 ——
   **数值抄对了，量纲没抄对**，从代码上完全看不出来。
3. **有一个可查的空档**：分位等价值 **17.29** 落在已测邻域（18/20/22）**之外**，只差 0.7。
   ⇒ 下一轮 `adx` 网格值得把下侧探到 **16–18**。这是一条具体的待办，不是结论。

#### 没做 / 不能说的

- **MACD 的 DIF/DEA 未验证**（找不到匹配日期），只验了柱的倍数关系。
- **国泰君安那一组是前复权**（三根线整体差 2.2%、比例一致，RSI 差 5.1e-4）⇒ 只能比形状。
  它顺带说明**前复权示例带二位小数舍入**，所以哪怕找对日期也到不了 1e-14。
- **没有换任何出厂参数，没有改任何指标实现。** 本节全部是「量一量差多少」。
- 未触碰测试集（本节不跑回测），`--touch-test` 计数不变（仍是 5）。

---

### 5.39 绩效口径的同一次对照：**最大回撤/年化/波动率同口径，夏普不同 —— 我们的夏普系统性偏高一倍**（2026-08-19）

与 §5.38 同一趟（聚宽 API 文档「风险指标」一节），把**绩效**定义也逐条对了一遍。
指标口径那次问的是「指标算得对不对」，这次问的是「**我们说的夏普 0.57，别人听到的是什么**」。

| 项 | 我们（`src/backtest/metrics.ts`） | 聚宽 | 判断 |
|---|---|---|---|
| 最大回撤 | `max((峰值 − 净值)/峰值)` | `Max((Px−Py)/Px), y>x` | **同一定义** |
| 年化收益 | `(1+P)^(1/years) − 1`，`years = bars/243` | `(1+P)^(250/n) − 1` | **同一公式**，只差年交易日常数 |
| 年交易日 | **243** | **250** | 差 2.8% ⇒ 我们的年化**略低**（12 年半的沪深300：11.55% vs 11.90%）。A 股实际约 242–244，**243 更准，250 是惯例** |
| 日收益标准差 | 样本标准差（除 n−1） | `sqrt(250/(n−1) × Σ(r−r̄)²)` | **同一口径**（都除 n−1） |
| **无风险利率** | **0**（`metrics.ts` 头注释：没有可靠本地来源，报告里注明） | **默认 0.04** | ⚠ **实质差异** |
| **夏普分子** | `日均收益 × √243`（算术） | `年化几何收益 − Rf` | ⚠ **实质差异** |
| Calmar | 年化 / 最大回撤 | 他们不给这个指标 | 我们自己的排序量，不受 Rf 影响 |

#### 量级：拿沪深300 被动持有当尺子（两个真实窗口）

| 窗口 | 总收益 | **我们的夏普** | **聚宽口径的夏普** | 拆开：换 Rf | 拆开：换分子 |
|---|---|---|---|---|---|
| 2005-04→2017-12（3091 日） | +301.70% | **0.533** | **0.281** | −0.142 | −0.111 |
| 2018-01→2023-12（1456 日） | −16.06% | **−0.054** | **−0.355** | −0.204 | −0.097 |

**两个来源同向叠加，合计约 0.25–0.30 个夏普**：几何年化永远 ≤ 算术日均年化（波动越大差得越多），
而扣 4% 无风险利率又减一截。⇒ **我们的夏普在长窗口上几乎是主流口径的两倍。**

#### 三条读法

1. **不改算法。** `metrics.ts` 头注释里「rf = 0 并在报告里注明」是有理由的选择
   （无风险利率没有可靠的本地来源，硬填一个数是新的未标定参数）。而且**夏普不参与任何门槛** ——
   标定排名口径是 Calmar、折上口径是总收益 —— 所以这条差异**不改变任何既有结论**。
2. **但它改变「怎么说」**：CHANGELOG 与 M2 里那些「夏普 0.57 / 0.33」的数字，
   换成外部口径大约是 **0.3 / 0.05**。⇒ **我们的夏普只能当口径统一的内部排序量，
   一句话都不许拿去和外部策略/产品的夏普横比。**（[§4.6](./下一阶段取舍与迭代计划.md) 早就写着
   「不能与外部夏普横比」，理由当时记的是自相关；现在知道**还有一个更大的、纯定义上的**理由。）
3. **年化那 243 vs 250 不用改**：我们的更准，而且方向保守（年化更小 ⇒ Calmar 更小）。
   只是引用外部年化数字时要知道差 2.8%。

**没做**：没跑任何回测（本节只用沪深300 指数序列本身），未触碰测试集，`--touch-test` 仍是 5。

---

### 5.40 【缺陷】后复权价位让 **10 只主池标的静默地一笔都不建仓**（2026-08-19 查出，未修）

**怎么查到的**：拿聚宽的「未来函数」清单逐条对我们的 docs/07 陷阱表时，发现「复权」那一行
仍写着「全程用前复权序列」—— 而 2026-08-12 早就换成后复权了（腾讯的前复权是加性的、会变负）。
顺着这处措辞去核实语义，撞上了下面这件事。**外部清单本身没有新东西，是它逼着做了一次自查。**

#### 事实

回测 fixture 的 `*Adj` 是**后复权**，锚在上市日 ⇒ 老票的后复权价可以是真实价的一两百倍：

| 标的 | 2018 首根真实价 | 2018 首根 `openAdj` | 一手（100 股） | 2018–2023 买不起的交易日 |
|---|---|---|---|---|
| SZ000001 平安银行 | 13.7 | **2069** | 20.7 万 | **100.0%** |
| SH600309 万华化学 | — | 1137 | 11.4 万 | 96.2% |
| SZ000858 五粮液 | — | 1181 | 11.8 万 | 94.1% |
| SH600809 山西汾酒 | — | 185 | 1.9 万 | 37.2% |

而 `--capital` 默认**每标的 10 万**，`lotsAffordable` 按 `openAdj` 算手数 ⇒ 返回 **0 手**。

**实测确认**（`--codes SZ000001,SH600309,SZ000858,SH600000 --from 2018-01-01 --to 2023-12-31`）：

```
SZ000001  收益 0.00%   0 笔  胜率 —
SH600309  收益 0.00%   0 笔  胜率 —
SZ000858  收益 0.00%   0 笔  胜率 —
SH600000  收益 -16.64% 9 笔  胜率 0.00%   ← 对照，正常交易
```

**报告上「0 笔」与「引擎没给信号」长得一模一样**，没有任何一行告警。

#### 范围

主池 261 只里：**整段买不起 1 只 · 部分买不起 9 只 · 全程正常 251 只**；
按「标的·交易日」加权 **1.48%**。

#### 三条读法

1. **机制早就记在案，范围没人量过。** CLAUDE.md 那条「拿指数或高价标的回测时必须同时调
   `--capital`」说的就是这个 `lotsAffordable` 返回 0、**不进任何计数器**的行为 ——
   但它是按「指数点位 3000–5000」写的，**没人想到后复权会把普通股票也顶上去**。
2. **偏差方向不是随机的**：后复权倍数 ≈ 累计分红送转，命中的恰恰是**分红历史最长的大盘股**
   （平安银行、五粮液、万华化学、山西汾酒）。⇒ 我们的回测在结构上**从不交易这一类标的**。
   这与「幸存者偏差」是两件独立的事，不能互相抵消着读。
3. **它有多重要不好说，但一定要写出来。** 平均资金占用只有 3.5–4%，1.48% 的标的·交易日
   大概率不改变任何既有结论；**可是没人验过**，而「验一验」的代价是重跑基线。

#### ✅ 处置：用户 2026-08-19 选 A，**当天实施**

`CodeResult.unaffordable` 计数 + 报告告警行（点名标的与次数），
`report-calibrate.test.ts` 两条用例钉着「有它就必须说」与「没有别乱说」。
**任何绩效数字都没变**（同一条命令重跑：总收益 −1.99% · 建仓 1097 次 · 建仓胜率 43.21% ·
平均每次 −477 元，与既有基线逐位一致）。

**实施后第一次拿到确切量级** —— 此前只知道「0 笔」，不知道被吞掉几次：

| | 训练窗口 261 只 · 2018–2023 |
|---|---|
| 被吞掉的建仓意图 | **31 次** |
| 同期实际建仓 | 1097 次 |
| 占比 | **2.8%** |
| 涉及标的 | 4 只：`SZ000001`×13 · `SH600309`×7 · `SZ000858`×7 · `SH600809`×4 |

注意「10 只受影响」（按价位算）与「4 只真被吞」（按实际建仓意图算）**不是同一个数**：
另外 6 只在买不起的那些日子里本来也没出买入信号。**引用时别混。**

**候选 B（按真实价定手数）仍然挂着**，不是被否 —— 只是它要重跑全部基线，
而当前所有结论都建立在旧口径上。下面那张表保留。

#### 两个候选处置（**A 已实施；B 仍挂着**）

| 选项 | 做法 | 代价 |
|---|---|---|
| **A 只让它可见** | 统计「因买不起一手而未建仓」的次数，进报告告警行（与「全样本仅 N 笔」同一档） | 不动任何数字 ⇒ 所有历史基线仍可比。缺点是问题还在，只是不再隐身 |
| **B 按真实价定手数** | 用不复权价决定手数、后复权价算盈亏（或按 `Adj/raw` 比例放大每标的资金） | **所有历史回测数字全部失效**（含 −0.94% 那条基线与八张标定网格），要重跑；换来的是「10 万元」这个仓位设定名副其实 |

**用户已选 A（2026-08-19）。** 当时的理由：B 改的是仓位模型，而当前所有结论都建立在旧口径上，
在「边缘约等于零」还没解决的时候重置全部基线，成本远大于 1.48% 这个量级。
但 A **必须**做 —— 一个「看起来像没信号」的静默排除，正是这个项目一直在防的那类失真。

**本节的改动只有计数与告警**（A），成交口径一个字没动；未跑标定，测试集触碰仍是 5。

---

### 5.41 聚宽第四轮：**组合层判据的口径对照 —— 一个候选判据被自己否掉**（2026-08-19）

**为什么读这一节**：08-19 方向转到「仓位/配置层」之后，[配置形态论证 §4 ③](./配置形态-论证.md)
写着一句悬着的话 ——「判据必须从逐笔交易统计换成**净值曲线层面**的比较，而这需要一个现在还没有的工具」。
所以这一轮去外面看的不是策略，是**判据**：净值层面的通行度量有哪些、它们在我们这种
「绝大多数时间空仓」的曲线上会给出什么。

**方法**（与 §5.38 / §5.39 同一条路，公开页、不登录、不触碰 JQData 协议）：
读聚宽 API 文档的「风险指标」与「投资组合优化」两节（`pnpm fetch:page --render`，18 万字），
把定义搬到**已经存在的四份报告**上重算。工具 `scripts/verify/jq-riskmetrics.mjs` ——
**只读**，`metrics.ts` 与 `report.ts` 一个字没改。
**没跑任何新模拟**（四份报告都是既有产物），`--touch-test` 仍是 **5**。

#### 四份既有报告，用组合层口径重读一遍

| 报告 | 池 / 窗口 | 年化（策略 / 基准） | **beta** | 占用 | **alpha rf=0** | alpha rf=4% | 超额 减法 / **除法** | 日胜率 / 基准下跌天数占比 |
|---|---|---|---|---|---|---|---|---|
| `t3fix` | 261 只 · 2018→2026-08 | −0.01% / 1.55% | **0.0205** | 3.49% | **−0.04%** | −3.96% | −14.19% / **−12.44%** | 49.9% / **49.7%** |
| `liq-base` | 252 只 · 训练窗口 | −0.34% / −2.88% | 0.0177 | 3.50% | **−0.28%** | −4.21% | **+14.06%** / +16.75% | 50.9% / 50.4% |
| `etf-train-boardfix` | 12 只 ETF · 训练窗口 | 0.35% / −2.88% | 0.0470 | 8.60% | **+0.49%** | −3.32% | +18.20% / +21.68% | 50.6% / 50.4% |
| `idx-cap-0517` | SH000300 · 2005→2017 | 1.71% / 11.55% | 0.0371 | 11.52% | **+1.29%** | −2.57% | **−277.56%** / **−69.10%** | 45.9% / 45.9% |

定义全部照聚宽那一节：`beta = Cov(Dp,Dm)/Var(Dm)` · `alpha = Rp − [Rf + β(Rm−Rf)]` ·
除法版超额 `(1+Rp)/(1+Rm) − 1` · `日胜率 = 当日策略收益跑赢基准的天数占比`。
年化仍用我们的 243（§5.39）。

#### ① beta 与「平均资金占用」互相印证 —— 这是好消息

`t3fix` 的 beta **0.0205** vs 占用 **3.49%**，两条完全独立的路（日收益回归 vs 持仓市值×天数）
落在同一个量级。⇒ 那条老纪律（「超额收益离开占用率就会被读反」）**第一次有了净值层面的独立确认**，
而 beta 的好处是**不含任何参数**、也不依赖建仓价近似。四份报告的 beta 与占用逐份同向
（占用 3.5% → beta 0.02；占用 11.5% → beta 0.037）。

#### ② **CAPM alpha 不能当判据 —— 它的符号由一个我们没有本地依据的参数决定**

这是本轮真正的产出，而且是个**否定结论**：`idx-cap-0517`（单指数择时，2005–2017）

- `alpha(rf=0)` = **+1.29%/年** ⇒ 读起来像「风险调整后是正的」；
- `alpha(rf=4%)` = **−2.57%/年**（聚宽的默认 Rf 就是 0.04）⇒ 读起来像「明显为负」；
- 而用户口径的事实是 **+24.14% vs 被动 +301.70%**（§5.32），十二年半只拿到三成净值。

根因是算术：低暴露策略几乎全程持现 ⇒ `α ≈ Rp − Rf(1−β) − βRm`，`β≈0.04` 时
`Rf` 那一项几乎是原封不动地进减号 ⇒ **`Rf` 支配结果**。
⇒ **不采纳 alpha 当任何门槛**（与 §5.39 对夏普的处置同一条：Rf 硬填一个数就是新的未标定参数）。
它只能当 beta 的附注，且**只报 rf=0 那一栏**，并且一句话都不许拿去说「风险调整后是正的」。
`liq-base` 那行同样要小心反过来的错：报告打印的**减法超额是 +14.06pp**（基准跌 16%、我们几乎空仓），
而 alpha(rf=0) 是 **−0.28%** —— **两个口径符号相反，都是同一条曲线**。
⇒ 别引用「训练窗口跑赢基准 14 个点」，那句话在算术上成立、在含义上是空的。

#### ③ **日胜率对低暴露策略毫无信息量**（实测，不是推理）

四份报告的日胜率与「基准下跌天数占比」逐份贴合，`idx-cap-0517` 上是 **45.9% vs 45.9%**。
原因一句话：空仓日的日收益是 0，基准跌它就「赢」。
⇒ 这个指标只在暴露接近满仓时才有意义 —— **配置形态可用，择时形态不可用**。
（这条本来是我的猜测，之所以值得记，是因为它被四条曲线量到了，其中一条逐位相等。）

#### ④ 超额收益：**减法版在大基准涨幅上读不出意思，除法版能读**

聚宽专门为此写了一段论证（净值是几何增长 ⇒ 自然的运算是除法）。量级上最明显的就是
`idx-cap-0517`：**减法 −277.56%**（这个数无法解释成任何东西）vs **除法 −69.10%**
（= 策略净值只有被动的 **30.9%**，可以直接读）。
⇒ **引用超额收益时用除法版**。**用户当天拍板允许调整，已实施**：报告现在两个都打印
（`基准 −16.06%  超额 14.06%（除法 16.75%）`），老字段 `excessReturn` 保留不动 ——
历史 M2 里所有引用都是减法版，改掉它会让旧结论对不上号。

#### ⑤ 顺带看到的：组合优化工具箱，以及它对我们的真实含义

聚宽 `jqlib.optimizer` 的形状是「**一个目标 + 一组约束 + 逐标的上下限**」：
目标有 `MinVariance` / `MaxProfit` / `MaxSharpeRatio(rf=0.0)` / `MinTrackingError` /
**`RiskParity(count=250, risk_budget=None)`** / `MaxScore` / `MaxFactorValue`；
约束有 `WeightConstraint` / `WeightEqualConstraint` / **`AnnualStdConstraint(limit, count=250)`** /
`AnnualProfitConstraint` / `IndustryConstraint` / `MarketConstraint('stock'|'etf'|'bond_fund'…)` /
`ExposureConstraint` / `BarraConstraint` / `TrackingErrorConstraint`，全部 `date=前一日` 取协方差，
且有 `return_none_if_fail`（**优化失败是一个显式的失败态**，他们的示例打印「警告：组合优化失败」——
与我们「拿不到就是 `UNKNOWN`，不许显示成 `CLEAR`」同一条纪律）。

三条读法，第三条最重要：

1. **我们的数据条件下能实现的只有「只需价格序列」那一档**：`MinVariance` / `RiskParity` /
   `MaxSharpeRatio` / `AnnualStdConstraint` / `WeightConstraint`。
   行业类（`IndustryConstraint` / 偏离度）、Barra 类、因子类、追踪误差类**全部落空** ——
   前者卡在[信源台账 §4](./信源台账.md) 那格「行业分类没有源」，后者卡在没有因子库。
2. **`AnnualStdConstraint(limit=0.15, count=250)` 说明外部把「波动率目标」写成
   「组合层约束 + 250 日协方差」**，而 §5.37 那次是「单标的 + 20 日已实现波动 + 缩放」。**这不构成重跑理由** ——
   [配置形态论证 §8](./配置形态-论证.md) 那条「不许换一个 `σ_target`/换一个窗口重跑到通过为止」仍然有效，
   任何新形态要**另立预注册、另写判据**。
3. **成熟工具箱的存在不是这条路变容易的证据，反而是变难的证据**：每个目标函数都自带
   `count=250` 这类窗口参数、每个约束都自带一个 limit ⇒ 参数数量放大，而论证 §4 ③
   那条「决策次数更少、样本量问题更严重」会立刻咬上来（约束 2 一视同仁：这些数一个都没有本地依据）。

#### 落地：用户当天拍板「允许调整」，报告加了两个量、**否掉的那两个一个都没加**

| 动了什么 | 判据 |
|---|---|
| `PerformanceBlock.excessReturnRatio`（除法版超额）+ 报告那行括号里打印 | ④ |
| `PerformanceBlock.beta` + 紧挨着「平均资金占用」打印 | ① |
| **没有** alpha、**没有** 日胜率 | ② ③ —— 理由写进了 `metrics.ts` 的 `betaOf` 头注释，**防的是日后有人「顺手补全」** |

**绩效数字一个没变**（同命令重跑 20 只 × 训练窗口：总收益 −1.99% · 占用 3.50% ·
beta 0.0177 · 除法超额 +16.75%，与 `scripts/verify/jq-riskmetrics.mjs` 在旧 JSON 上算的
**逐位相同** ⇒ 报告内实现与独立脚本互为交叉验证）。改动是纯增量：老字段 `excessReturn`
与所有既有门槛（标定排名 Calmar、写回配对 Δ）一个字没动。
用例：`costs-metrics.test.ts` 两条（beta 的 Cov/Var 与 null 边界；除法版拿 §5.41 那组真实数字）·
`report-calibrate.test.ts` 两条（缺基准时四个量一律 null；beta 精确 0.5 的对照曲线）。

#### 这一轮明确**没有**做的

- **没改任何绩效算法**（夏普、Calmar、最大回撤、占用率一个字没动）；上面那两个字段是新增量，
  不参与任何门槛。`scripts/verify/jq-riskmetrics.mjs` 保留 —— 它现在唯一的独有用途是
  **算那两个被否掉的量给人看**（alpha 的 Rf 敏感性、日胜率与基准下跌天数占比的贴合）。
- **没有重判 §5.37**，也没有据此改任何判据 —— 上面 ①③④ 是**下一次**预注册可以选用的度量，
  已登记在[配置形态论证 §9](./配置形态-论证.md)。用它们回头去读一个已经报过的实验就是移动球门。
- **ETF 那份 alpha(rf=0) = +0.49% 不构成新证据**：同一条曲线、同一批 59 次建仓、
  验证窗口依然反向，只是换了一种量法。它唯一的作用是与「ETF 是唯一见过 Calmar > 0 的形态」自洽。
- 没跑新模拟、没触碰测试窗口（`--touch-test` 仍是 5）。

---

### 5.42 【预注册】同 regime 口径的块定义：按 regime 段整段交换（2026-08-19 写，**结果未看**）

**背景**：[§5.36](./M2-偏差报告.md) 把择时零分布从「逐次独立抽日」换成「按建仓月整块位移」，
无条件口径成功（吸附距离恒 0、四层分位一致朝 50% 移 +7~14pp），但**同 regime 口径失败并自动降级**
—— 那一档的候选池只含同状态的天、稀疏，共用位移只能靠吸附落地（中位 11 / P90 116 根），
实测按分层引入偏置（TREND_UP 90.8 / RANGE 22.3，方向相反）。
⇒ **看板上那些同 regime 的分位（TREND_UP 66.5% / RANGE 65.0% / TRANSITION 22.0%）至今是
「未调整上界、偏向显著」**，而 alpha 配对胜率是本项目唯一可信的判据。
[迭代计划 §4.6](./下一阶段取舍与迭代计划.md) 把它登记成唯一的未完成项，候选修法也写在那里。

**这一轮要做的**（就是那条登记的候选修法，不是新想的）：

| 项 | 设定 |
|---|---|
| 块的定义 | **(标的, 一段连续同状态行情)** —— 用 `--match-regime` 已经在算的 regime 序列切出极大同状态段，块 = 落在同一段里的那些建仓 |
| 零样本怎么生成 | 整块**刚性平移**到同一只票、**同一状态的另一段**里：块内成员间距逐位保留 |
| 合法性 | 候选平移量只取「**所有成员都落在自己 `pool` 里**」的那些 ⇒ **吸附距离恒 0 是结构保证**，不是实测结果。空集 ⇒ 该块退回独立抽样并计数 |
| 不动的东西 | `pool` 的构造（regime + 预热 + 跨度边界）· 分层键 · 配对方式 · 真实组一侧 · 无条件口径那条路（它已经精确） |
| 样本 | `t3fix.json` 的 1675 次建仓（0.2.8 · 261 只 · 全期），与 §5.36 同一份基线 |
| 种子 | **1 / 2 / 3**，三个都报 |
| 报告 | **一次**。看到结果之后不许改块定义再报第二次 |

**判据（先写死，四条全过才算修好）**：

1. **吸附距离恒 0**（若实现后不为 0，说明实现错了，不是「接近就行」）；
2. **块覆盖率 ≥ 80%** 的建仓；**逐层也报覆盖率**，某层 < 80% ⇒ 那一层继续标「未调整上界」；
3. **三个种子方向一致**（与 §5.36 对无条件口径的要求相同）；
4. 分位的变化方向要与「旧零分布方差偏小」这个机制**一致** —— 也就是朝 50% 移动。

**预测（写在看到结果之前）**：

- 分位整体**朝 50% 移动**，量级参考无条件口径那次的 +7~14pp；
- 逐层：TREND_UP 66.5% **下降** · RANGE 65.0% **下降** · TRANSITION 22.0% **上升** · ALL 46.5% **上升**；
- **覆盖率我预测落在 60–90%**，且 **TREND_UP 是最低的那一层**（上升趋势段少而短，
  同一只票上找不到第二段够长的同状态行情）⇒ 若它 < 80%，按判据 2 那一层维持「未调整上界」；
- **若出现反向变化或跨种子不一致 ⇒ 判修法失败**，与吸附那次同一形状（块定义又引入了新偏置），
  处置是**保留自动降级**并把这一节记成第二次否定结论。

**这一轮不做的**：不跑测试窗口（`--touch-test` 仍是 5）· 不动引擎与任何参数 ·
不因为分位变好/变差去调整任何策略候选（这是判据升级，[skill](../../.claude/skills/迭代/SKILL.md)
把它列在「✅ 是自我进化」那一栏，而「看到结果再改判据」在另一栏）。

#### 结果（2026-08-20 跑完，三个种子，只报这一次）

命令：`pnpm audit:random -- --baseline reports/calib/t3fix.json --fixtures ./data/history
--match-regime --shuffle-spans --trials 200 --seed {1,2,3}`
（产物 `reports/calib/rb-mr-s{1,2,3}.json`，seed 1 另存 `random-mr-regime-block.json` 供看板取）

**结构指标：全部达成，而且比预测好得多**

| 项 | 结果 | 对预注册的判据 |
|---|---|---|
| 吸附距离 | **中位 0 / P90 0** | ✅ 判据 1（结构保证，不是「接近 0」） |
| 块 | 1660 块，覆盖 **1672/1675 = 99.8%** | ✅ 判据 2 |
| 逐层覆盖 | TREND_UP **100%** · TRANSITION **100%** · RANGE 99.7%（920/923） | ✅ 每一层都过门槛 |

⚠ **我事先预测「覆盖率 60–90%，TREND_UP 最低」—— 两半都错。** 上升趋势段虽然少，
但每只票上"另一段同状态行情"几乎总能找到；真正找不到的只有 3 次（RANGE），
原因是那只票整段序列里 RANGE 只有一段。**这条预测被证伪要记下来**：
我当时把「段少」想成了「段短且难以容纳」，而块的跨度中位数只有几根，容得下。

**分位（配对胜率 %，`--shuffle-spans` 档）**

| 层 | 建仓 | 旧（独立抽日） | 段块 s1 | s2 | s3 | 段块均值 | 变化 |
|---|---|---|---|---|---|---|---|
| **ALL** | 1675 | 46.5 | 32.5 | 40.0 | 37.0 | **36.5** | **−10.0** |
| TREND_UP | 207 | 66.5 | 62.0 | 71.0 | 68.0 | 67.0 | +0.5（跨种子横跨旧值） |
| **RANGE** | 923 | 65.0 | 44.5 | 42.5 | 43.5 | **43.5** | **−21.5** |
| TRANSITION | 545 | 22.0 | 23.0 | 28.0 | 27.0 | 26.0 | +4.0 |

被动分位同向：ALL 11.5 → 8.0/5.5/9.5 · RANGE 43.0 → 19.0/21.0/25.5 ·
TRANSITION 13.0 → 15.5/14.5/13.5 · TREND_UP 33.5 → 26.0/29.0/27.5。

#### 判据逐条：**结构目标达成，但我预注册的机制解释被证伪**

| # | 判据 | 结果 |
|---|---|---|
| 1 | 吸附恒 0 | ✅ |
| 2 | 覆盖率 ≥ 80%（逐层） | ✅ 99.8%，逐层 ≥ 99.7% |
| 3 | 三种子方向一致 | ✅ ALL / RANGE / TRANSITION（三个种子同侧）· ❌ **TREND_UP**（62.0 在旧值下方，68.0/71.0 在上方） |
| 4 | 变化方向与「旧零分布**方差**偏小」这个机制一致（⇒ 朝 50% 移动） | ❌ **ALL 反而远离 50%**（46.5 → 36.5） |

**判据 4 不过，而且诊断出了确切原因** —— 拿随机组自己的两个矩去看：

| 层 | 随机组均值（旧 → 新 s1/s2） | 随机组 sd（旧 → 新 s1/s2） |
|---|---|---|
| ALL | 0.363% → **0.470% / 0.442%** | 0.264% → 0.251% / 0.257%（**几乎没动**） |
| RANGE | 0.422% → **0.610% / 0.613%**（+0.19pp/次） | 0.324% → 0.287% / 0.322%（没动） |
| TRANSITION | 0.485% → 0.449% / 0.388% | 0.507% → 0.530% / 0.435% |

⇒ **两个零分布的差别几乎全在均值（成分），不在方差。** 也就是说
「旧口径把成批建仓当独立样本 ⇒ 方差偏小 ⇒ 分位偏乐观」这个我写在预注册里的机制，
**在同 regime 这一档上不成立**（在无条件那一档 §5.36 是成立的）。
真正发生的是：`findBases` 按**位置**均匀抽落点 ⇒ **长段贡献更多落点 ⇒ 随机组被推向「长的同状态行情」**，
而那些日子平均更赚（RANGE 上每次 +0.19pp）⇒ 真实组相对更差。

#### 三条读法（比上面那张表重要）

1. **配对胜率对零分布定义极度敏感，敏感度比我们以为的大一个量级。** 同一个 ALL：
   **46.5%**（独立抽日 · 同 regime）· **55.5%**（建仓月块 · 无条件，§5.36）· **36.5%**（段块 · 同 regime）
   —— 跨度 **19pp**。⇒ **从今天起引用配对胜率必须带零点定义**，
   「ALL 46.5%」这种说法单独拿出来已经没有意义。
2. **RANGE 那个 65% 站不住，而这次与 §5.35 独立同向。** 段块给 43.5%、
   §5.35 的跨池口径给 35.5% ⇒ **三个口径里两个说 RANGE 低于 50%**，65% 是最乐观的那个。
   这**加强**了 08-19「停止在个股日线择时上找边缘」的决定 ——
   但注意顺序：**决定在前，这个数在后**，它不是那个决定的依据。
3. **TREND_UP 仍然悬着，而且这次也没说清。** 均值 67.0 几乎不动，但跨种子 9pp、n 只有 207
   ⇒ 与 §5.35 那个 99.5% 一起留在「等样本，不等灵感」那条待办里。**不许拿这次的 68 或 71 去立项。**

#### 处置

- **`REGIME_BLOCK` 采用为 `--match-regime` 下的默认零点**（`--independent-days` 仍可复现旧口径做对照）。
  理由不是「它更对」，而是两条：**无吸附**（§5.36 那个按分层引入偏置的病没有了），
  且它给出的数**更保守** —— 判据只能变难不能变松。
- **报告与看板都印零点定义 + 覆盖率 + 那条"成分不是方差"的警告**（已实现），
  并明说两档**不可互相替代**。
- **登记一个新候选，不在这一轮做**：落点按**段**均匀抽（先均匀选一段同状态行情，再在段内选位置），
  以消掉上面那个长段权重。**它是另一个设计，不是 bug 修复** —— 现在改就是「看到结果再改块定义」，
  所以要另写预注册与预测（记进[迭代计划 §4.6](./下一阶段取舍与迭代计划.md)）。

**没做**：没跑新的回测、没动引擎与参数、没碰测试窗口（`--touch-test` 仍是 5）。

---

### 5.43 【预注册】段块的落点权重：按**段**均匀抽 vs 按**位置**均匀抽（2026-08-20 写，**结果未看**）

**背景**：§5.42 的段块实现里，`findBases` 把所有合法落点摊平成一个列表、**按位置均匀抽** ⇒
**长段贡献更多落点 ⇒ 随机组被推向「长的同状态行情」**，而那些日子平均更赚
（RANGE 每次 +0.19pp）。诊断显示两个零点的差别几乎全在**均值**（成分）而不是方差。
§5.42 把「按段均匀抽」登记成一个**新候选**而不是 bug 修复 —— 因为它是另一个设计，
看完结果立刻改就是「改块定义直到好看」。这一节是它自己的预注册。

**要做的**：`findBases` 的落点按段分组；抽样改成**两步** ——
① 在「有合法落点的段」里**均匀选一段**；② 在该段内的合法落点里均匀选一个。
旧行为（按位置均匀）保留在 `--block-weight positions` 后面，默认改成 `runs`。

**为什么这可能更对（语义判据，先写下来）**：block permutation 的语义是**交换块**，
一次抽样对应「这一簇建仓如果发生在**另一段**同状态行情里」——
那是**按段**的一次抽取。按位置均匀等于用段长给段加权，那更像「在同状态的日子里随机挪」，
反而靠近它要取代的独立抽日。⇒ **采用哪个默认由语义定，不由数字定**（这条最重要）。

**判据（先写死）**：

1. **覆盖率不下降**（应当逐位相同 —— 结构没变，只换了抽样权重）；吸附仍恒 0；
2. **随机组均值应当回落**（这是这个改动的**机制预测**）：`RANGE` 的随机组加权收益
   从 §5.42 的 0.610% 往 0.422%（独立抽日那档）的方向回落，且**三个种子同向**；
3. **默认口径按上面那条语义判据定为 `runs`，与分位的高低无关** ——
   若结果显示 `runs` 的分位更高（对策略更有利），**照样采用**，并在报告里注明它更有利；
4. **只报一次**。

**预测（写在看到结果之前）**：

- 覆盖率 99.8% 不变、吸附恒 0；
- 随机组均值回落 ⇒ **四层配对胜率一致朝独立抽日那一侧回移**，
  幅度我猜是把 §5.42 的变化**吃掉一半左右**：`RANGE` 43.5% → **48–56%**、
  `ALL` 36.5% → **40–45%**、`TRANSITION` 26.0% → 24–26%、`TREND_UP` 67.0% → 66–70%；
- **若 `RANGE` 回到 50% 以上**，那么「RANGE 有没有边缘」这个问题会重新变成三个口径**二比一**
  （跨池 35.5% 说没有，段块-runs 与独立抽日说有）⇒ 结论要写成**不确定**，
  **不许**据此推翻 08-19 那个方向决定（那个决定的依据是 §5.35 的跨池口径与分类器上界，不是这个数）；
- 若**四层几乎不动**（< 2pp），说明长段权重不是主因，那 §5.42 那条诊断要被降级成「不完整」。

**不做的**：不跑测试窗口 · 不动引擎与参数 · 不因为哪个口径的分位好看就改默认（判据 3）。

#### 结果（2026-08-20，三个种子，只报这一次）：**机制预测第二次被证伪，而且方向是反的**

命令同 §5.42，加 `--block-weight runs`（现在是默认；`positions` 可复现 §5.42 那一版）。
产物 `reports/calib/rw-mr-s{1,2,3}.json`。

| 层 | 独立抽日 | 段块 · 按位置（§5.42） | **段块 · 按段（本节，新默认）** |
|---|---|---|---|
| ALL | 46.5 | 36.5 | **31.3**（29.0 / 35.0 / 30.0） |
| TREND_UP | 66.5 | 67.0 | **74.2**（70.5 / 76.5 / 75.5） |
| RANGE | 65.0 | 43.5 | **39.2**（39.5 / 44.0 / 34.0） |
| TRANSITION | 22.0 | 26.0 | **20.3**（20.0 / 22.0 / 19.0） |

**判据 2（随机组均值应当回落）⇒ ❌ 不通过，而且反了**：

| 随机组加权收益 | 独立抽日 | 按位置 | 按段 |
|---|---|---|---|
| ALL | 0.363% | 0.453% | **0.467%** |
| RANGE | 0.422% | 0.601% | **0.657%** |
| TREND_UP | −0.223% | −0.133% | **−0.386%** |

我预注册的机制是「长段被按位置加权 ⇒ 随机组被推向长段（那些日子更赚）⇒ 均值上移」，
据此预测改成按段均匀会让均值**回落**。实测**继续上移**（RANGE 0.601 → 0.657），
而 TREND_UP 反而大幅**下移**（−0.133 → −0.386）。
⇒ **「长段权重」不是那个成分偏移的主因**，§5.42 那条诊断只能降级成**不完整**。
真实情况是：**短段与长段的日子性质不同，而且在不同 regime 里方向相反** ——
RANGE 的短段更赚、TREND_UP 的短段更亏。按段均匀给短段更多权重，于是两个方向同时放大。

判据 1 ✅（覆盖 99.8% 逐位不变、吸附恒 0）· 判据 3 ✅ 见下 · 判据 4 ✅（只报这一次）。

#### 处置：**默认取 `runs`，理由只有语义那一条**（判据 3 说了「与分位高低无关」）

这次的结果对策略**更不利**（ALL 31.3 / RANGE 39.2 都比按位置那档更低），只有 TREND_UP 更高。
按判据 3 照样采用 —— 那条判据存在的全部意义就是防止「按结果挑口径」，
而这次恰好是「更保守的那个碰巧也是语义上对的那个」，**别把这种巧合当成验证**。

#### 这一节真正的产出：**同 regime 的分位有一个我们控制不住的口径自由度**

四个零点下的同一批 1675 次建仓：

| 层 | 独立抽日 | 段块·位置 | 段块·段 | 跨池（§5.35） | 极差 |
|---|---|---|---|---|---|
| ALL | 46.5 | 36.5 | 31.3 | 55.0 | **23.7pp** |
| RANGE | 65.0 | 43.5 | 39.2 | 35.5 | **29.5pp** |
| TREND_UP | 66.5 | 67.0 | 74.2 | 99.5 | 33.0pp |

⇒ **口径的效应（20–30pp）比我们历史上任何一次判定逻辑改动的效应（几个 pp）都大一个量级。**
这不是「哪个零点才对」的问题 —— 三个都在同一个合理设计空间里，而它们分不出胜负。
**结论层面的两条纪律**（写进 [docs/07 §3.6 第 ⑤ 条](../07-回测与验证方案.md)）：

1. **同 regime 的分位只能支撑「粗结论」**：方向（>50% / <50%）与跨口径的一致性，
   **不能**支撑「66.5 比 62.0 好」这种精细比较，更不能当写回门槛。
2. **改动前后必须同一零点**，且**报告必须印零点与权重档**（工具已经强制）。

**顺带的一条观察（不是这次要测的东西，也不许据此立项）**
—— ⚠ **2026-08-20 被 [§5.45](#545) 的中位口径推翻了一半，读的时候必须带上那半**：
`TREND_UP` 在**四个**零点下都 > 65%（66.5 / 67.0 / 74.2 / 99.5），**但那全是加权口径**；
中位口径下它只有 **35.2%**（而 `ALL` 中位 2.8%、`TRANSITION` 0.2%）⇒
「唯一跨口径一致为正的层」这句**不成立**，正确的说法是「加权口径下唯一为正的层」；
`RANGE` 反过来 —— 四个里三个 ≤ 50%。这**加强**了 08-19 那两条既有判断
（RANGE 没边缘 · TREND_UP 进待办等样本），但样本仍只有 207 次建仓，
按 [§3.11.4 第 2 条](./下一阶段取舍与迭代计划.md)**分层结果不许拿来立项** —— 要立必须另写论证与预注册。

**没做**：没跑新回测、没动引擎与参数、没碰测试窗口（`--touch-test` 仍是 5）。

---

### 5.44 【预注册】§5.40 的敏感性对照：把 `--capital` 放大 5 倍（2026-08-20 写，**结果未看**）

**为什么跑它**：§5.40 的候选 B（按真实价定手数）要重跑全部基线、复核唯一那个 `CALIBRATED`
参数的依据，代价很大。而 B3 那条捷径（**只把 `--capital` 调大**）一行不改代码就能把
「买不起一手」与大部分取整损失一起消掉 ⇒ **它给出的是 B 能带来的绩效变化的近似上界**。
先买这个数，再决定要不要付那笔账。

**先量清现状的偏差有多大**（2026-08-20 用 `data/history` 实测，主池 261 只，中位价位）：

| | 现状（后复权定手数） | 真实价口径 |
|---|---|---|
| 一手都买不起 | **3 只**（SZ000001 · SZ000858 · SH600309） | 0 只 |
| 每次建仓的闲置资金 | 均值 **4.23%** / 中位 1.63% | 均值 1.03% / 中位 0.53% |
| 闲置 > 10% 的标的 | **21 只**（最糟 45.2%） | 2 只 |

⇒ 偏差有**两层**，而 08-19 只量了第一层（31 次建仓意图被吞）：第二层是
**高 `k` 的票每次只投进 80–90% 的钱**（`k` = 后复权倍数，主池中位 3.04 / p90 13.7 / p99 76.7）
⇒ 组合权重实际按 `(1 − 闲置)` 加权，而它**与分红历史相关，方向不随机**。

| 项 | 设定 |
|---|---|
| 对照 | 同一份 252 只 · 训练窗口 2018-01→2023-12 · 同一参数指纹 `c38e329b`（既有基线 `recheck-after-idx.json`，−1.99% 已验证可复现） |
| 处理 | 唯一差别：`--capital 500000`（5×）。**主判据看它**；另跑 `--capital 200000`（2×）**只做单调性检查** |
| 主判据 | 训练窗口**总收益**与 **Calmar** 的变化 |
| 次判据 | 建仓次数、平均资金占用、`unaffordable` 计数是否归零 |
| 不碰 | 测试窗口 · 引擎与参数 · 任何既有基线文件（这一跑是**对照**，不替换基线） |

**三种读法（写在看到结果之前，这就是决定规则）**：

1. **几乎不动**（|Δ收益| < 0.3pp 且 Calmar 同号同量级）⇒ **候选 B 不值得做**。
   A 那条告警继续挂着，等日后真做仓位管理时一起改。这也是我的**预期**（平均占用只有 3.5%，
   受影响的是其中一小部分）。
2. **明显改善**（尤其 Calmar 转正）⇒ 这就不是记账整洁问题，而是**红线过不去的原因之一**，
   值得付重跑那笔账。
3. **明显恶化** ⇒ 说明既有基线被这个 artifact **美化**过 ⇒ 更得修，而且要连着说清历史数字偏乐观。

⚠ **这一跑不是新基线**：`--capital` 放大同时缩小了取整损失**与**「10 万元」这个仓位设定的
现实性（真实账户确实要面对整手约束）。它只回答「如果没有可负担性与取整损失会怎样」，
**不许拿它的绩效数字去替换任何既有引用**。

#### 结果（2026-08-20）：**读法 1 —— 候选 B 不值得做**

| capital | 总收益 | 年化 | 最大回撤 | **Calmar** | 建仓 | 建仓级胜率 | 占用 | 夏普 | 买不起 | 0 笔标的 |
|---|---|---|---|---|---|---|---|---|---|---|
| **10 万**（基线） | **−1.99%** | −0.34% | 2.96% | **−0.113** | 1097 | 43.21% | 3.50% | −0.412 | **31** | 27 |
| 20 万 | −1.92% | −0.32% | 2.93% | −0.110 | 1102 | 43.65% | 3.56% | −0.390 | 16 | 23 |
| **50 万**（5×） | **−1.99%** | −0.34% | 3.01% | **−0.111** | 1114 | 43.81% | 3.61% | −0.403 | **0** | 23 |

控制组（10 万）与既有基线 `recheck-after-idx.json` **逐位一致**，可复现性确认。

**主判据**：Δ总收益 **+0.07pp（2×）/ 0.00pp（5×）**，都在预注册的 ±0.3pp 带内；
**Calmar −0.113 → −0.111**，离红线（> 0）差的还是那 0.11，量级一个数量级没动。
次判据：`unaffordable` **31 → 0**（5× 下彻底归零）· 建仓 1097 → 1114（**+17**）·
0 笔标的 27 → 23（正是那 4 只回来了）· 占用 3.50% → 3.61%（闲置确实少了）。

⇒ **按预注册的读法 1：候选 B 不值得做。** 那个 artifact 是真的，但它**不解释红线过不去**。

#### 为什么效应这么小（回来的那 4 只是混合的，不是单向的）

| 标的 | 10 万 | 50 万 |
|---|---|---|
| SZ000001 平安银行 | 0 笔 | 12 笔 / **+0.49%** / 胜率 42% |
| SH600309 万华化学 | 0 笔 | 6 笔 / **−14.00%** / 胜率 50% |
| SZ000858 五粮液 | 0 笔 | 7 笔 / **+8.17%** / 胜率 43% |
| SH600809 山西汾酒 | 0 笔 | 11 笔 / **−13.53%** / 胜率 27% |

四只合计 36 笔卖出、净盈亏 **−94,362 元**，而组合净盈亏是 −2,619,333 元 ⇒ 占 3.6%，
且**两只赚两只亏**。⇒ 08-19 那句「被排除的恰恰是分红历史最长的大盘股」在**标的组成**上
仍然成立（这是事实），但**在绩效上它们不构成一个方向** —— 所以补回来既不救也不毁结论。
另一个细节：建仓级胜率反而**升了 0.6pp**（43.21 → 43.81）而总收益不变 ⇒
那批交易赢得更频繁但幅度更小，又一次是「胜率与盈亏比在搬家」（困境说明 Q2 那个形状）。

#### 处置与影响范围

- **候选 B 降级为「不做」**，但**理由要写准**：不是「这个 artifact 不存在」，而是
  **它的量级（≤0.07pp / 0.002 Calmar）比我们要解决的问题（Calmar 差 0.11）小两个数量级**。
  A 那条告警继续挂着，让它保持可见。
- **日后什么时候要重新捡起来**：真正做仓位管理的时候（[配置形态论证 §10](./配置形态-论证.md)）。
  那时「每个标的分多少钱」是决策本身，而现在它被复权倍数决定着 ——
  **那是一个设计缺陷，只是它今天不影响任何结论。**
- **既有基线一个都不用重跑**（这次的三份是对照，不替换任何引用）；
  `strategy.squeezeBbwPct = 20` 的标定依据**不需要复核**（配对 Δ 在同一 capital 口径下比较）。
- **`--touch-test` 仍是 5**（只用训练窗口）。

---

### 5.45 【预注册】入场的**已知暴露**自检：短期反转与流通市值（2026-08-20 写，**结果未看**）

**为什么做它**：[与机构量化系统的差距 §7](./与机构量化系统的差距.md) 把这两条列成优先级 2、3
（「只读分层、不碰交易逻辑、不新增指标」），而它们至今**未测**（§8 明确写着「没有本地证据，
不许当结论引用」）。问的是一句话：**我们那点 alpha 是不是某个已知暴露的影子。**

这一轮的时机变好了一件事：[§5.43](#543) 刚量出「配对胜率的绝对水平有 20–30pp 的口径自由度」。
而本节问的是**同一零点内、跨分层的相对顺序**（单调性）—— 那恰好是那个判据仍然支撑的
「粗结论」档，**不需要绝对水平可信**。

| 项 | 设定 |
|---|---|
| 样本 | `t3fix.json` 的 1675 次建仓（0.2.8 · 261 只 · 2018-01→2026-08），与 §5.42/§5.43 同一份 |
| 零点 | `--match-regime --shuffle-spans`，段块 · **按段均匀**（现默认）· 种子 **1/2/3** |
| 分层 A | **入场前 5 日 / 20 日涨幅**的**当日横截面分位**（在池内所有当天有数据的票里排），五等分 Q1–Q5 |
| 分层 B | **入场时流通市值**的当日横截面分位，五等分 Q1–Q5 |
| 数据 | A：fixtures 的 `closeAdj`，零新数据 · B：`data/liquidity-em/`（280 只 × 逐日 `floatCap`） |
| 口径 | 分位在**信号那根**（`entryIdx − 1`）上算，与 regime 读取同一根；每桶 `minCount ≥ 30` 才打印 |
| 报告 | **一次**。三个种子一起报 |

**⚠ 数据口径的两条边界（写在前面，免得结果出来再解释）**：
① `floatShares` 是**逐月采样**反推的（东财 `f61` 换手率，`_meta` 里写着），日度市值 =
该月股本 × 当日不复权价 ⇒ 月内粒度粗，但**不是**「今日股本回推历史」那个已被否的未来函数；
② `liquidity-em` 覆盖 280 只 vs 主池 261 只，**覆盖不全的建仓要单独计数并报出来**
（与 `missingAnnouncements` 同一处置：静默漏掉会让分层看起来是全样本的）。

**判据（先写死）**：

1. **单调性**：Q1→Q5 的配对胜率要么单调、要么至多一处反转，且 **Q1 与 Q5 差 ≥ 10pp**
   才算「有暴露」；差 < 10pp 一律读成「没有可辨认的暴露」；
2. **三个种子同向**（每一层的 Q1−Q5 符号一致）；
3. **只报一次**，不许换分桶数（五等分写死）或换窗口（5/20 日写死）重报；
4. **处置规则先定**：这一节**只产出观测，不改引擎**。若真出现单调暴露，处置是
   ①写进池选择与判据的注意事项、②若要据此改判定逻辑，必须**另立候选**并走
   [docs/07 §3.6](../07-回测与验证方案.md) 的五条门槛（含「砍掉一批」那条子集稳定性检验）。

**预测（写在看结果之前）**：

- **涨幅分层：我预测低分位更好且大致单调**（Q1 > Q5，差 ≥ 10pp）。依据是本地已有的两条：
  [§5.24](#524) 实测 `T3_BREAKOUT:BUY` 在 `TREND_UP` 里投的那一票「投给了最危险的时刻」，
  而占样本 55% 的 `RANGE` 本来就是均值回归的地盘 ⇒ 我们实际吃的更可能是**反转**而不是动量。
- **市值分层：我预测没有可辨认的单调性**（|Q1−Q5| < 10pp）。依据是**引擎没有任何市值输入** ——
  若真出现单调，那只能是通过波动率/流动性间接来的，**那时需要一个额外的解释而不是一个结论**。
- **若两条都没有单调性**：那这一节的产出是「入场质量与这两个已知暴露无关」，
  差距文档 §8 那两条推论**被证伪**，也算结案。

**不做的**：不跑测试窗口（`--touch-test` 仍是 5）· 不动引擎与参数 · 不新增指标 ·
不因为分层结果去改任何既有候选（判据 4）。

#### 结果（2026-08-20，三种子，只报这一次）

覆盖率先报：**250/250 只有市值数据 · 1675/1675 次建仓全部进了市值分层**（零缺失）。

**⚠ 跑完第一版就发现测法有问题，先补了工具再读**：配对胜率此前**只算在加权口径上**，
而 `weightedPnlPct` 在窄分层上会被单笔极值支配（先例：SZ002969 一笔 +325.8% 翻转过整组符号）。
实测 `流通市值 Q2`（368 次建仓）**加权 87.0%、而中位口径下两组几乎相同**
（真实 −0.545% vs 随机 −0.532%）—— 那 87% 是一两笔妖股撑的。
⇒ 新增 `pairedMedianWinFraction`（中位口径的同一个配对检验），报告与看板都并排印两个。
**下面一律以中位口径为准**（CLAUDE.md 读数纪律 2）。

| 分层 | n | 加权配对胜率 | **中位配对胜率**（三种子） |
|---|---|---|---|
| **前 5 日涨幅 Q1**（跌最多） | 270 | 32.2% | **24.8%**（24/27/25） |
| Q2 | 349 | 46.2% | 45.2% |
| Q3 | 290 | 55.5% | **58.5%** |
| Q4 | 216 | 50.5% | 39.8% |
| **Q5**（涨最多 = 追高） | **550** | 37.5% | **0.5%**（1/0/1） |
| 前 20 日涨幅 Q1→Q5 | 229/428/399/315/304 | 47.0 / 49.0 / 6.2 / 43.2 / 75.3 | **40.0 / 51.2 / 5.3 / 17.2 / 7.0** |
| 流通市值 Q1→Q5 | 304/368/339/336/328 | 25.8 / 87.0 / 1.5 / 82.3 / 22.2 | **24.3 / 49.2 / 7.7 / 47.3 / 4.0** |

#### 判据逐条

1. **流通市值：无可辨认的单调暴露**（中位 24/49/8/47/4，非单调）⇒
   **[差距文档 §8](./与机构量化系统的差距.md) 那条「261 只里的小市值票靠壳价值驱动、污染了结论」
   的推论被证伪。** ✅ 我的预测命中（我事先写「预测市值没有可辨认单调性」）。
2. **前 5 日涨幅：两个假设**（短期反转 / 动量）**双双被证伪。** 反转假设要求低分位最好，
   而 Q1 只有 24.8%；动量假设要求高分位最好，而 Q5 是 **0.5%**。
   实际形状是**倒 U**：极端两侧都差，**追高那一侧差到几乎每次试验都输**。
   ⇒ **我事先写的「预测低分位更好且大致单调」也错了**（我当时的依据是 RANGE 占 55%、
   R1/R2/R4 是均值回归 —— 那个推理漏掉了「跌最多的那一档同样是接飞刀」）。
3. 前 20 日涨幅：**没有可读形状**（40/51/5/17/7，Q3 那个 5.3% 无法解释）⇒ 记为不可读，
   不做任何推断。
4. 三种子同向 ✅（每一层三个种子的差 ≤ 5pp，`Q5` 那个 0.5% 是 1/0/1）。

**⚠ 判据措辞的一处缺陷（如实记）**：我写的是「要么单调、要么至多一处反转」，
而倒 U 恰好只有一处反转 ⇒ 字面上满足，但那不是我想表达的「接近单调」。
下次这类判据要写成「**端点差 ≥ 10pp 且中间不出现比端点更极端的桶**」。

#### 这一轮最值钱的一个数，以及它**不是**什么

**`前5日涨幅 Q5`：n = 550（占全部建仓 33%），中位配对胜率 0.5%。**
也就是说：**在「过去 5 日涨幅处于池内最高那一档」的时候建仓，几乎每一次试验都输给随机日。**
它与 [§5.24](#524) 那次判定逻辑改动**独立同向** —— 那次的论证是
「在已被判为上升趋势的状态里，又创新高不提供新信息，T3 那一票投给了最危险的时刻」。

**但它现在不是一个候选，按判据 4 也不许直接改引擎**：
① 这是**入场质量分层**，不是一条规则 —— 「不在 Q5 建仓」会砍掉 33% 的建仓，
直接踩穿 [docs/07 §3.6](../07-回测与验证方案.md) 门槛 ② 的「≤15%」；
② 那条门槛还要求**四个独立子集同向**（代码前/后半、时间前/后段），本节没做；
③ 「砍掉一批已知负 alpha 的交易必然提升剩余部分的 alpha —— 那是算术不是发现」。
⇒ 要动，必须**另立候选 + 另写论证与预注册**，走五条门槛。

#### ⚠ 顺带推翻我昨天写的一句话（§5.43 与 CLAUDE.md 都已改）

昨天我写「`TREND_UP` 在**四个**零点下都 > 65%，是唯一跨口径一致为正的层」。
中位口径出来之后这句话**只在加权口径下成立**：`TREND_UP` 加权 74.2% / **中位 35.2%**。
同样地 `ALL` 加权 31.3% / **中位 2.8%**、`TRANSITION` 加权 20.3% / **中位 0.2%**、
`RANGE` 加权 39.2% / 中位 40.5%（这一层两个口径一致）。

⇒ **「加权口径被少数大赢家托着」这件事在总体层面也成立**，不只在窄分层上。
从今天起引用配对胜率要同时给两个口径，看板已改。

**一个没解决的口径矛盾（记下来，不装作懂）**：既有字段 `passiveMedianPercentile`
（未打散跨度的中位分位）在 `RANGE` 上是 **100.0**，而中位配对胜率是 40.5% ——
两者一个说「真实中位高于所有随机中位」、一个说「配对下打不过」。
差别来自打散跨度（`holdingBars` 内生性），方向与文档记的相反。**本节不解释它**，
留作独立问题：在那之前**别引用 `passiveMedianPercentile`**。

**没做**：没跑新回测、没动引擎与参数、没碰测试窗口（`--touch-test` 仍是 5）。

---

### 5.46 【预注册】rank IC：一个**不需要零分布**的第二判据（2026-08-20 写，**结果未看**）

**为什么要它**：[§5.43](#543) 量出配对胜率的绝对水平有 **20–30pp 的口径自由度**（零点定义），
[§5.45](#545) 又量出它在加权/中位两个口径间能差 **28pp**（ALL：31.3% vs 2.8%）。
⇒ 我们**唯一可信的判据自己有两个控制不住的自由度**，而在补上一个独立判据之前，
任何策略层改动都测不出来。

rank IC 是横截面因子检验的通行口径，它的性质恰好补这个洞：**它是描述性统计
（秩相关系数），不构造零分布、不做配对、不依赖随机化** ⇒ 那两个自由度都不适用于它。

| 项 | 设定 |
|---|---|
| 问题 | **引擎的组合得分对未来收益有没有横截面排序能力** |
| 样本 | 261 只 · 2018-01→2026-06 的**全部判定根**（不是只有建仓那些）—— 这是与 §5.45 的关键差别：那边条件在「引擎出手了」，这边不条件 |
| 得分 | `evaluate()` 的组合得分（`combine` 层输出），**只取买入方向**（`BUY` ∪ `NEXT_DAY_WATCH`），其余当无信号 |
| 前瞻收益 | 成交那根（`i+1`）开盘买入，持有 **5 / 10 / 20** 个交易日后收盘卖出的收益率，用 `closeAdj` |
| 口径 | 逐日横截面 **Spearman rank IC**；只算当日**有 ≥ 10 个候选**的交易日；同时给**分位组单调性**（按得分五等分，各组前瞻收益中位数） |
| 主判据 | ① `IC` 均值的 **t = mean(IC)/(sd(IC)/√n)**（n = 有效交易日数）· ② 五等分**单调性**（Q1→Q5 的中位前瞻收益，端点差 ≥ 0.3pp 且中间不出现比端点更极端的桶） |
| 报告 | **一次**。三个持有期一起报（它们不是三次尝试，是同一问题的三个时间尺度） |
| 不碰 | 测试窗口以外的任何东西；不动引擎与参数；不新增指标 |

**预测（写在看结果之前）**：

- **我预测 IC 的 t 值达不到 2，且五等分没有单调性。** 依据是本地已有的三条：
  §3.11 的 S0/S1 已经测过「得分能不能排序」并**判死**（那次是配对口径）；
  §5.45 刚测出入场质量与两个已知暴露都无关；而 `combine.scoreThreshold` 那一轮标定
  15 个候选最大 t = 1.4（「怎么动都测不出差别」）。
- **若 IC 的 t ≥ 2 且单调**：那会是一个**与 §3.11 相反**的结果，处置**不是**立刻采信 ——
  而是先解释为什么两个口径给出相反结论（我倾向的解释会是「不条件 vs 条件在出手」这个差别），
  在解释清楚之前不改任何东西。
- **若 IC 接近 0 而单调性也没有**：那这一节的产出是**判据层面的**：
  「得分没有横截面排序能力」这件事在**不依赖零分布**的口径下被独立确认一次
  ⇒ 与 §3.11 的判死互相印证，而且这个确认不受那两个自由度影响。

**⚠ 一条边界**：rank IC 测的是**得分的排序能力**，不是「这套系统赚不赚钱」。
IC 接近 0 **不否定**风控层的价值（回撤 2.96% vs 基准 45%+），也不否定 ETF 那条前向记录。
反过来 IC 好也不等于能赚钱（还要过成本）。**这一节只回答排序能力那一个问题。**

#### 【修正】动手前发现上面这份预注册自相矛盾，改这两条（**结果仍未看**）

写代码时才发现「样本 = **全部判定根**」与「得分**只取买入方向**（其余当无信号）」这两行冲突，
而且后者在**已归档的数据上就不可行**：[§5.33](#533) 实测「同一天有几只票同时给出买入方向信号」
中位数是 **1 只**、只有 401 天 ≥ 2 ⇒ 「当日 ≥ 10 个候选」几乎永远不成立，
按原文跑会得到约 0 个有效交易日。**这是可行性事实，不是结果**（来自旧归档，不是这次的输出），
所以在跑之前改：

1. **得分改为每个判定根的 `combine.breakdown.BUY.final`**（买入方向的组合得分，
   **不管有没有触发**）⇒ 逐日横截面约 250 只，与「样本 = 全部判定根」那一行一致，
   也是更标准的因子检验（不条件在「引擎出手了」上）。
2. **前瞻收益用收盘到收盘**（`closeAdj[i+h]/closeAdj[i] − 1`），不模拟次日开盘成交。
   理由：IC 问的是排序能力，不是可实现收益；把成交与成本混进来会让它变成一个小号回测。
3. **并列（ties）要单独报**：大量判定根的买入得分是 0，Spearman 在并列上取平均秩。
   所以同时给两档 ——「**全体**（含 0 分并列）」与「**得分 > 0 的子集**」，
   并把并列比例打印出来。⚠ 这两档**现在就写死**，不是看到结果再挑。

判据与预测**一个字不改**（t ≥ 2 与五等分单调性；我预测两个都不达标）。

#### 结果（2026-08-20）：**得分是一个负向因子 —— 我的预测两半都错**

工具 `pnpm audit:ic`（`src/backtest/ic-audit.ts`，只读、不构造零分布）。
252 只 · **387,097 个判定根** · 买入得分为 0 的占比 **70.1%**（并列会稀释 IC，纪律 4）。

**得分 > 0 的子集**（可读的那一档）：

| 窗口 | 有效交易日 | 5 日 IC | 10 日 IC | 20 日 IC | Q1−Q5（20 日） |
|---|---|---|---|---|---|
| **训练 2018–2023** | 1140 | **−4.41%**（t −8.9） | **−4.98%**（t −10.0） | **−4.74%**（t −8.9） | +0.75pp |
| **验证 2024–2025H1** | 354 | −1.43%（t −1.4） | −0.87%（t −0.9） | **−3.15%**（t −3.5） | +1.49pp |
| 全期（含测试窗口的日子） | 1741 | −3.57% | −3.90% | −4.42% | +1.03pp |

五等分（训练窗口、5 日）：**−0.23 / −0.46 / −0.60 / −0.75 / −0.67%** —— 近单调递减。

⇒ **得分越高，未来收益越低。** 方向在两个窗口一致，`Q1−Q5` 全部为正（得分最低那档反而更好）。

**我的两个预测都错了**：我预测「t 达不到 2」（实际 |t| 到 8.9–10.0）与「没有单调性」
（实际训练窗口近单调）。**错的方向值得记**：我当时的依据是「§3.11 已判死得分的排序能力」，
而那次测的是**条件在触发上**的小样本（同日有信号的票之间比），这次是**全体判定根**的横截面
（387,097 根）—— **两者不矛盾，问的不是同一个问题**。

#### 三条必须一起读的限制

1. **`t` 是上界，别引用它。** 它把 1741 个交易日当独立样本，而 IC 序列有自相关、
   横截面又同涨同跌 —— 与 [§4.6](./下一阶段取舍与迭代计划.md) 那三处同一个病。
   可引用的是**符号与量级**（训练 ≈ −4.4%、验证 −0.9 ~ −3.2%），不是 t。
2. **五等分的收益**水平**含当日市场共同项**（没做去均值）⇒ 只能看**组间差**，
   不能读成「买 Q1 能赚 1.26%」。而 **IC 本身是干净的**：逐日横截面秩相关，
   市场共同项在当天内被消掉。
3. **强度在验证窗口衰减 2–5 倍**，5 日与 10 日在那里**测不出**（|t| < 1.5）。
   ⇒ 正确的说法是「**符号稳定、强度不稳定**」，不是「稳定的负向因子」。

#### 它与 §5.45 互相印证（两个独立口径、同一个方向）

§5.45 刚测出「前 5 日涨幅最高那一档（n=550）中位配对胜率 **0.5%**」——
而买入得分高的那些根，多数正是突破/趋势票（刚涨过）。
⇒ **两条独立证据同向**：追高最差、得分越高越差。而这次这条**不依赖零分布**，
恰好补上 [§5.43](#543) 量出的那个自由度。

#### ⚠ 三条**不许**从这里推出去的结论

1. **不许推「反着做就能赚」。** IC 是收盘到收盘、**零成本**；A 股散户**不能卖空**；
   而 `Q1`（得分最低）那一档基本上是「一个子信号都没触发的票」，
   买它等于随机持有 + 市场 beta —— 那不是策略。
2. **不许据此改引擎。** 与 §5.45 同一条：这是**入场质量的描述**，不是一条规则。
   要动必须另立候选、另写论证与预注册，走 [docs/07 §3.6](../07-回测与验证方案.md) 五条门槛
   （其中②「建仓数降幅 ≤ 15%」几乎注定拦住任何「高分不买」的写法）。
3. **不许说「这否定了整套系统」。** IC 只回答排序能力。风控层的价值
   （回撤 2.96% vs 基准 45%+）与 ETF 那条前向记录都不在这个问题里。

#### 如实披露两处

- **全期那一档包含测试窗口的日子**（2025-07 之后）。这是描述性统计、**没有据它选任何候选**，
  但看过就要说。**单独的测试窗口读数没做**：第一次尝试因 300 根预热把窗口吃光（0 有效日），
  之后我**刻意不补做** —— 留着它比多看一次值钱。
- **工具第一版的窗口切法是错的**：直接 `--from 2024-01-01` 跑验证窗口只剩 **54** 个有效日
  （预热吃掉了），与 CLAUDE.md 那条「跑验证窗口必须带段前历史」是同一个坑。
  已加 `--eval-from`（早于它的判定根只做预热、不进横截面），上表的验证窗口是修好之后的。

---

## 6. 指标交叉验证的验证状态

docs/07 §2.1 要求三方比对（我们的实现 / Python TA-Lib / 行情软件截图）。实际做到的：

| 参照 | 状态 |
|---|---|
| **独立参照实现**（`scripts/verify/reference.mjs`，不 import src/core，风格刻意相反） | ✅ 已比对，23 条序列全部一致（相对误差 < 1e-6） |
| **第三方口径对照**（聚宽公开数据字典的示例值，其备注为「与通达信/东财/同花顺相同」） | ✅ **2026-08-19 已做**，见 [§5.38](#538-口径的第三方交叉验证bollrsi-逐位一致atr-与-adx-口径不同2026-08-19)：BOLL / RSI **逐位一致**，MACD 柱的 ×2 确认；**ATR 与 ADX 口径不同**（他们是 MA(TR,14) 与通达信 EXPMEMA） |
| Python `pandas-ta` / TA-Lib | ❌ **未执行** —— 本机无 Python 科学计算库；上面那条已经把它要回答的问题答掉大半 |
| 行情软件截图人工抽查 | ❌ **未执行** —— 需要真实标的与真实软件 |

即：**算法一致性已验证，口径一致性已验证一半**。
「我们的 MACD 柱是否与通达信显示的一致」「STD 除 n 是否与同花顺一致」这两句原文里的问题
现在都有实证（都是**一致**）；新查出来的是另外两个**没人问过**的：
ATR 与 ADX 与国内平台口径不同 —— 后者让「`adx.baseThreshold = 20` 这个转述值」
连量纲都对不上（§5.38 ④）。`scripts/verify/README.md` 记了具体做法。

用于比对的 500 根日线是**合成**的（`scripts/verify/synthetic.mjs`，固定种子），
原因同 §5.1。合成序列刻意串了「上涨 → 震荡 → 下跌 → 再上涨」四段，
保证 ADX / BBW 分位不是一条直线。

---

## 7. M2 明确没做的

> **2026-08-13：下面这几条里，提醒分发 / 表情驱动 / `alert_log` / 持仓录入 UI 已由 M3 交付。**
> 本节保留原文作为 M2 当时的记录，不改写 —— 但读的时候别把它当成现状。
> `signal:markRead` 这条通道在 M3 里换成了 `alert:markRead`（按提醒行而不是信号行标记）。

- **提醒分发**：气泡、系统通知、托盘角标、防抖、冷却、频率上限、免打扰探测 —— 全是 M3。
  信号在 M2 的唯一出口是**面板列表**。
- **桌宠表情仍不由信号驱动**：`WATCHING` / `EXCITED` / `ALERT` 一律不点亮。
  表情属于提醒分级的表现层，而分级要经过 docs/05 §4 的四道闸门才算数。
  提前点亮等于绕过还没实现的闸门直接骚扰用户。
- **`signal:markRead`**：要写 `alert_log`，而那张表在 M3 才有内容。仍列在启动日志的未实现通道里。
- **背离检测（T6）**：docs/04 §3.1 明确「首版只做提示不计入得分」，M2 连提示也没做 ——
  它容易过拟合，且没有回测就无从判断，留到有数据之后。
- **持仓录入 UI**：通道已通，界面属 M3（持仓风控强制通道在那时才有意义）。
- **影子运行**：M4。

---

### 5.25 公告日上的入场质量（2026-08-16）：**与随机无异，而且引擎的建仓在公告日上完全没有富集**

**问题**：「用消息面把全市场缩小到部分个股，再让引擎盘中观测」这条设想值不值得做。
判据只有一个 —— **公告日上引擎的入场 alpha 是多少**。

**这次不改任何一笔交易**。做法是把 `t3fix.json`（261 只 · 0.2.8 · 1675 次建仓）里已有的建仓
按「信号那根 K 线当天有没有公告」切成两组，各自配自己的随机基准。
两组出自**同一次模拟**，所以直接可比。

#### 数据

历史公告 264 只 × 2018-01-01~2026-08-15，`scripts/fetch-announcements.mjs`（巨潮），
**256,419 条 / 79,310 个「股票·公告日」/ 0 失败 / 约 30 分钟**。三条形状是实测定死的：

- **`stock` 参数必须是 `代码,orgId`**，只给六位代码**静默返回 0 条**（不报错）；
- **`pageSize` 上限就是 30**，要 100/200 都只给 30 —— 翻页次数是硬成本；
- 平均 **3.23 条/公告日**（公告成簇发布，半年报会拆成正文/摘要/财务报告若干份）。

#### 主判据（打散跨度 · 同 regime · seed 1 · 200 试验 · 只跑了一次）

| 分层 | 建仓 | 配对胜率 | 读法 |
|---|---|---|---|
| ALL | 1675 | **46.50%** | 全体（§5.21 的既有读数） |
| **公告 有** | **226** | **51.50%** | **与随机无异** |
| 公告 无 | 1449 | 48.50% | 同上 |

**结论：公告日上没有正 alpha。** 51.5% 与 50% 的距离在 226 次建仓的噪音带里，
而它相对「公告 无」的 +3pp 同样如此。

#### 两个比主判据更有信息量的副产品

**① 引擎的建仓在公告日上完全没有富集。**

- 公告日占全部股票日：79,310 / (264 × ~2080) = **14.4%**
- 建仓落在公告日的比例：226 / 1675 = **13.5%**

两个数几乎相同 ⇒ **引擎的选点与「有没有公告」之间没有关联**。
这条比配对胜率更硬：它说明公告不是一个能把引擎往好处推、也不是往坏处推的维度，
而是一个**正交**的维度。「用公告缩小范围」因此不改变引擎的入场质量分布，
只是把样本量砍到 13.5%。

**② 我事前的假设方向对、量级错。** 起草时的判断是「公告日会富集 TRANSITION
（引擎 alpha 最差的那档），所以公告选股会把标的推向引擎最差的场景」。实测：

| | TREND_UP | TRANSITION | RANGE |
|---|---|---|---|
| 公告 有（226） | 34（15.0%） | 88（**38.9%**） | 104（46.0%） |
| 公告 无（1449） | 173（11.9%） | 457（**31.5%**） | 819（56.5%） |

TRANSITION 占比确实高了 **7.4pp**，方向对；但远不是「把池子推向最差场景」那种量级。
而且**层内方向还相反**：TRANSITION 里公告日反而更好（40.5% vs 23.0%），
TREND_UP 与 RANGE 里公告日更差（60.0% vs 66.0%、58.0% vs 63.0%）。
三个方向不一致 ⇒ 这几个细分层（最小 34 次建仓）**读不出机制，只能当噪音**。

#### 因此

**「公告选股 + 引擎判定 + 盘中荐股」这条路，在现有引擎上不成立。**
不是因为取数做不到（取数已经证明可做），是因为**公告这个维度对引擎的入场质量没有增量**。

**判据边界（必须一起读）**：

1. 本次测的是「引擎已经选出的建仓里，落在公告日的那些表现如何」，
   **不是**「把候选池换成公告股之后引擎能选出什么」。后者需要在 261 只之外的标的上重跑，
   而那要先解决全市场取数（[P2 评估](./P2-全市场扫描评估.md) 判为当前不可做）。
   两者不等价，但前者是后者的必要条件 —— 必要条件不成立，后者就不必做了。
2. **只报了一次分位**（迭代 skill 的红线）。参数在跑之前定死：
   `--match-regime --shuffle-spans --trials 200 --seed 1 --announce-days 1`，与 §5.21 的基线同配置。
3. 细分层是事后切的，没有做多重比较校正。**`TREND_UP · 公告 有` 那 34 次建仓不许单独引用。**
4. 判据挂在**信号那根 K 线**的日期上，不是成交日 —— 回测 D 收盘判定、D+1 开盘成交，
   拿成交日比公告日会整体错开一天。

#### 工具改动（只读，不碰交易）

`random-audit.ts` 加 `--announcements <dir>` 与 `--announce-days <n>`：**只多两个分层维度，
一笔交易都不改**。一条实现纪律记在这里 —— **没有公告文件的票返回 `null` 而不是 `false`**，
那些建仓整个退出公告分层并在日志里报条数。抓取是分批可中断的，
把「没抓到」和「确认没有公告」混成同一个值，会让一次跑到一半的抓取悄悄把大批建仓塞进
「公告 无」层，而表上完全看不出来（首次冒烟测试时 1041/1675 正是这种情况）。

#### 补测：按公告方向拆开（2026-08-16，同一次会话内追加）

**上面那一轮有一个真实缺口**：判据是「有没有公告」这个二值，**利好与利空被混在一起**。
若两者方向相反，聚合后正好会长成「与随机无异」—— 也就是我拿到的 51.5%。
所以补一次按方向的拆分。

**分类器在看到任何 alpha 结果之前就定死并写进代码**（`toneOf`，`random-audit.ts`）：
高精度低召回，只收方向明确的类别，其余一律中性。四条刻意排除，每条都是「看着有方向、其实没有」：
`业绩预告`（标题多数不写预增预减）· `异常波动`（**价格已经动过的结果，不是原因**）·
`停牌`（重组或风险都可能）· `解除质押`（与质押相反，必须先减掉）。
一天多条时合并规则 **NEG > POS > NEU**（风险优先），同样是事前定的。

语料构成先说，它本身就是半个答案：**方向明确的公告只占几个百分点**。
高频的是程序性公告 —— 股东大会 8.67% · 募集资金 5.54% · 定期报告 7.55% ·
关联交易 3.14% · 担保 2.94%。而减持 1.74% · 回购 2.68% · 问询函 0.90% ·
增持 0.36% · 中标 0.32% · 业绩预增 0.09%。

**结果（同一次模拟、同一套参数，`--min-count 20`）**：

| 分层 | 建仓 | 配对胜率 | 读法 |
|---|---|---|---|
| ALL | 1675 | 46.50% | |
| 公告 无 | 1449 | 48.50% | |
| **公告 中性** | **166** | **39.50%** | **唯一样本量像样的公告桶，明显低于随机** |
| 公告 利空 | 35 | 49.50% | n 太小 |
| **公告 利好** | **25** | **78.50%** | **n = 25，见下** |
| 公告 有（合计） | 226 | 51.50% | |

**「利好 78.5%」不能当成发现**，三条理由：

1. **n = 25。** 这个统计量的有效样本是 25 次建仓（200 次试验之间高度相关 ——
   真实入场那 25 个结果是固定的），所以它在零假设下的方差极大。
2. **这一轮打印了 88 个分层，没有做多重比较校正**（工具自己的脚注也这么写）。
   二十几个薄层里冒出一个 78.5%，是完全可以由噪音产生的。
3. **表内自相矛盾**：`RANGE · 公告 利空` 是 **85.50%（n=20）**，比利好那一档还高。
   若「利好好、利空差」成立，这一格不该出现。三个方向不一致 ⇒ 这些薄层读不出机制。

**唯一样本量像样的公告桶（中性，n=166）指向的是反方向：39.5%，明显低于随机。**

**因此上一节的结论不变，但要连着这一条读**：
公告作为**二值筛子**对入场质量没有增量（51.5%，且建仓在公告日上零富集）；
按方向拆开之后，**没有任何一个桶达到「可据以行动」的样本量**。
利好那 25 次是这次唯一值得日后回头看的东西，但它现在**不构成证据**。

**要把它变成证据需要什么**（若日后有人想追）：
样本量得上一个数量级 —— 要么扩标的池（而全市场取数被 [P2 评估](./P2-全市场扫描评估.md) 判为当前不可做），
要么放宽公告窗口（`--announce-days`）。**两者都必须先注册判据再跑，且只报一次分位** ——
在同一份数据上换窗口、换关键词表反复试，就是把这次测量变成一次过拟合搜索，
而这条纪律是本项目目前唯一可信的判据（§5.21 起）。
**本次不做这个追加**：把关键词表或窗口调到 78.5% 变显著为止，正是它要防的事。

#### 再补：事件 + 滞后确认（10 日窗口，2026-08-16，**预注册后只跑一次，此方向就此封盘**）

**上面两轮测的都是「建仓当天恰好有公告」—— 同日巧合。** 而事件驱动的正常形状是
「利好公告 → 追踪一段时间 → 引擎出信号才建仓」，即**事件 + 滞后确认**。
两者不是同一件事，且后者顺带解决 n=25 的样本量问题。

**预注册（跑之前写死，事后一个字没改）**：`--announce-days 10`（自然日 ≈ 7 个交易日，
事件研究常规短窗口）· 其余参数与前两轮逐字相同 · 关键词表不动 · 合并规则 NEG > POS > NEU 不动。
**判据三条同时满足才算支持**：① 利好桶 n ≥ 100 · ② 配对胜率显著高于「公告 无」·
③ 三个市场状态方向一致（上一轮正是栽在这条）。

**结果**：

| 分层 | 建仓 | 配对胜率 |
|---|---|---|
| ALL | 1675 | 46.50% |
| **公告 利好** | **167** | **32.00%** |
| 公告 中性 | 648 | 32.50% |
| 公告 利空 | 228 | 63.00% |
| **公告 无** | **632** | **65.00%** |
| 公告 有（合计） | 1043 | 35.00% |

**判据 ① 达成（n=167），②③ 决定性失败**：利好 32.0% 比「公告无」的 65.0% **低 33pp**，
方向与假设**相反**；三个状态也不一致（TREND_UP·利好 48.0% · RANGE·利好 61.5% ·
TRANSITION·利好 **11.5%**）。

**这一轮同时杀掉了上一轮那个 78.5%。** 同一个概念、窗口从 1 天放宽到 10 天、
样本量从 25 涨到 167，读数从 78.5% 掉到 32.0% —— 这正是「n=25 那格是噪音」的预期表现。
**别再引用 78.5%。**

**一个方向相反的观察（不是结论）**：`公告 利好 32.0%` 与 `公告 中性 32.5%` 几乎相同，
而 `公告 无` 是 65.0%。**方向标签没有区分度，起作用的是「最近有没有公告」本身，且是负向。**
两个桶的 n 是 1043 / 632（除 ALL 外最大的两个），30pp 的差距不像薄切片假象。

可能的解释是**公告密度在代理别的东西**（公司活跃度 / 事件性 / 波动率 —— 爱发公告的公司
本身就更"事多"），而本次测量**分不开这些**。要确认它需要**另起一次预注册**
（比如按公告年频率分桶、控制市值与波动率），**不是**在这份数据上再换几个窗口试。

**⇒ 此方向封盘。** 「公告选股」「公告 + 滞后确认」两种形状都已测过，
判据事前定、只跑一次、结果照报。要再动这个方向必须换数据（扩池 / 延长窗口 / 补基本面控制变量），
不是换参数。

#### 收尾：「发公告频繁的股票要不要避开」（2026-08-16，预注册后只跑一次）

**这是对同一批 1675 次建仓的第 4 次检验**，多重比较的代价是累积的 —— 记在这里，
下一个人要再看这份数据时应当知道它已经被看过四次。

**问的不是同一个问题**：前三轮测的是「这次建仓前有没有公告」（**时点**属性），
这一轮测「这只票爱不爱发公告」（**股票**属性）。

**预注册**：频率口径 = 公告日数 ÷ **该股自己的 K 线年数**（不是除固定年限 ——
晚上市的票分子天然小，除固定年限会同时压低频率与建仓数，制造虚假相关）·
四分位分桶 · 参数与前三轮逐字相同 · **判据三条同时**：① Q1→Q4 单调 ·
② Q1 与 Q4 差 ≥ 15pp · ③ **四个独立切法（时间前/后半 × 代码前/后半）方向一致**。

**结果（250 只 · Q1 10.9 ~ Q4 85.8 公告日/年）**：

| 桶 | 建仓 | 配对胜率 |
|---|---|---|
| Q1 最少 | 384 | 45.50% |
| Q2 | 412 | **70.50%** |
| Q3 | 444 | 38.00% |
| Q4 最多 | 435 | 41.00% |
| ALL | 1675 | 46.50% |

**三条判据全部失败**：① 不单调 · ② Q1 与 Q4 只差 4.5pp · ③ **Q2 那个 70.5% 被四切法当场拆穿**
—— `Q2 · 代码前半 15.50%` vs `Q2 · 代码后半 98.50%`，同一个桶换一刀就从最差变最好，
而时间切法（前半 74.0% / 后半 62.0%）看起来还很稳。

**⇒ 「避开高公告频率的股票」不成立**（Q4 41.0% vs Q1 45.5%，噪音带内）。

**这一格的价值主要在方法上**：Q2 若只报总体的 70.5%，会是一个非常好看的"发现"，
而它整个由半边代码空间撑着。这正是 CLAUDE.md 那条
「砍掉一批已知负 alpha 的交易必然提升剩余部分的 alpha —— 那是算术不是发现」
所要求的四切法检验存在的理由。**下次任何「剔除某类标的」的候选都必须先过这一关。**

**⇒ 公告这条线（选股方向）到此全部封盘**：同日巧合 · 事件+滞后确认 · 股票层面频率，
三种形状都测过，每次预注册、只跑一次、结果照报，三次都是否定。
再动必须换数据（扩池 / 补基本面控制变量），不是换参数。
消息面在本项目里的位置因此就是 [docs/11](../11-盘外消息面简报功能需求.md) 已经定的那个：
**只列不判的一屏内容，不进选股、不进闸门、不进回测。**

---

### 5.26 补退市股（2026-08-17）：幸存者偏差从「无解」变成 **−0.15pp**，而 `audit:random` 对它基本不敏感

#### ① 一句写在代码里三个月的论断是错的

`scripts/build-universe.mjs` 的文件头与 `params/universe-broad.json` 的 `knownBias` 都写着：

> 幸存者偏差无法消除：已退市股票在**任何免费行情接口上都取不到日线**，必然缺席。

**取得到。** 腾讯 `fqkline` 有退市股的完整日线，一直延伸到各自的退市日，
不复权与后复权两轨都在，`assertKey()` 不报错，格式与现有 fixture 逐字段一致 ——
`fetch-history.mjs` **一个字都不用改**。四只已知案例：乐视退 SZ300104（2018-01-24..2020-07-20）·
中弘退 SZ000979（..2018-12-27）· 退市保千 SH600074（..2020-05-26）· 康得退 SZ002450（..2019-07-05）。

[下一阶段取舍与迭代计划 §2.1](./下一阶段取舍与迭代计划.md) 在 2026-08-15 已经验证过日线可得，
并把这条的剩余需求收窄成「**等一份退市股名单**」，方案是注册 Tushare 拿 token。
**名单也不需要新数据源**：巨潮的 `szse_stock.json`（`fetch-announcements.mjs` 抓公告时
早就在用它取 orgId）实测 **6234 只，含退市股**，名称就是退市后的名称（「乐视退」「退市保千」）。

#### ② 名单怎么筛出来（`scripts/build-delisted.mjs`，新增）

| 步 | 做法 | 结果 |
|---|---|---|
| 候选 | 巨潮三层内 4942 只 ∖ 新浪 `hs_a` 今日在交易 4594 只 | **348 只** |
| 筛 | 逐只拿 `2018-01-01 → 今天, count=300` 探一次，看有没有数据、末根是哪天 | **240 只**有数据 |
| 定 | 末根 < `2026-07-01`（还在退市整理期的不取，它们的序列还会往后长） | **236 只**（沪 97 / 深 100 / 创 39） |

**一个请求同时解决「有没有数据」和「退市日是哪天」** —— 靠的是腾讯「返回从 `to` 往回数的
最后 N 根」这个特性（fetch-history.mjs 文件头实测第 1 条）。348 只逐只探约 3 分钟，
远低于被 501 拦的阈值（实测约 1800 请求）。

按退市年份：2018:8 · 2019:10 · 2020:18 · 2021:19 · **2022:42 · 2023:46 · 2024:49** · 2025:30 · 2026:14。
逐年上升，与 2020 年退市新规后退市加速的事实一致 —— 这是名单可信的一个旁证。

⚠ 候选里混着两类**不是**退市清盘的，靠上面第二步筛掉大部分，但没筛干净：
2002 年前退市的（`PT金田A`、`PT中浩A`，拉不到 2018 后数据 ⇒ 自动出局）与
**吸收合并/代码变更**的（`深赤湾A` → 招商港口、`外运发展`、`中航善达` → 招商积余）。
后者的股东是**换股**不是亏光，抽中的 19 只里有 3 只属于这类（16/19 名称带「退」或 ST，
与全体 274/348 的比例一致）。方向是**低估**退市的代价。

#### ③ 抽 19 只，不是 236 只

判据是**真实占比**：2018-01-01 时点三层内约 3530 只（4594 今存 − 约 1300 只 2018 后新上市
+ 236 只窗口内退市）⇒ 退市股真实占比 **236/3530 ≈ 6.7%** ⇒ 旧池 261 只应补 `x/(261+x)=6.7%` ⇒ **19 只**（沪 8 / 深 8 / 创 3，层内代码升序等距抽样）。

**过采样是有代价的**：把 236 只全放进去，池子 497 只、退市股占 47%，
整体绩效就再也不能当全市场代表值读（偏低多少取决于抽样而非市场）。
「退市股上引擎表现如何」是另一个问题，要单独跑并单独报。

#### ④ 主结果：**−0.15pp**，且旧结论逐位复现

出厂参数 0.2.8、`2018-01-01 → 2026-08-13`、预热 300 根、基准 SH000300：

| | A：旧池 261（今天还活着） | B：新池 280（+19 只退市） | Δ |
|---|---|---|---|
| 建仓 | **1675** | 1735 | +60 |
| 全期总收益 | **−0.09%** | **−0.24%** | **−0.15pp** |
| 建仓级胜率 | 45.31% | 45.24% | −0.07pp |
| 净盈亏 | −27,208 元 | −71,089 元 | **−43,881 元** |
| 最大回撤 | 3.86% | 3.67% | −0.19pp（分母变大） |
| 平均资金占用 | 3.49% | 3.35% | −0.14pp |

**A 是复现出来的**：建仓 1675 次与 §5.25 的 `t3fix.json` 逐位相同，全期 −0.09% 与 §5.24 一致，
`audit:random` 的 `ALL = 46.50%` 也与 §5.25 **逐位相同**。
⇒ 新增的 `--delisted` 通路对不传它的旧路径**零影响**，Δ 干净地只归因于「补了退市股」。

净盈亏的差额 −43,881 元与那 19 只**单独跑**的净盈亏**逐位相等**（每只标的独立仓位，
组合净值 = 各仓位之和），可加性无误。19 只单独跑：60 次建仓 · −2.31% · 建仓胜率 43.33% ·
平均资金占用仅 **1.43%**。

**为什么只有 −0.15pp** —— 三条一起看，缺一条就会把这个数读成「幸存者偏差不要紧」：

1. 退市股只占池子 6.8%，且**平均资金占用只有 1.43%**（远低于全池 3.49%）；
2. 19 只里 **3 只完全没建仓**（`SH600270`/`SH600401` 判定根为 **0**、`SZ002143` 仅 39 根）——
   长期停牌导致根数够不到 300 根预热。**这不是抽样失误，是退市股的固有形态**；
3. 见 ⑤，风控在退市之前就把仓位清掉了。

#### ⑤ `DELISTED` 强制平仓**一次都没触发** —— 这是结论不是 bug

`simulate.ts` 新增了「退市日收盘强制平仓并记一笔 `trade`」的分支（必要性：未平仓的建仓
不产生 `trade` 行，而建仓级胜率与 `audit:random` 的配对 alpha **都只读 `trades`**，
不补就是「池子补了、口径没补」）。**实测触发 0 次**：那 19 只的 180 笔离场全部是
`DRAWDOWN_REDUCE` 99 · `PROFIT_PROTECT` 37 · `STOP_LOSS` 22 · `TRAILING_STOP` 18 · `R4`/`R3` 各 2。

⇒ **8% 止损、回撤减仓、移动止损在退市日之前就把仓位赶出来了。**
这个分支在当前参数下不改变任何数字，但它是必要的防御 —— 一旦风控放宽（比如 `stopLossPct`
上调）就会真的用上，而缺了它，那笔亏损会只进净值、不进任何一张统计表。

#### ⑥ 反直觉的一格：补进更差的样本，配对胜率反而**上升**

`audit:random`，参数**跑之前定死**且两池完全相同（`--match-regime --shuffle-spans --trials 200 --min-count 30`）：

| 分层 | A·s1 | A·s2 | B·s1 | B·s2 | Δ·s1 | Δ·s2 |
|---|---|---|---|---|---|---|
| **ALL** | 46.50% | 45.00% | 55.00% | 53.00% | **+8.5** | **+8.0** |
| RANGE | 65.00% | 64.50% | 73.50% | 66.50% | +8.5 | +2.0 |
| 信号 `R3+R4` | 67.50% | 66.50% | 77.00% | 71.50% | +9.5 | +5.0 |
| TREND_UP | 66.50% | 67.00% | 66.00% | 69.50% | −0.5 | +2.5 |
| `T2+T3+T4` | 81.50% | 86.50% | 88.00% | 78.00% | +6.5 | **−8.5** |

**两条读数，第二条是这一节最重要的产出**：

**（a）`RANGE` / `R3+R4` 的弱正 alpha 补完退市股仍然存在**（四格全部 > 50%，66.5%~77.0%）。
§5.21 那个「全项目唯一的正面资产」**没有被幸存者偏差推翻** —— 这是本次要回答的主问题，答案是肯定的。

**（b）但「补退市股让 alpha 变强」这个说法只在 `ALL` 上跨种子稳（+8.0/+8.5），分层上不稳**
（`RANGE` 的 Δ 是 +8.5 / +2.0，`T2+T3+T4` 甚至变号）。按项目自己的红线
（「只有跨种子稳定的形状才算读数」），**分层的 Δ 一律不许引用**。
注意 A 跨种子极稳（ALL 差 1.5pp）而 B 明显更抖 —— 多出来的 19 只里有退市暴跌这种极端行情，
60 次建仓就足以放大整体方差，正是 §5.21「几十次建仓的层只看加权会把一次行情读成一个机制」那个形状。

**`ALL` 上升 8pp 不是「策略变好了」，它暴露的是 `audit:random` 的一个盲区：**

> 随机基准是「**在同一只票上**随机挑入场日」，所以**票本身的 beta 在两组之间被抵消掉了**。
> 于是配对胜率**测不出「选到了一只烂票」**这件事 —— 而幸存者偏差恰恰主要作用在这个维度。
> 在一只持续暴跌到退市的票上，随机入场几乎必亏，引擎至少还挑得出几个反弹点，
> 于是**相对**优势反而变大，而**绝对**绩效同时变差（−0.15pp）。

⇒ **两个口径必须分开说**：`audit:random` 的 alpha 结论对幸存者偏差**不敏感**（好消息：
§5.21 起的全部 alpha 结论不用重做）；**绝对绩效**的结论对它**敏感**，方向是被高估。
以后引用 −0.09% / −0.24% 这类数必须说明是哪个池子。

#### ⑦ 这个 −0.15pp 是**下界**，三层截断都要一起说

1. **停牌段没有数据，而退市前的暴跌常常就发生在停牌前后**（§2.1 在 2026-08-15 已经指出）。
   本轮实测印证：3 只根数不足到完全没建仓。
2. **结算价用退市日收盘价**，而真实退市股在整理期常连续跌停卖不掉、之后进老三板近乎归零。
   `report.ts` 的 warnings 有一条固定写这个（本轮因触发 0 次而未出现）。
3. **抽中的 19 只里有 3 只是吸收合并**（股东换股不是亏光），见 ②。

三层都指向同一个方向：**真实的幸存者偏差比 −0.15pp 更大**。
但它已经从「无解，只能声明」（[量化系统现状盘点 §13](./量化系统现状盘点.md)）
变成了一个**有下界、可复算、可随时加大样本重测**的数。

#### ⑧ 处置

- 新增 `scripts/build-delisted.mjs` → `params/universe-delisted.json`（19 只 + 每只的 `delistedAt`）。
- `simulate.ts` 加 `SimulateOptions.delistedAt`（**不给则逐位相同**，`simulate.test.ts` 有一条钉着）；
  `cli.ts` 加 `--delisted <file>`，给了却解析出空表**直接抛错**（静默当成没给，会让一次
  「以为修了幸存者偏差」的回测悄悄跑成没修的版本，而报告上完全看不出来）。
- `build-universe.mjs` 与 `universe-broad.json` 里那句错的论断已更正，并指向新脚本。
- **两个池子刻意不合并**：合成一个文件，「含退市 vs 不含退市」这个对照就再也做不出来了。

#### ⑨ 全量复核（同日追加）：抓满 236 只，**19 只抽样的估计站得住**

④ 那个 −0.15pp 建立在 **60 次建仓**上，统计很薄。于是把全部 236 只抓下来单独跑一遍
（`params/universe-delisted-all.json`，`build-delisted.mjs --take 236 --out …`）。

**抓取：222/236 落盘（94%）。14 只失败，其中 13 只是同一个原因 —— 后复权价出现非正值。**
后复权是乘性的、本该恒正，所以这**不是**把轨拉成了 qfq（`assertKey` 另外挡掉 1 只真的只有 `day`），
是腾讯对这批退市股的复权因子本身有问题。这 13 只被排除，方向上**又是一层乐观**
（算不出来的往往是最极端的那些）。

**D · 222 只退市股单独跑**（0.2.8，同区间同参数）：

| | 值 | 与主池 A 对照 |
|---|---|---|
| 建仓 | 787 次 | — |
| 全期总收益 | **−2.08%** | A 是 −0.09% |
| 建仓级胜率 | 43.07% | A 是 45.31% |
| 平均资金占用 | **1.82%** | A 是 3.49% |
| 净盈亏 | −462,033 元 | 每只平均 **−2081 元** |
| **零建仓的标的** | **40 只（18%）** | 长期停牌 ⇒ 够不到 300 根预热 |

**重估：把 19 只的实测换成 222 只的均值。** 存活股抽样率 261/3294 = **7.923%**
⇒ 同率下应补退市股 236 × 7.923% = **18.70 只**（与实际抽的 19 只几乎相同，抽样设计无误）。
按净盈亏口径统一算：

| 估计方式 | 全期收益 | Δ（相对 A 的 −0.1042%） |
|---|---|---|
| A：旧池 261 只 | −0.1042% | — |
| B：19 只抽样**实测** | −0.2539% | **−0.1496pp** |
| E：222 只均值**重估** | −0.2364% | **−0.1322pp** |

**两个独立估计相差 0.017pp** ⇒ ④ 的 −0.15pp 不是抽样运气，它站得住。
取值上以 **−0.13pp**（用了 222 只信息，方差小得多）为准，−0.15pp 作为同量级的旁证。

**为什么退市股差这么多（−2.08%）却只拖累 0.13pp** —— 两个乘数，缺一个就会把这条读错：

1. **占比小**：真实占比 6.7%；
2. **引擎在退市股上天然很少出手** —— 平均资金占用只有 **1.82%**（主池 3.49%），
   且 **18% 的标的一笔都没建仓**（长期停牌导致根数够不到预热）。
   ⇒ 幸存者偏差的影响被「数据不足 ⇒ 不判信号」这条机制**自动削弱**了一半。
   这也是它的**天花板**：想让这个数更准，得先有停牌期间的数据，而那是没有的。

**`DELISTED` 强制平仓在 222 只里触发 1 次**（19 只时是 0 次），报告的那条乐观假设警告
如期出现。⑤ 的结论不变且更强：**风控几乎总是先于退市把仓位清掉**，
787 次建仓里只有 1 次真的持有到退市日。

> ⚠ D 的分状态归因里 `TREND_UP` 是 **+125,778 元**（与主池 TREND_UP 系统性亏损相反）。
> **不许单独引用**：样本、窗口、标的构成都与主池不同，且是事后切分、未做多重比较校正。
> 要拿它说事得按 docs/07 §3.6 重新设计实验。

**顺带修掉一个健壮性缺陷（`fetch-history.mjs`）**：`merge()` 的数据质量断言抛错时
**不在 try/catch 内**，异常冒泡穿过 `Promise.all` 直接结束 main —— `SH600091` 那 19 根非正值
一次带走了后面还没抓的 **169 只**，而输出看起来只是「跑完了，少了一些」（没有 `[结果]` 段是唯一线索）。
这与该文件头「逐只落盘，被拦的代价只是剩下的没补上」的设计意图直接矛盾。
断言保留（那种数据确实不可用），处置降级为「跳过这一只 + 计入失败清单」。

#### ⑩ 顺带回答一个主线问题：**那条红线仍然过不去**

`calibrate.ts` 的红线是「训练集 Calmar ≤ 0 直接淘汰」，而 `Calmar = 年化 / 最大回撤`、
回撤恒正 ⇒ **红线等价于「训练窗口总收益 ≤ 0」**，看符号就行，不必跑网格。

训练窗口 `2018-01-01 → 2023-12-31`（`DEFAULT_SPLITS`），0.2.8 出厂参数：

| | A：旧池 252 只 | B：+19 只退市 271 只 |
|---|---|---|
| 总收益 | **−1.99%** | **−2.03%** |
| 年化 | −0.34% | −0.34% |
| 最大回撤 | 2.96% | 2.87% |
| **Calmar** | **−0.115** | **−0.118** |
| 建仓 | 1097 | 1152 |
| 建仓级胜率 | 43.21% | 43.23% |

（只数少于全期的 261/280，是因为 9 只在 2018–2023 区间内没有日线 —— 2024 年后才上市的新股。）

**两条结论：**

**（a）[docs/09 开头](../09-下一阶段开发计划.md)那个「主线卡死」的状态没有改变。**
出厂值仍被红线淘汰 ⇒ 配对 Δ 仍拿不到 ⇒ 每张网格的裁决仍必为 `INCONCLUSIVE`。
**§5.24 的 T3 判定逻辑改动没有解开它**：训练窗口从 0.2.7 的 −2.55% 改善到 0.2.8 的 −1.99%
（+0.56pp），方向对，但离转正还差得远。补退市股当然也解不开（那不是它的用途）。
⇒ 想标定，仍然只有 docs/09 那三条路，且**方向 ① 还得继续查买点**。

**（b）训练窗口的幸存者偏差只有 −0.04pp，远小于全期的 −0.13pp。**
不是矛盾，是**窗口覆盖了多少退市事件**决定的：退市高峰在 2022–2024（42/46/49 只），
而训练窗口截在 2023 年底 —— 抽中的 19 只里有相当一部分那时还在正常交易，行为与普通股无异。
⇒ **引用幸存者偏差的量必须连着窗口一起说**。全期 −0.13pp、训练窗口 −0.04pp，
它们不是同一个数的两次测量，而是两个不同的量。

#### ⑪ 全量样本定稿：救回 11 只之后，**−0.15pp**

⑨ 里失败的 14 只中，13 只的原因是「后复权价出现非正值」。逐年查下去发现
**非正值全部集中在退市整理期的尾段**，且是贴着 0 的小负数（SH600091 最后 19 根 −0.063~−0.812、
SZ000502 最后 15 根、SH600767 最后 4 根），而前面几年完全正常
（SH600091 的 2018–2021 最低收盘 1.075，一根非正都没有）。
⇒ 腾讯对**退市整理期那一小段**用的是加性处理，股价跌到几毛钱时减成了负数。

**处置：截断尾段，而不是丢弃整只。** 丢弃整只等于让这只退市股完全缺席，
那正是幸存者偏差本身；而那一小段本来就是连续跌停、回测里也成交不了。
`fetch-history.mjs` 的断言因此改成按**位置与占比**分流（`MAX_TRAILING_BAD_RATIO = 10%`）：
尾部一小段 ⇒ 截断并告警；中间出现或占比过大 ⇒ 照旧抛错。
阈值不需要精调 —— 实测通过的是 1.8% / 1.4% / 0.3%，而真正拉成加性前复权是**全程**变负
（中远海控 714/2088 = 34%），两者差一个量级。**实测正确拒绝了 SH600870（88.9%）与 SZ000693（17.9%）。**

救回 11 只 ⇒ **233/236**。重跑：

| | D（222 只） | **D2（233 只）** |
|---|---|---|
| 建仓 | 787 | 815 |
| 全期总收益 | −2.08% | **−2.40%** |
| 建仓级胜率 | 43.07% | 42.82% |
| 净盈亏 | −462,033 元 | **−559,849 元**（每只 −2402） |

救回的那批正是最惨的（收益从 −2.08% 掉到 −2.40%）。按 ⑨ 的同一套加权重估：

| 估计 | Δ vs A |
|---|---|
| 19 只抽样实测 | −0.1496pp |
| 222 只重估 | −0.1322pp |
| **233 只重估（定稿）** | **−0.1537pp** |

**定稿取 −0.15pp**，与 19 只抽样的 −0.1496pp 几乎逐位吻合。

> ⚠ **截断改变了末根日期，而清单里的 `delistedAt` 是用不复权轨探出来的**，两者会差几根到二十几根。
> `simulate.ts` 的判据是「末根 >= delistedAt」，对不上就**恒假 ⇒ 那只票不会被强制平仓**，
> 而是悄悄变成「期末未平仓」—— 报告上只多一行无关痛痒的 warning，那笔亏损照样不进 `trades`，
> 绕一圈又漏回本节要修的东西。`build-delisted.mjs` 因此加了 `--sync-fixtures <dir>`：
> 按 fixture 实际末根校准退市日（实测校准了 11 只；19 只那份 0 调整，所以 ④ 的 A/B 对照不受影响）。

---

### 5.27 跨票随机基准（2026-08-17）：**工具做出来了，但它没能补上那个盲区 —— 预测被证伪**

#### 动机

§5.26 ⑥ 暴露了 `audit:random` 的一个结构性盲区：随机基准在**同一只票内**抽日子，
于是**票本身的涨跌在两组之间被抵消**，它测的是「择时」，答不了「该不该选这批票」。
证据是补进退市股后**绝对绩效变差 0.15pp 而配对胜率反而升 8pp**。

于是加 `--cross-code`：**固定入场日期，随机换一只别的票**。日期对齐 ⇒ 市场 beta 对齐，
变的是标的。三条实现纪律：排除同一只票（否则退化成真实入场）；候选是那天有 K 线的票
（停牌/未上市/已退市当天没有行，自然出局）；与 `--match-regime` **互斥**（零点定义不同，
混用会产出一个说不清是什么的数）。

**一处必须修的实现缺陷**：候选池起初取自 `positions`（建过仓的票，250 只）——
那已经被引擎筛过一轮，拿它当零点会系统性**低估**选股 alpha。改成取 `report.meta.codes`
的完整池（261 / 280 只），下面的数字是修正后的定稿。

#### 预测与结果：**证伪**

事前预测写在这里：若跨票口径真能测出「选到烂票」，那么补进 19 只退市股之后
B 的分位应当**下降**（或至少不升）。

| 分层 | A：旧池 261 | B：+19 退市 280 | Δ |
|---|---|---|---|
| **ALL** | 74.50% | **82.00%** | **+7.5** |
| RANGE | 71.50% | 71.50% | 0 |
| 信号 `R3+R4` | 74.00% | 73.00% | −1.0 |
| TREND_UP | 70.50% | 73.00% | +2.5 |
| TRANSITION | 68.00% | 71.00% | +3.0 |

**`ALL` 仍然上升 7.5pp ⇒ 预测被证伪，跨票口径同样测不出「选到烂票」。**

**根因（这条比工具本身有价值）**：跨票随机的候选池**也包含那 19 只退市股**（266→280 只）。
池子变差会**同时拉低两组**，而引擎相对随机的优势不变甚至上升 ——
在一只跌到退市的票上随机入场几乎必亏，引擎至少挑得出几个反弹点。

> **推广：任何「池内配对随机化」在结构上都测不了「池子本身好不好」，因为基准就在同一个池里。**
> 要测它必须有一个**池外参照**。报告里 `benchmarkReturn` 那一行本来就是池外的，
> 但它满仓、而我们平均资金占用 3.5%，两者不可比（§5.13）。
> ⇒ **真正缺的判据是「相同资金占用下的被动持有」**，那是另一个工具，不是这个。

#### 副产品：择时与选股在这套引擎上是两回事

同一批建仓、同一套参数，只换零点定义：

| 分层 | 择时（同票换日期） | **选股（同日换标的）** |
|---|---|---|
| ALL | 46.50% | **74.50%** |
| RANGE | 65.00% | 71.50% |
| 信号 `R3+R4` | 67.50% | 74.00% |
| TREND_UP | 66.50% | 70.50% |

**读法（三条限定缺一不可）**：

1. **这不是「策略赚钱」**。相对优势 ≠ 绝对收益 —— 同一份数据的全期总收益仍是 **−0.09%**。
   引擎选的票比随机票好，但仍然不赚钱（成本 + 市场）。
2. **有一个尚未排除的替代解释**：跨票随机会抽到那天毫无异动的票（长期横盘、低流动性），
   而引擎选的至少是「有突破/有动量」的。所以 74.5% 可能反映的是**「选了活跃的票」**，
   而不是「选了会涨的票」。要排除它得把候选池限定在「当天也有信号的票」里 —— 那是下一个实验。
3. **只报了一次分位**（迭代 skill 的红线），参数跑前定死
   （`--cross-code --shuffle-spans --trials 200 --seed 1 --min-count 30`）。
   **未做跨种子稳健性检验**，所以上表**只能当方向，不能当量**。

#### 处置

- `--cross-code` 进仓库，**只统计不改参数**，与 `--match-regime` 互斥（parse 阶段抛错）。
- 报告头的「抽样口径」那一行会显式打印是**择时**还是**选股** ——
  两份报告长得几乎一样，不标注的话事后没有任何办法分辨。
- **本节的结论是否定的**：那个盲区仍然存在。别因为工具做出来了就以为它补上了。


---

### 5.28 提醒层的第一个干净交易日（2026-08-17）：真机日志掉出两个缺陷，都不在策略里

**这一节不是标定实验**，记在这里是因为它是 M3 出口条件（「提醒日志中被抑制条目的原因分布合理」）
第一次被真的当成判据来读 —— 而一读就发现那份日志既漏报又读不出分布。

#### ① 前三天的日志不可用作判据：盘中重启把内存态清零了

`AlertDispatcher` 的冷却、配额、强制类台阶全在内存里（那是刻意的，见它的头注释）。
按日志里 `窗口与托盘就绪` 的出现次数：

| 交易日 | 启动次数 | 其中盘中 | 三只止损票各发出的 L3 气泡 |
|---|---|---|---|
| 2026-08-13 | 14 | 0（当天 16:39 起才开始跑） | 8–11 条 |
| 2026-08-14 | 27 | 12 | 10 条 |
| **2026-08-17** | **0** | 0 | **0 条** |

08-14 那 30 条气泡里，**每一条都能对上一次启动时刻**（首条北京 10:52:10 = 本机 09:52，
逐条对齐）。所以「一天 10 条止损气泡」不是闸门失效，是开发期在重启 ——
**M3 的「自用一周」实际从 08-17 起算**。判据已写进 M3 清单 §4.0（一个 `grep`）。

#### ② 08-17 的日志：0 条气泡 · 1580 行「台阶未扩大」

| 项 | 值 |
|---|---|
| signal 行 | 15（08-14 是 668，见下面 ④） |
| alert_log 候选 | 1590 |
| 真发出 | **2**（都是 L1 状态点：SH600276 的盘中买入 + 15:00 那条收盘失效提示） |
| 实际拦截 | COOLDOWN 1580 · DEBOUNCE 5 · QUIET 3 |
| 独立会拦（`would_block`） | COOLDOWN 1584 · QUIET 688 · DEBOUNCE 5 · **CAP 0** |
| 持仓中跌破止损线 | 3 只（SZ002716 / SZ001296 / SZ000157，浮亏 −7.8% → −8.4%） |

**成因是一次记账顺序**：09:30:05 开盘第一轮屏幕锁定，三条 L3 强制提醒被闸门④降为 L1
（不弹气泡 —— 这是设计），但 `commit()` 在 `if (level === 'L1') return` **之前**
就把台阶记成了 −7.8%。解锁之后跌到 −8.4% 只多了 0.6pp，够不上 2% 的台阶 ⇒
整天再没有一条气泡，只剩 1580 行「强制提醒台阶：跌幅 −8.3% 未比上次（−7.8%）再扩大 2%」。

docs/05 §4.2 那句「既不骚扰又不漏报」当天只剩后半句被打穿：**没骚扰过任何人，只漏报**。
而 §4.3 早就写着「被降级成 L1 的不占 L2/L3 额度」—— **台阶就是一种额度**，只有它漏在早退之前。

#### ③ 那份日志本身读不出原因分布：1590 行 ≈ 6 件事

两个独立成因，修一个不修另一个都没用：

- **判重签名含 `reason` 文案**，而文案里嵌着连续量（跌幅 −8.2 / −8.3 / −8.4 来回抖、
  冷却「还有 87 分钟」）⇒ 每抖一下就是一行。与 `signalSignature` 里 `reasons[0]`
  那个坑**同一形状**（那次两天落了 243 行同一条止损）。011 迁移加 `suppressed_gate`
  这一列正是为分组准备的，但签名当时没换过来。
- **判重键只有 `code`**，而同一只票同时出 `SELL` 与 `REDUCE` 两条信号，
  交替来对比「上一条」⇒ 永远匹配不上。SZ000157 一天 626 行（REDUCE 419 + SELL 209），
  语义上是 2 件事。

#### ④ 一个被证实的预测（顺带）

`signalSignature` 移除 `reasons[0]` 之后：同样三只票整天跌破止损，
**signal 行数 668 → 15**（08-14 有 647 行 `PROVISIONAL SELL`，08-17 只有 3 行）。
CLAUDE.md 里「两天 769 行按新签名回放只剩 13 行」的离线估计，真机复现为 15 行。

#### 处置

- **① 是读数纪律**，进 M3 清单 §4.0 与 §4.5，不改代码。
- **② ③ 已修**（CHANGELOG 2026-08-17 条目，`ENGINE_VERSION` 不变 —— 全在 `src/main/alerts`，
  递增会让影子运行刚攒下的第一个净值点清零）。四条用例都先在旧代码上红过。
- **影子运行**：08-17 落下第一个净值点（1,000,000 / 0 笔委托）。当天 CONFIRMED 全是持仓
  `REDUCE`，而影子自己没有持仓，无可卖 —— 符合前向纪律，不是 bug。

---

### 5.29 【预注册】流动性 / 市值过滤能不能把训练窗口推过 Calmar > 0（2026-08-17 写，**结果未看**）

> ⚠ **这一节的上半部分写在跑实验之前**，这是它存在的全部意义。
> 判据、阈值、样本、报告次数在此锁定；下半部分「结果」在跑完之后追写，**不许回头改上半部分**。
> 依据：迭代 skill 第二步半 ③（预注册）与 §5.24 那条「每个候选机制只报一次分位」。

#### 背景（为什么做这个而不是继续跑网格）

出厂参数在 261 只池的训练窗口 2018→2023 是 **−1.99% / Calmar −0.115**（0.2.8），
标定的红线因此把出厂值本身淘汰 ⇒ 八张网格 82 组候选的裁决全是 `INCONCLUSIVE`（§5.20 ③）。
而 82 组里**没有任何单个一阶参数能把它翻正**（最好 `rsi.osBase = 25` → −1.40%）。
⇒ 参数不是原因，继续跑网格产出零证据。

这一轮换的是**维度**而不是参数：标的池是按代码段等距抽样的（`build-universe.mjs`，
刻意不按市值/流动性筛），所以它里面有大量微型、低流动性标的 ——
而 2018–2023 的 A 股小市值段是系统性亏钱的。**这个替代解释此前无法检验**，
因为项目没有市值与成交额（[信源台账 §4](./信源台账.md) 那两格）。
2026-08-17 解掉了：东财 kline 的 `f57`/`f61` 历史整段可得，流通市值由换手率反推。

#### 假设（可证伪）

> **H1**：出厂参数在训练窗口的亏损中，有可观的一部分来自「本来就不该交易的标的」
> （微型市值 / 低流动性）。剔除它们之后训练窗口 Calmar 上移。
>
> **H0**：亏损与市值/流动性无关（§5.20 ④ 已知它是弥散的：147 只负 / 103 只正，
> 中位数 −2.58%，最差 10 只只占负和的 19.2%）—— 那么剔除后 Calmar 基本不动。

**H0 是当前更可能的答案**（弥散那条证据指向它），所以这一轮**大概率是否定结论**。
按 skill 的要求，否定结论同样归档，且**不许因为没过线就去调阈值重跑**。

#### 判据（**跑之前锁定**）

| 项 | 值 | 为什么是这个 |
|---|---|---|
| 主判据 | 训练窗口（2018-01-01 → 2023-12-31）**Calmar > 0** | 用户 2026-08-17 拍板的口径；也是标定红线与 L2 第①条的原文 |
| 市值阈值 | 剔除当日横截面**流通市值最小 30%** | CH-3 的标准做法（[差距 §4](./与机构量化系统的差距.md) 那条自检），**不搜索、不试第二个数** |
| 流动性阈值 | 剔除当日**过去 20 日均成交额最小 30%** | 与市值同一口径，同样不搜索 |
| 判定时点 | **逐日、点内**（当天用当天的横截面 + 过去 20 日） | 用整段中位数算分位是前视偏差 —— 那会让结论无效 |
| 横截面范围 | **本池 261 只之内**（不是全市场） | 池外没有数据；报告里必须写明这一点 |
| 报告次数 | **一次**。三种组合各跑一次：仅市值 / 仅流动性 / 两者并用 | 三个是**不同机制**不是同一机制的三档，所以不算「调到达标」 |
| 测试窗口 | **不碰**（预算已 5 次） | 主判据在训练窗口上，测试段与本轮无关 |
| 归档 | 无论正负都写进本节「结果」 | 否定结论比工具本身值钱（§5.27 的形状） |

#### 边界（这一轮**不做**什么）

1. **不动引擎。** 过滤实现在**回测的池层面**（`--liquidity` + `--drop-*-pct`），
   不是风控层的新裁决 —— 那会改变用户每天看到的信号条数，而用户 2026-08-17 明确说
   「不能再少」。搬不搬进引擎是**下一次**的取舍，前提是这一轮先证明它有用。
2. **不改价格轨。** 指标与净值继续用腾讯后复权（不随抓取日变，`fetch:history` 一个字不改）；
   东财只补 `amount` / `turnoverRate` / 反推市值三列，**两家的价格不混用**。
3. **不改参数。** 出厂值一个数都不动，本轮测的是「在哪些标的上交易」。
4. **不做行业中性化**（`f127` 虽然也能取到）—— 一次只验一个东西。

#### 修正一（2026-08-17，**仍在看结果之前**）：成交额改用代理，市值那一支等数据

东财在连发 3 个大请求之后把这台机器整族限流（[台账 §1](./信源台账.md) 那条新坑），
半小时未恢复。于是这一轮拆成两支：

- **流动性那一支现在就跑**，`amount` 用**现有腾讯 fixture 的 `成交量(手) × 100 × 不复权收盘价`** 代理。
  误差是「收盘价 vs 当日均价」，**实测**（用户库里 24538 个有真实成交额的「股票·日」）：
  中位 **−0.00%**、p5/p95 **−1.53% / +1.88%**、绝对误差中位 **0.44%** / p95 **2.51%**
  （另有极少数离群行，最大 271 倍 —— 单日误排一只票对 30% 分位无实质影响）。
  而**横截面上不同标的的成交额差几个数量级**，所以对「最小 30%」这个排序几乎无影响。
  **这是永久口径不是临时凑数** ——
  同一支实验报两次（先代理后真值）会违反「每个机制只报一次分位」，
  所以就用代理，误差与方向写在这里。
- **市值那一支等东财恢复**（换手率反推流通股本没有替代路径）。

两支各自只报一次。三种组合里的「两者并用」因此也等市值到手再跑。

#### 结果 · 流动性那一支（2026-08-17，**只报这一次**）

训练窗口 2018-01-01 → 2023-12-31 · 261 只 · 出厂参数 `0.2.8-unvalidated+c38e329b` ·
预热 300 根（判定自 2019-04 起）· 基准 SH000300：

| | 总收益 | 年化 | 最大回撤 | **Calmar** | 建仓 | 建仓级胜率 | 平均占用 | 被挡建仓 |
|---|---|---|---|---|---|---|---|---|
| 基线（不过滤） | **−1.99%** | −0.34% | 2.96% | **−0.11** | 1097 | 43.21% | 3.50% | 0 |
| 剔除 20 日均额最小 30% | **−2.06%** | −0.35% | 2.67% | **−0.13** | 678 | 40.41% | 1.90% | 721 |

**基线先复现无误**：−1.99% 与 [docs/09](../09-下一阶段开发计划.md) 记的 0.2.8 训练窗口逐位相同。

**H1 被证伪，H0 成立。** 三条读法：

1. **主判据没动**：Calmar 从 −0.11 到 −0.13（更差一点），总收益差 **0.07pp** ——
   在这个项目的噪音带（±1pp）里完全不可分辨。**红线仍然过不去。**
2. **不是「剔得太少」**：挡掉 721 次建仓机会、实际建仓 1097 → 678（**−38%**），
   占用 3.50% → 1.90%。剔除的力度很大，结果就是没变化。
3. **方向甚至是反的**：被剔掉的那批**胜率更高** —— 留下的那 678 次建仓级胜率
   43.21% → **40.41%**。也就是说低流动性标的在这套引擎上并不比别的差，
   拿掉它们只是让剩下的样本更集中在同样亏钱的地方。

⇒ 与 [§5.20 ④](#) 的「亏损是弥散的」（147 只负 / 103 只正，最差 10 只只占负和 19.2%）
完全一致。**「亏在不该交易的标的上」这个替代解释，在流动性这个维度上被排除了。**

#### 市值那一支：**放弃（未做）**，理由是三条已有证据都指向 H0（2026-08-17）

⚠ **这不是一个跑出来的结论，是一个「不跑」的决定。** 按纪律不许把它写成 H0 已被验证 ——
它是「未做」，只是现有证据让它的期望产出接近零。日后要补做，脚本与产物都在
（`params/float-shares.json` 已 217/280 只），一个限流窗口就能抓完剩下 63 只。

**为什么放弃**（三条互相独立）：

**① 按流通市值分三档给基线做分解 —— 亏损不在小票上，方向与 H1 相反。**
（对 `liq-base.json` 那次已有运行做的描述性分解，189 只有市值且有交易的标的，
不是新实验、不消耗「只报一次」）

| 中位流通市值 | 净盈亏 | 逐笔 | 亏钱的票 |
|---|---|---|---|
| **最小 1/3**（9–36 亿） | **−1.3 万元**（几乎打平） | 1129 | 31/63 |
| 中间 1/3（36–92 亿） | **−26.4 万元** | 848 | 44/63 |
| 最大 1/3（92–7840 亿） | −20.0 万元 | 898 | 38/63 |

剔除最小 30% 等于**剔掉唯一不亏的那一档**，与流动性支「被剔那批胜率反而更高」同一形状。

**② 缺的 63 只在规模上与已有的 217 只几乎同分布 ⇒ ① 那个分解没有瞎在小票那一头。**
用全池 280 只都有的成交额代理定位：那 63 只在分位上 p10/中位/p90 = **10% / 51% / 89%**
（均匀铺开，不是一条尾巴），中位日成交额 **0.75 亿 vs 0.74 亿**（差 1%）。
⚠ 限定：这是在**成交额**维度上判的同分布，而成交额与流通市值的秩相关只有 0.735
（见 ③），所以这个判断带噪音 —— 但三个分位点同时对齐、中位数差 1%，很难是巧合。

**③ 全池 280 只的流动性支已经是 H0**（上一小节），而它与市值支**不是同一件事但高度相关**：
中位流通市值 vs 中位日成交额的 Spearman 秩相关 **0.735**，
各取「最小 30%」时**重叠 58%**（38/65）。
⇒ 成交额支**不能替代**市值支（四成是不同的票，这一条本来是我以为能替代、实测被打掉的），
但它确实覆盖了「小而不活跃」那一大半，且结果是 H0。

**合起来**：H1 的前提（亏在本来就不该交易的标的上）在**能测到的每一个维度上都不成立** ——
流动性维度实测 H0，市值维度的分解方向相反。§5.29 因此就此收尾。

#### 原本还没做的那一支（保留原文，供日后补做时对照）

**换机器接力看 [交接-流动性维度](./交接-流动性维度.md)**（恢复步骤、剩多少只、判据别改哪几条）。

**市值**（剔除流通市值最小 30%）要等东财限流解除 —— 换手率是反推流通股本的唯一入口。
2026-08-17 已拿到 **87/280** 只的逐月流通股本，压成 `params/float-shares.json` 进了版本控制
（208 KB；逐日展开是离线的，往返核对 87 只最大偏差 1.89%）。
另外两条路已被排除：网易 CSV **502**、腾讯快照只有今日股本（回推 2018 年中位偏 **1.28 倍**、
最大 12.95 倍，30/53 只偏离 > 20%）—— 见台账。
两者并用那一组同样等它。**已抓好的 280 份流动性文件里 `floatCap` 全是 null**
（fixture 代理路径给不出股本，刻意不编），所以那一支现在跑等于跑一个空过滤。

⚠ 预注册的「只报一次」对**每一支**分别生效：市值那一支跑出来是什么就是什么，
**不许因为流动性这一支没过线就去调市值那一支的阈值**。

#### 这一轮的净收获（即使是否定结论）

- **一个替代解释被排除**，而它此前完全无法检验（没有数据）。
- **两个数据维度从「没有源」变成「有源且已落盘」**（[台账 §4](./信源台账.md) 那两格）：
  成交额（真值口径已验证，代理误差实测中位 0.44%）与流通市值（反推法已用三只票交叉核对）。
  它们此后可以支撑行业中性化之外的任何横截面工作。
- **回测多了一个正交的旋钮**（池过滤），且它默认关闭、开启时会在报告里显式告警。
- 一个新的信源坑进了台账：东财大请求的限流是**按整族 host** 生效的，换门救不了。


---

### 5.30 【预注册】风控四条规则的消融：钱漏在哪一条（2026-08-17 写，**结果未看**）

> ⚠ 上半部分写在跑之前。判据、候选、报告次数在此锁定，**跑完只许追写「结果」**。

#### 为什么是这个方向（方向 D）

[§5.29](#) 收尾之后，「亏在不该交易的标的上」这个替代解释在**能测到的每个维度上都不成立**。
于是问题回到系统自己。而买点那条路（方向 A）与用户 2026-08-17 定的
「**出手频率不能再少**」冲突 —— 砍交易是 A 的最小实现。

**卖点这条路不冲突**：它改的是出场，买入信号一条不少。而它的分量被低估了：

| 事实 | 数 |
|---|---|
| 训练窗口离场由**风控**触发 | **3198 / 3305 = 96.8%** |
| 由**策略卖出信号**触发 | 107 笔（3.2%） |
| 决定这一切的 `risk` 参数 | 6 个，档位 `KEPT` —— 而那一轮 [§5.18](#) 是在**40 只旧池**上跑的，**261 只池上从未重测** |

⚠ **不要**把逐笔按规则分组的净盈亏读成「哪条规则在亏钱」：回撤减仓把一次建仓拆成多行，
亏的那条腿记在它名下、剩下的仓由移动止损了结记在后者名下
（实测 `TRAILING_STOP` 逐笔胜率 **99.7%**、`DRAWDOWN_REDUCE` **4.5%** —— 那是记账形状，不是优劣）。
这正是 CLAUDE.md「一行 trade 是一次卖出」那条坑。**所以必须用消融，不能用分组归因。**

#### 候选（4 个，**消融不是搜索**）

各关一条规则，用不可达阈值关、不改代码（`params/ablate-no-*.json`，手法与当年测冲突裁决相同）。
**这一轮不扫任何参数值** —— 目的是定位漏点，不是选参数。

#### 判据（**跑之前锁定**）

| 项 | 值 |
|---|---|
| 主判据 | 训练窗口（2018-01-01→2023-12-31）**Calmar > 0** —— 用户 2026-08-17 定的口径，也是红线原文 |
| 同向确认 | 验证窗口（2024-01-01→2025-06-30）**同向**（不要求同幅度） |
| 硬校验 | **建仓数变化 ≤ 10%**。它只改出场，若建仓数大变说明改到了入场（离场时点平移会让后续建仓机会小幅移动，所以给 10% 而不是 0） |
| 出手频率 | **不许下降**（这是本方向存在的前提，用户约束） |
| 报告次数 | **一次**。四个消融是四个不同机制，不是同一机制的四档 |
| 测试窗口 | **不碰** |

#### 预测（写在前面，便于事后判自己错没错）

**最可能的结果是「关掉任何一条都变差」** —— 风控是这套系统 96.8% 的离场，
而它在旧池上被判过 `KEEP`。若真如此，方向 D 也走不通，问题就**只剩买点**（方向 A），
那时「出手频率不能再少」这条约束必须重新拍。

次可能：**回撤减仓（③）关掉后变好** —— 它是 1875/3305 笔的主力机制，
且 `drawdownReducePct = 0.07` 与移动止损的 `0.03`、止损的 `0.08` 之间没有任何标定依据。

#### 结果（2026-08-17，**只报这一次**）

**训练窗口 2018-01-01 → 2023-12-31 · 261 只 · 只关掉一条风控规则、入场参数一个字没动：**

| 候选 | 总收益 | 年化 | 回撤 | **Calmar** | 建仓 | **建仓级胜率** | 逐笔 | 占用 |
|---|---|---|---|---|---|---|---|---|
| 基线 | −1.99% | −0.34% | 2.96% | −0.113 | 1097 | 43.21% | 3305 | 3.50% |
| **关③ 回撤减仓** | **−0.79%** | −0.13% | **2.32%** | **−0.057** | 1075 | **51.07%** | 2187 | 4.22% |
| 关① 固定止损 | −1.51% | −0.25% | 2.71% | −0.093 | 1089 | 43.80% | 4353 | 3.56% |
| 关② 移动止损 | −1.68% | −0.28% | 3.01% | −0.094 | 1075 | 40.09% | 4816 | 4.04% |
| 关④ 盈利保护 | −2.24% | −0.38% | 3.21% | −0.117 | 1084 | 39.67% | 3057 | 3.69% |

**我预注册的主预测（「关掉任何一条都变差」）被证伪**，次预测（③ 是漏点）命中。照实记。

**验证窗口（判定≈2024-01 → 2025-06-30）：**

| 候选 | 总收益 | 回撤 | **Calmar** | 建仓 | 建仓级胜率 |
|---|---|---|---|---|---|
| 基线 | +0.58% | 1.03% | **0.205** | 287 | 50.52% |
| 关③ 回撤减仓 | +0.43% | 1.18% | **0.134** | 286 | 53.15% |

⚠ **第一次跑验证窗口跑错了，记在这里免得再犯**：直接 `--from 2024-01-01` 只喂了窗口内的
K 线，300 根预热把前 14 个月吃掉 ⇒ 只剩 34 次建仓、两组数字一模一样。
正确做法是**带完整段前历史**（这里用 `--from 2022-10-01`，实测到 2024-01-01 之间正好
**302 根**）。标定工具的 `runSplit` 本来就这么做，但它在训练段就把候选淘汰了、**根本没算验证段**
（报告里 validation 为 `—`），所以只能手工构造。CLAUDE.md 里那条「每一段都要带段前历史」
说的就是这件事。

#### 判决：**不写回**

对着预注册的判据逐条：

| 判据 | 结果 |
|---|---|
| 主判据：训练窗口 Calmar > 0 | ❌ **−0.057，仍是负的**（改善了约一半，但没过线） |
| 同向确认：验证窗口同向 | ❌ **反向**（Calmar 0.205 → 0.134） |
| 硬校验：建仓数变化 ≤ 10% | ✅ 训练 −2.0%（1097 → 1075）· 验证 −0.3% |
| 出手频率不许下降 | ✅ 入场逻辑一字未动，−2% 来自离场时点平移 |

⇒ 按预注册，这个候选**不满足写回门槛**。`params.ts` 一个数都不动。

#### 但这一轮拿到了三条真东西

1. **`risk` 那 6 个数在 261 只池上是真的有问题**，不是「怎么动都测不出差别」。
   关掉四条中的三条都让训练窗口变好（③ +1.20pp、① +0.48pp、② +0.31pp），只有 ④ 变差。
   它们的档位是 `KEPT`，而那个 `KEEP` 来自**40 只旧池**的一轮 —— 这一轮说明那个结论**不可移植**。
2. **③ 的改善不是靠少做**：建仓数只降 2.0%，而**建仓级胜率 +7.86pp**（43.21% → 51.07%）。
   这是同一批入场、更好的结果 —— 与「砍掉一批交易必然提升剩余 alpha」那个算术陷阱**不同形状**。
   机制也说得通：回撤减仓在浮亏时卖掉一半，价格恢复时只剩一半仓位 ——
   **它把浮亏变成了实亏**，而这个池子里的回撤大多会恢复。
3. **③ 的作用是市况相关的**，这才是它两个窗口反向的原因：
   训练窗口含 2018 与 2022 两轮熊市（基线 −1.99%），减仓帮不上忙、只锁定亏损；
   验证窗口是修复期（基线 +0.58%），减仓反而躲过了后续下跌。
   ⇒ 「一刀关掉」在稳定性上过不去，这与判据说的是同一件事。

#### 下一步的两个候选（**都要新一轮预注册，不许在本节续跑**）

- **放宽而不是关掉**：`drawdownReducePct` 0.07 → 三点网格（0.07 / 0.10 / 0.14），
  报一次，要求训练窗口转正**且**验证窗口不恶化。
  ⚠ 危险在于「搜到达标为止」——那正是红线要防的。所以必须**预注册三个值、只报一次**。
- **按市况给不同的减仓幅度**：那是**判定逻辑改动**，要走 [docs/07 §3.6](../07-回测与验证方案.md) 四条门槛，
  且必须先有论证（含「可能错在哪」）。

**这一轮不许做的事**：拿 ③ 的训练窗口数字去说「策略改好了」。
主判据没过、验证窗口反向，那两个字（改好）现在还不能用。

---

### 5.31 【预注册】`risk.drawdownReducePct` 三点：0.07 / 0.10 / 0.14（2026-08-17 写，**结果未看**）

> ⚠ 上半部分写在跑之前。**只报一次**，不许看完结果再加第四个值。

#### 为什么是这三个值（两条独立证据在同一个参数上汇合）

1. **2026-08-13（§5.18，40 只旧池）**：`7% → 10%` **四个口径同向改善**，但 `t = 0.77` 不达标。
   当时的处置写进了 `params-view.ts` 的备注：「**最优先复核**，需扩池到约 264 只」。
   池子 2026-08-14 已扩到 261 只（§5.20）。
2. **2026-08-17（§5.30，261 只新池）**：把这条规则**整个关掉**是四个消融里最好的一个 ——
   训练窗口 +1.20pp、建仓级胜率 +7.86pp、建仓数只降 2.0%。

⇒ 「关掉」在验证窗口反向（§5.30 的判决是不写回），但**放宽**介于两者之间，
而它是**三个月前就写下的待复核项**，不是看完消融结果才想出来的。

#### 候选与判据（**跑之前锁定**）

`drawdownReducePct ∈ {0.07（出厂）, 0.10, 0.14}`，其余一个字不动。

| 判据 | 门槛 |
|---|---|
| 主判据 | 训练窗口（2018-01-01→2023-12-31）**Calmar > 0** |
| 同向确认 | 验证窗口（判定≈2024-01→2025-06-30，`--from 2022-10-01` 带 302 根预热）**Calmar 不低于基线** |
| 建仓级胜率 | **不低于基线**（两个窗口都要） |
| 硬校验 | 建仓数变化 ≤ 10%（它只改出场） |
| 报告次数 | **一次**。三个值是同一个机制的三档 ⇒ 合起来算一次 |
| 测试窗口 | **不碰** |

**若三个值都过不了主判据，结论就是「方向 D 到此为止」** —— 那时问题只剩买点（方向 A），
而 A 与「出手频率不能再少」的冲突必须由人重新拍板。**不许再加第四个值去试**。

#### 结果（2026-08-17，**只报这一次**）

| `drawdownReducePct` | 训练收益 | 训练 **Calmar** | 训练建仓 | 训练建仓级胜率 | 验证收益 | 验证 **Calmar** | 验证建仓级胜率 |
|---|---|---|---|---|---|---|---|
| **0.07（出厂）** | −1.99% | **−0.113** | 1097 | 43.21% | +0.58% | **0.205** | 50.52% |
| 0.10 | −0.99% | **−0.066** | 1077 | 50.05% | +0.51% | 0.160 | 52.80% |
| 0.14 | −0.79% | **−0.057** | 1075 | 51.07% | +0.43% | 0.134 | 53.15% |
| 参照：整个关掉（9.99） | −0.79% | −0.057 | 1075 | 51.07% | +0.43% | 0.134 | 53.15% |

#### 判决：**三个值都没过主判据 ⇒ 方向 D 到此为止**

| 判据 | 结果 |
|---|---|
| 主判据：训练窗口 Calmar > 0 | ❌ **全部为负**，最好 −0.057（0.14） |
| 同向确认：验证窗口 Calmar 不低于基线 | ❌ 全部低于 0.205，且**单调下降**（0.160 / 0.134） |
| 建仓级胜率不低于基线 | ✅ 两个窗口全部上升（训练 +6.8~7.9pp、验证 +2.3~2.6pp） |
| 建仓数变化 ≤ 10% | ✅ −1.8% / −2.0% |

按预注册的原话：「**若三个值都过不了主判据，结论就是方向 D 到此为止**……不许再加第四个值去试。」
`params.ts` 一个数都不动。

⚠ **验证窗口那两个差值（−0.07pp / −0.15pp）落在本项目的 ±1pp 噪音带里**，
严格说「不低于基线」这条是被一个噪音级的差距判否的。但**不许因此放宽它** ——
主判据（训练 Calmar > 0）差得远（−0.057 vs 需 > 0），那一条已经独立地把这一轮判死了。

#### 新发现：`drawdownReducePct ≥ 0.14` 是**算术死区**（与 §5.16 的 `adx.maxThreshold` 同一形状）

`0.14` 与「整个关掉（9.99）」**逐位相同** —— 收益、Calmar、建仓数、胜率、两个窗口全都一样。

成因是它与固定止损的合取：③ 的条件是 `fromPeak <= -x && !belowStop`，而
`belowStop` 是「自成本亏 8%」。持仓峰值通常就在成本附近（`peakRaw` 从建仓价起算），
于是 `fromPeak <= -14%` 几乎必然意味着 `profit <= -14% < -8%` ⇒ `belowStop` 为真 ⇒ ③ 被短路。
**只有先赚到 ≥ 约 7% 再回撤 14% 的持仓才够得着它，而这个池子上实测一个都没有（差值逐位为 0）。**

⇒ 归档动作：`drawdownReducePct` 的**可测上界是 0.14**，往上标定是在一个死参数上取极值。
与 `adx.maxThreshold` 同一条处置（M2 §5.16）：**别再扫它的上侧**。

#### 这一轮真正的收获（否定结论 + 一个机制解释）

**放宽减仓在两个窗口上单调反向**：训练窗口越放宽越好（−1.99 → −0.79），
验证窗口越放宽越差（+0.58 → +0.43），而**建仓级胜率两个窗口都单调上升**。

这不是「找到了更好的值」，是**一个跨窗口的权衡**：这个参数管的是「回撤里认赔还是扛住」，
而 2018–2023（含两轮熊市）与 2024–2025.6（修复期）给出的最优答案相反。
胜率两边都升说明「扛住」确实让更多建仓最终为正 —— 但在验证窗口里，
被扛住的那批的**盈亏比**变差了，于是总收益反而低。
⇒ 单一常数解不了它。**要动它只能按市况分档，而那是判定逻辑改动（docs/07 §3.6）**，
不属于「调一个数」。

#### 方向 D 结束后的处境

- 风控那 6 个数**仍是 `KEPT`，一个都没动**。但备注已更新：证据指向 `drawdownReducePct`，
  而它的两个可动方向（放宽 / 按市况分档）分别被「验证窗口反向」与「属于逻辑改动」挡住。
- **训练窗口 Calmar 仍是负的**（最好 −0.057），红线依旧过不去，
  `BLOCKED` 那 21 项依旧无法标定。
- ⇒ 问题回到**买点**（方向 A），而它与用户 2026-08-17 定的「**出手频率不能再少**」冲突。
  **这个冲突必须由人重新拍板**，不许自己挑一个继续做。

---

### 5.32 【预注册】单指数长历史：这套引擎能不能给**市场 beta** 择时（2026-08-17 写，**结果未看**）

> ⚠ 上半部分写在跑之前。**只报一次**。

#### 为什么问这个（它是 ETF 那条路的真正问题）

§5.29 起的 ETF 首测（12 只，训练窗口 +2.14% / Calmar +0.092）是今天唯一见过的正 Calmar，
但用户决定**不扩池**（决定 4），而实测 **`N_eff = (Σλ)²/Σλ² = 2.97`**、
第一主成分解释 **54.4%** ⇒ **那 12 只在统计上就是「市场 beta + 两三个行业倾斜」**。

⇒ 「ETF 择时好不好」这个问题的**实质**是「**能不能给 beta 择时**」。
而后者可以在**单一指数的长历史**上测：沪深300 有 **5183 根**（2005-04 起），
功效比 12 只相关 ETF 高一个量级，且只需一只标的、没有横截面相关性问题。

**它还顺带补上[困境说明](./困境说明-2026-08-17.md) §6 那个「池外参照真空」** ——
单指数上「相同窗口的被动持有」是天然可算的，不需要新工具。

#### 标的与窗口

| 用途 | 标的 | 窗口 | 说明 |
|---|---|---|---|
| **主测** | SH000300 | **2005-04-08 → 2017-12-31** | **完全没被看过的窗口**（项目三段划分都从 2018 起）。预热 300 根 ⇒ 判定约从 2006-07 起，约 11.5 年 |
| 同期对照 | SH000300 | 2018-01-01 → 2023-12-31 | 与个股训练窗口同期，可直接对比 |
| 可交易性检查 | SH510300 | 2012-05-28 → 2017-12-31 | 指数不可交易，ETF 才是载体 |
| 第二资产 | SH000001 | 2005-01 → 2017-12 | 与沪深300 高度相关，只作稳健性参考、不进主判据 |

#### 成本口径（明确的假设）

指数按 **ETF 载体**计费：**免印花税与过户费**（`--stamp-tax 0 --transfer-fee 0`），
佣金 0.025%（最低 5 元）与滑点 0.1% 照常。

⚠ **本轮不计 ETF 的跟踪误差与管理费**（现实约 0.15–0.5%/年）⇒ **结果偏乐观**。
这一句必须跟着任何正面结论一起出现。

#### 判据（**跑之前锁定**）

| 项 | 门槛 |
|---|---|
| **主判据** | 配对随机化 alpha（`audit:random --shuffle-spans --trials 200 --seed 1`）：**ALL 层配对胜率 > 50% 且被动分位 ≥ 90%** |
| 次判据 | 主测窗口 **Calmar > 0** |
| **池外参照** | 同窗口被动持有的收益与最大回撤，**必须与策略绩效并列报出**，并注明策略的平均资金占用（不许拿「按占用折算的年化」说事，M2 §5.13 三条） |
| 分层 | **只看 ALL** —— 单标的分 regime 后样本不够（`--min-count 30` 会挡掉） |
| 报告次数 | **一次**。四个窗口是同一机制的不同样本，合起来算一次 |
| 不碰 | **2024 年之后**（那是现有验证/测试窗口） |

#### 预测（写在前面，便于事后判自己错没错）

**主预测：alpha ≈ 50%（与随机无异）。** 依据：个股池 ALL 层是 46.5%，
而 beta 择时的先验更差 —— 指数的噪音更小但趋势更慢，而这套引擎的子信号
（均线金叉、MACD 零轴、布林突破）在慢趋势上更容易反复。

**次预测：绝对收益可能是正的** —— 2005-2017 含 2006-07 与 2014-15 两轮大牛市，
指数本身涨了几倍。**但那是 beta 不是 alpha**，所以主判据必须是配对随机化，
而池外参照那一栏就是防这件事的：若策略赚了 80% 而被动持有赚了 300%，那不叫会择时。

#### 结果（2026-08-17/18，**只报这一次**）

| 标的 / 窗口 | 建仓 | 策略收益 | **被动持有（池外参照）** | Calmar | 平均占用 |
|---|---|---|---|---|---|
| **SH000300 2005-04→2017-12**（全新窗口） | **8** | +24.14% | **+301.70%** | +0.092 | 11.5% |
| SH000300 2018-01→2023-12 | 5 | −1.34% | −16.06% | −0.017 | 11.5% |
| SH510300 2012-05→2017-12（可交易载体） | **2** | +1.70% | +54.16% | +0.027 | 10.1% |
| SH000001 2005→2017（第二资产，仅参考） | 8 | +38.23% | +301.70% | +0.318 | 10.6% |

#### 判决：**主判据无法评估（样本量），但池外参照给了一个硬的否定结论**

| 判据 | 结果 |
|---|---|
| 主判据：配对 alpha ≥ 90 分位 | ⛔ **无法评估** —— 11.5 年只有 **8 次建仓**，远低于预注册的 `--min-count 30`。**刻意不降低 min-count 去凑一个数**（那正是「调到达标」） |
| 次判据：Calmar > 0 | 形式上 +0.092 ✅，但 n=8 ⇒ 无意义 |
| **池外参照** | ❌ **策略远不如被动持有**，三个样本方向一致 |

**池外参照是这一轮真正的产出，也是这个项目第一次有它**（[困境说明](./困境说明-2026-08-17.md) §6
那条「池外参照仍是真空」到此可以划掉）：

> **2005-2017：指数涨了 3 倍（+301.70%），而这套择时只拿到 +24.14%。**
> 平均占用 11.5% —— 即便按占用「折算」（这件事本身不许拿去说事，§5.13），
> 满仓等效也只有约 +210%，仍明显低于 +301.70%。

2018-2023 那一行看着像「跑赢」（−1.34% vs −16.06%），但那是**空仓 88.5% 的时间**换来的，
不是择时准 —— 与 §5.13 那条「超额收益离开占用率会被读反」完全同形。

#### 第二个发现：**引擎在指数上几乎不出手 ⇒「ETF 择时」不可能是 beta 择时**

11.5 年 **8 次**建仓（0.7 次/年）；沪深300 ETF 五年半 **2 次**。
而 12 只 ETF 池同期（2018-2023）有 59 次 —— 拆开看：**行业 9 只贡献 44 次 / +2.45 万，
宽基 3 只 15 次 / +0.21 万**。

⇒ ETF 池那个 +2.14% 的来源是**行业 ETF 的个体波动**，不是市场 beta。
「N_eff ≈ 3、第一主成分 54.4%」说明**统计上**它接近 beta，
但**产生信号的那部分**恰恰是残差的 46%。两句必须一起读，否则会得出相反的结论。

#### 第三个发现（一个坑）：拿指数当标的时，10 万资金**静默地**买不起一手

第一次跑主测得到 **0 建仓**、0 抑制、0 涨跌停，我差点写成「引擎在指数上不出手」。
成因是单位：指数点位 3000–5000，一手 = 100 单位 ⇒ **一手要 30–50 万**，
超过 `--capital` 默认的每标的 10 万 ⇒ `lotsAffordable` 返回 0 手。
**它不进任何计数器**（不是抑制、不是涨跌停），报告上只表现为「有评估、没交易」。

判据是子信号计数：SH000300 与 SH510300 同期几乎逐位相同
（T4 BUY 155 vs 186、T1 BUY 23 vs 23、T3 BUY 39 vs 34）⇒ 信号层没差别 ⇒ 问题在成交层。
**拿指数或高价标的回测时必须同时调 `--capital`**，否则会得到一个看起来像结论的空结果。

顺带记一条我自己的错：主测第一版传了 `--benchmark none`，于是情绪序列恒为 0.5，
与个股/ETF 那些跑（情绪来自 SH000300）不可比。已改用 `--benchmark SH000300` 重跑。

#### 对 ETF 路线的影响（诚实的下调）

这一轮**削弱**了「换成 ETF 就好」的先验，理由是机制被否掉了：
ETF 池的正结果既不是 beta 择时（指数上几乎不出手且远不如持有），
也不是「更干净的标的」（N_eff≈3 说明它们高度相关）。
剩下的解释只有「行业 ETF 的残差波动恰好被这套子信号抓到了 44 次」——
而 44 次 / 6 年、净 +2.45 万（占 120 万本金的 2%），与噪音不可区分。

**不构成「放弃 ETF」的结论**（那是用户的主观判断，决定 3），
但作为建议：**ETF 那条路的证据强度比昨天看起来弱**，
而它唯一还站得住的支撑是「前向记录会自己累积」（影子运行 + 双轨已就绪）。

---

### 5.33 【S0】横截面存不存在：同一天有几只票同时给出可执行买入（2026-08-18）

> 这是 [§3.11](./下一阶段取舍与迭代计划.md) 四阶段里的第一刀，**零回测开销**：
> 只读现有产物。**结论是负面的 —— 但负面来自我自己写的判据，而那条判据有缺陷**，
> 两件事必须分开记（见下面「判据自身的缺陷」）。

#### 口径

- 产物 `reports/calib/t3fix.json`：`0.2.8-unvalidated+c38e329b` ——
  **与当前引擎（含 T3 修正）逐位相同的指纹**，261 只，entry 2019-04-01 → 2026-06-22。
- 建仓级定义与 `metrics.ts` 的 `groupPositions()` 逐字相同：`key = code@entryDate`
  （回撤减仓会把一次建仓拆成两三行，不去重会把「同日多只」算大）。
- **交叉验证**：建仓总数 **1675**，与 §5.26/§5.27 的 `audit:random` ALL 层样本数**逐位相同**
  ⇒ 读的是同一批建仓，没有拿错文件。
- **未新增测试集触碰**：没跑回测、没读任何绩效数字，只数 `entryDate` 的分布。

> 一处必须声明的偏倚方向：`entryDate` 是**成交日**（次日开盘），且涨跌停会让它顺延最多 5 根，
> 所以「同日」相对「同一个信号日」有轻微模糊。更重要的是**它只数得到成交了的建仓** ——
> 被硬抑制、被涨停挡掉、买不起一手的信号都不在里面 ⇒ 这份计数是**下界**，
> 真实的「同日多信号」只会更多。偏倚方向对「判死」有利，对「重开」不利，所以照报。

#### 结果（全期，2090 个交易日）

| 项 | 数 |
|---|---|
| 建仓总数 | **1675** |
| 有建仓的交易日 | 832（占全部交易日 **39.8%**） |
| 其中「同日 ≥2 只」 | **401 天** = 有建仓日的 **48.2%** = 全部交易日的 **19.2%** |
| 每个有建仓日的建仓数 | 中位数 **1** · 均值 2.01 · **最大 12** |
| **落在「同日 ≥2 只」那些日子里的建仓** | **1244 / 1675 = 74.3%** |

每日建仓数分布（天数）：1 只 **431** · 2 只 206 · 3 只 90 · 4 只 47 · 5+ 只 **58**。

#### 时间切分：四个独立子集切得出来（§3.6 ④ 的可行性）

| 子集 | 有建仓日 | 多建仓日 | 占比 | 建仓 | 其中在多建仓日 |
|---|---|---|---|---|---|
| 训练 2019-04→2023-12 | 555 | 260 | 46.8% | 1121 | **73.7%** |
| 验证 2024-01→2025-06 | 150 | 72 | 48.0% | 308 | **74.7%** |
| 测试窗口（**仅计数**） | 127 | 69 | 54.3% | 246 | 76.4% |
| 时间前段 →2022-06 | 334 | 143 | 42.8% | 630 | **69.7%** |
| 时间后段 2022-07→ | 498 | 258 | 51.8% | 1045 | **77.0%** |

**五个切法上都在 70–77%**，没有「效应只在某一段」的形状（对比 §5.23 那个被否掉的候选：
时间前后段相差 12 倍）。多建仓日里的建仓按 regime：`RANGE` 743 · `TRANSITION` 360 · `TREND_UP` 141。

#### 描述性：得分有离散度（**部分证伪我自己的 S2 预测理由 ①**）

| 项 | 值 |
|---|---|
| 全样本 `entryScore` | 最小 **0.600**（= `scoreThreshold`，截断确实在）· 25% 0.738 · 中位 0.809 · 75% 0.894 · 最大 1.000 |
| 同日多只时的极差 max−min（401 天） | 中位 **0.149** · 25% 0.073 · 75% 0.226 · 最大 0.398 |

§3.11.3 的 S2 预测给了两条理由，**第一条（阈值截断 ⇒ 离散度太小）被数据削弱**：
四分位距 0.156，而同日极差中位数 0.149 —— **同日两只票的得分差与全样本的离散度同量级**，
排序的空间是有的。**第二条仍然站着**（子信号权重是写死的常量、不在 62 个叶子里、
网格扫不到，见 §3.11.3 那个框）。

#### ⚠ 判据自身的缺陷（这一段比上面的数重要）

§3.11.2 预注册的判死条件是：

> **同日多信号的天数占比 < 20%，或中位数只有 1 只 ⇒ 横截面根本组不起来，整节判死**

**按字面读，S0 判死**：中位数确实是 1；而「天数占比」在**全部交易日**这个分母下是 19.2%，
恰好压在 20% 以下。**但这条判据我写坏了两处**：

1. **分母没定义。** 同一个「天数占比」有两个读法：48.2%（分母 = 有建仓日）
   与 19.2%（分母 = 全部交易日）—— 一个通过一个不通过。
   这与本项目已经踩过的 `breadth` 分母那条（「分母必须是有行情的只数，不是清单长度」）
   是**同一形状的错**，我在写判据时没有套用自己那条纪律。
2. **「中位数」对这个分布是错的统计量。** 分布高度偏态（均值 2.01、最大 12、5+ 只有 58 天），
   中位数被大量单建仓日主导 —— 而**单建仓日既不需要排序也不干扰排序**。
   判据想问的是「有没有东西可排」，对应的量是**「有多少建仓有同日同伴」= 74.3%**，
   不是「按天数取中位」。

**处置：我不放宽它。** 项目规则明写「放宽判据让候选通过」是禁止项，
而我是在**看过数据之后**才发现判据写坏的 —— 这在形式上与移动球门无法区分。
所以：**S0 按预注册字面记为「判死」**，是否按修正后的判据重开，交用户拍板；
若重开，按规则记成**新候选**（§3.11b）而不是修改这一条。

#### 处置

- S0 已执行，结论如上，**§3.11 的 S1/S2 因此暂停**，等拍板。
- 复现（10 行，不需要新工具）：
  ```js
  const r = JSON.parse(fs.readFileSync('reports/calib/t3fix.json', 'utf8'))
  const pos = new Map()   // code@entryDate → 建仓（去掉回撤减仓的重复行）
  for (const t of r.trades) pos.set(`${t.code}@${t.entryDate}`, t.entryDate)
  const byDate = new Map()
  for (const d of pos.values()) byDate.set(d, (byDate.get(d) ?? 0) + 1)
  const n = [...byDate.values()]
  // 401 天 ≥2、中位 1、74.3% 的建仓落在 ≥2 的日子里
  ```

---

### 5.34 【S0 复测】改测**预注册的那个量**：判据一字不改，结论翻转（2026-08-18）

> §5.33 的问题不在数字，在**测错了量**：判据写的是「同日 ≥2 只票出现**买入方向信号**」，
> 而我数的是**建仓**（成交级）。用户拍板「先测那个真正被预注册的量」，
> 于是**判据一个字不改**，换测量。新工具 `pnpm audit:crosssec`（`src/backtest/crosssec-audit.ts`，
> 只统计不改参数，与 `audit:regime` / `audit:subsignals` 同类）。
>
> **这一节与 §5.33 是两条记录，不是一条的修订** —— 「调整了就是新候选」那条规则同样适用于测量。

#### 口径（三条与 simulate.ts 对齐、一条刻意不对齐）

| 项 | 取值 | 理由 |
|---|---|---|
| 判定根集合 | `i >= warmup` · `i < len−1` · `hasGap` 跳过 | 与 `simulate.ts` 逐条对齐，否则占比与回测报告对不上号 |
| 时段 | `SETTLE` / `minutesSinceOpen = 240` | 同上 |
| 买入方向 | **`BUY` ∪ `NEXT_DAY_WATCH`** | 收盘轮的买入 100% 被 `T1_LATE_BUY` 改写（那是算术）。只认 `BUY` 会数出 0 条 |
| 持仓 | **恒不传**（刻意不对齐） | 问题是「引擎今天对这只票说了什么」，不是「模拟组合恰好持不持有」。持仓会让风控产出 `SELL`/`REDUCE` 把 `BUY` 压掉，那是**组合状态**不是信号 |

**自检（工具内置，不成立就抛错）**：买入信号必须是回测建仓的**超集** ——
每一次建仓都来自一条买入信号，而信号还多含被硬抑制的、开盘涨停买不到的、买不起一手的。
实测 **1675 建仓 ≤ 2439 信号 ✓**。

#### 结果（261 只 · 395 905 个判定根 · 2018-01→2026-08）

| 项 | 值 |
|---|---|
| 买入方向信号（未被硬抑制） | **2439** 条（另有 **221** 条被硬抑制，上界 2660） |
| 有买入信号的交易日 | **963** |
| **「同日 ≥2 只」的天数** | **549** |
| 每个有信号日的只数 | **中位数 2** · 均值 2.53 · **最大 17** |
| 落在「同日 ≥2 只」的信号 | **2025 / 2439 = 83.0%** |

每日只数分布（天数）：1 只 414 · 2 只 226 · 3 只 127 · 4 只 67 · **5+ 只 129**。

#### 判据（**一个字没改**）与裁决

预注册条件：**「同日多信号的天数占比 < 20%，或中位数只有 1 只 ⇒ 整节判死」**。

| 读法 | 值 | 是否触发判死 |
|---|---|---|
| 天数占比，分母 = **有信号日** | 57.0% | 否 |
| 天数占比，分母 = **判定日总数**（1789 天） | **30.7%** | 否 |
| 天数占比，分母 = 全窗口交易日（2090 天） | 26.3% | 否 |
| 中位数 | **2** | 否 |

**⇒ S0 通过。** 而且 §5.33 记的那个分母缺陷**在这份数据上不影响结论** ——
三个分母都在 20% 以上，所以不需要重新定义分母就能裁决。这一点必须记下来：
判据写得含糊是事实，但这次没有被用来救候选。

#### 时间切分（§3.6 ④ 的四个独立子集）

| 子集 | 有信号日 | ≥2 只 | 占比 | 中位 | 信号 | 在多信号日 |
|---|---|---|---|---|---|---|
| 训练 →2023-12 | 643 | 365 | 56.8% | 2 | 1681 | 83.5% |
| 验证 2024→2025-06 | 166 | 99 | 59.6% | 2 | 426 | 84.3% |
| 时间前段 →2022-06 | 391 | 196 | 50.1% | 2 | 870 | 77.6% |
| 时间后段 2022-07→ | 572 | 353 | 61.7% | 2 | 1569 | 86.0% |

四个子集上**中位数都是 2、占比 50–62%、信号在多信号日的比例 78–86%** ——
没有「效应只在某一段」的形状（对比 §5.23 那个被否掉的候选：时间前后段相差 12 倍）。

#### 代理量与预注册量差了一个身位，而它恰好跨过判死线

| | 建仓级（§5.33，下界） | **信号级（本节，预注册的量）** |
|---|---|---|
| 同日 ≥2 只的天数 | 401 | **549** |
| 中位数 | **1**（触发判死） | **2**（不触发） |
| 天数占比（判定日为分母） | 19.2%〜48.2% | **30.7%〜57.0%** |
| 落在多的那一侧的量 | 74.3% | **83.0%** |

差额的来源是明确的：建仓要额外过「未被硬抑制（221 条）· 开盘不涨停 · 买得起一手 ·
未被顺延」四道，而**那四道都不影响「横截面上有几个候选」** ——
提醒层看得见被涨停挡住的那只票，`dispatcher` 也会把它排进得分序列。

**教训（值得单独记）**：用代理量套一条为原量写的判据，可以把结论翻个面。
§5.33 差一点就把这一节判死了。

#### 处置

- S0 **通过**，§3.11 的 S1 解锁。工具进仓库（`pnpm audit:crosssec`），产出落
  `reports/calib/crosssec-261.json`（按日期的 `signaled` / `suppressed` 名单，
  四切法可直接后处理、不必重跑）。
- **未新增测试集触碰**：本节只数信号，没有读任何绩效。

---

### 5.35 【S1】收窄候选池之后的选股 alpha：**74.5% → 55.0%，按预注册判死**（2026-08-18，只报这一次）

> [§3.11.6](./下一阶段取舍与迭代计划.md) 预注册的参数与判据，跑前锁死、跑后一字未改。
> **事前预测「S1 会失败，落到 50–60%」—— 预测被证实。**

#### 它排除的是什么

§5.27 测出「选股」配对胜率 **74.50%**，但同一节的读法第 2 条留了一个未排除的替代解释：
跨票随机会抽到当天**毫无异动**的票（长期横盘、低流动性），而引擎选的至少是「有突破/有动量」的
⇒ 74.5% 可能只反映**「选了活跃的票」**。S1 把候选池收窄到**「当天也有买入方向信号的票」**
（S0 产出的名单，§5.34），于是两侧都是「同样有动量」的票。

#### 口径（`--cross-pool`，新增开关，只统计不改参数）

| 项 | 取值 |
|---|---|
| 命令 | `pnpm audit:random -- --baseline reports/calib/t3fix.json --fixtures ./data/history --cross-code --cross-pool reports/calib/crosssec-261.json --shuffle-spans --trials 200 --seed 1 --min-count 30` |
| 候选池 | 963 个执行日、2439 个候选。**按每只票自己的序列推执行日**（`signalIdx + 1`），不按统一日历 —— 停牌会让同一信号日对应到不同执行日 |
| **单信号日摘除** | **305 / 1675 次建仓当天没有其他候选** ⇒ 余 **1370 次**进配对。两侧必须是同一批建仓，否则「配对」这个词就没有内容 |
| 指标 | 「真实赢的试验占比」（`shuffled.pairedWinFraction`，打散跨度那张表），与 §5.27 同一列 |

#### 结果

| 分层 | 建仓 | **配对胜率** | 真实入场·加权 | 随机入场·加权 |
|---|---|---|---|---|
| **ALL** | 1370 | **55.00%**（= 110/200 次试验） | +0.33% | +0.30% |
| RANGE | 809 | **35.50%** | +0.59% | +0.69% |
| TRANSITION | 407 | **26.00%** | −0.01% | +0.35% |
| TREND_UP | 154 | **99.50%** | −0.15% | −1.93% |

#### 裁决（判据一字未改）

预注册：**ALL ≤ 55% ⇒ 判死；> 60% ⇒ 进 S2；55–60% ⇒ 记「不确定」并停**。

**实测 ALL = 55.00%，恰好落在判死那一侧（`≤`）⇒ S1 判死 ⇒ §3.11 整节结案，S2 / S3 不做。**

> 边界值只差一次试验（110 vs 111 / 200）。**这不构成重跑或换种子的理由** ——
> 「每个候选机制只报一次分位」正是为了防止「差一点就过了，再试一个种子」。
> 换种子就是新候选，而新候选要重走论证与预注册。

**与 §5.27 的 74.5% 不可直接比大小**（§3.11.4 第 3 条）：两者的对照组不同，答的是两个问题。
能说的是**方向**：把「当天毫无异动的票」从对照组里剔掉之后，优势从 74.5% 掉到 55.0%
⇒ **§5.27 那 74.5% 的绝大部分确实来自「选了活跃的票」，而不是「选了会涨的票」**。
那条替代解释被证实，§5.27 的「选股 74.5%」从此不能再被单独引用。

#### 分层是预注册要求报的，但**不许拿它救候选**

`--cross-code` 与 `--match-regime` 互斥 ⇒ 收窄后 regime 不再对齐，所以分层是必报项
（§3.11.4 第 2 条）。报出来之后看到一个刺眼的形状：

- **占 88% 样本的两个最大层（RANGE 809 + TRANSITION 407）都低于 50%** —— 即在同样有信号的票里，
  引擎实际下手的那只**不如随机挑一只**；
- **TREND_UP（154 次）是 99.50%**，而它恰好是绝对绩效最差、被反复怀疑的那一层。

⇒ **ALL = 55% 是一个混合值，不是一个效应。** TREND_UP 那个 99.5% 是一个**新假设**，
不是这条候选的救援：要追它必须另立候选、另写论证与预注册（样本只有 154 次，
且 §5.24 的 T3 修正刚刚改过 TREND_UP 的建仓构成）。
**「只报一次分位」这条纪律保护的正是这种「再切一刀直到有一层达标」的冲动。**

#### 一条连带的结论：气泡按得分抢占，前提被否了一次

`alerts/dispatcher.ts:270` 按得分降序发配额与气泡（docs/05 §4.3）。
它成立的前提是「**在同日的候选之间，引擎的排序带信息**」；
而 S1 否掉的正是这个前提的最弱版本（不是「按得分排序好不好」，而是「引擎选中的那只有没有优势」）。
S2（直接测得分的排序能力）**没有跑**，所以这条规则现在的状态是
**「前提被否一次，排序本身未被直接检验」** —— 已写进 docs/05 §4.3。

#### 处置

- §3.11 **结案（判死）**。§3.10 那条「因子化改造不做」从**断言**升级为**有数据支撑的结论**
  （限于「用现有 `final` 得分做横截面选择」这个具体版本）。
- `--cross-pool` 进仓库（只统计、报告头会打印收窄口径 —— 与 §5.27 的处置同一条理由：
  两份报告长得几乎一样，不标注事后没法分辨）。产物 `reports/calib/s1-crosssec.json`。
- **未新增测试集触碰**：只用信号名单与既有基线报告，没有读测试窗口绩效。

---

### 5.36 判据升级（2026-08-19）：折间标准误改聚类稳健、择时零分布改块位移 —— **门槛只会更难过**

> **这一节不是一个候选，是一次判据变更。** 触发它的是 [迭代计划 §4.6](./下一阶段取舍与迭代计划.md)：
> 项目里三处统计量都建立在「样本独立」的假设上，而三处的样本都不独立 ⇒ 它们全是**乐观上界**。
> 时机是刻意选的 —— **门口没有任何候选**。等到某天看见 `t = 2.3` 才想起来要不要做相关性调整，
> 那就是看过数据之后再改判据（2026-08-18 在 §3.11 的 S0 上刚踩过同一形状）。
> **引擎参数与判定逻辑一行没动**，`ENGINE_VERSION` 不变。

#### 落点一：折间标准误 → 按时间片聚类（CR1）

`pairedDelta` 的 `stdev(Δ)/√n` 里那个 `√n` 收敛只在折独立时成立。而 12 个折单元是
**4 个标的子集 × 3 个时间片、从同一次模拟里切出来的**：同一片里的四个子集共享那段行情的
市场 beta（A 股同涨同跌），它们不是四份独立信息。

改法是均值的**聚类稳健方差**，聚类单位 = 时间片：

```
Var(mean) = G/(G−1) × (1/N²) × Σ_c (Σ_{i∈c} (d_i − mean))²
```

等大簇时它恰好化简为 `stdev(簇均值)/√G` —— 也就是说 **横截面折不再贡献 √n，只贡献簇内平均**。
这句话是这次改动的全部内容：**多切标的子集能让每个时间片的估计更稳，但不增加独立样本数**。
`Resolution.requiredClusters` 因此换了单位（要多少个**独立时间片**），
报告里那句「要么扩标的池」也跟着改成「只有拉长窗口才增加时间片」。

**用例钉的是判据而不是算术**：`calibrate-folds.test.ts` 里两条用例喂的是**逐位相同的分数**
（12 折，Δ 片内恒定、片间 0.030 / 0.012 / 0.001），差别只有「有没有告诉工具哪些折属于同一片」，
而裁决从 **WRITE_BACK 翻成 KEEP**（朴素 t = 3.98 → 聚类 t = 1.70）。
这正是 §4.6 要咬的那种候选 —— 靠刚过线立住的那个。

⚠ **调整之后仍然偏乐观，别写成「已经修好了」**：时间片之间也不独立（片连续、持仓跨片重叠），
聚类只掐掉了横截面那一层。彻底做要上按时间片整块重抽的 block bootstrap，未做。

#### 落点二：择时零分布 → 按建仓月整块位移（block permutation）

旧口径给每一次真实建仓**独立**抽一个随机日。但真实建仓不是独立发生的 ——
引擎在同一段行情里成批出手。独立抽日把这种聚集打散，零分布的方差被系统性压小 ⇒ 分位偏向显著。

改法：按**建仓月**分块，块内共用一个位移 δ（交易根），各成员再吸附到自己 `pool` 里最近的合法日。

**三种子实测（同一份基线 `t3fix.json`、1675 次建仓、200 次试验、配对胜率）**：

| 口径 | 独立抽日 | 块位移 | 吸附距离 |
|---|---|---|---|
| **无 regime 匹配**（块位移**精确**） | ALL 46.5 · TREND_UP 36.7 · RANGE 76.0 · TRANSITION 24.7 | ALL **55.5** · TREND_UP **43.2** · RANGE 73.2 · TRANSITION **38.5** | 中位 0 / P90 0 |
| **同 regime** | ALL 46.7 · TREND_UP 69.2 · RANGE 62.8 · TRANSITION 22.3 | ALL 42.8 · TREND_UP **90.8** · RANGE **22.3** · TRANSITION 39.0 | 中位 11 / P90 116 |

**两行必须一起读，否则会得出相反结论。**

- **精确那一行是结论**：改动 +7 ~ +14pp、四层方向一致（都朝 50% 走）。
  也就是说**旧零分布确实太紧，旧分位确实偏向显著** —— §4.6 的预测被证实。
- **吸附那一行是假象**：+22 / **−40**，方向相反。两行唯一的结构差别就是吸附
  ⇒ 那些摆动来自吸附，不是「块结构揭示了真实的小样本」。

`--match-regime` 的候选池只含同状态的那些天，稀疏 ⇒ 共用 δ 落不下去、只能吸附
（中位 11 根、P90 116 根），块内同步性名存实亡，而吸附方向依赖该状态在这只票上的分布
⇒ **按分层引入偏置**。于是工具**在 `--match-regime` 下自动降级回独立抽日**，
并把降级原因打印在报告头上。**假装调整过比不调整更坏** —— 后者至少是可见的。

⇒ **同 regime 口径的分位（§5.21 起那批，含看板上的 TREND_UP 66.5%）仍然是未调整上界，
这次没有修好它。** 修它要么另设块定义（按 regime 段整段换），要么换统计量，属于未完成项。

一处实现细节值得记，因为它是一个**方向性**错误：吸附的第一版从二分落点按**池下标**交替外扩，
而 `pool` 稀疏且不等距（左边一格可能跳 40 根、右边一格跳 1 根）⇒ 稳定偏向某一侧
= 给零分布加了方向偏置，而零分布的中立性是这个工具全部可信度的来源。
现在按**值的距离**比，等距平局按 `target` 奇偶分两侧（确定性、可复现、两侧各半）。
`tests/unit/backtest/random-audit.test.ts` 六条钉着。

#### 落点三：夏普的 `×√243` —— 停在「如实标注」档，这是刻意的

日收益不 iid（持仓跨日、同池标的同涨同跌）⇒ 年化夏普偏大。
按 §4.6 的三档修法这里只做「立刻」档：**报告加一句「未做自相关调整 ⇒ 偏大」，算法不改**。
理由是夏普**不参与任何门槛**（标定排名口径是 Calmar，折上口径是总收益），
而 Newey-West / Lo (2002) 要选截断滞后阶数 —— 那是一个新的判断，
该单独做一次并单独归档，不该顺手塞进一次判据修复里。

#### 对已有结论的影响：**一条都不翻，但引用强度要降**

没有一条历史结论是靠刚过线立住的（`WRITE_BACK` 迄今只有 1 个且是一整片高原、
`KEEP` 16 个是「怎么动都测不出差别」、`BLOCKED` 21 个是基线自己被红线淘汰），
所以偏乐观的标准误在「什么都没通过」的场景下不改变任何裁决。
但**择时分位那批数要按新的量级引用**：无条件口径下真实的配对胜率比过去报的高 7~14pp
（更接近 50% = 更接近「与随机无异」），同 regime 那批仍未调整。

#### 处置

- 判据变更进 [CHANGELOG](../../CHANGELOG.md)，**不记成任何候选的救援**。
- 迭代计划 §4.6 结案，但留一条未完成项：**同 regime 口径的块定义**。
- 未新增测试集触碰。

---

### 5.37 【预注册】配置形态第一实验：纯波动率目标化 vs 常年满仓 —— **主判据不通过**

> 预注册全文在 [配置形态 · 论证](./配置形态-论证.md) §5，**写于 2026-08-18 00:26，早于本次运行**。
> 用户 2026-08-19 拍板「跑」。工具是 `pnpm exp:vol-target`（`src/backtest/vol-target.ts`），
> **不 import `src/core` 的任何引擎代码、不碰任何未标定参数** —— 这正是选它当第一个实验的理由之一。

#### 预注册（原文照搬，一个字没改）

| 项 | 设定 |
|---|---|
| 标的 | SH000300（2005-04→2017-12 **全新窗口** + 2018-01→2023-12 同期对照） |
| 规则 | `w = min(1, σ_target/σ)`，σ = 过去 20 日已实现波动（年化）。**无方向、无 regime、无指标** |
| 阈值 | `σ_target = 15%`，**预注册一个值，不搜索** |
| 调仓 | 每 5 个交易日；成本照扣（佣金万 2.5 + 滑点 0.1%，ETF 载体免印花税与过户费） |
| 对照 | 常年满仓，同窗口同成本口径 |
| **主判据** | **Calmar 与 Sharpe 同时改善，且两个窗口同向** |
| 次判据 | 最大回撤下降；总收益不低于被动的 80% |
| 报告 | **一次** |
| **预测** | Calmar 与 Sharpe 改善、回撤显著下降、**总收益略低于被动**；若总收益**反而更高**要警惕 —— 那通常是样本期运气 |

#### 结果

| 窗口 | 腿 | 总收益 | 年化 | 最大回撤 | 夏普 | Calmar | 平均暴露 | 调仓 | 累计成本 |
|---|---|---|---|---|---|---|---|---|---|
| **W1** 2005-04-08→2017-12-29（全新） | 波动率目标化 | **+327.80%** | 12.11% | **41.67%** | **0.822** | **0.290** | 69.06% | 506 | 10.82% |
| | 常年满仓 | +301.20% | 11.54% | 72.30% | 0.533 | 0.160 | 100% | 1 | 0.13% |
| **W2** 2018-01-02→2023-12-29（= 训练窗口） | 波动率目标化 | −13.14% | −2.32% | 40.21% | −0.051 | −0.058 | 83.83% | 204 | 2.16% |
| | 常年满仓 | −14.99% | −2.67% | 43.22% | −0.042 | −0.062 | 100% | 1 | 0.13% |

- W1：Δ 夏普 **+0.288** · Δ Calmar **+0.131** · 回撤 **−30.63pp** · 收益占被动 **108.8%**
- W2：Δ 夏普 **−0.009** · Δ Calmar +0.004 · 回撤 −3.01pp · 收益占被动 87.7%

#### 裁决：**主判据不通过 —— 两个窗口不同向**

W1 两项都改善且幅度很大；W2 夏普**变差**（虽然只有 −0.009，量级上等于「什么都没发生」）。
按预注册的写法这就是不通过，**不许改判据、不许换 σ_target 再报一次** ——
「每个候选机制只报一次分位」这条纪律管的正是这种冲动。

#### 而且预注册里那条警告**恰好命中了**

原文写着「若**总收益反而更高**，要警惕 —— 那通常意味着规则恰好躲过了某一次特定崩盘（样本期运气）」。
W1 的收益是被动的 **108.8%**，而 W1 恰好包含 2008 与 2015 两次崩盘（被动回撤 72.30%）。
W2 没有那种形状的崩盘（被动回撤 43.22%、六年阴跌），效果就归零了。
⇒ **W1 的漂亮数字最可能就是「躲过两次大崩盘」这一件事**，不是一个可迁移的机制。
这条警告是事先写下的，所以它现在是**证据**而不是事后解释。

#### 两条不许漏读的

1. **平均暴露 69.06% 却拿到 108.8% 的收益** —— 这不是「低仓位也能赚更多」的一般规律，
   是崩盘期把仓位压到很低的直接结果。离开这一列读收益就会把它读成前者（§5.13 同一个坑）。
2. **成本不是障碍**：W1 十二年半累计 10.82%、W2 六年 2.16%，
   与论证 §3 里「配置形态天然低换手 ⇒ 成本几乎归零」那条**方向一致**，
   这一条本地证据经受住了检验（尽管主判据没过）。

#### 处置

- 配置形态那条路**不自动作废也不自动推进** —— 论证 §5 写的是「若连它都不成立，整条路不用再走」，
  而结果是「一个窗口成立、一个窗口不成立」，**不是干净的否**。
  这是一个需要用户拍板的分叉，判据（样本期依赖）恰恰是论证 §4 ① 列的那条「最大弱点」，
  **它被自己的第一个实验验证了**。
- 未新增测试集触碰：两个窗口都在 2024 年之前，`--touch-test` 计数不变（仍是 5）。

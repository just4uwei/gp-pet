# 05 · 风控与提醒规则

## 1. 风控层的定位

本软件**不下单**，所以风控层的产物不是「拒绝成交」，而是三件事：

1. **抑制**无执行意义的信号（涨跌停、停牌、T+1 限制）
2. **改写**信号（把弱买入降级为观察，把持仓风险升级为强制卖出提醒）
3. **附加**仓位建议文本（不做资金计算，只给比例建议）

## 2. 风控规则

### 2.1 硬抑制（信号直接丢弃，仅落库不提醒）

| 规则 | 条件 | 理由 |
|---|---|---|
| 停牌 | 快照标记停牌或成交量为 0 | 无法交易 |
| 涨停 | `close ≥ limitUp - 0.001` 且方向为 BUY | 买不到 |
| 跌停 | `close ≤ limitDown + 0.001` 且方向为 SELL | 卖不掉 |
| 数据陈旧 | 快照时间戳距今 > 5 分钟且处于连续竞价时段 | 结论不可信 |
| 数据不足 | 日线 < 40 根 | 见 [04 §1.10](./04-指标与信号引擎.md) |
| 新股次新 | 上市 < 60 个交易日 | 无稳定形态，指标失真 |

### 2.2 降级

| 规则 | 条件 | 动作 |
|---|---|---|
| T+1 尾盘 | BUY 信号且当前时间 ≥ 14:50 | 改为 `NEXT_DAY_WATCH`，文案「明日开盘观察」，提醒降至 L1 |
| ST / *ST / 退市整理 | 名称含 ST | BUY 信号一律降为 L1 并加风险标注 |
| 行业集中度 | 该标的所属行业在持仓中占比 > 20% | BUY 降一级并提示「行业集中度已达 X%」 |
| 极度扩张 | `BBW_PCT > 90` 且方向为 BUY | 降一级，文案「波动率处于历史高位，追高风险大」 |

### 2.3 持仓风控（仅对 `position` 表中有记录的标的生效）

这一类**不经过组合层得分**，是独立的强制通道——止损不需要投票。

| 规则 | 条件 | 动作 | 提醒级别 |
|---|---|---|---|
| 固定止损 | `(price - cost)/cost ≤ -8%` | `SELL` 强制 | **L3** |
| 回撤减仓 | `(price - peak)/peak ≤ -7%` 且当前仍盈利或微亏 | `REDUCE`，建议减仓 50% | **L3** |
| 盈利保护 | 曾达 `+5%` 后回落至 `+2%` 以下 | `REDUCE`，建议保护利润 | L2 |
| 移动止损 | 盈利 ≥ 5% 后，从 `peak` 回撤 ≥ 3% | `SELL` | **L3** |

`peak_price` 由每个交易日收盘时更新（`max(peak, high)`），存于 `position` 表。

> ⚠ 这些百分比直接采自来源文档，未经验证。8% 止损对高波动科技股可能过紧、对低波动蓝筹可能过松。**P1 迭代**：按标的自身 ATR 自适应（如止损 = `max(8%, 2×ATR14/close)`），并在设置中可切换固定/自适应。

#### 用户可以重画固定止损那条线（2026-08-14，`009_position_stop.sql`）

固定止损是 L3、不可降级，于是一只已经跌破 8% 的票会天天提醒同一件事 ——
而用户早就知道了、并且决定扛着。**反复提醒同一件已知的事与不提醒一样有害**：
它会让用户开始整体忽略这个软件的提醒。

做法不是「静默这只票」，而是让他**重新画一条线**：在持仓页确认「接受这一段亏损」，
把止损线挪到 `position.stop_floor`（不复权绝对价，建议值 = 现价 × (1 − 8%)，可改）。
此后固定止损的判据从「亏够百分比」换成「跌破那个价」。

四条边界，改这块之前逐条对：

1. **只换判据，不取消提醒。** 跌破新线照样是 L3 强制类。
2. **只作用于固定止损这一条。** 回撤减仓 / 盈利保护 / 移动止损照旧 ——
   用户接受的是「这一段下跌」，不是「所有风控都别响了」。
   注意回撤减仓的「仍盈利或微亏」原先写成 `profit > -stopLossPct`（把固定止损的判据
   抄了一遍），重画线之后那两者会分叉：抄的那份会在 −8% 就把回撤减仓关掉，而固定止损
   要等跌破新线才响 —— 中间那一段两条**都不响**，那不是用户答应的事。
   `positionVerdict` 因此让两条共用同一个 `belowStop`。
3. **加仓与清仓必须清掉它**（`PositionRepo.set` 里那三个 `= NULL`）：成本变了，
   旧的那条线不再是同一个判断；清仓后留着它，下次建仓会莫名其妙地不提醒。
4. **绝不进回测与影子运行。** 它是用户对某一次具体持仓的决定，不是策略的一部分 ——
   让它影响 `src/backtest` 或 `main/shadow` 会把「我扛住了没卖」记成策略绩效。

界面上有三件事是硬要求（这是在**主动关掉一个安全提醒**，而
「少发的错误用户发现不了」）：确认前把代价原话写出来（「跌到 8.46 之前，不会再因为亏损提醒你」）、
说清哪些规则还会响、以及**随时可撤销且撤销入口一样显眼**。

### 2.4 组合级提示（不阻断，仅信息）

面板顶部常驻展示，不主动弹窗：
- 持仓行业分布与最大集中度
- 高波动 / 低波动标的比例（按 `ATR14/close` 的 250 日分位，> 70 分位算高波动）
- 当日已触发信号数（异常多说明市场剧烈波动或参数过敏感）

---

## 3. 提醒分级

「不干扰日常工作」是 P0 目标，所以提醒的**默认档位必须偏保守**：多数信号只改变悬浮条上的状态点，不弹任何东西。

> **2026-08-13 起渠道只剩两个**：状态点与气泡。托盘角标、托盘图标闪烁、系统通知
> （连同「可发声」这件事本身）全部移除 —— 提醒的可见出口唯有气泡。
> **级别没有合并**：L2 与 L3 表现相同，但冷却窗口（2h vs 当日一次）与每日上限不同，
> 那才是三级制真正在管的事。下表「是否可发声」一列因此整列作废。

| 级别 | 表现 | 触发条件 | 同键冷却 |
|---|---|---|---|
| **L1 静默** | 状态点变化 + 未读计数 +1 + 面板列表新增 | 得分达阈值但 < 0.75 的普通信号；`PROVISIONAL` 弱信号；信号失效通知 | 30 分钟 |
| **L2 气泡** | 状态点 + Bubble 窗口淡入，6s 后自动淡出，`showInactive` 不抢焦点 | 得分 ≥ 0.75 的信号；盈利保护类提醒 | 2 小时 |
| **L3 气泡（强）** | 同 L2 | `CONFIRMED` 且得分 ≥ 0.75；**全部持仓强制止损/移动止损/回撤减仓** | 当日一次 |

用户可在设置中整体上调/下调一档 —— 但**不作用于持仓强制类**：把止损从 L3 调成 L2
意味着跌停那天不响，而这类「少发」的错误用户自己发现不了（`alerts/candidates.ts`）。

### 3.1 提醒有三类来源，都过同一套闸门

`buildAlerts()` 产出的候选来自三处，**没有第四条路**：

| 来源 | 方向 | 级别 | 说明 |
|---|---|---|---|
| 策略信号 | 引擎判的方向 | 风控层定 | 主线 |
| 收盘失效提示 | **`NONE`** | L1 | 盘中那条没被收盘确认（docs/04 §6） |
| **观察点命中**（P2 续） | **`NONE`** | L2 | 用户确认过的盯盘条件成立了 |

后两类的方向都刻意用 `NONE`：同键冷却的键是 `code:direction`，沿用原方向会让上午那条
买入提醒的 2 小时冷却把它们一起吃掉。

**观察点是什么**：AI 解读的第四段（「如果判断错了，会先看到什么」）给出一个带具体数值的
失效条件 → 用户在表单里确认（可改）→ 引擎每轮机械比较 → 命中后走这四道闸门。
触发时刻**不涉及模型**，判定是一次纯比较。

**它不是策略参数。** 与 `src/core/params.ts` 的区别是硬的：后者是引擎的、全局的、
依据必须是本地回测标定（ADR-0003）；观察点是用户的、单标的、一次性、会过期，
依据是「用户确认」。两者混在一起会污染设置页那张标定状态表。
表结构与边界见 `storage/migrations/003_watch.sql` 的头注释。

命中提醒**不是**强制类：它照过防抖、冷却、频率上限与免打扰。
理由是「少发一条用户自己设的观察点」与「少发一条止损」不是同一量级的错误 ——
前者用户看得见（观察点页里那条还在「正在跟踪」），后者看不见。

到期未命中记为**已过期**，只落库与显示、**不发气泡**：
「你当时那个判断没兑现」是个结论，但一天几十条这种通知是骚扰。

### 3.2 内置「行业ETF」组走**独立配额的第二条轨**（2026-08-17 改；此前是整个摘掉）

应用内置 15 只行业 ETF（`shared/industry-etf.ts`，每个行业一只）用来看行业动向。

**2026-08-15 的做法是把它们在 `buildAlerts()` 之前整个摘掉**（`alertable()`）。
理由是频率上限**全局共享**（每小时 L2+L3 ≤ 6、每日 L3 ≤ 10）：
15 只观察标的与 7 只真持仓标的抢同一份配额，等于用行业数据换掉真提醒 ——
而被挤掉的那条止损，用户不会知道自己漏了。

**2026-08-17 用户拍板改成双轨**：它们进闸门，但走 `OBSERVE` 轨。
「不挤占」这条保证由**两处**共同实现，少一处都不成立：

| 出口 | 做法 | 在哪 |
|---|---|---|
| **配额** | `OBSERVE` 有自己的每日 L2+L3 上限（`observeDailyLimit`，默认 **2**），**完全不读也不占** `perCodeToday` / `recentHigh` / `l3Today` | `dispatcher.ts` 的 `checkCaps` / `commit` |
| **气泡** | 一次只弹一个气泡，挑的时候**先按轨道再按得分** —— 观察标的得分再高也抢不到 | `service.ts` 挑气泡那一段 |

于是 `PRIMARY`（用户自选股）的行为**逐位不变**（M3 正在攒的提醒日志证据不受干扰），
而每日打扰上限最多增加 2 条。

⚠ **那个 2 是未标定的猜测值**，与 `alert.bubbleScore` 同一档：判据只能来自提醒日志
「这几条值不值得被打断」，回测原理上测不到。取 2 的理由是保守 ——
观察标的的信号是配置级的慢事件，而每加一条就是每天多一次打扰。

**`alertable()` 那个钩子仍然在**，只是不再给 ETF 用：日后若有别的东西需要
「完全不进提醒链路」，靠的还是它。摘在哪一层的道理没变 ——
拦在引擎里会连信号一起没了（那正是要观察的东西），
拦在闸门后会在 `alert_log` 里留一堆「被挡」的行，而它们根本没进过闸门，
那是在谎报一个不存在的拦截（§6 的可审计性靠的就是这张表说真话）。

### 3.2a 有持仓就翻回 `PRIMARY`（2026-08-18 改；此前是整组恒 `OBSERVE`）

2026-08-15 那三条限制（不进日报、不进悬浮条跑马灯、不给持仓入口）成立的前提是
**这 15 只不会有持仓**。用户 2026-08-18 拍板取消了最后一条：行业 ETF 可以真的建仓。

持仓一旦存在，止损 / 回撤减仓那套**持仓强制类** L3 提醒就会对它生效，
而 `OBSERVE` 轨的两条性质正好把它废掉：日配额只有 2 条、且**抢不到气泡**。
于是「跌破止损」可能一声不响 —— **少发的错误用户发现不了**，
这正是 §4.2 那条台阶 bug 的同一个形状。所以：

| 状态 | 提醒轨 | 日报 | 悬浮条跑马灯 | 信号列表的颜色 |
|---|---|---|---|---|
| 行业ETF，**无持仓** | `OBSERVE` | 不进 | 只在今天出了未静默信号时露出 | 中性青色 + 「行业」标记 |
| 行业ETF，**有持仓** | `PRIMARY` | 进 | 恒在 | 与个股一样的红/黄 |
| 自选个股 | `PRIMARY` | 进 | 有持仓恒在，否则出了信号才露出 | 红/黄 |

判据在 `main/alerts/track.ts`（纯函数，有用例），**每轮现读** ——
用户刚录完一笔成交，下一轮就该按个股待遇提醒。
渲染层那个中性色的判据必须与它逐字一致（`panel/App.tsx` 的 `observeCodes`）：
颜色说的是「这条不会像个股那样提醒你」，与实际行为分叉就是在说假话。

代价说清楚：M3 正在攒的提醒日志里，`PRIMARY` 那一侧从此可能混进 ETF 的行。
但那些行对应的是**真持仓**，本来就属于「该不该被打断」这个问题的样本，要拆也拆得开（按 code）。

> **跑马灯那一栏 2026-08-18 一并收窄了范围**：从「全部自选轮流显示」改成
> **「有持仓的 + 今天出了未静默信号的」**（判据 `shared/ticker.ts` 的 `visibleTicker`）。
> 顺带修掉一处既有的假话：原先那行「过滤掉行业ETF 分组」只滤了 `buildTicker` 的 `items` 入参，
> 而它的主集合是 `items ∪ quotes ∪ 今日有信号的`，`push:quoteTick` 推的又是全部自选
> —— **那条过滤从来没有生效过**，而界面上完全看不出来。

---

## 4. 去重、冷却、防抖

四道闸门依次通过，任一不过则记录 `alert_log.suppressed_reason` 后丢弃。**被抑制的信号仍然入库**，用户在面板里能看到「今日被静默的 5 条信号」，不制造信息黑洞。

> **口径修正（2026-08-14）：每一条*不同的*裁决写一行，重复的记在同一行上。**
>
> 原先是「每一条候选都写一行」，而 `buildAlerts` 对**每一轮** tick 的每个非 NONE 信号
> 都造候选、分发器按同键冷却把它丢掉 —— 一条持续一上午的买入信号会在日志里刷
> 200+ 行一模一样的东西，用户翻不动，真正发出去的那几条被淹掉。
>
> 现在判重签名是 `signalId | level | channels | suppressedReason`（`AlertService`），
> 相同则把首行的 `repeat_count + 1` 并更新 `last_at`（migration 006），日志上显示
> 「×47 · 持续到 14:52」。**信息一条没少**：「这个状态持续了多久」正是
> 「它是不是漏提醒了」需要的那一半。
>
> 三条配套纪律：`signalId` 必须在签名里（新信号就是新事件）；重复**不动** `read_at`
> （未读数答的是「有几件新事」）；签名表是内存态，重启后同一状态会多留一行 ——
> 可以接受，不要为了省一行日志在每轮 tick 的热路径上加一次查询。

### 4.1 防抖：连续确认

盘中信号必须**连续 N 个 tick 成立**才允许提醒（默认 `N=2`，即 ~60s）。指标在阈值附近抖动时会反复触发又消失，单 tick 触发是骚扰的最大来源。

```
signalKey = `${code}:${direction}:${topSubSignalId}`
连续计数存内存 Map；不成立即清零
```

### 4.2 去重：同键冷却

同一 `${code}:${direction}` 在冷却期内不重复提醒：

| 级别 | 冷却期 |
|---|---|
| L1 | 30 分钟 |
| L2 | 2 小时 |
| L3 | 当日一次 |

例外：**持仓强制止损类不受冷却限制**，但改为「每次跌幅每扩大 2% 提醒一次」，避免既骚扰又漏报。

> **台阶只被「真的弹过气泡」的那一条消耗**（2026-08-17 补，真机日志掉出来的缺陷）。
> 被闸门③④降级成 L1 的那条没打扰过任何人 —— 让它消耗台阶就只剩「漏报」这一半：
> 08-17 开盘第一轮屏幕锁定，三只跌破止损线的持仓（浮亏 −7.8%）被降为 L1，
> 台阶却记成了 −7.8%，于是解锁之后跌到 −8.4% 整天再没有一条气泡（1580 行「台阶未扩大」）。
> 这与 §4.3 那条「被降级成 L1 的不占 L2/L3 额度」是同一条纪律 —— **台阶就是一种额度**。
> 代价是免打扰持续期间每轮都重新造一条候选并留痕（被 006 的判重折成一行），
> 换来的是免打扰一解除、那条止损气泡立刻补上。
> `tests/unit/main/alerts.test.ts` 两条钉着（免打扰与频率上限各一条）。

### 4.3 频率上限

- 单标的每日提醒（L2+L3）≤ 4 条
- 全局每小时 L2+L3 ≤ 6 条，超出的按得分排序保留最高的，其余降为 L1
- 全局每日 L3 ≤ 10 条

超限时未读计数仍累加、面板列表仍新增，用户主动查看不受任何限制。

> **⚠「按得分排序保留最高的」这条的依据状态（2026-08-18）：前提被否了一次。**
>
> 实现在 `alerts/dispatcher.ts:270`（`[...survivors].sort((a, b) => b.score - a.score)`），
> 它是全应用里**唯一一处横截面选择** —— 同一天多只票都够格时，配额与气泡给谁。
>
> 它成立的前提是「同日候选之间，引擎的排序带信息」。2026-08-18 测了这个前提的**最弱版本**
> （不是「按得分排序好不好」，而是「引擎选中的那只有没有优势」）：把随机对照组收窄到
> 「当天也有买入方向信号的票」之后，1370 次建仓的配对胜率 **55.0%**，
> 而占 88% 样本的两个最大分层 **RANGE 35.5% / TRANSITION 26.0%** —— 低于 50%
> 意味着在同样有信号的票里，引擎实际下手的那只**不如随机挑一只**。
> 详见 [M2 §5.35](./notes/M2-偏差报告.md)。
>
> **排序本身尚未被直接检验**（那是 §3.11 的 S2，因 S1 判死而没有跑）。
> 所以这条规则现在的状态是「**前提被否一次，排序未直接测**」，不是「已验证」。
> 眼下**不改它** —— 没有替代依据；随机排序或按 level 排序都同样没有依据，
> 而换一个同样没依据的规则不是改进。要改先走 [docs/07 §3.6](./07-回测与验证方案.md) 的四条门槛。

### 4.4 免打扰探测

以下任一成立 → 所有 L2/L3 降级为 L1（不弹气泡，只改状态点并计入未读）：

| 条件 | 检测方式 |
|---|---|
| 用户设定的静默时段 | 设置中的时间段 |
| 全屏应用 / 演示模式 | Windows `SHQueryUserNotificationState`（值为 `BUSY`/`PRESENTATION_MODE`/`RUNNING_D3D_FULL_SCREEN` 时静默）；取不到则回退为「前台窗口尺寸 == 屏幕尺寸」 |
| 系统专注助手开启 | 同上 API 返回 `QUIET_TIME` |
| 手动免打扰 | 托盘 / 悬浮条右键菜单切换，可设 30min / 2h / 直到收盘 |
| 屏幕锁定 / 会话断开 | `powerMonitor` 的 `lock-screen` 事件 |

免打扰期间悬浮条也进入**低调态**：跑马灯停滚，状态点只在有信号时做一次极轻微的变化（不进 WATCHING）。

> **实现（2026-08-13）**：`SHQueryUserNotificationState` 在 Electron 里没有对应 API，
> 表里写的回退方案（比较前台窗口尺寸）同样需要原生调用。实现走 **PowerShell 子进程**
> （`-EncodedCommand` 传一段 P/Invoke），不引入原生 FFI 依赖 ——
> 判据是「这条探测失败的后果只是照常提醒，不是功能不可用」，不值得为它再养一个
> 需要跟 Electron ABI 对齐的原生模块。代价是每次探测起一个进程，所以：
> **分发路径只读缓存**（TTL 15s，一轮 tick 30s，最多陈旧一轮）、单飞、
> 连续 3 次失败即永久停用并留一条 warn。
> 停用后状态为 `UNKNOWN`，**判为可以提醒** —— 与本节的错误不对称一致。
> 代码见 `src/main/alerts/notification-state.ts`，聚合判定见 `src/main/alerts/dnd.ts`。
>
> 五个来源里，**手动免打扰排在优先级第一**：它只影响「显示哪条原因」，不影响结论，
> 但用户自己按下去的开关不该被「检测到全屏」这种解释盖过。
> 同理菜单里的「解除免打扰」只管得着**手动**那一项 ——
> 用聚合值判会让静默时段里的点击变成「解除一个我没开过的开关」，点了没反应。
> （悬浮条形态下双击是「开面板」，C8 的一键静默因此只在右键菜单里，见 docs/06 §4。）

---

## 5. 提醒文案规范

结构固定为四段，便于扫读：

```
[方向徽章] 名称 代码            置信 78%
金叉突破 · 趋势市
MA5上穿MA20 / MACD零轴上金叉 / 放量1.4倍
14:32  现价 12.34 (+2.1%)
```

规范：
- **不使用**「必涨」「牛股」「抄底」「稳赚」等词
- **不使用**「胜率」描述置信度（见 [04 §4.3](./04-指标与信号引擎.md)）
- 依据行最多列 3 条最高权重的子信号，完整依据在面板展开
- 卖出/止损类用暖橙色而非红色（A 股红涨绿跌，红色作警示会与涨跌色冲突）
- 每条提醒底部固定小字：**仅供参考，非投资建议**

---

## 6. 提醒可审计

面板提供「提醒日志」视图，列出每条信号及其归宿：

| 时间 | 标的 | 方向 | 得分 | 结果 |
|---|---|---|---|---|
| 10:32 | 600xxx | BUY | 0.78 | 已提醒 L2 |
| 10:45 | 000xxx | BUY | 0.64 | 静默（冷却期内） |
| 11:02 | 300xxx | SELL | 0.81 | 静默（检测到全屏演示） |
| 15:00 | 600xxx | BUY | — | 收盘失效（INVALIDATED） |

用户能回答「它是不是漏提醒了」，也能据此调整灵敏度。这是建立信任的关键界面。

> **两张表两件事，别合并**（2026-08-13 实现时定的）：
> `signal` 表回答「引擎判了什么」，面板的**今日信号**读它；
> `alert_log` 表回答「它有没有真的提醒我，没提醒是被哪道闸门挡的」，**提醒日志**读它。
>
> 于是 §2.1 的**硬抑制**（停牌、涨跌停、数据不足、次新股）**不进 alert_log** ——
> 那一类的原话是「信号直接丢弃，仅落库不提醒」，它已经带着原因躺在 `signal` 表里，
> 在今日信号里显示为「已静默：停牌」。再往 alert_log 写一遍，提醒日志就变成了信号日志，
> 两张表说同一件事，而「有没有漏提醒」这个问题反而被稀释了。
>
> 一条实现约束：`alert_log.signal_id` 是指向 `signal(id)` 的外键（`PRAGMA foreign_keys = ON`），
> 所以**落不了库的提醒不发**（`src/main/alerts/candidates.ts`）——
> 发出去却没有审计记录，等于在这个界面上凭空消失一条。

# CHANGELOG

本文件只记录**影响引擎行为的变更**：参数标定结论、算法口径修正、`ENGINE_VERSION` 递增。
UI 与工程改动看 git 历史，不必往这里搬。

每条参数标定条目必须写清三件事：**动了哪个数、依据是哪次回测、什么条件下这个结论会失效**。
理由见 [ADR-0003](./docs/adr/ADR-0003-来源文档数值不作为出厂默认.md) —— 出厂默认值的来历
不允许是黑箱。

---

## 缺陷修复：行业分类在真机上从来没有落过库 —— 行业集中度风控因此一直是空转（2026-08-30，`ENGINE_VERSION` **不变**，**回测数字一个没动**）

**为什么进这个文件。** 它改的不是引擎算法，而是**喂给引擎的一个输入**：
`risk.industryConcentrationCap = 0.2`（docs/05 §2.2）判的是 `EngineContext.industryShare`，
而那个数由 `signals.ts` 的 `industryShares()` 从**自选表的 `industry` 列**算出来 ——
真机上那一列 **79/79 全空** ⇒ 每只票的 `industryShare` 都是 `undefined`
⇒ **那条风控规则从上线起一次都没有可能触发**。`industry_history`（014，2026-08-23 上线）
**一行都没有**，是同一个根因的另一个出口。**回测不受影响**：它本来就读不到这个字段
（`params-view.ts` 里 `industryConcentrationCap` 标的正是这一条），所以既有报告一个数都不用重跑。

**根因（真机 `provider_health` 逐条可查，不是推测）。** 2026-08-26 11:35:23：
eastmoney 连续三次失败（`minute SZ300750` / `calendar 2026` / `calendar 2027`）触发
`failureThreshold = 3` ⇒ 5 分钟冷却；而**每周一次的 `refreshProfiles()` 恰好在同一秒开跑**
⇒ 79 只 profile 全部由腾讯服务（腾讯的 `fetchProfile` 明写「industry 不提供」）
⇒ 拿到「有名字、没行业」的资料，刷新报**成功**并盖下时间戳 ⇒ 下一次要等七天。

2026-08-22 加的那个「降级时按字段重试一次」本该救这一下，而它**八天里一次都没触发**：
判据写的是 `result.degraded`，当时的实现是 `attempts.length > 1` ——
**主源在冷却里会被 `ordered()` 排到最后、压根不被试**，备源第一个就成功
⇒ `attempts.length === 1` ⇒ `degraded === false`。
**函数按注释工作，注释与实现说的不是一件事。**

**动了三处。**

1. `providers/registry.ts`：`degraded` 改判「**值不是来自优先级最高的那个源**」
   （含主源被冷却跳过）。要区分「试过并失败」与「压根没试」的调用方自己数 `attempts`
   —— 信息没有丢，只是不再被一个布尔盖住。
2. `engine/watchlist.ts`：字段级重试的判据换成 `degraded && attempts.length > 1`
   （= 主源这一轮真的被试过且失败了 ⇒ 立刻重试有机会）。
   **主源在冷却里的那一种不再原地重试** —— 再问一次必然还是同一个备源，白烧一个请求。
3. `engine/tick.ts`：新增 `INDUSTRY_RETRY_INTERVAL_MS = 24h` 的补行业 pass
   （meta 键 `industry_retry_at`）。只问 `pendingIndustry()` 那批（**按板块筛**，
   ETF / 指数结构上没有行业，留在名单里会让它永远清不空），整周刷新刚跑过的那一轮不跑，
   一个都不缺时零请求。上界是「还缺行业的股票数」个请求／天，且**拿到即收敛**。

**什么条件下这个修法失效。** ① eastmoney 换掉 `f127` 或对 profile 端点长期不可用
—— 那时名单清不空，日志里「这趟之后还缺 N 只」会一直不降，判据在那一行；
② 自选里出现「板块是股票、但确实没有行业」的标的（未见）—— 它会每天被问一次，
上界仍是每天 1 个请求，可接受但要在台账记一笔。

**⚠ 这次没有做的**：不把腾讯判成失败（会连名字一起丢，且与「空结果不算失败」冲突）·
不动熔断参数（5 分钟冷却与 `failureThreshold = 3` 是另一件事的判据）·
不给 `industry_history` 补历史（它记的是**观测**，补出来的不是观测）。

---

## 口径修正：影子运行的强制离场改按**影子自己的持仓**判定（2026-08-28，`ENGINE_VERSION` **不变**，**回测数字逐位不变**）

**动了什么。** 影子持仓此前**基本没有离场通路**。闸门那条持仓强制通道
（止损 / 移动止损 / 回撤减仓 / 盈利保护）读的是**用户手工录入的 `position` 表**
（`engine/signals.ts` 取 `storage.positions`），而 `positionVerdict()` 第一句就是
「没有持仓 → null」⇒ 影子持有、用户没录入的票，**那四条规则一条都不会触发**；
`REDUCE` 只由那条通道产出 ⇒ 对这类票**永远不可能出现**；
即使用户也持有同一只，判据用的还是**用户的成本价与峰值**，不是影子的建仓价。

修法：`@core/risk` 那四条规则抽成窄入参 `forcedExit()`（**纯重构**），
影子在盯市之后按 `entryPriceRaw` / `peakRaw` / 当日 `close` 调一次
（`shadow/portfolio.ts` 的 `shadowExitOrder`，docs/07 §2.3c）。
**规则实现仍然只有一份** —— 与闸门、回测同一份代码。

**依据。** ① 回测里 **96.9% 的离场由风控触发**（[M2 §5.24](./docs/notes/M2-偏差报告.md)，
策略卖出子信号全池只有 171 次）⇒ 缺这条通路，影子量的**不是回测那套策略**，
它的数字与任何一份回测都不可比；② 旁证：`peakRaw` 一直在逐日维护并落库
（那正是移动止损与回撤减仓的输入），而修复前**全项目没有任何地方读它**；
③ 立刻可验：2026-08-28 直读账本试算 7 只影子持仓，
**`SZ300003` 已满足 `DRAWDOWN_REDUCE`**（成本 12.492 / 峰值 13.010 / 现价 12.060，
自峰值 −7.30%，门槛 −7%）—— 而用户不持有它 ⇒ 那一天它完全没有风控通道。

**⚠ `ENGINE_VERSION` 不变、回测数字一个没动**，而这句话是**实测**的：
出厂口径训练窗口（252 只 / 2018-01-01→2023-12-31）跑两遍 ——
一遍用改后的 `src/core/risk`，一遍把那个文件换回 `HEAD` 的版本、其余全部不动 ——
两份报告**去掉 `generatedAt` 后逐字节相同**（2451903 bytes）。
（`src/main/shadow` 影响不到这条：`src/backtest` 不 import 它，方向是单向的。）
`tests/unit/risk/risk.test.ts` 另有一条钉住两个入口在同一批输入上逐位一致
（并断言那组输入真的把四条规则各走了一遍 —— 否则 `toEqual` 会在两侧都是 null 时空过）。

**⚠ 别拿归档的 `reports/calib/l2a-train.json` 当这次的对照** ——
那份生成于 2026-08-26，**早于 08-27 的 `isST` 修复**（`48dcf71`），
所以重跑必然与它有微小差异（训练窗口实测 **−1.99228% → −1.99096%，+0.0013pp**）。
那正是那次修复自己预告过的「下次重跑时数字会有微小变化 —— 那是这次修复不是引擎变了」，
量级与它记的存活腿 −0.0211pp 同级。**既有基线仍然不用重跑，`−1.99%` 那个引用不动。**
判「某次改动纯不纯」只能拿**同一时刻的两份代码**比，不能拿归档报告比 ——
中间可能夹着别的已修缺陷。

**⚠ 记录没有清空，所以影子曲线分两段。** 判据：修复之前 `shadow_trade`
**一行都没有**（实测 9 个净值点、7 只持仓全在、**0 笔往返成交**）
⇒ 第一段里不存在带着旧口径的成交 ⇒ 「两套口径混进同一条曲线、事后拆不开」
在那份数据上不成立。分界点落在 `SHADOW_KEYS.exitRulesFrom` + 一行 `RULES_CHANGED` 流水，
面板用**中性色**说出来。**「参数一变就停止累积」那条纪律防的是事后拆不开**，
而记下分界点恰好把它拆得开。⇒ **引用影子绩效必须带上分段日期。**

**什么条件下这个结论会失效**：① 若日后 `hardSuppressions` 里出现一条**与用户持仓无关、
且执行层不处理**的抑制规则，影子这侧就需要跟着补（现在刻意只留「停牌」一条，
逐条理由在 `shadowExitOrder` 头注释）；② 若影子改成会读 `stopFloor` / `lockedShares`，
这条修正立刻失效 —— 那两个字段是用户对某一次具体持仓的决定，
让它们影响影子等于把「我扛住了没卖」记成策略绩效。

---

## 判据补强：夏普之差的 `p` 多了一条 LW 原文推荐的**循环块自举**（2026-08-27，`ENGINE_VERSION` **不变**，**没有任何数被重判**）

见 [M2 §5.80 预注册 / §5.81 结果](./docs/notes/M2-偏差报告.md)。
`metrics.sharpeDiffBootstrap` 实现 **Ledoit & Wolf (2008)** §3.2.2 + Remark 3.2
（**Politis & Romano 1992** 的循环块自举 + **Götze & Künsch 1996** 的块结构标准误 + Eq. (9) 的
studentized `p`），一手核对自原文 PDF。

**动机**：LW 原文说 **HAC 推断在中小样本上偏自由** ⇒ 落在 0.01–0.10 里的 `p_HAC` 系统性偏小，
而这个项目已在三处引用过那样的 `p`，只能照抄「它是下界」这句没量过的话。

**结论：能用，但它买到的东西只有 ≈ 2pp。** 三个配对 × 块长 `{1,5,10,20,40}` 实测，
自举与 HAC 最大差 **2.4pp**，`b=1` 那档甚至更小。
**根因是原文那条警告是 `T = 120` 的模拟结论，而我们的配对 `T = 1214–4548`（10–38 倍）。**
⇒ 「`p` 是下界」现在有数了：**下界宽度约 2pp，不是一个倍数。**

**⚠ 三条边界**：
① **`p` 仍然不参与任何门槛**，也**没有重判任何裁决** ——
§5.79 主判据按 Calmar 反向判的不通过、§5.59 判据 B 按 16.50% 拖累判的不通过，都保持原样；
② **块长报整条网格、不提供选一档的接口**（同 DSR 的 `N`）—— 现成的理由：
§5.79 训练窗口那个 `p` 在 `b=1→40` 上走 0.0884 → 0.1142，**骑在 0.10 这条线上**，
挑哪一档都能自圆其说；
③ **块长校准（原文 Algorithm 3.1）刻意不做**（它买到的是「选一档 `b`」）·
**预白化 QS 核仍未换**（§5.60 那处偏差还在，但本轮顺带削弱了换它的必要性）。

**什么条件下要重新看**：出现一个 `T` 在**几百量级**的比较（那时原文的小样本警告才真的适用），
或者某个 `p` 真的要进门槛。

---

## 判据变更：配置类候选的承重对照换成「静态等价」，并新增一条次判据（2026-08-27，`ENGINE_VERSION` **不变**，**引擎数字一个没动**）

见[论证 §15](./docs/notes/配置形态-论证.md) 与 [M2 §5.79](./docs/notes/M2-偏差报告.md)。
只动配置形态那条线的判据，**不碰引擎、不碰 `params.ts`、不碰回测与 alpha 的任何口径**。

**① 承重对照：固定权重 1/3 → A₁ 静态等价**（= ERC 在该窗口的**平均权重向量**，固定不变）。
依据：[§14.2](./docs/notes/配置形态-论证.md) 那个混淆 ——
旧对照堵不住「ERC 的优势只是把钱挪去了恰好在牛市里的那条腿」，
而「两窗同向」那道原本用来堵它的防线**实测失效**（两个窗口都在同一段国债牛市里）。
A₁ 按构造把「债多少」固定住（实测两臂平均权重差 ≤ 0.06pp、ρ = 0.94–0.96）⇒ 混淆被**结构性**排除。
⚠ A₁ **事后构造、不可实施** ⇒ 它是归因对照；**A₀ 输了不能推出「静态倾斜可实施」**。

**② 次判据新增**：**回撤不高于对照，且总收益 ≥ 对照的 80%**。
依据是 [§4④](./docs/notes/配置形态-论证.md) 2026-08-27 用户拍板「回撤优先」——
接受「年化少 1–2pp 换回撤降到约三分之一」。
⚠ **那个 80% 沿用 §5 第一次预注册就写着的数**，不是看完实测的 66.2% / 99.8% 之后新定的。

**③ 停止规则的粒度**：本轮不通过 ⇒ 关闭的是**规则**（ERC / 风险平价），**不是配置层** ——
「静态债券倾斜」本身就是一个配置决定，把前者的失败读成后者会砍掉本轮没测的东西。

**结果**（一次报告）：训练 夏普 +0.209 / Calmar +0.166 · 验证 **+0.072 / −0.105**
⇒ **不通过** ⇒ ERC 这条规则关闭。**顺带关掉一条更省事的路**：
`w ∝ 1/σ` 与 ERC 实测无差别（−0.028 / +0.035）⇒ **协方差非对角那一半买到的约等于零**。

**什么条件下这些判据要重新看**：换资产腿（相关性结构变了 ⇒ ③ 那条「协方差没贡献」可能翻）、
或出现一个**事先可定**的债券权重规则（那时 A₁ 的「事后构造」这条限制就不再适用）。

---

## 判据变更：`audit:random` 的默认 `trials` 200 → **2000**（2026-08-27 用户拍板，`ENGINE_VERSION` **不变**，**绩效与 alpha 的点估计一个没动**）

见 [M2 §5.76](./docs/notes/M2-偏差报告.md)。**动的是判据的精度，不是引擎、不是门槛、不是零点。**

**动了什么**：`src/backtest/random-audit.ts` 的 `parse()` 默认 `trials: 200` → `2000`。

**依据**：`pairedMedianWinFraction` = 「多少比例的试验里真实赢过随机」= **试验数上的二项比例**
⇒ `SE = √(p(1−p)/trials)`。实测三个种子的极差：

| `trials` | 出厂基线 seed 1/2/3 | 极差 | 单次耗时 |
|---|---|---|---|
| 200（旧默认） | 8.50 / 14.50 / 11.00 | **6.00pp** | 6 秒 |
| **2000（新默认）** | 10.55 / 12.15 / 11.65 | **1.65pp** | 14 秒 |

⇒ **旧默认的噪音地板（6.00pp）比 [M2 §5.72](./docs/notes/M2-偏差报告.md) 预注册的
「咬得住」门槛（5pp）还大** —— 那一档在自己的预注册口径下**根本不可能被干净地判定**，
§5.73 的裁决「不确定」就是这么来的（最大 Δ 4.75pp）。

**⚠ 为什么这不是移动球门**：`trials` 是**蒙特卡洛复制次数，不是样本量**。
提高它不让数据多说一个字，只把同一个量测得更准。
**估计量、零点定义、判据门槛、以及任何一条已报过的裁决全部一字未改。**

**同步落的三件事**：
- 报告与看板都把本次 `trials` **印在配对胜率旁边**，并印噪音地板（含「每层各自量」：
  `RANGE` 4.90pp · `TRANSITION` 0.25pp）。归档里 `trials < 2000` 的报告会被额外点名 ——
  **一条纪律挡不住这个坑，把数印在同一行才挡得住**（同 §5.74 的处置）；
- 出厂口径的 alpha 基线按新默认重出一份（`random-l2a-train-t2k.json`，零点逐字沿用
  `REGIME_BLOCK` · `--shuffle-spans` · `--block-weight runs` · `seed 1`）。
  `ALL` 中位 **8.50% → 10.55%**，覆盖率 99.64% 与 1097/1097 配对逐位不变；
- 用例钉住默认值与那两行纪律（`tests/unit/backtest/random-audit.test.ts`），
  **验证过它抓得住**：改回 200 或删掉纪律行，立刻变红。

**⚠ L2 条件① 不受影响**：`ALL` 中位 10.55% 距门槛（> 50%）仍差 **39.45pp**，裁决一字不动。
**§5.30/§5.31 那四个出场候选的裁决也一字不动** —— 不许借这次精度提高去重判它们（§5.73 ⑦）。

**什么条件下这个结论失效**：若日后 `trials` 成为耗时瓶颈（例如分层数或建仓数增长一个量级，
单次跑到分钟级），那时要重新权衡 —— 但**降回去同样是一次判据变更，同样要人拍板**。

---

## 读数纪律：配对胜率是效应量的**饱和变换** —— 撤回 §5.73 ④ 并装上闸门（2026-08-27，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

见 [M2 §5.74](./docs/notes/M2-偏差报告.md)。这是一次**自我纠正**：同日写的 §5.73 ④
把 `RANGE` 的 13.1pp 读成「分辨力集中在 RANGE」，**那是错的**。

**机制**：`pairedMedianWinFraction ≈ Φ(μ/σ_D)`（`μ` = 真实中位 − 随机中位）
⇒ 灵敏度 `φ(z)/σ_D` **在 50% 处最大、往 0%/100% 两端塌缩**，而 `σ_D ∝ 1/√n`
⇒ **层越大，同样的效应给出越极端的胜率**。

**定量**（以 `RANGE` 为参照校准，预测增益 ∝ `φ(Φ⁻¹(基线胜率)) × √n`）：

| 层 | n | 基线胜率 | 预测增益 | 实测增益 | 比值 |
|---|---|---|---|---|---|
| RANGE | 639 | 51.4% | 136.9 | 136.9 | 1.00 |
| TREND_UP | 120 | 45.4% | 59.0 | 48.1 | 0.82 |
| **TRANSITION** | 338 | **0.9%** | **5.8** | **6.3** | **1.09** |

⇒ **最极端那一档（24 倍增益差）预测到 9% 以内。** `RANGE` 的 `μ` 只动 +0.09~0.13pp
就让胜率动 12.7pp，而 `TRANSITION` 的 `μ` 动了 −0.19~−0.34pp（**2.7 倍大**）胜率才动 0.6pp。
33 个分层里按胜率排 vs 按 `μ` 排 **Spearman 只有 0.865，最大错位 11 位**
（`ALL` 胜率排 30/33 而 `μ` 只排 19/33 —— 因为它 n 最大）。

**⚠ 影响范围的三分（必须一起读，否则会被当成「判据作废」）**：

1. ✅ **阈值型完全有效** —— `Φ` 单调 ⇒ 「> 50%」≡「μ > 0」，**L2 条件① 一个字不改**；
2. ✅ **符号与排序方向**有效，但排的是**信噪比不是效应量**；
3. ❌ **幅度与跨层的 Δ 无效** —— 撤回的就是这一类。

**动了什么（唯一的生产代码改动）**：`src/backtest/random-audit.ts` 的打散跨度表
**加了一列「效应量 μ」**（数据早就在 JSON 的 `passiveMedianMean`/`randomMedianMean` 里，
只是从没印过差值），并在表下印了饱和纪律与「不受影响的用法」；
`tools/iterate/status.ts` 那张四层表加了同一条（它最常被竖着读），
**并把 μ 接成了一列**（`effectSize`）。`renderText` 因此改为导出，供用例断言。

接上看板那一列之后**立刻纠正了一个正在生效的误读**：只看胜率会以为 `ALL`（中位 **8.50%**）
几乎和 `TRANSITION`（**0.00%**）一样差，而 μ 说 **`TRANSITION` 是 −1.26% 而 `ALL` 只有 −0.36%
（差 3.5 倍）**，`ALL` 其实更靠近 `TREND_UP`（−0.24%）。
⚠ 这张四层表上**排序方向没有倒挂**（两种口径都把 `TRANSITION` 排最差、`RANGE` 最好）
—— 倒挂出现在 33 层那张宽表上。**失真的是间距，不是次序。**
⚠ 看板那一列目前只有**类型层**保护（`effectSize` 必填，删了 typecheck 会红），
没有文案用例 —— `status.ts` 的渲染整体没有测试。

**闸门**：`tests/unit/backtest/random-audit.test.ts` **+5 条**（1845 → 1850）。
**验证过它抓得住** —— 临时删掉纪律行与 μ 列，3 条立刻变红。
**回归自检**：加列之后重跑出厂臂，**33 层的两个配对胜率逐位不变**（只加展示、没改计算）。

**顺带修掉一个我自己引入的坑**：昨天为量噪音地板建的 `random-l2a-train-s2/s3.json`
把看板的 alpha 来源从 seed 1 挤成了 **seed 3** —— 正是 §5.44 那个形状
（看板挑报告只看 mtime + `knobs.deviations`，**对同口径的种子副本没有防线**）。
已重命名为 `seedvar-*` 复原。⚠ 消融臂那一侧的闸门是**有效**的：6 份 `random-abl-*`
被 `deviations` 正确跳过**并点名**。

**什么条件下这个结论失效**：`σ_D` 是**推出来的不是测出来的**（由 `∝1/√n` 推），
模型只解释到 **0.82–1.44×** ⇒ 它是**读数纪律的依据，不是可以拿去算的公式**，
**不许用它「校正」任何已报过的胜率**。

---

## 判据核对：出场类改动的 alpha **裁决「不确定」**，而副产品是「配对胜率的默认精度不够」（2026-08-27，`ENGINE_VERSION` **不变**，**绩效数字一个没动**，**未改一行生产代码**）

预注册 [M2 §5.72](./docs/notes/M2-偏差报告.md)、结果与记分 §5.73。登记项 `alpha-gate-exit-class` 关闭。

**问的是什么**：[docs/07 §3.6a](./docs/07-回测与验证方案.md) 门槛④ 把出场类改动的主判据
**从 `audit:random` 的配对胜率换成了逐次建仓的配对 Δ**，而那个替换**只有结构论证、没有实测**
（`random-*.json` 里一份 `abl-*` 都没有）。四份出场消融各跑一次，零点逐字沿用。

**裁决：预注册三档的中间那档「不确定」**（`ALL` 层 · 中位口径 · `trials=2000`）：

| 臂 | 中位配对胜率 | **Δ** |
|---|---|---|
| 出厂基线 | 10.55% | — |
| `no-drawdown-reduce` | 15.30% | **+4.75pp** |
| `no-trailing-stop` | 14.15% | +3.60pp |
| `no-stop-loss` | 12.95% | +2.40pp |
| `no-profit-protect` | 11.40% | +0.85pp |

「咬不住」要求四臂全部 < 2pp（不成立，三个 ≥ 2.40）；
「咬得住」要求至少一个 ≥ 5pp 且 > 地板 2×（最大 4.75pp，**差 0.25pp**）。
⇒ **门槛没动。** 差 0.25pp 就把 5pp 改成 4.5pp 是这个项目最该防的那件事。

**§3.6a 的决定不变，但依据被改写**：四个 Δ **全为正**、**3/4 超过噪音地板**
⇒ 正确的说法不是「结构上**测不到**」，而是「**测得到、分辨力不足到不能承重、驱动机制不明**」。
（我押的机制也被证伪：Δ 与跨度异质性无关，ΔCV 相关 **−0.033**。）

**⚠ 副产品比裁决更影响日常引用：`--trials` 默认 200 太小。**
`pairedMedianWinFraction` 是**试验数上的二项比例**（`SE = √(p(1−p)/trials)`）：

| `trials` | 出厂基线 `ALL` 中位（seed 1/2/3） | 极差 |
|---|---|---|
| **200**（当前默认） | 8.50 / 14.50 / 11.00 | **6.00pp** |
| **2000** | 10.55 / 12.15 / 11.65 | **1.65pp** |

⇒ **看板与 §5.61 引用的「`ALL` 中位 8.50%」是 `trials=200` 的一次抽样。**
**L2 条件①（> 50%）不受影响**（差门槛 38–41pp），但**引用配对胜率必须带 `trials`**。
分层的地板要各自量（`RANGE` **4.90pp** · `TREND_UP` 1.65pp · `TRANSITION` 0.25pp）。
⚠ **提高默认值是一次判据变更**，已登记 `random-trials-default` 待拍板（单次只要 6/14 秒）。

**⚠ 另一条线索不许拿来翻裁决**：分辨力**集中在 `RANGE`**（Δ 到 **13.1pp** = 2.7× 该层地板），
而 `TREND_UP` / `TRANSITION` **方向相反**并把 `ALL` 稀释掉。
预注册写死「主判据层 = `ALL`，分层不承重」⇒ 这条要**另立预注册**，已登记。

**什么条件下这个结论失效**：① 四份消融报告 `knobs.unverifiable = ["成本口径…"]`
（早于 2026-08-20 加 `meta.costs`）⇒ **无法核对它们与出厂基线用同一套成本**
⇒ **每个臂的绝对 alpha 不许单独引用**（四臂共用同一前提，所以"有没有分辨力"这个问题的形状不受影响）；
② `trials` 再提高一个量级可能让 4.75pp 那个数移动；
③ 本节**不判任何出场候选的好坏** —— §5.30/§5.31 那四个「没过红线」的裁决一字不动。

---

## 归因结论：**那个负 IC 主要是一个 20 日动量暴露**（2026-08-27，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

预注册 [M2 §5.70](./docs/notes/M2-偏差报告.md)、结果与记分 §5.71。
`audit:ic` 加 `--risk-factors`（不给时 payload 与旧报告逐字段相同）。

**方法换了**：从 §5.69 的「分组去均值」换成**秩上的横截面 OLS**
（`neutralizeByRegression`）—— 连续控制只吃 **1 个自由度**，
而分组法在组规模中位 **2** 上已经到顶。分组去均值是「对哑变量回归」的特例，两节同一框架。

**结果**（`positiveOnly` · 10 日 = 主判据，七条臂全部预注册）：

| 控制变量 | 训练 IC | Δ | 验证 IC | Δ |
|---|---|---|---|---|
| 原始 | −4.981%（t −5.38） | — | −0.868% | — |
| 市值 | −5.165% | **−0.184pp** | −1.610% | −0.742pp |
| **动量20** | **−1.434%** | **+3.548pp** | +2.423% | **+3.291pp** |
| **市值 + 动量20（主判据）** | **−1.458%**（t −1.97） | **+3.523pp** | +1.805%（t +1.66） | +2.673pp |

⇒ **结局 B：已知暴露解释了大部分，而贡献几乎全部来自动量20**（市值几乎零，
「小市值暴露」这条在 §5.45 被证伪过一次，这里换方法第二次同向）。
⇒ **§2.1 风险因子归因那一格三层做完了**：行业无贡献且不稳（§5.69）· 市值无贡献 · **动量是主要贡献者**。

**⚠ 四条不许漏**：

1. **剥掉动量之后剩下的约等于零，且符号不稳** —— 训练 −1.458%、验证 **+1.805%**，
   两个 \|t\| 都 < 2 ⇒ **不许说「中性化之后转正了」**。**没有出现结局 C。**
2. **分不开两种解释**（预注册限制 2 事先写着）：「负 IC 是动量的影子」与
   「中性化把得分本身砍掉了」在数据上等价 ⇒ 能说的只是
   「**得分的横截面信息与 20 日动量高度重合**」，**不能说**「引擎没有信息」。
3. **A5（再加行业哑变量）不许承重** —— 24 列 / 残差自由度中位 34，
   20 日点估计甩到 +1.048%，那是自由度不足不是发现（预注册限制 1 事先写着）。
4. **把「中性化掉动量」变成引擎改动是完全独立的一件事**，要走 docs/07 §3.6 的五条门槛。
   §5.71 **不构成**那个候选的论证。

**一件本项目少见的事**：动量20 的 Δ 在两个窗口是 **+3.548 / +3.291pp（差 7%）**
—— §5.51 ③ 早已量出「均值那一侧根本测不动」，而这是第一次看到机制量级跨窗口稳定。
⚠ 稳定的是「**暴露有多大**」，不是「策略有多好」。

**闸门**：`tests/unit/backtest/industry-neutral.test.ts` 增至 **11 条**。
手写 OLS 解错了不报错、只给出另一个数 ⇒ 核心是**不变量③交叉验证**：
两类别的哑变量回归必须**逐位等于**已被 §5.69 用例钉过的分组去均值。
另有「只有截距时 IC 不变」「拿得分自己当控制变量则残差恒 0 ⇒ `days = 0` 而非 IC = 0」
「共线列被丢弃**并计数**」（静默丢列等于悄悄换了一个模型）。

**什么条件下这个结论失效**：① 动量定义写死为 `closeAdj` 的 5 / 20 日收益，**没扫 N** ——
换别的回看期可能给别的数（但 5 与 20 的对比已经显示 20 更强，且两窗一致）；
② 控制变量取当日横截面**秩** ⇒ 对单调变换免疫，但也因此**测不到非单调的暴露**；
③ 它答不了「20 日动量因子本身有没有 alpha」—— 中性化减掉了那一层，不是测量了它。

---

## 判据升级：`audit:ic` 加 `--industry`，行业中性 IC（2026-08-27，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

**这是判据变更不是引擎变更。** 预注册 [M2 §5.68](./docs/notes/M2-偏差报告.md)、结果与记分 §5.69。

**动了什么**：`src/backtest/ic-audit.ts` 加 `--industry <file>` —— 给了才多算两块
（`industryNeutralAll` / `industryNeutralPositiveOnly`）。
**不给时 payload 逐字段与旧报告相同**，既有 `ic-*.json` 不受影响。
中性化写成对 `byDate` 的**纯后处理** ⇒ 原始那一臂走的是同一个 `collect` + `icOf`
（自检：原始臂**逐位复现** §5.46 的 −4.406 / −4.981 / −4.737 与有效日数）。

**口径**：逐日先算全横截面的秩，再在**申万一级**组内减组均值；组规模 < 2 整组丢弃；
拿不到行业的行丢弃、**不并成「其它」组**；行业**按时点取**（`startDate <= 该日`）。
能干净复用 `icOf` 是因为 Spearman 对单调变换不变。

**结果 —— 结局 A：负 IC 活下来了**（训练窗口 `positiveOnly`）：

| 持有期 | 原始 IC | 行业中性 IC | Δ |
|---|---|---|---|
| 10 日（主判据） | −4.981%（t −5.38） | **−4.399%**（t −4.27） | **+0.582pp**（门槛 1pp） |

⇒ §5.46 那个负号是**个股层面**的，不是行业倾斜的副产品。**没有出现结局 C**（转正）。

**⚠ 两条不许漏的限制**：

1. **Δ 的符号在两个窗口相反** —— 验证窗口中性化让 IC **更负**（10 日 −0.765pp、
   20 日 −1.489pp 且 t = −2.43）⇒ **「行业解释了 11.7%」这个数不许引用**。
   能引用的只有「行业中性化之后负 IC 基本不变」。
2. **功效很低** —— 组规模中位只有 **2**（预注册估的是 6–9，估错了：
   `positiveOnly` 丢掉 70.8% 的判定根 ⇒ 每日横截面 **60 只**不是 252）
   ⇒ 结论读成「**在这个粗糙的行业分辨率下看不到行业分量**」，不是「已排除行业」。

**闸门**：`tests/unit/backtest/industry-neutral.test.ts`（5 条）。
核心是**不变量①：全部票同属一个行业时，中性 IC 必须逐位等于原始 IC**
（去掉全横截面常数不改变秩）—— 它能抓住绝大多数实现错误。

**什么条件下这个结论失效**：① 行业分辨率 —— 换成申万二级或换个池子（横截面更宽）
可能看得到行业分量，本轮看不到不等于没有；② 这份表的 PIT 性未独立验证（见上一条）；
③ **它答不了「行业轮动有没有 alpha」** —— 中性化把那一层减掉了，不是测量了它。

⚠ **不重开「个股 · 日线 · 择时」那条研发线**（2026-08-19 拍板）：本轮只读、
不改引擎、不动参数、不新增指标，产出是**归因**不是候选。

---

## 申万行业变迁史落地：`pnpm fetch:industry`（2026-08-27，`ENGINE_VERSION` **不变**，**零新增依赖**，不进引擎）

**这不是引擎变更**，是补一个**数据维度**：行业分类此前只有**当前**归属
（东财 `f127`），拿它回标历史是未来函数 ⇒ 行业中性化与集中度风控一直落空
（[差距文档 §2.7](./docs/notes/与机构量化系统的差距.md)）。
申万这份是唯一**既零鉴权又给历史归属**的源（聚宽那条许可禁止入库分发）。

**新增**：`scripts/fetch-industry.mjs` + `scripts/certs/`（一张公开的中间证书）。
产出 `data/industry/sw-industry-history.json`（**gitignored，可重出**）。

```bash
pnpm fetch:industry                      # 抓取 + 解析 + 落盘
pnpm fetch:industry -- --as-of 2018-01-01  # 顺带打印对我们两个池的覆盖自查
pnpm fetch:industry -- --preview           # 只看表头与前几行，不写文件
```

**实测**：12,903 行 / 5,912 只 / 38 个一级行业 / 最多变更 10 次
—— 与外部工具包 2026-08-19 报的 12,893 / 5,905 / 38 / 10 **独立复现**（差 10 行 = 八天的更新）。
覆盖：两个池 **100% 在表里**；as-of 2018-01-01 存活池 **183/264** 可定位（28 个一级行业），
2023-12-31 是 **254/264**（30 个）。

**两个实现决定**：

1. **零新增依赖** —— 源只有 `.xls`（`.xlsx`/`.csv` 实测 404）且是真 OLE2 复合文档，
   但这份表只用到 `LABELSST` / `NUMBER` / `RK` 三种单元格记录、单个 Sheet
   ⇒ 自己写 CFB + BIFF8 的窄解析器，比一个 xlsx 依赖便宜，也不进运行时
   （`dependencies` 仍是 4 个）。
2. **TLS 补中间证书，不是关校验** —— 服务器只发叶证书、漏了
   `GeoTrust G2 TLS CN RSA4096 SHA256 2022 CA1`（链到 DigiCert Global Root G2，
   有效期至 2032-12-14）⇒ 把它连同系统根传给 undici。
   🛑 **`NODE_TLS_REJECT_UNAUTHORIZED=0` 是错的修法**：它把这一次的「链不全」
   变成永久的「谁都不验」。

**三条使用纪律（写在脚本头注释里）**：

- 🛑 **不许回填进 `security.industry`** —— 那一列是东财 `f127` 的口径，申万是**另一套体系**
  （代码与名称都不通用）。混在一列里，「这只票属于哪个行业」就再也答不准。
  `014_industry_history.sql` 那条**逐日留痕照常继续**，两者不互相替代。
- **它是研究输入不是引擎输入**：不进 `src/core`、不进 `params.ts`、不新增指标（约束 5）。
- **表结构变了要炸**：`COLUMNS` 少一列直接抛错并打印实际表头。
  ⚠ 实际表头只有四列 `股票代码` / `计入日期` / `行业代码` / `更新日期` ——
  **没有名称列**，别去别的源拿名字来拼。

**什么条件下这个结论失效**：① 申万随时会改表头或换 URL（表结构断言会炸，不会静默）；
② 那张中间证书 2032 年过期；③ **这份表的 PIT 性我们没有独立验证过** ——
它给的「计入日期」是不是当年真实的归属日，只有申万自己知道。

---

## 回测 fixture 的 `isST` 恒为 false —— 已量并已修（2026-08-27，`ENGINE_VERSION` **不变**，判定逻辑一个字没动）

**动了什么**：`src/backtest/data.ts` 的 `openFixtureSource` 此前调 `fallbackProfile(code)`
**不传名字** ⇒ `label = code` ⇒ `isSTName(code)` 恒 `false`
⇒ **所有 fixture 标的一律按非 ST（±10%）算涨跌停**。现在读 `_meta.nameAtFetch`。
影响两处而一个修复同时覆盖：`simulate.ts` 的成交层 `limitBlocked` 与
`core/risk/index.ts` 的风控层 `HARD_LIMIT_UP/DOWN`。

**依据**（预注册 [M2 §5.66](./docs/notes/M2-偏差报告.md)，结果与记分 §5.67）：
549 份 fixture 里 **225 份**名称带 ST/退 —— 存活池 11/261（4.2%）、**退市池 212/233（91.0%）**。
四组回测（出厂参数 · 2018-01-01→2026-06-30）：

| | 存活 264 | 退市 236 | 合并 500 | 幸存者偏差 |
|---|---|---|---|---|
| 现状 | −0.1014% | −2.4028% | −1.1869% | −1.0855pp |
| 修后 | −0.1225% | −2.1929% | −1.0990% | −0.9765pp |
| **Δ** | −0.0211pp | **+0.2099pp** | +0.0878pp | **+0.1089pp** |

⇒ 预注册的主判据（任一条腿 \|Δ\| ≥ 0.10pp）达成 ⇒ **修**。

**⚠ 但 §5.26 的 −0.13pp 不变。** 偏差收缩 10.04% ⇒ **−0.1322 → −0.1189pp**，
变化 **0.0133pp**，**小于 §5.26 自己那两个独立估计之间的 0.017pp**
⇒ 取值仍写 −0.13pp，§5.26 一个数没改（只加了指向 §5.67 的指针）。

**⚠ 既有基线不重跑**（存活腿只动 0.0211pp）。但**下一次重跑基线时数字会有微小变化 ——
那是这次修复，不是引擎变了**。

**闸门**：`tests/unit/backtest/data-profile.test.ts`（5 条，**验证过回退修复后会变红**）。
加它的理由：`fallbackProfile` 的原注释承诺「缺时……**并在报告里注明**」，
而报告里从来没有那一行 ⇒ 又一个「写下一条纪律不等于装上一道闸门」。

**什么条件下这个结论失效 / 只修了一半**：`nameAtFetch` 是**抓取时（2026-08）的名字**，
不是逐日状态 ⇒ 「用今天的 ST 状态回标 8 年」**仍是时点错误**（方向不定）。
真修法是 baostock 的逐日 `isST`，等一个装得上 pandas 的 Python，已登记 `pit-isst-daily`。

⚠ **一处措辞纠正**：预注册写「偏差方向确定**偏乐观**」—— 那**只对「执行许可」成立，不对「收益」**。
被放行的委托在退市池上是亏钱的（涨停追高，§5.45 中位配对胜率 0.5%）
⇒ 旧数字在那条腿上其实**偏悲观**。两者不矛盾，但不能互相替代。

---

## 复权口径第一次被第二源核过（2026-08-27，`ENGINE_VERSION` **不变**，**绩效数字一个没动**，只新增一个只读工具）

**这不是引擎变更，记在这里是因为它动了「回测数字可不可信」这个前提。**
学习任务（外部工具包 [`a-stock-data`](https://github.com/simonlin1212/a-stock-data) 对照），
全部数字与预测记分见 [M2 §5.65](./docs/notes/M2-偏差报告.md)。

**新增**：`scripts/verify/adjust-crosscheck.ts`（只读，不改 fixture、不改引擎、不进 `params.ts`）。

```bash
npx tsx scripts/verify/adjust-crosscheck.ts --stride 5 --limit 120
```

**背景**：[信源台账 §1](./docs/notes/信源台账.md) 写着腾讯 `fqkline` **是 `fetch:history` 的唯一源**
—— 549 份 fixture 的 `*Adj` 列、进而**全部回测收益**都只有这一个出处，而它从没被核对过。
新浪复权**因子**（`finance.sina.com.cn/realstock/company/{sym}/hfq.js`，≈1.8KB、零鉴权）
第一次让这件事做得了。

| 组 | 只数 | 事件数 | 逐次除权日收益差 中位 | **全窗累计漂移 中位** |
|---|---|---|---|---|
| **存活** | 58 | 373 | **0.067pp**（max 1.30） | **2.39%** |
| **退市** | 21 | 52 | 0.050pp（max 0.71） | **38.12%** |

**结论两半，必须一起读**：

1. ✅ **存活股的回测输入拿到外部背书** —— 两家都正确抹掉了除权跳空
   （中远海控 2021-07-14：不复权 −25.00%，腾讯 −2.46% / 新浪 −2.50%）。
2. ⚠ **退市股两家大幅分歧**（漂移差 **16 倍**，分歧 top-10 **10/10 是退市股**，
   抽查水平差达 **11×**）⇒ 与台账已记的「退市整理期尾段后复权是加性的」**同源但范围更大**：
   原以为只是尾部几根，实为整条序列的水平。
   ⇒ **幸存者偏差 −0.13pp（M2 §5.26）从「已解决」降为「有一条未量化的口径风险」**。

**什么条件下这个结论失效**：① 两家都是**非官方源**，本工具只报「一致到什么程度」，
**判不出谁对** —— 退市股那一格要第三个源才能结（已登记 `delisted-adjust-third-source`）；
② 存活组那个 0.067pp 是**除权日当天**的收益差，它**不保证**非除权日的水平一致
（累计漂移中位 2.39% 就是那部分）；③ 新浪对**北交所**无因子文件（外部实测 404，我们未复测）。

⚠ **不许据此重跑或重判任何既有报告** —— 这一轮**没有推翻**任何绩效数字，
只是给它们加了一条已知的口径风险。

**顺带订正两条既有结论**（详见 M2 §5.65 与台账更新记录）：
`SZ001330`/`SZ301583`/`SZ301717` 没有 `hfqday` **不是「源的上限」而是「一次除权都没有过」**
（因子恒 1.0）· **BaoStock「通路已断」判错了**（真主机 `public-api.baostock.com:10030`
实测 TCP 通，此前用的是**猜的**主机名；仍不接，但理由从「源没了」换成「要外挂 Python」）。

---

## 成交记账补上真实时刻与提醒关联（2026-08-26，schema **15 → 16**，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

登记条目 `trade-decision-link` 做掉，**判据是「拖不起」不是「最有价值」**：
`trade_log` 那 20 行的 `traded_at` 存的是**本机 12:00**、且没有 `signal_id`
⇒ 「几点几分成交的」「照哪条提醒做的」**已经丢的补不回来，每多交易一天就多丢一天**，
而队列上其余每一项延迟的代价都是零。

**动了什么**（`016_trade_decision.sql`，四列全部可空）：

| 列 | 是什么 | 纪律 |
|---|---|---|
| `traded_at_exact` | 真实成交时刻（含分钟） | **NULL = 不知道**，不许拿 `traded_at` 顶替 —— 那是把「不记得」写成「中午 12 点成交」 |
| `signal_id` | 照哪条提醒做的 | **用户手动指认，程序不猜**（自动关联会造出一批看起来有依据、实际是猜的链接） |
| `decision_at` / `decision_price` | 决策时刻与决策价的**冗余快照** | 因为**不加外键**（`signal` 按 2 年裁剪而账本永不裁剪，先例 `008_ai_explain`），所以要把 IS 需要的两个数存下来 |

配套：`shared/time.ts` 新增 `shanghaiMsFrom`，把成交日期的口径从**本机**换成**北京**
—— 顺带修掉「极西时区上 T+1 卖出锁定多锁一天」那条老坑（同一个字段，条目点名一起改）。
关联的判据抽成纯函数 `trades/ledger.ts` 的 `resolveDecision`（查不到 / code 不一致
一律**报错**，绝不静默落 NULL；快照一律取库里的，渲染层送来的不采信）。
表单加可选时刻框 + 「刚刚成交」按钮 + 「照哪条提醒做的？」下拉
（默认未关联；**当天恰好只有一条时高亮并给一键关联，但不预选** —— 预选等于把巧合当因果）。

**三条别读错**：

1. **真实那一侧的 `C_i`（冲击）不会因此变得可测**，别把 016 读成「IS 四项齐了」。
   拆 `C_d`/`C_i` 需要**下单时价**（arrival price），而用户不会记录
   「几点挂的单 vs 几点成交」。016 给到的是另外两样：**决策 → 成交按分钟对齐**、
   以及**样本不再静默丢**（§5.53 实测 13:04 的信号 vs 假时刻 13:00，**差 4 分钟**整对配不上）。
2. **`C_o` 仍算不出数，但卡的东西换了** —— 不再是缺字段，而是**缺带关联的成交**。
3. **旧的 20 行四列全 NULL，不猜不回填**（迁移期无从得知写入方的时区）。
   `impl-shortfall.ts` 每次都印覆盖率，现在是 **0/13** —— **那不是故障，是它该显示的样子**。

**这条不会让策略变好。** L1 的另一半是时间（真机 4 笔卖出、`shadow_trade` 0 行）；
工程先就位，是为了不浪费接下来的成交。

---

## 判据变更：L2 条件① 从「训练窗口 Calmar > 0」换成「训练窗口中位配对胜率 > 50%」（2026-08-26，`ENGINE_VERSION` **不变**，**引擎与绩效数字一个没动**）

**只动判据，不动引擎。** 用户拍板。决策过程见
[2026-08-26 决策记录](./docs/notes/2026-08-26-决策记录.md)，**新口径本身**写在
[迭代 skill](./.claude/skills/迭代/SKILL.md) 的「L2 条件① 的口径」一节（阶梯表所在地），
数字与预测记分在 [M2 §5.61](./docs/notes/M2-偏差报告.md)。

**动了什么**：条件① 换成「训练窗口（2018-01-01 → 2023-12-31）`ALL` 层
**中位配对胜率 > 50%**」，零点预承诺为 `REGIME_BLOCK`
（`--match-regime --shuffle-spans --block-weight runs`，seed 1，trials 200）。

**依据（两条都与「个股能不能过线」无关 —— 这是它与移动球门的分界线）**：

1. **旧口径可被「少做」机械满足**：回撤只有 2.96% ⇒ `Calmar > 0` 等价于「总收益转正」，
   而实测**砍掉 `TREND_UP` 一层就够**（全期 207 次 / −170,680 元 / 占建仓 12.4%，
   连 §3.6 门槛② 的 15% 都不触）。一个砍一刀就能通过的门槛不构成门槛。
2. **与读数纪律第 1 条冲突**：「判据是 alpha 配对胜率，不是绩效」，而旧口径用的是绩效。

**去掉绩效那一半为什么安全**：**条件③（池外参照 GH1）已经承担它**，
且 GH1 是**同风险匹配**的 ⇒ 收窄会同时压低参照暴露 ⇒ 对「少做」免疫。
①判 alpha、③判绝对，两条都绕不过。⇒ [困境说明 Q3](./docs/notes/困境说明-2026-08-17.md) 结案。

**当天就跑了它**（§5.61，基线**逐位复现** −1.99%，且新基线带 `meta.costs`/`meta.pool` ⇒ 口径可核对）：
**`ALL` 中位配对胜率 8.5%，离门槛差 41.5pp ⇒ 不通过。**
而**四个最大层自己全都 ≤ 45%**（RANGE 45.0 · R3+R4 44.5 · TREND_UP 37.5 · TRANSITION 0.0）
⇒ **没有任何收窄能救** —— 这正是换判据要换来的东西。
顺带定位到一件事：「全池唯一赚钱的引擎块」`R3+R4` 全期 +189,632 元，
**训练窗口只有 −16,093 元** ⇒ 那笔钱落在训练窗口之外。

**什么条件下这个结论失效**：① **不动 `calibrate.ts` 的红线**（它防的是「训练亏钱、验证赚钱」
那类候选，§5.14 有实例）⇒ **21 个 `BLOCKED` 仍标不了，条件② 不受影响，这次改动不解锁标定**；
② **不重判任何已报过的实验** —— §5.29/§5.30/§5.31 那些「主判据 Calmar > 0 不通过」的裁决
按当时判据成立，原样保留；③ 报告里有五个**窄分层**中位 > 50%（32–301 次建仓），
**一条都不许拿去立候选**（§3.11.4 第 2 条 · 窄层跨零点极差 20–30pp · 从 33 层里挑最高的
就是在同一份数据上做选择）。

---

## 判据变更：两条**相关**曲线的夏普之差有标准误了（2026-08-26，`ENGINE_VERSION` **不变**，**引擎与绩效数字一个没动**）

**只动判据，不动引擎。** 来自一次[学习任务 · A 道](./.claude/skills/迭代/SKILL.md)，
全部数字与预测记分在 [M2 §5.60](./docs/notes/M2-偏差报告.md)。

**动了什么**：

1. `src/backtest/metrics.ts` 新增 **`sharpeDiffHac`** —— **Ledoit & Wolf (2008)**,
   *Robust performance hypothesis testing with the Sharpe ratio*, **J. Empirical Finance
   15(4) 850–859** 的 Eq. (2)/(4)/(5)（原文 PDF 一手核对）。归属链是
   **Jobson & Korkie (1981) → Memmel (2003) → Ledoit & Wolf (2008)**，
   与单条曲线那条（Lo 2002 / **Mertens** 2002）**不是同一条** ——
   两篇的作者名差一个字母，混起来会以为 §5.50 已经做过了。
2. `scripts/verify/sharpe-diff.ts` 新增：自检 ①（解析 `Ψ` 上逐位复现 Memmel 闭式）
   + 自检 ②（定种子样本走完整数据路径）+ 两个 `ρ` 差很远的配对。
3. `scripts/verify/vol-insurance.ts` 判据 B 的夏普那半条**从「没有标准误」变成有数**：
   Δ 0.183 **± 0.147**、ρ = 0.927、z = 1.247 ⇒ **不显著**。
   ⚠ **A/B 的裁决一个字没变**（A 极差 14.50 倍 · B 拖累 16.50% vs 门槛 3%）。

**依据**：朴素合成 SE（两条单腿平方相加）隐含 `Cov = 0`，实测高估
**1.35 倍**（策略 vs 基准，ρ = 0.423）到 **3.5 倍**（目标化 vs 满仓，ρ = 0.92–0.93）
⇒ 它会把**测得出的差别判成测不出**，方向**不保守**。

**顺带证伪两件事**：① 网上流传的两种 Memmel 闭式**都是错的**
（`−½(1+ρ²)·SR_a·SR_b` 那版差 0.03–0.12%，**小到看不见** · 另一版在 ρ = 0.92 上差 **11.4 倍**）
⇒ 要落文档的公式必须一手核对；② 「**beta 小 ⇒ 相关小**」是错的 ——
3.50% 占用、beta 0.0205 的策略与基准 ρ 仍有 **0.423**（ρ 是尺度不变的）。

**什么条件下这个结论失效**：① 它**不进任何写回门槛**（同 DSR）——
门槛判逐折配对 Δ，这个判单条曲线之差，不是同一个问题；
② **不推翻 §5.51 ③** 那张「均值那一侧测不动」的表（那里两腿是**不相交子段** ⇒ 独立
⇒ 合成口径本来就对）；③ LW 原文说 **HAC 推断在中小样本上偏自由**
⇒ `p` 刚过 0.05 的不许承重，真要承重得上原文的循环块自举（已登记 `sharpe-diff-bootstrap`）。

---

## 判据变更：`docs/07 §3.6a`「出场类改动」的门槛立起来了（2026-08-25，`ENGINE_VERSION` **不变**，**引擎与绩效数字一个没动**）

**只动判据，不动引擎。** 08-24 那条（下面一节）查出「②③ 对出场类改动结构上不设约束」
并登记成 `gate-exit-class`，本轮把它从散文装成闸门 —— 这正是
「**写下一条纪律不等于装上一道闸门**」那条教训的兑现（同形状的三次：`auditKnobs`、
§5.56 的 `persist()`、这一条）。

**动了什么**（[docs/07 §3.6a](./docs/07-回测与验证方案.md)，新增一节）：

1. **先分类再选门槛**，判据是两个数而不是命名：与基线**共有的建仓**占比
   （出场类实测 **97.99–99.27%**）与共有建仓里 **`pnl` 变了的比例**（**34.80–58.38%**）。
   混合类按两套门槛**取严**。
2. **② 照报但不是门槛**（结构上自动通过：保留率 97.99–99.27% vs 线 85%），
   它的作用变成「证明这确实是出场类改动」。
3. **③ 换成「每次建仓的期望不降」**（建仓级胜率 × 盈亏比并排报）。
   依据是 §5.31 实测的搬家现象：关回撤减仓胜率 **+7.9pp** 而盈亏比 **1.11 → 0.91**，乘积仍为负。
   ⚠ **不是新阈值**（约束 2）：「不降」的参照是同窗口基线自己那个数。
4. **主判据换成逐次建仓的配对 Δ**（按 `(code, entryDate)` 配 `pnl`，标准误按时间片聚类 CR1，
   门槛沿用 `|Δ|/stderr ≥ 2`）。§3.6 排除配对 Δ 的理由是「逻辑改动会改变交易的集合」，
   而**出场类改动恰好不改变它** ⇒ 前提成立。**工具还不存在**，已登记 `exit-paired-delta`。
5. **同一节把 `F1` 与「缺召回门槛」两个已否结论连数字写死**（0.6034 → 0.6751；
   `②+③ ⇒ recall ≥ 0.85`）。

**另加一条程序纪律（§3.6）：验收报告必须与基线同窗口。**
先例是 `t3fix.json` 跑的是全期 ⇒ §5.24 那次改动的门槛②③ 事后一次都复算不出来
（[M2 §5.57 ⑤](./docs/notes/M2-偏差报告.md)）。想看全期就跑两份。

**什么条件下这些结论失效**：① 若某个出场类候选的**建仓集合真的变了**
（共有占比掉出 97.99–99.27% 那个区间很远），则第 4 条那个「前提成立」不再成立，
配对 Δ 要退回 `audit:random`；② `audit:random` 的配对胜率对这一类咬不咬得住**从未验证**
（`random-*.json` 里没有一份 `abl-*`）—— 已登记 `alpha-gate-exit-class`，
**在它有答案之前既不许假设它咬得住、也不许假设它不咬**；
③ 本轮**没有重判任何已报实验**（§5.30/§5.31 的六个候选仍然是「没过红线」，
新门槛只作用于**下一次**出场类改动）。

---

## 判据决定：`F1` 不进任何门槛；`docs/07 §3.6` 的门槛只对「入场过滤」类改动有效（2026-08-24，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

**这是判据侧的结论，引擎与门槛本轮都一个字没动。** 依据是
[M2 §5.57](./docs/notes/M2-偏差报告.md)（学习任务 A 道，预注册在先，
工具 `npx tsx scripts/verify/meta-label.ts`，只读 7 份已有报告、不跑模拟）。

**① `F1` 判「不能用」，连数字一起记住**：把 meta-labeling 的度量
（López de Prado, *AFML* 第 3 章）映射过来 —— **③ 就是 precision**、**② 就是保留率** ——
然后 `F1` 在 `abl-no-drawdown-reduce`（**已被 §5.30/§5.31 判为没过红线**）上
从基线 **0.6034** 升到 **0.6751**。原因是 `F1` 内嵌 1:1 的错误代价、且完全不看真阴。
⇒ 与 [§5.41](./docs/notes/M2-偏差报告.md) 否掉 CAPM alpha / 日胜率同一档：**不许进门槛**。

**② 门槛②③ 对「出场类改动」结构上不设约束**（可落地，已登记 `gate-exit-class`）：
出场改动几乎不改变建仓集合 —— 实测保留率 **97.99% ~ 99.27%**（门槛线 85%）⇒ ② 咬不住；
而 ③ 只看胜率，能被「拿盈亏比换胜率」机械满足（§5.31：关回撤减仓胜率 **+7.9pp**、
盈亏比 **1.11 → 0.91**）。实测 **4 / 6** 个候选同时过②③，其中 2 个胜率 **51.07%**
还越过了「纯过滤」下的算术天花板 `P₀/0.85 = 50.83%` —— 越过本身就证明它们不是过滤。
⇒ ③ 自带的那句警告说的正是这件事，**但它是散文不是闸门**。

**③ 顺带否掉一个我们自己的猜测**：「§3.6 缺一条召回门槛」**不成立** ——
`P₁ ≥ P₀`（③）且 `N₁/N₀ ≥ 0.85`（②）在纯过滤下**已经隐含 `recall ≥ 0.85`**。

**什么条件下这些结论失效**：① 若日后给 `F1` 换成代价敏感的加权版（`F_β` 或直接用赔付矩阵），
那是**另一个度量**，要重新判；② ② 的天花板 `×1.176` 是**单次改动的速率上限不是墙**
（每次改动都重定基线，两次连做可到 ×1.383）—— **不许写成「胜率永远上不去 51%」**；
③ 那条恒等式只在**纯过滤**下成立，本轮六个候选**六个都不是**（共有建仓里
34.8%–58.4% 的 `pnl` 变了），非纯过滤时「推导 recall」是个没有含义的数（实测三次 > 100%）。

**本轮不做什么**：不引入任何副模型（§2.5 自己写着必须克制到「一条规则」——
1675 次建仓训练分类器是过拟合机器）· 不改 §3.6 的任何一条 · 不重判 §5.24 的采纳结论。

---

## 修掉 `signal` 落库去重的一个静默失效（2026-08-24，`ENGINE_VERSION` **不变**，**判定一个字没改**）

**动了什么**：`persist()` 里「`direction === NONE` 且无风控裁决」那条早退分支
**不再覆盖** `persistedSignature`（`src/main/engine/signals.ts`）。删掉一行 `set`。

**依据**：[M2 §5.56](./docs/notes/M2-偏差报告.md)（预注册在先，工具
`npx tsx scripts/verify/signal-dedup.ts`，真机库只读）。签名修复（08-14 晚）**之后**仍有
**206 / 408** 个相邻对签名**逐字段相同**（`stage` 事后改写的上界只有 38）。
而 SAME 对的间隔中位 **153s**（≥ 5 轮 tick）⇒ 不是「每轮都插」，
而是**中间有一轮算出了别的签名、把去重记忆擦掉了，且那一轮自己不落行** ——
`persist()` 里能这么干的分支只有那一个。
于是「有信号 → 什么都没发生 → 同一个信号」每次**回来**都落一行新的。
实例 `SH601933` 2026-08-21：73 行、全天只有 **3** 种签名、`score` / `price_at` 逐行相同。

**效果**：按观测到的签名流回放，修复后那 446 行 ⇒ **240 行（−46%）**，`SH601933` 那组 **73 → 3**。
⇒ `signal:history` 那道 `perCode` 闸门的压力随之降低（**但它留着**，100 只自选仍会压到边缘）。

**不是什么**：判定逻辑一个字没改、`params` 没动、`ENGINE_VERSION` 不变、
回测与影子的数字一个没动（`signal` 表不进回测也不进影子）。
**也不许顺手把 `subs` 从签名里移出去** —— 它占可见变化的 66.8%，但那些行**本来就该落**
（2026-08-14 把子信号集合加进签名，正是为了不让 `signal.evidence` 变陈旧）。

**什么条件下这个结论失效**：如果日后给那条早退分支**加上**「NONE 也要落行」的需求
（例如要审计「今天什么都没发生」），去重记忆就需要**两个槽**（上次已落库的 / 上次见过的），
而不是把它们合成一个 —— 合成一个就是这次的缺陷。

**回归**：`tests/unit/main/signals.test.ts` 的「A → 什么都没发生 → A：只落一行」。
它必须写成「**同一个引擎实例、中途换行情**」——这个缺陷在 `signalSignature()` 里看不见
（那个纯函数一直是对的、有用例钉着），坏的是**用它的状态机**，而状态只存在于跨轮的 Map 里。
已验证：把那行 `set` 放回去，这条用例变红（`expected 2 to be 1`），其余 40 条照过。

---

## 三道拍板题结案（2026-08-24，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

三条都是**判据/口径**决定，用户逐条拍的。共同点：它们各自关掉一个**能翻符号或没有终点**的自由度。

**① 「前向记录攒够时间」这条判据 → 选 A：改判据的用途**（[计划 §4.11](./docs/notes/下一阶段取舍与迭代计划.md)）。
前向记录**不再用于统计显著性**（MinTRL：夏普 0.5 要 11 年，而影子有 5 个净值点），
改去回答「有没有结构性失效 / 执行滑移多大 / 工程跑没跑对」。
⇒ **ETF 那条路从此没有任何统计判据，而这是明知代价后选的**，不是遗漏。
**失效条件**：出现效应量大得多的候选（夏普 ≥ 0.8 ⇒ 4.4 年），或用户撤销 08-19 那个决定 ——
两者都是**新事实**，不是重新解释旧数据。**不许**日后把置信度调松来换一个「够了」（那是没选的 B）。

**② IS 的 `C_o`（机会成本）→ 选 ②：只算 L3 强制类**（口径写死在
[`ledger.ts`](./src/main/trades/ledger.ts) 头注释）。
只有持仓强制类（止损那种）没执行才折成金额；普通买入建议**只报计数不折金额**。
理由：全算等于假设「提醒即应执行」，而**故意不动手是 L0 设计里人该有的权力** ——
把正确的克制记成成本会让指标推着系统去追依从率，那是 L4 的语义。
**失效条件**：升到 L3（订单草稿）之后「没执行」的含义变了，这条要重新拍。
⚠ 它**今天还算不出数**（缺真实成交时刻与 `signal_id` 关联）。

**③ GH1（同风险的被动持有）→ 进报告 · 按 σ 匹配**。
实现 `metrics.sameRiskPassive`（**单一出处**，`outside-pool.ts` 改成复用并逐位自检 ⇒ 差 `0.00e+0`），
报告里**紧贴除法版超额**打印 —— 因为两者**符号相反且两句都对**
（训练窗口 +16.75pp vs **−1.71pp**；小样本实例：超额 +2.37% 而 GH1 −4.26%）。
**占用匹配没选**（它在单指数那份上给 +0.72pp，与 σ 匹配的 −16.40pp 符号相反）⇒
只留在调研工具里，**不许日后在报告里换过去**。
**失效条件**：`alignedReturns` 的严格配对被改动，或有人给现金计息（参照假设 0 息）。
新增用例三条（σ 权重 · 恒定权重复利 ≠ 线性近似 · 缺基准给 null 不给 0）。

**绩效影响**：无。`sameRiskPassive` 只是多算一个派生量，净值、收益、回撤、夏普、Calmar 一个都没动
⇒ 既有基线仍可逐份横比。

---

## 判据边界：**成交量硬上限替代不了凸成本模型**（2026-08-24，`ENGINE_VERSION` **不变**，**代码零改动**）

**做了什么**：上一条留下的缺口是「换成什么形态」。去读了外部成熟平台的答案
（聚宽 API 文档 `help/api/help?name=api`，公开页 + `--render`；当日重取与 08-19 的副本
**逐字节相同** 299,408 B ⇒ 按一手原文计），然后在**同一批** 1097 次建仓上量了它能不能替代
（`npx tsx scripts/verify/volume-cap.ts`，只读，**没跑任何模拟**，`--touch-test` 仍是 5）。
详见 [M2 §5.55](./docs/notes/M2-偏差报告.md)。

**外部形态是什么**：聚宽**没有**凸成本模型 —— `FixedSlippage` / `PriceRelatedSlippage` /
`StepRelatedSlippage` 三种滑点**全部与成交量无关**（原文写明「我们认为您的落单的多少
**并不会影响**您最后的成交价格」），大单靠 `set_option('order_volume_ratio', value)`
这个**成交量硬上限**兜，而它**默认 1.0 = 不设限**。

**依据**（覆盖率 100%，1097/1097）：

- **两者几乎正交**：按冲击排序与按参与率排序的 top-50 **Jaccard 仅 0.176**
  （top-100 是 0.205 ⇒ 对 `N` 不敏感）。冲击名单 `σ` 中位 **2.69%** vs 参与率名单 **1.25%**
  ⇒ **凸成本抓波动，硬上限抓流动性**。冲击最大那笔（`SH600744` 44.45 bp）
  在参与率排序里排 **311/1097**，任何 `ratio` 都抓不到。
- **门槛差 10–30 倍**：凸成本 `k*` 中位 1.9–5.2×；最严的 `ratio=0.10` 要到 `m*` 中位 **55×**
  ⇒ **出厂资金下三档 `ratio` 的截断次数全是 0**，加了是恒等变换。

**动了什么**：**什么都没动。** 不加成交量上限、不改成本模型、不进报告。
唯一的变化是[差距文档 §2.6](./docs/notes/与机构量化系统的差距.md) 那条条目
`convex-cost-before-scaling` 从「该做」加强为「**没有替代路线，必须真做**」（判据字符串不动）。
工程上只把 §5.54 与本轮共用的建仓载入层抽成 `scripts/verify/entries.ts`
（两个名单必须落在同一批建仓上）—— 抽完 `impact.ts` 的输出**逐字节未变**，已 diff 验证。

**什么条件下这个结论失效**：① 资金放大到 **≥ 10×**（那时硬上限才第一次触发，
它的正确定位是**补充不是替代** —— 无自由参数，管的是「物理上下不下得去」）；
② 若日后把一次建仓拆成多笔下单，聚宽原文自己说了 `ratio` 是**每单**约束、多次下单可绕过；
③ `σ` 与参与率的负相关（−0.4663）是这个结论的全部机制，换池换窗口要重算。

**⚠ 预测 5/5 全中，而这不是好消息**：五条都是「已知那个 −0.4663」的机械推论
⇒ **这一轮没什么可赌的不确定性**。记分的价值在「错在哪个方向」（对照 §5.54 的 6 错 3、
§5.50 的「把 HAC 威力估大了」），不在正确率。

---

## 判据边界：常数比例滑点在 **2–7×** 资金上失效（2026-08-24，`ENGINE_VERSION` **不变**，**`DEFAULT_COSTS` 一个字没改**）

**做了什么**：把平方根冲击律 `I = Y·σ_D·√(Q/V_D)` 套在既有基线的 1097 次建仓上
（`npx tsx scripts/verify/impact.ts`，只读 `reports/calib/cap-100000.json` + fixture，
**没跑任何模拟**，`--touch-test` 仍是 5）。
归属：Loeb (1983) → **Torre & Ferrari (1997)**, *Market Impact Model Handbook*, BARRA →
Grinold & Kahn (1999/2000) → **Almgren, Thum, Hauptmann & Li (2005)**, *Risk* 18(7) 58–62。
详见 [M2 §5.54](./docs/notes/M2-偏差报告.md)。

**依据**：覆盖率 100%（1097/1097）。保守口径 `Y=1`：中位冲击 **7.33 bp = 出厂那条
10 bp 滑点线的 73%**、**27.6% 的建仓已超线**、`k*` 中位 **1.9×**；
外部锚定 `Y=0.6`（从原文 Table 3 反解）给 `k*` **5.2×**，`Y=1`·exp 0.6 给 **4.8×**
⇒ 可引用的门槛是**区间 2–7×、中心 ≈ 5×**。

**动了什么**：**成本模型一个字没动**，报告里也没加任何冲击列。改的只有判据边界：
[差距文档 §2.6](./docs/notes/与机构量化系统的差距.md) 那句「对 3.5% 占用的小资金影响有限」
**被推翻**（它只在 `Y ≤ 0.5` 那档下成立），并登记成条目
`convex-cost-before-scaling`（等条件：真要放大单标的资金或做 §5.44 候选 B 时）。

**顺带查出一条耦合**：候选 B「按真实价定手数」干的事恰好是把 `Q` 从「模拟实际」拉到
「名义 10 万」，实测这一步把冲击中位 **4.09 → 7.33 bp**、超线占比 **8.7% → 27.6%**
⇒ **要做 B 就得同时换成本模型**。这两件事此前是两条独立记录。

**什么条件下这个结论失效**：
① `Y` 的文献取值 0.34–1.0、指数 0.45–0.6 ⇒ 水平约 **3× 不确定度**，
所以它**只能当资金量级门槛**，不进任何写回门槛、不许重判已报绩效
（点名 §5.44 —— 而且成本被低估只会让 5× 那份更差，方向对它的「不做」结论有利）；
② 外部锚来自 **美国大盘股**、且是从 10% 参与率外推到我们的 0.18%（跨 2.7 个数量级）；
③ 若主池的流动性结构变了（换池、换窗口），`ADV_20` 中位 5453 万元这个输入要重算。

---

## 判据基建：四条就绪项做掉（2026-08-23，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

四条都是**给下一轮学习清边界条件**的，不产生新知识 -- 但它们各自堵住一个已经咬过人的口子。

**① 报告认得出「预热吃掉了窗口」**（`src/backtest/report.ts`）。
判据是**首个净值变动日占全长的比例**：> 50% 弱告警（评估期偏短）· > 80% 强告警（绩效数字不可用）。
起因是 §5.52：`abl-valid-base`（`--from 2024-01-01` 无段前历史）18 个月里
**前 15 个月净值一动不动**、建仓只有 34 次 -- 而 CLAUDE.md 那条
「跑验证窗口必须带段前历史」里写的「只剩 34 次建仓」**就是拿它写的**，
一天之后我又把它挑回来用了两次。
⚠ **纪律在文档里，但没有任何东西挡住加载它的产物** -- 这是第三次同形状（§5.44 那条）。
实测：那个窗口现在打「⚠ **预热占窗口 100%**（**整段净值一次都没动过**）」；
而出厂口径的 300 根预热 + 1157 根评估（20%）**不告警**，不误报。

**② 报告印「本窗口的显著性门槛」与 HAC 标准误**（`sharpeThreshold` / `sharpeSeHac`）。
门槛解 `PSR(SR,0,T,γ₃,γ₄) = 0.95` 的正根（`SR` 的二次方程），
标准误走 Lo (2002) 的 `V_GMM`、滞后阶用 Andrews 规则（**预承诺，不挑**）。
**两个都与策略无关、都调不动**，⚠ **只印不当门槛** -- 写回门槛判的是逐折配对 Δ（§5.48 判据 1）。
印它是因为「这个窗口本来就短到测不出任何东西」是读绩效**之前**该知道的事。
老报告没这两个字段 ⇒ `undefined` ⇒ 整行不印（不用 0 或「—」冒充）。

**③ `audit:ic` 落逐日 IC 序列**（`IcResult.dailyIc`）。§5.51 把平稳性压着的六处逐处判了一遍，
**唯一没测到的恰好是唯一承重主判据的那一处** -- rank IC 的 NW `t`，因为报告只落聚合量。
⚠ **落盘之后不要顺手重算 §5.47 的 `t`**（移动球门）；新序列的用途是**下一次预注册**里测它的平稳性。

**④ `scripts/` 进 `tsconfig.node.json`**。此前 `pnpm typecheck` 覆盖不到 `scripts/verify/*.ts`
（每轮学习都新写一个）。接上之后**当场查出一个真实类型错误**：
`jq-crosscheck.ts` 给 `dmi()` 传的 `PriceSeries` 缺 `open`/`volume` 两个键 -- 已修。

**顺带修一处自己的登记错误**：`signal-dedup-regression` 的判据串 `去重没生效`
**就出现在登记它的那段散文里** ⇒ 登记当天就被判「已落地」。
登记时我只查了 `src/` 没查 `docs/`。判据已换成只有结论才会出现的串。
⇒ **「登记时确认判据现在还不存在」要在全仓查，不是只查代码。**

测试 **+6**（1761 → **1767**），typecheck / lint 全绿。

---

## 静默缺陷修复 + 新增行业留痕（2026-08-22，`ENGINE_VERSION` **不变**，schema **13 → 14**）

**背景**：用户 2026-08-22 拍板「**先把免费的榨干**，需要每天累积的进每天的迭代任务」。
四条免费信源里**只有行业分类会衰减** —— 数据源只给**当前**行业名，
拿它回标历史是**未来函数** ⇒ 历史只能从接上的那天起一天天攒，**其余三条什么时候补都能补齐**。

**⚠ 查下来它不是「还没做」，是一个静默缺陷。** 逐段核过，链路每一节都在：
东财 `PROFILE_FIELDS` 含 `f127` ✅ 在取 · `refreshProfiles()` ✅ 已挂在休市维护
（`meta.profile_refreshed_at` 真机 08-19 11:35 跑过）· 写库 ✅
`INSERT … ON CONFLICT … industry = COALESCE(excluded.industry, watchlist.industry)`。
**而真机 `watchlist.industry` 仍然 79/79 全空。**

**根因：降级链把这个字段吃掉了。** `provider_health` 里 **13 次
`profile …: other side closed` 全是 eastmoney**（它记录在案的间歇性症状），
而每次紧跟一条 **tencent 成功** —— 腾讯 `fetchProfile` 明写着「industry / listedAt 不提供」。
⇒ 拿到一个**有名字、没行业**的 profile → `fromProvider = true` → 刷新报「成功」
→ 行业永远为空。**系统在按设计工作，而少的那一块没有任何人看得见。**

**修法是按字段重试，不是把腾讯判成失败**（那会连名字一起丢，且与
`fetchMinutes`/`fetchAnnouncements` 那条「空结果不算失败」的纪律冲突 ——
方向相反但同一类：拿整体成败去判一个字段）。三条边界各有用例钉着：
① 只在 `degraded === true` 时重试（主源自己给空说明它就是没有）·
② 只在**存量也没有**时重试（拿到过一次就被 `COALESCE` 保住，ETF 这类不会每轮多打）·
③ 只重试**一次**。

**新增 `industry_history`（014）**：一行一次**变化**不是一行一天
（79 只 × 一年 ≈ 2 万行同名重复）。**不进裁剪** —— 与影子账本同一档：无法重建。
⚠ 一条边界写在 SQL 头注释里：**首行的 `observed_date` 只是「我们开始记的那天」**，
不是「它那天才变成这个行业」⇒ **不许把首行往前外推**，那正是这张表要防的未来函数；
`at()` 在开始记录之前返回 `null`（= 「那天我们还没开始记」，不是「没有行业」）。

**看板每天可见**：`pnpm iterate` 的「真机运行」下新增一行，三态分开 ——
**读不到**（表不存在）· **一行都没有**（⚠ 沉默 = 在丢东西）· **N 只 / M 行，自 X 起**。
理由与 `shadow_journal` 那张表同源：**攒东西的过程必须看得出在不在跑**。

**⚠ 2026-08-23 真机验证**：`pnpm dev` 启动，日志 `[db] 迁移前已备份 → market.db.bak-13` +
`[db] 迁移完成 → v14（014_industry_history）` ⇒ **better-sqlite3 驱动下迁移干净**（单测走的是 node:sqlite）。

**⚠ 同时纠正一处我自己写错的说法：观测频率是「每周」不是「逐日」。**
它挂在 `refreshProfiles()` 上，而那个由 `MAINTENANCE_INTERVAL_MS = **7 天**` 门着
⇒ 真机上 `profile_refreshed_at` 停在 08-19 11:35，**第一行要等到 08-26 11:35**。
写「逐日留痕」时没查那个常量。**这不影响用途** —— 行业调整是**季度级**事件，
一周分辨率绰绰有余，而 `observed_date` 本来就只是「我们看到的那天」不是生效日。
⚠ **不要为它调小 `MAINTENANCE_INTERVAL_MS`**：那个常量同时门着日历刷新、裁剪与备份，
为一个季度级字段把它们全部提频是错误的代价分配。看板那一行已改成如实说「最多等一周」。

---

## 判据纠正：「执行滑移 = ledger × 影子」这个提法是错的（2026-08-22，`ENGINE_VERSION` **不变**，**只读真机库，什么都没改**）

取了 **Perold (1988)**（*The Implementation Shortfall: Paper vs. Reality*, **JPM 14(3) 4–9**）
的口径之后，差距文档 §7 第 5 项 / §1.3 那句「`ledger × 影子组合` 的差值 = 执行滑移」
**站不住**。全部记录在 [M2 §5.53](./docs/notes/M2-偏差报告.md)，工具 `scripts/verify/impl-shortfall.ts`。

**口径**：`IS = C_d(延迟) + C_i(冲击) + C_e(显性) + C_o(机会)`，
`IS(bps)` 的分母用**想要的**股数不是成交的股数。**正 = 执行侵蚀了收益。**
⚠ 「滑移」只是 `C_i` 一项，`IS` 是从**决策**到最终结果的整个差距、**含未成交部分**
⇒ 我们的 `--slippage` 只是 `C_i`、`trade.costs` 只是 `C_e`，**`C_d` 与 `C_o` 从来没量过**。

**三条结论**：

1. **两边的差主项是 `C_d` 且符号是反的。** 影子**只吃 CONFIRMED**（收盘后才有）
   且**次日开盘成交**，而人看到的是**盘中**信号、当天就动手 ⇒ **人比纸面组合早**。
   实测重叠标的差 **≈ 41 bp**（真实更好），拆开 = **10 bp 影子按设计收的滑点**
   + **≈ 31 bp 一个交易日的时间差**（`|C_d|/|C_i| ≈ 3.1×`）。
   ⇒ **那是前向纪律的代价，不是执行质量。** 这一行改成「先分解，再谈执行」。
2. **决策价的口径能翻符号，而这次能定对错。** `SH601919` 08-18：
   按**信号日收盘**（16.31）算，用户是 **−36.8 bp**（看起来像能力）；
   按 **`price_at`**（09:42 气泡弹出时的 16.22）算，是 **+18.5 bp**（等了三小时的延迟成本）。
   **这是个「提醒 → 人决策」的产品，决策发生在气泡弹出那一刻** ⇒ 正确的是后者。
   ⚠ 与前一条（GH1 的匹配口径）**不是同一情形**：那里两个口径都站得住、只能预承诺；
   **这里能判对错**。⇒ 「发现两个口径 ⇒ 问能不能符号相反」之后还要接一问
   **「其中一个是不是错的」**。
3. **对 L0 这个形态，`C_o` 比 `C_i` 重要得多。** `C_i` 上界只有滑点 10 bp，
   而 `C_o` 覆盖的是「引擎 7 天说了 52 条 CONFIRMED、用户动了 6 次」那一整块。
   ⚠ 但**「没动手」算不算「想买但没买」是产品判断** ——
   用户完全可以是故意不动手，而那正是 L0 设计里人该有的权力 ⇒ 已登记成拍板项。

**一处自检通过**：4 笔影子成交**全部**恰好 = 当日开盘 × 1.001，与出厂滑点差 **−0.00 bp**
⇒ 影子的成交口径没有与回测分叉。

**⚠ 一个会静默丢样本的缺陷**：`trade_log.traded_at` 存的是**本机 12:00**
⇒ `SH601788` 08-18 的首条信号在 **13:04**、那个假时刻是北京 **13:00**，
**差 4 分钟**就让整对配不上，输出里只剩一个「—」。
⇒ 那个假时刻**不只是把 `C_d` 变粗，它会让样本凭空消失**。已登记。

**两条顺带发现**（只报不追，都已登记）：
① **`shadow_trade` 的行数不能用来判断影子有没有在交易** —— 它记的是**已平仓的往返**，
4 笔买入还持着时 `count = 0` 与「没在跑」长得一样（我当天就这么判错过一次并自我纠正）；
判「在不在跑」要看 `shadow_journal`。
② **CLAUDE.md 里「每只每天约 2 行」在真机上不成立** —— 实测每(只×天)
**中位 6 · p90 69 · 最大 244**，且 08-18 之后仍有 31/69/72 的组。

---

## 判据新增：池外参照有了口径（GH1），而它与现有的「超额」符号相反（2026-08-22，`ENGINE_VERSION` **不变**，**绩效数字一个没动**）

**差距文档 §2.2 那个「优先级最高的真空」结案。** 全部记录在
[M2 §5.52](./docs/notes/M2-偏差报告.md)，工具 `scripts/verify/outside-pool.ts`（只读）。

**口径**：**GH1**（**Graham & Harvey**, *JFE* 42 (1996) 397–421 / *FAJ* 53 (1997) 54–66）——
把**基准**与现金按**每日恒定权重**混到 `σ` 等于组合的 `σ`，再比收益。
现金按 0 计息（与 `performance.sharpe` 的 rf = 0、与「回测不给现金计息」一致）。

| 样本 | `w(σ)` | **GH1** | 报告现在印的除法版超额 |
|---|---|---|---|
| 训练 2018–2023 | 4.18% | **−1.71pp** | **+16.75pp** |
| 验证（修正后 · 评估期） | 5.06% | **−0.37pp** | −13.51pp |
| 指数择时 2005–2017（校准用） | 19.01% | **−16.40pp** | −69.10pp |

**四条要记住的**：

1. **同风险下我们输给被动**：把 4~5% 的钱放进沪深300、其余持现，比这套主动择时更好。
   两个窗口同向为负。
2. **它与现有口径在训练窗口上符号相反**（+16.75 vs −1.71）。
   「跑赢了下跌的基准」与「输给同风险的被动持有」**同时为真** ——
   前者答「跟满仓比」，后者答「值不值得做」，而报告只印了前者。
   ⇒ **引用超额时两个必须一起给。**
3. **`GH2 / M²` 判不能用**，三条理由都有数：需要 `σ_m/σ_p ≈ 20–24` **倍杠杆**（A 股个人账户拿不到）·
   `M²(rf=0)` **−5.14pp** vs `M²(rf=2%)` **−50.96pp**（`R_f` 系数 22.9 ⇒ **符号由 rf 支配**，
   与 M2 §5.41 否掉 CAPM alpha 同源、放大 24 倍）· `M² ≡ (SR_p−SR_m)·σ_m`
   数值验证到 **1e-17** ⇒ **零新信息**，只是把夏普换成百分点。
4. **它自带一个能翻符号的自由度**：匹配口径。指数那份上 σ 匹配 **−16.40pp**、
   占用匹配 **+0.72pp**。⇒ 与零点定义 / 加权口径 / DSR 的 `N` / MinTRL 的 `SR*` 同一形状，
   **必须预承诺**。⇒ 因此 **GH1 今天没有进报告**，先登记成拍板项（建议 σ 匹配）。

**两条方法上的产出**：

- **GH1 必须只在评估期上算。** 预热段里策略是**结构性**空仓而基准照常涨跌
  ⇒ 带预热段等于白送被动参照一段市场暴露，**偏差方向固定不利于策略**。
- **线性近似 `w × 基准收益` 绝不能用** —— 与真实的恒定权重复利差最大 **16.81pp**，
  而**符号由基准方向决定**（基准跌 ⇒ 混合组合比线性好；涨 ⇒ 比线性差）。主项是**复利的凸性**。

**⚠ 同时更正 2026-08-21 的一处归因**（[M2 §5.51](./docs/notes/M2-偏差报告.md) ④）：
那一格把 `abl-valid-base` 的病态矩归给「离群簇」，**真根因是它没有段前历史** ——
18 个月里**前 15 个月净值一动不动**（首个变动日 2025-04-07），建仓只有 **34** 次。
⇒ 那一格的三个数（σ 0.38× · γ₄ 6.06× · \|z\| 1.95）**作废**，它比的是
「1456 天真交易 vs 49 天真交易」。**§5.51 其余部分与 §5.31 都不受影响**
（后者明写着 `--from 2022-10-01` 带 302 根预热）。
**这个坑的形状值得记**：CLAUDE.md 那条「跑验证窗口必须带段前历史」里写着「只剩 **34** 次建仓」
—— 它当初就是拿这份报告写的，一天之后我又把它挑回来了 ⇒ 又一次
**「写下一条纪律不等于装上一道闸门」**。已登记条目（报告自己认出预热占比）。

---

## 判据边界：平稳性第一次被检验，H1 在方差上被拒 —— 六处假设各自划了边界（2026-08-21，`ENGINE_VERSION` **不变**，**任何数字都没动**）

**这一条不改任何数，它只给已有的数装上「适用到哪」。** 起因是用户问「我们有做过平稳性检验吗」
—— 答案是**没有**，「平稳」这个词此前在整个仓库里只出现在归属注释里，而它压着六处
（`sharpeRatioHac` · MinTRL/DSR · **rank IC 的 NW `t`** · `audit:random` 的块置换 ·
`pairedDelta` 的 CR1 · 训练/验证划分）。全部记录在 [M2 §5.51](./docs/notes/M2-偏差报告.md)。

**口径**：CUSUM-of-squares。IID 版归 **Inclán & Tiao (1994)**，
HAC 分母那版是 **Sansó, Aragó & Carrion (2004)** 的 **κ₂**，
临界值 **1.358** = 布朗桥上确界的 5% 分位（Kolmogorov–Smirnov）。
⚠ **Brown, Durbin & Evans (1975) 是祖先不是出处**（他们做递归残差 + 平行直线边界）——
我第一版写错了，两次独立查询纠正的。
自检是**经验水平与功效**（种子固定的 200 次蒙特卡洛）：水平 **2.5% / 3.5%**（名义 5%）·
功效 **100%**。偏保守这一点与文献一致（Garbade 1977 / Hackl 1980）⇒ **每次拒绝都是被低估过的**。

**结果**：

| 序列 | 三段 σ 极差 | **三段 VIF 极差** | CUSUMSQ κ₂ |
|---|---|---|---|
| 策略 · 训练 2018–2023 | **2.30×** | 1.43×（1.09–1.56） | **1.858** ⇒ 拒绝 |
| 基准 · 同期 | 1.20× | 1.19× | 1.657 ⇒ 拒绝 |
| 基准 · 2005–2017 | 1.45× | 1.11× | **2.927** ⇒ 拒绝 |

**三条要记住的**：

1. **方差恒定被一致拒绝，市场自己也拒绝** ⇒ 不是「只有我们的暴露在动」。
   但策略的 σ 极差是基准的近 2 倍，那个差额才是暴露那个通道。
2. **VIF 是这里面最稳的量**（比值、尺度不变）⇒ §5.50 那个 1.32 **继续可用**，
   但从今天起要写成「**训练窗口上的** 1.32（分段 1.09–1.56）」。已写进
   `sharpeRatioHac` 的头注释。顺带一个机制：**基准自己的 VIF 是 0.9606（< 1）**
   ⇒ 策略那个正自相关来自**它自己的持仓结构**，不是市场。
3. **均值那一侧根本测不动**：训练窗口三段年化夏普两两之差 \|z\| = 0.86 / 1.08 / 0.24
   （合成 HAC SE 约 ±1.1）。⇒ **「验证窗口反向」反复出现却推不出结论，根因就是这个** ——
   485 个观测下夏普的标准误就有 ±0.74–0.89。以后引用那类反向必须同时给出标准误。

**一个新发现的坑**：`abl-valid-base`（验证窗口）报的 `performance.sharpe = 1.191`
**去掉绝对值最大的 3 天变成 0.148**，而最大的 5 天**全部落在 2025-06**（窗口最后一个月）；
γ₃ = +7.21、γ₄ = **79.70**（训练是 −0.47 / 13.15）。⇒ **那个 +1.19 不许当「验证窗口表现好」引用。**
⚠ **不影响 §5.31 的消融结论** —— 那是同窗口内的**配对**比较，共同的离群簇在两臂上都在。

**什么条件下这些边界要重划**：换了标的池、换了窗口、或占用率量级变了（VIF 的稳定性
建立在「σ 与 S₁₁ 同步缩放」上，而占用率是那个缩放的主要来源）。

**顺带暴露一个可修的缺口，已登记**：六处里**唯一没测到的恰好是唯一承重主判据的那一处**
（rank IC 的 NW `t`）—— `audit:ic` 只落聚合量，逐日 IC 序列没落盘。

---

## 判据变更：夏普方差的自相关修正落成正式口径（Lo 2002），`η(q)` 判不能用（2026-08-21，`ENGINE_VERSION` **不变**，**引擎数字一个没动**）

**只动了标准误与 MinTRL 的年数乘子，任何绩效数字都没动。** 夏普的**点估计**
（`performance.sharpe`）永久是 rf = 0 + `×√243`，这次一个字没碰。

**为什么做**：[M2 §5.49](./docs/notes/M2-偏差报告.md) 的 MinTRL 是 2026-08-17 那条
「ETF 前向记录攒够时间」判据唯一的量化出口，而它的年数只对 IID 成立 ——
自相关那一格当时只给了个**均值上**的 Newey-West 膨胀因子（1.29–1.31），
自己标着「近似 · 不是口径 · 不许当门槛」，并把 Lo (2002) 登记成待测项。今天补上了。

**口径**：`sharpeRatioHac`（`src/backtest/metrics.ts`）= Lo (2002) 的
`V_GMM = ∇g′ S ∇g`，`θ = (μ, σ²)`、`∇g = (1/σ, −μ/2σ³)′`、`S` 是矩条件
`[r_t−μ, (r_t−μ)²−σ²]` 的 **HAC 长期协方差**（Bartlett 核）。
`lag = 0` 时**逐位退回**既有的 `sharpeVarianceTerm`（Mertens/Christie 闭式），
那条嵌套恒等式是它唯一的正确性保证，单测里就地重写闭式钉着（不复用被测实现）。
滞后阶**预承诺**：Andrews 7 与「持仓期−1」13，两个都报。

**实测（`cap-100000` 训练窗口，1456 个日收益）**：

| | Andrews 7 | 持仓期 13 |
|---|---|---|
| **夏普方差 VIF**（新口径） | **1.3208** | **1.2975** |
| 均值 VIF（§5.49 那个近似） | 1.3126 | 1.2865 |
| 差 | +0.62% | +0.86% |

⇒ **那个近似当时就是对的**，MinTRL 年数乘子从 ×1.31/×1.29 变成 ×1.32/×1.30
（0.5 那一档 11.0 → 14.5 年），**查表七行一个字没改**。
原因不是巧合：`V_GMM` 三项里 `S₁₁/σ²` 占 **100.5%**，因为日频 `SR_日 = −0.0264`
把后两项的权重压到 `SR` 与 `SR²/4`（1.7e-4）量级。
**波动率聚集在数据里很强**（`S₂₂` 自己膨胀 **2.056×**，三项里最强）**但进不到结果里**。

**什么条件下这个结论失效**：`SR_原始频率` 不再接近 0 —— 月频且夏普 ~1 时
`S₂₂` 那一项的权重是 0.25，「均值 VIF ≈ 夏普 VIF」立刻不成立（论文里那些对冲基金正是这一档）。

**判不能用的一项**：`η(q)`（Lo 的时间聚合因子，`SR(q) = η(q)·SR`）在 `q = 243` 上不可估 ——
1456 个观测估 242 个 `ρ_k`，满阶给 22.000、截断到 Andrews 给 13.464（`√243` = 15.588），
相差 63.4% 且**连修正方向都不一致**。⇒ 它**没有**进 `metrics.ts`，只活在验证脚本里。
而且不需要它：截断口径下 `√q/η(243) = 1.1578` 与 `√VIF = 1.1457` 只差 1.1%，
两者本来就是同一个长期方差的两个核。

**顺带纠了一处归属错误**（`scripts/verify/stats.ts`）：那个方差项的链条原写成
「Mertens (2002) 假设 IID → Christie (2005) 用 GMM 去掉 IID → Opdyke 给出渐近分布」，
实际是 **Jobson & Korkie (1981)** → **Lo (2002)** → **Mertens (2002)（是对 Lo 的更正**，
指出 Lo 那段只在 IID **正态**下成立）→ **Christie (2005)** → **Opdyke (2007)（证明后两者同式）**。
⚠ 最容易读错的是下一句：「Mertens 的闭式在平稳遍历下也成立」**不等于**「自相关已经处理了」
—— 它用同期矩，自相关只能从长期协方差进来。混读会得出「不用做了」这个相反的结论。

**顺带一个描述性的数**：训练窗口年化夏普 −0.4119 的 HAC 年化标准误 **0.4671** ⇒ \|t\| **0.882**
⇒ 与 0 区分不开。**这不推翻任何既有结论**（从来没有一处声称过它显著），
与 §5.48 的 DSR 是方向一致但独立的两个量，别互相当证据。

**工程副作用一处**：`ic-audit.ts` 的 `neweyWestVariance` 改成薄封装，
自协方差循环只剩 `metrics.ts` 一份（既有 8 条断言全绿，数值逐位不变）。

---

## 口径新增：夏普给出 rf ≠ 0 的第二列，机会成本**只按逐日持仓占用收**（2026-08-21，`ENGINE_VERSION` **不变**）

**既有的每一个数都逐位未变。** 用归档的 `cap-100000` 那 252 只、同窗口重跑了一遍逐份核对：
`performance` 的 12 个标量、`trades`/`positions` 全部统计、1457 个净值点、3305 笔成交
**全部逐位相同**。`performance.sharpe` 的口径**永久是 rf = 0**，所以老报告之间照旧可以横向比。

**为什么做**：用户 2026-08-21 拍板「无风险利率按 2% 减」。而**直接减是罚两次** ——
这套策略逐日占用只有 3.65%，而回测与影子**都不给现金计息** ⇒ 那 96% 的钱在账本里一分没赚，
再按满仓的标准扣掉整个 rf，扣的是一笔它从来没拿到过的收益。

**口径**：`超额_t = r_t − (rf/243) · w_{t−1}`，`w` = 上一日收盘持仓市值 ÷ 净值。
它与「给现金按 rf 计息、再整体减 rf」代数上恒等（`r + rf(1−w) − rf = r − rf·w`），
但**不去动净值曲线**，而这一点是决定性的:真把利息累进去,六年的现金利息会把
`totalReturn` 从 −1.99% 抬成**正数** —— 那是货币基金赚的,不是策略赚的,
**这个项目最不能产出的就是那种数字**。

**实测（`cap-100000` 训练窗口）**：

| 口径 | 年化夏普 |
|---|---|
| rf = 0（`performance.sharpe`，不变） | **−0.412** |
| rf = 2%，只对持仓收（`performance.sharpeNet`） | **−0.502** |
| rf = 2%，直接减（**不许引用**） | −2.888 |

差 2.4 个夏普全部来自这个 artifact:分母被空仓稀释了约 24 倍(日标准差 0.052% vs
沪深300 1.239%),`rf/243` 占策略日标准差的 15.9%、占指数的 0.66%
⇒ **同一个 rf 对低暴露策略的伤害是对指数的 25 倍**(沪深300 自身 −0.054 → −0.158)。
这与「低暴露策略上 CAPM alpha 的符号由 `Rf` 决定」同源（M2 §5.41），那一条已经否掉了 alpha 当判据。

**结论方向没变**:−0.502 与既有结论同向(训练窗口 Calmar −0.113、98 个候选里 0 个夏普为正、
DSR 门槛 ≈ 1.0)。换 rf 只是把「本来就是负的」说得更准 ——
**它回答的是「值不值得做,还是全放货币基金」,答案仍是不值得。**

**顺带**:影子面板的夏普此前是**光着印**的,而 4 个净值点(其中一天全空仓、日收益率恰好
0.0000%)算出年化 **12.58** —— 那个量级在真实日频权益策略上不存在,它唯一表达的是样本量。
现在两个 rf 口径并排、rf 的取值写在标签上（自由参数不许脱开取值引用），
且交易日不足 20 时显示「样本不足」而不是数字。**20 是展示阈值不是统计阈值** ——
真门槛是 MinTRL 且以**年**计（夏普 0.5 要 11 年），任何按天设的线都拦不住「这个数还不能承重」。

**什么条件下这个结论失效**：① `rf` 是**自由参数**，2% 是用户对当下货币基金档的判断 ——
引用 `sharpeNet` 必须带上 `meta.riskFree`／面板标签上那个取值，同 DSR 的 `N`；
② 若日后真在 `simulate.ts` 里给现金计息，这个恒等式的前提就没了，`totalReturn` 那条警告要重读；
③ `×√243` 的自相关问题 rf 一点都没改变（Lo 2002 的修正仍在待办）。

## 判据变更：「前向记录要攒多长」终于有数了 —— **年数 ≈ 2.71 / 夏普²**（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎、参数、任何既有报告的数字一个都没动。** 新增只读脚本 `scripts/verify/mintrl.ts`
（与 `dsr.ts` 共用 `scripts/verify/stats.ts` 里那份夏普估计量方差 —— **两处各写一份就会给出两个门槛**）。

**为什么做**：2026-08-17 用户把 ETF 的判据从「回测统计显著」换成「**前向记录攒够时间**」，
2026-08-19 又拍板停掉个股·日线·择时的研发 ⇒ 前向记录是现在活着的那条路。
而**「够」是多少，从那天到今天没有任何一个数在回答**。DSR 在这条路上用不上（前向 `N = 1`，
没有选择偏差可扣）。

**口径**：**MinTRL / PSR** —— Bailey & López de Prado, *The Sharpe Ratio Efficient Frontier*,
**Journal of Risk 15(2) 3–44**（SSRN 1821643）。
`MinTRL = 1 + [1 − γ₃·SR̂ + ((γ₄−1)/4)·SR̂²] · (Z_α/(SR̂ − SR*))²`。
脚本自检两条：**往返**（结果代回 PSR ⇒ 0.950000）与**跨脚本**
（喂 `dsr.ts` 报的「N=1 需要年化 0.6810」⇒ 拿回 1456 个观测，正是那份报告的长度）。

**算出来的数**（95% 单侧 · 一年 243 交易日 · 依据 M2 §5.49）：

| 假设年化夏普 | 0.1 | 0.2 | 0.3 | **0.5** | 0.8 | 1.0 |
|---|---|---|---|---|---|---|
| 需要的前向记录 | 271 年 | 68 年 | 30 年 | **11 年** | 4.4 年 | 2.8 年 |

再乘自相关的方差膨胀 **×1.3**（`L=7`/`L=13` 两档都是 1.29–1.31，量级参考不是口径）。
**近似式 `年数 ≈ Z²/SR² = 2.7055/SR²` 准到 5% 以内。**

**三条读法，混一起就会读错**：
① **这不是「ETF 路线被否」** —— 它只说「靠前向记录自己攒到统计显著」在这些效应量上
没有人类时间尺度的解，ETF 作为产品选择一个字没被否定；
② **也不是对 ETF 回测那份的重判** —— 那份的年化夏普只当**效应量输入**用了一行
（⇒ 152 年），**没有**算它的 PSR/DSR，它的既有结论一字未改；
③ **`SR*` 是自由参数**：从 0 换成基准，0.1 那档从 271 年掉到 114 年 ——
不是策略变好，是 2018–2023 沪深300 自己夏普为负（**−0.0545**）。
与 §5.48 的 `N`、§5.47 的滞后阶同形状 ⇒ **两档都报，不许只引宽的那档**。

**一条推导出来的口径纠正**：文献「用不违反 IID 的最高采样频率报告」容易被读成
「换频率能更快达标」。**IID 下每年观测数会被约掉** ⇒ `MinTRL_年 = varTerm·Z²/SR_年²`，
**换频率省不下日历时间**；提高频率唯一的收益是让 `varTerm` 更接近 1，而那一项实测只有 0.3%–7%。
⇒ **能省时间的只有把效应量做大**（平方反比：夏普翻倍，时间变 1/4）。

**什么条件下这个结论失效**：① 置信度或 `SR*` 的取法变了；② 若日后改用**双样本** MinTRL
（比较两个策略的夏普差）—— 那是另一个公式；③ 自相关那 ×1.3 是**近似**
（HAC 作用在均值上，夏普方差的自相关修正要走 Lo (2002)），真做门槛要先补那个口径。

---

## 判据变更：多重比较有了一条能算出数的门槛（DSR），但它**不进写回门槛**（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎、参数、任何既有报告的数字一个都没动。** 新增的只有一个只读脚本
`scripts/verify/dsr.ts`（读 `reports/calib/` 下已有的 JSON，不跑模拟，`--touch-test` 仍是 **5**）。

**为什么做**：[差距文档 §2.3](./docs/notes/与机构量化系统的差距.md) 那句「多重比较的正式处理 ——
**现在靠人的克制**」是这个项目最明显的裸露面（十一轮标定 · 10 张网格 · 98 个配置，
全靠一句写在文档里的「每个机制只报一次分位」在挡）。而同一天刚证过
**「写下一条纪律不等于装上一道闸门」**（[§5.44](./docs/notes/M2-偏差报告.md) 的预注册
写着「不许拿它当新基线」，`pnpm iterate` 的看板当天就违反了）。

**学到的口径**：**DSR**（Bailey & López de Prado 2014, JPM 40(5) 94–107）
= **PSR**（Bailey & LdP 2012）把门槛换成「N 次试验下的期望最大夏普 `SR̂₀`」。
PSR 分母那个非正态下的夏普估计量方差归 **Mertens (2002)**，去掉 IID 那步归 **Christie (2005)**
（后两条是从作者讲稿正文一手核到的）。脚本内置**自检**：复现作者自己的算例
（年化 SR 2.5 / N=100 / T=1250 ⇒ 作者答「只有 90%」），本地算 **0.8997** ✅。

**算出来的数**（依据 M2 §5.48，预注册在前、预测记分在后）：
6 年训练窗口上，一个候选要拿到**年化夏普 ≈ 1.0** 才算「扣掉我们这 98 次搜索后仍是发现」。
而其中**多重比较只占 0.30**，剩下 **0.68 是「1457 根 K 线就是不够长」** ——
即使只试一个候选、完全没有选择偏差，也要 0.68。
⇒ **「跑出一个夏普 0.3 的候选」在这个窗口长度上本来就说明不了任何事，与试了多少次无关。**

**为什么不进写回门槛**：现有门槛判的是**逐折配对 Δ**（`|Δ|/stderr ≥ 2`，按时间片聚类 CR1），
量「候选比出厂值好多少」；DSR 判**单条曲线的夏普够不够高**。两者问的不是同一个问题。
把它接进 `calibrate.ts` 是一次独立的判据变更，要单独决定（已列进迭代计划）。
⇒ 现在它的**唯一合法用途是进下一次预注册**。

**PBO / CSCV 判「刻意不做」**（不是待办）：输入不存在（要 `T × N` 逐期绩效矩阵，
而网格报告只存聚合指标、`foldScores` 108 行全空 —— 每个候选都在算折分**之前**
就被「训练集 Calmar ≤ 0」红线淘汰），补上要重跑 98 次模拟；**且算出来也承不了重** ——
PBO 量的是排名的持续性而非绝对绩效，而我们 **0/98 个候选夏普为正**。
重开条件只有一个：候选族里出现多个夏普为正、需要从里面挑一个的那天。

**什么条件下这个结论失效**：① `N` 的口径变了（现在预承诺三档 27/98/300，
主口径 98 = 按 `paramsFingerprint` 去重后的网格配置数）—— 往后每新增一张网格都该重算；
② 训练窗口的长度变了（`0.68` 那个数完全由 `√(T−1)` 决定）；
③ 若日后主判据从「配对 Δ」换成「夏普」，这条「不进门槛」的结论要重新讨论。
**⚠ 一条纪律**：`N` 由研究者自己申报、外部无法审计 ⇒ 与「调滞后阶到显著为止」同形状的
p-hacking 通道（这里叫「调小 `N` 到通过为止」）。三档一起报是刻意的，脚本**不提供**选一档的接口。

---

## 判据变更：rank IC 的 t 改用 Newey-West（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎与参数一行没动，IC 的均值、五等分与既有报告逐位相同**（6/6 格实测相等）——
动的只有**标准误**，即那个 t 值。这是[迭代计划 §4.6](./docs/notes/下一阶段取舍与迭代计划.md)
那个病（样本不独立 ⇒ 标准误偏小 ⇒ 显著性虚高）的**第四处**，
且它是 §4.6 结案**之后**新长出来的：[§5.46](./docs/notes/M2-偏差报告.md) 把
`t = mean(IC)/(sd/√days)` 写进了预注册的主判据，又在限制里自己标了「t 是上界、别引用」。

**这一处比另外三处硬**：前瞻收益是**重叠**的（每天算一次 h 日收益）⇒ IC 序列在结构上带
**MA(h−1)**。那不是「可能不独立」的估计，是机械已知的 —— h=20 时相邻两天共享 19/20 的窗口。

**动了什么**：`src/backtest/ic-audit.ts` 加 Bartlett 核的 Newey-West 长期方差
`Var(x̄) = (1/T)(γ₀ + 2·Σ(1 − k/(L+1))·γₖ)`。**滞后阶两档都预承诺、并排报**：
主口径 `L = h−1`（机械已知的重叠阶数），另一档 Andrews (1991) 的 `⌊4(T/100)^(2/9)⌋`。
朴素 t 保留并标「上界，不许引用」。

**依据**：M2 §5.47（预注册在前，结果与预测记分在后）。训练窗口调整后 `|t|` **3.7–6.1**
（三个持有期都过 2），验证窗口 5/10 日**测不出**（−0.91 / −0.52）、20 日 **−2.01 压线**。
缩小倍数 **1.5–2.4×**，且**随 h 严格单调** —— MA(h−1) 那条机制被数据证实。
⇒ **§5.46「t 是上界别引用」那条限制可以撤回**，主判据第一次有了能引用的数；
但**朴素 t（−8.9 ~ −10.0）永久不许引用**。

**顺手修掉一个隐性坑**：`icOf` 原来直接遍历 `byDate.values()`，而插入顺序由扫描顺序决定
（起始更早的票会把早期日子追加到尾部）。IC 均值与五等分**与顺序无关**，所以这个坑在加 NW
之前**没有任何症状**；而自协方差按相邻位置算，顺序错了那个数没有意义**且不报错**。
已改为先按日期排序，用例里有一条「对顺序敏感」钉着理由。

**什么条件下这个结论失效**：① 若日后把前瞻收益改成**不重叠**采样（每 h 天取一个点），
`L = h−1` 这个主口径就失去依据，要重新论证；② `−2.01` 那个压线读数**不许承重**，
也**不许再去找一个能把它推过或推下线的滞后阶规则** ——
**调滞后阶到显著为止是文献里有记录的 p-hacking 通道**（有论文专门建模：算 0..4 阶取最小 p 值），
这正是两档滞后阶都写死在看结果之前的理由。

## 看板把实验跑当成了「回测基线」（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎与参数一行没动，任何一个绩效数字都没变。** 记在这里的理由同下面 08-19 那条：
它改变的是**系统说不说实话**，而这次说错话的地方是[迭代 skill](./.claude/skills/迭代/SKILL.md)
指定「每次会话开头必读」的那一份状态层输出 —— 错在这里等于给整轮迭代喂一个错的起点。

**症状**：`pnpm iterate` 的「回测基线」那一行显示的是 `cap-500000.json` ——
§5.44 候选 B 的 **5× 资金实验跑**（1114 建仓 / 43.81% / 占用 3.61%），
而出厂口径那份是 **1097 / 43.21% / 3.50%**。`tools/iterate/status.ts` 的 `latestBaseline()`
只按「最新 + 标的数 ≥ 200 + 含 trades」挑，**三个旋钮（资金 / 参数指纹 / 成本）一个都不查**。

**为什么它不显眼**：偏差方向不随机 —— 调大 `--capital` 只会让「一手都买不起」变少、
数字更好看，**没有任何一处看起来像坏了**。而 §5.44 的预注册里就写着
「这一跑不是新基线，不许拿它的绩效数字去替换任何既有引用」——
那条纪律写在文档里，却没有任何东西**执行**它，于是同一天就被违反了。

三处改动：

- **`meta.costs` 补记，且是必填**（`src/backtest/report.ts`）。此前成本口径**完全没记** ⇒
  一份 `--slippage 0` 的跑（`noslip-train.json`，**−1.21%** vs 出厂口径 **−1.99%**）
  在归档里**结构上认不出来**，而滑点占负期望的 69%（§5.29）。做成必填是为了漏传时编译不过
  （与 `TradeInput.board` 同一条理由）。
- **`auditKnobs()` 是唯一的判据出处**（同文件）。报告自己按它打「非出厂口径」告警
  （与「池过滤」「一手都买不起」两条 no-silent-caps 同形），看板按它挑基线 ——
  照抄一份的症状是「报告说这是实验跑、看板说这是基线」。
  `renderReport` 同时加了一行**口径行**（资金 + 四项费率），让 `.txt` 归档自描述。
- **看板跳过非出厂口径的报告并点名跳过了哪份**（`tools/iterate/status.ts`）。
  一份都挑不到时按纪律 3 报「读不到」+ 列出跳过的，**不退一步显示实验跑**
  （这个项目的历史是数字会被引用、警告会被读过去）。

**三态刻意分开**：偏离 / 没有偏离 / **无法核对**。08-20 之前的报告都没有 `meta.costs`
⇒ 落 `unverifiable` 并在看板上显式说「未记录 ≠ 等于出厂」。把它判成「等于出厂」
与「用 0 冒充未预热的指标值」是同一个错误方向（约束 4 的精神）。

**什么条件下这个结论失效**：不失效 —— 它不是一个标定结论。但**判据会漂**：
日后新增任何一个改变绩效口径的 CLI 旋钮（新的成本项、新的池过滤维度），
都必须同时进 `meta` 与 `auditKnobs`，否则这个洞按同一形状重新打开。
**已知未修的一处**：`audit:random` 的输出 meta 只记 `baseline` 路径、不继承基线的
资金与成本 ⇒ 拿非出厂口径的报告跑出来的 alpha 同样认不出，而 **alpha 是主判据**。
见[迭代计划 §4.9](./docs/notes/下一阶段取舍与迭代计划.md)。

## 四处「看不见」的修复（2026-08-19，`ENGINE_VERSION` **不变**）

**引擎参数与判定逻辑一行没动，回测与影子的任何一个绩效数字都没变。**
记在这里的理由与下面「把『一手都买不起』从静默变成告警」相同：这四件事改变的是
**系统说不说实话**，而它们全都属于同一类 —— 系统在按设计工作，用户却没有办法看出来，
或者状态在一个用户看不见的地方悄悄丢了。**少发/少显示的错误用户自己发现不了**，
这是本项目最贵的错误类别。

- **① 日报当天永远不定稿**。`stage = FINAL` 要求每只有行情的自选票都拿到当日收盘线，
  而 `needsQuotes('CLOSED')` 为 false 让应用 15:10 之后一个日线请求都不发，
  个股日线数据源却是 15:05–15:30 才发布 ⇒ 当天的收盘线要到**次日盘前**才入库，
  而 `reportSubjectDate` 在次日 09:30 之后就切到新一天 ⇒ **定稿版只在次日
  08:05–09:30 那段窗口里存在**。实测 08-19 15:29 库里当天日线只有 12/80 只。
  新增收盘后 15:10–16:00 的**只补日线**窗口（`engine/tick.ts` 的 `CLOSE_CATCHUP`），
  两道停手闸门（补齐即停 / 至多 8 轮）保证它不违反 docs/03 §2.4 的请求礼节。
  **收盘确认补跑一个字没动** —— 它的判据是 `through < ctx.date`，收盘后恒不成立。
- **② 影子运行看不出在不在跑**。全空仓时净值曲线是一条 1000000 的直线，
  与「压根没推进」长得一模一样；而委托一旦成交 `clearOrder` 就把它删了 ⇒
  事后拼不出「那天挂了哪两单」。新增 `shadow_journal`（013）逐日操作流水
  + 面板的运行状态与待成交明细。**`NOT_ADVANCED` 那一档最值钱**：用户下午才开机时
  按前向纪律不喂影子 ⇒ 那个交易日的记录**永久缺失**，此前这件事只在主进程日志里有一行。
  流水与 `shadow_trade` 同一档：不进裁剪、不加指向 `signal(id)` 的外键。
- **③ 重启即失忆**。(a) 快照缓存在内存里，重启即空，而休市时段不会有任何一轮 tick 去补
  ⇒ 晚上/周末重启后面板与悬浮条的价格一直空到下一个交易日 09:00。
  现在展示投影按 `quote_tick` → 日线两级回落，标 `STORED` + 灰显 + 数据时刻，
  **引擎侧一个字不改**（它照旧看不到快照，不会拿上周五的价做判断）。
  (b) 冷却/配额/强制类台阶改为落库（`meta.alert_gate_state`）——
  **这推翻了 `dispatcher.ts` 原来那条「刻意不落库」**，依据是真机数据：
  08-13 启动 14 次、08-14 二十七次，那两天「一天 10 条止损气泡」逐条对得上启动时刻。
  旧顾虑用三件事堵掉（恢复即按 now 裁剪 · 防抖计数不落 · 闸门用量可见 + 逃生口），
  逐条在 [docs/05 §4.0](./docs/05-风控与提醒规则.md)。
- **④ 影子净值的基准列 2/2 行是 null**。推进与日线回补在同一跳里，回补先失败、
  0.24 秒后推进就读到 null，而那一天不会再推进 ⇒ 永久留 null；
  `summarize()` 的 `lastBenchmark` 取最后一行 ⇒ 整条同期对比为 null。
  两处修：历史 null 会在后续推进时补齐（**只补基准这个事实列，不补跑任何信号与成交**，
  边界在 docs/07 §2.3b）；根因是应用内的腾讯 provider 不知道「指数无复权」——
  `parseKline` 现在只对 `board === 'INDEX'` 回退用 `day`。
  **ETF 与个股维持严格失败**：它们真的有复权，拿不复权顶替是错的失败方式。
- **什么条件下这些结论失效**：① 若数据源改到 15:30 之后才发布日线，
  `CLOSE_CATCHUP.to` 那个 16:00 要跟着往后挪（现在是「够用」不是「保险」）；
  ② 若日后给闸门状态加字段，`DispatcherState.v` 要递增 —— 读不认识的版本一律丢弃，
  猜一个半对的状态比清零危险得多；③ 那 8 只在腾讯拿不到复权轨的 ETF
  **不在这次修复范围里**，它们仍然单源依赖 eastmoney（78%）。

---

## T+1 卖出锁定 + 建仓体检（2026-08-19，`ENGINE_VERSION` **不变**）

**回测与影子运行的数字逐位不变** —— 这次不是参数变更也不是判定逻辑变更，
新增的 `Position.lockedShares` 是可选字段，而 `src/backtest` 与 `main/shadow`
**永不设置它**（与 `Position.stopFloor` 完全同一个先例，见 `core/types.ts` 那两段注释）。
`tests/unit/risk/risk.test.ts` 有一条用例把「缺省 ≡ 显式给 0」钉成等价，
`tests/unit/main/signals.test.ts` 另有一条钉「不传这个依赖时逐位保持旧行为」。
因此**不需要** docs/07 §3.6 那套判定逻辑改动的验收门槛（建仓数、配对胜率、alpha 稳定性）：
那套门槛是给「会改变交易集合」的改动准备的，而这次一笔交易都没变。

- **动了什么（一）· T+1 卖出锁定**：文档三处（docs/01 §6、docs/05 §1、docs/02 §111）
  一直写着风控要「抑制无执行意义的信号（涨跌停、停牌、**T+1**）」，但代码里只有买入侧的
  `T1_LATE_BUY`。**卖出侧一个字都没有** —— 今天刚买进的票当天跌破 8%，
  `positionVerdict` 照样出 L3 强制卖出并弹气泡，而用户根本卖不掉。
  现在：全仓都是当日买入 ⇒ `T1_SELL_LOCK` 硬抑制（先例是「已跌停，卖不掉」）；
  部分锁定 ⇒ `T1_PARTIAL_LOCK` 标注，**不抑制**（老仓那部分真的卖得掉）。
  做T建议同步改判**可卖**股数（`intraday-t.ts` 头注释本来就写着「T+0 只能用老仓做」，
  代码判的却是 `position.shares`）。判据与代价逐条在 [docs/05 §2.1a](./docs/05-风控与提醒规则.md)。
- **动了什么（二）· 建仓体检**（`core/risk/entry.ts`，`trade:entryCheck`）：
  把 `hardSuppressions` / `downgrades` / `positionVerdict` 按**买入**方向再跑一遍，
  连同止损参考价与日内位置一起摆在持仓页的买入表单上方。
  **一个新参数都没加** —— `params.ts` 与 `settings/params-view.ts` 的 `STATUS` 表一行没动，
  `GUESS` / `BLOCKED` 仍为 0。它**只复述不推导**（与日报 `tomorrow` 同一条纪律）。
- **顺带修的一处结构**：`evaluate()` 现在把它实际喂给风控层的 `GateInput` 原样带在
  `Evaluation.gateInput` 上。体检必须用**同一份**输入 —— 另拼一份的症状是
  「体检说没事、提醒说涨停买不到」，而没人分得清哪个才对。
- **代价（必须知道）**：硬抑制 ⇒ `alerts/candidates.ts` 见 `suppressed` 直接 `continue`
  ⇒ **连 `alert_log` 都不落**。「今天买、今天就跌破止损线」当天完全静默。
  这是刻意的（今天确实卖不掉），且不是永久静默：次日开盘第一轮 `lockedShares` 归零、
  止损照发 L3。原因仍随裁决进 `signal` 表，持仓卡上也显示「可卖 N，M 股今日买入」。
- **什么条件下这个结论失效**：① 若日后 `GatedDirection` 增加一个 `NEXT_DAY_SELL`
  之类的方向，这条抑制应当改写成与 `T1_LATE_BUY` 对称的**降级 + 改写**，
  那时它就该从 §2.1 挪到 §2.2；② 成交录入侧的提示（`t1SellNotice`）**刻意不硬拒** ——
  跨境 / 债券 / 黄金 ETF 与可转债是 T+0，若日后按板块细分了 T+0/T+1，
  硬抑制那一侧也要跟着细分（现在对所有品种一视同仁，对 T+0 的 ETF 是过严的）。

---

## 判据的分辨力上限：段块落点权重改按段均匀，**同 regime 的分位从此只能支撑粗结论**（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎与参数一行没动。** 这是 §5.42 那条「按段均匀」候选的落地，预注册（含事先预测与
四条判据）写在跑之前，见 [M2 §5.43](./docs/notes/M2-偏差报告.md)。

- **动了什么**：`findBases` 的落点改**按段分组**返回；抽样改成两步（先均匀选一段同状态行情、
  再在段内均匀选落点）。新开关 `--block-weight runs|positions`，**默认 `runs`**；
  `positions` 复现 §5.42 那一版。报告与看板都印用了哪一档。
- **依据（语义，不是数字）**：block permutation 的语义是**交换块** ——
  一次抽样 = 「这一簇建仓若发生在另一段同状态行情里」，那是**按段**的一次抽取；
  按位置均匀等于用段长给段加权。预注册的判据 3 事先写死「默认由语义定，与分位高低无关」，
  所以照它执行 —— 而这次 `runs` 的数字对策略**更不利**（ALL 31.3 / RANGE 39.2）。
- **⚠ 机制预测第二次被证伪，方向还是反的**：预测「按段均匀 ⇒ 随机组均值回落」，
  实测**继续上移**（RANGE 0.601% → 0.657%），TREND_UP 反而大幅**下移**（−0.133% → −0.386%）。
  ⇒ 「长段权重是主因」不成立；真实情况是**短段与长段的日子性质不同，且在不同 regime 里方向相反**。
- **这条变更真正的产出是一条限制**：同一批 1675 次建仓在四个零点下极差 **20–30pp**
  （`RANGE` 65.0 / 43.5 / 39.2 / 35.5 · `ALL` 46.5 / 36.5 / 31.3 / 55.0），
  而三个零点都在合理设计空间里、分不出胜负 ⇒ **口径效应比任何一次判定逻辑改动大一个量级**。
  [docs/07 §3.6 第 ⑤ 条](./docs/07-回测与验证方案.md)因此加了半句：
  **同 regime 的分位只能支撑粗结论（方向 + 跨口径一致性），不能支撑精细比较，也不能当写回门槛。**
- **跨口径一致的只有两件事**：`RANGE` 四个零点里**三个 ≤ 50%** ·
  `TREND_UP` 四个**都 > 65%**（66.5 / 67.0 / 74.2 / 99.5）。后者样本仍只有 207 次建仓，
  按 §3.11.4 第 2 条**分层结果不许拿来立项** —— 它留在「等样本」那条待办里。
- **什么条件下这个结论失效**：若日后有了「哪个零点更对」的独立依据（例如同时保住跨票同步
  与段内聚集的第三种块定义），这三组数字要重出；在那之前**再调块定义只会得到第四组数字**，
  而我们已经知道它们的分散度比要测的效应大 —— 那不是精度问题，是这个判据的**分辨力上限**。

---

## 与基准比的量改走严格配对 + 影子面板加 beta（2026-08-20，`ENGINE_VERSION` **不变**）

**回测数字逐位未变**（同命令重跑 20 只 × 训练窗口：信息比率 0.04 · beta 0.0177，与改动前一致）——
这次修的是一个**latent** 的坑：它在回测数据上从没被触发，但在影子那边随时会。

- **动了什么**：新增 `metrics.ts` 的 `alignedReturns()`，回测报告的**信息比率与 beta**
  改用它；`ShadowSummary` 加 `beta`（影子面板紧挨着「平均资金占用」显示）。
- **依据**：`returnsOf(points,'equity')` 与 `returnsOf(points,'benchmark')` **各自**会跳过
  自己缺值的那些期 ⇒ 基准列一有空洞，两个数组的**下标就错位**，而这两个量都是按下标配对的
  ⇒ 算出来的是「策略第 100 天 vs 基准第 103 天」，**不报错、只给一个错的数**。
  回测 fixture 里基准零空洞（实测 t3fix 2090 行、ETF 1457 行都是 0 个 null）所以一直没踩到；
  但**影子的基准列真的会缺** —— 2026-08-19 真机上 2/2 行都是 null。
- **为什么影子也要有 beta**：它与回测量的是同一件事，两边各写一份口径，日后
  「回测说赚、影子说亏」就分不清是策略退化还是口径差异（`backtest/index.ts` 头注释那条边）。
- **刻意没做**：**影子面板不给超额收益**（哪个版本都不给）。那一屏的既有决定是
  「不算差值」—— 基准满仓、策略绝大多数时间空仓，两个收益率不在同一个分母上，
  而除法版**并不解决**这个分母问题。加一个没人读的字段也会变成
  `AppSettings.sensitivity` 那种死设置，所以 `ShadowSummary` 里也不留。
- **什么条件下这个结论失效**：若日后影子改成近满仓形态（配置形态），
  「不算差值」这个决定要重新讨论 —— 那时分母问题消失，超额才有意义。

---

## 判据变更：同 regime 的择时零分布换成 **regime 段整段平移**（2026-08-20，`ENGINE_VERSION` **不变**）

**引擎与参数一行没动，任何一笔交易都没变。** 变的是 `audit:random` 的**零点定义** ——
也就是「alpha 配对胜率」这个判据的分母，所以记在这里。预注册（含事先预测）在
[M2 §5.42](./docs/notes/M2-偏差报告.md)，写在跑之前。

- **动了什么**：`--match-regime` 下不再「自动降级回独立抽日」，改用 `REGIME_BLOCK`：
  块 = **(标的, 一段连续同状态行情)**，整段**刚性平移**到同状态的另一段，
  落点必须让每个成员都落在自己的 `pool` 里 ⇒ **吸附距离恒 0 是结构保证**（实测中位 0 / P90 0），
  覆盖 **99.8%**（逐层 ≥ 99.7%）。`--independent-days` 仍可复现旧口径做对照。
- **依据**：§5.36 的老病 —— 建仓月块在同 regime 下只能靠吸附落地（中位 11 / P90 116 根），
  而吸附方向依赖该状态在这只票上的分布 ⇒ **按分层引入偏置**（TREND_UP 90.8 / RANGE 22.3，
  方向相反）。段块把 regime 约束交给「目标段本身就是那个状态」，吸附因此消失。
- **⚠ 结论比「修好了」复杂，这段必须一起读**：四条预注册判据过了两条半 ——
  吸附恒 0 ✅ · 覆盖率 ✅（我预测 60–90%、「TREND_UP 最低」，**两半都错**）·
  三种子方向一致 ✅ 三层 / ❌ TREND_UP（62.0/68.0/71.0 横跨旧值 66.5）·
  **「朝 50% 移动」❌**（ALL 46.5 → 36.5，**远离** 50%）。
  诊断：随机组 **sd 几乎没动**（0.264% → 0.251%）、**均值上移**（RANGE 0.422% → 0.610%）
  ⇒ 两个零点的差别在**成分**不在方差（`findBases` 按位置均匀抽 ⇒ 长段权重更大）。
  ⇒ **不许说「新口径才是对的」**，只能说「换了零点，且它更保守、无吸附」。
- **由此产生的引用纪律**（比改动本身重要）：**配对胜率必须带零点定义**。
  同一个 ALL 三个零点三个答案：46.5%（独立抽日 · 同 regime）· 55.5%（建仓月块 · 无条件）·
  36.5%（段块 · 同 regime），跨度 **19pp**。段块口径下四层是
  **ALL 36.5 · TREND_UP 67.0 · RANGE 43.5 · TRANSITION 26.0**（三种子均值）。
- **刻意没做**：落点改成按**段**均匀抽（消掉长段权重）—— 那是另一个设计而不是 bug 修复，
  看到结果之后立刻改就是「改块定义直到好看」。已登记为待办并要另写预注册（迭代计划 §4.6）。
- **什么条件下这个结论失效**：若日后把落点改成按段均匀抽、或候选池定义再变，
  上面那四个数全部要重出；而**跨口径的任何比较都必须重新做**，不能沿用本条里的数字。

---

## 回测报告：超额收益补上除法版口径 + beta，而 **alpha 与日胜率刻意不做**（2026-08-19，`ENGINE_VERSION` **不变**）

**引擎参数与判定逻辑一行没动，任何一个绩效数字都没变**（同命令重跑 20 只 × 训练窗口：
总收益 −1.99% · 占用 3.50%，与既有基线逐位一致）。纯增量字段，**不参与任何门槛**
（标定排名仍是验证集 Calmar、写回仍是配对 Δ）。

- **动了什么**：`metrics.ts` 新增 `betaOf` / `ratioExcessReturn` / `sampleCovariance`；
  `PerformanceBlock` 新增 `beta` 与 `excessReturnRatio`；报告那两行改成
  `超额 14.06%（除法 16.75%）` 与 `平均资金占用 3.50%  beta 0.0177`。
  老字段 `excessReturn` **保留不动** —— 历史 M2 里所有引用都是减法版。
- **依据**：[M2 §5.41](./docs/notes/M2-偏差报告.md)（拿聚宽公开 API 文档「风险指标」一节的
  通行定义在四份**已有**报告上重算，工具 `scripts/verify/jq-riskmetrics.mjs`）。
  ① **beta 与「平均资金占用」逐份同向**（主池 3.49% ↔ 0.0205 · 单指数 11.52% ↔ 0.0371）
  ⇒ 「超额收益离开占用率会被读反」那条纪律第一次有了净值层面、**无参数**的独立确认；
  ② 减法版超额在基准涨幅大的窗口上给出 **−277.56%**（读不出意思），除法版 **−69.10%**
  = 策略净值只有被动持有的 **30.9%**。
- **刻意没做的两件事**（写进了 `betaOf` 的头注释，防的是日后「顺手补全」）：
  **不算 CAPM alpha** —— 低暴露策略上 `Rf` 支配符号，实测**连符号都翻**
  （单指数 2005–2017：rf=0 → +1.29%/年、rf=4% → −2.57%/年，而事实是 +24.14% vs 被动 +301.70%），
  而 `Rf` 没有可靠的本地来源（与夏普那里 rf=0 同一条理由，只是后果更重：
  一个正的 alpha 会被读成「风险调整后跑赢了」）；
  **不算日胜率** —— 空仓日日收益为 0、基准跌它就「赢」，实测约等于「基准下跌天数占比」
  （单指数 45.9% vs 45.9%）⇒ 零信息量。两个都能在那个调研脚本里按需算出来看，**不进报告**。
- **顺带记一个读反风险**：训练窗口基线的减法超额是 **+14.06pp**，而同一条曲线的
  alpha(rf=0) 是 **−0.28%/年** —— 符号相反。**别引用「训练窗口跑赢基准 14 个点」**，
  它说的全部内容是「那六年基本没投钱」。
- **什么条件下这个结论失效**：若日后策略形态换成近满仓（配置形态），
  日胜率与 EI 类度量会重新变得有意义（[配置形态论证 §9](./docs/notes/配置形态-论证.md) 那张表按暴露分档），
  但**必须在预注册里事先选定**，不许拿它回头去读已经报过的实验。

---

## 回测报告：把「一手都买不起」从静默变成告警（2026-08-19，`ENGINE_VERSION` **不变**）

**引擎参数与判定逻辑一行没动，任何一个绩效数字都没变。** 记在这里的理由与上一条相同：
它改变的是「**报告说不说实话**」，而报告是所有判据的入口。

- **动了什么**：`CodeResult.unaffordable` 新计数器 + 报告告警行（点名标的与次数）。
- **依据**：[M2 §5.40](./docs/notes/M2-偏差报告.md)。回测按 `*Adj`（**后复权**）价成交，
  而后复权锚在上市日 ⇒ 分红送转多的老票后复权价可达真实价的一两百倍
  （平安银行 2018 首根：真实 13.7 元 / 后复权 2069 元 ⇒ 一手 20.7 万 > 默认 `--capital` 10 万）
  ⇒ `lotsAffordable` 返回 0 手，那只票**整段一笔不建仓**，
  而报告上只显示「0 笔」，**与「引擎没给信号」无法区分**。
  实测训练窗口（261 只 · 2018–2023）：**31 次建仓意图被吞掉 / 1097 次建仓 = 2.8%**，
  涉及 4 只（`SZ000001`×13 · `SH600309`×7 · `SZ000858`×7 · `SH600809`×4）。
  **偏差方向不随机** —— 被排除的恰恰是分红历史最长的大盘股。
- **刻意没做的**：**不改手数口径**（M2 §5.40 的候选 B）。那会让全部历史基线失效，
  是另一个决定，用户 2026-08-19 选了「先 A」。
- **什么条件下这个结论失效**：换成按真实价定手数、或默认 `--capital` 改大之后，
  这条告警会自然消失 —— 那时要回头确认 §5.40 里那批标的的绩效是否改变了既有结论。

---

## 判据升级：折间标准误改聚类稳健、择时零分布改块位移（2026-08-19，`ENGINE_VERSION` **不变**）

**这是一次判据变更，不是任何候选的救援。** 引擎参数与判定逻辑**一行没动**。
记在这里的理由：它改变的是「**一个候选算不算通过**」，而这正是本文件在管的事。

时机是刻意的 —— **门口没有任何候选**。等到某天看见 `t = 2.3` 才想起来要不要做相关性调整，
那就是看过数据之后再改判据（2026-08-18 在横截面 S0 的分母上刚踩过同一形状）。
**三处改动都让门槛更难过，不会让任何东西通过。**

- **动了什么（一）**：`pairedDelta` 的标准误从 `stdev(Δ)/√n` 改成**按时间片聚类的 CR1**。
  12 个折单元是 4 个标的子集 × 3 个时间片、从同一次模拟里切出来的，同一片里的子集共享市场
  beta ⇒ 不是 12 份独立信息。等大簇时新式子化简为 `stdev(簇均值)/√G`，
  也就是**横截面折不再贡献 √n**。判据 `minDeltaT` 卡的从此是 `clusteredT`；
  朴素值保留并标注「未调整上界」，报告每次都印调整状态。
  `Resolution` 的「还差多少折」换成「还差多少个**独立时间片**」。
- **动了什么（二）**：`audit:random` 的择时零分布默认从「逐次独立抽日」改成
  **按建仓月整块位移**（block permutation）。`--independent-days` 保留旧口径供对照。
- **动了什么（三）**：报告里的夏普加一句「`×√243` 未做自相关调整 ⇒ 偏大」。**算法不改** ——
  夏普不参与任何门槛，而 Newey-West/Lo 要选截断阶数，那是单独一次改动。
- **依据**：[M2 §5.36](./docs/notes/M2-偏差报告.md)。折间那条有用例直接钉着：
  同一份分数，不给聚类标签 ⇒ **WRITE_BACK**（朴素 t 3.98），给了 ⇒ **KEEP**（聚类 t 1.70）。
  块位移那条三种子实测：**无 regime 匹配时块位移精确**（吸附距离恒 0），
  配对胜率 ALL 46.5 → 55.5 · TREND_UP 36.7 → 43.2 · TRANSITION 24.7 → 38.5
  —— **四层方向一致、都朝 50% 走 = 旧零分布确实太紧**。
- **一处没修好，别当成修好了**：`--match-regime` 的候选池稀疏，块位移只能靠吸附落地
  （实测中位 11 / P90 116 根），会**按分层引入偏置**（TREND_UP 90.8 / RANGE 22.3，方向相反）。
  工具因此在这一档**自动降级回独立抽日**并把原因打印在报告头上。
  ⇒ **同 regime 口径的分位仍是未调整上界**（含看板上那个 TREND_UP 66.5%）。
- **什么条件下这个结论失效**：① 若把折从「同一次模拟切出来」改成「独立重跑」，
  聚类就不再必要；② 若给 `--match-regime` 换一种块定义（按 regime 段整段换）
  并证明吸附距离回到 0，那一档的降级应当撤销。

---

## 补跑不再把「跳过」报成「已推进影子运行」（2026-08-18，`ENGINE_VERSION` **不变**）

**编排层缺陷修复。** 记在这里的理由与下面那条 08-17 的一样：它改变的是
「**某个交易日的前向记录会不会产生**」，而影子运行是「量策略值不值钱」的那把尺子
（docs/07 §2.3 的四条前向纪律）。

- **动了什么**：`engine/settle.ts` 原先把「`shadow.advance()` 没抛错」读成「已推进」。
  而 `advance()` 返回 **null** 表示它自己跳过了（`ALREADY_DONE` 幂等闸门 /
  `ENGINE_VERSION_CHANGED`）。现在按返回值判，并把跳过原因带出来
  （`SettleResult.shadowSkip`），`engine/tick.ts` 的日志把它翻成人话
  （`ALREADY_DONE` 明说「该交易日永久缺失」）。
- **依据**：真机。2026-08-18 08:04 的日志写着「2026-08-17 收盘确认补跑：评估 78 只，
  新落 3 行，**已推进影子运行**」，而实际上 08-17 盘前（修复前的代码）已经写过一行净值
  ⇒ `repo.hasDate('2026-08-17')` 为真 ⇒ `ALREADY_DONE` 返回 null ⇒
  **第 ⑥ 步「用今天的 CONFIRMED 信号挂明天的委托」根本没跑**，而日志说它跑了。
- **为什么这类缺陷最贵**：它不是少做了一件事，而是**让「少做了」这件事不可见**。
  与 08-17 那条「台阶被降级成 L1 的提醒消耗掉」是同一形状 —— 都属于
  「少发/少做的错误，用户发现不了」。
- **代价照实记**：08-17 那一天的影子记录**永久缺失**（影子不补跑历史）。
  当天的 CONFIRMED 只有 3 只 `REDUCE`，而影子账本无持仓 ⇒
  `toShadowAction(REDUCE, holding=false)` 返回 null ⇒ **本来也挂不出委托**，
  所以这一天的经济内容是零。**没有做任何数据补写。**
- **用例**：`tests/unit/main/settle.test.ts` 三条（null 不许报成已推进 / 没有 `lastSkip`
  也要给理由 / 抛错报 `ERROR`）。
- **为什么不递增 `ENGINE_VERSION`**：引擎参数与判定逻辑一行没动，全在 `src/main/engine`。
  递增会作废指标缓存**并让影子运行停止累积**，而影子的净值点现在只有一个。

---

## 强制类台阶只被「真的弹过气泡」的那条消耗（2026-08-17，`ENGINE_VERSION` **不变**）

**提醒层缺陷修复，不是参数标定也不是判定逻辑改动。** 记在这里是因为它改变了
「同一笔浮亏会不会再提醒一次」这件事，而那是 docs/05 §4.2 的规定。

- **动了什么**：`AlertDispatcher.commit()` 里 `lastForcedLoss` 的记账移到
  `if (level === 'L1') return` **之后**。被闸门③（频率上限）④（免打扰）降级成 L1 的
  那条没有弹气泡，从此不再消耗那 2% 的台阶。
- **依据**：不是回测，是真机提醒日志（M3 自用期第 3 天，也是**第一个没有重启过的交易日**）。
  08-17 09:30:05 开盘第一轮屏幕锁定，三只跌破止损线的持仓（浮亏 −7.8%）被降为 L1；
  台阶却记成 −7.8%，于是解锁之后跌到 −8.4% 也再不提醒（要再扩大 2%）：
  当天 **0 条气泡**、**1580 行**「强制提醒台阶：跌幅 −8.3% 未比上次（−7.8%）再扩大 2%」。
  §4.2 那句「既不骚扰又不漏报」当天只剩后半句被打穿，而这是**少发的错误**——用户发现不了。
- **同一轮修的第二件事**：`alert_log` 的判重签名（006）改用离散的 `blockedBy` 取代
  `reason` 文案，判重键从 `code` 改成 `code:signalId`。文案里嵌着连续量（跌幅、剩余分钟数），
  与 `signalSignature` 里 `reasons[0]` 那个坑同一形状；而同一只票同时出 `SELL` 与 `REDUCE`
  时两条交替来比「上一条」，去重整块失效。两个成因合起来让当天 1590 行日志只对应约 6 件事。
- **为什么不递增 `ENGINE_VERSION`**：引擎参数与判定逻辑一行没动，这里全在 `src/main/alerts`。
  递增会作废指标缓存**并让影子运行停止累积**，而影子刚在 08-17 攒下第一个净值点。
- **什么条件下这个结论失效**：如果实测显示免打扰解除后补发的止损气泡变成骚扰
  （例如用户长时间锁屏、解锁瞬间涌来一批），那时该调的是台阶宽度或补发策略，
  **不是把记账挪回去** —— 挪回去等于用漏报换清静。判据是提醒日志里解锁后那一批的条数。
- **验证**：`tests/unit/main/alerts.test.ts` 两条（免打扰 / 频率上限各一条）+
  `alert-service.test.ts` 两条（文案连续量 / 两条信号交替）。四条都在旧代码上先红过。

---

## 时钟校准 + 北京日界（2026-08-15，`ENGINE_VERSION` **不变**）

**这不是参数标定也不是判定逻辑改动**，记在这里是因为它改变了喂给引擎的 `now`，
而引擎的每一条时间相关判定都读它。

- **动了什么**：`now` 从本机系统钟换成「本机钟 + 校准量」，校准量由 HTTP `Date`
  响应头估计（`scheduler/clock-sync.ts`，搭盘中快照请求取样，零额外请求）。
  另把提醒配额的日界与面板/悬浮条的「今天」从**宿主本地日**统一到**北京日**
  （`shared/time.ts`）。设计与两条纪律在 [docs/03 §3.1](./docs/03-数据源与存储设计.md)。
- **依据**：不是回测，是两条可复现的算术。
  ① `src/core/risk/index.ts` 的 `age = input.now.atMs - snapshot.at` 左边是本地钟、
  右边是远端成交时刻，本机快 N 分钟就会把连续竞价里**所有**买入信号判成
  `STALE_SNAPSHOT: SUPPRESS`（`data.staleSnapshotMs` 出厂 5 分钟）。
  ② 本地日界在 UTC−5 上等于北京 13:00，「每日 L2+L3 ≤ 4」会在午盘开盘那一刻重置。
  两条都是**少发/多发都看不出来**的失真。
- **为什么不递增 `ENGINE_VERSION`**：时钟是环境量，参数与判定逻辑一行没动。
  递增它会作废指标缓存**并让影子运行停止累积** —— 影子正等着攒第一个净值点，
  为一次环境修正把它清零是纯损失。
- **什么条件下这个结论失效**：如果三家数据源的 `Date` 头被证明不可靠（例如经过某个
  缓存层返回陈旧时刻），校准量会系统性偏移，那时应改用专门的时间源并重估。
  判据是日志里的校准量长期非零且与 `Snapshot.at` 的下界矛盾。
- **实测**：真启一次，`[clock] 已校准：本机快 571 ms（源 HTTP Date，样本 1）`。
  本机偏差远小于 60s 的提示阈值，横幅不显示 —— 这是预期行为。

---

## `T3_BREAKOUT:BUY` 在 `TREND_UP` 不计票（2026-08-15，`ENGINE_VERSION` `0.2.7-unvalidated` → **`0.2.8-unvalidated`**）

**这是本项目第一次判定逻辑改动，不是参数标定。** 前面所有条目改的是「某个数取多少」，
这一条改的是「哪些信号成立」—— 交易的**集合**变了，所以历史回测报告与新报告**不可逐笔比较**。

- **动了什么**：`src/core/strategies/trend.ts` 的 `T3_BREAKOUT:BUY` 增加条件 `regime !== 'TREND_UP'`。
  `SELL` 方向不动，`RANGE` / `TRANSITION` / `TREND_DOWN` 不动。**不引入任何新数值**
  （结构条件，因此不产生新的 `GUESS` 参数）。
- **论证先于代码**：[docs/04 §3.1a](./docs/04-指标与信号引擎.md)。
  一句话：在已被判定为上升趋势的状态里，「又创新高 + 带宽扩张」不提供新信息 ——
  「在涨」是判定前提，`T4_ALIGNMENT` 几乎必然同时成立（实测 **396/396 = 100%**，已退化成常量），
  而带宽扩张在趋势中段是延续、末端是加速赶顶，日线上分不开。
- **依据**：随机入场基准（`pnpm audit:random`，M2 §5.21/§5.23）。
  改动前 TREND_UP 入场的配对胜率 **19.5 / 27.0 / 29.5%**（三个种子），改动后 **66.5 / 67.0 / 69.0%**。
  验收走 [docs/07 §3.6](./docs/07-回测与验证方案.md) 四条门槛：
  建仓数 −9.6%（≤ 15% ✅）· 建仓级胜率 +0.47pp（不降 ✅）· alpha 上移 ✅ ·
  子集稳定性四个切法同向（−0.73 ~ −1.50pp ✅）。
- **⚠ 绩效数字不作为依据**：全期 −0.94% → −0.09%、净盈亏 −249,777 → −27,208 元。
  砍掉 9.6% 的交易本来就会改善绩效（§5.20 ⑦ 的教训），这一组只作参考。
  **全期仍为负，训练窗口未翻正，21 个 `BLOCKED` 参数一个都没解锁。**
- **什么条件下这个结论失效**：
  ① 若 `regime` 判定口径改变（`TREND_UP` 的门槛松紧变了），这条规则的作用域跟着变，必须重测；
  ② 若 `T4_ALIGNMENT` 的条件改动、使它在 TREND_UP 里不再恒成立，
     「T3 与 T4 同现所以无区分度」这个论点的前提就没了；
  ③ 若标的池再换，四个子集的稳定性数字要重测 —— 它们是池相关的；
  ④ 若日后 `RANGE` 的表现出现无法解释的下滑，要回头查是不是这条改动的间接影响
     （docs/04 §3.1a 列的第一条证伪条件，本次实测未发生：RANGE 建仓 922 → 923、
     配对胜率落在同一区间）。
- **副作用（固定代价，非本次特有）**：参数指纹变 ⇒ 指标缓存整体作废重算 ⇒
  影子运行按纪律停止累积。本次无实际损失（影子账本尚无第一个净值点）。
- 详见 [M2 §5.24](./docs/notes/M2-偏差报告.md)。

---

## 标定池扩到 261 只 + 5 个参数按「读代码」归档（2026-08-14，`ENGINE_VERSION` **不变**）

**没有任何出厂值被改动。** 记在这里是因为它改变了**该怎么读这些出厂值**。

- **标的池** 40 → 264 抽样 / **261 只**拿到日线（`scripts/build-universe.mjs` 的 `take` 98/86/80，
  比例按全市场分层规模；抽样规则未变）。标定要显式给 `--code-folds 28` ⇒ 84 折。
- **依据**：同一份代码、同一套出厂参数，全期（2018-01-01 → 2026-08-13）在旧池是 **+5.93%**、
  在新池是 **−0.94%**；训练窗口（2018 → 2023）**−2.55% / Calmar −0.125**。
  旧池数字为复现（净盈亏 231,154 元与 M2 §5.18 逐位相同），故差异来自池而非流水线。
- **后果**：标定工具的红线「训练集 Calmar ≤ 0 直接淘汰」把出厂值本身淘汰 ⇒
  配对 Δ 全为 null ⇒ 网格裁决只能是 `INCONCLUSIVE`。**docs/09 那 22 个 `GUESS` 在这个池上暂时无法归档。**
- **归档掉的 5 项**（都不改值）：`ma.periods` / `volume.shrinkRatio` / `macd.preset` ⇒ `INERT`
  （分别是：信号层只读 5/20/60 字面量、全仓无读者、纯标签无读者）；
  `data.minBars` / `data.insufficientPenalty` ⇒ `UNTESTABLE`（回测预热 ≥ `fullBars`，分支走不到）。
- **失效条件**：① 若日后把预热下限与 `data.fullBars` 解耦（`--warmup` 钉住），后两项变成可测；
  ② 若给 `ma` 的快/慢/长周期加显式参数（现在是 `trend.ts` 里的字面量 5/20/60），
  `ma.periods` 的惰性结论立即失效；③ 池若再变，②里那两行绩效数字必须重测 —— 它们是池相关的。
- 详见 [M2 §5.20](./docs/notes/M2-偏差报告.md)。

---

## 补跑收盘确认轮（2026-08-14，`ENGINE_VERSION` **不变**）

不含参数标定，但它**改变引擎行为**：收盘确认轮此前**从来没有成功过**，现在会跑了。

### 症状：三张表同时为空

实测用户库（两个交易日、8 只标的）：`signal` 表 769 行里 `CONFIRMED` **0 行**、
`indicator_daily` **0 行**、影子三张表 **全 0 行**。

成因是两个时间窗口对不上：`expectedLastBar()` 在 15:00 之后才把回补目标切到当天，
而数据源发布**个股**日线通常在 15:05–15:30 —— **晚于应用的 SETTLE 窗口（15:00–15:10）**。
15:10 一到 `needsQuotes` 变 false，引擎当天不再跑。于是引擎判的永远是拼出来的临时线，
`stage` 恒为 `PROVISIONAL`，而下面三处**全部以 `CONFIRMED` 为前提**：
`reconcile()`（当日盘中信号永远不会被推进为 CONFIRMED / INVALIDATED）、
`cacheIndicators()`（指标截面永远不落库）、`carryover()`（昨日「明日观察」的次日兑现永不触发）。

日线其实会补进来，只是在**第二天盘前**。

### 做法：换一个「历史行情」适配器，不加任何判定逻辑

`createSignalEngine` 本来就全都会。`src/main/engine/settle.ts` 只是用
`market.getContextThrough(code, D)`（截至 D 的收盘线、不拼临时线、不带快照）
替掉 `getContext`，末根就成了 D 的真实收盘线 → `stage === 'CONFIRMED'`，其余一路照旧。
**刻意一行判定都不写** —— 写了就会与当日流水那条路分叉，而「补跑出来的结论和当天本该
得到的结论不一样」没有任何人看得出来。

三条边界：**不发提醒**（那天已经过去了）、**不接影子运行**（见下）、
**只补上一个交易日**（往前追等于给应用没开机的那些天凭空造出信号历史）。

### 影子运行**刻意还没接**

它的四条前向纪律里有「不补跑历史」，而这里的判断很微妙：D 的 CONFIRMED 信号按影子的
成交模型是**次日开盘**成交 —— 补跑发生在 D+1 盘前时那一刻开盘还没发生，**仍是前向的**；
但用户 D+1 下午才开应用的话，开盘早过了，同一段代码就变成了回填。
要接得先有「成交机会是否已过」的闸门，那是一次独立的取舍。

### 真实数据验证

用户库的一致性副本（`VACUUM INTO`，**不是 `cp`**）跑 `settleDay('2026-08-13')`：
7 只全部评估，新落 3 行 CONFIRMED，另有 3 条当日 PROVISIONAL 被推进 →
`CONFIRMED` **0 → 6**、指标缓存 **0 → 7**，落库行 `created_at` = 北京时间当日 15:00。

---

## 日内做T建议与「明日观察」的次日出口（2026-08-14，`ENGINE_VERSION` `0.2.6` → **`0.2.7-unvalidated`**）

这一轮**同样没有标定任何参数**。递增版本号是因为新增了 `params.tTrade` 参数块 ——
参数集变了，指纹就变了，于是 `indicator_daily` 整体失效重算、影子运行按纪律暂停累积。
这是加参数块的固定代价，不是这次改动的副作用。

### 起因：一次实测，「从来没有买入建议」有两个成因，都不是数据问题

按实盘口径把引擎重跑了 46 只 2024-01-01 起的真实日线（28973 个判定根）：

| | 收盘确认轮 | 盘中轮（11:30 临时线） |
|---|---|---|
| `BUY` | **0** | 157（0.54%） |
| `NEXT_DAY_WATCH` | **182**（0.63%） | 0 |
| `SELL` | 180 | 166 |

① **收盘轮的买入 100% 被改写。** 组合层给出 182 次 `BUY`，`T1_LATE_BUY:DOWNGRADE`
**也正好是 182 次** —— 一次不漏。成因在 `engine.ts` 的 `minuteOfDayFrom()`：
它把 `minutesSinceOpen` 钳在 240，于是**收盘后任何一轮算出来的 `minuteOfDay` 恒等于 900**
（15:00），而 T+1 尾盘窗口是 890–910。收盘确认轮永远落在窗口里。
那条改写本身是对的（收盘之后确实买不进），缺的是**后半句**。

② **「明日观察」到第二天没有任何跟进**，那条建议就此消失。加上悬浮条取「当日最后一条
未静默信号」（收盘轮那条必然最后），上午真出过的买入也会被它盖掉。

### 新增：昨日「明日观察」的次日兑现（`SignalOutcome.carriedOver`）

- 判据是**两次独立成立**，不是把昨天那条搬过来：昨日收盘那条已 `CONFIRMED` 且方向为
  `NEXT_DAY_WATCH`，**并且今天自己也判出 `BUY`**。隔夜跳空与消息面都可能让它不再成立，
  照搬等于用一根过期的 K 线下结论。
- **不新发提醒**，而是把今天那条买入抬到 L2 并在第一条依据里写清来历。
  一天两条说同一件事只会让人觉得吵；而两次独立成立确实比一条孤立的盘中买入更值得一看。
- 「昨天」取的是**该股自己上一根 K 线的日期**（停牌、节假日天然处理），不是日历减一天。
- 四道闸门 + 一天只报一次，逐条见 `engine/signals.ts` 的 `carryover()`；
  8 条用例在 `tests/unit/main/signals.test.ts`。

### 新增：日内做T建议（`params.tTrade`，`core/risk/intraday-t.ts`）

**只对持仓给、只在盘中给、不发提醒、不改 `direction`、不落库、不进回测与影子运行。**

- 判据只用快照本来就带的四个数（今日最高/最低/现价/昨收）：
  振幅 ≥ 3% 且现价落在振幅的 ≥80%（高抛）或 ≤20%（低吸）。
- **尾盘不给低吸**（沿用 `risk.lateBuyCutoffMinutes`）：T+1 下今天买的明天才能卖，
  过了那条线再买进来就是加仓，而用户以为自己在做T。高抛不受此限。
- 持仓强制类（止损/减仓）或硬抑制命中时**不给** —— 那两种场合并排一句「可考虑高抛」
  自相矛盾，或者会把要紧的事挤掉。

⚠ **`tTrade` 三个数在 `params-view.ts` 里属 `UNTESTABLE` 而不是 `GUESS`**，差别是硬的：
`GUESS` 是「还没跑网格」，而这三个是**日线回测原理上测不到**的 ——
一根日线只有开高低收，不知道当天先到高点还是先到低点，「日内位置」在日线上没有对应量。
**不要把它们放进标定网格**：扫出来的「最优值」是在一个不存在的量上取的极值。
依据只能来自实盘使用，与 `alert.bubbleScore` 同一档。

失效条件：若日后接入分钟级历史数据并能复现日内路径，这三个数就变成可标定的，
届时应从 `UNTESTABLE` 移出并按 M2 清单 4.9a 走标定流程。

### 修正：`signalSignature` 里混进了一个连续量，一天多落 56 倍的行

查上面那个问题时在真实库里撞见的。**签名的头注释写着「只放离散量，一个连续量都不许进来」，
而 `gated.reasons[0]` 违反了它** —— 它是一句文案，止损那条写的是
「已亏损 −32.7%，触及 8% 止损线」，每 0.1pp 就是一个新字符串。

实测（SZ002716，跌破止损线后被强制通道接管，2026-08-14 一天）：
子信号集合 1 种、裁决集合 1 种、level 1 种、方向 1 种，
**而 `reasons[0]` 有 22 种，落了 243 行**。
（243 > 22 是因为去重比的是「上一次」而不是「见过的集合」：
−32.7% → −32.6% → −32.7% 来回抖，每一次都算「变了」。）

**已从签名中移除。** 它不会漏掉任何该落的新行 —— 能表达的离散信息已经全在
`subs`（子信号集合）与 `verdicts`（风控裁决）里，它独有的那部分恰恰全是连续量：
百分比、以及「谁最强」这个由 `score × weight` 决定的排序。
两天 769 行按新签名回放**只剩 13 行**。

连带修掉一个它造成的、更难发现的后果：`signal:history` 是
`ORDER BY created_at DESC LIMIT 200`，那 664 行让当天的窗口只覆盖 12:38–14:00 这 82 分钟，
**上午的 377 行在面板与悬浮条上根本不存在**，而界面不会报错。
`SignalQuery` 因此新增 `perCode`（`ROW_NUMBER() OVER` 按票各取最近 N 条），
面板传 20、悬浮条传 3。根因修掉后行数已经很少，但 100 只自选仍会压到 200 的边缘 ——
这道闸门是把「一只票能不能挤掉别的票」从**能**改成**不能**，不是那个 bug 的补丁。

---

## M4 · 影子运行与灵敏度接线（2026-08-13，`ENGINE_VERSION` **不变**）

这一轮**没有标定任何参数**，`ENGINE_VERSION` 仍是 `0.2.6-unvalidated`。
记在这里是因为其中两件事**会改变引擎行为**，符合本文件的收录标准。

### `AppSettings.sensitivity` 从死设置变成真的会换参数

在此之前它在 schema 里有、**没人读** —— 与 `autoLaunch` 曾经的问题同一类，
但更隐蔽：开机自启不生效用户能发现，灵敏度不生效发现不了。

- 三档预设从 `src/backtest/args.ts` **搬进 `src/core/params.ts`**（`SENSITIVITY_PRESETS`），
  回测的 `--sensitivity` 改为重导出。两边共用同一张表，
  否则「回测里的均衡档」与「设置里的均衡档」会悄悄分叉。
- 新增 `withSensitivity(tier, base)`：**只动 `combine.scoreThreshold` 与
  `combine.voteThreshold`**，其余一律保持出厂值。刻意窄 ——
  档位是「要多少信号」的旋钮，不是「换一套策略」；哪天想让某一档顺带放宽 `risk`，
  那是新增机制、要单独标定，不能藏进这个函数（否则「我把灵敏度调低了怎么止损也变了」无从解释）。
- **换档 = 换参数指纹 = 换 `engineVersionOf()`**，于是：`indicator_daily` 整体失效重算、
  历史 `signal` 与新信号不可横向比较、影子运行暂停累积。这三件事都是必须的，
  不是副作用。UI 上先弹确认框说明这个代价。
- **均衡档与出厂参数逐位相同**（用例钉着 `engineVersionOf(withSensitivity('BALANCED')) ===
  engineVersionOf(DEFAULT_PARAMS)`）—— 默认档不该悄悄换掉引擎版本。
- **三档本身未标定**：`BALANCED` 恰是出厂值（已上网格、裁决 KEEP，M2 §5.15），
  另两档**一格都没跑过**。UI 上因此只说「出信号更多 / 更少」，不许说「更准」或「更稳」。

### 影子运行（docs/07 §2.3）落地 —— 前向绩效记录

schema **v1 → v2**（`002_shadow.sql`：`shadow_order` / `shadow_position` /
`shadow_trade` / `shadow_equity`）。它不改引擎的判定，但它是此后**评价**引擎的依据，
所以四条口径写在这里，将来读到影子数字的人要能查到它们是怎么算出来的：

1. **成交价用次日开盘**（与 `backtest/simulate.ts` 同源）。T 日收盘确认的信号在 T+1 开盘成交。
   用当日收盘价会凭空多出一段收益 —— 15:00 之后的收盘确认轮里那个价格已经买不到了。
2. **只吃 CONFIRMED。** 盘中 PROVISIONAL 会随临时 K 线抖动、还可能被收盘轮判 INVALIDATED；
   按它下单等于把**提醒层的抖动**记成**策略的绩效**。
3. **成本照扣**，直接复用 `backtest/costs.ts`（双边佣金 + 印花税 + 过户费 + 0.1% 滑点）。
4. **风控硬抑制的信号不进影子；被提醒闸门挡掉的照样进。** 前者是「无执行意义」，
   后者是「今天不打扰你」—— 影子量的是策略，不是提醒策略。

**记账**：一个现金池（起始 100 万）+ 每笔固定名义金额（10 万，与回测 `capitalPerCode` 对齐）。
不按标的分账（自选随时增删会让净值曲线的分母自己在动）。现金不足时不开仓并计
`skippedNoCash` —— 那个计数**必须显示**，静默跳过会让「信号密集期的收益」凭空消失。

**引擎版本一变立刻停止累积**，不是继续。混进去的曲线不属于任何一套参数，
而它**无法事后拆开**：历史 K 线能重算回测，重算不出前向记录。

**这个结论什么条件下会失效**：上面的绩效口径假设「一只标的同时只有一个仓位」
（与回测 M2 §3.3 相同）。若将来允许同标的多仓并存，建仓级胜率的归并键
（`code + entryDate`）会静默地把两个仓位合成一次建仓 —— 那时要改成显式持仓 id。

### 顺带修掉的一处取数隐患

`KlineRepo` 新增 `recentThrough(code, through, limit)`。影子推进原先用 `recent(code, 2)`
取「今天这根 + 昨收」，它隐含「库里最新那根就是今天」这个假设 ——
只要库里存在比目标日更新的行（跨日唤醒后一次补多天），拿到的就是错的那两根，
**症状是「委托莫名不成交」，从净值上完全看不出来**。写用例时才发现。

## ENGINE_VERSION 0.2.6-unvalidated（2026-08-12）

### 算法口径修正（无参数标定）

**`R2_REVERT_TO_MID` 的两个条件改写**：`当日 close > MID` → `当日 crossUp/crossDown(close, MID)`，
`HIST 转正/转负`（过零）→ `HIST 朝有利方向`（软确认）。

- **依据**：[M2-偏差报告 §5.11](./docs/notes/M2-偏差报告.md)，新工具 `pnpm audit:subsignals`
  对三项合取逐项计数（39 只 × 61534 根判定日）。
- **为什么改**：旧写法两项都错，方向相反 ——
  `close > MID` 在 **49%** 的判定日上成立，放在合取里等于没有这个条件；
  `HIST` 过零（= `DIF` 上穿 `DEA`）是慢事件，在一次收盘破轨之后到过零的中位数间隔
  **35 根以上**，而 `revertLookback` 只有 2–5 根，两者时间常数差一个量级。
  合起来的结果是 R2 在 61534 根上只触发 **90 次（0.15%）**，且对唯一的参数
  `revertLookback` 逐位惰性（取 2/3/5 回测结果完全相同）。
  改后触发 **213 次（0.35%）**，`revertLookback` 变成活参数且单调（3→213、5→592、11→1807）。
- **这不是一次绩效改进，别当成绩效改进读**：全期（39 只，2018-01-01→2026-08-11）
  769 笔 / +5.93% / 夏普 0.543，对比旧口径 762 笔 / +6.25% / 0.573，**略差**
  （在本项目一直用的 ±1pp 噪音带内，读作「测不出差别」）。
  `revertLookback` ∈ {3,5,8,12,20} 的验证集 Calmar **逐位相同**（全 0.084）。
  保留的理由是口径正确（穿越是事件，与 docs/04 §2 的穿越纪律一致）+ 参数从死变活。
- **这个结论什么条件下会变**：改写把约束从子信号层推到了组合层 ——
  213 次触发里 **185 次（87%）是孤立的**（同一根上没有同方向的其他均值回归子信号），
  因为 R2 要求价格在中轨附近而 R1/R3/R4 要求偏离中轨，几何互斥。
  而 `combine.voteThreshold.meanReversion = 2` 要求同策略两票，
  **R2 在结构上无法自己促成一次触发**。票数线一改（属 `combine` 块标定，未做），
  上面所有绩效数字都要重测。

### 引擎行为

- `indicator_daily` 缓存按新版本号整体失效重算。
- 历史 `signal` 行带 `0.2.5-unvalidated+…`，与新信号**不可横向比较**。
- 后缀仍是 `-unvalidated`：这一版没有标定任何参数，出厂已标定清单仍只有
  `strategy.squeezeBbwPct`。

---

## ENGINE_VERSION 0.2.5-unvalidated（2026-08-12）

### 参数标定

**`strategy.squeezeBbwPct`：10 → 20**（首个被标定的出厂参数）

- **依据**：[M2-偏差报告 §5.10](./docs/notes/M2-偏差报告.md)。
  两轮网格（`params/grid-mean-reversion.json` 27 个候选 + `params/grid-mean-reversion-squeeze.json`
  10 个候选）× 两种基线（只留均值回归 / 两个策略都留），39 只宽基样本，
  训练 2018-01-01→2023-12-31 / 验证 2024-01-01→2025-06-30，按验证集 Calmar 排名。
- **为什么改**：原值 10 是 docs/04 §1.4 转述的「BBW_PCT < 10 是变盘前夜」，
  它把 R3 卡得几乎不出手，使均值回归策略的边际贡献只有 +0.14pp（噪音带内），
  两次被提议砍掉。放宽后该策略的边际贡献变成 **+2.0 ~ +3.4pp 收益 / +0.11 ~ +0.24 夏普 /
  回撤不变**（全期消融，2018-01-01→2026-08-11：只留趋势 459 笔 +2.84% 夏普 0.33，
  取 20 则 762 笔 +6.25% 夏普 0.57）。
- **为什么是 20**：5 与 10 在两种基线下都被验证集红线淘汰（验证集年化 < 0）；
  16–30 是一整片通过的高原，不是噪音尖峰；20 是高原上唯一在训练集与验证集上都靠前的值。
  按验证集 Calmar 单独排名，工具的首选是 30，但 30 在只留均值回归的口径下明显更差
  （0.16 vs 20 的 0.36），说明它靠的是与趋势策略的交互，证据更薄。
- **这个结论什么条件下会失效**：
  - 样本是同一批 39 只有幸存者偏差的票；同期基准 +14.10%，取 20 的全期超额是 **−7.85%**。
    这条标定说的是「R3 的压缩线该放宽」，**不是**「这套策略能跑赢基准」。
  - 测试窗口在那一轮被触碰四次（`--grid` 每次调用都对该次优胜者跑一遍测试集，
    而扩网格重跑了一轮 × 两种基线），docs/07 §3 ④ 的「测试集只跑一次」已不严格成立，
    结论只按训练集 + 验证集读。
  - 同轮发现的两条**结构**问题未修，它们会影响后续对 R1/R2 的任何标定：
    `R2` 对 `revertLookback` ∈ {2,3,5} 逐位惰性，`R1` 的 RSI 带宽在收紧方向上近乎惰性。

### 为什么仍带 `-unvalidated`

只有一个参数被标定，另外二十来个仍是转述值。摘掉后缀会让回测报告不再打「参数未标定」
提示（`src/backtest/report.ts` 的 `unvalidatedParams`、M2 清单 5.4），等于用一个参数的证据
给整套参数背书。摘后缀的条件见 M2 清单 4.9 的「标定完成」一档。

### 引擎行为

- `indicator_daily` 缓存在下次启动时按新版本号整体失效重算（K 线不重拉）。
- 历史 `signal` 行仍带 `0.2.4-unvalidated+…`，与新信号**不可横向比较** —— 这是版本号存在的目的。

---

## ENGINE_VERSION 0.2.4-unvalidated（2026-08-12）

### 组合层瘦身（无参数标定，属结构变更）

- **删除 `params.weights` 整张策略权重表**，两个策略改为等权。两轮对照（第一轮带口径 bug、
  第二轮解耦后）都测不出动态权重优于固定权重，差值在 ±1pp 内正负交替。
  见 [M2-偏差报告 §5.5–§5.8](./docs/notes/M2-偏差报告.md) 与
  [docs/08 关键决策点 2](./docs/08-开发路线图.md)。
- **`combine.voteThreshold` 改为按策略给**（`{ trend: 3, meanReversion: 2 }`），
  触发判据是「任一策略达到自己的线」而非两边票数相加。均值回归只有 4 个子信号，
  与趋势共用一条线对它系统性不利。
- 这两处的取值本身**仍未标定**。

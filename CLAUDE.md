# CLAUDE.md

本文件为在此仓库工作的 AI 助手提供上下文。**设计细节以 [`docs/`](./docs/README.md) 为准，本文件只讲「动手前必须知道的事」。**

## 这是什么

蹲点（2026-08-14 由 GP Pet 改名）：Windows 桌面悬浮条 + 托盘，跟踪用户自选的 A 股，用量化策略判断买卖时机并以不打断工作的方式提醒。
**改名只改了显示名与应用身份**（`productName` / 窗口标题 / 托盘 / `APP_ID` / 安装包文件名）——
`package.json` 的 `name` 仍是 `gp-pet`，所以**数据目录仍是 `%APPDATA%/gp-pet`**，
`CONFIG_BUNDLE_FORMAT` 也仍是 `gp-pet-config`（改了会让改名前导出的配置一律导不进来）。
理由与代价见 [`src/main/identity.ts`](./src/main/identity.ts) 头注释：换 APP_ID 丢的是任务栏归组，换 name 丢的是数据。
Electron + React + TypeScript · 本地 SQLite · 免登录 · 无服务端 · **不接券商、不下单**。

**⚠「不下单」是当前阶段的约束，不是永久定位**（2026-08-17 用户确立）。
终局是一个能协助个人资本做投资配置与自动化管理的**自动交易**个人量化软件；
现在处在阶梯的 **L0**（判定 → 提醒 → 人决策人下单），而这条阶梯**每一级都有可检验的进入条件** ——
完整定义在 [迭代 skill](./.claude/skills/迭代/SKILL.md) 的「这个系统最终要长成什么样」。
三条不因演进而放松：① **L0–L2 期间「不接券商、不下单」完全有效**，到 L3 才是重新讨论它的时机
（前提是 L2 三个条件做完，不是「先接上再慢慢调」）；② 下面五条约束在任何一级都有效 ——
自动化提高的是执行速度，**不降低证据标准**；③ **越自动，错误越贵**，每升一级验证强度只能提高。
目前卡在 L2 的三条上，而**第一条 2026-08-26 换了判据**（用户拍板，
[当天的决策记录](./docs/notes/2026-08-26-决策记录.md)）：
从「训练窗口 Calmar > 0」换成「**训练窗口 `ALL` 层中位配对胜率 > 50%**」，零点预承诺。
理由**与个股能不能过线无关**（这是它与移动球门的分界线）：① 旧口径可被「少做」机械满足
—— 回撤只有 2.96% ⇒ 它等价于「总收益转正」，而**砍掉 `TREND_UP` 一层就够**
（207 次 / −170,680 元 / 占建仓 12.4%）；② 它与读数纪律第 1 条（「判据是 alpha 配对胜率，
不是绩效」）冲突。**去掉绩效那一半是安全的，因为条件③（GH1）已经承担它且同风险匹配 ⇒ 对「少做」免疫。**
⚠ **`calibrate.ts` 的红线一个字没动** ⇒ **21 个 `BLOCKED` 仍标不了，条件② 不受影响，
这次改动不解锁标定**（[困境说明 Q3](./docs/notes/困境说明-2026-08-17.md) 只结了另一半）。
**当天就跑了新判据**（[M2 §5.61](./docs/notes/M2-偏差报告.md)，基线逐位复现 −1.99%、口径可核对）：
**`ALL` 中位配对胜率 8.5%，差门槛 41.5pp ⇒ 不通过**，而且**四个最大层自己全都 ≤ 45%**
（RANGE 45.0 · R3+R4 44.5 · TREND_UP 37.5 · TRANSITION 0.0）⇒ **没有任何收窄能救**，
（⚠ 那一组数是 **`trials = 200`** 的抽样；默认 2026-08-27 已提到 **2000**，同一份基线新精度下是
**`ALL` 10.55 · RANGE 51.40 · TREND_UP 45.35 · TRANSITION 0.85** —— **裁决不变**，
差门槛 39.45pp，但**两组数不许混着引用**，见 [§5.76](./docs/notes/M2-偏差报告.md)）
这正是换判据换来的东西。顺带定位到：「全池唯一赚钱的引擎块」`R3+R4` 全期 +189,632 元，
**训练窗口只有 −16,093 元** ⇒ 那笔钱落在训练窗口之外。
⚠ 报告里有五个**窄分层**中位 > 50%（32–301 次建仓），**一条都不许拿去立候选**（§5.61 三条理由）。
另注意「自我进化」**不等于自动调参** —— 那是过拟合的定义，区分见 skill 里那张表。

当前处于 **M4（打磨）代码就绪**：五层引擎、回测 CLI、标定工具、面板列表（M2）、
提醒分发（M3：`tick → SignalEngine → AlertService → 四道闸门 → alert_log + 状态点 / 气泡`）之上，
M4 补齐了**影子运行**（前向模拟绩效，schema v2）、**设置页全项**（含只读参数表）、
**数据库周期备份**、**首启免责声明引导**与 **Playwright E2E**（11 条，真启 Electron）。
**M3 的出口条件（自用一周）与 M4 的三项真机验收都未做** ——
逐条见 [M3 验收](./docs/checklists/M3-提醒层验收.md) 与 [M4 验收](./docs/checklists/M4-打磨验收.md)。
M4 剩下的是：125%/150% DPI 走查、**装一遍 / 卸一遍**、代码签名（无证书）——
`pnpm package` 本身已经真跑过，产物能起来且没走回退驱动（见 M4 清单 §5）。

**2026-08-19 补了四处「看不见」**（`ENGINE_VERSION` 不变，绩效数字一个没动，见 [CHANGELOG](./CHANGELOG.md)）：
① 日报当天永远不定稿 → 收盘后 15:10–16:00 的**只补日线**窗口；
② 影子看不出在不在跑 → `shadow_journal`（013）逐日操作流水 + 面板运行状态；
③ 重启即失忆 → 行情展示两级回落 + **提醒闸门状态落库**（推翻了 `dispatcher.ts` 原来那条「刻意不落库」）；
④ 影子净值的基准列 2/2 行为 null → 事后回填 + 腾讯 provider 认识「指数无复权」。
四件事同一类：**系统在按设计工作，而用户没有办法看出来**，或者状态在看不见的地方悄悄丢了。

**影子运行是「量策略值不值钱」的那把尺子，它的可信度全靠「真的是前向的」这一条。**
所以 `src/main/shadow/` 里写死了四条纪律：不补跑历史（补出来的叫回测）、
成交价用**次日开盘**（当日收盘价在 15:00 之后已经买不到了）、成本照扣（复用 `backtest/costs.ts`）、
只吃 CONFIRMED（盘中信号会抖、还可能被收盘轮判失效，按它下单等于把提醒层的抖动记成策略绩效）。
**引擎参数一变立刻停止累积**，不是继续 —— 混进去的曲线不属于任何一套参数，而它无法事后拆开。
被提醒闸门挡掉的信号**照样**进影子组合（影子量策略，不量提醒策略），
但**风控硬抑制的不进**（那是「无执行意义」）—— 这两句看着像一回事，是相反的两个决定。

**⚠ 2026-08-28 补了一个结构性缺口：影子持仓此前基本没有离场通路**
（[CHANGELOG](./CHANGELOG.md) · [docs/07 §2.3c](./docs/07-回测与验证方案.md)。
`ENGINE_VERSION` 不变，**回测数字逐位不变**，用出厂口径训练窗口基线重跑对比过）。
病根：闸门那条持仓强制通道（止损/移动止损/回撤减仓/盈利保护）读的是**用户手工录入的
`position` 表**，而 `positionVerdict()` 第一句就是「没有持仓 → null」⇒ **影子持有、
用户没录入的票，那四条规则一条都不会触发**；`REDUCE` 只由那条通道产出 ⇒ 对这类票
**永远不可能出现**；即使用户也持有同一只，判据用的还是**用户的成本价与峰值**。
而回测里 **96.9% 的离场由风控触发**（M2 §5.24）⇒ 缺了它，**影子量的不是回测那套策略**。
旁证：`peakRaw` 一直在逐日维护并落库（那正是移动止损的输入），修复前**没有任何地方读它**。
修法是把那四条抽成窄入参 `forcedExit()`（纯重构，**规则实现仍只有一份**），
影子按 `entryPriceRaw` / `peakRaw` / 当日 `close` 调一次（`shadowExitOrder`）。
⚠ 三条别踩：**绝不设 `stopFloor` / `lockedShares`**（那会把「我扛住了没卖」记成策略绩效）·
**口径全走不复权**（混进复权价会在除权那天凭空止损）· `hardSuppressions` 那一整套
**刻意只留「停牌」一条**（逐条理由在 `shadowExitOrder` 头注释，别顺手补全）。
⚠ **用户拍板不清空重来**，判据是修复前 `shadow_trade` **一行都没有**
（实测 9 个净值点 / 7 只持仓 / **0 笔往返成交**）⇒ 第一段里不存在带着旧口径的成交
⇒ 「混进同一条曲线拆不开」在那份数据上不成立。分界点落在 `SHADOW_KEYS.exitRulesFrom`
+ 一行 `RULES_CHANGED` 流水 ⇒ **引用影子绩效必须带上分段日期**。
（那条「参数一变就停止累积」的纪律**防的是事后拆不开**，记下分界点恰好把它拆得开。）

**`AppSettings.sensitivity` 到 M4 之前是死设置**（schema 里有、没人读），与 `autoLaunch` 曾经的
问题同一类，但更隐蔽：开机自启不生效用户能发现，灵敏度不生效发现不了。现在
`withSensitivity()`（住在 `src/core/params.ts`，与回测 `--sensitivity` **共用同一张表**）
真的重建引擎参数 —— 于是**换档会递增引擎版本 → 指标缓存作废重算 → 影子运行暂停**。
这不是副作用而是必须如此，UI 上先弹确认框说明这个代价。

**设置页不给参数编辑框**（2026-08-13 的取舍，docs/01 §5.5 已改）。只给一张逐项标注标定状态的
只读表（`src/main/settings/params-view.ts`）。判据是约束 2：真正标定过并写回的**只有一项**，
一张不分档的表会让二十来个转述猜测看起来同等可信。
`tests/unit/main/params-view.test.ts` 钉住「`CALIBRATED` 恰好只有 `strategy.squeezeBbwPct`」——
往那一档加行就是把一个参数的证据扩张成整套参数的背书，要加得走 M2 清单 4.9a。

**闸门是唯一的点亮路径。** 状态点只认 `PetStateMachine`，而它只接受
**过了四道闸门**的提醒（被降级成 L1 的算过了，被丢弃的不算）。不要在渲染层直接读
`signal:history` 去点亮状态 —— 那是一条绕过闸门的旁路。`tests/unit/main/alert-service.test.ts`
有两条用例钉着这件事。

**⚠ 形态是「托盘 + 悬浮条」，桌宠与皮肤系统已整块删除**（2026-08-13）。常驻置顶窗口只有
一种形态：`OverlayWindow` 加载 `src/renderer/bar/`，300×38。跟着一起删掉的有
`src/main/skin/`、`src/renderer/pet/`、`resources/pet/`、`tools/asset-build/`、docs/09、
`docs/skins/`、`pnpm assets:build` / `verify:assets`，以及 `AppSettings` 的
`appearance` / `skin` / `minimalMode` 三个字段。**产品因此不依赖任何美术素材**；
仍需图片的只剩托盘与应用图标（`resources/icons/app/`，静态文件，缺失时退到内置兜底图标）。
这**不是** docs/08 决策点 1 那条退路被触发（C1/C2 还没真机验收），是产品选择。
IPC 通道名、`PetState`、`PetStateMachine` 的名字仍带 `pet`（改名要动 preload 与渲染入口，
无功能收益）—— **名字是历史，形态是现状**。见 [docs/06 §2.1](./docs/06-桌宠交互与非干扰设计.md)。

**⚠ 提醒的可见出口只有气泡**（2026-08-13）。托盘角标、托盘图标闪烁、系统通知
（连同「可发声」这件事本身与 `AppSettings.soundEnabled`）全部移除，`AlertChannel`
只剩 `PET`（状态点）与 `BUBBLE`。**级别没有合并** —— L2 与 L3 表现相同，但同键冷却
（2h vs 当日一次）与每日上限不同，那才是三级制在管的事，别因为 `CHANNELS_BY_LEVEL`
里两行长得一样就去合并它们。历史 `alert_log` 行里仍有 `TRAY` / `OS_NOTIFY`，
`parseChannels` **刻意不做白名单过滤**：滤掉会让当时真弹过的行看起来像被静默了。

**两条出口条件尚未达成**：M1 的「跑满一个交易日 + 健康度 > 99%」要真机联网；
M2 的「回测报告产出并据此确定出厂参数」还差大半 —— 但**它的读法已经明确**（2026-08-13）：
「确定」有三种合法结局，写回新值 / 已测保持出厂值（裁决 `KEEP`）/ 已判惰性，三种都算确定；
另有四个数（`provisionalDiscount`、`alert.bubbleScore`、`lateBuyCutoffMinutes`、
`staleSnapshotMs`）是**日线回测原理上测不到**的，归影子运行与 M3，不属于这条出口条件。
（M3 接线后，`alert.bubbleScore` 的依据来源已经有了：提醒日志里「今天发出的几条值不值得
被打断」，见 [M3 验收清单 §4](./docs/checklists/M3-提醒层验收.md)。判据是日志，不是回测 Calmar。）
目前 62 个叶子参数（2026-08-15 实测）：`CALIBRATED` **1** · `KEPT` **16**（`adx` 4 +
`regime` 2 + `combine` 4 + `risk` 6；其中 `adx.baseThreshold = 20` / `volScale = 8`
**有正面证据** —— ±2 邻域里每个方向都更差，两个邻居还被训练集红线淘汰）·
`INERT` **13**（`multiTf` 整块 7 个 + 6 个零散）· `UNTESTABLE` **11**（含 `tTrade` 3 个）·
`BLOCKED` **21** · `GUESS` **0**。

**⚠ `GUESS = 0` 不等于「都测过了」，恰恰相反。** 2026-08-15 新增了 `BLOCKED` 档
（`macd`/`boll`/`rsi`/`volume` 与 `strategy`/`regime`/`data` 的零散项，共 21 个）：
**网格跑过了，但出厂值自己被「训练集 Calmar ≤ 0」淘汰 ⇒ 裁决必为 `INCONCLUSIVE`**。
它与 `GUESS` 必须分开，因为**后续动作相反**：`GUESS` 是去跑网格，
`BLOCKED` 是**先让基线转正**再重测（把两者混成一档已经制造过一次「同一张网格跑两遍」）。
摘 `-unvalidated` 的条件因此是 **`GUESS` 与 `BLOCKED` 两档都为 0**，
`params-view.test.ts` 有两条用例钉着这件事。
`src/core/params.ts` 里的数值因此仍整体是未标定的初始猜测（见约束 2）。
**以 `src/main/settings/params-view.ts` 的 `STATUS` 表为准**（有用例钉着），
这里的概述会过期 —— 补完计划见 [docs/09](./docs/09-下一阶段开发计划.md)。

**⚠ 标定这条主线 2026-08-14 撞上一个硬结论：出厂参数在一个有代表性的标的池上是亏钱的。**
标的池按 docs/09 B0 扩到 264 抽样 / **261 只**拿到日线（`--code-folds 28` ⇒ 84 折）之后，
同一份代码、同一套出厂参数：全期 **−0.94%**（40 只旧池是 +5.93%，且旧池数字**复现无误**），
训练窗口 2018→2023 **−2.55% / Calmar −0.125**。于是标定工具按红线「训练集 Calmar ≤ 0 直接淘汰」
把**出厂值本身**淘汰掉 —— 所有配对 Δ 都是 null，**每张网格的裁决只能是 `INCONCLUSIVE`**（退出码 2）。
**这不是工具坏了**，别去改 `calibrate.ts` 的红线；也**别再引用 40 只池上的 +5.93% / +231,154 元**
说明「策略赚钱」。亏损弥散（147 只负 / 103 只正，中位数 −2.58%）且集中在
**TREND_UP 的买入**（1245 笔 / 胜率 39.39% / −385,997 元，而 RANGE 是 +152,221 元），
96.9% 的离场由风控触发。全部数字与三条可选方向见 [M2 §5.20](./docs/notes/M2-偏差报告.md)
与 [docs/09 开头](./docs/09-下一阶段开发计划.md)。

**数据现状（2026-08-12 实测）**：三家接口都能用。**eastmoney 不是被封，是间歇性的**
—— undici（应用用的客户端）成功率约 78%，失败症状 `other side closed`，重试能过；
而 curl 对它 100% 失败（客户端指纹差异）。**别再用 curl 的结果判断这个接口的可用性。**
历史日线用 `pnpm fetch:history` 从腾讯拉（不复权 + 后复权两轨，自带重试），
落成回测 fixture，**不需要先跑通 `pnpm dev`**。
所以标定卡的不再是数据，而是「标的池怎么选」与「结论谁来复核」。
**腾讯那个接口会按 host 把本机拦掉**（HTTP 501 + JS 挑战页，2026-08-14 实测 `--concurrency 4`
拉到第 99 只就被拦，十分钟不恢复）。脚本里备了三个门（`web.ifzq` / `ifzq` / `proxy.finance.qq.com`，
同源、`hfqday` 逐位相同），撞到 501 自动换门；**并发上限已压到 2**，且改成了逐只落盘
（原先是全拉完再统一写，被拦那次前 98 只一起丢了）。判断它恢复没有只能拿**同一个 URL** 再探。

**⚠ 组合层已经瘦身完毕（2026-08-12），形态定了**：
- **`params.weights` 整张表删除，两个策略等权** —— 两轮实测都看不出动态权重有效
  （解耦口径后差值仍在 ±1pp 内正负交替），按 [docs/08 关键决策点 2](./docs/08-开发路线图.md) 砍掉。
- **`combine.voteThreshold` 按策略给**（趋势 3 / 均值回归 2 —— 后者只有 4 个子信号，
  共用一条线对它系统性不利），触发判据是「**任一策略达到自己的线**」而不是两边票数相加。
- **两个策略都留下**，`enabledStrategies` 出厂两项皆 `true`。当前 `ENGINE_VERSION` `0.2.8-unvalidated`
  （0.2.8 = **第一次判定逻辑改动**：`T3_BREAKOUT:BUY` 在 `TREND_UP` 不计票，
  见下面那两段与 [CHANGELOG](./CHANGELOG.md)）。

**「均值回归要不要砍」这一问已经有答案了：留下。** 它此前测不出贡献（+0.14pp，噪音带内），
根因是 **`strategy.squeezeBbwPct` 的出厂值 10 太紧**，把 R3 卡得几乎不出手 ——
那个 10 直接来自来源文档的「BBW_PCT < 10 是变盘前夜」，是 ADR-0003 的典型转述值。
标定显示 10 在两种基线下都被验证集红线淘汰，而 **16–30 是一整片通过的高原**；
换成高原上任意一个值，均值回归的边际贡献变成 **+2.0 ~ +3.4pp 收益 / +0.11 ~ +0.24 夏普 / 回撤不变**
（`sq20`：两个都留 762 笔 / +6.25% / 0.57，只留趋势 459 笔 / +2.84% / 0.33）。
**出厂值已经人工复核写回为 20**（高原上唯一在训练集与验证集上都靠前的值），
`ENGINE_VERSION` 到 `0.2.5-unvalidated` —— **这是全项目第一个被标定过的出厂参数**，
依据与失效条件在 [CHANGELOG](./CHANGELOG.md)。**`-unvalidated` 后缀留着**：它跟的是整套参数
的状态，另外二十来个数值仍是转述猜测（清单 4.9 已拆成「部分写回 / 标定完成」两档）。
同轮还查出两条**结构**问题（调参解决不了）：`R2` 对唯一参数 `revertLookback` 完全惰性
（取 2/3/5 结果逐位相同），`R1` 的 RSI 带宽在收紧方向上近乎惰性。
全部数字见 [M2 §5.7–§5.10](./docs/notes/M2-偏差报告.md)。

**R2 的惰性已经修完了（2026-08-12，`ENGINE_VERSION` `0.2.6-unvalidated`），但结论要连着后半句读**：
旧写法的「当日 `close > MID`」在 **49% 的判定日上成立**，放在合取里等于没这个条件；
「`HIST` 转正」（= `DIF` 上穿 `DEA`）却是慢事件，破轨之后到过零的中位数 35 根以上，
与 2–5 根的回溯窗口差一个量级。改成「**曾在轨道外 + 今日穿越中轨（事件）+ HIST 朝有利方向**」后
触发数 90 → 213，`revertLookback` 变成活参数。**但绩效没变好**（全期 −0.32pp / −0.03 夏普，
噪音带内），`revertLookback` 取 3/5/8/12/20 的验证集仍逐位相同 —— 因为 213 次触发里
**185 次（87%）是孤立的**：R2 要求价格在中轨附近，R1/R3/R4 要求偏离中轨，几何互斥，
而 `voteThreshold.meanReversion = 2` 要两票。**约束已从子信号层转移到组合层的票数线**，
归入 `combine` 块那一轮标定。诊断过程与全部数字见 [M2 §5.11](./docs/notes/M2-偏差报告.md)。

**⚠ `T3_BREAKOUT:BUY` 在 `TREND_UP` 状态下不计票（2026-08-15，`ENGINE_VERSION` 0.2.8）。**
这是全项目**第一次判定逻辑改动**（前面所有变更都是「某个数取多少」）。
理由：在已被判定为上升趋势的状态里，「又创新高 + 带宽扩张」不提供新信息 ——
「在涨」是判定前提，`T4_ALIGNMENT` 实测出现在 **396/396 = 100%** 的 TREND_UP 建仓里、
已退化成常量，而带宽扩张在趋势中段是延续、末端是加速赶顶，日线上分不开。
于是 T3 那一票投给的恰恰是最危险的时刻。**只动 BUY、只动 TREND_UP、不引入任何数值。**
论证在 [docs/04 §3.1a](./docs/04-指标与信号引擎.md)（**写在代码之前**，约束 5 / ADR-0003 的要求），
验收门槛在 [docs/07 §3.6](./docs/07-回测与验证方案.md)，实测在 [M2 §5.24](./docs/notes/M2-偏差报告.md)。
**判据是配对胜率不是绩效**：TREND_UP 从 19.5/27.0/29.5% 变成 66.5/67.0/69.0%；
全期 −0.94% → −0.09% 那一组数**不许拿去说「策略变好了」**（砍掉 9.6% 交易本来就会改善绩效）。
（⚠ 那六个数是**独立抽日零点**下算的 —— 2026-08-20 换成段块零点后 TREND_UP 是 62/68/71，
见下面那条「配对胜率对零分布定义极度敏感」。改动前后是同一零点下比的，**结论不受影响**，
但**这六个数不能与新口径的数混着引用**。）
`tests/unit/strategies/trend.test.ts` 有一组「**同一根 K 线只换 regime**」的对照用例钉着 ——
必须这么写，因为**没有任何 fixture 能走到 TREND_UP**（实测三个突破类 fixture 全是 TRANSITION），
造一个上升趋势 fixture 去断言「不触发」会因为构造不出那个状态而假通过（§5.19 的形状）。

**⚠ 判定逻辑改动有独立的验收门槛（docs/07 §3.6），别套调参那一套。**
配对 Δ / `|Δ|/stderr ≥ 2` 的前提是两个候选测**同一批**交易，而逻辑改动会改变交易的集合。
四条门槛：先有论证（含「可能错在哪」）· 建仓数下降 ≤ 15% · 建仓级胜率不降 ·
**alpha 上移且在独立子集上稳定**。最后那半条是新加的：
**砍掉一批已知负 alpha 的交易，必然提升剩余部分的 alpha —— 那是算术不是发现**，
所以主判据无法区分「找对机制」与「删掉任何一批差交易」。
规则命中的那批必须在四个独立切法（代码前/后半、时间前/后段）上都同向更差。
被这条否掉过一个候选：「TREND_UP 持续 ≥ 11 根不买」总量上很好看，
但按时间切开后 2022-07 之后的差距只剩 −0.26pp（2022 前是 −3.30pp，**相差 12 倍**）。
**并且每个候选机制只报一次分位** —— 反复调到达标为止等于把随机基准变成新的过拟合目标，
而它是目前唯一可信的判据。**报告还必须与基线同窗口**（2026-08-25 加）：`t3fix.json` 跑的是全期，
于是 §5.24 那次改动的门槛②③ 事后**一次都复算不出来**。

**⚠ 但那四条只对「入场过滤」类改动有效 —— 出场类改动走 [docs/07 §3.6a](./docs/07-回测与验证方案.md)**
（2026-08-25 立，依据 [M2 §5.57](./docs/notes/M2-偏差报告.md)）。出场改动几乎不动**建仓的集合**、
只改「同一批建仓的结局」（实测共有建仓 **97.99–99.27%**，而其中 **34.8–58.4%** 的 `pnl` 变了）
⇒ **② 结构上自动通过**（门槛线 85%）、**③ 能被「拿盈亏比换胜率」机械满足**
（§5.31：关回撤减仓胜率 +7.9pp 而盈亏比 1.11 → 0.91，乘积仍为负）——
实测 **4/6 个候选同时过②③，而它们早已被判没过红线**。
所以：**先按那两个数分类，别靠命名判断**；③ 换成「**每次建仓的期望不降**」；
主判据换成**逐次建仓的配对 Δ**（出场类恰好满足「测同一批交易」这个前提，工具还没写）。
两条别读错：**`F1` 不许进任何门槛**（在已判为负的候选上 0.6034 → **0.6751**）；
**「§3.6 缺一条召回门槛」不成立**（纯过滤下 ②+③ 已隐含 `recall ≥ 0.85`）。

**不要动 `params.enabledStrategies`**（消融用的测量工具，出厂两项必须都是 `true`，
标定网格不得把它当维度扫），**更不要把权重表加回来**，它已经被测过一次了。

**⚠ 2026-08-17：方向变了 —— 先读 [当天的决策记录](./docs/notes/2026-08-17-决策记录.md)。**
那一天把问题重新定义了：**每次建仓期望 −477 元 /（10 万名义），其中 343 元是成本
（**均值口径** 71.9%）⇒ 入场信号的原始边缘只有 −0.13%**。
（`trade.costs` **不含滑点** —— 滑点作用在成交价上，算真实成本要跑 `--slippage 0` 对照。
我因此把「成本不是主因」判错过一次。）

**⚠ 但「成本占 69%」这句话 2026-08-25 拆开之后必须带上「均值口径」四个字**
（[M2 §5.58](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/cost-budget.ts`，只读报告）：
**中位建仓亏 1192 元（是均值的 2.5 倍差），同样的 347 元成本在它上面只占 29.1%**
⇒ 均值口径让「降成本」看起来是主线，**中位口径说它是配角**，而读数纪律说以中位为准。
拆开之后三块的可动性完全不同：**印花税 + 过户费 96 + 2 元 = 20.5%**（L0 能动 ——
场内基金免征，`isFundBoard` 已经在做）· **滑点 192 元 = 40.4%**（L0 管不着，不下单）·
**佣金 53 元 = 11.0%**（要换券商）⇒ **整池换 ETF 的算术上限是把期望抬到 −379 元，仍为负。**
⇒ **降成本不能当研发主线**（上限 20.5%，缺口 100%），但它是 ETF 那条路的一条独立算术支撑
（⚠ 不许因此说「换成 ETF 就能赚」）。
两条被证伪的直觉一起记住：① **「减少碎片化出场能省成本」是错的** ——
一次建仓平均拆成 **3.013** 次卖出，而多付只有 **2.1 元 = 总成本 0.61%**
（比例费率对次数免疫，且 §5.54 的凸成本下拆开还更便宜）⇒ 碎片化的代价在「多一次择时」不在成本；
② **「关掉某个成本再相减」这种反事实不是纯的** —— 关掉滑点会让触发止损那一根换一天，
851 个配对里 21 个（2.5%）走了另一条路径，**把均值拉偏 10.8% 而中位只差 0.1 元**。

**「改出场」这条路已经走完且结论为负**：四条风控规则各关一次 + `drawdownReducePct`
三点网格（0.07/0.10/0.14），**训练窗口 Calmar 最好 −0.057，全部没过红线**，
且验证窗口反向（§5.30/§5.31）。关键发现是**每个出场改动都只在胜率与盈亏比之间搬家**
（关回撤减仓：胜率 +7.9pp 但盈亏比 1.11→0.91），乘积始终为负 —— 那是「边缘约等于零」的指纹。
**别再在训练窗口上搜 `drawdownReducePct`**，预注册已判「到此为止」；
另外它 **≥ 0.14 是算术死区**（被 8% 止损短路，与关掉逐位相同）。

**唯一见过 Calmar > 0 的形态是 ETF 池**：12 只宽基/行业 ETF、同一套引擎、训练窗口
**+2.14% / Calmar +0.092 / 建仓级胜率 54.24%**。**但样本量只有 59 次建仓、
验证窗口反向（−0.52%，10 次建仓）⇒ 这不算测试。**
**⚠ 不要提「扩 ETF 池」**：2026-08-17 算过了，12 只的
**有效独立标的数 `N_eff = (Σλ)²/Σλ² = 2.97`**，第一主成分解释 54.4%（那就是市场 beta），
医药×医疗 0.926、酒×消费 0.891、三只宽基彼此 0.83+。**扩池或剔冗余都改善不了统计功效**，
用户已决定**保持现状**（活跃度全部合格，中位日成交额 2.70–39.57 亿，没有要剔的）。
⇒ ETF 的判据从「回测统计显著」换成「**前向记录攒够时间**」，而影子运行 + 双轨今天刚让它开始累积。
**⚠ 那条「ETF 择时 = beta 择时」的推测 2026-08-18 已测并被否**（M2 §5.32）：
引擎在指数上几乎不出手（沪深300 **11.5 年 8 次**、其 ETF 五年半 2 次），
而 12 只 ETF 池同期 59 次里**行业 9 只贡献 44 次、宽基 3 只只有 15 次** ——
ETF 池的正结果来自**行业 ETF 的残差波动**，不是 beta。
（「N_eff≈3」说的是统计相关性高，「信号来自残差 46%」说的是产生信号的那部分 ——
两句必须一起读，否则会得出相反结论。）
**顺带补上了「池外参照真空」，而它的答案是负面的**：2005-2017 指数 +301.70%，
择时只拿到 +24.14%（占用 11.5%）⇒ **在 beta 上择时远不如被动持有**。
⇒ ETF 那条路的证据强度比首测看起来弱，唯一还站得住的支撑是「前向记录会自己累积」。

**⚠ 2026-08-19：配置形态的第一个实验跑完了，主判据不通过 —— 但它不是干净的否。**
纯波动率目标化（`w = min(1, 15%/σ)`，20 日已实现波动，无方向无 regime 无指标，
`pnpm exp:vol-target`）vs 常年满仓，SH000300 两窗口：
**2005–2017 夏普 +0.288 / Calmar +0.131 / 回撤 72.30% → 41.67% / 收益 108.8% 于被动**，
而 **2018–2023 夏普 −0.009**（量级上等于什么都没发生）。
主判据是「两项同时改善**且两窗口同向**」⇒ 不通过。
**最值钱的产出是那条事先写下的警告命中了**：预注册里写着「总收益反而更高要警惕，
那通常是躲过了某次特定崩盘」，而 W1 恰好含 2008 与 2015 ——
⇒ [论证](./docs/notes/配置形态-论证.md) §4 ① 那条「样本期依赖是最大弱点」从担心变成观测结果。
顺带证实了成本那条本地证据（W1 十二年半累计成本 10.82%）。
**这是个需要用户拍板的分叉，我不替他判**；**绝不许换一个 `σ_target` 重跑到通过为止**。
三条可能的下一步列在论证 §8。数字见 [M2 §5.37](./docs/notes/M2-偏差报告.md)。

**⚠ 2026-08-25 用户拍板：选第 2 条 —— 接受「它只在崩盘型市场里有用」这个更弱的命题**
（重写在[论证 §11](./docs/notes/配置形态-论证.md)，§1–§10 原文不动）。
**关键是它换了读法而不是换了阈值**：那条「要先知道未来像 W1 还是像 W2」的陷阱只在
「**崩盘前打开它**」这种读法下成立；而规则 `w = min(1, 15%/σ)` **从不预测崩盘，
它对已经发生的波动上升作反应** ⇒ 读成「**常年开着的保险**」就不需要任何预测。
于是问题变成量两个数：**保费**（W2 六年少拿 12.3% 终值 ≈ 年化 2.2%，而夏普/Calmar 基本不动）
与**赔付**（W1 回撤 −30.63pp，但那是 2008 + 2015 两次的合成，拆不开）。
**判据也因此换掉**：旧的「两窗同向改善」对保险命题是**错的判据**（保险在非崩盘期
本来就不该改善任何东西）⇒ 新判据是 **A 赔付在 ≥ 3 次独立崩盘上一致**
（事件由**基准自己的**回撤 > 30% 定义 —— 不许用规则的输出定义样本）
**＋ B 保费有界**（非崩盘期年化拖累 ≤ 3% 且夏普不显著变差），两半必须同时成立。
⚠ 三条纪律：`σ_target` **不许再动** · 新判据**不许回头重判 §8**（移动球门）·
**「更弱的命题成立」也只支撑「它是一份值不值得买的保险」，不支撑「它能提高收益」**。

**⚠ 同日（2026-08-25）那两条判据跑完了：**A 与 B 都不通过****
（[M2 §5.59](./docs/notes/M2-偏差报告.md) · [论证 §12](./docs/notes/配置形态-论证.md)，
工具 `npx tsx scripts/verify/vol-insurance.ts`）。5 次崩盘事件 **5/5 同向**但**极差 14.5 倍**
（A 差第三条）· 非崩盘期年化拖累 **16.50% ± 0.08%**（门槛 3%）。
**失败的形状是一个机制，比结论值钱**：削减幅度与**事件内平均暴露**一一对应 ——
2008 / 2015 两次**快跌**暴露被压到 38–40%、削减 **27–36pp**，而 2009–2014 / 2018 /
**2021–2023（阴跌，就是现在这种市场）**暴露仍有 72–88%、削减只有 **2.5–3.2pp**。
⇒ **规则对波动率对称、对方向中立**，而 A 股暴力反弹紧跟暴力崩盘（最贵的保费段
2008-11→2009-07 年化 **108%**，紧贴事件 1 谷底）⇒ **崩盘里省下的会在反弹里还回去相当一部分**
—— 这同时解释了 §8 为什么 W1 好看、W2 归零、以及 W1 总收益反而更高。
⚠ **「它只保波动率飙升型的快跌」是本轮长出的假设，不是结论** —— 要用它得另立预注册，
而第一个要答的问题是「快跌与阴跌能不能**事先**分开」（又回到 §4 ② 那条「降暴露仍是预测」）。
⚠ **事件切分口径现场换过一次**（第一版「峰值→收复」在这份数据上退化成一个 16 年的单一事件
—— 沪深300 至今未收复 2007 峰值）；换的依据是 §11.4 的预测**自己点名了四次事件**，
两版都留在工具里且每次都打印。~~三条下一步在论证 §12.3，等用户定。~~

**⚠ 2026-08-27 用户拍板走 §12.3 第 3 条（换一条不依赖波动率的配置规则），
第三次预注册当天跑完 —— 主判据第一次通过，但通过的方式与「大部分持有国债」分不开**
（预注册 [论证 §13](./docs/notes/配置形态-论证.md) · 结果 §14 · 数字 [M2 §5.77](./docs/notes/M2-偏差报告.md)，
工具 `pnpm exp:risk-budget`）。规则是**股 + 债 + 金的等风险贡献（ERC）**
（`SH510300` / `SH511010` / `SH518880`，债腿与黄金长历史 2026-08-27 新抓，共同窗口 2013-07 起）。
**承重的对照是同一批资产的固定权重 1/3**（不是满仓股票 —— 后者量的是资产选择不是配置能力）：
两窗**夏普 +0.655 / +0.639 · Calmar +0.646 / +0.611** ⇒ **主判据通过**
（GH1 同风险也为正：+14.46pp / +10.11pp）。⇒ 停止规则不触发，**这条线不结案**。
**⚠ 但四条限制必须一起读，少一条就会把它读成「找到了配置能力」**：
① **§13.5 ① 那个混淆没被排除，反而更像真的** —— ERC 债腿权重 **71.6% / 78.7%**，
而**训练窗口 ERC 夏普 1.171 < 单独持有国债 ETF 的 1.277**，且两个窗口都落在**同一段国债牛市**里
⇒ 「两窗同向」在这里**没有提供任何独立证据**；
② `sharpeDiffHac` 的 `p` = **0.0174 / 0.0160** 恰落在 LW 说 HAC「偏自由」的区间 ⇒ 只能当**下界**，
不许说「显著」（顺带让登记项 `sharpe-diff-bootstrap` 的前置成立，但**本轮不许换用自举**）；
③ 一处口径与预注册措辞不一致（模拟没建月内漂移 ⇒ 对照白得逐日再平衡）——
**方向对 ERC 保守**，量没量，已登记；
④ **验证窗口少赚 12.44pp 换回撤 10.16% → 3.48%** ⇒ [论证 §4④](./docs/notes/配置形态-论证.md)
那道「值不值得取决于用户风险偏好」的**拍板题现在活了**，已登记等你拍。
⚠ **`COV_WINDOW` 与固定权重对照不许回头去搜**，**不许拿本轮重读 §8 / §12**（移动球门）。

**⚠ 同日又跑了第四轮（§14.5 那道题），而它把上面那个「通过」拆开了 —— 主判据不通过**
（预注册 [论证 §15](./docs/notes/配置形态-论证.md) · 结果 §16 · 数字 [M2 §5.79](./docs/notes/M2-偏差报告.md)）。
承重对照换成 **A₁ 静态等价**（= ERC 在该窗口的**平均权重向量**，固定不变，零自由参数）
⇒ 「债多少」按构造固定住（两臂平均权重差 ≤ 0.06pp、ρ = 0.94–0.96）
⇒ **§14.2 那个混淆排除了**。结果：训练 夏普 +0.209 / Calmar +0.166 ·
验证 **+0.072 / −0.105** ⇒ **未同时改善 ⇒ 不通过**（次判据在验证窗口也不过，回撤 +0.55pp）。
⇒ 按 §15.4 的停止规则：**「ERC / 风险平价这条规则」关闭，配置层不关闭**（事先写死的区分）。
**本轮真正的产出是把「平均权重」与「时变」拆开了**：对固定权重 1/3 的 Δ夏普 +0.657/+0.631
vs 对 A₁ 的 **+0.209/+0.072**，GH1 +14.55/+10.09pp vs **+4.37/+0.82pp**
⇒ **§14.1 那个「通过」主要是 ERC 选的平均权重在做功，不是它的时变**，
而时变那一半在验证窗口把**回撤做大了**（3.48% vs 2.93%）。
⚠ **不许读成「买一个 71/11/17 的静态组合就行」** —— A₁ 的权重是从 ERC 的结果里
**事后**读出来的（§15.5 ① 事先写着这条）⇒ 正确的话是「增量来自那个平均权重，
而它怎么**事先**选出来本轮没有回答」。⇒ 下一道题（要另立预注册）：
**债券权重该定在哪，且必须事先可定**（第一问是「用什么信息、那个信息当时拿得到吗」，
与 §4 ② 那条「降暴露仍是预测」同族）。
**顺带关掉一条更省事的路**：`w ∝ 1/σ` 与 ERC 实测无差别（−0.028 / +0.035，训练窗口 1/σ 还略优）
⇒ **协方差非对角那一半买到的约等于零** ⇒ 真要做风险平价先用 1/σ（无迭代、无收敛风险）。
**§4④ 同日已拍板：回撤优先** ⇒ 配置类候选从 §15 起多一条次判据
「回撤不高于对照 且 总收益 ≥ 对照的 80%」（那个 80% 沿用 §5 第一次预注册的数，不是新定的）。

产品路线是用户拍的：**B 双轨 → ETF 稳定且质量高则考虑分层 → 个股若无法进化则只做 ETF**。
**「个股无法进化」由用户主观认定，不设量化判据** —— 我不许自己宣布个股路线死亡、
也不许用「几轮没过红线」推导出切换；但**要主动建议**，因为判据是主观的，
喂给那个判断的证据质量就是我的责任（节奏是**有新证据时**，不是每天一条）。

**⚠ 2026-08-19 用户拍板：停止在「个股 · 日线 · 择时」上找边缘**
（[当天的决策记录 §三](./docs/notes/2026-08-19-决策记录.md)）。研发力气转向
**仓位/配置层**（[M2 §5.37](./docs/notes/M2-偏差报告.md) 那条分叉）与 **ETF 双轨的前向记录**
（不需要新研发，时间自己在攒证据）。触发它的是两个把老前提推翻的实测：
**RANGE 从 65% 掉到 35.5%**（§5.35 把对照组收窄到「当天也有信号的票」）⇒
占样本 88% 的两个最大层（RANGE + TRANSITION）**都低于 50%**；而分类器改造的
天花板实测只有 12.9pp（三条突变条件整条删掉，TRANSITION 也只 66.6% → 53.7%）。
**四条别读错**：① **这不是「个股路线死亡」** —— 个股照常评估/提醒/进影子运行与记账，
停的是研发投入，用户**没有**做那个主观认定；② 不是回测/标定工具作废（配置形态实验正在用）；
③ A（TRANSITION 不出手）与 C（改分类器）是**因为已知上界太低**而不选，不是被判死，
有新证据可重开；④ **`TREND_UP` 在收窄对照组下的 99.5%（154 次建仓）进待办，
等样本不等灵感** —— 分层结果不许拿来救已判死的候选，要追必须另立候选、另写预注册。
⇒ [困境说明](./docs/notes/困境说明-2026-08-17.md) 的 **Q1 结案**，Q2/Q3 降级为「若重开个股线再谈」。

**提醒已改双轨（2026-08-17）**：「行业ETF」组不再整个摘掉，改走 `OBSERVE` 轨 ——
自己一份日配额（默认 2）、不占个股任何计数器、**抢不到气泡**。
「不挤占」由配额与气泡两处共同保证，少一处都不成立（见 docs/05 §3.2）。

**⚠ 2026-08-18 的完整记录在 [当天的决策记录](./docs/notes/2026-08-18-决策记录.md)**
（四类事：产品面 · 三个静默缺陷 · §3.11 横截面当天立当天判死 · 判据与经验）。
**那份记录的 §八/§九 是唯一可迁移的部分** —— 三个被我当天自己推翻的判断（S0 判据的分母没定义 ·
JQData 许可 · 换手率其实早就取通过）与由它们得出的十条方法论。
新开会话若要引用当天任何数字，先看那两节，别只读结论。

**⚠ 2026-08-18 加了一个例外：有持仓的 ETF 翻回 `PRIMARY`**（用户同一天拍板放开了
行业 ETF 的持仓入口）。OBSERVE 的那两条性质恰好会把持仓强制类 L3 废掉 ——
2 条日配额 + 抢不到气泡 = 跌破止损可能一声不响。判据在 `main/alerts/track.ts`。
于是「`PRIMARY` 行为逐位不变」**不再成立**：M3 的提醒日志里会混进持仓 ETF 的行。
那是刻意接受的代价（那些行对应真持仓，本来就属于「该不该被打断」的样本，按 code 拆得开）。

**成本认板块了**：场内基金免印花税与过户费（`isFundBoard`）。
`TradeInput.board` 是**必填**的 —— 记账靠缺省会让 ETF 的成本价与已实现盈亏偏高，
必填让漏传时编译不过。回测缺省仍按股票收满（偏保守是安全方向）。

里程碑见 [docs/08](./docs/08-开发路线图.md)，偏差见 [docs/notes/](./docs/notes/)。

## 五条不可违反的约束

1. **`src/core` 是纯函数库。** 不得 import Electron、Node IO 模块、或 `src/main` 的任何东西；不得读时钟（`Date.now()` 一律禁止，时间由调用方传入）。ESLint 已强制，违反即 CI 失败。理由见 [ADR-0004](./docs/adr/ADR-0004-核心引擎与-electron-解耦.md) —— 这是回测与实盘同源、且无未来函数的架构保证。

2. **参数值不是事实。** [`src/core/params.ts`](./src/core/params.ts) 里的数值（MACD 12/17/9、8% 止损、0.6 得分阈值…）几乎全部来自需求文档的网络转述，**未经任何验证**。不要在文档、UI 或注释里把它们写成「经过验证的最优参数」。见 [ADR-0003](./docs/adr/ADR-0003-来源文档数值不作为出厂默认.md)。
   **唯一的例外是 `strategy.squeezeBbwPct = 20`**（2026-08-12 标定并人工复核，见 [CHANGELOG](./CHANGELOG.md)）。已标定清单以 `params.ts` 顶部那张清单为准 —— 往里加行要走清单 4.9a，**不要笼统地说「参数已验证」**，那会把一个参数的证据扩张成整套参数的背书。

3. **零干扰契约是硬指标。** [docs/06 §1](./docs/06-桌宠交互与非干扰设计.md) 的 C1–C10，尤其 C1（永不抢焦点）与 C2（点击穿透）。任何为视觉效果牺牲它们的方案一律否决。Overlay/Bubble 窗口的 `focusable: false` 不得改动。注意 **C5 的口径已变**：提醒没有声音渠道了，那一条从「默认无声」读作「永不发声」。

4. **指标序列的未定义值填 `null`，绝不填 0。** 用 0 冒充未预热的指标值是回测失真的经典来源。`noUncheckedIndexedAccess` 已开启，这会让指标代码写起来啰嗦一些 —— 那是刻意的。

5. **不新增未在文档中论证的指标。** 信号质量问题优先从数据质量、参数标定、提醒策略三处找原因。加指标几乎总是错误的应对方式（[docs/08 §关键决策点](./docs/08-开发路线图.md)）。

## 常用命令

```bash
pnpm dev              # 启动开发环境（electron-vite）
pnpm test             # 单元 + 集成测试（Vitest，不需要启动 Electron）
pnpm test:cov         # 覆盖率；src/core 门槛 90%，其余 60%
pnpm test:e2e         # Playwright E2E：先 build，再真启 Electron 跑 11 条（workers=1）
pnpm typecheck        # 三个 tsconfig（node / web / e2e）分别校验
pnpm lint
pnpm verify:indicators           # 重出指标黄金用例；加 -- --check 只校验不重写
pnpm fetch:history -- --codes SH600000,SZ000001 --from 2018-01-01   # 拉真实日线 → data/history/
pnpm fetch:page -- <url> [--render] [--state <file>] [--links]      # 调研取页（WebFetch 在本机永久不可用，见下）
pnpm backtest -- --codes SH600000 --fixtures ./data/history --from 2020-01-01 --to 2026-06-30
pnpm backtest -- --codes ... --fixtures ./data/history --grid params/grid.json   # 参数标定
pnpm audit:regime -- --codes ... --fixtures ./data/history --to 2026-08-11       # Regime 判定的成因分解
pnpm audit:subsignals -- --codes ... --fixtures ./data/history --to 2026-08-11   # 子信号触发数 + R2 合取项分解
pnpm audit:ic -- --codes ... --fixtures ./data/history --from 2018-01-01 --to 2026-06-30   # rank IC（不构造零分布的第二判据）
pnpm audit:ic -- ... --price-factors      # 加三条收益形态因子臂 + 动量中性化（M2 §5.83–§5.85）
pnpm audit:ic -- ... --liq-factors        # 加 Amihud 非流动性 + 纯成交量分量五条臂（M2 §5.88–§5.91）
pnpm iterate          # 迭代看板：当前指标 + 今天该做什么（-- --write 刷新 docs/iteration/看板.md）
pnpm package          # electron-builder 打包 Windows
```

## 分层与依赖方向

```
src/core     纯引擎：指标 → 市场状态 → 策略 → 组合 → 风控。零依赖、可回测
src/main     Electron 主进程：窗口、调度、数据源、存储、提醒编排、影子运行、AI 解读、观察点
src/preload  contextBridge 窄接口
src/renderer pet / panel / bubble 三个独立入口
src/shared   主/渲染共享的纯类型
src/backtest 回测 CLI，复用 src/core
```

依赖方向单向：`main → core`、`renderer → shared`。反向依赖一律禁止。

**M4 新增了一条横向边：`main/shadow → backtest`**（只碰 `costs.ts` 与 `metrics.ts` 两个纯模块）。
这不是反向依赖，是 `src/backtest/index.ts` 头注释里本来就写着的「供……应用内影子运行复用」。
理由：影子运行与回测量的是同一件事，成本模型、手数取整、绩效口径各写一份，两边数字就再也对不上，
而「回测说赚、影子说亏」到底是策略退化还是口径差异会变成一个查不清的问题。

**同一条边的第二个用例是 `main/trades → backtest/costs`**（2026-08-14，实盘成交记账）：
用户录入的成交按同一套费率算手续费，实盘盈亏才与影子绩效可比。
但**记账绝不套 `buyFill` / `sellFill` 的滑点** —— 那两个是回测在模拟「不知道会成交在哪」，
而用户填的就是真实成交价，再偏一次等于凭空把它改坏，然后一路进成本、进止损线。
这是那个文件里最容易被「顺手复用」错的地方，`src/main/trades/ledger.ts` 头注释写着。

**渲染层不许 import `src/main`**（包括那个纯函数记账器）。成交表单的「录入后会变成什么样」
走 `trade:preview` 这趟 IPC，与落库共用同一个 `applyTrade` ——
在渲染层照抄一份口径才是坏选择：症状会是「表单说成本变成 12.34，存完变成 12.31」，
而用户没法判断哪个才对。`tsconfig.web.json` 的 include 会直接把这种 import 拦下来。

## 上手前先读

| 要做的事 | 先读 |
|---|---|
| 写指标或策略 | [docs/04](./docs/04-指标与信号引擎.md)（公式与口径都在这，含与来源文档的差异说明） |
| 接数据源 | [docs/03](./docs/03-数据源与存储设计.md) + [src/main/providers/README.md](./src/main/providers/README.md) |
| 改窗口或交互 | [docs/06](./docs/06-桌宠交互与非干扰设计.md)（§2.1 悬浮条，§5 皮肤系统已删的说明） |
| 改「当前指标」那一屏 | [`src/shared/indicator-catalog.ts`](./src/shared/indicator-catalog.ts) 头注释（三条文案纪律）+ [docs/06 §2.4](./docs/06-桌宠交互与非干扰设计.md) 的「行情页签的当前指标」 |
| 改分时图 | [docs/03 §2.5](./docs/03-数据源与存储设计.md)（用户触发取数的三条边界）+ [`src/main/engine/intraday.ts`](./src/main/engine/intraday.ts) 头注释（远端/本机的取舍） |
| 换托盘 / 应用图标 | [resources/icons/README.md](./resources/icons/README.md)（png 是手绘资产；只有 `icon.ico` 是生成件，跑 `node tools/logo/make-ico.mjs` 重出，它**不在** package.json 的 scripts 里） |
| 改提醒逻辑 | [docs/05](./docs/05-风控与提醒规则.md) |
| 改影子运行 | [docs/07 §2.3](./docs/07-回测与验证方案.md)（四条前向纪律 + 记账口径） |
| 改成交记账 / 持仓 | [`src/main/trades/ledger.ts`](./src/main/trades/ledger.ts) 头注释（含费摊薄、卖出不动成本、**不套滑点**、T+1 只提示不硬拒）+ [007_trade_log.sql](./src/main/storage/migrations/007_trade_log.sql) |
| 改建仓体检 / T+1 卖出锁定 | [docs/05 §2.1a / §2.5](./docs/05-风控与提醒规则.md) + [`src/core/risk/entry.ts`](./src/core/risk/entry.ts) 头注释（三条边界：只复述、无新阈值、verdict 纯派生） |
| 改设置页 / 参数表 | [docs/01 §5.5](./docs/01-产品需求与范围.md) + [ADR-0003](./docs/adr/ADR-0003-来源文档数值不作为出厂默认.md) |
| 改 AI 解读 | [src/main/ai/index.ts](./src/main/ai/index.ts) 头注释（五条纪律）+ [src/main/ai/service.ts](./src/main/ai/service.ts)（两层缓存 / 只有 done 落库）+ [AiPanel.tsx](./src/renderer/panel/AiPanel.tsx) 头注释（为什么是页签、搬过两次家）+ [docs/08 §后续](./docs/08-开发路线图.md) |
| 改 AI 历史 / 保留策略 | [008_ai_explain.sql](./src/main/storage/migrations/008_ai_explain.sql) 头注释（不加外键、不裁剪、只能手删） |
| 改止损 / 「接受这段亏损」 | [docs/05 §2.3](./docs/05-风控与提醒规则.md) + [009_position_stop.sql](./src/main/storage/migrations/009_position_stop.sql) 头注释（只作用于固定止损、加仓清空、不进回测） |
| 改观察点 | [003_watch.sql](./src/main/storage/migrations/003_watch.sql) 头注释（它不是什么）+ [docs/05 §3.1](./docs/05-风控与提醒规则.md) |
| 写测试或回测 | [docs/07](./docs/07-回测与验证方案.md)（回测陷阱清单必读） |

## 容易踩的坑

- **「当前指标」那一屏只解释口径，不解释涨跌**（2026-08-20，`shared/indicator-catalog.ts`）。
  每个指标带三样：**定义** · **与国内平台的口径差异** · **相关阈值的标定状态**。
  ⚠ **不许加「金叉看涨」这类话** —— 08-20 实测买入得分与前瞻收益的横截面秩相关是**负的**
  （M2 §5.46），在界面上印那种常识等于把一个已被本地数据否掉的说法当事实；
  得分也不叫「概率」或「胜率」（措辞纪律）。`tests/unit/shared/indicator-catalog.test.ts`
  钉着五条：**快照的每个键都要有文案**（写这一屏时就抓到 `ma120` 漏项）· 目录里不许有快照给不出的键 ·
  禁词表 · ADX/ATR/MACD 柱/BBW 四项必须带口径说明 · 引用的参数路径必须是真实叶子。
  取数走 `indicators:current`，**不发网络请求**（主进程就地评估一次），所以不占轮询预算。
- **内置的 WebFetch 工具在这台机器上永久不可用，别再拿它当「站点访问不了」的证据。**
  它抓之前要做一次域名安全校验，而校验要摸 `claude.ai` —— 实测 `curl https://claude.ai` **000**
  ⇒ 它对**所有**域名都失败，包括同一时刻 curl 能 200 的站点
  （`www.joinquant.com` 200 · `raw.githubusercontent.com` 200 · 而 `api.github.com` 确实 000）。
  调研取页一律走 `pnpm fetch:page`（静态页用 fetch，SPA 加 `--render` 用**系统已装的 Edge/Chrome**，
  不下载 Playwright 的 Chromium）。**它不是 provider**：不进应用、不进降级链、不占
  docs/03 §2.4 那份轮询预算。需要登录的站点由**人在有界面窗口里登录**，
  会话（`storageState`）放在**仓库之外**（`%LOCALAPPDATA%`）—— 它含 cookie，进仓库等于提交凭据。
  ⚠ **但要读一份开源仓库的源码，首选 `git clone --depth 1`**（2026-08-31 实测可用，
  竞品那一轮 165,335 行全部一手）—— `api.github.com` 仍然 **000**，而 **git 协议与
  `raw.githubusercontent.com` 都通** ⇒ **「github 被拦」只对 API 域成立**，三个主机是三条独立判断。
  克隆到**仓库之外**（`/tmp`）。拿不到的只有 star 数 / release / issue（只能靠 WebSearch，按二手算）。
- **⚠ 规则类常量要带生效日期。** `priceLimitRatio` 曾对主板 ST 写死 0.05，而规则
  **2026-07-06 已改成 10%**（[CHANGELOG](./CHANGELOG.md) · [M2 §5.82 ②](./docs/notes/M2-偏差报告.md)，
  一级源：上交所/深交所《交易规则（2026 年修订）》。**2026-08-31 已修**，
  常量 `MAIN_ST_LIMIT_WIDENED_ON`，`priceLimitRatio(board, isST, asOf)` 第三参**必填** ——
  给默认值等于让漏传的调用点静默沿用旧规则）。科创/创业板 ST 仍 20%、北交所 30%
  ⇒「只对 `MAIN` 特殊处理」那一层一直是对的，错的只有那个数与「不看日期」。
  **失效方式值得记住**：涨 6% 的主板 ST 被判「已涨停」⇒ `HARD_LIMIT_UP` **硬抑制**
  ⇒ `candidates.ts` 直接 `continue` ⇒ **连 `alert_log` 都不落**；跌侧把止损那条 L3 整条吞掉
  —— 而**不带日期的规则常量错的时候没有任何东西会报警**。
  约束 1（`src/core` 不许读时钟）⇒ 日期由调用方给：回测/随机基准传 `bar.date`、
  闸门传**当根 K 线**的日期（`GateInput.now` 刻意只有分钟与时段）、影子传 `ctx.bar.date`、
  三个 provider 传 `shanghaiDate(at)`（**快照自己的时刻，不是「现在」**）。
  ⚠ 再改一次规则时是**加一个生效日**，不是改这个常量的值 —— 改值会让历史回测用错规则。
- **`pnpm fetch:history` 会把基准（`SH000300`）自动加进抓取列表，并按你给的 `--from` 整文件重写。**
  于是「补几只票」这种小请求会把已有的长历史**截短**，一声不响 —— 2026-08-19 真踩过：
  一次 `--codes 三只票 --from 2018-01-01` 把基准从 **5183 根（2005-04-08 起）压成 2093 根**，
  而 2005–2017 那段正是池外参照（M2 §5.32）与波动率目标化实验（§5.37）的输入。
  **已加保护**：新起点晚于现有起点即**拒写并列名**，要截短得显式 `--allow-shrink`；
  补拉少数标的时用 `--benchmark none`。**这不只是基准的问题** —— 任何一只票用更短的窗口重拉都会这样。
- **`universe-broad.json` 是 264 个代码，但只有 261 只拿得到日线 —— 别再当作「还没拉完」去重试。**
  `SZ001330` / `SZ301583` / `SZ301717` 在腾讯 `fqkline` 上**没有 `hfqday` 那一轨**
  （2026-08-14 与 **2026-08-19 两次实测**都是「期望 hfqday，实际只有 [day]」），
  而 `assertKey` 拒绝拿不复权顶替 —— 那是对的失败方式。
  ⚠ **但「这是源的上限」这个说法 2026-08-27 已订正**（[M2 §5.65](./docs/notes/M2-偏差报告.md)）：
  新浪复权因子显示这三只的 hfq 因子**恒等于 1.0** ⇒ **它们一次除权除息都没有过**，
  腾讯因此根本不生成那一轨 —— 不是「数据缺失」。**处置基本不变**（`301583`/`301717`
  上市才几周、`SH600083` 在同花顺上 `total = 0` ⇒ **只有 `SZ001330` 真的可补**，
  261 → 262 且窗口仅 3.5 年，不值得动 `fetch:history`），变的是**依据**：
  真要补时放行的理由是「因子实测恒 1」而不是猜测。
  另注意池里有 56 只**首根晚于 2018**，那是它们的上市日，不是抓取缺口
  （「新上市偏差没量化」这条仍然成立，见困境说明 §6）。
- **⚠ `RegistryResult.degraded` 判的是「值来不来自主源」，不是「这次试了几个源」**
  （2026-08-30 改，[CHANGELOG](./CHANGELOG.md)）。旧实现是 `attempts.length > 1`，
  它**漏掉主源在冷却里被跳过**那一种：`ordered()` 把冷却中的排到最后 ⇒ 备源第一个就成功
  ⇒ 只有一次 attempt ⇒ 值来自备源却报 `degraded === false`。
  代价是真的：真机 **`watchlist.industry` 79/79 全空、`industry_history`（014）一行都没有**，
  而 `risk.industryConcentrationCap` 判的正是由它算出来的 `industryShare`
  ⇒ **那条风控规则从上线起一次都没有可能触发**（回测不受影响 —— 它本来就读不到这个字段）。
  根因是 2026-08-26 11:35:23 eastmoney 连续三次失败进 5 分钟冷却，
  而**每周一次的 `refreshProfiles()` 恰好同一秒开跑** ⇒ 79 只全走腾讯（它不提供行业），
  刷新报「成功」并盖下时间戳 ⇒ 下一次要等七天。08-22 加的「降级时按字段重试」
  因为判据写的是 `degraded` 而**八天一次都没触发**。
  ⇒ 三条别再踩：① 要区分「主源试过并失败」与「主源压根没试」，**判据是 `attempts.length`**，
  别拿一个布尔盖住 —— 前者立刻重试有机会，后者立刻重试必然还是同一个备源；
  ② **「刷新报成功」不等于「每个字段都拿到了」**，按整体成败判一个字段是这个坑的通用形状
  （与 `fetchMinutes` 那条「空结果不算失败」方向相反、同一类）；
  ③ 补救那趟（`tick.ts` 的 `INDUSTRY_RETRY_INTERVAL_MS`，一天一次）**必须按板块筛**
  —— ETF 与指数结构上没有行业，留在待补名单里会让它每天白打请求且永远清不空。
- **复权**：指标用前复权（`*Adj` 字段），展示与持仓成本用不复权。混用会伪造出金叉死叉。
- **回测数据里的 `*Adj` 是后复权，不是前复权。** 腾讯的前复权是加性的（减价差），高分红股上会
  变成**负数** —— 实测中远海控（SH601919）2018–2026 有 714 根前复权收盘价 ≤ 0，最低 -5.145，
  而负价上的 MA / MACD / RSI / 布林带全是垃圾。`fetch:history` 因此拉后复权：对指标与净值等价
  （指标全是比率型，净值用相邻比值），且历史值不随抓取日变。**别改回 qfq**，脚本里有断言挡着。
- **退市股是取得到的 —— 别再照着「幸存者偏差无解」写结论**（2026-08-17 推翻，见
  [M2 §5.26](./docs/notes/M2-偏差报告.md)）。腾讯 `fqkline` 有退市股完整日线（延伸到退市日、两轨都在、
  `fetch:history` 一个字不用改），**名单**来自巨潮 `szse_stock.json`（抓公告时已经在用它取 orgId，
  6234 只含退市股）—— 不需要 Tushare token。`scripts/build-delisted.mjs` 产出
  `params/universe-delisted.json`，回测加 `--delisted <file>` 即可。
  **幸存者偏差 ≈ −0.13pp**（全期总收益）：19 只抽样实测 −0.1496pp，抓满 222 只后按真实占比
  重估 −0.1322pp，两个独立估计差 0.017pp。取 **−0.13pp**（用了 222 只信息，方差小得多）。
  ⚠ **2026-08-27 降级为「有一条未量化的口径风险」**（[M2 §5.65](./docs/notes/M2-偏差报告.md)，
  工具 `npx tsx scripts/verify/adjust-crosscheck.ts`）：这个估计的输入是**退市股的 `closeAdj`**，
  而拿新浪复权因子核腾讯 `hfqday` 时，**存活股两家高度一致**（373 次除权事件逐次收益差
  中位 **0.067pp** ⇒ 回测输入第一次有外部背书），**退市股全窗累计漂移中位却是 38.12%**
  （差 16 倍，分歧 top-10 **10/10 是退市股**）。**不推翻 −0.13pp**（分歧方向不定、
  配对随机化对标的自身 beta 不敏感），但引用时要带上这句。
  ⚠ **两列必须并排读**：退市组「逐次差同样小」是假象 —— 它们的除权绝大多数在 2018 之前，
  窗口内只匹配到 52 次 ⇒ **在只统计了「被匹配上的那些」的数据里找一致性，
  会系统性漏掉没被匹配的那部分**（与 §5.56 那条同源）。
  ⚠ **同一格上 2026-08-27 还查出并修掉一个 `isST` 缺陷，但 −0.13pp 仍然不变**
  （[M2 §5.66/§5.67](./docs/notes/M2-偏差报告.md)）：`openFixtureSource` 此前**不传名字**
  ⇒ `isST` 恒 false ⇒ 全按 ±10% 算涨跌停，而 549 份 fixture 里 **225 份**名称带 ST/退
  （**退市池 212/233 = 91%**）。修后退市腿 −2.4028% → **−2.1929%（0.21pp）**，
  而幸存者偏差只从 −0.1322 动到 **−0.1189pp（0.0133pp）** —— 小于 §5.26 自己两个估计间的
  0.017pp ⇒ **取值仍是 −0.13pp**。⚠ 只修了一半：`nameAtFetch` 是**抓取时**的名字不是逐日状态
  ⇒ 「用今天的 ST 状态回标 8 年」仍是时点错误，真修法要 baostock 逐日 `isST`（已登记）。
  ⚠ **既有基线不用重跑**（存活腿只动 0.0211pp），但**下次重跑时数字会有微小变化 ——
  那是这次修复不是引擎变了**。闸门在 `tests/unit/backtest/data-profile.test.ts`（验证过它抓得住）。
  退市股单独跑是 −2.08%（建仓胜率 43.07%）却只拖累 0.13pp，因为**引擎在它们上面天然很少出手** ——
  平均资金占用仅 1.82%（主池 3.49%）、**18% 的标的一笔没建仓**（长期停牌够不到 300 根预热）。
  **`audit:random` 有一个结构性盲区，加 `--cross-code`（跨票随机）也补不上**（§5.27 实测证伪）：
  池内配对随机化**测不了「池子本身好不好」**，因为基准就在同一个池里 —— 补进退市股会同时拉低两组，
  相对优势反而升。要测它得有**池外参照**（相同资金占用下的被动持有），那个工具还没有。
  副产品值得记：同一批建仓换个零点，**择时** 46.5% vs **选股** 74.5% ——
  但那**不是「策略赚钱」**（全期仍 −0.09%）。
  **⚠ 而且「选股 74.5%」从 2026-08-18 起不许再单独引用**（M2 §5.35）：那一节留的替代解释
  「只是选了活跃的票」当天被证实了 —— 把随机对照组收窄到「**当天也有买入方向信号的票**」
  （`audit:random --cross-pool`）之后，1370 次建仓的配对胜率掉到 **55.0%**，
  而占 88% 样本的 `RANGE` **35.5%** / `TRANSITION` **26.0%** 低于 50%（= 在同样有信号的票里，
  引擎实际下手的那只**不如随机挑一只**）。要引就引 55.0% 那个数，并说清它的对照组是什么。
  三条一起读，少一条就会把它读错：① `universe-broad.json` 那 261 只**仍然**全是今天还活着的股票，
  基于它的绝对绩效仍被高估；② −0.15pp 是**下界**（停牌段无数据、结算价按退市日收盘、
  抽中 3 只是吸收合并）；③ **`audit:random` 的配对胜率对这个偏差基本不敏感** ——
  随机基准是「同一只票上随机挑日子」，票本身的 beta 在两组间抵消，它测不出「选到烂票」。
  所以补完退市股是**绝对绩效变差、相对 alpha 反而升**（ALL +8pp，跨种子稳）：
  §5.21 起的 alpha 结论不用重做，但**引用绝对绩效必须说明是哪个池子**。
  两个池子**刻意不合并** —— 合成一个，「含退市 vs 不含退市」这个对照就再也做不出来了。
- **盘中 K 线是临时的**：`Candle.provisional` 为 true 时指标会抖，信号只能是 `PROVISIONAL`，最高 L2 提醒，收盘确认轮再定论。
- **盘中量比必须按时间归一化**，否则上午永远显示「缩量」。
- **MACD 柱用 `2×(DIF−DEA)`**（国内平台口径），与来源文档的 `DIF−DEA` 不同，但不影响任何穿越判定。
- **布林带标准差除 n 而非 n−1**（国内平台口径）。
- **上面这两条 2026-08-19 已被第三方证实，同一次对照还查出两个口径「不一致」的地方 ——
  别把结论只读前半句**（[M2 §5.38](./docs/notes/M2-偏差报告.md)，工具
  `scripts/verify/jq-crosscheck.ts`）。拿聚宽公开数据字典的示例值对（其备注是
  「计算方式与通达信、东方财富和同花顺相同」）：**BOLL 逐位一致（1e-14）· RSI 一致（1e-9）·
  MACD 柱的 ×2 代数确认**；而 **ATR 与 ADX 与国内平台不是同一个数** ——
  他们的 ATR 是 `MA(TR,14)`（我们是 `Wilder(TR)/14`）、DMI 用通达信 `EXPMEMA`（α=2/15）
  且 **ADX 平滑周期 MM=6**（我们是 Wilder + 14）。**口径不改**（Wilder 是 ADX 的原始定义），
  但后果要记住：我们池子里 699,907 个「股票·交易日」上，我们的 ADX 中位 **23.06**、
  通达信口径 **30.26**，「ADX ≥ 20」在 **30.4%** 的日子上结论相反。
  ⇒ `adx.baseThreshold = 20` 这个转述值**抄对了数、没抄对量纲**（约束 2 最隐蔽的一种形状）；
  它站得住是本地标定的功劳。分位等价值约 **17.29**，落在已测邻域（18/20/22）之外
  ⇒ 下一轮 `adx` 网格把下侧探到 16–18。
- **聚宽数据字典里示例的 `check_date` 与打印出来的数值不是同一天**，别照着字面比。
  按文档写的 2017-01-04 比，**一项都对不上**（连 MA20 都差 0.8%），看起来像我们整套指标错了；
  真实日期要用 MA20 + 20 日标准差反查（BOLL/RSI 那组是 2017-01-26，DMI 那组约 2016-05-04）。
- **组合层没有策略权重了**（2026-08-12 起）：得分是 `clamp01(趋势项 + 均值回归项 × 下跌买入折价)`，
  两个策略等权。**不要「顺手把权重表加回来」** —— 它被测过两轮都看不出效果，而且那张表制造过一个
  很贵的坑：得分上限恰等于策略权重、与 `scoreThreshold` 乘性耦合，`TRANSITION`（占判定根 66.6%）
  的单策略信号曾在算术上无法触发；解掉那层耦合后残留的行内比值又让 `RANGE` 一笔交易都出不来。
  两次都记在 [M2 §5.5–§5.8](./docs/notes/M2-偏差报告.md)。想表达「某状态下某策略更可信」，
  要新增显式参数、说清与旧机制的差别并单独标定，不是复用那张表。
  `tests/unit/combine/combine.test.ts` 用「三档灵敏度都够得着」这条可达性不变量钉住结果，
  **它变红是提醒你去复核 docs/04 §4.1/§4.2，不是让你改断言**。
- **票数线是按策略给的对象**：`combine.voteThreshold = { trend, meanReversion }`，判据是
  「任一策略达到自己的线」，**不是两边票数相加**。`params/*.json` 里写成数字（2026-08-12 前的写法）
  会让 `votes >= undefined` 恒假、回测一声不响地跑出 0 笔 —— `src/backtest/cli.ts` 的
  `assertParamsShape()` 专门挡这一下，别把它删了。
- **`combine.downtrendBuyPenalty` 与已删的权重表不是一回事**：它是方向级抑制（下跌趋势里
  均值回归**买入**打 0.5，docs/04 §4.1「别接飞刀」），只作用于均值回归那一项，从未被单独测过，
  因此保留待标定。别把它当成「权重表的残留」删掉。
- **信号出不来时别先动 regime 的阈值**：`pnpm audit:regime` 已量化过上界 —— 把三条突变条件整条删掉，
  TRANSITION 也只从 66.6% 降到 53.7%，死区几乎消灭只买回 3.6 个点。成因分解在报的是事实，先看它。
- **某个策略「看起来没用」时，先查它的子信号条件是不是被某个转述阈值卡死了。**
  均值回归被判过两次死刑，两次都是别的东西的锅：第一次是共用票数线，第二次是
  `strategy.squeezeBbwPct = 10`（来源文档的「< 10 变盘前夜」）把 R3 卡得几乎不出手。
  放宽到 16–30 之后它的边际贡献从 +0.14pp 变成 +2.0 ~ +3.4pp（出厂值现已是 20）。
  见 [M2 §5.9/§5.10](./docs/notes/M2-偏差报告.md)。
- **某个参数「取什么值结果都一样」时，别当成「不敏感所以随便取」—— 先用 `pnpm audit:subsignals`
  把它所在的合取拆开逐项计数。** 两次都不是取值问题：`revertLookback` 惰性是因为同一个合取里
  另有一项在 49% 的交易日上成立（等于没有条件）、还有一项的时间常数差一个量级；
  regime 阈值看着钝是因为 TRANSITION 的成因不在那三条突变条件上。
  **合取里混着一个「几乎总成立」的项，会让同组其他参数集体失去作用** —— 这是本项目已经踩过的形状。
- **改子信号的触发条件之前，先确认它有没有可能与同策略的其他子信号同时成立。**
  票数线要求同策略 N 票，而 R2（要求价格回到中轨附近）与 R1/R3/R4（要求偏离中轨）几何互斥：
  R2 的 213 次触发里 87% 是孤立的，因此**再怎么改 R2 都换不来一次触发**。
  子信号之间的可共现性是组合层能不能用上它的前提，见 [M2 §5.11](./docs/notes/M2-偏差报告.md)。
- **`--grid` 的分段绩效曾把预热段的净值点一起算进去（2026-08-12 已修）。** 每一段都带完整段前历史
  做预热（否则测试集短于 300 根就永远 0 笔），但净值曲线必须按 `split.from` 切回本段再算绩效 ——
  不切的话验证集年化被压小 5.1 倍、基准收益取的是全程涨跌（**符号都反**）。
  `runSplit` 里那行 `.filter((point) => point.date >= split.from)` **别删**。
  段内排名不受影响，但**跨段的「Calmar 衰减 > 50%」红线会假阳性**（实测 3/16 全是）。
- **标定的排名口径是验证集 Calmar，但训练集亏钱的候选要先淘汰。** 用训练集排名等于直接过拟合，
  所以排名口径是对的；盲区是「6 年训练窗口亏钱、18 个月验证窗口赚钱」的候选会排到很前面。
  `calibrate.ts` 已加这条红线（实例：`voteThreshold.meanReversion = 1`，训练 −3.73% / 验证 +2.77%，
  原先排第 2）。见 [M2 §5.14](./docs/notes/M2-偏差报告.md)。
- **回测里「胜率」有两个口径，别混用。** 一行 `trade` 是**一次卖出**，而回撤减仓会把一次建仓
  拆成两三行 —— 逐笔胜率 **33.16%**、建仓级胜率 **49.28%**（同一份数据）。
  用户体验到的是后者（「我按提醒买了一次，最后赚没赚」），标定「提高胜率」也只能盯后者
  （`groupPositions()`，报告里「建仓」那一行）。
  同一个成因曾制造过一处符号错误：分 Regime 归因的「平均」原是逐行**未加权**百分比，
  四种市况全打 −1.2% ~ −1.6%，而净盈亏是 **+231,154 元** —— 半仓那一行和满仓那一行被
  等同看待了。已改为按仓位加权（`weightedPnlPct`）。**`TradeStats.avgPnlPct` 保留但不要用它
  判断赚没赚**，它的符号可以与净盈亏相反（M2 §5.18）。
- **「提高胜率」不能单独当标定目标。** 把 `profitProtectTrigger` 调低（小赚就跑）能机械地把
  胜率做到 70%、同时把盈亏比压到 0.5，那是更差的系统。`calibrate.ts` 的写回门槛因此是
  **「建仓级胜率不降 + 收益显著提高」两条一起卡**，别只改一条。
- **回测的「超额收益」离开「平均资金占用率」就会被读反。** 基准（沪深300）是满仓的，
  信号策略绝大多数时间空仓 —— 实测出厂参数下平均占用只有 **4.15%**，超额 −8.17pp，
  但最大回撤 2.44% vs 基准 45.60%、Calmar 0.275 vs 0.034，风险调整后方向是反的。
  `performance.exposure` 已经进了报告并紧挨着超额那一行打印，**别把它删了**。
  反过来「按占用资金折算成年化 10.8%」也不许拿去说事（M2 §5.13 列了三条理由）。
  **2026-08-19 这条有了净值层面的独立确认，同时否掉两个「看起来更专业」的度量**
  （[M2 §5.41](./docs/notes/M2-偏差报告.md)）：**beta** 与占用率逐份同向
  （3.49% ↔ 0.0205 · 11.52% ↔ 0.0371）⇒ **已进报告并紧挨着占用率打印**（与除法版超额一起）；
  而 **CAPM alpha 不许当判据** —— 低暴露策略上它的**符号由 `Rf` 决定**（同一条曲线
  rf=0 → +1.29%、rf=4% → −2.57%，而真相是 +24.14% vs 被动 +301.70%）；
  **日胜率也不许** —— 实测 ≈ 基准下跌天数占比（45.9% vs 45.9%），空仓日基准跌它就「赢」。
  另注意**减法超额在训练窗口上是 +14.06pp 而 alpha(rf=0) 是 −0.28%**（同一条曲线、符号相反）
  ⇒ 别引用「跑赢基准 14 个点」。引用超额一律用**除法版**（`idx-cap-0517` 上减法给 −277.56%，
  除法给 −69.10% = 净值只有被动的 30.9%）。
  顺带：所有消融差值都在 ±1pp 这条噪音带里，是因为**那是总资金口径**，4% 的占用把差异压扁了约 24 倍。
- **⚠ 夏普里「减无风险利率」不许整体减，只能按逐日持仓占用收**（2026-08-21，
  `riskFreeAdjustedSharpe`）。用户拍板 rf = 2%,而**直接减是罚两次**:逐日占用只有 3.65%,
  回测与影子**都不给现金计息** ⇒ 那 96% 的钱在账本里一分没赚,再按满仓标准扣掉整个 rf,
  扣的是它从来没拿到过的收益。实测同一条曲线:rf=0 **−0.412** · 只对持仓收 **−0.502** ·
  直接减 **−2.888**(⇐ 不许引用)。差 2.4 个夏普全来自分母被空仓稀释约 24 倍
  (`rf/243` 占策略日标准差 15.9%、占沪深300 的 0.66% ⇒ **同一个 rf 对低暴露策略的伤害
  是对指数的 25 倍**),与上一条那个「alpha 的符号由 `Rf` 决定」同源。
  两条边界:① **`performance.sharpe` 永久是 rf = 0**,新口径走并排的 `sharpeNet` +
  `meta.riskFree`(`--rf`,默认 0 ⇒ 默认不算不打印,`auditKnobs` 刻意不认它 ——
  它不改任何模拟结果);② **别真去给现金计息** —— 六年的现金利息会把 `totalReturn`
  从 −1.99% 抬成正数,而那是货币基金赚的。**引用 `sharpeNet` 必须带上 rf 的取值**(自由参数,同 DSR 的 `N`)。
- **⚠ 影子面板的夏普在样本不足时显示「样本不足」,不是数字。** 真机 4 个净值点
  (其中一天全空仓、日收益率恰好 0.0000%)算出年化夏普 **12.58** —— 世界上最好的基金
  常年是 2–3,那个量级唯一表达的是**样本量**。阈值 `SHARPE_MIN_BARS = 20` 是**展示阈值
  不是统计阈值**:真门槛是 MinTRL 且以**年**计(夏普 0.5 要 11 年),按天设的线只拦最离谱那一档。
- **消融差值不等于某个机制的作用量。** 「459 + 491 = 950，实际 762，所以裁决压掉 188 笔」——
  错的，那个差值里混着裁决压掉的、两个策略得分相加后新触发的、单标的单持仓挤掉的三件事。
  单独关掉裁决测出来是 128 笔。要测一个机制就得单独关掉它，**而且往往不用改代码**：
  冲突裁决判矛盾的条件是 `gap < conflictBand`，把 `conflictBand` 设成 0 就恒假，
  行为恰好等于「弱侧让路」。已测结论：**出厂的「压掉双方」在每个维度上都优于弱侧让路**
  （M2 §5.12），别去改 `adjudicate()`。
- **标定报告要读的是最上面那行「裁决」，不是「优胜候选」那一行。** 2026-08-12 之前
  这个工具只会按验证集 Calmar 排名并推荐第一名，于是十一轮标定里每一轮都是「工具报出优胜者
  → 人看表发现它是孤峰或验证窗口友好 → 否掉」，判据全在人脑子里。现在三态：
  `WRITE_BACK`（走写回流程）· `KEEP`（**出厂值站得住，这是结论**，按清单 4.9c 归档）·
  `INCONCLUSIVE`（出厂值自己被红线淘汰或折数不足，什么都不能说）。
  **`KEEP` 不是失败**，把它读成失败就会有人把同一个网格再跑一遍（已经发生过）。见 M2 §5.15。
- **候选是与出厂值「逐折配对」比的，不是比谁的分数高。** 折单元 = 标的子集 × 时间片
  （默认 4 × 3 = 12），**从同一次模拟里切出来，加折不加耗时** —— 加折的代价是每折更薄。
  写回门槛是 `|Δ|/标准误 ≥ 2` 且 ≥ 2/3 的折为正。项目一直口头在用的「±1pp 噪音带」
  「正负交替 = 噪音」就是这个数的手算版本，现在别再手算了。
  单折绩效**不可**与整池绩效横向比较，也不可单独引用（窗口短、标的少、回撤分母小）。
- **相关性调整 2026-08-19 已做（三处做了两处半），但有一处刻意没修，别当成全修好了。**
  病根：`stdev(Δ)/√n` 的 `√n` 收敛只在折独立时成立，而 12 折是**从同一次模拟里切出来的**。
  现在 `pairedDelta` 的判据是**按时间片聚类的 CR1**（等大簇 ⇒ `stdev(簇均值)/√G`，
  也就是**横截面折不再贡献 √n** —— 加折只让每片更稳、不增加独立样本），
  朴素值保留并标「未调整上界」，报告每次都印调整状态。
  `audit:random` 的择时零分布默认改**按建仓月整块位移**，`--independent-days` 是旧口径。
  三条读法：
  ① **无条件口径的块位移是精确的**（吸附距离恒 0），实测四层配对胜率一致朝 50% 走
  （ALL 46.5 → 55.5 · TREND_UP 36.7 → 43.2 · TRANSITION 24.7 → 38.5）⇒ 旧分位确实偏向显著；
  ② **`--match-regime` 那一档 2026-08-20 换成了 `REGIME_BLOCK`**（块 = 标的 × 一段连续
  同状态行情，整段刚性平移到同状态的另一段 ⇒ **吸附恒 0 是结构保证**，覆盖 99.8%）——
  旧的「自动降级回独立抽日」已经取消，`--independent-days` 仍能复现旧口径；
  ③ **夏普的 `×√243` 只加了标注、算法没改** —— 它不参与任何门槛，
  而 Newey-West/Lo 要选截断阶数，那该单独做一次。
  实验与全部数字见 [M2 §5.36](./docs/notes/M2-偏差报告.md)，
  结案与未完成项见 [迭代计划 §4.6](./docs/notes/下一阶段取舍与迭代计划.md)。
  **跨票口径本来就没这个病**（它固定日期，时间聚集原样保留），所以 §5.35 的 55.0% 不受影响。
- **与基准比的量（信息比率 / beta）必须走 `alignedReturns` 严格配对，别各算一遍再按下标塞。**
  `returnsOf(points,'equity')` 与 `returnsOf(points,'benchmark')` 各自会跳过自己缺值的期
  ⇒ 基准列一有空洞两个数组就**错位**，算出来的是「策略第 100 天 vs 基准第 103 天」，
  **不报错、只给一个错的数**。回测 fixture 里基准零空洞所以从没踩到，但**影子的基准列真的会缺**
  （2026-08-19 真机 2/2 行 null）。2026-08-20 两处都改成严格配对，回测数字逐位未变
  （信息比率 0.04 / beta 0.0177 重跑一致）。影子面板同时加了 beta，**但刻意不给超额** ——
  那一屏的既有决定是「不算差值」（基准满仓 vs 策略空仓），而除法版并不解决这个分母问题。
- **⚠ 配对胜率对零分布定义极度敏感，引用它必须带零点定义**（2026-08-20，
  [M2 §5.42](./docs/notes/M2-偏差报告.md)）。同一个 ALL 三个零点三个答案：
  **46.5%**（独立抽日 · 同 regime）· **55.5%**（建仓月块 · 无条件）· **36.5%**（段块 · 同 regime），
  跨度 19pp ⇒ 「ALL 46.5%」这种说法单独拿出来已经没有意义。
  **四个零点下同一批 1675 次建仓的极差是 20–30pp**（[§5.43](./docs/notes/M2-偏差报告.md)）：

  | 层 | 独立抽日 | 段块·按位置 | **段块·按段（默认）** | 跨池（§5.35） |
  |---|---|---|---|---|
  | ALL | 46.5 | 36.5 | **31.3** | 55.0 |
  | RANGE | 65.0 | 43.5 | **39.2** | 35.5 |
  | TREND_UP | 66.5 | 67.0 | **74.2** | 99.5 |

  ⇒ **口径的效应比任何一次判定逻辑改动（几个 pp）都大一个量级**，而三个零点都在合理设计空间里、
  分不出胜负。**两条纪律**：① 同 regime 的分位只能支撑**粗结论**（方向 + 跨口径一致性），
  不能支撑「66.5 比 62.0 好」这种精细比较，也不能当写回门槛；② 改动前后必须**同一零点**。
  ⚠ 两次机制预测**都被证伪**：先是「旧口径方差偏小」（实测 sd 几乎没动、是均值移动），
  再是「长段权重是主因」（改成按段均匀之后均值继续上移、TREND_UP 反向大幅下移）——
  真实情况是**短段与长段的日子性质不同，且在不同 regime 里方向相反**。
  所以 `--block-weight` 默认取 `runs` **只有语义依据**（block permutation 的语义是交换块），
  **不是**因为它的数字更好看（它对策略更不利）。
  跨口径一致的只有一件事：**RANGE 四个里三个 ≤ 50%**。
  ⚠ **「TREND_UP 四个零点都 > 65%」这句 2026-08-20 被中位口径推翻** —— 它只在**加权**口径下成立：
  `TREND_UP` 加权 74.2% / **中位 35.2%**；`ALL` 加权 31.3% / **中位 2.8%**；
  `TRANSITION` 加权 20.3% / **中位 0.2%**（`RANGE` 两个口径一致，39.2 / 40.5）。
  ⚠ **2026-08-27 加上第三个自由度，而它是纯精度问题：`--trials` 的默认值 200 太小**
  （[M2 §5.73](./docs/notes/M2-偏差报告.md)）。`pairedMedianWinFraction` 是**试验数上的二项比例**
  （`SE = √(p(1−p)/trials)`）⇒ `trials=200` 下三个种子的极差实测 **6.00pp**，
  而 `trials=2000` 压到 **1.65pp**（分层不同：`RANGE` 4.90pp · `TRANSITION` 0.25pp，**要各自量**）。
  ⇒ **看板与 §5.61 引用的「`ALL` 中位 8.50%」是 `trials=200` 的一次抽样**，
  同一个量在 `trials=2000` 下是 **10.55 / 12.15 / 11.65%**。
  **L2 条件①（> 50%）不受影响**（差门槛还有 38–41pp），但**引用配对胜率必须带 `trials`**。
  单次只要 6 秒（200）/ 14 秒（2000）⇒ 提高它几乎不花钱。
  ⇒ **✅ 2026-08-27 用户拍板：默认已改成 2000**（[§5.76](./docs/notes/M2-偏差报告.md) ·
  [CHANGELOG](./CHANGELOG.md)）。`trials` 是**蒙特卡洛复制次数不是样本量** ⇒ 估计量、零点、
  门槛与所有已报裁决一字未改。报告与看板现在都把本次 `trials` 印在配对胜率旁边、
  并印噪音地板，**`trials < 2000` 的归档报告会被点名**（用例钉着，改回 200 会变红）。
  ⚠ 出厂口径的 alpha 基线现在是 `random-l2a-train-t2k.json`；200 那份**刻意留着**（对照的另一半）。
  ⚠ **第四个自由度，而它是最容易读错的一个：配对胜率是效应量的饱和变换 ⇒ 幅度不可跨层比**
  （2026-08-27，[M2 §5.74](./docs/notes/M2-偏差报告.md)）。
  `胜率 ≈ Φ(μ/σ_D)`（`μ` = 真实中位 − 随机中位）⇒ 灵敏度 `φ(z)/σ_D` **在 50% 处最大、
  往 0%/100% 两端塌缩**，而 `σ_D ∝ 1/√n` ⇒ **层越大，同样的效应给出越极端的胜率**。
  实测：`RANGE`（基线 51.4%）的增益是 `TRANSITION`（0.9%）的 **24 倍**
  —— `RANGE` 的 `μ` 只动 +0.09pp 就让胜率动 12.7pp，而 `TRANSITION` 的 `μ` 动了 −0.34pp
  （**2.7 倍大**）胜率才动 0.6pp。33 层里按胜率排 vs 按 `μ` 排 **Spearman 只有 0.865，
  最大错位 11 位**（`ALL` 胜率排 30/33 而 `μ` 只排 19/33 —— 因为它 n 最大）。
  ⇒ **三分清楚**：① **阈值型完全有效**（`Φ` 单调 ⇒ 「> 50%」≡「μ > 0」，**L2 条件① 一字不改**）；
  ② **符号与排序方向**有效，但排的是**信噪比不是效应量**；③ **幅度与跨层的 Δ 无效**。
  ⇒ 要比幅度就看 `pnpm audit:random` 打印表里新加的 **效应量 μ** 那一列
  （闸门在 `tests/unit/backtest/random-audit.test.ts`，删掉纪律行会变红）。
  ⚠ **我自己因此读错过一次**：§5.73 ④ 把 `RANGE` 的 13.1pp 写成「分辨力集中在 RANGE」，
  已在原处加撤回标注 —— **原数一字未改，撤回的是读法**。
- **⚠ 引擎的买入得分是一个**负向**因子（2026-08-20，[M2 §5.46](./docs/notes/M2-偏差报告.md)）。**
  新工具 `pnpm audit:ic`（rank IC，**不构造零分布** ⇒ 不吃上面那两个自由度）：
  252 只 · 387,097 个判定根 · 得分 > 0 子集，逐日横截面 Spearman IC
  **训练窗口 −4.41% / −4.98% / −4.74%（5/10/20 日）· 验证窗口 −1.43% / −0.87% / −3.15%**
  ⇒ **得分越高、未来收益越低**，五等分近单调，两个窗口**符号一致但强度差 2–5 倍**。
  与 §5.45「追高那档中位配对 0.5%」**独立同向**。三条限制必须一起读：
  ① ~~**t 值是上界别引用**~~ —— **这条 2026-08-20 已撤回**（[§5.47](./docs/notes/M2-偏差报告.md)
  的 Newey-West 调整）：训练窗口调整后 `|t|` **3.7–6.1**（三个持有期都过 2）⇒ 主判据有能引用的数了；
  但**朴素 t（−8.9 ~ −10.0）永久不许引用**，验证窗口 5/10 日**测不出**、20 日 −2.01 **压线不许承重**；
  ② 五等分的收益**水平含当日市场共同项**，只能看组间差（IC 本身干净，秩相关在当天内消掉市场项）；
  ③ **不许推「反着做能赚」**（零成本、不能卖空，且 Q1 基本是「没有任何信号的票」）。
  ⚠ **2026-08-31 补上一条对照，它同时是「判外部因子」的标准动作**（[M2 §5.83–§5.85](./docs/notes/M2-偏差报告.md)，
  `pnpm audit:ic -- … --price-factors`）：竞品因子目录里的三个「收益形态」因子在**同一批判定根**上
  未控制时全部通过判据，**控制 20 日动量之后两个塌掉**（`ret_skew_20d` → `|t| < 2` ·
  `up_days_20d` **符号翻正**），只有 **`max_ret_20d`（20 日内最大单日涨幅 = 彩票效应）几乎没掉**
  （−9.02% → −7.36%，收缩 **18%**，`|t|` 5.47，两窗同号），而它的 IC **比我们自己的买入得分强一倍**。
  ⇒ **判一个外部因子有没有信息之前，先减掉我们已知的那个暴露** —— 引擎得分自己的负 IC
  在控制 mom20 后就掉到 `|t| < 2`（§5.70）⇒ 不做这一步会把同一个暴露的第四、五、六次测量
  说成三个新发现。⚠ 两条别读错：**它不是「加了一个因子」**（约束 5 要论证先于代码，那份论证还没写）·
  **可用形态是入场过滤不是选股**（IC 为负、不能卖空）。
  ⚠ 还有一条读法：**中性化过的臂里 `quintileMedians` 是残差秩不是收益**（能打出 `+649%` 这种数）。
- **⚠ 判据里写「符号一致」时必须把符号一起写死 —— 否则翻转的因子会以「一致」的名义通过**
  （2026-09-01，[M2 §5.88 预注册 / §5.89 结果](./docs/notes/M2-偏差报告.md)，
  `pnpm audit:ic -- … --liq-factors`）。Amihud 非流动性 `amihud_20d` 未控制时 IC
  **+3.89 ~ +6.97%**（流动性溢价方向，`|t|` 4.0–4.5），**而控制掉它自己的分母之后
  符号翻转成 −4.3 ~ −4.7%**（`|t|` 3.48–5.60、验证同号）⇒ 主判据「三个持有期符号一致 +
  至少两个 `|t| ≥ 2` + 验证同号」**字面全部成立**，而那个「一致」是三个都翻成了负
  ⇒ 字面裁决与实质结论指向相反方向。**裁决 `INCONCLUSIVE`**（残差主要是分子 `|r|` =
  波动率代理，而那个暴露本地已确认为负；而本轮四条臂里 B3 是「**A** + `max_ret`」
  不是「**B2** + `max_ret`」⇒ 缺的正是能判它的那条，已登记 `amihud-residual-vol`）。
  ⚠ 它与 §5.85 里 `up_days_20d` 那次「符号翻正」同形状，**当时判对纯属侥幸**
  （翻转后 `|t| < 2`，两种判据给同一答案）⇒ **一条判据「用过几次没出错」不构成它正确的证据。**
  **能判的那一半有强证据**：`corr(完整, 纯成交量分量)` **0.893 / 0.867**、分量单独比完整测度
  强 **1.5–1.8×**、控制它之后正号完全消失 ⇒ **Lou & Shu 在 A 股完整重现**
  ⇒ **Amihud 那个正 IC 不是价格冲击信息。**
  ⚠ 三条别读错：① **`neg_dvol_20d`（IC 比整套引擎强 1.5–2.2×、两窗同号）不是候选** ——
  它在预注册里的身份是**控制变量**，看过数之后给一个量换身份等于事后换臂；
  且它与小市值分不开、成本方向恰好最不利（「越冷门越好」命中的正是 §5.54 冲击最凶那批）。
  ② **`amount` 列全空 ≠ 算不出 Amihud** —— `DVOL = volume × close`（**两边都不复权**）就是成交额，
  这条代理本项目自己验证过（`data/liquidity/*.json` 的 `units` 行：误差中位 **0.44%**）
  ⇒ 「缺某一列 ⇒ 这个量算不出来」这个推断，**先问一遍「它能不能由已有的列构造」**。
  用 `closeAdj × volume` 会得到放大上百倍的假成交额且**不报错**，只把老票判成「最流动」。
  ③ **拿文献的数当锚必须取到定义式**（§5.89 ⑨）：事后取原文查出我构造的 `−mean(DVOL)`
  **不是**原文那个 `A_C = mean(1/DVOL)` ⇒ 本地 0.893 与原文 0.90 相符属巧合；
  且 0.90 / 0.75 是**两个不同测度**（`A` 与换手率版 `AT`）。
  **WebSearch 摘要在「结论」层可靠、在「构造」层不可靠。**
- **⚠ 登记项 / 判据里的「A/B」是个陷阱：只要 `corr(A,B) ≠ 1`，那就是两个问题**
  （2026-09-02，[M2 §5.90 预注册 / §5.91 结果](./docs/notes/M2-偏差报告.md)）。
  补上 §5.89 缺的那条臂（**B4 = mom20 + 纯成交量分量 + `max_ret_20d`**，`residVolArm`）之后
  `amihud_20d` 训练 **−2.47 / −2.68 / −2.64%（`|t|` 4.35 / 3.49 / 2.53，三个都过 2）**、
  验证三个同为负、不复权臂同号 ⇒ **主判据通过**（这次符号写死了，字面与实质同向），
  **`max_ret_20d` 只吃掉 42.0%** ⇒ 裁决从 `INCONCLUSIVE` 换成
  「通过（**限定：独立于彩票效应**）」，进「值得写一份论证」那一档 ——
  它是**本地目前唯一一个减掉所有已知暴露之后还站着的外部因子**，
  且**成本方向有利**（滤掉高残差 = 滤掉 §5.54 冲击最凶那批，与 `neg_dvol_20d` 相反）。
  ⚠ **但通过的只有「独立于彩票效应」，「独立于波动率」压根没测** —— 登记项标题写的是
  「波动率/彩票效应」，而臂只有 `max_ret` 一条；BCW (2011) 原文报 `corr(IVOL, MAX) ≈ 0.75`
  （**不是 1**），本地 `corr(B2 残差, max_ret)` = **0.500 / 0.554**
  ⇒ **那份论证要等「B2 + 20 日已实现波动」那条臂**（已登记 `amihud-residual-truevol`），
  **不许在 §5.91 那批数上顺手加**（事后换臂）。
  ⚠ 两条读臂的经验，都是我事先押反的：① **比率型因子的横截面相关方向由主导项决定** ——
  `amihud` 由分母主导（`corrWithDvol` 0.893）⇒ 它与 `max_ret` 的秩相关是 **−0.128**
  （我押 +0.15~+0.50，符号都错），因为 `corr(neg_dvol, max_ret) = −0.436`
  ⇒ **A 股暴涨日伴随成交额放大**；② **秩中性化下多加一个真正相关的控制变量是降方差的**
  （`sd(IC)` 14.76% → **11.07%** ⇒ `|meanIc|` 掉 42% 而 `|t|` 只掉 22%，**效应更小但更干净**）
  ⇒ **别把线性回归「共线抬高标准误」的直觉搬到秩残差上**。
  ⚠ 一条元经验：本轮**先取原文再写预注册**（BCW 三个门全通，几分钟），四条口径全抄准，
  **而 5 条预测只对 2 条** ⇒ **原文解决「口径对不对」，不解决「本地会不会这样」。**
- **⚠ 幸存者偏差的方向不能靠机制推理定，必须在「那个量」上量一次**（2026-08-31，
  [M2 §5.86 预注册 / §5.87 结果](./docs/notes/M2-偏差报告.md)）。同日把 `max_ret_20d` 放到
  含退市并集池（**485 只** = 252 存活 + 233 退市）上重跑：控制 mom20 后训练
  **−7.54 / −8.36 / −9.48%**（`|t|` 11.59 / 9.37 / 8.30）· 验证同号 ·
  不复权稳健性臂差 ≤ 0.25pp（⇒ §5.65 那条「退市股复权口径分歧 38%」在这个用法上**不 binding**）
  ⇒ 主判据通过；而效应比存活池**强 28.8–34.6%** ⇒ **存活池是低估，不是高估**。
  我事先按「暴涨过的票后来退市 ⇒ 存活池缺了最差结局 ⇒ 高估」推出的方向**是反的** ——
  漏掉的是「退市股同时是**因子暴露最强**的那一批」。
  ⇒ **§5.26 那个 −0.13pp 是「总收益」上的幸存者偏差，不能外推到「因子 IC」上**：
  同一个池子缺失，在两个量上**连符号都可能不同**（总收益高估 / IC 低估），
  而 `audit:random` 的配对胜率对它基本不敏感（同一只票内比，票自身暴露在两组间抵消）
  —— **三个量、三种敏感性，别互相外推。**
  ⚠ 同一轮还长出一条：**并集池上 `ret_skew_20d` 的数变好了，但那不构成「它通过了」** ——
  §5.86 的预注册明写「只判 `max_ret`」且数已被看过 ⇒ **它永久不能在这批数据上判**；
  §5.85 对它的「不过」只能**降级为 `INCONCLUSIVE`**（用错了池子）。
  **撤回一个裁决 ≠ 立一个新裁决**，这是那一格最容易滑坡的地方。
  ⚠ 跑短窗口**必须带段前历史 + `--eval-from`** —— 直接 `--from 2024-01-01` 只剩 54 个有效日
  （300 根预热吃光了），测试窗口是 0 天。这与「跑验证窗口必须带段前历史」是同一个坑。
- **⚠ 配对胜率有加权与中位两个口径，背离时以中位为准，而它们背离得很厉害**（2026-08-20，
  [M2 §5.45](./docs/notes/M2-偏差报告.md)）。加权口径用 `weightedPnlPct`，**被少数大赢家托着**
  （先例：SZ002969 一笔 +325.8% 翻转过整组符号）；窄分层上更极端 —— `流通市值 Q2`（368 次建仓）
  加权 **87.0%** 而中位口径下两组几乎相同（−0.545% vs −0.532%）。
  `pairedMedianWinFraction` 已进报告与看板，**两个必须并排读**；旧报告（08-20 之前）没有这个字段。
- **⚠ 「多重比较」终于有数了，但它管的量不是我们的主判据**（2026-08-20，
  [M2 §5.48](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/dsr.ts`）。
  DSR（Bailey & López de Prado 2014）在 6 年训练窗口上给出的门槛是**年化夏普 ≈ 1.0**
  （N = 98 个去重后的网格配置）—— 而**多重比较只占 0.30，剩下 0.68 是「1457 根就是不够长」**
  ⇒ **「跑出一个夏普 0.3 的候选」在这个窗口上本来就说明不了任何事，与试了多少次无关。**
  四条别读错：① **它不进写回门槛** —— 门槛判的是逐折配对 Δ，DSR 判的是单条曲线的夏普，
  不是同一个问题；唯一合法用途是**进下一次预注册**（接不接进 `calibrate.ts` 是独立的判据变更，
  见[迭代计划 §4.10](./docs/notes/下一阶段取舍与迭代计划.md)）；
  ② **不许拿它回头重判已经报过的实验** —— 点名 `squeezeBbwPct = 20` 与 ETF 池那份
  （+2.14% / Calmar +0.092），那是移动球门；③ 计算**全程日频**：报告里的 `sharpe` 是 `×√243` 的，
  直接配 `T = 1457` 会把统计量放大 **15.6 倍**（脚本有一条自检复现作者算例、算出 0.8997 挡这一下）；
  ④ **`N` 由研究者自己申报、外部无法审计** ⇒ 与「调滞后阶到显著为止」同形状的通道，
  所以三档 `N`（27/98/300）一起报，脚本不提供选一档的接口。
  **PBO / CSCV 刻意不做**（不是待办）：它要 `T × N` 逐期矩阵，而网格只存聚合指标、
  `foldScores` 108 行全空（候选在算折分之前就被红线淘汰）；且它量的是**排名持续性**，
  而我们 **0/98 个候选夏普为正** ⇒ 算出来承不了重。重开条件是「候选族里出现多个正夏普」。
  ⚠ **真正卡在关键路径上的是 MinTRL 不是 DSR** —— 见下一条（同日已做）。
- **⚠ 「前向记录攒够时间」这条判据有数了：年数 ≈ 2.71 / 夏普²**（2026-08-20，
  [M2 §5.49](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/mintrl.ts`）。
  MinTRL（Bailey & LdP, *Sharpe Ratio Efficient Frontier*, J. Risk 15(2)）在 95% 单侧下：
  **年化夏普 0.5 要 11 年 · 0.3 要 30 年 · 0.2 要 68 年**，再乘自相关的 **×1.3**。
  而影子运行现在有 **3 个净值点**。⇒ **「等前向记录变显著」不是一个可执行的计划** ——
  这是 [迭代计划 §4.11](./docs/notes/下一阶段取舍与迭代计划.md) 那道**拍板题**，
  四条路（改判据用途 / 事先降置信度 / 重开研发做大效应量 / 什么都不改）等用户选，**我不替他选**。
  **三条读法别混**：① **不是「ETF 路线被否」**（否掉的只是那一个计划）；
  ② **不是对 ETF 回测那份的重判**（它的夏普只当效应量输入用了一行 ⇒ 152 年，
  +2.14% / Calmar +0.092 / 54.24% 一字未改）；③ **`SR*` 与置信度都是自由参数** ——
  `SR*` 从 0 换成基准能把 0.1 那档从 271 年砍到 114 年，而那一半来自
  **2018–2023 沪深300 自己夏普为负（−0.0545）**，不是策略变好。
  ⚠ **一个看起来像杠杆、实际不是的**：提高采样频率。IID 下每年观测数会被约掉
  ⇒ `MinTRL_年 = varTerm·Z²/SR_年²`，**换频率省不下日历时间**（文献那句
  「用最高采样频率报告」很容易被读反）；提高频率唯一的收益是让 `varTerm` 更接近 1，
  而那一项实测只有 **0.3%–7%**。**能省时间的只有把效应量做大**（平方反比）。
- **⚠ 「HAC 的 p 偏乐观」这句照抄了三处的文献警告 2026-08-27 量出来了：**只值 ≈ 2pp****
  （[M2 §5.80/§5.81](./docs/notes/M2-偏差报告.md)，`metrics.sharpeDiffBootstrap`）。
  接上 LW 原文推荐的**循环块自举**（Politis & Romano 1992 + Götze & Künsch 1996 + Eq. (9)）
  之后，三个配对 × 块长 `{1,5,10,20,40}` 实测自举与 HAC **最大差 2.4pp**，`b=1` 那档还更小。
  **根因是原文那条警告是 `T = 120` 的模拟结论，而我们的配对 `T = 1214–4548`（10–38 倍）**
  ⇒ **引用一条「小样本上有偏」的文献警告时，必须把原文的 `T` 一起抄下来**，
  否则会把别人样本量上的修正当成自己样本量上同样重要。
  ⚠ 三条边界：**`p` 仍不参与任何门槛、没有重判任何裁决** ·
  **块长报整条网格、不提供选一档的接口**（同 DSR 的 `N` —— 现成理由：§5.79 那个 `p`
  在 `b=1→40` 上走 0.0884 → **0.1142**，**骑在 0.10 线上**，挑哪档都能自圆其说）·
  **块长校准与预白化 QS 核刻意不做**。
- **⚠ 上一条那个「×1.3」2026-08-21 从近似升成了正式口径，而它当时就是对的**
  （[M2 §5.50](./docs/notes/M2-偏差报告.md)，`sharpeRatioHac` + `npx tsx scripts/verify/lo-sharpe.ts`）。
  补的是 **Lo (2002)** 的 `V_GMM = ∇g′ S ∇g`（`S` 是 `[r−μ, (r−μ)²−σ²]` 的 HAC 长期协方差）：
  夏普方差 VIF **1.3208 / 1.2975**（L=7 / 13）vs 原来那个**均值** VIF 1.3126 / 1.2865，**差 < 1%**。
  **但「差不多」有前提，别外推**：`V_GMM` 三项里 `S₁₁/σ²` 占 **100.5%**，是因为日频
  `SR_日 = −0.0264` 把后两项权重压到 `SR` 与 `SR²/4 ≈ 1.7e-4` 量级 ——
  **月频且夏普 ~1 时那个权重是 0.25，这条近似立刻不成立**。
  顺带两个：① **波动率聚集在数据里很强**（`S₂₂` 自己膨胀 **2.056×**，三项里最强）
  **但进不到结果里** —— 「机制存在、被权重抹掉」的干净例子；
  ② **`η(q)`（Lo 的时间聚合因子）在日频 q=243 上判不能用** —— 1456 个观测估 242 个 `ρ_k`，
  满阶给 22.0、截断给 13.5（`√243` = 15.6），相差 63.4% 且**连修正方向都不一致**
  ⇒ 它**没进** `metrics.ts`；也不需要它（`√q/η ≈ √VIF`，同一个长期方差的两个核，差 1.1%）。
  ⚠ **归属链此前写错了一环**（已改 `scripts/verify/stats.ts`）：正确顺序是
  **Jobson & Korkie (1981) → Lo (2002) → Mertens (2002)（是对 Lo 的更正**，指出 Lo 那段
  只在 IID **正态**下成立）**→ Christie (2005) → Opdyke (2007)（证明后两者同式）**。
  而**最容易读反的是下一句**：「Mertens 的闭式在平稳遍历下也成立」**不等于**
  「自相关已经处理了」—— 它用**同期**矩，自相关只能从**长期**协方差进来。
- **⚠ 「`ledger × 影子` 的差 = 执行滑移」这个提法是错的，别照着它做**
  （2026-08-22，[M2 §5.53](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/impl-shortfall.ts`）。
  按 **Perold (1988)** 的 `IS = C_d + C_i + C_e + C_o` 分解，两边的差主项是
  **`C_d`（延迟）且符号是反的** —— 影子**只吃 CONFIRMED**（收盘后才有）且**次日开盘成交**，
  而人看到的是**盘中**信号、当天就动手 ⇒ **人比纸面组合早**。实测重叠标的差 **≈ 41 bp**
  = 10 bp 影子按设计收的滑点 + **≈ 31 bp 一天的时间差**（`|C_d|/|C_i| ≈ 3.1×`，n=1）。
  ⇒ **那是前向纪律的代价不是执行质量**，要「先分解，再谈执行」。
  三条一起记：① **「滑移」只是 `C_i`** —— `--slippage` 是 `C_i`、`trade.costs` 是 `C_e`，
  **`C_d` 与 `C_o` 我们从来没量过**；② **决策价的口径能翻符号，而这次能定对错** ——
  按信号日**收盘**算用户是 −36.8 bp（像能力），按 **`price_at`**（气泡弹出那一刻）算是
  **+18.5 bp**（延迟成本）；**这是「提醒 → 人决策」的产品，决策发生在气泡弹出那一刻**
  ⇒ 用后者。⚠ 与 GH1 那个匹配口径**不同**：那里两个都站得住只能预承诺，**这里能判对错**
  ⇒ 「发现两个口径 ⇒ 问能不能符号相反」之后要再接一问「**其中一个是不是错的**」；
  ③ 对 L0 这个形态 **`C_o`（没照提醒执行的）比 `C_i` 重要得多**（后者上界只有 10 bp），
  但它的口径是**产品判断**（故意不动手是人该有的权力）⇒ 等拍板。
- **⚠ `shadow_trade` 的行数不能用来判断影子有没有在交易。** 它记的是**已平仓的往返** ——
  4 笔买入还持着的时候 `count = 0`，与「影子压根没跑」长得一模一样（2026-08-22 我照着它判错过一次）。
  判「在不在跑」要看 **`shadow_journal`**（013 那张表存在的理由就是这个）。
  ⚠ **但 2026-08-28 之前它长期为 0 还有第二个原因，别把两件事混起来**：影子的四条强制
  离场规则读的是**用户的**持仓表 ⇒ 影子持有、用户没录入的票根本不会离场
  （见上面那段与 [docs/07 §2.3c](./docs/07-回测与验证方案.md)）。
  实测 2026-08-28：9 个净值点、7 只持仓全在、`shadow_trade` **0 行** ——
  那不只是「还持着」，是**这批仓当时没有任何离场通路**。
- **⚠ 「每只每天约 2 行」这个说法在真机上不成立，而根因 2026-08-24 已判清**
  （[M2 §5.56](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/signal-dedup.ts`）。
  签名修复**之后**仍有 **206 / 408** 个相邻对签名**逐字段相同**（`stage` 事后改写的上界只有 38）。
  **但机制不是「每轮都插」** —— SAME 对间隔中位 **153s**（≥ 5 轮 tick）⇒ 中间那些轮没落行
  ⇒ 只剩一个解释：**`persist()` 里「`direction === NONE` 且 `verdicts` 为空」那条早退分支
  不落行、却照样 `set` 签名**（`signals.ts:270-273`）⇒ 把**上一次真落库的**签名擦掉了，
  于是「有信号 → 什么都没发生 → 同一个信号」每次**回来**都落一行新的。
  按观测流回放：446 → **240 行（−46%）**，`SH601933` 08-21 那组 **73 → 3**。
  **同日已修**（那个 `set` 删掉；它想做的去重由早退语句自己就完成了）——
  `ENGINE_VERSION` 不变、判定一个字没改，回归用例是「A → 什么都没发生 → A：只落一行」，
  **必须写成同一个引擎实例中途换行情**（这个缺陷在纯函数里看不见）。
  ⚠ **不要顺手把 `subs` 从签名里移出去** —— 它占可见变化的 66.8%，但那些行**本来就该落**
  （2026-08-14 加它进签名正是为了不让 `evidence` 变陈旧）。**真正错的是「约 2 行」这句话**：
  它把「重复被挡住」写成了「一天只有两件事发生」。
  ⚠ 两条可迁移的：① **函数正确 ≠ 用它的状态机正确** —— `signalSignature` 有用例钉着，
  而 `persist()` 这个状态机一条都没有；② **「相邻两条记录一模一样」时，第一问不是「谁在抖」，
  而是「这两条之间还发生过什么」** —— 在只记录了「被记录下来的状态」的数据里找抖动，
  会系统性漏掉那个**没被记录**的状态。
- **⚠ 「超额」有两个答案而且能符号相反：跟满仓比 vs 跟同风险的被动持有比**
  （2026-08-22，[M2 §5.52](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/outside-pool.ts`）。
  差距文档 §2.2 那个「优先级最高的真空」补上了，口径是 **GH1**
  （**Graham & Harvey**, JFE 42 (1996) / FAJ 53 (1997)）：把**基准**与现金按每日恒定权重
  混到 `σ` 等于组合的 `σ`，再比收益。训练窗口 `w = 4.18%` ⇒ **GH1 = −1.71pp**，
  修正后的验证窗口 **−0.37pp**，**两窗同向为负** ⇒ **同风险下，4~5% 的钱买沪深300、
  其余持现，比这套主动择时更好。** 而报告现在印着的除法版超额是 **+16.75pp**
  ⇒ **两句同时为真**（基准跌了 16%），**引用超额必须两个一起给**。
  四条边界：① **匹配口径是个能翻符号的自由度** —— 指数那份 σ 匹配 −16.40pp、
  占用匹配 **+0.72pp**（同零点定义 / 加权口径 / DSR 的 `N` 那一族）⇒ 未拍板前**没进报告**；
  ② **`GH2 / M²` 判不能用** —— 要 `σ_m/σ_p ≈ 20–24` **倍杠杆**（拿不到）、`R_f` 系数 22.9
  ⇒ **符号由 rf 支配**（同 §5.41 否掉 CAPM alpha，放大 24 倍）、且 `M² ≡ (SR_p−SR_m)·σ_m`
  ⇒ **零新信息**；③ **只能在评估期上算** —— 带预热段等于白送被动参照一段市场暴露，
  且偏差方向固定不利于策略；④ **线性近似 `w × 基准收益` 绝不能用**（差最大 **16.81pp**，
  **符号由基准方向决定**，主项是复利的凸性不是波动拖累）。
- **⚠ `abl-valid-base` / `abl-valid-nodd` 这一批（`--from 2024-01-01`）是「预热吃掉窗口」的物证，
  别拿它们算任何东西**（2026-08-22 踩过两次）。它 18 个月里**前 15 个月净值一动不动**
  （首个变动日 2025-04-07），建仓只有 **34** 次 —— 上面那条「跑验证窗口必须带段前历史」
  里写的「只剩 34 次建仓」**就是拿它写的**。验证窗口一律用 `v2-*` / `dd-*-valid`
  （`--from 2022-10-01`，302 根预热）**并切到评估期 `>= 2024-01-01`**。
  ⚠ **别删那两份报告** —— 它们是消融对的两半，删了那个对照就再也做不出来；要的是认得出来。
  ⇒ 因此 [M2 §5.51](./docs/notes/M2-偏差报告.md) ④ 那一格（σ 0.38× · γ₄ 6.06× · \|z\| 1.95）**已作废**，
  §5.51 其余部分与 §5.31 不受影响。
- **⚠ 上面那一整族（HAC / 聚类 / 块置换 / MinTRL）压着的「平稳」2026-08-21 第一次被检验，
  H1 在方差上被拒 —— 但结论是「每个数带上窗口」，不是「工具作废」**
  （[M2 §5.51](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/stationarity.ts`）。
  CUSUM-of-squares（IID 版归 **Inclán & Tiao 1994**、HAC 分母那版是
  **Sansó, Aragó & Carrion 2004** 的 **κ₂**、临界值 1.358 = 布朗桥上确界 5%）
  在**策略 · 基准同期 · 基准 2005–2017 三条序列上一致拒绝**，而实测这个检验偏保守
  （水平 2.5–3.5% / 名义 5%，与 Garbade 1977 / Hackl 1980 一致）⇒ **每次拒绝都被低估过**。
  **四条要记住的**：
  ① **VIF 是这族里最稳的量**（比值、尺度不变）⇒ §5.50 那个 1.32 继续可用，
  但**引用必须带窗口**（「训练窗口上的 1.32」，分段是 1.09–1.56，而同期 σ 极差 2.30×）；
  ② **基准自己的 VIF = 0.9606（< 1，负自相关）** ⇒ 策略那个正自相关来自**持仓结构**
  （平均 14 根）而不是市场 —— 这是「持仓期 −1」那个副滞后阶的机制依据；
  ③ **均值那一侧根本测不动**（训练窗口三段夏普两两 \|z\| = 0.86 / 1.08 / 0.24，
  合成 HAC SE ±1.1）⇒ **「验证窗口反向」反复出现却推不出结论，根因就是这个**，
  以后引用那类反向要同时给出标准误；
  ④ **`abl-valid-base` 报的 `sharpe = 1.191` 不许引用** —— 去掉绝对值最大的 3 天变 **0.148**，
  最大的 5 天全落在 2025-06（窗口最后一个月），γ₄ = **79.70**。
  ⚠ 但它**不影响 §5.31 的消融结论**（同窗口内的**配对**比较，离群簇两臂都在）。
  **⚠ 别做两件事**：**不要对收益率跑 ADF**（它检验水平序列的单位根，而日收益已经差分过
  ⇒ 以天文数字的显著性「通过」，给出**虚假的安心**，同「日胜率 ≈ 基准下跌天数占比」那一类）；
  **不要拿 `r²` 的 Ljung-Box 去判 H1**（条件异方差**不违反** H1 —— Lo 原文写着平稳性允许
  "time-varying conditional volatilities"，H1 要的是**无条件**矩恒定，混起来两个结论都错）。
- **⚠ 引用一份回测报告之前先看它的口径行：出厂口径 vs 实验跑，此前在归档里认不出来**
  （2026-08-20 修，[CHANGELOG](./CHANGELOG.md) 顶部那条）。踩过的事：`pnpm iterate` 的
  「回测基线」那一行把 §5.44 候选 B 的 **5× 资金实验跑**当基线显示了一整天
  （1114 建仓 / 43.81% / 占用 3.61%，而出厂那份是 **1097 / 43.21% / 3.50%**）——
  挑选逻辑只看「最新 + 标的数 ≥ 200 + 含 trades」，三个旋钮一个都不查，
  而**偏差方向不随机**：调大 `--capital` 只会让「一手都买不起」变少、数字更好看，
  没有一处看起来像坏了。判据现在只有一个出处 `auditKnobs()`（`src/backtest/report.ts`）——
  报告自己按它打「非出厂口径」告警、看板按它挑基线，**别在第三处照抄一份**。
  三条读法：① **`meta.costs` 是 08-20 才加的**，之前的报告一律落「**无法核对**」那一档，
  而它与「没有偏离」是两件事 —— `noslip-train.json`（`--slippage 0`）是 **−1.21%**
  而出厂口径是 **−1.99%**，把「未记录」当「等于出厂」正是要防的事；
  ② 看板挑不到出厂口径时**报「读不到」并点名跳过的**，不退一步显示实验跑；
  ③ **`audit:random` 那一侧还没修**（输出 meta 不继承基线的资金与成本 ⇒ 非出厂口径的
  alpha 报告同样认不出，而 alpha 是主判据），见[迭代计划 §4.9](./docs/notes/下一阶段取舍与迭代计划.md)。
  **这条坑的形状比它本身值钱**：§5.44 的预注册里**写着**「这一跑不是新基线，不许拿它的
  绩效数字去替换任何既有引用」，纪律在文档里却没有任何东西执行它，同一天就被工具违反了
  ⇒ **写下一条纪律不等于装上一道闸门**，凡「不许拿 X 当 Y」都要先问「谁在挡」。
- **`--grid` 默认不再跑测试集**，要读得显式加 `--touch-test`，并去 docs/07 §3 ④ 的计数里记一笔。
  以前每次调用都自动烧一次测试窗口，历史上 5 次里几乎每一次的优胜者最后都没被采用。
- **`adx.maxThreshold` 在出厂配置下是算术上的死参数，不要标定它的上侧。**
  `adxTrend = clamp(base + volScale × volPct, base, maxThreshold)` 而 `volPct ∈ [0,1]`，
  所以被夹的那一项最大就是 `base + volScale` = 20 + 8 = **28**，恰好等于出厂的 `maxThreshold` ——
  上界永远咬不住，调到 34 / 100 / ∞ 逐折逐位相同（实测 `28 → 34` 动 0/12 折）。
  它只在 `base + volScale > maxThreshold` 时才活。往下调（24）才有作用。见 M2 §5.16。
- **`regime.hysteresisDays` 往上调不花钱，但别拿回测当判据。** 实测 1 是 −0.34pp（明显更差），
  而 2（出厂）/3/4/5 是一片分辨不出的高原（M2 §5.17）。
  **⚠ 2026-09-02 把那句「判据应该是提醒日志的每日改口次数」量了一遍，两件事同时成立**
  （预注册 [M2 §5.92](./docs/notes/M2-偏差报告.md) / 结果 [§5.93](./docs/notes/M2-偏差报告.md)，
  `pnpm audit:regime` 新增【改口率与段长】一节）：
  ① **数有了** —— 训练窗口 252 只 / 237,324 个相邻判定根对（下标相邻 **99.97%**）：
  **生效改口率 9.41%**（逐标的中位 9.35%，两个口径不背离）· **原始 16.94%**
  ⇒ **迟滞压掉 44.4%**，段长中位 **7** / 均值 **10.52** 根（原始 3 / 5.87），两窗差 0.60pp
  ⇒ 它**不是惰性参数**（对照 `adx.maxThreshold` 那个算术死参数）。
  顺带：**`TRANSITION` 平均段长 14.14 根 ≈ 三个星期、四态最长**（其余三态 6.5–7.6）
  ⇒ 「67% 落在 TRANSITION」的形状是「**长时间待在不出手的那一态**」，不是「四态之间抖」。
  ② **而那句判据本身是错的** —— `alert_log` 与 `signal` 表**都答不了 regime 改口**：
  `signalSignature` 不含 regime + `persist()` 那条「方向 `NONE` 且无裁决 ⇒ 不落行」的早退分支
  ⇒ 真机 15 个交易日只有 **22/79 只**出现过、相邻日配对覆盖率 **17.86%**（门槛 50% 事先写死）
  ⇒ 副臂判**不可用**，硬算那个 9.09% **不许引用**（缺的是「没事发生」的日子 ⇒ 系统性**高估**）。
  ⚠ **这个数单独仍然选不了参数** —— 缺的是「改口率应该是多少」，而它没有客观答案
  （是「少改口」与「转折认得晚」之间的取舍）⇒ 已拆成两条登记项：
  `regime-flip-curve`（待测，画那条曲线）与 `regime-flip-target`（**拍板**）。
  ⚠ 与 §5.56 撞上同一形状两次 ⇒ 通用问法：**「这张表里没有的那些行，是『没发生』还是『没记录』？」**
- **`KEEP` 有两种强度，归档时不许混**：`combine` 块是「怎么动都测不出差别」
  （15 个候选最大 t = 1.4，出厂值只是没被推翻）；`adx.baseThreshold`/`volScale` 是
  **「往测过的方向动都显著更差」**（最差 −1.06pp / t 4.1），那是**支持这个转述值**的正面证据 ——
  这个项目最缺的就是这种证据，别把它写成「都测不出」。报告的 KEEP 那一行会自动区分。
- **网格文件里不要塞 `_comment` / `_label`**：`expandGrid()` 把**顶层每个键都当成一个参数块**展开，
  多出来的键会静默地把网格乘大一圈（而且会被 `withParams` 合并进参数集、污染指纹）。
  说明写在 `params/README.md` 里。
- **收盘确认轮的买入 100% 会被改写成「明日观察」，这是算术，不是概率。**
  `engine.ts` 的 `minuteOfDayFrom()` 把 `minutesSinceOpen` 钳在 240，于是**收盘后任何一轮**
  算出来的 `minuteOfDay` 恒等于 900（15:00），而 T+1 尾盘窗口是 890–910。
  实测 46 只 2024 年起 28973 个判定根：组合层 182 次 `BUY`，`T1_LATE_BUY` **也是 182 次**。
  所以「买入」这个标签**只可能来自盘中轮**（实测 0.54%/股票日，约每只票 185 个交易日一次），
  而且收盘轮那条 `NEXT_DAY_WATCH` 会按「当日最后一条」把它盖掉。
  查「为什么没有买入建议」时先想到这条，别去翻数据源。
- **「明日观察」的次日出口是 `carryover()`，判据是两次独立成立。**
  昨日收盘那条已 `CONFIRMED` 且方向 `NEXT_DAY_WATCH`，**并且今天自己也判 `BUY`** ——
  昨天的结论不构成今天的理由（隔夜跳空能让它彻底失效）。它**不新发提醒**，
  只把今天那条抬到 L2 并写清来历：一天两条说同一件事只会让人觉得吵。
  「昨天」取的是**该股自己上一根 K 线的日期**，不是日历减一天（停牌与节假日天然处理）。
- **做T建议不是一个方向，别并进 `GatedDirection`。** 它挂在 `GatedSignal.tTrade`，
  因为 `signal` 表、回测的 `toOrder()`、影子运行**都只读 `direction`** ——
  混进去等于让那三处认识「日内位置」，而它们只有日线，看不见日内路径。
  四条边界：只对持仓、只在盘中、**不发提醒**（不进 alert_log、不点状态点）、不落库。
  持仓强制类或硬抑制命中时不给（止损那一刻并排一句「可考虑高抛」自相矛盾）。
  **尾盘不给低吸**最容易漏：T+1 下今天买的明天才能卖，过了 14:50 再买就是加仓，
  而用户以为自己在做T。见 `src/core/risk/intraday-t.ts`。
  **`TTradeInput.shares` 是「可卖」股数不是「持有」股数**（2026-08-19 改，`sellableShares()`）
  —— 今天刚买进的那部分今天卖不掉，把它当底仓会让同一句判据变成一条开仓建议。
  高抛与低吸**两侧都要**（低吸也是「先买，卖的是老仓那部分」）。**别改回 `position.shares`。**
- **`Position.lockedShares` 与 `stopFloor` 同一条纪律：回测与影子运行绝不设它**（2026-08-19）。
  它是「持有的这些股里今天买进的有多少」，A 股 T+1 下今天卖不掉。全仓锁住 ⇒ 卖出/减仓
  **硬抑制**（`T1_SELL_LOCK`，先例是「已跌停，卖不掉」）；部分锁住 ⇒ 只标注
  （`T1_PARTIAL_LOCK`）—— **两者混成一条**会让「今天买了 100 股」把 900 股老仓的止损提醒
  一起吞掉。代价要记住：硬抑制 ⇒ `candidates.ts` 直接 `continue` ⇒ **连 `alert_log` 都不落**，
  「今天买、今天就跌破止损线」当天一条气泡都没有（次日开盘归零后照发 L3）。
  可卖股数只有 `sellableShares()` 一处定义，三个调用方共用（硬抑制 / 做T底仓 / 界面）。
  见 [docs/05 §2.1a](./docs/05-风控与提醒规则.md)。
- **建仓体检只复述，不推导，且一个新阈值都不许有**（`src/core/risk/entry.ts`，2026-08-19）。
  它的每一条都来自一个已经存在的裁决（`hardSuppressions` / `downgrades` / `positionVerdict`
  按 **BUY** 方向再跑一遍），`verdict` 纯由 items 派生。往里加一个「涨幅超过 X% 就警告」
  等于凭空造一个未标定参数，而它会挂着「体检」这个看起来很权威的名字。
  因此 `dayPosition` **只报位置不判高低**（判高低要阈值）。
  它用的是 `Evaluation.gateInput`（`evaluate()` 原样带出来的**同一份**上下文）——
  另拼一份的症状是「体检说没事、提醒说涨停买不到」，与 `trade:preview` 那条纪律同一形状。
  拿不到评估时是 `UNKNOWN` 而不是 `CLEAR`：**「不知道」不许显示成「没问题」**。
- **成交录入的 T+1 检查是提示（`TradePreview.warning`）不是拒绝（`error`），别「顺手」改硬。**
  跨境 / 债券 / 黄金 ETF 与可转债确实是 T+0，用户还可能在补录历史或把日期填错 ——
  把一条合法成交挡在外面，症状是「我明明这么成交的，软件说存不进去」。
  判据按**这笔成交自己的 `tradedAt`** 算，不是「今天」（否则补录上周那笔卖出会每次都报）。
  措辞在 `trades/ledger.ts` 的 `t1SellNotice()`（纯函数，有用例钉着「不硬拒」这条）。
  ⚠ `traded_at` 存的是**本机**中午 12:00（`TradePanel` 的 `parseDate`），与北京日界比较
  在 UTC+7/+8 上正确、在极西时区会多锁一天 —— 修法是换那个表单的日期口径，属另一处改动。
- **`params.tTrade` 三个数是 `UNTESTABLE` 不是 `GUESS`，别放进标定网格。**
  差别是硬的：`GUESS` 是「还没跑」，这三个是**日线回测原理上测不到** ——
  一根日线只有开高低收，不知道当天先到高点还是先到低点，「日内位置」没有对应量。
  扫出来的「最优值」是在一个不存在的量上取的极值。与 `alert.bubbleScore` 同一档。
- **观察点命中改写买卖 tag 的判据只有一条：命中的来源信号就是当前显示的那条。**
  （`shared/watch-mark.ts`）否则就是拿一条针对三天前那条信号的命中，去改写今天早上
  这条毫不相干的结论。代价是有些命中不改 tag —— 那是对的，面板时间线里那次命中照样在。
  **不许读 `watch_point.verdict` 去改方向**：那是模型当时的判断，不是引擎的结论，
  写进买卖 tag 等于让 AI 直接给交易建议，与「AI 只读、不回流」冲突。
- **`signalSignature` 里一个连续量都不许进，`reasons[0]` 已经因此被移出（2026-08-14）。**
  它是一句嵌着百分比的文案（止损写「已亏损 −32.7%，触及 8% 止损线」）。
  实测一天：某只跌破止损线的票，子信号集合 / 裁决 / level / 方向**各只有 1 种**，
  而 `reasons[0]` 有 22 种，**落了 243 行同一条止损**（去重比的是「上一次」，
  −32.7 → −32.6 → −32.7 来回抖每次都算变）。两天 769 行按新签名回放**只剩 13 行**。
  它能表达的离散信息已经全在 `subs` / `verdicts` 里，**别加回来**。
  **同一个形状在 `alert_log` 的判重签名里存在了三天**（2026-08-17 才修）：那边的
  `decisionSignature` 含 `reason` 文案（「跌幅 −8.3% 未比上次（−7.8%）再扩大 2%」），
  于是一天 1590 行只对应约 6 件事。现在两处都只用离散字段
  （signal 用 `subs`/`verdicts`，alert_log 用 011 加的 `suppressed_gate`）。
  判重键也从 `code` 改成 `code:signalId` —— 同一只票同时出 `SELL` 与 `REDUCE`，
  两条交替来比「上一条」会让去重整块失效。
- **提醒被降级成 L1 就不许消耗任何额度，包括强制类那 2% 的台阶。**
  `AlertDispatcher.commit()` 里 `lastForcedLoss` 必须记在 `if (level === 'L1') return`
  **之后**。原先记在之前：2026-08-17 开盘第一轮屏幕锁定，三只跌破止损线的持仓
  （浮亏 −7.8%）被闸门④降为 L1（不弹气泡，设计如此），台阶却照记 ——
  解锁后跌到 −8.4% 也再不提醒，**整天 0 条气泡、1580 行「台阶未扩大」**。
  docs/05 §4.2 那句「既不骚扰又不漏报」只剩后半句被打穿，而这是少发的错误、用户发现不了。
- **⚠ 提醒的闸门状态 2026-08-19 起落库了（`meta.alert_gate_state`），这条老坑到此为止。**
  此前冷却/配额/台阶全在内存里，重启即清零 ⇒ 同一条止损重新发一次；实测 08-13 启动 14 次、
  08-14 二十七次，那两天「一天 10 条止损气泡」逐条对得上启动时刻。
  **推翻 `dispatcher.ts` 那条「刻意不落库」的正是这批真机数据**，但旧顾虑
  （「重启后被冷却挡住、什么都不弹」很难查）要靠三件事同时成立才算堵住，
  **缺一件都不该落库**：① `restore()` 立刻按 `now` 跑 `rollDay` + `pruneHourly`
  ⇒ 陈旧状态在结构上卡不住闸门；② **防抖计数 `streaks` 不落** ——
  重启跨过一段没有观测的时间，续上等于用不存在的连续性放行一条提醒；
  ③ 面板提醒日志页给出闸门用量 + 「清空…」逃生口。逐条在 docs/05 §4.0。
  **判读老日志时那个 grep 仍然要用**（08-13/08-14 那两天的数据没有因此变得可用），
  但从 08-19 往后重启不再污染提醒日志。加字段要递增 `DispatcherState.v` ——
  读不认识的版本一律整份丢弃，猜一个半对的状态比清零危险得多。
- **「今日信号」这类列表要传 `perCode`，否则一只刷屏的票能把别的票整个挤出窗口。**
  `signal:history` 是 `ORDER BY created_at DESC LIMIT n`，答的是「最近 n 行」而不是
  「哪些票出了信号」。上面那 664 行让 200 的窗口只覆盖了 12:38–14:00 这 82 分钟，
  上午的 377 行在面板与悬浮条上**根本不存在** —— 而界面不报错，只是少了东西。
  根因修掉后每只每天约 2 行，但 100 只自选仍会压到边缘，所以那道闸门留着。
  实现用 `ROW_NUMBER() OVER`（SQLite ≥ 3.25，两个驱动都够），
  **写错不抛异常只少给行**，改它要跑 `tests/integration/storage/signal-repo.test.ts`。
- **⚠ 「15:10 之后一个请求都不发」2026-08-19 起有一个例外：只补日线的收尾窗口。**
  判据在 `engine/tick.ts` 的 `CLOSE_CATCHUP`（15:10–16:00，只补日线、不拉快照、不跑引擎）。
  修的是「**日报当天永远不定稿**」：`stage = FINAL` 要求每只有行情的自选票都拿到当日收盘线，
  而当日日线 15:05–15:30 才发布 ⇒ 要到次日盘前才入库，而 `reportSubjectDate` 在次日 09:30
  之后就切到新一天 ⇒ **定稿版只在次日 08:05–09:30 那段窗口里存在**
  （实测 08-19 15:29 库里当天日线只有 12/80 只）。
  能挂在休市时段上的前提是三条同时成立：只补日线（`backfill` 已补齐时零请求）·
  16:00 收口 · **两道停手闸门**（补齐即记 `dailyCompleteDate`；补不齐按 8 轮上限停手）。
  **第二道闸门不是保险是必需**：那 10 只结构性拉不到的 ETF 会把窗口里每一轮都烧满。
  别把窗口拉长或把上限调高 —— 15:30 还没发布的，16:00 也不会有。
  **收盘确认补跑不受影响**（它的判据是 `through < ctx.date`，收盘后恒不成立）。
- **收盘确认轮靠「次日盘前补跑」才成立，不是靠 SETTLE 那 10 分钟。**
  数据源发布**个股**日线在 15:05–15:30，晚于 SETTLE 窗口（15:00–15:10），
  而 15:10 之后 `needsQuotes` 变 false 引擎不再跑 —— 于是实测两天下来
  `CONFIRMED` **0 行**、`indicator_daily` **0 行**、影子三张表全空
  （`reconcile` / `cacheIndicators` / `carryover` 都以 CONFIRMED 为前提）。
  `engine/settle.ts` 在次日盘前那一跳补跑，用的是
  `market.getContextThrough(code, D)`（截至 D 的收盘线、不拼临时线、不带快照）。
  三条边界：**不发提醒**、**不接影子运行**（前向纪律，见那个文件的边界 2）、
  **只补上一个交易日**（往前追等于给应用没开机的那些天凭空造出信号历史）。
  `closedAt` 必须走 `closeMsOf()` 按北京时间算，用本机时区会让补跑的行落进错误的自然日。
- **拷 `market.db` 一律用 `VACUUM INTO`，`cp` 出来的副本是坏的。**
  这条本来只写在备份那一节，实际调试时同样会踩：WAL 下主文件不自足，
  `cp` 出来的库**少了最近的写却照样能打开** —— 症状是拿它跑出来的结论莫名其妙
  （实测副本里当天的信号与 K 线整块缺失，而查询一条错都不报）。
- **⚠ 「明日预览」是只读的，别把它改成会落库的**（2026-08-21，`src/main/engine/preview.ts`）。
  它答「明天准备买 / 卖 / 减什么」：用 `settleDay` 那套市场适配器（`getContextThrough` +
  不看快照）配 **`SignalEngine.assess()`** —— 后者的契约就是**什么都不写**
  （不落 `signal` 表、不写指标缓存、不 `bumpPeak`、不跑收盘确认与复活）。
  **改成 `run()` 就会落库并搅乱 `persistedSignature` 的去重状态**，症状是次日真信号
  被误判成「没变」而不再落行。方向→动作走影子的 `toShadowAction`（唯一出处）。
  三条别读错：① **它不等于影子明天会挂的委托** —— 预览按**你自己的持仓**算，影子按影子组合的；
  ② **`status = 'UNAVAILABLE'` 是「算不出来」不是「明天没有要做的」**（当日收盘线一根都没入库，
  盘中打开就是这样）；③ **不并进日报的 `tomorrow` 一节** —— 那一节要求每项都指回一个
  已经存在的东西，而预览不落库、指不回去。
  **⚠ 收盘确认轮 2026-09-02 起会提前到当天，但只在「补齐才提前」这一版**
  （用户拍板；判据在 `tick.ts` 的 `settleDue`，`ENGINE_VERSION` 不变、判定一个字没改）。
  **被否掉的仍然是「试到底就算数」那一版**：它会让一次**不完整**的补跑写下
  `lastSettledDate` 与 `shadow_equity.trade_date` 两道幂等闸门 ⇒ **次日那次完整补跑
  被整个挡掉**，缺线票当天的确认永久缺失（2026-08-17 那个缺陷的同一形状）。
  ⇒ 现在的判据是 `meta.dailyCompleteDate === 今天`（一只不缺），**补不齐的日子什么都不写**
  ⇒ 次日盘前照旧补跑，行为逐字不变。覆盖率是**近五个交易日 3/5**
  （08-24 第 3 轮 · 08-26 第 1 轮 · 08-28 第 5 轮，`grep 已补齐` 可查；
  另外两天卡在 `backfillDaily` 的 all-or-nothing，登记项 `daily-complete-partial`）。
  ⚠ 三条别踩：① **当日那条路只从 `closeCatchup`（15:10–16:00）进** —— 15:10 之前盘中引擎
  还在跑，先补跑会把 `lastSettledDate` 写下，之后那几轮落的 `PROVISIONAL` 行就再也不会被
  `reconcile()` 推进；② `closeCatchup` 里「已补齐」那道闸门**只挡取数不挡补跑**
  （挡了的话「SETTLE 轮就补齐」的日子反而一次都跑不成）；③ **当日路影子恒喂** ——
  走到那里必然在收盘之后，下一个开盘还没到；顺带解掉老路那个隐性条件
  「用户得在次日 09:30 前开着应用，否则那个交易日的影子记录永久缺失」。
  见[迭代计划 §4.12](./docs/notes/下一阶段取舍与迭代计划.md)与 `engine/settle.ts` 头注释。
- **收盘日报只复述，不推导。** `report/build.ts` 的 `tomorrow` 每一项都必须指回一个
  **已经存在**的东西（今日的 `NEXT_DAY_WATCH` 信号 / 仍在盯的观察点 / 未了结的持仓裁决）。
  让它自己判「今天涨得多，明天值得看」会造出一个与信号层可能相反的结论，
  **而用户没有办法判断该信哪个** —— 与「观察点命中不写进 signal 表」同一条纪律。
  用例里有一条专门给一条高置信买入信号、断言 `tomorrow` 为空。
  同理 `highlights` 是**陈述**不是评价（「3 只跌破止损线」可以，「今天表现不错」不行），
  真正的评价留给 AI 那个按钮。
- **日报的两个数据源不许混，而且要标注用了哪个。** 当日收盘线（`CLOSE`）与盘中最后一个
  快照（`SNAPSHOT`）差一个集合竞价，涨跌幅的分母必须与 `close` 同源 ——
  拿收盘价配快照的昨收会算出一个哪边都不对的数。逐只带 `source`，整体给 `stage`。
- **日报报的是「当前交易日」，不是「库里最后一根日线的日期」**（2026-08-18 改，
  判据在 `report/build.ts` 的 `reportSubjectDate`）。旧口径的症状很隐蔽：当日日线
  数据源 15:05–15:30 才发布，而应用 15:10（`SESSION_BOUNDS.settleEnd`）之后不再取数
  ⇒ 今天那根要到**次日盘前**才入库 ⇒ **整个今天（含盘中）日报都停在昨天**，
  `stage` 还算出 `FINAL` 打「已定稿」；而同一屏的信号 / 提醒 / 观察点统计按
  `dayStart`（今天北京 00:00）切 —— **一份报告里混着「昨天的价 + 今天的信号」**，
  界面上完全看不出来。新判据是「开盘（09:30）之后就是今天」，今天的收盘线还没入库
  由 `stage = PROVISIONAL`（badge 写「**未定稿**」，不写「盘中」—— 16:00 也会出现它）说。
  开盘前与休市日仍退回最后一天有数据的那天。
- **日报每一节标的是「数据时刻」，不是「重算时刻」**（`stamps`，判据在 `stampsOf`）。
  各节新鲜度天生不同：行情每 30 秒一跳，而「今日提醒」可能从早上 09:03 起就没变过，
  全标成生成时刻等于每节都说「刚更新过」。三条边界：取**最新**（停牌股几天前的快照
  不该把整节拉旧，单只有多旧由那一行自己的 `quote.at` 回答）· 一条事实都没有就是
  **null**（不是 0、不是生成时刻）· `summary` 是派生节，取它复述的那几节里最新的。
  **这些时刻一个都不许进 `reportFactDigest`** —— 它每轮都变，进指纹就让
  「这段 AI 评价已过期」恒亮，等于废掉那个功能（`report-ai.test.ts` 有两条钉着）。
- **面板上的时刻一律走 `shared/time.ts` 的 `shanghaiHhmm` / `shanghaiHhmmss` /
  `shanghaiMdHhmm`，不许用 `getHours()` 或 `toLocaleTimeString()`。**
  那六处（提醒日志 / 信号列表 / 观察点 / 盘前简报 / AI 历史 / 气泡）原先各写一份、
  用的都是**宿主本地时区**：UTC+8 上恰好对，本机（UTC+7）上北京 09:03 的提醒写成 08:03，
  而页头那个「北京时间 HH:mm:ss」时钟就在同一屏上 —— 同一条提醒两个时刻，
  用户没法判断哪个对。与「`setHours(0,0,0,0)` 不能当日界」是同一个坑的展示层版本。
- **日报只做「最近一个交易日」，别顺手加历史翻页。** `position` 表是**当前**状态，
  拿它算三天前那天的浮盈亏会得到一个错的数，而错的方式用户看不出来。
  要做得先用 `trades/ledger.ts` 的 `replayTrades` 重建那一天的持仓。
- **日报的「今日环境」是独立的一节，不许并进 `stocks` / `overview`**（2026-08-15，docs/11 N1）。
  `overview` / `stocks` / `tomorrow` 只算用户自己的自选（那 15 只行业 ETF 被排除在外），
  把观察标的混进去会让用户的 7 只票被埋掉一多半，而计数再也拆不开。判据在
  `report/environment.ts`，`buildDailyReport` 只**透传**（`environment` 必填不可选 ——
  可选会让调用方漏传时静默少一整节）。两条同源纪律：`breadth` 的分母是「**有行情的只数**」
  而不是清单长度（用清单长度会把缺数显示成平盘），缺失必须**显式列进 `missing`**
  （静默少几行会让「今天行业普涨」凭空成立）。
  **环境目前不喂给 AI**：喂了就得进 `reportFactDigest`，而它含盘中快照、每轮都在变，
  指纹每次都变会让「这段已过期」提示恒亮 —— 那正是 `digest.ts` 在防的事。
- **公告解析里，六位数字不许按「SH → SZ → BJ」顺序试**（2026-08-15 踩过，docs/11 N2）。
  SH 的代码段表里有 `['000','INDEX']`，于是 `000157`（中联重科，深市主板）会被判成
  `SH000157`（上证指数段）—— **整个深市 000 段**（000001 平安银行、000002 万科…）
  的公告都会带着错代码出来，被编排层当成「未点名的票」丢掉，**用户永远看不到这些票的公告，
  而界面上完全看不出来**（实测一次请求丢 8 条）。走 `resolveAnnounceCode`：
  `ann_type` 优先（`A,SHA` / `A,SZA` / `A,CYB` 实测可靠），退化时用 `parseCode`
  —— 它本来就把指数段排除在推断之外了。**这个 bug 在手写 fixture 上永远暴露不出来**，
  是拿真实响应跑端到端才掉出来的。
- **`fetchAnnouncements` 绝不设 `emptyIsFailure`。** 绝大多数票绝大多数天就是没有公告，
  返回 0 条是合法结果；记成失败会降级三个源、跳熔断，把 tick 一起拖下水 ——
  与 `fetchMinutes` 同一形状。另外**公告的失败必须是可见的失败态**（`ok: false`），
  不许显示成「今天没有公告」：那等于替一个从来没查过的范围担保。
- **公告的 `display_time` 与 `notice_date` 是两个东西，切窗口用前者。**
  实测 `display_time = 2026-08-14 17:30` 对应 `notice_date = 2026-08-15`（发布日的次日）。
  按 `notice_date` 切「昨收盘之后」会漏掉一整晚的公告，而界面不报错、只是少了东西。
  时刻一律走 `shanghaiToEpochMs` 解析：**`Date.parse` 对那个格式不报错**（毫秒是冒号分隔的，
  V8 照样吃），只是按本机时区给出一个偏掉的时刻 —— 本机 UTC+7 时差整整一小时。
- **`announcement` 表进 `pruneAll`，`ai_explain` / `report_note` / 影子账本不进。**
  判据是「能不能重建」：公告再拉一次就有，那三样花过钱或无法重建。别把它们合并成一条策略。
- **公告不做 AI 解读**（2026-08-16 决定，docs/11 N4）。**理由不是「公告没用」**：
  ① 模型看到的和用户看到的是**同一行标题** —— 现有 AI 解读值钱是因为它拿到子信号明细、
  指标值、闸门结论这些用户拼不出的东西，公告这里没有这种不对称；
  ② 分诊已由**确定性的分类白名单**做掉（`brief/build.ts` 的 `SPOTLIGHT_CATEGORIES`，
  按数据源的**完整分类名**精确匹配 —— 实测 175 个分类，粒度足够）；
  ③ 提示词禁令是尽力而为，而模型会被推向判方向，**而方向这个维度在本项目数据上
  测不出任何东西**（M2 §5.25 三轮实测）；④ 喂给 AI 就得进 `reportFactDigest`，
  而公告每次拉取都变 ⇒「已过期」提示恒亮。
  **重新考虑的唯一条件是接了正文解析**，那时模型才第一次拿到用户没有的信息。
  ⚠ 别因为「AI 那套机器现成的」就把它加回来 —— 复用成本低不是做一件事的理由。
- **`SPOTLIGHT_CATEGORIES` 的判据是「用户漏看了会不会想知道」，不是「预示涨跌」。**
  它按**完整分类名**精确匹配，不是关键词子串 —— 子串写法实测同时有误报
  （`'担保'` 命中「提供/对外担保公告」占 2.6%、`'问询'` 命中 IPO 发行流程、
  `'停牌'` 命中可转债转股停牌）与漏报（真实分类叫「重组进展公告」而不是「重大资产重组」）。
  数据源新增分类时**默认不高亮**，那是安全的失败方向：那一条照样列出，只是不带标记。
- **`ai:explain` 的目标标识有两种取值，格式只在 `shared/ai-target.ts` 定义一处。**
  日报的 AI 评价借道那条通道（`report:<date>`），为的是复用两层缓存防重复计费、
  在途去重、取消、流式接续那一整套 —— 另起一条通道等于把踩出来的东西再抄一遍。
  代价是渲染层与主进程必须用**同一个**函数拼/认前缀：各写一份的症状是
  「点了没反应」，或者更糟 —— 走到解释单条信号那条路上，拿日期去信号表里查，
  报一句「该信号已不在库中」。
- **日报评价不复用 `ai_explain` 表。** 那张表的 `code`/`direction`/`stage`/`score`/`signal_at`
  是 NOT NULL 且刻意冗余（原信号被裁剪后仍要可读），而一份日报没有这些 ——
  塞空串和 0 进去正是这个项目一直在防的「用假值冒充」，还会污染抽屉里**按 code**
  的历史列表。它住 `report_note`（010），`trade_date` 是主键 = 一天一条 = 幂等闸门。
- **`reportFactDigest` 里不许放生成时刻或 `highlights`。** 它答的是「这段评价是对着
  哪一版事实写的」（盘中版 vs 定稿版）。把时刻或派生量算进去，指纹每次都变，
  「这段已过期」的提示就恒亮 —— 等于没有这个功能。`report-ai.test.ts` 两条钉着。
- **流式 AI 的状态机在 `renderer/panel/useAiStream.ts`，但 `AiExplain` 还没迁过去。**
  那不是遗漏：`AiExplain` 还带着建议块剥离与观察点表单，而项目**没有渲染层测试** ——
  在同一次改动里重构它，等于让一条真花钱的路径失去唯一的验证手段。迁移是单独一次改动。
  新增流式 AI 的地方一律用这个 hook，别再抄第三份。
- **拿指数或高价标的回测时必须同时调 `--capital`。** 指数点位 3000–5000、一手 100 单位
  ⇒ 一手要 30–50 万，超过默认的每标的 10 万 ⇒ `lotsAffordable` 返回 0 手，
  而它**不进任何计数器**（不是抑制、不是涨跌停），报告上只表现为「有评估、没交易」。
  2026-08-18 差点把它写成「引擎在指数上不出手」这个结论。
  判据是子信号计数：SH000300 与 SH510300 同期几乎逐位相同 ⇒ 信号层没差别 ⇒ 问题在成交层。
  **⚠ 2026-08-19：这条不只对指数生效，主池里已经在生效了**（[M2 §5.40](./docs/notes/M2-偏差报告.md)，
  **已知缺陷、未修**）。回测 fixture 的 `*Adj` 是**后复权**、锚在上市日 ⇒ 老票的后复权价可达
  真实价的一两百倍（平安银行 2018 年首根：真实 13.7 元 / 后复权 **2069 元** ⇒ 一手 20.7 万）。
  实测 `SZ000001` **整段 0 笔**、`SH600309` 96.2% 的交易日买不起、`SZ000858` 94.1%；
  主池 261 只里 **10 只**受影响（按标的·交易日加权 1.48%）。
  **偏差方向不随机** —— 后复权倍数 ≈ 累计分红送转，命中的恰恰是分红历史最长的大盘股。
  **处置 A（只让它可见）2026-08-19 已实施**：`CodeResult.unaffordable` 计数 + 报告告警行点名标的，
  绩效数字一个没变。实施后才拿到确切量级 —— 训练窗口 **31 次建仓意图被吞掉 / 1097 次建仓 = 2.8%**，
  真被吞的是 4 只（**「10 只受影响」按价位算、「4 只真被吞」按建仓意图算，别混**）。
  **⚠ 2026-08-20 候选 B（按真实价定手数）已量化并判「不做」**（[M2 §5.44](./docs/notes/M2-偏差报告.md)）：
  把 `--capital` 放大到 5× 让 `unaffordable` **31 → 0**、建仓 1097 → 1114、
  闲置资金均值 **4.23% → 1.03%**，而**总收益 −1.99% → −1.99%、Calmar −0.113 → −0.111**
  ⇒ **效应比要解决的问题（Calmar 差 0.11）小两个数量级**。回来的 4 只两赚两亏
  （平安银行 +0.49% · 万华 −14.00% · 五粮液 +8.17% · 汾酒 −13.53%，合计占组合亏损 3.6%），
  **在绩效上不构成方向** —— 08-19 那句「命中分红最长的大盘股」在**标的组成**上仍成立，
  在**绩效**上不成立，两句别混。⇒ 既有基线不用重跑、`squeezeBbwPct = 20` 的依据不用复核，
  A 那条告警继续挂着保持可见。**理由要写准**：不是「artifact 不存在」，是「它今天不影响任何结论」
  —— 真做仓位管理时要重新捡起来（[论证 §10](./docs/notes/配置形态-论证.md)）。
  ⚠ 顺带一个**没修的**：闲置资金那一层 08-19 完全没量（只量了「被吞的 31 次」）——
  高 `k` 的票每次只投 80–90%（21 只 > 10%，最糟 45.2%）⇒ **「每只 10 万等权」名不副实**，
  组合实际按 `(1 − 闲置)` 加权。它今天同样不影响结论，但**做仓位管理前必须先修**。
  **⚠ 2026-08-24 又查出候选 B 与成本模型是耦合的**（[M2 §5.54](./docs/notes/M2-偏差报告.md)）：
  B 干的事恰好是把下单额从「模拟实际」拉到「名义 10 万」，而平方根冲击律实测这一步
  把冲击中位 **4.09 → 7.33 bp**、超 10 bp 占比 **8.7% → 27.6%**
  ⇒ **要做 B 就得同时换掉常数比例成本模型**（不换等于修掉一个偏乐观的 artifact、
  引入另一个）。⇒ 顺带一条读数纪律：**别拿回测里「真的下了多少」去论证成本模型没问题** ——
  两个 `Q` 口径差 **1.8×（冲击）/ 3.2×（超线占比）**，而偏差方向固定偏乐观。
- **⚠ 「每标的 10 万」已经在常数成本模型的边缘上，不是「影响有限」**（2026-08-24，
  [M2 §5.54](./docs/notes/M2-偏差报告.md)，`npx tsx scripts/verify/impact.ts`）。
  平方根冲击律 `I = Y·σ_D·√(Q/V_D)`（Loeb 1983 → **Torre & Ferrari 1997** → Grinold & Kahn
  → **Almgren et al., Risk 18(7) 2005**）套在基线 1097 次建仓上（覆盖率 100%）：
  保守口径 `Y=1` 下**中位冲击 7.33 bp = 那条 10 bp 滑点线的 73%**、**27.6% 的建仓已超线**；
  临界资金倍数 **区间 2–7×、中心 ≈ 5×**（`Y=0.6` 外部锚定给 5.2× · `Y=1`·exp 0.6 给 4.8×）。
  ⇒ **放大单标的资金之前必须先换凸成本模型**，而 §5.44 那次实验用的正是 5×
  （**但不许拿它去重判 §5.44** —— 成本被低估让 5× 偏乐观，而它的结论是「几乎没差别所以不做」，
  方向对那个结论有利）。三条别读错：
  ① **`DEFAULT_COSTS` 一个字没动**，`ENGINE_VERSION` 不变，绩效数字一个没动；
  ② **不许把冲击数加进任何报告** —— `Y` 文献取值 0.34–1.0、指数 0.45–0.6 ⇒ 约 3× 不确定度，
  它只能当**资金量级门槛**；
  ③ **失效有两条独立路径**（`σ` 与参与率 Spearman **−0.47**，冷门票同时是低波动票
  ⇒ 两个因子相互抵消而非相乘放大）：高波动·低参与率与低波动·高参与率
  ⇒ 换模型时**按单一维度分档会漏掉一条**。
  ⚠ **别想着「加个成交量上限就行了」，那条捷径实测不存在**（同日 [M2 §5.55](./docs/notes/M2-偏差报告.md)，
  `npx tsx scripts/verify/volume-cap.ts`）：外部成熟平台（聚宽）**根本没有凸成本模型** ——
  三种滑点全部与成交量无关，大单靠 `order_volume_ratio` 硬上限兜，而它**默认 1.0 = 不设限**。
  那个形态**替代不了**：按冲击排序与按参与率排序的 top-50 **Jaccard 仅 0.176**
  （冲击名单 σ 中位 2.69% vs 参与率名单 1.25% ⇒ **凸成本抓波动、硬上限抓流动性，几乎正交**，
  冲击最大那笔在参与率排序里排 **311/1097**），门槛还差 **10–30 倍**
  （凸成本 `k*` 中位 1.9–5.2× vs 最严的 `ratio=0.10` 要 `m*` 中位 **55×**）
  ⇒ **出厂资金下三档 `ratio` 截断次数全是 0**，加了是恒等变换。
  硬上限的正确定位是**补充不是替代**（无自由参数，管「物理上下不下得去」），**≥ 10× 才有意义**。
  ⚠ 另外「Almgren 说指数 0.6」与「平方根律的 0.5」**不在同一个自变量上**：
  他们的 `3/5` 挂在**交易速率** `X/(V·T)` 上，平方根挂在**总量** `Q/V` 上（原文 Table 3 核过）。
- **`trade.costs` 不含滑点。** 滑点作用在成交价上（`buyFill`/`sellFill`），不进费用列。
  算「成本占多少」必须跑一次 `--slippage 0` 对照 —— 实测滑点 176 元/建仓、手续费 151 元，
  合计占负期望的 **69%**。只看 `costs` 字段会把结论判反（2026-08-17 踩过）。
  **我们的成本假设比聚宽默认略乐观**（2026-08-19 对照）：他们默认滑点
  `PriceRelatedSlippage(0.00246)` = 单边 **0.123%**（我们 0.1%）、佣金双边万 3（我们万 2.5，
  但我们另收过户费万 0.1）⇒ 单次往返摩擦 **≈0.352% vs ≈0.406%，我们低约 13%**。
  方向上不改变任何结论（负期望只会更负），但引用「成本占 69%」时要知道这个口径差。
- **跑验证窗口必须带段前历史。** 直接 `--from 2024-01-01` 只喂窗口内 K 线，
  300 根预热吃掉前 14 个月 ⇒ 只剩 34 次建仓、两组候选数字一模一样（看着像「没差别」）。
  用 `--from 2022-10-01`（到 2024-01 正好 302 根）。⚠ 标定工具的 `runSplit` 虽然预热正确，
  但**候选在训练段被红线淘汰时它根本不算验证段**（报告里 validation 是 `—`）。
- **判读 2026-08-19 之前的提醒日志时，先数那天重启过几次**：
  `grep -c "窗口与托盘就绪" "$APPDATA/gp-pet/logs/main-<日>.log"`。
  那时冷却/配额/台阶全在内存里，重启即清零 ⇒ 同一条止损会重新发一次。
  实测 08-13 启动 14 次、08-14 二十七次，那两天的日志不能用（M3 清单 §4.0）。
  **08-19 起闸门状态落库**（上面那条），重启不再污染日志 —— 但老数据不会因此变得可用。
- **穿越只在相邻两根间判定一次**，不做「N 日内曾金叉」的模糊匹配；去重是提醒层的职责，不是指标层的。
- **better-sqlite3 是原生模块**，需在 Electron ABI 下重建（`electron-rebuild`）。`pnpm-workspace.yaml` 里已显式跳过它的默认构建。
- **主进程/preload 的外置依赖清单在 `electron.vite.config.ts`**，从 `package.json` 的 `dependencies` 派生。别用 `rollupOptions.external` 去覆盖它 —— 漏外置 `electron` 会让 `import { app } from 'electron'` 解析到 npm 上那个「返回 exe 路径」的启动器包，**构建照样成功，启动才炸**。
- **`electron-builder.yml` 的 `files` 里不许出现正向的 `node_modules/...` 条目。**
  只要有一条，electron-builder 26 就会**原地改写源码树里的 `package.json`** ——
  `scripts` 与 `devDependencies` 整块消失，接着 pnpm 按「没有 devDependencies」
  把开发依赖整批剪掉（`node_modules/electron` 被删），下一次打包报
  `The specified electronDist does not exist`，**看起来像 electronDist 配错了**。
  这不是产物被改，是仓库被改。要排除 node_modules 里的东西一律用排除式 glob
  （`'!node_modules/x/!(keep).node'`）。恢复：`git checkout -- package.json pnpm-lock.yaml`
  → `pnpm install --frozen-lockfile` → `node node_modules/electron/install.js`
  （electron 的 `dist/` 要单独解压，`pnpm install` 不会补它）。
- **`files` 别写 `out/**`。** `out/` 下不只有运行时产物：三个 tsconfig 的 `outDir` 也在那儿
  （`out/types/*.tsbuildinfo`，约 220 KB，里面记着全部源文件与测试文件的清单）。
  写成 `out/main/** out/preload/** out/renderer/**` 三条白名单，
  日后谁再往 `out/` 下加中间产物也不会静默跟着进包。
- **preload 必须打成 CJS**：安全基线要求 `sandbox: true`，而沙箱化的 preload 不支持 ESM。electron-vite 5 默认输出 ESM，配置里已显式改回。
- **改完主进程要真启一次**（`pnpm dev`）。typecheck + build 全绿也可能启动即崩 —— 上面两条就是这么发现的。
- **造 K 线 fixture 时，横盘段的噪音必须没有周期。** 固定错拍的锯齿（旧 `chopCloses`）
  振幅够、看起来完全正常，但 +DM 与 −DM 长期抵消，320 根之后 **ADX ≈ 8**，
  而 `adxRange` 是 15–23 —— 于是 `T1_MA_CROSS:BUY` 与 `T4_ALIGNMENT` 在所有基于它的 fixture 里
  **结构上无法触发**，两条断言因此三次提交没绿过，还差点被误判成引擎缺陷。
  真实数据的判据是 **46 只 2709 次金叉里 60.6% 当日 ADX 就够**（M2 §5.19）。
  同一形状的坑此前只记了「真直线 → BBW 恒 0」那个显眼版本。
  **新增场景之后打印一次子信号集合**，别信「断言不触发」自己通过。
- **编排层的用例把组合阈值调低到 0.3 分 / 1 票**（`simulate.test.ts` / `signals.test.ts` 都这么做）。
  用出厂阈值（0.60 / 趋势 3 票）时 fixture 里没有一根凑得够票数，`persist()` 一行不写，
  去重用例会退化成 `0 === 0` 的假通过。信号稀疏本身是对的（M2 §5.13：真实占用只有 4%），
  但**验编排不该被信号质量卡住**。
- **指标黄金用例的期望值来自独立参照实现**（`scripts/verify/reference.mjs`，刻意不 import `src/core`）。
  改了 `params.ts` 或指标算法后要 `pnpm verify:indicators` 重出黄金值 —— 那条用例会先告诉你「参数不一致」。
  **不要靠改断言来让它变绿**，那会让回归基线失去意义。
- **回测的资金按前复权价计价，持仓成本另存不复权价**：止损用的是用户真实成交价，
  混用会在除权后凭空触发一次卖出提醒。
- **`src/backtest` 用相对路径 import**（不用 `@core/*`）：CLI 由 tsx 直接跑，
  而根 `tsconfig.json` 是 solution 风格、不带 `paths`，别名在那里解析不到。
- **悬浮条的命中区是「整块减掉四个圆角」**（窗口即本体，没有透明留白）。
  `barHitRects` 的圆角半径必须与 `styles.css` 的 `border-radius` 一致，否则会盖到圆角外，
  吞掉本该穿透的点击（C2 是底线）。零干扰相关的东西（`focusable`、`setIgnoreMouseEvents`、
  拖拽吸附、多屏校验）全在 `OverlayWindow` 一处。
- **移动 overlay 窗口不要用 `setPosition`，用 `OverlayWindow.moveTo`（把宽高一起写回）。**
  在缩放比不是 100% 的显示器上（Windows 的 125%/150% 是常态），Electron 会把 bounds 在
  DIP 与物理像素之间来回换算、两个方向都按「包住」取整，于是一次「只挪位置」的调用
  也可能把宽高各撑大 1px；一次拖拽发几百次 `dragBy`，条子就越拖越大。
  症状的后半段更难归因：撑大之后渲染层挂载时算的命中区（出厂 300×38）盖不住窗口，
  鼠标压在条子上却判成穿透 —— **表现是「拖完之后点什么都没反应」，看起来像 IPC 挂了**。
  命中区因此也改成跟着 `resize` 重算，两处一起才堵得住（2026-08-13）。
- **状态点只跟 `push:petState` 走，不许自己从信号里推断。** 闸门（docs/05 §4）是唯一的
  点亮路径，主进程的 `PetStateMachine` 是唯一的判定者。渲染层若自己去读 `signal:history`
  点亮 WATCHING/EXCITED/ALERT，就等于开了一条绕过冷却与免打扰的旁路。
- **`now` 已经不是本机系统钟了（2026-08-15）。** 它是「本机钟 + 校准量」，校准量由
  HTTP `Date` 响应头估计（`scheduler/clock-sync.ts`，搭盘中快照取样，零额外请求）。
  三件事别踩：① **不要拿 `Snapshot.at` 去校时** —— 那是「最后成交时刻」不是「服务器当前时刻」，
  停牌与冷门股会让它合法地落后，于是「远端比本地小」分不清是本地快了还是没人成交；
  ② **首次直接采用、之后每轮最多挪 ±2s** —— 冷却窗口、每小时配额、跨日重置全部假设 `now`
  单调，一次回拨会让刚发过的提醒重新变成「冷却已过」（首次安全是因为那时状态还是空的）；
  ③ **`data-layer.ts` 里 `localNow` 与 `now` 是两个钟，别合并** —— 量「过了多久」的
  （`latencyMs`、provider 健康统计、分时 30s 缓存）必须用本地钟，校准量一挪它们就会算出
  偏了的延迟，而延迟是判断数据源好坏的唯一依据。
  这条修掉的是一个静默失真：风控的 `age = atMs - snapshot.at` **左边本地钟、右边远端时刻**，
  本机快 6 分钟就会把连续竞价里所有买入信号判成 `STALE_SNAPSHOT` 压掉。见 docs/03 §3.1。
- **内置的「行业ETF」组（15 只）的待遇分两档，判据是「有没有持仓」不是分组**（2026-08-18 改）。
  `shared/industry-etf.ts` 每个行业一只，`data-layer` 每次启动补齐缺的（**只在库里没有时插**
  —— 无脑 upsert 会把数据源刷出来的名字覆盖回清单里那个短名，也会让用户手动加进「自选」
  的同一只 ETF 被拽走）。它们**照常评估、照常落 `signal` 表、照常进「今日信号」**，
  而分组现在只回答三件事：**不可移除**（删了下次启动会被补回来）、
  **无持仓时走 `OBSERVE` 轨**（独立日配额、抢不到气泡）、**无持仓时不进日报**。
  **有持仓即全部按个股待遇**（`main/alerts/track.ts` + `report/build.ts` 的 `reportableItems`，
  两处都有用例）—— 否则止损那条 L3 会留在只有 2 条日配额、且抢不到气泡的轨上，
  一声不响，而少发的错误用户发现不了。**「不给持仓入口」这条已经取消**。
  见 [docs/05 §3.2 / §3.2a](./docs/05-风控与提醒规则.md)。
  界面上**无持仓的**那些方向标签是中性青色 + 「行业」标记（`observeCodes`，判据必须与
  `alertTrackOf` 逐字一致）；有持仓的照常画买卖的红/黄 —— 它的结论是可执行的。
- **面板左栏两屏分的是板块，不是分组**（2026-08-18，`shared/watch-split.ts`）：
  「个股」与「ETF」，用户自己加的 ETF 与内置那 15 只在同一屏。两屏都**持仓优先**，
  但**段内必须保持用户排的顺序**、上移/下移**按段禁用** —— 跨段交换后显示顺序一点都不变，
  表现是「点了没反应」。添加框在 tab 那一行两屏共用，加完按**主进程回写的代码**切屏并滚过去
  （在渲染层照着输入串猜代码等于把规范化抄第二遍）。
- **悬浮条跑马灯只跑「有持仓的 + 今天出了未静默信号的」**（2026-08-18，`ticker.ts` 的
  `visibleTicker`）。⚠ **收窄必须作用在 `buildTicker` 的产物上**：它的主集合是
  `items ∪ quotes ∪ 今日有信号的`，而 `push:quoteTick` 推的是全部自选 ——
  2026-08-15 那条「跑马灯不含行业ETF」只滤了 `items`，**从来没有生效过**，
  而界面上完全看不出来。空态因此有三档，第三档（有自选、有报价、但既无持仓也无信号）
  是常态不是故障，不许显示成「等待行情…」。
- **「今天」一律走 `shared/time.ts` 的 `shanghaiDayStartMs`，不要写 `setHours(0,0,0,0)`。**
  提醒配额的日界、面板与悬浮条的「今天」原先用宿主本地日：UTC+8 上恰好对、UTC+7 上无害
  （日界落到北京 01:00），但 UTC−5 上本机 00:00 是**北京 13:00** ——「每日 L2+L3 ≤ 4」
  会在午盘开盘那一刻重置，而多发的配额没有任何人看得见。
  `tests/unit/main/alerts.test.ts` 有一条钉着默认日界（其余用例注入了 `utcStartOfDay`，
  测不到默认值 —— 这正是它单独存在的理由）。
- **提醒相关的一切都不读时钟。** `AlertDispatcher` / `PetStateMachine` / `resolveQuiet`
  的 `now` 全部由调用方传入，与 `src/core` 同一条纪律 —— 「15:00 那一轮会不会重发」
  「跨午夜的静默时段算不算」必须能写成用例，而不是靠改系统时间试。
- **`alert_log` 与 `signal` 是两张表两件事，别合并。** `signal` 答「引擎判了什么」，
  `alert_log` 答「有没有真的提醒我、被哪道闸门挡的」。**风控硬抑制的信号不进 alert_log**
  （它已经带着原因在 signal 表里）。另外 `alert_log.signal_id` 是外键且 `foreign_keys = ON`，
  所以拿不到 signalId 的候选**不发** —— 发了却没有审计记录，等于在提醒日志里凭空消失一条。
- **用户的「整体上调/下调一档」不作用于持仓强制类。** 把止损从 L3 调成 L2 的症状是
  「跌停那天没响」，事后极难归因 —— 少发的错误用户发现不了。
  `tests/unit/main/alert-candidates.test.ts` 有用例钉着。
- **收盘失效提示的方向必须是 `NONE`。** 同键冷却的键是 `code:direction`，沿用原方向会让
  上午那条买入提醒的 2 小时冷却把撤销提示一起吃掉 —— 而撤销恰恰是「上午那条别当真了」。
- **全屏/专注助手探测是 PowerShell 子进程，不是原生绑定**（判据见 docs/05 §4.4 的注）。
  分发路径**只读缓存**（`probe.current()`，TTL 15s），绝不 await 它；连续 3 次失败即永久停用，
  停用后状态 `UNKNOWN` → **判为可以提醒**。不要「顺手」把它改成同步等待，
  那会让每轮 tick 卡 100–300ms。
- **菜单里的「解除免打扰」只管得着手动那一项**（`manualQuiet`）。用聚合的 `quiet` 去判，
  会让静默时段或全屏应用期间的点击变成「解除一个我没开过的开关」—— 点了没反应。
  （悬浮条的双击是**开面板**，C8 的一键静默只在右键 / 托盘菜单里，见 docs/06 §4。）
- **托盘不参与提醒。** `TrayController` 只剩「按免打扰状态换图标」+「挂菜单」两件事；
  未读角标、tooltip 计数、L3 图标闪烁都已删除。**别把它们加回来** ——
  提醒的可见出口只有气泡是刻意的（docs/05 §3）。tooltip 也刻意是静态的：
  让一个常驻图标随行情变文字，本身就是一种低强度的持续打扰。
- **单实例锁不是体验优化，删了会静默损坏数据。** `market.db` 只有一个写者的假设写进了
  整个存储层（WAL + 5s busy_timeout 会让两个进程都不报错地互相覆盖），而 `AlertDispatcher`
  的冷却与配额**全在内存里**，两个实例各有一份 —— 「每小时 ≤ 6 条」在用户那里变成 12 条，
  而两边日志都显示自己守规矩。`app.requestSingleInstanceLock()` 必须在**做任何事之前**判，
  拿不到锁的那个不开库、不建窗口、不注册协议。
  `second-instance` 只开面板，**不顺手显示悬浮条** —— 条子若是被用户按 C9 隐藏的，
  替他打开等于否决他的选择。
- **`autoLaunch` 在 `pnpm dev` 里刻意不生效**（日志会打一行「未打包，跳过」）。
  `process.execPath` 在 dev 里是 `node_modules/electron/dist/electron.exe`，注册进去等于
  让用户开机启动一个裸 Electron，而且卸载应用也带不走它。**别为了「本地能验」把这条去掉**，
  要验就打包后验。同理它只在与 `app.getLoginItemSettings().openAtLogin` 不一致时才写：
  无条件写会覆盖用户在「任务管理器 → 启动」里的手动禁用。
- **日志是按天分文件的**（`main-YYYY-MM-DD.log`，保留 7 天）。`resolvePathFn` 每写一条都会被
  调用，所以跨午夜自然滚到新文件、不需要定时器（日期串按天缓存）。`maxSize` 保留为**单日**
  硬上限。清理**认不出名字的文件一律不删** —— 包括改动前留下的 `main.log` 与用户自己丢进来的
  东西；误删别人的文件比多占几 MB 贵得多（`tests/unit/main/logging.test.ts` 钉着这条）。
- **从 agent / CI 的 shell 里跑 `pnpm dev` 会「启动即崩」**：这类环境常设
  `ELECTRON_RUN_AS_NODE=1`，于是 Electron 以纯 Node 模式启动，`require('electron')`
  解析到 npm 上那个「返回 exe 路径」的启动器包，第一个用到 `protocol` / `app` 的地方就炸
  （`Cannot read properties of undefined (reading 'registerSchemesAsPrivileged')`）。
  **这不是代码问题**，用 `env -u ELECTRON_RUN_AS_NODE pnpm dev`。
  症状与「漏外置 electron」完全一样，别照着那条去查构建配置。
- **仍然不要假设托盘图标存在**：`resources/icons/app/` 读不到时走 `fallback-icon.ts` 的
  内置兜底位图并打一条 warn，**不能让托盘建不出来** —— 退出与「显示悬浮条」只有托盘这一条路（C9）。
  测试不许拿 `resources/icons/` 下的文件当 fixture。
- **「影子在不在跑」不能靠净值曲线判 —— 全空仓时它是一条 1000000 的直线，
  与「压根没推进」长得一模一样。** 判据是 `shadow_journal`（013，2026-08-19 加）：
  挂单 / 建仓 / 平仓 / 作废 / 顺延 / **整轮没推进**，一行一个动作。
  加这张表的直接原因是**委托一旦成交 `clearOrder` 就把它删了** ⇒ 只靠现有三张表
  事后拼不出「那天挂了哪两单」。它与 `shadow_trade` 同一档：**不进 `pruneAll`**、
  **不加指向 `signal(id)` 的外键**。`NOT_ADVANCED` 那一档最值钱 —— 用户下午才开机时
  `feedShadow` 为 false ⇒ 那个交易日的记录**永久缺失**，此前只有主进程日志里一行。
  面板上**不许**把「落后一天」画成告警：影子按次日盘前推进，正常就晚一天。
- **补 `shadow_equity.benchmark` 不算破前向纪律，但只有它不算。**
  前向纪律管的是信号、委托、成交（用历史 K 线补出来的叫回测）；而沪深300 某天的收盘价
  是**与我们的决策无关的事实**，当时没有它只是取数失败。真机上曾 2/2 行都是 null：
  推进与日线回补在同一跳里，回补先失败、0.24 秒后推进就读到 null，而那天不会再推进 ⇒
  永久留 null，而 `summarize()` 的 `lastBenchmark` 取**最后一行** ⇒ 整条对比为 null。
  根因是**应用内的腾讯 provider 不知道「指数无复权」**（`fetch:history` 那条路一直知道，
  两条路不共用解析器）—— `parseKline` 现在只对 `board === 'INDEX'` 回退用 `day`，
  **ETF 与个股维持严格失败**（它们真的有复权，拿不复权顶替是错的失败方式）。
- **重启后价格空白的修法在展示投影里，别去恢复 `market-data.ts` 的快照缓存。**
  那个缓存是**引擎的输入**，塞一个上周五的价进去等于让引擎拿旧价做判断；
  而 `data-layer` 的 `quoteTicks()` 只喂界面，按 `quote_tick` → 日线两级回落、
  标 `source: 'STORED'` + `stale: true` + 真实数据时刻就够了。
  症状本身很隐蔽：休市时段 `needsQuotes` 为 false ⇒ **不会有任何一轮 tick 去补**
  ⇒ 晚上/周末重启后一直空到下一个交易日 09:00，看起来像「软件把我的股票忘了」。
- **影子运行取 K 线要用 `klines.recentThrough(code, date, n)`，不是 `recent(code, n)`。**
  后者取的是「库里最后 n 根」，它隐含「最新那根就是今天」这个假设 —— 只要库里存在比目标日
  更新的行（跨日唤醒后一次补多天、测试预置数据），拿到的就是错的那两根，
  而症状是「委托莫名不成交」，从净值上完全看不出来。这条是写用例时才发现的。
- **备份不能拷 `market.db` 文件，要用 `VACUUM INTO`。** WAL 下主文件不是自足的（最近的写还在
  `-wal` 里），拷出来的库**少了最后几分钟数据却照样能打开** —— 这种备份最坏，因为它看起来
  成功了。`VACUUM INTO` 由 SQLite 保证一致性快照且能在运行中做。另外它挂在**休市维护**里
  （与裁剪同一处），别挪到盘中：它要读全库，会和取数抢同一个 SQLite 连接。
- **影子账本不进保留策略，`shadow_trade` 也不加指向 `signal(id)` 的外键。**
  `signal` 按 2 年裁剪，而影子是**无法重建**的前向绩效记录（用历史 K 线补出来的叫回测）。
  加外键等于把绩效记录挂在日志保留策略上，裁剪那天会连带删掉它。
  同理「清缓存」刻意很窄：只动指标缓存 + 过期日志 + VACUUM，**不动** K 线、自选、持仓、影子账本。
- **`shadow_equity.trade_date` 是主键，这就是推进的幂等闸门。** 盘后会跑好几轮 tick，
  少了这道闸门每轮都会多加一根净值点 —— 而净值曲线「多了一段」这件事从收益率上看不出来。
- **换灵敏度必须重建整个 `SignalEngine`，不能只换 params。** 引擎版本是构造时算的（它是缓存键
  与落库字段），做成运行时可变的会让「这一行 signal 是哪套参数下产出的」失去答案。
  data-layer 因此把 `signals` 存成 `let` 并给 pipeline 传 `{ run: (t) => signals.run(t) }` 转发 ——
  **直接把 `signals` 传进去的话，流水线会一直握着旧那个**，换完档以后信号还按旧参数出，
  几乎无从发现。
- **`quote:intraday` 是唯一一条会发网络请求的 invoke，别把它当成一次 SQLite 查询。**
  分时图 2026-08-14 起画的是数据源的逐分钟分时（`QuoteProvider.fetchMinutes`），
  `quote_tick` 降级成拉不到时的兜底。它是全应用**唯一**由用户交互直接触发的取数，
  docs/03 §2.4 那份「每日 < 1000 次」的轮询预算管不到它 —— 前提恰恰是**量由人决定**。
  于是三条边界：**不许从 tick 里调 `fetchMinutes`**（那会变成「每 30 秒一整个自选列表」）；
  闸门是 `engine/intraday.ts` 那道 30s 缓存（缓存 Promise 不缓存结果，失败立即剔除）；
  registry 的 `fetchMinutes` **刻意不传 `emptyIsFailure`** —— 停牌股与开盘前会合法地
  返回 0 个点，记成失败会降级三个源、跳熔断，把 tick 路径一起拖下水。
- **分时图的 x 轴那一天由 `tradeDate` 推，不是由「今天」推。** 休市日请求这类接口，
  数据源返回的是**上一个交易日**那条曲线（trends2 的 `ndays=1`、腾讯 minute 都如此）。
  按今天画的症状是一张日期错位、图上却毫无破绽的假曲线。`MinuteSeries.tradeDate`
  因此取自数据本身而不是本机日期，落在请求窗口外时整段作废并降级成 LOCAL。
  同理**分时的时刻一律按北京时间读写**（`shared.ts shanghaiToEpochMs`、渲染层的
  `CST_OFFSET_MS`）：用 `getHours()` 会让非 +08 的机器上 09:30 那根竖线跑到曲线中间。
- **两家的分时后两列含义完全相反，别照着一边写另一边。** 东财 trends2 是
  「这一分钟的量 + 接口直接给的均价」，腾讯 minute 是「**当日累计**量 + **当日累计额**」，
  均价得自己除（`额 / (量 × 100)`）。把腾讯第 4 列直接当均价会得到一个七位数，
  它会被算进纵轴范围 —— **症状是整条分时线被压成贴着框底的直线**，
  图上完全看不出是哪一列读错了。两边实测同一分钟均价都是 9.132，可以拿来交叉验证。
  另外腾讯那个端点**没有 `prec`**（网上示例里有），昨收实际来自 `qt[4]`。
- **远端分时与本机留痕不合并，`IntradaySeries.source` 必须跟着实际用了哪份数据走。**
  分钟收盘价与 30s 快照是两种采样口径，拼在一条线上会出现肉眼可见的锯齿，
  而用户没有任何办法看出那是两个来源。`source` 也不是「试了远端就算 REMOTE」——
  渲染层靠它决定要不要说「覆盖全天」，说错了就是在替一条半截曲线担保。
  `tests/unit/main/intraday.test.ts` 钉着这几条。
- **AI 解读是 `StockDrawer` 的一个页签，状态挂在 `App`，别把它挪回信号列表里**（2026-08-14）。
  内嵌在信号行里时，列表的重渲染能摘掉一次正在跑的模型调用：每组只渲染时间线的头，
  同一只票再来一条动态头就换人，正在流式生成的那条从列表里消失 → 组件卸载 →
  `ai:cancel` → 等了四十秒的界面自己没了，而那次调用已经计过费。
  抽屉与列表**没有父子关系**，这是结构性保证，不是小心翼翼。
  （中间做过一版叠在抽屉之上的独立第二层，能解决同一个问题，但它挡住了同一只票的
  行情与持仓 —— 而那两样恰恰是看解读时最想对照的东西。）
  `pinnedSignal` 仍然留着，但理由换成了「正读着的那行不该凭空消失」，与 AI 无关。
- **AI 解读的四条钱相关的纪律**（`ai_explain` 表 + `AiService`，2026-08-14）：
  ① **打开页签不等于发请求** —— `explain()` 按「内存缓存 → `ai_explain` 表 → 真发」找，
  别为了「打开就有反应」把查库那层去掉，那会让翻一次历史重复计费一次；
  ② **切页签/关抽屉不取消**（请求在主进程跑完并落库，切回来靠在途去重接上，
  这就是「能看未分析完那条」的全部实现），**只有「停止」按钮真的 `ai:cancel`** ——
  卸载即取消那条老纪律已经删掉了，别加回来；
  ③ **只有 `done` 落库**，停止/失败的半截不存，而 `ai_explain` **永不自动裁剪**
  （`pruneAll` 与「清缓存」都不碰它），删除只有用户手动一条路；
  ④ **思考链与正文分开累积，绝不拼进正文**（`AiDelta.kind`）。正文要落库、要抽观察点建议，
  混进草稿会让建议块解析错位。而且**「有没有输出」只认正文**：`client.ts` 的 `sawText`
  是「HTTP 200 但一个字都没有」那条错误的唯一判据，把思考也算进去的话，
  选错协议会退化成「只有思考、没有正文」而**不报错**。
- **观察点命中不许写进 `signal` 表。** 它在展示层与信号合流成一条按股票的时间线
  （`groupSignals` 的第二个入参 → `events`），但那张表答的是「引擎判了什么」——
  混进去之后「今天引擎给了几条信号」再也答不准，还会污染影子运行与统计。
  同理 `counts` / `total` **只数信号**，命中数单独一个 chip：
  「几条信号」与「命中几次」是两个问题，合成一个数就再也拆不开。
- **用户可以重画固定止损那条线（`position.stopFloor`），三条边界别踩**（2026-08-14，docs/05 §2.3）：
  ① **只作用于 `STOP_LOSS`** —— 移动止损/回撤减仓/盈利保护照旧。注意回撤减仓的
  「仍盈利或微亏」原本是把固定止损的判据抄了一遍（`profit > -stopLossPct`），
  重画线之后那两者会分叉、中间一段**两条都不响**，所以现在共用 `belowStop`；
  ② **加仓与清仓必须清空那三列**（`PositionRepo.set` 里的 `= NULL`）——
  成本变了旧线就不是同一个判断，而少发的错误用户发现不了；
  ③ **绝不进回测与影子运行** —— 它是用户对某一次具体持仓的决定，
  让它影响 `src/backtest` 或 `main/shadow` 会把「我扛住了没卖」记成策略绩效。
- **悬浮条的「鼠标离开」只有主进程判得准，渲染层判不了。** 这个 bug 修了两次：
  第一次把 CSS `:hover` 换成渲染层的命中判定 —— **还是不行**，因为命中判定跑在
  `mousemove` 上，而鼠标移出窗口之后就再也没有 `mousemove` 了，最后收到的那一次
  坐标仍然落在本体内；`document` 上的 `mouseleave` 在这个
  `focusable: false` + `setIgnoreMouseEvents` 的窗口上实测不可靠。
  现在由 `OverlayWindow.watchPointer()` 按**真实光标位置**（`screen.getCursorScreenPoint()`）
  轮询裁决，结论经 `push:overlayPointer` 回渲染层。**进入仍归渲染层**（只有它知道四个圆角）。
  两个后果一显一隐：显的是跑马灯永久卡在暂停态（条子常驻，用户解不掉），
  隐的是窗口停在可交互状态、吃掉下层那一小块的点击 —— **C2 被破**，
  而用户只会觉得「那个位置有时候点不动」。轮询只在压着条子时跑，且拖拽期间要闭嘴
  （甩得快时窗口追不上光标，会被判成离开 → 关掉可交互 → 收不到 mouseup → 拖拽卡死）。
- **分时图的 x 轴折叠午休**（2026-08-14）：11:30 与 13:00 画在同一点，
  中间那 90 分钟不占宽度（否则 430px 的图白让掉 27%）。
  代价是「午休没有交易」这件事**只剩两样东西在说**：`GAP_MS` 造成的断线，
  与那条合并刻度上的虚线。**两样都不许省** —— 少一样，午休前后两个价就会被
  一条实线连起来，看着像那 90 分钟在连续成交。脏数据（停牌复牌）落在午休区间时
  一律夹到上午收盘处，别让它插进下午段（那会画出一条时间倒流的线）。
- **面板的概览页保持挂载、只切 `display`，而且不能用 `hidden` 属性。** Tailwind 的 `grid` 类
  会盖掉 `hidden` 带来的 `display: none`（v4 下常见的坑），所以是显式切 `grid` / `hidden` 两个类。
  保持挂载是因为它订阅了 `push:quoteTick`，卸载再装回来会丢掉滚动位置与正在编辑的持仓行。
- **E2E 里面板要走 `panel:toggle` 打开，不能去 `BrowserWindow.getAllWindows()` 里找。**
  `PanelWindow` 是懒加载的（首次打开才建），直接找会等到超时。
  同理测「第二个实例」要用 `spawn` 而不是 `electron.launch`：拿不到锁的实例在建任何窗口之前就
  退出了，而 Playwright 会把那个**正确行为**报成「进程退出得太早」。
- **E2E 不要断言悬浮条高度等于 38。** 实测 150% 缩放下 `getSize()` 报 300×**40** ——
  缩放比不是 100% 时 Electron 在 DIP 与物理像素之间两个方向都按「包住」取整。
  这不影响 C2（命中区在渲染层按 `window.innerHeight` 实测重算），写死 38 只会让用例
  在任何非 100% 缩放的机器上红。
- **AI 的 API key 不得放进 `AppSettings`。** `config:export` 把整份设置写进用户选的 JSON
  文件，而那个文件的用途恰恰是「发给另一台机器」—— key 进设置就等于跟着导出走。
  它单独住 `<用户数据目录>/ai.json`，用 `safeStorage` 加密；**加密不可用时拒绝保存**，
  不退化成明文落盘（那是「看起来成功了」的那一类失败）。
  `tests/unit/main/config-transfer.test.ts` 有一条用例钉着导出文件里搜不到 key。
- **AI 客户端不得复用 `net/http.ts`。** 那一层只有 `get()`、默认 3s 超时、GBK 解码，
  而且它的限流器**与行情取数共用**（全局并发 4）。一次 40 秒的 LLM 调用挂上去，
  盘中每 30 秒一轮的 tick 就会饿死 —— 症状是「行情突然不动了」，
  而没人会想到是刚才点了 AI 解读。`src/main/ai/client.ts` 另起一套：POST + SSE、
  自己的超时、并发 1。**全市场扫描要做也得走这条路**（见 [P2 评估 §1.2](./docs/notes/P2-全市场扫描评估.md)）。
- **AI 解读仍是只读的解释层：它不改参数、不动引擎、不进影子运行。**
  唯一的例外形状是**观察点**（P2 续）—— 而它不是「AI 回流」：
  模型只给一个**建议值**，人在表单里确认（可改）之后落成 `watch_point` 一行，
  判定是每轮的一次**纯比较**（不涉及模型），命中后走**正常的四道闸门**。
  状态点仍只认 `PetStateMachine`，仍只接受过了闸门的提醒。
- **观察点不是策略参数，别把它往 `params.ts` 或参数表里靠。**
  `params.ts` 是引擎的、全局的、长期的，依据必须是本地回测标定（ADR-0003）；
  观察点是用户的、单标的、一次性、会过期，依据是「用户确认」。
  往 `params-view.ts` 那张标定状态表里掺进用户随手确认的阈值，那张表就废了 ——
  它的全部价值就在于「哪个数有依据」。边界写在 `003_watch.sql` 头注释里。
- **观察点的 `PRICE` 必须用不复权价。** 用前复权价会让除权那天凭空命中或凭空不命中，
  而症状是「明明没跌到就提醒我了」，用户只会认为软件算错了 ——
  与「持仓成本用不复权」是同一个坑。
- **观察点判定里 `null` 指标一律跳过，绝不当 0**（约束 4）。未预热的 `rsi` 是 null；
  当 0 会让所有 `rsi <= 30` 的观察点在预热期**全部命中**。
  **组合条件（`a 且 b`，2026-08-25）把这条推得更严：任何一条取不到值，整点就不命中** ——
  把「取不到」当成立会让组合条件比单条件**更容易**误报，而用户加第二个条件的本意
  恰恰是想更严。判据只有「同一轮同时成立」，**不做闩锁**（先 a 后 b 是另一个功能）；
  **「或」写成两个观察点**，各自命中各自提醒。
- **`watch_point` 的 `metric` / `op` / `threshold` / `hit_value` 四列自 015 起只是
  `conditions[0]` 的镜像，不是判据。** 直接读它们会把一个「a 且 b」的观察点读成只盯 a
  —— 也就是**读成一个更宽松的条件**，症状是该等的时候提前提醒。所有读取一律走
  `WatchPointRepo`（它负责把 015 之前的旧行也统一成 `conditions` 数组）。
  条件文本只有 `shared/watch-metrics.ts` 的 `conditionsText` 一处出处，
  提醒文案 / 观察点列表 / 信号时间线 / 日报「仍在盯」/ 移除确认框五处共用 ——
  日报那一处此前直接打印裸键名（`bollMid` 而不是「布林中轨」），就是各写一份的结果。
- **`watch_point` 的两个外键带来两处删除顺序**（错了就是运行时
  `FOREIGN KEY constraint failed`）：① `WatchlistRepo.remove()` 先清观察点再删自选；
  ② `pruneAll()` 先清**已结束**的观察点再删信号，而**还被观察点引着的信号不能删** ——
  ACTIVE 的观察点刻意不裁剪，于是它的来源信号也要留下。
  这一条是写用例时才发现的，`watch-repo.test.ts` 三条钉着。
- **命中提醒的方向必须是 `NONE`、级别是 L2 且照过闸门。** 方向沿用原方向会被那条买入提醒的
  2h 冷却吃掉（与收盘失效提示同一条理由）；而它**不是强制类** ——
  少发一条用户自己设的观察点，用户在观察点页里看得见，与漏一条止损不是同一量级。
- **发给模型的上下文必须带参数标定状态。** 缺了它，模型会**默认**引擎结论经过验证，
  然后用非常有说服力的语气把一套未标定的转述阈值讲成定论（ADR-0003 要防的正是这件事），
  而从输出上完全看不出是上下文漏了一块。`tests/unit/main/ai-context.test.ts` 钉着这一块。
  同理**免责与来源标注不靠提示词**：模型可能不照做，那两行是渲染层固定的 DOM。
- **AI 接口协议是按「路径形状」选的，不是按厂商选的。** 两种都支持：
  OpenAI 兼容（`{base}/chat/completions`）与 Anthropic 兼容（`{base}/v1/messages`），
  差异只有四个点（路径 / 鉴权头 / system 放顶层还是放 messages / 增量字段名），全在
  `src/main/ai/protocols.ts`。**同一家服务可能两条路都提供且形状不同** ——
  火山方舟 `…/api/coding` 是 Anthropic 协议，`…/api/coding/v3` 才是 OpenAI 兼容
  （实测两条路都存在，返回 401 而非 404），而 coding 与 plan 两种订阅的 key 还互不通用。
  填地址时 `detectProtocol()` 会自动识别，识别结果显示在设置页选择器上、可手动改回。
- **选错协议的症状有一个是「HTTP 200 但一个字都没有」。** 用 OpenAI 的取字逻辑去读
  Anthropic 的流，每一帧都取不出增量 —— 不报错、不断流，就是没字。所以
  `createAiClient` 在 `sawAny === false` 时**必须抛错并点名协议**，不许静默返回空串。
  `tests/unit/main/ai-client.test.ts` 有一条用例专门跑这个交叉组合。
- **Anthropic 协议的 `system` 是顶层字段，不是 `messages` 里的一条。**
  它的 role 只有 `user` / `assistant`，塞一条 `role: 'system'` 进去会被拒。
  两种协议的报文形状各有一条用例钉着。
- **Anthropic 协议只发 `x-api-key`，不要同时发 `Authorization: Bearer`。**
  实测火山方舟两种都认，但真 Anthropic 同时收到两种凭据会拒绝请求 ——
  `x-api-key` 是唯一一个两边都能用的选择。

## 措辞纪律

面向用户的文案中：
- 置信度**不得**称为「胜率」或「概率」
- **不得**出现「必涨」「抄底」「稳赚」「牛股」等词
- **不得**展示任何未经本地回测验证的绩效数字
- 每条提醒底部固定小字：**仅供参考，非投资建议**

node_modules/
out/
dist/
release/
*.log
logs/

# 本地运行产物
.env
.env.local
*.db
*.db-journal
*.db.bak-*
reports/
# 抓下来的历史行情（scripts/fetch-history.mjs 的产物，可重出）
data/history/
# 申万行业变迁史（scripts/fetch-industry.mjs 的产物，可重出）。
# 不进版本控制的另一个理由：申万随时会更新这份表，提交一份快照会让
# 「我手上这份是哪天的」变成一个要翻 git log 才知道的问题 —— 文件里的 fetchedAt 才是出处。
data/industry/

# 公告源探针的累计样本（scripts/probe-announcements.mjs，docs/11 N0）。
# 它是**本机这条网络**上的观测，换台机器不通用，所以不进版本控制；
# 结论归档在 docs/notes/公告源-可达性实测.md
data/probe/
# 历史公告（scripts/fetch-announcements.mjs 的产物，可重出）
data/announcements/
# 流动性维度（scripts/fetch-liquidity.mjs 的产物，逐日文件都可重出）。
# ⚠ 真正难重出的那一小块是**逐月流通股本**（东财大请求约 50–60 个就限流），
# 它压在 params/float-shares.json 里、**进版本控制**，逐日展开是离线的：
#   node scripts/fetch-liquidity.mjs --from-shares params/float-shares.json --prices data/history --out data/liquidity-em
# 恢复步骤见 docs/notes/交接-流动性维度.md
data/liquidity/
data/liquidity-em/
# 口径交叉验证用的三只票（scripts/verify/jq-crosscheck.ts，可重出）。
# ⚠ 刻意与 data/history 分开：往主 fixture 目录里补 2015–2017 的 K 线会改变
# 那 261 只的预热段，历史回测基线就再也复现不出来了。重出命令写在那个脚本的头注释里
data/verify-jq/
# 财务业绩报表（scripts/fetch-financials.mjs 的产物，M2 §5.62）。
# 可重出：一只票一个请求，261 只约 4 分钟、实测零失败、无需 token。
# ⚠ **退市股一只都抓不到**（源里就没有，不是限流）—— 那不是待重试，是台账里的硬限制。
data/financials/
.vite/
.eslintcache

# 工具
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# 文档转换的中间产物
strategy*.txt

# Claude Code 本地设置
.claude/settings.local.json

# 无关素材
*_raw.jpeg

# 覆盖率报告是构建产物
coverage/

# Playwright E2E 的产物（trace / 截图 / HTML 报告）
test-results/
playwright-report/
.playwright/
